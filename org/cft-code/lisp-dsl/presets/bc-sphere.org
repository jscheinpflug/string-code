:PROPERTIES:
:ID:       20260609T004300.000000
:END:
#+title: Lisp bc Sphere Preset
#+auto_tangle: t
#+PROPERTY: header-args:lisp :tangle ../../../../lisp/presets/bc-sphere.lisp :mkdirp yes

The sphere \(bc\) preset declares a single \(b(z)c(w)\) rational-pole
contraction and the \(c\) top-form zero mode.

#+begin_src lisp
(in-package #:string-code.cft.presets)
#+end_src

#+begin_src lisp
(define-cft-preset bc-sphere
  (:theory-id 4
   :kind-namespace bc
   :basis (:presentation bc
           :backend (:bc)
           :tick-denominator 1
           :quantum-numbers ((ghost-number))
           :seed-bits ((0 :operator c :weight -1)
                       (1 :operator c :weight 0))))
  (surface sphere :sphere :rational)
  (quantum-number ghost-number (:u1))
  (chiral-field b :fermionic
         :weight (rational 2 1)
         :infinity primary
         :quantum-numbers ((ghost-number -1)))
  (chiral-field c :fermionic
         :zero-mode t
         :weight (rational -1 1)
         :infinity primary
         :quantum-numbers ((ghost-number 1)))
  (wick (b z) (c w)
    (/ 1 (- z w)))
  (zero-mode (grassmann-top-form :field c)))
#+end_src

* Full Copy

The full-copy sphere fixture is descriptor data.  Boundary-specific disk code
still lives in Zig until the boundary DSL is designed, but bulk
antiholomorphic-copy behavior no longer needs a handwritten bulk preset.

#+begin_src lisp
(define-cft-preset bc-sphere-full
  (:theory-id 9
   :kind-namespace bc
   :basis (:presentation bc
           :backend (:bc)
           :tick-denominator 1
           :quantum-numbers ((ghost-number))
           :seed-bits ((0 :operator c :weight -1)
                       (1 :operator c :weight 0))))
  (surface sphere :sphere :rational)
  (quantum-number ghost-number (:u1))
  (chiral-field b :fermionic
         :weight (rational 2 1)
         :infinity primary
         :quantum-numbers ((ghost-number -1)))
  (chiral-field c :fermionic
         :zero-mode t
         :weight (rational -1 1)
         :infinity primary
         :quantum-numbers ((ghost-number 1)))
  (anti-chiral-field bt :fermionic
         :weight (rational 2 1)
         :infinity primary
         :quantum-numbers ((ghost-number -1)))
  (anti-chiral-field ct :fermionic
         :zero-mode t
         :weight (rational -1 1)
         :infinity primary
         :quantum-numbers ((ghost-number 1)))
  (wick (b z) (c w)
    (/ 1 (- z w)))
  (wick (bt zbar) (ct wbar)
    (/ 1 (- zbar wbar)))
  (zero-mode (grassmann-top-form :field c))
  (zero-mode (grassmann-top-form :field ct)))
#+end_src
