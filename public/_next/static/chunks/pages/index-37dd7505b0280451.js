(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[405],{48312:function(e,n,t){(window.__NEXT_P=window.__NEXT_P||[]).push(["/",function(){return t(32426)}])},32426:function(e,n,t){"use strict";t.r(n),t.d(n,{Graph:function(){return t_},GraphPage:function(){return tb},default:function(){return tg}});var r,o=t(85893),i=JSON.parse('{"a":{"nodes":[{"tags":[null],"properties":{"CATEGORY":"overview","ID":"20260602T200000.000000","BLOCKED":"","FILE":"overview.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Overview","file":"overview.org","id":"20260602T200000.000000"},{"tags":[null],"properties":{"CATEGORY":"cft-code","ID":"20260603T063945.054265","BLOCKED":"","FILE":"cft-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"CFT-Code","file":"cft-code.org","id":"20260603T063945.054265"},{"tags":[null],"properties":{"CATEGORY":"sft-code","ID":"20260603T064210.664096","BLOCKED":"","FILE":"sft-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"SFT-Code","file":"sft-code.org","id":"20260603T064210.664096"},{"tags":[null],"properties":{"CATEGORY":"nlsm-code","ID":"20260603T064230.276471","BLOCKED":"","FILE":"nlsm-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"NLSM-Code","file":"nlsm-code.org","id":"20260603T064230.276471"},{"tags":[null],"properties":{"CATEGORY":"on-shell-code","ID":"20260603T070744.656277","BLOCKED":"","FILE":"on-shell-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"On-Shell-Code","file":"on-shell-code.org","id":"20260603T070744.656277"},{"tags":[null],"properties":{"CATEGORY":"pure-spinor-code","ID":"20260603T085219.077418","BLOCKED":"","FILE":"pure-spinor-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Pure-Spinor-Code","file":"pure-spinor-code.org","id":"20260603T085219.077418"},{"tags":[null],"properties":{"CATEGORY":"normal-ordering","ID":"20260603T094037.040938","BLOCKED":"","FILE":"normal-ordering.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Normal-Ordering","file":"normal-ordering.org","id":"20260603T094037.040938"},{"tags":[null],"properties":{"CATEGORY":"ope","ID":"20260603T094112.928043","BLOCKED":"","FILE":"ope.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"OPE","file":"ope.org","id":"20260603T094112.928043"},{"tags":[null],"properties":{"CATEGORY":"operators","ID":"20260603T094125.997360","BLOCKED":"","FILE":"operators.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Operators","file":"operators.org","id":"20260603T094125.997360"},{"tags":[null],"properties":{"CATEGORY":"correlators","ID":"20260603T094138.285453","BLOCKED":"","FILE":"correlators.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Correlators","file":"correlators.org","id":"20260603T094138.285453"},{"tags":[null],"properties":{"CATEGORY":"coefficient","ID":"20260604T083119.372970","BLOCKED":"","FILE":"coefficient.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Coefficient","file":"coefficient.org","id":"20260604T083119.372970"},{"tags":[null],"properties":{"CATEGORY":"tensor-code","ID":"20260605T075909.433647","BLOCKED":"","FILE":"tensor-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor-Code","file":"tensor-code.org","id":"20260605T075909.433647"},{"tags":[null],"properties":{"CATEGORY":"expressions","ID":"20260605T082209.767536","BLOCKED":"","FILE":"expressions.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Expressions","file":"expressions.org","id":"20260605T082209.767536"},{"tags":[null],"properties":{"CATEGORY":"theory","ID":"20260605T090102.250727","BLOCKED":"","FILE":"theory.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Theory","file":"theory.org","id":"20260605T090102.250727"},{"tags":[null],"properties":{"CATEGORY":"basis-generation","ID":"20260606T063253.759714","BLOCKED":"","FILE":"basis-generation.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Basis-Generation","file":"basis-generation.org","id":"20260606T063253.759714"},{"tags":[null],"properties":{"CATEGORY":"abstract","ID":"20260606T064054.119217","BLOCKED":"","FILE":"abstract.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Abstract","file":"abstract.org","id":"20260606T064054.119217"},{"tags":[null],"properties":{"CATEGORY":"wick","ID":"20260606T064107.303587","BLOCKED":"","FILE":"wick.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Wick","file":"wick.org","id":"20260606T064107.303587"},{"tags":[null],"properties":{"CATEGORY":"bosonization","ID":"20260606T064118.058500","BLOCKED":"","FILE":"bosonization.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Bosonization","file":"bosonization.org","id":"20260606T064118.058500"},{"tags":[null],"properties":{"CATEGORY":"symmetry","ID":"20260606T085016.945165","BLOCKED":"","FILE":"symmetry.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Symmetry","file":"symmetry.org","id":"20260606T085016.945165"},{"tags":[null],"properties":{"CATEGORY":"clebsch-gordan","ID":"20260606T085254.829431","BLOCKED":"","FILE":"clebsch-gordan.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Clebsch-Gordan","file":"clebsch-gordan.org","id":"20260606T085254.829431"},{"tags":[null],"properties":{"CATEGORY":"kernel","ID":"20260606T150001.000000","BLOCKED":"","FILE":"kernel.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Kernel","file":"kernel.org","id":"20260606T150001.000000"},{"tags":[null],"properties":{"CATEGORY":"cft-kernel","ID":"20260606T150002.000000","BLOCKED":"","FILE":"cft-kernel.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"CFT Kernel","file":"cft-kernel.org","id":"20260606T150002.000000"},{"tags":[null],"properties":{"CATEGORY":"string-code","ID":"20260606T150003.000000","BLOCKED":"","FILE":"string-code.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"String-Code","file":"string-code.org","id":"20260606T150003.000000"},{"tags":[null],"properties":{"CATEGORY":"presets","ID":"20260606T163500.000000","BLOCKED":"","FILE":"presets.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Presets","file":"presets.org","id":"20260606T163500.000000"},{"tags":[null],"properties":{"CATEGORY":"shared","ID":"20260606T183100.000000","BLOCKED":"","FILE":"shared.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Preset Shared Runtime","file":"shared.org","id":"20260606T183100.000000"},{"tags":[null],"properties":{"CATEGORY":"free-boson","ID":"20260606T183200.000000","BLOCKED":"","FILE":"free-boson.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Free Boson Preset","file":"free-boson.org","id":"20260606T183200.000000"},{"tags":[null],"properties":{"CATEGORY":"bc","ID":"20260606T183300.000000","BLOCKED":"","FILE":"bc.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"bc Preset","file":"bc.org","id":"20260606T183300.000000"},{"tags":[null],"properties":{"CATEGORY":"composition","ID":"20260606T183400.000000","BLOCKED":"","FILE":"composition.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Preset Composition","file":"composition.org","id":"20260606T183400.000000"},{"tags":[null],"properties":{"CATEGORY":"representation-store","ID":"20260606T190101.000000","BLOCKED":"","FILE":"representation-store.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Representation Store","file":"representation-store.org","id":"20260606T190101.000000"},{"tags":[null],"properties":{"CATEGORY":"realization","ID":"20260606T190103.000000","BLOCKED":"","FILE":"realization.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Realization","file":"realization.org","id":"20260606T190103.000000"},{"tags":[null],"properties":{"CATEGORY":"coupling","ID":"20260606T190105.000000","BLOCKED":"","FILE":"coupling.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Coupling","file":"coupling.org","id":"20260606T190105.000000"},{"tags":[null],"properties":{"CATEGORY":"rendering","ID":"20260606T190106.000000","BLOCKED":"","FILE":"rendering.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Rendering","file":"rendering.org","id":"20260606T190106.000000"},{"tags":[null],"properties":{"CATEGORY":"backend","ID":"20260606T210001.000000","BLOCKED":"","FILE":"backend.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Backend","file":"backend.org","id":"20260606T210001.000000"},{"tags":[null],"properties":{"CATEGORY":"decomposition","ID":"20260606T210002.000000","BLOCKED":"","FILE":"decomposition.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Decomposition","file":"decomposition.org","id":"20260606T210002.000000"},{"tags":[null],"properties":{"CATEGORY":"projector","ID":"20260606T210003.000000","BLOCKED":"","FILE":"projector.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Projector","file":"projector.org","id":"20260606T210003.000000"},{"tags":[null],"properties":{"CATEGORY":"context","ID":"20260606T211001.000000","BLOCKED":"","FILE":"context.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Context","file":"context.org","id":"20260606T211001.000000"},{"tags":[null],"properties":{"CATEGORY":"root-data","ID":"20260606T230001.000000","BLOCKED":"","FILE":"root-data.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Tensor Root Data","file":"root-data.org","id":"20260606T230001.000000"},{"tags":[null],"properties":{"CATEGORY":"correlator-performance","ID":"20260607T130000.000000","BLOCKED":"","FILE":"correlator-performance.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Correlator Performance Benchmarks","file":"correlator-performance.org","id":"20260607T130000.000000"},{"tags":[null],"properties":{"CATEGORY":"free-fermion","ID":"20260608T090000.000000","BLOCKED":"","FILE":"free-fermion.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Free Fermion Preset","file":"free-fermion.org","id":"20260608T090000.000000"},{"tags":[null],"properties":{"CATEGORY":"eta-xi","ID":"20260608T100000.000000","BLOCKED":"","FILE":"eta-xi.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"eta-xi Preset","file":"eta-xi.org","id":"20260608T100000.000000"},{"tags":[null],"properties":{"CATEGORY":"expression-performance","ID":"20260608T154500.000000","BLOCKED":"","FILE":"expression-performance.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Generated Expression Benchmark","file":"expression-performance.org","id":"20260608T154500.000000"},{"tags":[null],"properties":{"CATEGORY":"descriptor","ID":"20260608T231500.000000","BLOCKED":"","FILE":"descriptor.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Generated Descriptor Boundary","file":"descriptor.org","id":"20260608T231500.000000"},{"tags":[null],"properties":{"CATEGORY":"descriptor-emitter","ID":"20260608T234500.000000","BLOCKED":"","FILE":"descriptor-emitter.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Descriptor Emitter","file":"descriptor-emitter.org","id":"20260608T234500.000000"},{"tags":[null],"properties":{"CATEGORY":"repl-fixtures","ID":"20260608T235500.000000","BLOCKED":"","FILE":"repl-fixtures.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Generated Fixture Declarations","file":"repl-fixtures.org","id":"20260608T235500.000000"},{"tags":[null],"properties":{"CATEGORY":"generated-abi","ID":"20260609T001000.000000","BLOCKED":"","FILE":"generated-abi.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Generated Fixture ABI","file":"generated-abi.org","id":"20260609T001000.000000"},{"tags":[null],"properties":{"CATEGORY":"system","ID":"20260609T003900.000000","BLOCKED":"","FILE":"system.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp CFT System","file":"system.org","id":"20260609T003900.000000"},{"tags":[null],"properties":{"CATEGORY":"preset-compiler","ID":"20260609T004000.000000","BLOCKED":"","FILE":"preset-compiler.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Preset Compiler","file":"preset-compiler.org","id":"20260609T004000.000000"},{"tags":[null],"properties":{"CATEGORY":"free-fermion-10","ID":"20260609T004100.000000","BLOCKED":"","FILE":"free-fermion-10.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Free Fermion Preset","file":"free-fermion-10.org","id":"20260609T004100.000000"},{"tags":[null],"properties":{"CATEGORY":"eta-xi","ID":"20260609T004200.000000","BLOCKED":"","FILE":"eta-xi.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Eta-Xi Presets","file":"eta-xi.org","id":"20260609T004200.000000"},{"tags":[null],"properties":{"CATEGORY":"bc-sphere","ID":"20260609T004300.000000","BLOCKED":"","FILE":"bc-sphere.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp bc Sphere Preset","file":"bc-sphere.org","id":"20260609T004300.000000"},{"tags":[null],"properties":{"CATEGORY":"free-boson-10","ID":"20260609T004400.000000","BLOCKED":"","FILE":"free-boson-10.org","PRIORITY":"B"},"olp":null,"pos":1,"level":0,"title":"Lisp Free Boson Preset","file":"free-boson-10.org","id":"20260609T004400.000000"}],"links":[{"type":"id","target":"20260603T094112.928043","source":"20260603T085219.077418"},{"type":"id","target":"20260603T094138.285453","source":"20260603T085219.077418"},{"type":"id","target":"20260603T063945.054265","source":"20260602T200000.000000"},{"type":"id","target":"20260605T075909.433647","source":"20260602T200000.000000"},{"type":"id","target":"20260603T070744.656277","source":"20260602T200000.000000"},{"type":"id","target":"20260603T064210.664096","source":"20260602T200000.000000"},{"type":"id","target":"20260603T064230.276471","source":"20260602T200000.000000"},{"type":"id","target":"20260603T085219.077418","source":"20260602T200000.000000"},{"type":"id","target":"20260603T063945.054265","source":"20260602T200000.000000"},{"type":"id","target":"20260605T075909.433647","source":"20260602T200000.000000"},{"type":"id","target":"20260603T070744.656277","source":"20260602T200000.000000"},{"type":"id","target":"20260603T064210.664096","source":"20260602T200000.000000"},{"type":"id","target":"20260603T064230.276471","source":"20260602T200000.000000"},{"type":"id","target":"20260603T085219.077418","source":"20260602T200000.000000"},{"type":"id","target":"20260603T063945.054265","source":"20260602T200000.000000"},{"type":"id","target":"20260605T090102.250727","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094138.285453","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094112.928043","source":"20260606T150002.000000"},{"type":"id","target":"20260606T063253.759714","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094138.285453","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094112.928043","source":"20260606T150002.000000"},{"type":"id","target":"20260605T082209.767536","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094112.928043","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094138.285453","source":"20260606T150002.000000"},{"type":"id","target":"20260603T094125.997360","source":"20260606T150002.000000"},{"type":"id","target":"20260606T063253.759714","source":"20260606T150002.000000"},{"type":"id","target":"20260605T090102.250727","source":"20260606T150002.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T183200.000000"},{"type":"id","target":"20260606T163500.000000","source":"20260606T183200.000000"},{"type":"id","target":"20260605T075909.433647","source":"20260606T183200.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T183200.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260608T100000.000000"},{"type":"id","target":"20260605T090102.250727","source":"20260606T163500.000000"},{"type":"id","target":"20260606T150002.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183200.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183300.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260608T090000.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260608T100000.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183400.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260609T004000.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260603T063945.054265","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T163500.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260608T090000.000000"},{"type":"id","target":"20260606T183200.000000","source":"20260606T183400.000000"},{"type":"id","target":"20260606T183300.000000","source":"20260606T183400.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T183400.000000"},{"type":"id","target":"20260606T183200.000000","source":"20260606T183100.000000"},{"type":"id","target":"20260606T183300.000000","source":"20260606T183100.000000"},{"type":"id","target":"20260606T183400.000000","source":"20260606T183100.000000"},{"type":"id","target":"20260606T150002.000000","source":"20260606T183100.000000"},{"type":"id","target":"20260606T183100.000000","source":"20260606T183300.000000"},{"type":"id","target":"20260605T082209.767536","source":"20260603T094138.285453"},{"type":"id","target":"20260604T083119.372970","source":"20260603T094138.285453"},{"type":"id","target":"20260605T090102.250727","source":"20260603T094138.285453"},{"type":"id","target":"20260606T064054.119217","source":"20260603T094138.285453"},{"type":"id","target":"20260605T075909.433647","source":"20260603T094138.285453"},{"type":"id","target":"20260606T064107.303587","source":"20260603T094138.285453"},{"type":"id","target":"20260606T064118.058500","source":"20260603T094138.285453"},{"type":"id","target":"20260605T075909.433647","source":"20260603T094138.285453"},{"type":"id","target":"20260606T064107.303587","source":"20260603T094138.285453"},{"type":"id","target":"20260606T064107.303587","source":"20260608T231500.000000"},{"type":"id","target":"20260605T082209.767536","source":"20260603T094112.928043"},{"type":"id","target":"20260605T082209.767536","source":"20260603T094112.928043"},{"type":"id","target":"20260605T090102.250727","source":"20260603T094112.928043"},{"type":"id","target":"20260603T094125.997360","source":"20260603T094112.928043"},{"type":"id","target":"20260603T094125.997360","source":"20260603T094112.928043"},{"type":"id","target":"20260606T063253.759714","source":"20260603T094112.928043"},{"type":"id","target":"20260603T094138.285453","source":"20260603T094112.928043"},{"type":"id","target":"20260603T094138.285453","source":"20260603T094112.928043"},{"type":"id","target":"20260606T063253.759714","source":"20260603T094112.928043"},{"type":"id","target":"20260603T094112.928043","source":"20260603T094112.928043"},{"type":"id","target":"20260604T083119.372970","source":"20260605T082209.767536"},{"type":"id","target":"20260603T094125.997360","source":"20260605T082209.767536"},{"type":"id","target":"20260605T075909.433647","source":"20260604T083119.372970"},{"type":"id","target":"20260605T090102.250727","source":"20260603T094125.997360"},{"type":"id","target":"20260605T075909.433647","source":"20260603T094125.997360"},{"type":"id","target":"20260604T083119.372970","source":"20260603T094125.997360"},{"type":"id","target":"20260609T004000.000000","source":"20260609T001000.000000"},{"type":"id","target":"20260608T234500.000000","source":"20260608T235500.000000"},{"type":"id","target":"20260608T231500.000000","source":"20260608T234500.000000"},{"type":"id","target":"20260605T082209.767536","source":"20260603T063945.054265"},{"type":"id","target":"20260603T094125.997360","source":"20260603T063945.054265"},{"type":"id","target":"20260604T083119.372970","source":"20260603T063945.054265"},{"type":"id","target":"20260603T094112.928043","source":"20260603T063945.054265"},{"type":"id","target":"20260603T094138.285453","source":"20260603T063945.054265"},{"type":"id","target":"20260606T063253.759714","source":"20260603T063945.054265"},{"type":"id","target":"20260606T150002.000000","source":"20260603T063945.054265"},{"type":"id","target":"20260605T075909.433647","source":"20260603T063945.054265"},{"type":"id","target":"20260604T083119.372970","source":"20260603T063945.054265"},{"type":"id","target":"20260603T094125.997360","source":"20260603T063945.054265"},{"type":"id","target":"20260605T075909.433647","source":"20260603T063945.054265"},{"type":"id","target":"20260606T163500.000000","source":"20260603T063945.054265"},{"type":"id","target":"20260605T090102.250727","source":"20260603T063945.054265"},{"type":"id","target":"20260606T150002.000000","source":"20260603T063945.054265"},{"type":"id","target":"20260603T094138.285453","source":"20260605T090102.250727"},{"type":"id","target":"20260603T094112.928043","source":"20260605T090102.250727"},{"type":"id","target":"20260606T150002.000000","source":"20260605T090102.250727"},{"type":"id","target":"20260603T094125.997360","source":"20260605T090102.250727"},{"type":"id","target":"20260603T094125.997360","source":"20260605T090102.250727"},{"type":"id","target":"20260606T064118.058500","source":"20260605T090102.250727"},{"type":"id","target":"20260606T063253.759714","source":"20260605T090102.250727"},{"type":"id","target":"20260603T094125.997360","source":"20260603T064210.664096"},{"type":"id","target":"20260603T094112.928043","source":"20260603T064210.664096"},{"type":"id","target":"20260603T094138.285453","source":"20260603T064210.664096"},{"type":"id","target":"20260605T075909.433647","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210002.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260605T075909.433647","source":"20260606T085254.829431"},{"type":"id","target":"20260606T230001.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T190101.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T190103.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210002.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210003.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210001.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T190106.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260605T075909.433647","source":"20260606T085254.829431"},{"type":"id","target":"20260606T230001.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210002.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210003.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T210001.000000","source":"20260606T085254.829431"},{"type":"id","target":"20260606T085016.945165","source":"20260605T075909.433647"},{"type":"id","target":"20260606T150001.000000","source":"20260605T075909.433647"},{"type":"id","target":"20260606T085016.945165","source":"20260605T075909.433647"},{"type":"id","target":"20260606T150001.000000","source":"20260605T075909.433647"},{"type":"id","target":"20260606T210001.000000","source":"20260605T075909.433647"},{"type":"id","target":"20260603T063945.054265","source":"20260603T070744.656277"},{"type":"id","target":"20260603T094125.997360","source":"20260603T070744.656277"},{"type":"id","target":"20260603T094138.285453","source":"20260603T070744.656277"},{"type":"id","target":"20260605T090102.250727","source":"20260603T070744.656277"},{"type":"cite","source":"20260602T200000.000000","target":"atick1987covariant"},{"type":"cite","source":"20260606T183200.000000","target":"yinStringbook"},{"type":"cite","source":"20260606T183200.000000","target":"yinStringbook"},{"type":"cite","source":"20260606T183300.000000","target":"yinStringbook"},{"type":"cite","source":"20260606T183300.000000","target":"yinStringbook"}],"tags":null}}'),a=t(82730),s=t(56714),l=t(57747),c=t(93717),d=t(32568),u=t(89993),p=t(48940),m=t(61829),f=t(61715),h=t(62218),g=t(9008),b=t.n(g),_=t(67294),y=t(32802),x=t.n(y),k=t(49281),v=t(95711),w=t(31122),C=t(7520),z=t(36194);let T=[],S={};for(let e in z.oY)for(let n in z.oY[e]){let t=e+n;"LinearNone"===t&&(t="Linear"),T.push(t),S[t]=z.oY[e][n]}let R={enabled:!0,charge:-700,collision:!0,collisionStrength:20,centering:!0,centeringStrength:.2,linkStrength:.3,linkIts:1,alphaDecay:.05,alphaTarget:0,alphaMin:0,velocityDecay:.25,gravity:.3,gravityOn:!0,gravityLocal:!1},I={orphans:!1,dailies:!1,parent:"heading",filelessCites:!1,tagsBlacklist:[],tagsWhitelist:[],dirsBlocklist:[],dirsAllowlist:[],bad:!0,nodes:[],links:[],date:[],noter:!0},E={method:"degree"},B={particles:!1,particlesNumber:0,particlesWidth:4,arrows:!1,arrowsLength:1,arrowsPos:.5,arrowsColor:"",linkOpacity:.8,linkWidth:1,nodeRel:3,nodeOpacity:1,nodeResolution:12,labels:2,labelScale:1.5,labelFontSize:10,labelLength:40,labelWordWrap:25,labelLineSpace:1,labelDynamicDegree:8,labelDynamicStrength:.5,highlight:!0,highlightNodeSize:1.1,highlightLinkSize:.7,highlightFade:.8,highlightAnim:!0,animationSpeed:360,algorithmOptions:T,algorithmName:"CircularOut",linkColorScheme:"gray.500",nodeColorScheme:["red.500","gray.600","yellow.500","green.500","cyan.500","blue.500","pink.500","purple.500","orange.500"],nodeHighlight:"purple.500",linkHighlight:"purple.500",backgroundColor:"white",emacsNodeColor:"gray.800",labelTextColor:"black",labelBackgroundColor:"",labelBackgroundOpacity:.7,citeDashes:!0,citeDashLength:35,citeGapLength:15,citeLinkColor:"gray.700",citeLinkHighlightColor:"",citeNodeColor:"black",refDashes:!0,refDashLength:35,refGapLength:15,refLinkColor:"gray.700",refLinkHighlightColor:"",refNodeColor:"black",nodeSizeLinks:.5,nodeZoomSize:1.2},F={follow:"zoom",localSame:"add",zoomPadding:200,zoomSpeed:2e3},D={highlight:"hover",local:"double",follow:"never",context:"right",preview:"click",backgroundExitsLocal:!1},P={neighbors:1},j=["red.500","orange.500","yellow.500","green.500","cyan.500","blue.500","pink.500","purple.500","white","gray.100","gray.200","gray.300","gray.400","gray.500","gray.600","gray.700","gray.800","gray.900","black"];var O=t(66712),L=t(4040),A=t(22757),W=t(62658),M=t(74970),q=t(59277),H=t(71293),Z=t(9580),N=t(38554),X=t.n(N),G=t(81982),K=t(88983),V=t(51526),U=t(77672);let Y=[.25,.1,.25,1],Q={enter:(e,n)=>({...e,delay:(0,K.hj)(n)?n:null==n?void 0:n.enter}),exit:(e,n)=>({...e,delay:(0,K.hj)(n)?n:null==n?void 0:n.exit})},$=e=>null!=e&&parseInt(e.toString(),10)>0,J={exit:{size:{duration:.2,ease:Y},opacity:{duration:.3,ease:Y}},enter:{size:{duration:.3,ease:Y},opacity:{duration:.4,ease:Y}}},ee={exit:e=>{var n;let{animateOpacity:t,startingSize:r,transition:o,transitionEnd:i,delay:a,dimension:s}=e;return{...t&&{opacity:$(r)?1:0},overflow:"hidden",[s]:r,transitionEnd:null==i?void 0:i.exit,transition:null!==(n=null==o?void 0:o.exit)&&void 0!==n?n:Q.exit(J.exit,a)}},enter:e=>{var n;let{animateOpacity:t,endingSize:r,transition:o,transitionEnd:i,delay:a,dimension:s}=e;return{...t&&{opacity:1},[s]:r,transitionEnd:null==i?void 0:i.enter,transition:null!==(n=null==o?void 0:o.enter)&&void 0!==n?n:Q.enter(J.enter,a)}}},en=_.forwardRef((e,n)=>{let{in:t,unmountOnExit:r,animateOpacity:i=!0,startingSize:a=0,endingSize:s="auto",dimension:l="height",style:c,className:d,transition:u,transitionEnd:p,...m}=e,[f,h]=_.useState(!1);_.useEffect(()=>{let e=setTimeout(()=>{h(!0)});return()=>clearTimeout(e)},[]),(0,Z.ZK)({condition:!!(Number(a)>0&&r),message:"startingSize and unmountOnExit are mutually exclusive. You can't use them together"});let g=parseFloat(a.toString())>0,b={startingSize:a,endingSize:s,animateOpacity:i,dimension:l,transition:f?u:{enter:{duration:0}},transitionEnd:X()(p,{enter:{overflow:"initial"},exit:r?void 0:{display:g?"block":"none"}})},y=!r||t,x=t||r?"enter":"exit";return(0,o.jsx)(V.M,{initial:!1,custom:b,children:y&&(0,o.jsx)(U.E.div,{ref:n,...m,className:(0,G.cx)("chakra-collapse",d),style:{overflow:"hidden",display:"block",...c},custom:b,variants:ee,initial:!!r&&"exit",animate:x,exit:"exit"})})});K.Ts&&(en.displayName="Collapse");let et=e=>{let{setTagColors:n,setFilter:t,filter:r,tagColors:i,target:s}=e,d=r.tagsBlacklist,u=r.tagsWhitelist,p=d.indexOf(s)>-1,m=u.indexOf(s)>-1,f=(0,a.q)();return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(M.s,{icon:(0,o.jsx)(l.xu,{bgColor:i[s]||"gray.600",borderRadius:"sm",height:3,width:3,borderColor:i[s]||"gray.600",borderWidth:1}),closeOnSelect:!1,onClick:f.onToggle,children:(0,o.jsx)(H.x,{children:"Change color"})}),(0,o.jsx)(en,{in:f.isOpen,children:(0,o.jsxs)(c.k,{ml:2,mt:2,flexWrap:"wrap",children:[(0,o.jsx)(l.xu,{children:(0,o.jsx)(l.xu,{tabIndex:0,cursor:"pointer",onClick:()=>n(e=>Object.fromEntries(Object.keys(e).filter(e=>e!==s).map(n=>[n,e[n]]))),bgColor:"",m:1,borderRadius:"sm",borderColor:"gray.600",borderWidth:1,height:3,width:3})},"empty"),j.map(e=>(0,o.jsx)(l.xu,{children:(0,o.jsx)(l.xu,{tabIndex:0,cursor:"pointer",onClick:()=>n({...i,[s]:e}),bgColor:e,m:1,borderRadius:"sm",height:3,width:3})},e))]})}),!m&&(0,o.jsx)(M.s,{onClick:()=>{if(!p){t(e=>({...e,tagsBlacklist:[...e.tagsBlacklist,s]}));return}t(e=>({...e,tagsBlacklist:e.tagsBlacklist.filter(e=>e!==s)}))},icon:p?(0,o.jsx)(q.MinusIcon,{}):(0,o.jsx)(q.ViewOffIcon,{}),children:p?"Remove from blocklist":"Add to blocklist"}),!p&&(0,o.jsx)(M.s,{onClick:()=>{if(!m){t(e=>({...e,tagsWhitelist:[...e.tagsWhitelist,s]}));return}t(e=>({...e,tagsWhitelist:e.tagsWhitelist.filter(e=>e!==s)}))},icon:m?(0,o.jsx)(q.MinusIcon,{}):(0,o.jsx)(q.ViewIcon,{}),children:m?"Remove from allowlist":"Add to allowlist"})]})},er=e=>{let{background:n,target:t,nodeType:r,coordinates:i,handleLocal:s,menuClose:l,scope:c,webSocket:d,setPreviewNode:u,setTagColors:p,tagColors:m,setFilter:f,filter:h}=e,{isOpen:g,onOpen:b,onClose:y}=(0,a.q)();return(0,_.useRef)(),(0,o.jsx)(o.Fragment,{children:(0,o.jsx)(O.v,{defaultIsOpen:!0,closeOnBlur:!1,onClose:()=>l(),children:(0,o.jsx)(L.q,{zIndex:"overlay",bgColor:"white",color:"black",position:"absolute",left:i.left,top:i.top,right:i.right,bottom:i.bottom,fontSize:"xs",boxShadow:"xl",children:"string"!=typeof t?(0,o.jsxs)(o.Fragment,{children:[t&&(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(A.X,{size:"xs",isTruncated:!0,px:3,py:1,children:t.title}),(0,o.jsx)(W.R,{borderColor:"gray.500"})]}),0!==c.nodeIds.length&&(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(M.s,{onClick:()=>s(t,"add"),icon:(0,o.jsx)(q.PlusSquareIcon,{}),children:"Expand local graph at node"}),(0,o.jsx)(M.s,{onClick:()=>s(t,"replace"),icon:(0,o.jsx)(k.DvO,{}),children:"Open local graph for this node"}),(0,o.jsx)(M.s,{onClick:()=>s(t,"remove"),icon:(0,o.jsx)(q.MinusIcon,{}),children:"Exclude node from local graph"})]}),0===c.nodeIds.length&&(0,o.jsx)(M.s,{icon:(0,o.jsx)(k.DvO,{}),onClick:()=>s(t,"replace"),children:"Open local graph"}),(0,o.jsx)(M.s,{icon:(0,o.jsx)(q.ViewIcon,{}),onClick:()=>{u(t),t&&history.replaceState(null,"",window.location.pathname+"#".concat(t.id))},children:"Preview"})]}):(0,o.jsx)(et,{target:t,tagColors:m,filter:h,setTagColors:p,setFilter:f})})})})};var eo=t(78590),ei=t(35238),ea=t(72567);let es=e=>{let{setJustification:n,setIndent:t,setFont:r,justification:i,setPreviewNode:a,canUndo:s,canRedo:l,resetPreviewNode:p,previousPreviewNode:m,nextPreviewNode:f,outline:h,setOutline:g,collapse:b,setCollapse:_}=e;return(0,o.jsxs)(c.k,{flex:"0 1 40px",pb:3,alignItems:"center",justifyContent:"space-between",pr:1,children:[(0,o.jsx)(c.k,{children:(0,o.jsxs)(eo.h,{isAttached:!0,children:[(0,o.jsx)(d.u,{label:"Go backward",children:(0,o.jsx)(u.h,{_focus:{},variant:"subtle",icon:(0,o.jsx)(q.ChevronLeftIcon,{}),"aria-label":"Previous node",disabled:!s,onClick:()=>m()})}),(0,o.jsx)(d.u,{label:"Go forward",children:(0,o.jsx)(u.h,{_focus:{},variant:"subtle",icon:(0,o.jsx)(q.ChevronRightIcon,{}),"aria-label":"Next node",disabled:!l,onClick:()=>f()})})]})}),(0,o.jsxs)(c.k,{children:[(0,o.jsx)(d.u,{label:"Justify content",children:(0,o.jsx)(u.h,{variant:"subtle","aria-label":"Justify content",icon:[(0,o.jsx)(k.v9V,{},"justify"),(0,o.jsx)(k.YSr,{},"left"),(0,o.jsx)(k.RXA,{},"right"),(0,o.jsx)(k.tr_,{},"center")][i],onClick:()=>n(e=>(e+1)%4)})}),(0,o.jsx)(d.u,{label:"Toggle outline view",children:(0,o.jsx)(u.h,{variant:"subtle","aria-label":"Justify content",icon:h?(0,o.jsx)(ea.XQM,{}):(0,o.jsx)(ea.JfF,{}),onClick:()=>g(e=>!e)})}),(0,o.jsx)(d.u,{label:"Toggle headers",children:(0,o.jsx)(u.h,{variant:"subtle","aria-label":"Toggle headers",icon:b?(0,o.jsx)(ei.omV,{}):(0,o.jsx)(ei.egf,{}),onClick:()=>_(e=>!e)})})]})]})};var el=t(8371);let ec=e=>{var n,t,r;let{filter:i,setFilter:a,tagColors:s,setTagColors:l,openContextMenu:d,previewNode:u}=e;return(null==u?void 0:u.tags)&&(null==u?void 0:null===(n=u.tags)||void 0===n?void 0:n[0])!==null?(0,o.jsx)(c.k,{mb:2,flexWrap:"wrap",children:null==u?void 0:null===(r=u.tags)||void 0===r?void 0:null===(t=r.map)||void 0===t?void 0:t.call(r,e=>{var n,t,r;let l=null!==(t=i.tagsBlacklist)&&void 0!==t?t:[],c=null!==(r=i.tagsWhitelist)&&void 0!==r?r:[],u=l.includes(e),p=c.includes(e);return(0,o.jsxs)(el.Vp,{tabIndex:0,mr:2,mt:2,onContextMenu:n=>{n.preventDefault(),d(e,n)},cursor:"pointer",onClick:()=>{if(u){a(n=>({...n,tagsBlacklist:n.tagsBlacklist.filter(n=>n!==e),tagsWhitelist:[...n.tagsWhitelist,e]}));return}if(p){a(n=>({...n,tagsWhitelist:n.tagsWhitelist.filter(n=>n!==e)}));return}a(n=>({...n,tagsBlacklist:[...n.tagsBlacklist,e]}))},size:"sm",variant:"outline",colorScheme:(null===(n=s[e])||void 0===n?void 0:n.replaceAll(/(.*?)\..*/g,"$1"))||void 0,children:[(0,o.jsx)(el.Sn,{children:e}),u?(0,o.jsx)(el.bq,{as:q.ViewOffIcon}):p?(0,o.jsx)(el.bq,{as:q.ViewIcon}):null]},e)})}):null};var ed=t(11684),eu=t(97258),ep=t(83424),em=t(77890),ef=t.n(em),eh=t(94986),eg=t.n(eh),eb=t(88541),e_=t.n(eb),ey=t(5622),ex=t.n(ey),ek=t(9994);t(13659);var ev=t(9861),ew=t(12861),eC=t.n(ew),ez=t(10043),eT=t.n(ez),eS=t(48443),eR=t(93545),eI=t.n(eR),eE=t(15941),eB=t.n(eE),eF=t(67597),eD=t.n(eF),eP=t(44549),ej=t.n(eP),eO=t(78818),eL=t.n(eO),eA=t(32883),eW=t(17191),eM=t(50151),eq=t(1702),eH=t(64325),eZ=t(72303),eN=t(82509),eX=t(86353);let eG={".headingFlex":{flexDirection:"row-reverse",justifyContent:"flex-end"},".outlineHeadingButton":{display:"none"},h1:{color:"black",lineHeight:"1.2",fontSize:"16",fontWeight:"bold",paddingTop:2},h2:{fontSize:"14",color:"black",fontWeight:"bold",fontStyle:"bold italic",paddingTop:2},h3:{fontSize:"13",color:"black",paddingTop:2},h4:{fontSize:"12",fontStyle:"italic",color:"black",paddingTop:2},".sectionContent":{paddingTop:2}},eK={".headingFlex":{flexDirection:"row",justifyContent:"flex-start"},".viewerHeadingButton":{display:"none"},".sectionContent":{mt:3,paddingLeft:4,ml:"11px",borderLeftWidth:"1px",borderLeftColor:"gray.500"},".preHeadingContent":{},"h1,h2,h3,h4,h5,h6,h7,h8":{pl:1,lineHeight:"1.25",color:"black",fontSize:15,fontWeight:700},".sec":{pt:1},".wrapClass > div > p":{mb:2},p:{fontWeight:500,fontSize:14,pb:2}},eV={".katex":{overflowX:"scroll"},ol:{paddingLeft:4,py:1},"li::marker":{fontSize:12,fontWeight:"bold"},li:{pt:1},ul:{paddingLeft:"5"},p:{fontSize:"14",fontWeight:"500 !important",pb:2},div:{hyphens:"auto !important"},".title":{textAlign:"center",marginBottom:".2em"},".subtitle":{textAlign:"center",fontSize:"medium",fontWeight:"bold",marginTop:0},".TODO":{color:"red.500"},".equationContainer":{display:"table",textAlign:"center",width:"100%"},".equation":{verticalAlign:"middle"},".equation-label":{display:"tableCell",textAlign:"right",verticalAlign:"middle"},".inlinetask":{padding:"10px",border:"2px solid gray",margin:"10px",background:"#ffffcc"},"#org-div-home-and-up":{textAlign:"right",fontSize:"70 % ",whiteSpace:"nowrap"},textarea:{overflowX:"auto"},".linenr":{fontSize:"smaller"},".org-info-js_info-navigation":{borderStyle:"none"},"#org-info-js_console-label":{fontSize:"10px",fontWeight:"bold",whiteSpace:"nowrap"},".org-info-js_search-highlight":{backgroundColor:"#ffff00",color:"#000000",fontWeight:"bold"},".org-svg":{width:"90%"},".DONE":{color:"green"},".priority":{fontFamily:"monospace",color:"orange"},".tag":{backgroundColor:"white",fontFamily:"monospace",padding:"2px",fontSize:"80%",fontWeight:"normal"},".timestamp":{color:"#bebebe"},".timestamp-kwd":{color:"#5f9ea0"},".org-right":{marginLeft:"auto",marginRight:"0px",textAlign:"right"},".org-left":{marginLeft:"0px",marginRight:"auto",textAlign:"left"},".org-center":{marginLeft:"auto",marginRight:"auto",textAlign:"center"},".underline":{textDecoration:"underline"},"#postamble p":{fontSize:"90%",margin:".2em"},"#preamble p":{fontSize:"90%",margin:".2em"},"p.verse":{marginLeft:"3%"},pre:{borderRadius:"3px",backgroundColor:"white",padding:"8pt",fontFamily:"monospace",overflow:"auto",margin:"1.2em"},"pre.src":{position:"relative",overflow:"auto"},"pre.src:before":{display:"none",position:"absolute",top:"-8px",right:"12px",padding:"3px",backgroundColor:"white"},"caption.t-above":{captionSide:"top"},"caption.t-bottom":{captionSide:"bottom"},"th.org-right":{textAlign:"center"},"th.org-left":{textAlign:"center"},"th.org-center":{textAlign:"center"},"td.org-right":{textAlign:"right"},"td.org-left":{textAlign:"left"},"td.org-center":{textAlign:"center"},".footpara":{display:"inline"},".footdef":{marginBottom:"1em"},".figure":{padding:"1em"},".figure p":{textAlign:"center"},".math.math-display .katex":{overflow:"auto",minHeight:"1.5em"}};var eU=t(86658);let eY=(e,n)=>e.split(".").reduce((e,n)=>e[n],n.colors),eQ=e=>{let{noUnderline:n,id:t,setSidebarHighlightedNode:r,setPreviewNode:i,nodeById:a,openContextMenu:s,href:l,children:c,isWiki:d}=e,{highlightColor:u}=(0,_.useContext)(eX.N),m=eY(u,(0,p.F)());l.replaceAll(/(.*?)\:?.*/g,"$1");let f=l.replaceAll(/.*?\:(.*)/g,"$1"),h=null!=t?t:f;return(0,o.jsx)(H.x,{as:"a",onMouseEnter:()=>r(a[h]),onMouseLeave:()=>r({}),tabIndex:0,display:"inline",overflow:"hidden",fontWeight:500,color:u,textDecoration:n?void 0:"underline",onContextMenu:e=>{e.preventDefault(),s(a[f],e)},onClick:()=>{let e=a[f];i(e),e&&history.replaceState(null,"",window.location.pathname+"#".concat(e.id))},_hover:{textDecoration:"none",cursor:"pointer",bgColor:m+"22"},_focus:{outlineColor:u},children:d?"[[".concat(c,"]]"):c})},e$=e=>{let{href:n,children:t}=e,{highlightColor:r}=(0,_.useContext)(eX.N);return(0,o.jsxs)(eA.r,{color:r,isExternal:!0,href:n,children:[t,(0,o.jsx)(q.ExternalLinkIcon,{mx:"1px",pb:"2px"})]})},eJ=e=>{var n,t,r,i,a;let{href:s,children:c,nodeById:d,setSidebarHighlightedNode:u,previewNode:p,setPreviewNode:m,nodeByCite:f,openContextMenu:h,outline:g,noUnderline:b,linksByNodeId:y,isWiki:x,macros:k,attachDir:v,useInheritance:w}=e,[C,z]=(0,_.useState)(null),[T,S]=(0,_.useState)(!1),R=s.replaceAll(/(.*?)\:.*/g,"$1"),I=()=>{fetch("notes/".concat(E)).then(e=>e.text()).then(e=>{if("error"!==e){z(e);return}}).catch(e=>(console.log(e),"Could not fetch the text for some reason, sorry!\n\n This can happen because you have an id with forward slashes (/) in it."))};if((0,_.useEffect)(()=>{R.replaceAll(/(http)?.*/g,"$1")||C||!T||I()},[T,C]),!R)return(0,o.jsx)(H.x,{color:"gray.700",children:c});if(R.replaceAll(/(http)?.*/g,"$1"))return(0,o.jsx)(e$,{href:s,children:c});let E=((e,n)=>{if("id"===e)return n;if(e.includes("cite")){var t;let e=null!==(t=f[n])&&void 0!==t&&t;return!e||(null==e?void 0:e.properties.FILELESS)?"":null==e?void 0:e.id}return""})(R,s.replaceAll(/.*?\:(.*)/g,"$1"));return(encodeURIComponent(encodeURIComponent(null===(n=d[E])||void 0===n?void 0:n.file)),E)?(0,o.jsx)(o.Fragment,{children:(0,o.jsxs)(eW.J,{gutter:12,trigger:"hover",placement:"top-start",children:[(0,o.jsx)(eM.x,{children:(0,o.jsx)(l.xu,{display:"inline",onMouseEnter:()=>S(!0),onMouseLeave:()=>S(!1),children:(0,o.jsx)(eQ,{id:E,setSidebarHighlightedNode:u,setPreviewNode:m,nodeById:d,href:s,children:c,nodeByCite:f,openContextMenu:h,noUnderline:b,isWiki:x},null!==(i=null===(t=d[E])||void 0===t?void 0:t.title)&&void 0!==i?i:E)})}),(0,o.jsx)(eq.h,{children:(0,o.jsxs)(eH.y,{transform:"scale(1)",boxShadow:"xl",position:"relative",zIndex:"tooltip",onMouseEnter:()=>{var e;u(null!==(e=d[E])&&void 0!==e?e:{})},onMouseLeave:()=>{u({})},children:[(0,o.jsx)(eZ.Q,{}),(0,o.jsx)(eN.b,{pb:5,fontSize:"xs",position:"relative",zIndex:"tooltip",transform:"scale(1)",width:"100%",children:(0,o.jsx)(eU.$B,{autoHeight:!0,autoHeightMax:300,autoHide:!0,renderThumbVertical:e=>{let{style:n,...t}=e;return(0,o.jsx)(l.xu,{style:{...n,borderRadius:0},...t})},children:(0,o.jsx)(l.xu,{w:"100%",color:"black",px:3,sx:{...eV,...g?eK:eG},children:(0,o.jsx)(nr,{previewText:C,nodeById:d,setSidebarHighlightedNode:u,setPreviewNode:m,nodeByCite:f,openContextMenu:h,outline:g,linksByNodeId:y,macros:k,attachDir:v,useInheritance:w,previewNode:d[E],collapse:!1})})})})]},null!==(a=null===(r=d[E])||void 0===r?void 0:r.title)&&void 0!==a?a:E)})]})}):(0,o.jsx)(H.x,{as:"span",display:"inline",className:s,color:"base.700",cursor:"not-allowed",children:c})};var e0=t(25675),e1=t.n(e0),e2=t(62520),e6=t.n(e2),e3=t(22338);let e8=e=>void 0===e?void 0:"number"!=typeof e&&isNaN(Number(e))?e.endsWith("px")?Number(e.replace("px","")):void 0:Number(e),e4=e=>{var n,t,r;let{org:i,file:a}=e,{src:s,...l}=i;if(!s)return null;let c=e=>{let{src:n,width:t,quality:r}=e;return"".concat(n)};if(null==s?void 0:s.startsWith("file:")){let e=s.replace(/file:/g,"./"),t=e8(l.width),r=e8(l.height),i=t||r?void 0:"responsive";return(0,o.jsx)(e1(),{...l,layout:i,loader:c,src:e,width:t,height:r,alt:null!==(n=l.alt)&&void 0!==n?n:"Wow, an image."})}if(/(http)?.*/g.test(s.replace(/(http)?.*/g,"$1"))){let e=e8(l.width),n=e8(l.height),r=e||n?void 0:"responsive";return(0,o.jsx)(e1(),{...l,layout:r,loader:c,src:s,width:e,height:n,alt:null!==(t=l.alt)&&void 0!==t?t:"Wow, an image."})}let d=s.replace(/file:/g,""),u=e6().dirname(a),p=e6().isAbsolute(d)||"~"===d.slice(0,1)?d:e6().join(u,d);return encodeURIComponent(encodeURIComponent(p)),(0,o.jsx)(e3.W,{my:4,position:"relative",children:(0,o.jsx)("img",{...l,alt:null!==(r=l.alt)&&void 0!==r?r:"Wow, an image.",src:p})})};var e5=t(17166);let e7=(0,_.createContext)({outline:!1,collapse:!0}),e9=null!==(r=t(34155).env.NEXT_PUBLIC_TITLE)&&void 0!==r?r:"ORUI",ne=e=>{let{children:n,className:t}=e,[r,i]=(0,_.useState)(!0),{collapse:a}=(0,_.useContext)(e7);return((0,_.useEffect)(()=>{i(!a)},[a]),n)?"h0Wrapper headingWrapper"===t?(0,o.jsxs)(l.xu,{className:"preHeadingContent",children:[" ",n]}):"string"!=typeof n&&Array.isArray(n)?(0,o.jsxs)(l.xu,{className:"sec",children:[(0,o.jsx)(l.xu,{display:"block",children:(0,o.jsxs)(c.k,{className:"headingFlex",alignItems:"baseline",children:[r&&n.length>0?(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(u.h,{className:"viewerHeadingButton",_focus:{},_active:{},"aria-label":"Expand heading",size:"xs",variant:"subtle",icon:(0,o.jsx)(q.ChevronUpIcon,{}),onClick:()=>i(!r),height:2,width:2}),(0,o.jsx)(u.h,{className:"outlineHeadingButton",_focus:{},_active:{},"aria-label":"Expand heading",size:"xs",variant:"subtle",icon:(0,o.jsx)(e5.zOz,{}),onClick:()=>i(!r),height:2,width:2})]}):(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(u.h,{className:"viewerHeadingButton",_active:{},_focus:{},"aria-label":"Collapse heading",height:2,width:2,size:"xs",variant:"subtle",icon:(0,o.jsx)(q.ChevronDownIcon,{}),onClick:()=>i(!r)}),(0,o.jsx)(u.h,{className:"outlineHeadingButton",_active:{},_focus:{},"aria-label":"Collapse heading",height:2,width:2,size:"xs",variant:"subtle",icon:(0,o.jsx)(e5.O6Y,{}),onClick:()=>i(!r)})]}),n[0]]})}),r&&(0,o.jsx)(l.xu,{className:"sectionContent",children:null==n?void 0:n.slice(1)})]}):(0,o.jsx)(l.xu,{className:"sectionContent",children:n}):null};var nn=t(65548);function nt(e){return["object"==typeof e.source?e.source.id:e.source,"object"==typeof e.target?e.target.id:e.target]}let nr=e=>{var n,t;let{nodeById:r,setSidebarHighlightedNode:i,setPreviewNode:a,previewText:s,nodeByCite:c,previewNode:d,openContextMenu:u,outline:p,collapse:m,linksByNodeId:f,macros:h,attachDir:g,useInheritance:b}=e,y=(0,ed.l)().use(eu.Z).use(eg()).use(e_(),{idDir:g||void 0,useInheritance:b}).use(ef()).use(ep.Z,{useSections:!0}),x=(null===(n=f[null==d?void 0:d.id])||void 0===n?void 0:n.reduce((e,n)=>{let t=nt(n).filter(e=>e!==d.id).join("");return{...e,[t]:r[t]}},{}))||{},k=Object.entries(x);(0,ed.l)().use(eC()).use(eB(),["yaml"]).use(eD()).use(eS.Z,{permaLinks:Object.keys(x),pageResolver:e=>{var n;let t=k.find(n=>{var t;return(null==n?void 0:null===(t=n[1])||void 0===t?void 0:t.title)===e});return[null!==(n=null==t?void 0:t[0])&&void 0!==n?n:""]},hrefTemplate:e=>"id:".concat(e)}).use(ej()).use(eI()).use(eT()).use(eL());let v=(null==d?void 0:null===(t=d.file)||void 0===t?void 0:t.slice(-3))===".md",w=(0,_.useMemo)(()=>y.use(ek.Z,{trust:e=>["\\htmlId","\\href"].includes(e.command),macros:{"\\eqref":"\\href{###1}{(\\text{#1})}","\\ref":"\\href{###1}{\\text{#1}}","\\label":"\\htmlId{#1}{}",...h}}).use(ev.Z,{Fragment:o.Fragment,jsx:o.jsx,jsxs:o.jsxs,createElement:_.createElement,components:{a:e=>{let{children:n,href:t}=e;return(0,o.jsx)(eJ,{nodeByCite:c,setSidebarHighlightedNode:i,href:"".concat(t),nodeById:r,linksByNodeId:f,setPreviewNode:a,openContextMenu:u,outline:p,previewNode:d,isWiki:v,macros:h,attachDir:g,useInheritance:b,children:n})},img:e=>(0,o.jsx)(e4,{org:e,file:null==d?void 0:d.file}),section:e=>{let{children:n,className:t}=e;return t&&(null==t?void 0:t.slice(-1))==="".concat(d.level)?(0,o.jsx)(l.xu,{children:n}):(0,o.jsx)(ne,{outline:p,collapse:m,className:null!=t?t:"",children:n})},blockquote:e=>{let{children:n}=e;return(0,o.jsx)(nn.m.blockquote,{color:"gray.800",bgColor:"gray.300",pt:4,pb:2,mb:4,mt:3,pl:4,borderLeftWidth:4,borderLeftColor:"gray.700",children:n})},p:e=>{let{children:n}=e;return(0,o.jsx)("p",{lang:"en",children:n})},code:e=>{let{children:n}=e;return(0,o.jsx)(nn.m.code,{px:1,py:.5,rounded:"sm",fontSize:"xs",color:"gray.800",bgColor:"gray.300",children:n})}}}).use(ex(),{ignoreMissing:!0}),[null==d?void 0:d.id]),C=(0,_.useMemo)(()=>w.processSync(s).result,[s]);return d&&f?(0,o.jsx)(e7.Provider,{value:{collapse:m,outline:p},children:C}):null},no=e=>{let{openContextMenu:n,setSidebarHighlightedNode:t,nodeById:r,nodeByCite:i,previewNode:a,setPreviewNode:s,outline:l,collapse:c,linksByNodeId:d,macros:u,attachDir:p,useInheritance:m}=e,[f,h]=(0,_.useState)(""),g=encodeURIComponent(encodeURIComponent(a.id));return(0,_.useEffect)(()=>{fetch("notes/".concat(g)).then(e=>e.ok?e.text():null).then(e=>{if(e){if(""===e)return"(empty node)";h(e)}else h("(no content yet)")}).catch(e=>(h("(could not find node)"),console.log(e),"Could not fetch the text for some reason, sorry!\n\n This can happen because you have an id with forward slashes (/) in it."))},[a.id]),(0,o.jsx)(o.Fragment,{children:f&&a&&(0,o.jsx)(nr,{nodeById:r,previewNode:a,setPreviewNode:s,previewText:f,nodeByCite:i,setSidebarHighlightedNode:t,openContextMenu:n,outline:l,collapse:c,linksByNodeId:d,attachDir:p,useInheritance:m,macros:u||{}})})};var ni=t(966),na=t(58166);let ns=e=>{var n;let{previewNode:t,setPreviewNode:r,setSidebarHighlightedNode:i,nodeById:a,linksByNodeId:s,nodeByCite:c,openContextMenu:d,outline:u,macros:p,attachDir:m,useInheritance:f}=e,h=(null!==(n=s[null==t?void 0:t.id])&&void 0!==n?n:[]).filter(e=>{let[n,r]=nt(e);return n!==(null==t?void 0:t.id)}).map(e=>e.source);return(0,o.jsxs)(l.xu,{className:"backlinks",borderRadius:"sm",mt:6,p:4,bg:"white",mb:10,children:[(0,o.jsx)("p",{style:{fontSize:16,fontWeight:600},children:"Linked references (".concat(h.length,")")}),(0,o.jsx)(ni.g,{py:2,spacing:3,alignItems:"start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:(null==t?void 0:t.id)&&h.map(e=>{var n,h;return null===(n=a[e])||void 0===n||n.title,(0,o.jsx)(l.xu,{overflow:"hidden",py:1,borderRadius:"sm",width:"100%",children:(0,o.jsx)(eJ,{linksByNodeId:s,nodeByCite:c,setSidebarHighlightedNode:i,href:"id:".concat(e),nodeById:a,previewNode:t,setPreviewNode:r,openContextMenu:d,outline:u,noUnderline:!0,attachDir:m,useInheritance:f,macros:p,children:null===(h=a[e])||void 0===h?void 0:h.title})},e)})})]})},nl=e=>{let{setPreviewNode:n,justificationList:t,justification:r,previewNode:i,nodeById:a,nodeByCite:s,setSidebarHighlightedNode:d,linksByNodeId:u,openContextMenu:p,outline:m,collapse:f,macros:h,attachDir:g,useInheritance:b}=e,_=m?eK:eG;return(0,o.jsx)(l.xu,{pr:8,pt:2,height:"100%",className:"org",sx:{...eV,..._,textAlign:t[r]},children:(null==i?void 0:i.id)&&(0,o.jsxs)(c.k,{className:"wrapClass",height:"100%",flexDirection:"column",justifyContent:"space-between",children:[(0,o.jsx)(no,{setPreviewNode:n,previewNode:i,nodeByCite:s,setSidebarHighlightedNode:d,openContextMenu:p,outline:m,collapse:f,nodeById:a,linksByNodeId:u,macros:h,attachDir:g,useInheritance:b}),(0,o.jsx)(ns,{setPreviewNode:n,previewNode:i,nodeById:a,linksByNodeId:u,nodeByCite:s,setSidebarHighlightedNode:d,openContextMenu:p,outline:m,attachDir:g,useInheritance:b,macros:h||{}})]})})},nc=e=>{let{previewNode:n}=e;return(0,o.jsx)(c.k,{maxW:"90%",children:(0,o.jsx)(A.X,{lineHeight:1.2,size:"md",fontWeight:600,pt:4,children:null==n?void 0:n.title})})};var nd=t(46312);function nu(e,n){var t,r;let o=arguments.length>2&&void 0!==arguments[2]?arguments[2]:{},i=(r=null!==(t=o.storage)&&void 0!==t?t:localStorage,{get(){let n=r.getItem(e);if(n&&"undefined"!==n)return JSON.parse(n)},update(n){r.setItem(e,JSON.stringify(n))},remove(){r.removeItem(e)}}),a=i.get(),s=void 0!==a?a:n,l=null!=a&&"object"==typeof a&&!1===Array.isArray(a)?{...n,...a}:s;l!==a&&i.update(l);let[c,d]=(0,_.useState)(l);return(0,_.useEffect)(()=>{c!==l&&d(l)},[e]),[c,e=>{if(e instanceof Function){d(n=>{let t=e(n);return i.update(t),t});return}d(e),i.update(e)}]}var np=e=>{let{isOpen:n,onOpen:t,onClose:r,previewNode:i,setPreviewNode:a,nodeById:s,linksByNodeId:d,nodeByCite:p,setSidebarHighlightedNode:m,canUndo:f,canRedo:h,resetPreviewNode:g,previousPreviewNode:b,nextPreviewNode:y,openContextMenu:x,scope:v,setScope:w,windowWidth:C,filter:z,setFilter:T,tagColors:S,setTagColors:R,macros:I,attachDir:E,useInheritance:B}=e,{highlightColor:F}=(0,_.useContext)(eX.N),[D,P]=(0,_.useState)(),[j,O]=nu("sidebarWidth",400);(0,_.useEffect)(()=>{if(!(null==i?void 0:i.id)){r();return}t(),P(i)},[null==i?void 0:i.id]);let[L,A]=nu("justification",1),[W,M]=nu("outline",!1),[q,H]=(0,_.useState)("sans serif"),[Z,N]=(0,_.useState)(0),[X,G]=(0,_.useState)(!1);return(0,o.jsx)(en,{animateOpacity:!1,dimension:"width",in:n,unmountOnExit:!0,startingSize:0,style:{height:"100vh"},children:(0,o.jsx)(nd.e,{size:{height:"100vh",width:j},onResizeStop:(e,n,t,r)=>{O(e=>e+r.width)},enable:{top:!1,right:!1,bottom:!1,left:!0,topRight:!1,bottomRight:!1,bottomLeft:!1,topLeft:!1},minWidth:"220px",maxWidth:C-200,children:(0,o.jsxs)(c.k,{flexDir:"column",h:"100vh",pl:2,color:"black",bg:"alt.100",width:"100%",children:[(0,o.jsxs)(c.k,{pl:2,alignItems:"center",color:"black",width:"100%",children:[(0,o.jsx)(c.k,{pt:1,flexShrink:0,children:(0,o.jsx)(es,{setJustification:A,setIndent:N,setFont:H,justification:L,setPreviewNode:a,canUndo:f,canRedo:h,resetPreviewNode:g,previousPreviewNode:b,nextPreviewNode:y,outline:W,setOutline:M,collapse:X,setCollapse:G})}),(0,o.jsx)(c.k,{whiteSpace:"nowrap",textOverflow:"ellipsis",overflow:"hidden",onContextMenu:e=>{e.preventDefault(),x(i,e)}}),(0,o.jsx)(c.k,{flexDir:"row",ml:"auto",children:(0,o.jsx)(u.h,{m:1,icon:(0,o.jsx)(k.T41,{}),"aria-label":"Options",variant:"subtle",onClick:e=>{x(i,e,{left:void 0,top:12,right:-C+20,bottom:void 0})}})})]}),(0,o.jsx)(eU.$B,{autoHide:!0,renderThumbVertical:e=>{let{style:n,...t}=e;return(0,o.jsx)(l.xu,{style:{...n,borderRadius:0},...t})},children:D&&(0,o.jsxs)(ni.g,{flexGrow:1,alignItems:"left",bg:"alt.100",paddingLeft:4,children:[(0,o.jsx)(nc,{previewNode:D}),(0,o.jsx)(ec,{filter:z,setFilter:T,tagColors:S,setTagColors:R,openContextMenu:x,previewNode:i}),(0,o.jsx)(nl,{setPreviewNode:a,previewNode:i,nodeById:s,nodeByCite:p,setSidebarHighlightedNode:m,justification:L,justificationList:["justify","start","end","center"],linksByNodeId:d,openContextMenu:x,outline:W,setOutline:M,collapse:X,macros:I,attachDir:E,useInheritance:B})]})})]})})})},nm=t(93108),nf=t(82215),nh=t(88087),ng=t(64071),nb=t(87334),n_=t(47234),ny=t(91170),nx=t(63500),nk=t(47647);let nv=e=>{var n;let{filter:t,listName:r,labelFilter:i,displayName:a,setFilter:s,options:l=[]}=e,{highlightColor:c}=(0,_.useContext)(eX.N),d=(null==l?void 0:l.map(e=>({value:e,label:i?e.replace(i,""):e})))||[],[u,p]=(0,_.useState)((null===(n=t[r])||void 0===n?void 0:n.map(e=>({value:e,label:i?null==e?void 0:e.replace(i,""):e})))||[]);return(0,o.jsx)(nk.CUIAutoComplete,{labelStyleProps:{fontWeight:300,fontSize:14},items:d,label:"Add tag to ".concat(a),placeholder:" ",onCreateItem:e=>null,disableCreateItem:!0,selectedItems:u,onSelectedItemsChange:e=>{e.selectedItems&&(p(e.selectedItems),s({...t,[r]:e.selectedItems.map(e=>e.value)}))},listItemStyleProps:{overflow:"hidden"},highlightItemBg:"gray.400",toggleButtonStyleProps:{variant:"outline"},inputStyleProps:{mt:2,height:8,focusBorderColor:c,color:"gray.800",borderColor:"gray.500"},tagStyleProps:{justifyContent:"flex-start",fontSize:10,borderColor:c,borderWidth:1,borderRadius:"md",color:c,bg:"",height:4,mb:2},hideToggleButton:!0,itemRenderer:e=>e.label})},nw=e=>{let{colorList:n,tagColors:t,setTagColors:r,highlightColor:i,tags:a}=e,s=a.map(e=>({value:e,label:e})),[d,p]=(0,_.useState)(Object.keys(t).map(e=>({value:e,label:e})));return(0,o.jsxs)(l.xu,{children:[(0,o.jsx)(nk.CUIAutoComplete,{items:s,labelStyleProps:{fontWeight:300,fontSize:14},label:"Add tag to color",placeholder:" ",disableCreateItem:!0,selectedItems:d,onSelectedItemsChange:e=>{e.selectedItems&&(p(Array.from(new Set(e.selectedItems))),r(Object.fromEntries(Array.from(new Set(e.selectedItems)).map(e=>{var n;return[e.label,null!==(n=t[e.label])&&void 0!==n?n:"gray.600"]}))))},listItemStyleProps:{overflow:"hidden"},highlightItemBg:"gray.400",toggleButtonStyleProps:{variant:"outline"},inputStyleProps:{height:8,focusBorderColor:i,color:"gray.800",borderColor:"gray.500"},tagStyleProps:{display:"none",rounded:"full",bg:i,height:8,paddingLeft:4,fontWeight:"bold"},hideToggleButton:!0,itemRenderer:e=>e.label}),(0,o.jsx)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:Object.keys(t).map(e=>(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",width:"100%",pl:2,children:[(0,o.jsx)(l.xu,{width:"100%",children:(0,o.jsx)(H.x,{children:e})}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,colorScheme:"",color:"black",children:(0,o.jsx)(l.xu,{bgColor:t[e],borderRadius:"sm",height:6,width:6})}),(0,o.jsx)(eq.h,{children:(0,o.jsx)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:n.map(n=>(0,o.jsx)(M.s,{onClick:()=>r({...t,[e]:n}),justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{bgColor:n,borderRadius:"sm",height:6,width:6})},n))})})]}),(0,o.jsx)(u.h,{"aria-label":"Delete tag color",variant:"ghost",icon:(0,o.jsx)(q.DeleteIcon,{}),onClick:()=>{r(Object.fromEntries(Array.from(new Set(d)).map(e=>{var n;return[e.label,null!==(n=t[e.label])&&void 0!==n?n:"gray.600"]}))),p(d.filter(n=>n.value!==e))}})]},e))})]})};var nC=t(4046);let nz=e=>{let{infoText:n}=e;return(0,o.jsx)(l.xu,{paddingLeft:"1",children:(0,o.jsx)(d.u,{label:n,placement:"top",color:"gray.100",bg:"gray.800",hasArrow:!0,children:(0,o.jsx)(q.InfoOutlineIcon,{})})})},nT=e=>{let{min:n=0,max:t=10,step:r=.1,value:i=1,...a}=e,{onChange:s,label:c,infoText:u}=a,{highlightColor:p}=(0,_.useContext)(eX.N);return(0,o.jsxs)(l.xu,{pt:1,pb:2,children:[(0,o.jsxs)(l.xu,{display:"flex",alignItems:"flex-end",mb:2,children:[(0,o.jsx)(H.x,{children:c}),u&&(0,o.jsx)(nz,{infoText:u})]}),(0,o.jsxs)(nC.iR,{value:i,onChange:s,min:n,max:t,step:r,children:[(0,o.jsx)(nC.Uj,{children:(0,o.jsx)(nC.Ms,{})}),(0,o.jsx)(d.u,{bg:p,label:i.toFixed(1),children:(0,o.jsx)(nC.gs,{bg:"white"})})]})]},c)},nS=(0,_.createContext)({subDirs:["dailies",".attach"],attachDir:".attach",useInheritance:!1,roamDir:"~/org",dailyDir:"dailies"});var nR=e=>{let{filter:n,setFilter:t,local:r,setLocal:i,tagColors:a,setTagColors:s,highlightColor:d,colorList:u,tags:p}=e,{roamDir:m,subDirs:f}=(0,_.useContext)(nS);return(0,o.jsxs)(l.xu,{children:[(0,o.jsxs)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",paddingLeft:7,color:"gray.800",children:[(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Link children to"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),colorScheme:"",color:"black",size:"sm",children:(()=>{switch(n.parent){case"parent":return(0,o.jsx)(H.x,{children:"File"});case"heading":return(0,o.jsx)(H.x,{children:"Heading"});default:return(0,o.jsx)(H.x,{children:"Nothing"})}})()}),(0,o.jsx)(eq.h,{children:(0,o.jsxs)(L.q,{bgColor:"gray.200",zIndex:"popover",children:[(0,o.jsx)(M.s,{onClick:()=>t(e=>({...e,parent:""})),children:"Nothing"}),(0,o.jsx)(M.s,{onClick:()=>t(e=>({...e,parent:"parent"})),children:"Parent file node"}),(0,o.jsx)(M.s,{onClick:()=>t(e=>({...e,parent:"heading"})),children:"Next highest heading node"})]})})]})]}),(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Orphans"}),(0,o.jsx)(nx.r,{onChange:()=>{t(e=>({...e,orphans:!e.orphans}))},isChecked:n.orphans})]}),(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Dailies"}),(0,o.jsx)(nx.r,{onChange:()=>{t(e=>({...e,dailies:!e.dailies}))},isChecked:n.dailies})]}),(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Org-noter pages"}),(0,o.jsx)(nx.r,{onChange:()=>{t(e=>({...e,noter:!e.noter}))},isChecked:n.noter})]}),(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Citations without note files"}),(0,o.jsx)(nx.r,{onChange:()=>{t({...n,filelessCites:!n.filelessCites})},isChecked:n.filelessCites})]}),(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Non-existent nodes"}),(0,o.jsx)(nx.r,{onChange:()=>{s({...a,bad:"white"}),t({...n,bad:!n.bad})},isChecked:n.bad})]}),(0,o.jsx)(nT,{label:"Number of neighbors in local graph",value:r.neighbors,onChange:e=>i({...r,neighbors:e}),min:1,max:5,step:1})]}),(0,o.jsxs)(nf.U,{padding:0,allowToggle:!0,allowMultiple:!0,paddingLeft:3,children:[(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:["Directory filters",(0,o.jsx)(nb.X,{})]}),(0,o.jsxs)(n_.H,{pr:0,mr:0,children:[(0,o.jsx)(nv,{filter:n,setFilter:t,options:f,displayName:"directory blocklist",listName:"dirsBlocklist",labelFilter:m}),(0,o.jsx)(nv,{filter:n,setFilter:t,options:f,displayName:"directory allowlist",listName:"dirsAllowlist",labelFilter:m})]})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:["Tag filters",(0,o.jsx)(nb.X,{})]}),(0,o.jsxs)(n_.H,{pr:0,mr:0,children:[(0,o.jsx)(nv,{filter:n,setFilter:t,options:p,displayName:"tag blocklist",listName:"tagsBlacklist"}),(0,o.jsx)(nv,{filter:n,setFilter:t,options:p,displayName:"tag allowlist",listName:"tagsWhitelist"})]})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:["Tag colors",(0,o.jsx)(nb.X,{})]}),(0,o.jsx)(n_.H,{pr:0,mr:0,children:(0,o.jsx)(nw,{tags:p,colorList:u,tagColors:a,setTagColors:s,highlightColor:d})})]})]})]})},nI=t(10429);let nE=e=>{let{value:n,onChange:t,label:r,infoText:i,children:a}=e;return(0,o.jsxs)(l.xu,{paddingTop:2,children:[(0,o.jsxs)(l.xu,{display:"flex",justifyContent:"space-between",paddingBottom:2,children:[(0,o.jsxs)(l.xu,{display:"flex",alignItems:"center",children:[(0,o.jsx)(H.x,{children:r}),i&&(0,o.jsx)(nz,{infoText:i})]}),(0,o.jsx)(nx.r,{isChecked:!!n,onChange:t})]}),(0,o.jsx)(nI.U,{in:!!n,animateOpacity:!0,children:(0,o.jsx)(l.xu,{paddingLeft:4,paddingTop:2,paddingBottom:2,children:a})})]},r)},nB=e=>{let{physics:n,setPhysics:t}=e,r=(0,_.useCallback)((e,n,r)=>{t(t=>({...t,[n]:e/r}))},[]);return(0,o.jsxs)(l.xu,{children:[(0,o.jsxs)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",paddingLeft:7,color:"gray.800",children:[(0,o.jsxs)(nE,{label:"Gravity",value:n.gravityOn,onChange:()=>t({...n,gravityOn:!n.gravityOn}),children:[(0,o.jsxs)(c.k,{justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Also in local"}),(0,o.jsx)(nx.r,{onChange:()=>{t(e=>({...e,gravityLocal:!e.gravityLocal}))},isChecked:n.gravityLocal})]}),(0,o.jsx)(nT,{label:"Strength",value:10*n.gravity,onChange:e=>r(e,"gravity",10)})]}),(0,o.jsx)(nT,{value:-n.charge/100,onChange:e=>r(e,"charge",-.01),label:"Repulsive Force"}),(0,o.jsx)(nT,{value:5*n.linkStrength,onChange:e=>r(e,"linkStrength",5),label:"Link Force"}),(0,o.jsx)(nT,{label:"Stabilization rate",value:50*n.alphaDecay,onChange:e=>r(e,"alphaDecay",50)})]}),(0,o.jsx)(l.xu,{children:(0,o.jsx)(nf.U,{paddingLeft:3,allowToggle:!0,children:(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:[(0,o.jsx)(H.x,{children:"Advanced"}),(0,o.jsx)(nb.X,{marginRight:2})]}),(0,o.jsx)(n_.H,{children:(0,o.jsxs)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",paddingLeft:3,color:"gray.800",children:[(0,o.jsx)(nE,{label:"Collision",infoText:"Perfomance sap, disable if slow",value:n.collision,onChange:()=>t({...n,collision:!n.collision}),children:(0,o.jsx)(nT,{value:n.collisionStrength/5,onChange:e=>r(e,"collisionStrength",.2),label:"Collision Radius",infoText:"Easy with this one, high values can lead to a real jiggly mess"})}),(0,o.jsx)(nT,{label:"Link iterations",value:n.linkIts,onChange:e=>r(e,"linkIts",1),min:0,max:6,step:1,infoText:"How many links down the line the physics of a single node affects (Slow)"}),(0,o.jsx)(nT,{label:"Viscosity",value:10*n.velocityDecay,onChange:e=>r(e,"velocityDecay",10)}),(0,o.jsx)(nE,{label:"Center nodes",value:n.centering,onChange:()=>t({...n,centering:!n.centering}),infoText:"Keeps the nodes in the center of the viewport. If disabled you can drag the nodes anywhere you want.",children:(0,o.jsx)(nT,{label:"Centering Strength",value:n.centeringStrength,max:2,step:.01,onChange:e=>r(e,"centeringStrength",1)})})]})})]})})})]})},nF=e=>{let{behavior:n,setBehavior:t,mouse:r,setMouse:i}=e;return(0,o.jsxs)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",paddingLeft:7,color:"gray.800",children:[(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Preview node"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),colorScheme:"",color:"black",children:(0,o.jsx)(H.x,{children:r.preview?r.preview[0].toUpperCase()+r.preview.slice(1):"Never"})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsxs)(L.q,{bgColor:"gray.200",zIndex:"popover",children:[(0,o.jsx)(M.s,{onClick:()=>i({...r,preview:""}),children:"Never"}),(0,o.jsx)(M.s,{onClick:()=>i({...r,preview:"click"}),children:"Click"}),(0,o.jsx)(M.s,{onClick:()=>i({...r,preview:"double"}),children:"Double Click"})]})]})]})]}),(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsxs)(c.k,{children:[(0,o.jsx)(H.x,{children:"Expand Node"}),(0,o.jsx)(nz,{infoText:"View only the node and its direct neighbors"})]}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),colorScheme:"",color:"black",children:(0,o.jsx)(H.x,{children:r.local?r.local[0].toUpperCase()+r.local.slice(1):"Never"})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsxs)(L.q,{zIndex:"popover",bgColor:"gray.200",children:[(0,o.jsx)(M.s,{onClick:()=>i({...r,local:""}),children:"Never"}),(0,o.jsx)(M.s,{onClick:()=>i({...r,local:"click"}),children:"Click"}),(0,o.jsx)(M.s,{onClick:()=>i({...r,local:"double"}),children:"Double Click"}),(0,o.jsx)(M.s,{onClick:()=>i({...r,local:"right"}),children:"Right Click"})]})]})]})]}),(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsxs)(c.k,{children:[(0,o.jsx)(H.x,{children:"Local graph"}),(0,o.jsx)(nz,{infoText:"When in local mode and clicking a new node, should I add that node's local graph or open the new one?"})]}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),colorScheme:"",color:"black",children:(0,o.jsx)(H.x,{children:"add"===n.localSame?"Add":"Replace"})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsxs)(L.q,{bgColor:"gray.200",zIndex:"popover",children:[(0,o.jsx)(M.s,{onClick:()=>t({...n,localSame:"replace"}),children:"Open that nodes graph"}),(0,o.jsx)(M.s,{onClick:()=>t({...n,localSame:"add"}),children:"Add node to local graph"})]})]})]})]}),(0,o.jsx)(nT,{label:"Zoom speed",value:n.zoomSpeed,min:0,max:4e3,step:100,onChange:e=>t({...n,zoomSpeed:e})}),(0,o.jsx)(nT,{label:"Zoom padding",value:n.zoomPadding,min:0,max:400,step:1,onChange:e=>t({...n,zoomPadding:e}),infoText:"How much to zoom out to accomodate all nodes when changing the view."})]})};var nD=t(41731);let nP=e=>{let{visuals:n,setVisuals:t}=e;return(0,o.jsx)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:(0,o.jsx)(l.xu,{children:(0,o.jsx)(nE,{label:"Highlight",onChange:()=>t(e=>({...e,highlight:!e.highlight})),value:n.highlight,children:(0,o.jsxs)(ni.g,{spacing:1,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.400"}),align:"stretch",paddingLeft:0,children:[(0,o.jsx)(nT,{label:"Highlight Link Thickness",value:n.highlightLinkSize,onChange:e=>t(n=>({...n,highlightLinkSize:e}))}),(0,o.jsx)(nT,{label:"Highlight Node Size",value:n.highlightNodeSize,onChange:e=>t(n=>({...n,highlightNodeSize:e}))}),(0,o.jsx)(nT,{min:0,max:1,label:"Highlight Fade",value:n.highlightFade,onChange:e=>t(n=>({...n,highlightFade:e}))}),(0,o.jsxs)(nE,{label:"Highlight Animation",onChange:()=>{t(e=>({...e,highlightAnim:!e.highlightAnim}))},value:n.highlightAnim,children:[(0,o.jsx)(nT,{label:"Animation speed",onChange:e=>t(n=>({...n,animationSpeed:e})),value:n.animationSpeed,infoText:"Slower speed has a chance of being buggy",min:50,max:1e3,step:10}),(0,o.jsx)(nD.P,{placeholder:n.algorithmName,onChange:e=>{t(n=>({...n,algorithmName:e.target.value}))},children:n.algorithmOptions.map(e=>(0,o.jsx)("option",{value:e,children:e},e))})]})]})})})})};var nj=t(1991),nO=t(18689);let nL=e=>{let{label:n,colorList:t,value:r,visValue:i,setVisuals:a,noEmpty:s}=e,d=(0,_.useCallback)(e=>a(n=>({...n,[r]:e})),[]);return(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:n}),(0,o.jsxs)(eW.J,{isLazy:!0,placement:"right",children:[(0,o.jsx)(eM.x,{children:(0,o.jsx)(nm.z,{colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:(0,o.jsx)(l.xu,{bgColor:i,borderRadius:"sm",height:6,width:6})})}),(0,o.jsx)(eq.h,{children:(0,o.jsx)(eH.y,{zIndex:"tooltip",maxW:36,position:"relative",children:(0,o.jsxs)(c.k,{flexWrap:"wrap",bgColor:"gray.200",children:[!s&&(0,o.jsx)(l.xu,{onClick:()=>d(""),justifyContent:"space-between",alignItems:"center",display:"flex",m:1,children:(0,o.jsx)(l.xu,{height:6,width:6,borderColor:"gray.600",borderRadius:"xl",borderWidth:1})}),t.map(e=>(0,o.jsx)(l.xu,{m:1,onClick:()=>d(e),justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{bgColor:e,borderRadius:"xl",height:6,width:6})},e))]})})})]})]})},nA=e=>{let{visuals:n,setVisualsCallback:t,highlightColor:r,setHighlightColor:i}=e;return(0,o.jsx)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:(0,o.jsxs)(l.xu,{children:[(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Nodes"}),(0,o.jsx)(d.u,{label:"Shuffle node colors",children:(0,o.jsx)(u.h,{"aria-label":"Shuffle node colors",size:"sm",icon:(0,o.jsx)(q.RepeatIcon,{}),variant:"ghost",onClick:()=>{var e;let r=null!==(e=n.nodeColorScheme)&&void 0!==e?e:[];t({...n,nodeColorScheme:r.map(e=>[Math.random(),e]).sort((e,n)=>{let[t]=e,[r]=n;return t-r}).map(e=>{let[n,t]=e;return t})})}})}),(0,o.jsx)(d.u,{label:"Cycle node colors",children:(0,o.jsx)(u.h,{"aria-label":"Shift node colors",icon:(0,o.jsx)(q.ArrowRightIcon,{}),size:"sm",variant:"ghost",onClick:()=>{var e;let r=null!==(e=n.nodeColorScheme)&&void 0!==e?e:[];t({...n,nodeColorScheme:[...r.slice(1,r.length),r[0]]})}})}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",closeOnSelect:!1,matchWidth:!0,children:[(0,o.jsx)(ny.j,{width:20,as:nm.z,colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:(0,o.jsx)(c.k,{height:6,width:6,flexDirection:"column",flexWrap:"wrap",children:n.nodeColorScheme.map(e=>(0,o.jsx)(l.xu,{bgColor:e,flex:"1 1 8px",borderRadius:"2xl"},e))})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsx)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:(0,o.jsx)(nj._,{width:500,type:"checkbox",defaultValue:n.nodeColorScheme,onChange:e=>{e.length&&t({...n,nodeColorScheme:e})},children:j.map(e=>(0,o.jsx)(nO.i,{isChecked:n.nodeColorScheme.some(n=>n===e),value:e,isDisabled:1===n.nodeColorScheme.length&&n.nodeColorScheme[0]===e,children:(0,o.jsx)(l.xu,{justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{bgColor:e,borderRadius:"sm",height:6,width:6})})},e))})})]})]})]}),(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Links"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:(0,o.jsx)(l.xu,{children:n.linkColorScheme?(0,o.jsx)(l.xu,{bgColor:n.linkColorScheme,borderRadius:"sm",height:6,width:6}):(0,o.jsx)(c.k,{height:6,width:6,flexDirection:"column",flexWrap:"wrap",children:n.nodeColorScheme.map(e=>(0,o.jsx)(l.xu,{bgColor:e,flex:"1 1 8px",borderRadius:"2xl"},e))})})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsxs)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:[(0,o.jsx)(M.s,{onClick:()=>t({...n,linkColorScheme:""}),justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(c.k,{height:6,width:6,flexDirection:"column",flexWrap:"wrap",children:n.nodeColorScheme.map(e=>(0,o.jsx)(l.xu,{bgColor:e,flex:"1 1 8px",borderRadius:"2xl"},e))})}),j.map(e=>(0,o.jsx)(M.s,{onClick:()=>t({...n,linkColorScheme:e}),justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{bgColor:e,borderRadius:"sm",height:6,width:6})},e))]})]})]})]}),(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Accent"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:(0,o.jsx)(l.xu,{bgColor:r,borderRadius:"sm",height:6,width:6})}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsx)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:j.map(e=>(0,o.jsx)(M.s,{onClick:()=>i(e),justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{bgColor:e,borderRadius:"sm",height:6,width:6})},e))})]})]})]}),(0,o.jsx)(nL,{colorList:j,label:"Link highlight",setVisuals:t,value:"linkHighlight",visValue:n.linkHighlight}),(0,o.jsx)(nL,{colorList:j,label:"Node highlight",setVisuals:t,value:"nodeHighlight",visValue:n.nodeHighlight}),(0,o.jsx)(nL,{colorList:j,label:"Background",setVisuals:t,value:"backgroundColor",visValue:n.backgroundColor}),(0,o.jsx)(nL,{colorList:j,label:"Emacs node",setVisuals:t,value:"emacsNodeColor",visValue:n.emacsNodeColor})]})})},nW=e=>{let{visuals:n,setVisuals:t,threeDim:r}=e;return(0,o.jsx)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:(0,o.jsxs)(l.xu,{children:[(0,o.jsx)(nT,{label:"Node size",value:n.nodeRel,onChange:e=>t({...n,nodeRel:e})}),(0,o.jsx)(nT,{label:"Node degree size multiplier",value:n.nodeSizeLinks,min:0,max:2,onChange:e=>t({...n,nodeSizeLinks:e})}),(0,o.jsx)(nT,{label:"Node zoom invariance",value:n.nodeZoomSize,min:0,max:2,infoText:"How much the graph will try to keep the nodesize consistent across zoom scales. 0 is no consistency, node will always be their true size, 1 is linear, 2 is quadratic.",onChange:e=>t(n=>({...n,nodeZoomSize:e}))}),r&&(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(nT,{label:"Node opacity",value:n.nodeOpacity,min:0,max:1,onChange:e=>t({...n,nodeOpacity:e})}),(0,o.jsx)(nT,{label:"Node resolution",value:n.nodeResolution,min:5,max:32,step:1,onChange:e=>t({...n,nodeResolution:e})})]}),(0,o.jsx)(nT,{label:"Link width",value:n.linkWidth,onChange:e=>t({...n,linkWidth:e})}),r&&(0,o.jsx)(nT,{label:"Link opacity",min:0,max:1,value:n.linkOpacity,onChange:e=>t({...n,linkOpacity:e})}),(0,o.jsxs)(nE,{label:"Link arrows",value:n.arrows,onChange:()=>t({...n,arrows:!n.arrows}),children:[(0,o.jsx)(nT,{label:"Arrow size",value:n.arrowsLength/10,onChange:e=>t({...n,arrowsLength:10*e})}),(0,o.jsx)(nT,{label:"Arrow Position",value:n.arrowsPos,min:0,max:1,step:.01,onChange:e=>t({...n,arrowsPos:e})}),(0,o.jsx)(nL,{colorList:j,label:"Arrow Color",setVisuals:t,value:"arrowsColor",visValue:n.arrowsColor},"arrow")]}),(0,o.jsxs)(nE,{label:"Directional Particles",value:n.particles,onChange:()=>t({...n,particles:!n.particles}),children:[(0,o.jsx)(nT,{label:"Particle Number",value:n.particlesNumber,max:5,step:1,onChange:e=>t({...n,particlesNumber:e})}),(0,o.jsx)(nT,{label:"Particle Size",value:n.particlesWidth,onChange:e=>t({...n,particlesWidth:e})})]})]})})},nM=e=>{let{visuals:n,setVisuals:t}=e;return(0,o.jsxs)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.400"}),align:"stretch",color:"gray.800",children:[(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",children:[(0,o.jsx)(H.x,{children:"Show labels"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:n.labels?n.labels<2?"On Highlight":"Always":"Never"}),(0,o.jsxs)(eq.h,{children:[" ",(0,o.jsxs)(L.q,{zIndex:"popover",bgColor:"gray.200",children:[(0,o.jsx)(M.s,{onClick:()=>t({...n,labels:0}),children:"Never"}),(0,o.jsx)(M.s,{onClick:()=>t({...n,labels:1}),children:"On Highlight"}),(0,o.jsx)(M.s,{onClick:()=>t({...n,labels:2}),children:"Always"}),(0,o.jsx)(M.s,{onClick:()=>t({...n,labels:3}),children:"Always (even in 3D)"})]})]})]})]}),(0,o.jsxs)(nI.U,{in:n.labels>1,animateOpacity:!0,children:[(0,o.jsx)(l.xu,{paddingTop:2,children:(0,o.jsx)(nT,{label:"Label Appearance Scale",value:2*n.labelScale,onChange:e=>t({...n,labelScale:e/2})})}),(0,o.jsxs)(l.xu,{paddingTop:2,children:[(0,o.jsx)(nT,{label:"Label dynamicity",infoText:"By default, labels of nodes with more links will appear earlier than those with fewer. This slider changes the strength of this effect, put it at zero to disable it.",value:n.labelDynamicStrength,min:0,max:1,step:.05,onChange:e=>t(n=>({...n,labelDynamicStrength:e}))}),(0,o.jsx)(nI.U,{in:n.labelDynamicStrength>0,children:(0,o.jsx)(nT,{label:"Dynamic zoom degree cap",infoText:"The maximum number of links that is considered for the 'dynamic zoom effect'. Past this number all number of links are treated the same.",value:n.labelDynamicDegree,min:1,max:15,step:1,onChange:e=>t(n=>({...n,labelDynamicDegree:e}))})})]})]}),(0,o.jsx)(nL,{colorList:j,label:"Text",setVisuals:t,value:"labelTextColor",visValue:n.labelTextColor}),(0,o.jsxs)(l.xu,{children:[(0,o.jsx)(nL,{colorList:j,label:"Background",setVisuals:t,value:"labelBackgroundColor",visValue:n.labelBackgroundColor}),(0,o.jsx)(nI.U,{in:!!n.labelBackgroundColor,animateOpacity:!0,children:(0,o.jsx)(l.xu,{paddingTop:2,children:(0,o.jsx)(nT,{label:"Background opacity",value:n.labelBackgroundOpacity,onChange:e=>{console.log(n.labelBackgroundOpacity),t({...n,labelBackgroundOpacity:e})},min:0,max:1,step:.01})})})]}),(0,o.jsx)(nT,{label:"Label font size",value:n.labelFontSize,min:5,max:20,step:.5,onChange:e=>t({...n,labelFontSize:e})}),(0,o.jsx)(nT,{label:"Max. label characters",value:n.labelLength,min:10,max:100,step:1,onChange:e=>t({...n,labelLength:e})}),(0,o.jsx)(nT,{label:"Max. label line length",value:n.labelWordWrap,min:10,max:100,step:1,onChange:e=>t({...n,labelWordWrap:e})}),(0,o.jsx)(nT,{label:"Space between label lines",value:n.labelLineSpace,min:.2,max:3,step:.1,onChange:e=>t({...n,labelLineSpace:e})})]})};var nq=t(5393);let nH=()=>{let{emacsTheme:e,setEmacsTheme:n,highlightColor:t}=(0,_.useContext)(eX.N);return(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",pl:7,pr:2,children:[(0,o.jsx)(H.x,{children:"Theme"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"bottom",closeOnSelect:!1,children:[(0,o.jsx)(ny.j,{as:nm.z,size:"sm",colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:String(e[0])}),(0,o.jsxs)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:[(0,o.jsx)(M.s,{onClick:()=>"",justifyContent:"space-between",alignItems:"center",display:"flex",children:(0,o.jsx)(l.xu,{height:6,width:6})}),Object.keys(nq.n).map((e,t)=>(0,o.jsxs)(M.s,{onClick:()=>n([e,nq.n[e]]),justifyContent:"space-between",alignItems:"center",display:"flex",children:[(0,o.jsx)(H.x,{children:e}),(0,o.jsx)(c.k,{height:6,width:20,flexDirection:"column",flexWrap:"wrap",children:Object.values(nq.n[e]).map(e=>(0,o.jsx)(l.xu,{bgColor:e,flex:"1 1 8px"},e))})]},e))]})]})]})},nZ=e=>{let{visuals:n,setVisuals:t}=e;return(0,o.jsx)(ni.g,{spacing:2,justifyContent:"flex-start",divider:(0,o.jsx)(na.c,{borderColor:"gray.500"}),align:"stretch",color:"gray.800",children:(0,o.jsxs)(l.xu,{children:[(0,o.jsxs)(nE,{label:"Dash cite links",infoText:"Add dashes to citation links made with org-roam-bibtex",value:n.citeDashes,onChange:()=>t({...n,citeDashes:!n.citeDashes}),children:[(0,o.jsx)(nT,{label:"Dash length",value:n.citeDashLength/10,onChange:e=>t({...n,citeDashLength:10*e})}),(0,o.jsx)(nT,{label:"Gap length",value:n.citeGapLength/5,onChange:e=>t({...n,citeGapLength:5*e})})]}),(0,o.jsx)(nL,{colorList:j,label:"Citation node color",setVisuals:t,value:"citeNodeColor",visValue:n.citeNodeColor}),(0,o.jsx)(nL,{colorList:j,label:"Citation link color",setVisuals:t,value:"citeLinkColor",visValue:n.citeLinkColor}),(0,o.jsx)(nL,{colorList:j,label:"Reference link highlight",setVisuals:t,value:"citeLinkHighlightColor",visValue:n.citeLinkHighlightColor}),(0,o.jsxs)(nE,{label:"Dash ref links",infoText:"Add dashes to citation links, whose target has a note, made with org-roam-bibtex",value:n.refDashes,onChange:()=>t({...n,refDashes:!n.refDashes}),children:[(0,o.jsx)(nT,{label:"Dash length",value:n.refDashLength/10,onChange:e=>t({...n,refDashLength:10*e})}),(0,o.jsx)(nT,{label:"Gap length",value:n.refGapLength/5,onChange:e=>t({...n,refGapLength:5*e})})]}),(0,o.jsx)(nL,{colorList:j,label:"Reference node color",setVisuals:t,value:"refNodeColor",visValue:n.refNodeColor}),(0,o.jsx)(nL,{colorList:j,label:"Reference link color",setVisuals:t,value:"refLinkColor",visValue:n.refLinkColor}),(0,o.jsx)(nL,{colorList:j,label:"Reference link highlight",setVisuals:t,value:"refLinkHighlightColor",visValue:n.refLinkHighlightColor})]})})},nN=e=>{let{coloring:n,setColoring:t}=e;return(0,o.jsxs)(c.k,{alignItems:"center",justifyContent:"space-between",pl:7,pr:2,children:[(0,o.jsx)(H.x,{children:"Graph coloring"}),(0,o.jsxs)(O.v,{isLazy:!0,placement:"right",children:[(0,o.jsx)(ny.j,{as:nm.z,size:"sm",colorScheme:"",color:"black",rightIcon:(0,o.jsx)(q.ChevronDownIcon,{}),children:"degree"===n.method?"Links":"Communities"}),(0,o.jsx)(eq.h,{children:(0,o.jsxs)(L.q,{minW:10,zIndex:"popover",bgColor:"gray.200",children:[(0,o.jsx)(M.s,{onClick:()=>t(e=>({...e,method:"degree"})),justifyContent:"space-between",alignItems:"center",display:"flex",children:"Number of links"}),(0,o.jsx)(M.s,{onClick:()=>t(e=>({...e,method:"community"})),justifyContent:"space-between",alignItems:"center",display:"flex",children:"Communities"})]})})]})]})},nX=e=>{let{coloring:n,setColoring:t,visuals:r,setVisuals:i,highlightColor:a,setHighlightColor:s,threeDim:l}=e,d=(0,_.useCallback)(e=>i(e),[]);return(0,o.jsxs)(ni.g,{justifyContent:"flex-start",align:"stretch",children:[(0,o.jsx)(nH,{}),(0,o.jsx)(nN,{coloring:n,setColoring:t}),(0,o.jsxs)(nf.U,{allowToggle:!0,defaultIndex:[0],paddingLeft:3,children:[(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{children:(0,o.jsxs)(c.k,{justifyContent:"space-between",w:"100%",children:[(0,o.jsx)(H.x,{children:"Colors"}),(0,o.jsx)(nb.X,{marginRight:2})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nA,{visuals:r,setVisualsCallback:d,highlightColor:a,setHighlightColor:s})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{children:(0,o.jsxs)(c.k,{justifyContent:"space-between",w:"100%",children:[(0,o.jsx)(H.x,{children:"Nodes & Links"}),(0,o.jsx)(nb.X,{marginRight:2})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nW,{visuals:r,setVisuals:d,threeDim:l})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{children:(0,o.jsxs)(c.k,{justifyContent:"space-between",w:"100%",children:[(0,o.jsx)(H.x,{children:"Labels"}),(0,o.jsx)(nb.X,{marginRight:2})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nM,{visuals:r,setVisuals:d})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{children:(0,o.jsxs)(c.k,{justifyContent:"space-between",w:"100%",children:[(0,o.jsx)(H.x,{children:"Highlighting"}),(0,o.jsx)(nb.X,{marginRight:2})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nP,{visuals:r,setVisuals:d})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{children:(0,o.jsxs)(c.k,{justifyContent:"space-between",w:"100%",children:[(0,o.jsx)(H.x,{children:"Citations"}),(0,o.jsx)(nb.X,{marginRight:2})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nZ,{visuals:r,setVisuals:d})})]})]})]})},nG=e=>{let{physics:n,setPhysics:t,threeDim:r,setThreeDim:i,filter:a,setFilter:s,visuals:c,setVisuals:p,mouse:m,setMouse:f,behavior:h,setBehavior:g,tags:b,tagColors:y,setTagColors:x,coloring:k,setColoring:v,local:w,setLocal:C}=e,[z,T]=nu("showTweaks",!1),{highlightColor:S,setHighlightColor:O}=(0,_.useContext)(eX.N);return z?(0,o.jsxs)(l.xu,{position:"absolute",bg:"alt.100",w:"xs",marginTop:2,marginLeft:2,borderRadius:"lg",paddingBottom:5,zIndex:10,boxShadow:"xl",maxH:"95vh",fontSize:"sm",children:[(0,o.jsxs)(l.xu,{display:"flex",justifyContent:"space-between",alignItems:"center",paddingRight:2,paddingTop:1,children:[(0,o.jsx)(d.u,{label:"2D",children:(0,o.jsx)(nm.z,{onClick:()=>i(!r),variant:"subtle",zIndex:"overlay",children:r?"3D":"2D"})}),(0,o.jsxs)(l.xu,{display:"flex",alignItems:"center",children:[(0,o.jsx)(d.u,{label:"Reset settings to defaults",children:(0,o.jsx)(u.h,{"aria-label":"Reset Defaults",icon:(0,o.jsx)(q.RepeatClockIcon,{}),onClick:()=>{p(B),s(I),f(D),t(R),g(F),v(E),O("purple.500"),C(P)},variant:"subtle",size:"sm"})}),(0,o.jsx)(u.h,{size:"sm",icon:(0,o.jsx)(q.CloseIcon,{}),"aria-label":"Close Tweak Panel",variant:"subtle",onClick:()=>T(!1)})]})]}),(0,o.jsx)(eU.ZP,{autoHeight:!0,autoHeightMax:.85*globalThis.innerHeight,autoHide:!0,renderThumbVertical:e=>{let{style:n,...t}=e;return(0,o.jsx)(l.xu,{...t,style:{...n,borderRadius:10},bg:S})},children:(0,o.jsxs)(nf.U,{allowMultiple:!0,allowToggle:!0,color:"black",children:[(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:[(0,o.jsx)(nb.X,{marginRight:2}),(0,o.jsx)(A.X,{size:"sm",children:"Filter"})]}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nR,{filter:a,setFilter:s,tagColors:y,setTagColors:x,highlightColor:S,colorList:j,tags:b,local:w,setLocal:C})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsx)(ng.K,{display:"flex",justifyContent:"space-between",children:(0,o.jsxs)(l.xu,{display:"flex",children:[(0,o.jsx)(nb.X,{marginRight:2}),(0,o.jsx)(A.X,{size:"sm",children:"Physics"})]})}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nB,{physics:n,setPhysics:t})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:[(0,o.jsx)(nb.X,{marginRight:2}),(0,o.jsx)(A.X,{size:"sm",children:"Visual"})]}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nX,{visuals:c,setVisuals:p,highlightColor:S,setHighlightColor:O,threeDim:r,coloring:k,setColoring:v})})]}),(0,o.jsxs)(nh.Q,{children:[(0,o.jsxs)(ng.K,{children:[(0,o.jsx)(nb.X,{marginRight:2}),(0,o.jsx)(A.X,{size:"sm",children:"Behavior"})]}),(0,o.jsx)(n_.H,{children:(0,o.jsx)(nF,{behavior:h,setBehavior:g,mouse:m,setMouse:f})})]})]})})]}):(0,o.jsx)(l.xu,{position:"absolute",zIndex:"overlay",marginTop:1,marginLeft:0,display:z?"none":"block",children:(0,o.jsx)(u.h,{variant:"subtle","aria-label":"Settings",icon:(0,o.jsx)(q.SettingsIcon,{}),onClick:()=>T(!0)})})};var nK=t(22663),nV=t.n(nK),nU=t(33179);let nY=e=>{var n;let{linksByNodeId:t,visuals:r,highlightedNodes:o,previouslyHighlightedNodes:i,opacity:a,node:s}=e,l=null!==(n=t[s.id])&&void 0!==n?n:[],c=l.length?l.filter(e=>"parent"===e.type).length:0,d=3+l.length*r.nodeSizeLinks-(nU.hX.parent?0:c);return 1===r.highlightNodeSize?d:d*(o[s.id]||i[s.id]?1+a*(r.highlightNodeSize-1):1)};function nQ(e,n){return"rgba("+(e=e.replace("#","")).match(RegExp("(.{"+e.length/3+"})","g")).map(n=>parseInt(e.length%2?n+n:n,16)).concat(isFinite(n)?n:1).join(",")+")"}let n$=(e,n,t,r,o)=>o?Math.max(e,r):1*e*(-1*(n.highlightFade*r-1)),nJ=e=>{let{ids:n,excludedIds:t,n:r,linksByNodeId:o}=e,i=[n[0]],a=[],s=[n[0]];return Array.from({length:r},()=>{i.forEach(e=>{var n;(null!==(n=o[e])&&void 0!==n?n:[]).forEach(e=>{let[n,r]=nt(e);if(!t.some(e=>[n,r].includes(e))){if(!s.includes(n)){a.push(n);return}s.includes(r)||a.push(r)}})}),i=a,a.forEach(e=>e&&s.push(e)),a=[]}),s},n0=(e,n,t)=>Math.min(Math.max(e,n),t),n1=e=>{var n,t;let{id:r,linksByNodeId:o,visuals:i,coloring:a,cluster:s}=e,l=null!==(t=null===(n=o[r])||void 0===n?void 0:n.length)&&void 0!==t?t:0;return"degree"===a.method?i.nodeColorScheme[n0(l,0,i.nodeColorScheme.length-1)]:i.nodeColorScheme[l&&s[r]%i.nodeColorScheme.length]},n2=e=>{var n,t;let{node:r,theme:o,highlightedNodes:i,previouslyHighlightedNodes:a,visuals:s,tagColors:l,highlightColors:c,opacity:d,emacsNodeId:u,linksByNodeId:p,cluster:m,coloring:f}=e,h=i[r.id]||a[r.id];if(s.emacsNodeColor&&r.id===u)return eY(s.emacsNodeColor,o);if(l&&(null==r?void 0:r.tags.some(e=>l[e]))){let e=l[null==r?void 0:r.tags.filter(e=>l[e])[0]];return h?c[e][e](s.highlightFade*d):c[e][s.backgroundColor](s.highlightFade*d)}return s.citeNodeColor&&(null==r?void 0:null===(n=r.properties)||void 0===n?void 0:n.ROAM_REFS)&&(null==r?void 0:null===(t=r.properties)||void 0===t?void 0:t.FILELESS)?h?eY(s.citeNodeColor,o):c[s.citeNodeColor][s.backgroundColor](s.highlightFade*d):s.refNodeColor&&r.properties.ROAM_REFS?h?eY(s.refNodeColor,o):c[s.refNodeColor][s.backgroundColor](s.highlightFade*d):h?s.nodeHighlight?c[n1({id:r.id,cluster:m,coloring:f,linksByNodeId:p,visuals:s})][s.nodeHighlight](d):eY(n1({id:r.id,cluster:m,coloring:f,linksByNodeId:p,visuals:s}),o):c[n1({id:r.id,cluster:m,coloring:f,linksByNodeId:p,visuals:s})][s.backgroundColor](s.highlightFade*d)},n6=(e,n)=>{var t,r;return(null===(t=e.source)||void 0===t?void 0:t.id)===(null==n?void 0:n.id)||(null===(r=e.target)||void 0===r?void 0:r.id)===(null==n?void 0:n.id)},n3=e=>{let{sourceId:n,targetId:t,linksByNodeId:r,visuals:o,coloring:i,cluster:a}=e;return r[n].length>r[t].length?n1({id:n,linksByNodeId:r,visuals:o,cluster:a,coloring:i}):n1({id:t,visuals:o,linksByNodeId:r,cluster:a,coloring:i})},n8=e=>{let{sourceId:n,targetId:t,needsHighlighting:r,theme:o,visuals:i,highlightColors:a,opacity:s,linksByNodeId:l,coloring:c,cluster:d}=e;return i.linkHighlight||i.linkColorScheme||r?r||i.linkColorScheme?r?i.linkHighlight||i.linkColorScheme?i.linkHighlight?i.linkColorScheme?a[i.linkColorScheme][i.linkHighlight](s):a[n3({sourceId:n,targetId:t,linksByNodeId:l,visuals:i,coloring:c,cluster:d})][i.linkHighlight](s):eY(i.linkColorScheme,o):eY(n3({sourceId:n,targetId:t,linksByNodeId:l,visuals:i,coloring:c,cluster:d}),o):a[i.linkColorScheme][i.backgroundColor](i.highlightFade*s):a[n3({sourceId:n,targetId:t,linksByNodeId:l,visuals:i,coloring:c,cluster:d})][i.backgroundColor](i.highlightFade*s):eY(n3({sourceId:n,targetId:t,linksByNodeId:l,visuals:i,coloring:c,cluster:d}),o)};var n4=t(35541),n5=t(19778),n7=t(69386),n9=t(44874),te=t(64859),tn=t(82140),tt=t(32735),tr=t(33090),to=t(54346),ti=t(73804),ta=JSON.parse('[{"id":"20260602T200000.000000","title":"Overview","tags":null,"content":"The aim of this project is to provide a set of convenient and performant tools for performing worldsheet string theory calculations. The project is split into the following parts:\\nCFT-Code is the kernel for performing worldsheet CFT calculations. It defines local operators, their OPE and correlation functions (on various Riemann surfaces).\\nTensor-Code defines tensor structures and provides utilities for their manipulation and generation.\\nOn-Shell-Code defines string amplitudes in the on-shell formalism.\\nSFT-Code defines string field theory vertices and brackets given an abstract set of local coordinate data. It provides utilities for the local coordinate data construction of open-closed \\\\(SL(2)\\\\) vertices.\\nNLSM-Code defines utilities for quantizing nonlinear sigma models on weakly curved target spacetimes. As such it defines the Polyakov path integral in a suitable regularization scheme. Composite operators are defined by inserting fields into the regularized path integral.\\nPure-Spinor-Code defines utilities for quantizing the \\\\(AdS_5 \\\\times S^5\\\\) pure spinor worldsheet theory by defining its path integral in a suitable regularization scheme. Composite operators are defined by inserting fields into the regularized path integral.\\nFor each part, we have a few physical goals in mind, guiding the implementation requirements:\\nCFT-CodeCovariantly solve R-sector OPE and correlation functions for GSO-projected free fermion + ghosts theories. By solving, we mean to all mass levels and at practical speeds. This provides the basic ingredients for the treatment of RR backgrounds in SFT and for computing elusive high-point on-shell amplitudes involving the R-sector.\\nTensor-CodeFor many applications (beyond the worldsheet), one needs to quickly generate independent invariant tensors contained in some tensor product of representations. Current Lie algebra packages are severely lacking in this regarding, and we aim to fill this gap.\\nOn-Shell-CodeCompute higher-point string amplitudes. A good testing ground is to reproduce known results regarding the SUSY completion of \\\\(R^4\\\\) term of Type IIB superstring, and to extend it further. This will require very efficient handling of tensor structures on both the string theory and the EFT side. For example, we expect there to be a 16-dilatino term and a 16-dilatino amplitude should have about 80 million independent invariant tensors in it.\\nAs a check, we also aim to compute the RR and NSNS amplitudes at four-point tree-level and one-loop and check they are related by SUSY . This is nontrivial, the R-sector amplitudes involve excited spin fields upon picture-raising.\\nSFT-CodeCompute the anomalous scaling dimension of the Konishi string in the large radius \\\\(AdS_5 \\\\times S^5\\\\) regime. To begin with, at tree-level, but more ambitiously also incorporate string loops, which go beyond integrability.\\nCarry out loop calculations on Calabi-Yau orientifolds, expressing the answers in terms of the 2d \\\\((2,2)\\\\) SCFT conformal data.\\nNLSM-CodeGiven numeric Calabi-Yau metrics, obtain the 2d \\\\((2,2)\\\\) SCFT conformal data in a large volume expansion. A good test-case is the IIA quintic as it only has a single volume modulus.\\nPure-Spinor-CodeSystematically quantize the \\\\(AdS_5 \\\\times S^5\\\\) pure spinor \\\\(PSU(2,2|4)\\\\) sigma model and use it to compute the pure spinor BRST cohomology at mass level 0 and 1.\\nIn a previously written Mathematica prototype, we have encountered that there are a few major bottlenecks towards achieving these goals.\\nFree field calculations fundamentally lead to a huge combinatorial blow-up of the number of terms involved. When scaling up, it is infeasible to store the intermediate expressions in RAM. Rather, we construct a stream of data that emits the various expressions and introduce filters along the way that extract the desired result. We are then only limited by the size of that result.\\nR-sector OPE was severely limited by the speed at which we could construct invariant tensors. Our algorithm was to construct a largely overcomplete set of invariants and then statistically (by plugging in random components) determine an independent subset. We must now leverage representation theory better to do less computation.\\nWe have not succeeded in writing the code abstractly enough so that adding new CFTs, combining CFTs to form a valid string worldsheet and adding boundaries, orientifolds could be done without large interference with the code. We must now design the CFT-Code kernel abstractly enough to avoid these issues.\\nDue to the large expressions involved, each part has a core written in a performant programming language that handles memory explicitly. We expose a well-defined API that allows one to easily write wrappers for say Python, Mathematica, ... backing a DSL. Also note that having such a concise API to carry out worldsheet calculations is useful for agentic coding as it saves a large amount of context.\\n"},{"id":"20260603T063945.054265","title":"CFT-Code","tags":null,"content":"The following module is the worldsheet CFT core of the project. It defines symbolic Expressions used in worldsheet CFT calculations i.e. local Operators multiplied by nontrivial Coefficients and computes their OPE and Correlators. It can also generate a basis of local operator with a given set of quantum numbers via Basis-Generation. The data of a CFT or BCFT is specified by generated preset constructors and executed by CFT Kernel.\\nAs far as dependencies go, this module only depends on Tensor-Code, which forms an integral part of the definition of Coefficients and determines the properties of various quantum numbers of Operators.  The public API follows Tensor-Code: implementation nodes such as Presets, Theory, and CFT Kernel organize the code, but clients import this root module and use the selected declarations below.\\nconst preset_impl = @import(\\"presets.zig\\");\\n\\n/// FreeBoson groups the public free-boson preset constructors.\\npub const FreeBoson = preset_impl.FreeBoson;\\n/// Bc groups the public bc-ghost preset constructors.\\npub const Bc = preset_impl.Bc;\\n/// FreeFermion groups the public NS free-fermion preset constructors.\\npub const FreeFermion = preset_impl.FreeFermion;\\n/// EtaXi groups the public eta-xi preset constructors.\\npub const EtaXi = preset_impl.EtaXi;\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub const product = preset_impl.product;\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub const boundary = preset_impl.boundary;\\n\\ntest \\"root CFT API exposes selected constructors without implementation namespaces\\" {\\n    const testing = @import(\\"std\\").testing;\\n    try testing.expect(@hasDecl(@This(), \\"FreeBoson\\"));\\n    try testing.expect(@hasDecl(@This(), \\"Bc\\"));\\n    try testing.expect(@hasDecl(@This(), \\"FreeFermion\\"));\\n    try testing.expect(@hasDecl(@This(), \\"EtaXi\\"));\\n    try testing.expect(@hasDecl(@This(), \\"product\\"));\\n    try testing.expect(@hasDecl(@This(), \\"boundary\\"));\\n    try testing.expect(@hasDecl(FreeBoson, \\"make\\"));\\n    try testing.expect(@hasDecl(FreeBoson, \\"boundary\\"));\\n    try testing.expect(@hasDecl(Bc, \\"sphere\\"));\\n    try testing.expect(@hasDecl(Bc, \\"diskBoundary\\"));\\n    try testing.expect(@hasDecl(FreeFermion, \\"sphere\\"));\\n    try testing.expect(@hasDecl(EtaXi, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(FreeBoson, \\"Config\\"));\\n    try testing.expect(!@hasDecl(FreeBoson, \\"BoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(Bc, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(Bc, \\"DiskConfig\\"));\\n    try testing.expect(!@hasDecl(FreeFermion, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(EtaXi, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"Handle\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"scalars\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeBoson\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"bcSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeFermionSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"etaXiSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeBosonBoundary\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"bcDiskBoundary\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"presets\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"kernel\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"theory\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"expressions\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"Generated\\"));\\n}\\nThis root block is the public CFT API for now.  It intentionally groups each\\nCFT family behind one compact object and does not export raw Wick-rule data,\\ncorrelator configs, generated-kernel internals, or the preset implementation\\nnamespace.  Application modules such as on-shell code, SFT code, NLSM code,\\nand pure-spinor code should build theories through the constructors above; a\\nlater public user-defined-CFT DSL should be one deliberate export rather than\\na leak of the preset runtime.\\n"},{"id":"20260603T064210.664096","title":"SFT-Code","tags":null,"content":"This module defines string field theory (SFT) brackets and vertices for open, closed and open-closed string fields theories (for all five superstrings and the bosonic string). As such, it applies conformal transforms defined by consistent local coordinate data on Operators and follows up by computing their OPE (in case of brackets) or Correlators in the case of vertices, integrated over for now abstract chains in the moduli space, with appropriate PCO insertions in the superstring case.\\n"},{"id":"20260603T064230.276471","title":"NLSM-Code","tags":null,"content":"This is an overview of NLSM-Code\\n"},{"id":"20260603T070744.656277","title":"On-Shell-Code","tags":null,"content":"This code consumes CFT-Code and handles on-shell string theory computations. That is, given a set of vertex Operators, it computes their string amplitudes from their Correlators, via prescriptions appropriate for the given string theory. We shall support all five superstrings: heterotic \\\\(SO(32)\\\\), \\\\(E_8 \\\\times E_8\\\\), Type IIA, Type IIB and Type I, and bosonic string theory. In both open, closed and open-closed sectors. For free fields, we can derive the open, open-closed sector data from that of the closed sector given a boundary condition on the fundamental fields (doubling trick). Note that each requires us to define the appropriate ghost system Theory sectors. The superstring theories each come with the a distinct notion of a GSO projection.\\n"},{"id":"20260603T085219.077418","title":"Pure-Spinor-Code","tags":null,"content":"This module computes OPE and Correlators in the CFT defined by the appropriately regularized pure spinor NLSM on \\\\(PSU(2,2|4)\\\\).\\n"},{"id":"20260603T094037.040938","title":"Normal-Ordering","tags":null,"content":"This module owns the generic normal-ordering group policy.  The group id stays\\non kernel.Call.LocalOp because Wick traversal needs it in the hot pair loop;\\nthis module only allocates, tags, and compares those compact ids.\\nconst kernel = @import(\\"../kernel.zig\\");\\n\\n/// Group is the compact id stored on local operators in one normal product.\\npub const Group = u16;\\n\\n/// none marks an operator that is not inside a normal-ordered product.\\npub const none: Group = 0;\\n\\n/// first is the first valid context-local normal-ordering group id.\\npub const first: Group = 1;\\nGroup ids are context-local.  Overflow wraps to none, and the next request\\nfails instead of silently producing an untagged normal product.\\n/// next returns a fresh normal-ordering group and advances the counter.\\npub fn next(counter: *Group) !Group {\\n    if (counter.* == none) return error.TooManyNormalOrderedProducts;\\n    const group = counter.*;\\n    counter.* +%= 1;\\n    return group;\\n}\\n\\n/// tag returns OP marked as part of GROUP.\\npub fn tag(op: kernel.Call.LocalOp, group: Group) kernel.Call.LocalOp {\\n    var tagged = op;\\n    tagged.normal_order_group = group;\\n    return tagged;\\n}\\nThe Wick kernel treats equal nonzero group ids as a forbidden contraction pair.\\nBulk tagging is used by both generated descriptor contexts and hand-written\\nZig preset builders.\\n/// same reports whether two operators are in the same nonempty normal product.\\npub fn same(left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) bool {\\n    const group = left.normal_order_group;\\n    return group != none and group == right.normal_order_group;\\n}\\n\\n/// tagLast marks the last COUNT operators as one normal-ordered product.\\npub fn tagLast(ops: []kernel.Call.LocalOp, count: usize, counter: *Group) !void {\\n    if (count == 0 or count > ops.len) return error.InvalidNormalOrderGroup;\\n    const group = try next(counter);\\n    const start = ops.len - count;\\n    for (ops[start..]) |*op| {\\n        op.* = tag(op.*, group);\\n    }\\n}\\n"},{"id":"20260603T094112.928043","title":"OPE","tags":null,"content":"This module defines the notion of an OPE, which takes symbolic Expressions and computes their OPE (again Expressions) via several possible tactics inferred from the definition of the Theory involved.\\nSince the OPE is a local expression, we evaluate it via the definition of the theory on the Riemann sphere. When the user defines a non-degenerate pairing (for a unitary CFT, can be the usual BPZ pairing) on the basis of Operators, we can evaluate the OPE by writing out all Operators that can appear in the OPE (we know the quantum numbers of the inputs so we know the quantum numbers of the output, being in their tensor product) via Basis-Generation and compute their coefficients by combining the pairing with Correlators. This way, we can immediately transfer optimizations of Correlators and Basis-Generation to an optimized OPE.\\n"},{"id":"20260603T094125.997360","title":"Operators","tags":null,"content":"const coefficient = @import(\\"coefficient.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nA local operator can be inserted at a point and carry extra information captured in the code by its body.\\n/// A local operator inserted at a point\\npub const Operator = struct {\\n    insertion: OperatorInsertion,\\n    body: OperatorBodyId,\\n};\\n\\n/// An identifier for operator body\\npub const OperatorBodyId = u32;\\nAn operator body can either be that of the identity, a generic atomic operator or a normal-ordered product of operator bodies (in free field theory).\\n/// An operator body is either atomic or normal-ordered\\npub const OperatorBody = union(enum) {\\n    identity,\\n    atomic: AtomicBody,\\n    normal_ordered: []const OperatorBodyId,\\n};\\nAn atomic operator body can come in different kinds specified when defining the Theory and can carry extra runtime information.\\n/// An atomic operator body comes in different kinds and can carry extra information\\npub const AtomicBody = struct {\\n    kind: OperatorKindId,\\n    extra: OperatorExtraId,\\n};\\n\\n/// An identifier for operator kind\\npub const OperatorKindId = u32;\\n/// An identifier for operator extra information\\npub const OperatorExtraId = u32;\\nAn operator kind contains what is required to define a local operator at the CFT level.\\n/// Defines the various kinds of operators\\npub const OperatorKind = struct {\\n    holomorphicity: Holomorphicity,\\n    conformal_weights: ConformalWeights,\\n    quantum_numbers: tensor.QuantumNumbers,\\n    supported_correlator_tactics: []const CorrelatorTactic,\\n};\\nIn the case of interest, that means definining whether the operator is holomorphic/antiholomorphic or both,\\n/// Determines whether an operator is holomorphic/antiholomorphic or both\\npub const Holomorphicity = enum {\\n    holomorphic,\\n    antiholomorphic,\\n    both,\\n};\\nits conformal weight(s) that can either be fixed at compile time or computed at runtime\\n/// ConformalWeights stores fixed weights or a rule for computing them.\\npub const ConformalWeights = union(enum) {\\n    fixed: FixedConformalWeights,\\n    computed: WeightRuleId,\\n};\\n\\n/// FixedConformalWeights stores optional left and right conformal weights.\\npub const FixedConformalWeights = struct {\\n    h: ?f16,\\n    hbar: ?f16,\\n};\\n\\n/// WeightRuleId names a rule that computes conformal weights from labels.\\npub const WeightRuleId = u16;\\nand lastly what quantum numbers supported by Tensor-Code it carries and what tactics of correlator computation it supports.\\nCurrently, the supported correlator tactics are to either keep the correlator abstract (only return zero if selection rule is no met, otherwise keep in abstract form), evaluate it via Wick contractions of input operators or bosonize the input operators first and then evaluate via Wick contractions.\\n/// A tactic for the the evaluation of correlators\\npub const CorrelatorTactic = enum {\\n    abstract,\\n    wick,\\n    bosonize,\\n};\\nThe operator insertion point be denoted by a single point such as \\\\(z\\\\) or \\\\(\\\\bar{z}\\\\), or both \\\\(z, \\\\bar{z}\\\\), which are typed as variables from Coefficient. We bundle the derivatives of a local operator with the insertion point.\\n/// Operator insertion data are specified by a position and a number of derivatives\\npub const OperatorInsertion = union(enum) {\\n    single: struct {\\n        position: coefficient.Variable,\\n        derivatives: u8,\\n        },\\n    pair: struct {\\n        holomorphic_position: coefficient.Variable,\\n        antiholomorphic_position: coefficient.Variable,\\n        holomorphic_derivatives: u8,\\n        antiholomorphic_derivatives: u8,\\n    },\\n};\\n"},{"id":"20260603T094138.285453","title":"Correlators","tags":null,"content":"This module defines the notion of a correlator, which takes symbolic Expressions and computes their correlator (a Coefficient) via several possible tactics inferred from the definition of the Theory involved.\\nThe base tactics are\\nAbstract: the correlator remains unevaluated, only returns zero if selection rules as inferred through Tensor-Code from the quantum numbers of the inputs (they must intertwine to a singlet modulo possible anomalies, which can vary from one Riemann surface to another i.e. ghost number adding up to 3 on the sphere for bc system) are not met.\\nWick: the correlator is evaluated by Wick contractions until a base case is reached. Both the value of the Wick propagator and what the base case is can depend on the Riemann surface, but the underlying abstract algorithm remains the same. What we mean by base case is that the correlator dependence on zero-modes cannot quite be inferred by a Wick contraction logic (e.g. why the base case <ccc> is not zero for c-ghosts of bc system on a sphere, note that e.g. the base case <c  c c> follows). The zero mode data is specified by the user, just as the Wick contractions are surface-by-surface, yet the underlying algorithm remains fixed.\\nBosonization: the correlator is evaluated by writing out all tensor structures of Tensor-Code that can appear in the result and matching their coefficients by plugging in particular values of group-theoretic indices to both sides of the correlator, evaluating the tensor structures numerically and evaluating the correlator via bosonization rules and the Wick tactic on the bosonized form of the operators.\\n"},{"id":"20260604T083119.372970","title":"Coefficient","tags":null,"content":"const tensor = @import(\\"tensor-code\\");\\nA coefficient is a rational function multiplied by tensor structures from\\nTensor-Code.  The full expression store will grow\\nlater, but Wick evaluation already needs a small exact vocabulary for scalar\\nmultipliers and coordinate kernels.  These records are deliberately plain\\nvalues so primitive contractions can pass them around without allocation.\\n/// Rational stores one normalized exact rational number.\\npub const Rational = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\nfn absInt(value: i64) u64 {\\n    if (value == @import(\\"std\\").math.minInt(i64)) return @as(u64, 1) << 63;\\n    return if (value < 0) @intCast(-value) else @intCast(value);\\n}\\n\\nfn gcd(first: u64, second: u64) u64 {\\n    var a = first;\\n    var b = second;\\n    while (b != 0) {\\n        const rem = a % b;\\n        a = b;\\n        b = rem;\\n    }\\n    return if (a == 0) 1 else a;\\n}\\n\\n/// rational constructs a normalized exact rational number.\\npub fn rational(numerator: i64, denominator: i64) ?Rational {\\n    if (denominator == 0) return null;\\n    var num = numerator;\\n    var den = denominator;\\n    if (den < 0) {\\n        num = -num;\\n        den = -den;\\n    }\\n    const factor: i64 = @intCast(gcd(absInt(num), absInt(den)));\\n    return .{\\n        .numerator = @divExact(num, factor),\\n        .denominator = @divExact(den, factor),\\n    };\\n}\\n\\n/// integer constructs an exact rational integer.\\npub fn integer(value: i64) Rational {\\n    return .{ .numerator = value, .denominator = 1 };\\n}\\nCoordinate kernels are the coordinate-dependent pieces produced by Wick\\ncontractions.  They are not a simplifier; they are the compact output of a\\nsingle primitive contraction after insertion derivatives have acted where the\\nWick rule requested them.\\n/// CoordinateDifference stores one ordered coordinate difference.\\npub const CoordinateDifference = struct {\\n    left: Variable,\\n    right: Variable,\\n};\\n\\n/// CoordinateKernel stores one coordinate-dependent kernel factor.\\npub const CoordinateKernel = union(enum) {\\n    difference_power: struct { coordinate: CoordinateDifference, exponent: i16 },\\n    logarithm: CoordinateDifference,\\n    green_kernel: struct { name: []const u8, coordinate: CoordinateDifference, left_derivatives: u8 = 0, right_derivatives: u8 = 0 },\\n    green_exponential: CoordinateDifference,\\n};\\n\\n/// CoordinateFactor stores a coordinate kernel with its rational derivative multiplier.\\npub const CoordinateFactor = struct {\\n    scalar: Rational = .{ .numerator = 1, .denominator = 1 },\\n    kernel: CoordinateKernel,\\n};\\nThe older coefficient shell is kept because higher layers already refer to a\\ncoefficient as a sum of tensor-weighted rational functions.  Its internals\\nremain private until the real simplifier exists.\\n/// A coefficient is a sum of rational functions multiplied by a tensor structure\\npub const Coeff = struct {\\n    summands: []const CoeffSummand,\\n};\\n\\nconst CoeffSummand = struct {\\n    rational_function: RationalFunction,\\n    tensor_structure: tensor.TensorStructure,\\n};\\n\\nA rational function can be written as a product of polynomial factors raised to a power.\\nconst RationalFunction = struct {\\n    factors: []const Factor,\\n};\\n\\nconst Factor = struct {\\n    base: Polynomial,\\n    exponent: i16,\\n};\\nA polynomial is a sum of monomial terms.\\nconst Polynomial = struct {\\n    terms: []const PolynomialTerm,\\n};\\n\\nconst PolynomialTerm = struct {\\n    monomial_coefficient: Scalar,\\n    monomial: Monomial,\\n};\\n\\nconst Scalar = f32;\\n\\nconst Monomial = struct {\\n    terms: []const MonomialTerm,\\n};\\n\\nconst MonomialTerm = struct {\\n   variable: Variable,\\n   power: u16,\\n};\\nVariables are internally assigned a numeric identifier.\\n/// Variables are internally assigned numeric identifiers\\npub const Variable = u32;\\n"},{"id":"20260605T075909.433647","title":"Tensor-Code","tags":null,"content":"This module handles finite-dimensional representation data for the symmetry\\nalgebras used by the other modules.  For a Lie algebra \\\\(\\\\mathfrak g\\\\), the\\ncentral problem is to construct bases of\\n\\\\[\\n  \\\\operatorname{Hom}_{\\\\mathfrak g}\\n  \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_k\\\\right),\\n\\\\]\\nusually with \\\\(T=\\\\mathbf 1\\\\).  These bases are Clebsch-Gordan data and\\ninvariant tensors.  Concrete index notation, such as gamma matrices, Young\\nsymmetrizers, metrics, epsilons, or structure constants, is a rendering layer\\nover this representation-theoretic basis.\\nThe public module exposes named declarations directly.  Source files such as\\nSymmetry and Tensor Kernel\\norganize implementation and prose, but clients should import this node and use\\nthe individual public declarations below.  The caller-facing object is Context:\\nit registers algebras, irreps, and realizations, then returns compact handles\\nfor invariant bases and rendered/evaluated output streams.\\nconst context = @import(\\"context.zig\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst kernel = @import(\\"kernel.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\n/// SimpleLieAlgebra stores the simple family and rank.\\npub const SimpleLieAlgebra = symmetry.SimpleLieAlgebra;\\n/// LieFamily names the supported simple Lie families.\\npub const LieFamily = symmetry.LieFamily;\\n/// QuantumNumbers is the tensor-product list of representation ids.\\npub const QuantumNumbers = symmetry.QuantumNumbers;\\n/// RepresentationId names a representation in a representation store.\\npub const RepresentationId = symmetry.RepresentationId;\\n/// TensorStructure stores simple built-in tensor structures.\\npub const TensorStructure = symmetry.TensorStructure;\\n\\n/// AlgebraHandle names a registered Lie algebra in a tensor context.\\npub const AlgebraHandle = store.AlgebraHandle;\\n/// IrrepHandle names an abstract irreducible representation.\\npub const IrrepHandle = store.IrrepHandle;\\n/// RealizationHandle names a concrete index realization of an irrep.\\npub const RealizationHandle = realization.RealizationHandle;\\n/// RealizationChannel selects one irrep copy inside a realization product.\\npub const RealizationChannel = realization.RealizationChannel;\\n/// BasisHandle names a generated invariant basis.\\npub const BasisHandle = coupling.BasisHandle;\\n/// InvariantHandle names one invariant inside a basis.\\npub const InvariantHandle = coupling.InvariantHandle;\\n/// Context is an opaque handle to tensor-code stores and caches.\\npub const Context = context.Context;\\n/// ContextOptions configures algebra and rendering defaults.\\npub const ContextOptions = context.ContextOptions;\\n/// AlgebraConventions selects configurable exact-algebra normalizations.\\npub const AlgebraConventions = root_data.AlgebraConventions;\\n/// AlgebraSpec describes a requested algebra before it is interned.\\npub const AlgebraSpec = store.AlgebraSpec;\\n/// IrrepSpec describes a requested representation before it is interned.\\npub const IrrepSpec = store.IrrepSpec;\\n/// ExternalLeg describes a public invariant leg with named free indices.\\npub const ExternalLeg = realization.ExternalLeg;\\n/// RealizationSpec describes a primitive or projected external index model.\\npub const RealizationSpec = realization.RealizationSpec;\\n/// InvariantBasisRequest is the CFT-facing request for invariant tensors.\\npub const InvariantBasisRequest = coupling.InvariantBasisRequest;\\n/// TensorExpansionTerm stores a CFT coefficient attached to one invariant id.\\npub const TensorExpansionTerm = coupling.TensorExpansionTerm;\\n/// RenderOptions controls streamed invariant rendering.\\npub const RenderOptions = rendering.RenderOptions;\\n/// EvalOptions controls component evaluation of one invariant.\\npub const EvalOptions = rendering.EvalOptions;\\n/// SymbolicTerm stores one streamed coefficient times symbolic atoms.\\npub const SymbolicTerm = rendering.SymbolicTerm;\\n/// SymbolicAtom stores one symbolic invariant contraction atom.\\npub const SymbolicAtom = rendering.SymbolicAtom;\\n\\n/// TensorExprId names a tensor expression stored by tensor-code.\\npub const TensorExprId = kernel.TensorExprId;\\nThis root block is the public tensor-code API for now.  The compatibility\\nexports QuantumNumbers, RepresentationId, and TensorStructure come from\\nSymmetry, while TensorExprId comes from\\nTensor Kernel.  They exist because CFT data\\nstructures currently store compact tensor and representation ids.\\nThe central workflow is:\\nregister an algebra with Context.registerAlgebra;\\nregister abstract irreps by Dynkin labels with Context.registerIrrep;\\noptionally register concrete index models with Context.registerRealization;\\nrequest a basis with Context.invariantBasis;\\nstream a concrete convention with Context.renderInvariant or\\n   Context.evaluateInvariant.\\nThe constructors are deliberately on the public data records, so callers do not\\nneed to import implementation helpers:\\nvar ctx = try tensor.Context.init(allocator);\\ndefer ctx.deinit();\\nconst algebra = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\nconst spinor = try ctx.registerIrrep(algebra, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\nconst leg = tensor.ExternalLeg.primitive(spinor, &.{\\n    .init(.spinor, \\"a\\"),\\n});\\nconst basis = try ctx.invariantBasis(.{\\n    .algebra = algebra,\\n    .external_legs = &.{leg},\\n});\\nConventions are configurable at context construction:\\nvar ctx = try tensor.Context.initWithOptions(allocator, .{\\n    .algebra_conventions = .{\\n        .quadratic_casimir_scale = .{ .numerator = 1, .denominator = 1 },\\n    },\\n    .default_signature = .complex,\\n});\\ndefer ctx.deinit();\\nHelper enums inside requests and render options are intentionally not root\\nexports; callers can use inferred literals such as .singlet, .auto,\\n.vector, and .complex.  Cache handles, decomposition handles, channel\\nhandles, stream sources, schema records, and backend capability tables stay\\nbehind this boundary until a caller needs them.\\nPublic API GlossaryLie algebra \\\\(\\\\mathfrak g\\\\): the symmetry algebra, such as \\\\(A_n\\\\), \\\\(D_n\\\\),\\n  or \\\\(E_8\\\\).  Tensor-code uses \\\\(\\\\mathfrak g\\\\) to decide which tensor\\n  structures are invariant.\\nRoot datum: the Cartan matrix, simple roots, coroots, and weight lattice\\n  conventions used internally for \\\\(\\\\mathfrak g\\\\).  CFT code should not handle\\n  this directly.\\nIrrep: an irreducible representation of \\\\(\\\\mathfrak g\\\\).  For simple Lie\\n  algebras this is identified by Dynkin labels, not by dimension.\\nDynkin label: the integer highest-weight label of an irrep.  It is the\\n  stable representation id used by the mathematics layer.\\nTensor product decomposition: the expansion\\n  \\\\[\\n    R\\\\otimes S = \\\\bigoplus_T m_T T\\n  \\\\]\\n  into irreps \\\\(T\\\\) with multiplicities \\\\(m_T\\\\).  Tensor-code caches this\\n  compactly; it does not expand component tensors during decomposition.\\nMultiplicity: the number of independent copies of the same irrep inside a\\n  product.  A dimension is never enough to distinguish these copies.\\nChannel: one copy of an irrep inside a tensor product.  If\\n  \\\\(R\\\\otimes S\\\\) contains two copies of \\\\(T\\\\), those two copies have the same\\n  irrep but different channel copy labels.\\nRealization: a concrete index model for an irrep.  A realization may be\\n  primitive, such as one spinor index, or constrained, such as a traceless or\\n  projected tensor product.\\nBackend: the internal family-specific capability table described in\\n  Tensor Backend.  It supplies decomposition,\\n  local projector, primitive invariant, and rendering routines for a Lie\\n  family without changing the public request flow.\\nPrimitive realization: a representation whose public indices are already\\n  the natural storage indices, such as a vector index, spinor index,\\n  fundamental index, or adjoint index.\\nConstrained realization: a representation embedded in a larger ambient\\n  tensor product, such as the \\\\(144\\\\subset 10\\\\otimes 16\\\\) gamma-traceless\\n  channel.  Tensor-code records the boundary projector once and then treats the\\n  leg as the irrep, not as the full ambient product at every internal step.\\nIndex: a caller-named free slot in a realization.  An index name is not a\\n  representation by itself.\\nExternal leg: one field/operator tensor slot supplied by a caller.  It has\\n  an irrep, a realization, and named free indices.\\nIntertwiner: a \\\\(\\\\mathfrak g\\\\)-equivariant map between representations.\\n  Clebsch-Gordan coefficients are component formulas for such maps.\\nInvariant: a basis vector in\\n  \\\\[\\n    \\\\operatorname{Hom}_{\\\\mathfrak g}\\n    \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_k\\\\right).\\n  \\\\]\\n  Usually \\\\(T=\\\\mathbf 1\\\\), so the invariant is a singlet tensor.\\nCoupling path: the compact internal description of one invariant as a tree\\n  of local Clebsch-Gordan maps.\\nProjector: an idempotent intertwiner selecting one channel or constrained\\n  realization from an ambient product.  Projectors may be rendered as named\\n  operators, block formulas, or fully expanded symbolic terms.\\nRenderer: the procedure that turns an invariant id into concrete notation,\\n  such as \\\\(\\\\delta\\\\)\'s, \\\\(\\\\epsilon\\\\)\'s, Young symmetrizers, gamma matrices, or\\n  structure constants.\\nSignature convention: the metric and Clifford-algebra convention used when\\n  rendering or evaluating components.  Complex, Euclidean, and Lorentzian\\n  signatures are conventions on the same abstract representation data.\\nTensor expansion: the object CFT code carries: rational-function ids times\\n  tensor invariant ids.  It does not require expanded tensor formulas.\\n"},{"id":"20260605T082209.767536","title":"Expressions","tags":null,"content":"const coefficient = @import(\\"coefficient.zig\\");\\nconst operators = @import(\\"operators.zig\\");\\nThe symbolic expressions we work with are simply Coefficients multiplied by local Operators.\\n/// A symbolic expression encountered in worldsheet calculations\\npub const Expr = struct {\\n    summands: []const ExprSummand\\n};\\n/// Each summand is a local operator with a coefficient\\npub const ExprSummand = struct {\\n    coeff: coefficient.Coeff,\\n    operators: operators.Operator\\n};\\n"},{"id":"20260605T090102.250727","title":"Theory","tags":null,"content":"A theory declaration says what it means to specify a CFT or BCFT before any\\nruntime correlator is evaluated.  This module is not the executor: it is the\\ncompact vocabulary of ids, sectors, operator families, conformal structures,\\npresentations, Wick and zero-mode rule names, boundary data, and local\\ncorrelator configs used by Correlators and OPE.  CFT Kernel\\nconsumes this data as comptime preset data.\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nIdentifier NamespaceMost theory objects are referenced by compact integer ids.  Keeping them in\\none namespace avoids a flat module full of unrelated public aliases while\\nstill making the table layout explicit.\\n/// Id groups compact handles used by CFT and BCFT presets.\\npub const Id = struct {\\n    const Sector = u16;\\n    const Strategy = u16;\\n    const StatisticsRule = u16;\\n    const SectorPresentation = u16;\\n    const ConformalStructure = u16;\\n    const QuantumRule = u16;\\n    const StrategySupportRule = u16;\\n    const WickRuleSet = u16;\\n    const Pairing = u16;\\n    const InversePairing = u16;\\n    const PairingData = u16;\\n    const ZeroModeRuleSet = u16;\\n    const CorrelatorConfig = u16;\\n    const LocalOpeConfig = u16;\\n    const CocycleTable = u16;\\n    /// FiniteProjection names finite projections used by public basis queries.\\n    pub const FiniteProjection = u16;\\n    const Lattice = u16;\\n    const LatticeVector = u32;\\n    /// ModeAlgebra names a mode algebra used by public descendant filters.\\n    pub const ModeAlgebra = u16;\\n    const ModeActionRule = u16;\\n    const LabelSchema = u16;\\n    /// LabelSpan names a compact slice of runtime body labels accepted by body builders.\\n    pub const LabelSpan = u32;\\n    const Extra = u32;\\n    const ConventionSet = u16;\\n    const WeightRule = u16;\\n    const CFT = u16;\\n    const LabelMap = u16;\\n    const BoundaryCondition = u16;\\n    const BoundaryStack = u16;\\n    const BoundarySpinData = u16;\\n    const ChanPatonMatrixUnit = u32;\\n    const Grading = u16;\\n};\\nBulk And Boundary SpecificationsThe bulk CFT spec owns only bulk data: Operators, sectors, quantum labels,\\npresentations, and rule tables.  A BCFT is a separate extension built from\\nbulk data plus boundary conditions, boundary stacks, boundary operator\\nfamilies, and Wick/zero-mode configs compiled for the boundary problem.\\n/// Spec groups bulk CFT and BCFT preset declarations.\\npub const Spec = struct {\\n    const Sector = struct {\\n        name: []const u8,\\n        tactic_preference: []const Id.Strategy,\\n        statistics_rule: Id.StatisticsRule,\\n        presentations: []const Id.SectorPresentation,\\n    };\\n\\n    /// CFT is the compile-time declaration of a bulk CFT preset.\\n    pub const CFT = struct {\\n        sectors: []const Sector,\\n        bulk_operator_kinds: []const operators.OperatorKind,\\n        conformal_structure: Id.ConformalStructure,\\n        conformal_structures: []const ConformalStructure,\\n        quantum_names: []const Label.QuantumName,\\n        label_schemas: []const Label.Schema,\\n        conventions: []const Rule.ConventionSet,\\n        weight_rules: []const Rule.Weight,\\n        quantum_rules: []const Rule.Quantum,\\n        statistics_rules: []const Rule.Statistics,\\n        strategy_support_rules: []const Rule.StrategySupport,\\n        wick_rule_sets: []const Rule.WickSet,\\n        pairing_data: []const Rule.PairingData,\\n        zero_mode_rule_sets: []const Rule.ZeroModeSet,\\n        correlator_configs: []const Runtime.CorrelatorConfig,\\n        local_ope_configs: []const Runtime.LocalOpeConfig,\\n        cocycle_tables: []const Rule.CocycleTable,\\n        presentations: []const Presentation.Sector,\\n        finite_projections: []const Rule.FiniteProjection,\\n        lattices: []const Presentation.Lattice,\\n        mode_algebras: []const Mode.Algebra,\\n        mode_action_rules: []const Mode.ActionRule,\\n    };\\n\\n    const ConformalStructure = struct {\\n        stress_tensor: Runtime.OperatorRef,\\n        susy: WorldsheetSusy,\\n        supercurrents: []const Runtime.OperatorRef,\\n        r_currents: []const Runtime.OperatorRef,\\n    };\\n\\n    const WorldsheetSusy = enum {\\n        none,\\n        n1_1,\\n        n2_2,\\n        n1_chiral,\\n        n2_chiral,\\n        custom,\\n    };\\n\\n    /// BCFT extends a bulk preset with boundary operator and Wick data.\\n    pub const BCFT = struct {\\n        name: []const u8,\\n        bulk: Id.CFT,\\n        boundary_conditions: []const Boundary.Condition,\\n        boundary_stacks: []const Boundary.Stack,\\n        boundary_stress_tensor: Runtime.OperatorRef,\\n        boundary_spin: []const Boundary.SpinData,\\n        boundary_operator_kinds: []const operators.OperatorKindId,\\n        boundary_changing_kinds: []const Boundary.ChangingKind,\\n        wick_rule_sets: []const Rule.WickSet,\\n        pairing_data: []const Rule.PairingData,\\n        zero_mode_rule_sets: []const Rule.ZeroModeSet,\\n        correlator_configs: []const Runtime.CorrelatorConfig,\\n        local_ope_configs: []const Runtime.LocalOpeConfig,\\n    };\\n};\\nBoundary NamespaceBoundary conditions are not part of the bulk CFT.  They enter only when a\\nBCFT extension is formed.  Stacks encode direct sums of boundary conditions;\\nChan-Paton multiplicity is label data on boundary bodies.\\n/// Boundary groups boundary-condition, stack, and Chan-Paton data.\\npub const Boundary = struct {\\n    const Condition = struct {\\n        name: []const u8,\\n        simple_data: Id.Extra,\\n    };\\n\\n    const Stack = struct {\\n        name: []const u8,\\n        entries: []const StackEntry,\\n    };\\n\\n    const StackEntry = struct {\\n        boundary: Id.BoundaryCondition,\\n        multiplicity: u32,\\n    };\\n\\n    /// ChangingKind identifies a boundary-local kind from one condition to another.\\n    pub const ChangingKind = struct {\\n        from: Id.BoundaryCondition,\\n        to: Id.BoundaryCondition,\\n        kind: operators.OperatorKindId,\\n    };\\n\\n    const SpinData = struct {\\n        id: Id.BoundarySpinData,\\n    };\\n\\n    const ChanPatonLabel = struct {\\n        endpoints: ChanPatonEndpoints,\\n        factor: ChanPatonFactor,\\n    };\\n\\n    const ChanPatonEndpoints = struct {\\n        from_stack: Id.BoundaryStack,\\n        to_stack: Id.BoundaryStack,\\n        from_entry: u32,\\n        to_entry: u32,\\n    };\\n\\n    const ChanPatonFactor = union(enum) {\\n        matrix_unit: ChanPatonMatrixUnit,\\n        tensor_factor: tensor.TensorExprId,\\n    };\\n\\n    const ChanPatonMatrixUnit = struct {\\n        row: u32,\\n        col: u32,\\n    };\\n};\\nLabels And RulesLabels carry runtime operator body data such as momenta, picture charge,\\nlattice vectors, derivative orders, and Chan-Paton endpoints.  Rule\\ndeclarations name client or preset procedures without storing their large\\npayloads here.\\n/// Label groups quantum-number and operator-label schema declarations.\\npub const Label = struct {\\n    const QuantumName = struct {\\n        name: []const u8,\\n    };\\n\\n    /// QuantumFilter constrains one named quantum-number value.\\n    pub const QuantumFilter = struct {\\n        name: u16,\\n        value: i64,\\n    };\\n\\n    const Schema = struct {\\n        name: []const u8,\\n        fields: []const Field,\\n    };\\n\\n    const Field = struct {\\n        name: []const u8,\\n        kind: FieldKind,\\n    };\\n\\n    const FieldKind = enum {\\n        integer,\\n        rational,\\n        lattice_vector,\\n        tensor_expr,\\n        opaque_data,\\n    };\\n};\\nconst Rule = struct {\\n    const ConventionSet = struct {\\n        name: []const u8,\\n    };\\n\\n    const Weight = struct {\\n        name: []const u8,\\n    };\\n\\n    const Quantum = struct {\\n        name: []const u8,\\n    };\\n\\n    const Statistics = struct {\\n        name: []const u8,\\n    };\\n\\n    const StrategySupport = struct {\\n        name: []const u8,\\n    };\\n\\n    const WickSet = struct {\\n        name: []const u8,\\n        conventions: Id.ConventionSet,\\n    };\\n\\n    const ZeroModeSet = struct {\\n        name: []const u8,\\n    };\\n\\n    const CocycleTable = struct {\\n        name: []const u8,\\n    };\\n\\n    const FiniteProjection = struct {\\n        name: []const u8,\\n    };\\n\\n    const PairingData = struct {\\n        sector: Id.Sector,\\n        pairing: Id.Pairing,\\n        inverse_pairing: Id.InversePairing,\\n    };\\n};\\nPresentations, Lattices, And ModesPresentations say how sectors are computed: direct fields, first-order\\nsystems, bosonized lattices, current algebras, or abstract conformal data.\\nLattices cover free bosons, RNS bosonized charges, SO(32) bosonization, and\\nE8xE8/Narain current-lattice presentations.\\nconst Presentation = struct {\\n    const Sector = struct {\\n        sectors: []const Id.Sector,\\n        name: []const u8,\\n        operator_map: []const Map,\\n        lattice: ?Id.Lattice,\\n        weight_rule: ?Id.WeightRule,\\n        wick_rules: ?Id.WickRuleSet,\\n    };\\n\\n    const Map = struct {\\n        from: operators.OperatorKindId,\\n        to: operators.OperatorKindId,\\n        label_map: Id.LabelMap,\\n    };\\n\\n    const Lattice = struct {\\n        name: []const u8,\\n        rank: u16,\\n        bilinear_form: Id.Extra,\\n        cocycles: ?Id.CocycleTable,\\n    };\\n};\\nModes are stored as operator-body data before evaluation.  This covers\\nfree-field modes, Virasoro descendants, beta-gamma modes, and Kac-Moody\\ncurrent modes for affine-module spectra generated by Basis-Generation.\\nconst Mode = struct {\\n    const Algebra = struct {\\n        name: []const u8,\\n        generators: []const Generator,\\n        action_rule: Id.ModeActionRule,\\n    };\\n\\n    const Generator = struct {\\n        name: []const u8,\\n        indices: []const IndexSlot,\\n        statistics: StatisticsData,\\n    };\\n\\n    const ActionRule = struct {\\n        name: []const u8,\\n    };\\n\\n    const IndexSlot = struct {\\n        representation: tensor.RepresentationId,\\n    };\\n\\n    const StatisticsData = struct {\\n        parity: u1,\\n    };\\n\\n    const Descendant = struct {\\n        algebra: Id.ModeAlgebra,\\n        generator: u16,\\n        mode_number: Runtime.Rational,\\n        target: operators.OperatorBodyId,\\n        labels: Id.LabelSpan,\\n    };\\n};\\nRuntime Config DataSurface and spin-structure declarations compile to small correlator configs.\\nThe runtime correlator receives only a pointer to such a config; OPE selects\\none of these local configs for sphere or disk computations.\\n/// Runtime groups small data passed to generic runtime procedures.\\npub const Runtime = struct {\\n    /// CorrelatorConfig selects the Wick and zero-mode rules for a correlator call.\\n    pub const CorrelatorConfig = struct {\\n        wick_rules: Id.WickRuleSet,\\n        zero_modes: Id.ZeroModeRuleSet,\\n    };\\n\\n    /// LocalOpeConfig selects the local correlator model used internally by OPE.\\n    pub const LocalOpeConfig = struct {\\n        domain: OperatorDomain,\\n        support: ChiralSupport,\\n        correlator_config: Id.CorrelatorConfig,\\n    };\\n\\n    /// OperatorDomain identifies whether an operator body is bulk or boundary data.\\n    pub const OperatorDomain = enum {\\n        bulk,\\n        boundary,\\n        boundary_changing,\\n    };\\n\\n    /// ChiralSupport records the chiral support of candidates and OPE targets.\\n    pub const ChiralSupport = enum {\\n        holomorphic,\\n        antiholomorphic,\\n        mixed,\\n        boundary,\\n    };\\n\\n    const OperatorRef = struct {\\n        kind: operators.OperatorKindId,\\n        labels: Id.LabelSpan,\\n    };\\n\\n    /// ConformalWeights stores holomorphic and antiholomorphic weights.\\n    pub const ConformalWeights = struct {\\n        h: ?Rational,\\n        hbar: ?Rational,\\n    };\\n\\n    /// LevelBound limits descendant level in basis or OPE generation.\\n    pub const LevelBound = struct {\\n        max: Rational,\\n    };\\n\\n    const Rational = struct {\\n        numerator: i64,\\n        denominator: i64,\\n    };\\n};\\n"},{"id":"20260606T063253.759714","title":"Basis-Generation","tags":null,"content":""},{"id":"20260606T064054.119217","title":"Abstract","tags":null,"content":""},{"id":"20260606T064107.303587","title":"Wick","tags":null,"content":"The Wick correlator is a generic driver.  It chooses primitive pair\\noperations and manages stack-bounded branch state; the tactic owns all\\nphysics.  The first implementation works on original input occurrences.  It\\ndoes not allocate an expression tree: a valid full branch is replayed directly\\nto the sink as primitive Wick terms followed by the zero-mode base case.\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\n\\nconst PairChoice = struct {\\n    left: u8,\\n    right: u8,\\n    term_index: u16,\\n    payload: u64 = 0,\\n};\\n\\nconst default_operator_count = 128;\\nconst no_candidate_index: u16 = 0xffff;\\n\\nconst PairCandidate = struct {\\n    left: u8,\\n    right: u8,\\n    term_index: u16,\\n    payload: u64,\\n    residuals: [2]u8,\\n    residual_count: u8,\\n    mandatory_before_base: bool,\\n    next_for_left: u16 = no_candidate_index,\\n    next_for_right: u16 = no_candidate_index,\\n};\\n\\nfn pairDepth(comptime operator_count: usize) usize {\\n    return operator_count / 2;\\n}\\n\\nfn pairCandidateCount(comptime operator_count: usize) usize {\\n    return (operator_count * (operator_count - 1)) / 2;\\n}\\n\\nfn tacticOperatorCount(comptime Tactic: type) comptime_int {\\n    return if (@hasDecl(Tactic, \\"traversal_operator_count\\")) Tactic.traversal_operator_count else default_operator_count;\\n}\\n\\nfn Scratch(comptime operator_count: usize) type {\\n    return struct {\\n        residual_indices: [operator_count]usize = undefined,\\n        pair_stack: [pairDepth(operator_count)]PairChoice = undefined,\\n        candidates: [pairCandidateCount(operator_count)]PairCandidate = undefined,\\n        first_candidate_by_operator: [operator_count]u16 = undefined,\\n        last_candidate_by_operator: [operator_count]u16 = undefined,\\n        first_candidate_by_left: [operator_count]u16 = undefined,\\n        end_candidate_by_left: [operator_count]u16 = undefined,\\n        zero_mode_survivable: [operator_count]bool = undefined,\\n        zero_mode_forced_mask: u128 = undefined,\\n        parities: [operator_count]bool = undefined,\\n        parity_mask: u128 = undefined,\\n\\n        pub fn init() @This() {\\n            return .{};\\n        }\\n    };\\n}\\nThe driver first builds a compact pair-candidate list.  Tactics may provide a\\ncheap count hook; otherwise a dry sink falls back to probing the pair emitter.\\nZero-mode viability follows the same pattern.  Valid branches are replayed\\nonce to the real sink, so invalid branches never leak prefix factors into the\\nstream.\\nconst NullSink = struct {\\n    /// emitWickTermStart discards a dry-run pair term boundary.\\n    pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n    /// emitWickScalar discards a dry-run scalar factor.\\n    pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n    /// emitWickCoordinate discards a dry-run coordinate factor.\\n    pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n    /// emitWickTensor discards a dry-run tensor factor.\\n    pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n    /// emitWickAction discards a dry-run action factor.\\n    pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n    /// emitWickTermEnd discards a dry-run pair term end.\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n    /// emitZeroModeFactor discards a dry-run zero-mode factor.\\n    pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n    /// emitZeroModeBaseEnd discards a dry-run zero-mode success marker.\\n    pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n};\\n\\nfn bit(index: usize) u128 {\\n    return @as(u128, 1) << @intCast(index);\\n}\\n\\nfn initialLiveMask(comptime operator_count: usize, len: usize) u128 {\\n    if (len == operator_count) return ~@as(u128, 0);\\n    return bit(len) - 1;\\n}\\n\\nfn live(mask: u128, index: usize) bool {\\n    return (mask & bit(index)) != 0;\\n}\\n\\nfn firstLiveIndex(mask: u128) usize {\\n    const low: u64 = @truncate(mask);\\n    if (low != 0) return @ctz(low);\\n    const high: u64 = @truncate(mask >> 64);\\n    return 64 + @ctz(high);\\n}\\n\\nfn residualSlice(scratch: anytype, len: usize, live_mask: u128) []const usize {\\n    var count: usize = 0;\\n    var index: usize = 0;\\n    while (index < len) : (index += 1) {\\n        if (live(live_mask, index)) {\\n            scratch.residual_indices[count] = index;\\n            count += 1;\\n        }\\n    }\\n    return scratch.residual_indices[0..count];\\n}\\n\\nfn pairTermCount(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize) !usize {\\n    if (@hasDecl(Tactic, \\"pairTermCount\\")) {\\n        return Tactic.pairTermCount(config_ptr, ops, left, right);\\n    }\\n    var null_sink = NullSink{};\\n    return Tactic.emitPairTerms(config_ptr, ops, left, right, &null_sink);\\n}\\n\\nfn pairTermResiduals(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize, residuals: *[2]usize) !usize {\\n    if (@hasDecl(Tactic, \\"pairTermResiduals\\")) {\\n        return Tactic.pairTermResiduals(config_ptr, ops, left, right, term_index, residuals);\\n    } else {\\n        return 0;\\n    }\\n}\\n\\nfn pairTermPayload(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize) !u64 {\\n    if (@hasDecl(Tactic, \\"pairTermPayload\\")) {\\n        return Tactic.pairTermPayload(config_ptr, ops, left, right, term_index);\\n    }\\n    return @intCast(term_index);\\n}\\n\\nfn pairTermInfo(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n    if (@hasDecl(Tactic, \\"pairTermInfo\\")) {\\n        return Tactic.pairTermInfo(config_ptr, ops, left, right, term_index, residuals, payload, residual_count);\\n    }\\n    payload.* = try pairTermPayload(Tactic, config_ptr, ops, left, right, term_index);\\n    residual_count.* = try pairTermResiduals(Tactic, config_ptr, ops, left, right, term_index, residuals);\\n}\\n\\nfn residualsKeepPair(left: usize, right: usize, residuals: []const usize) bool {\\n    var keeps_left = false;\\n    var keeps_right = false;\\n    for (residuals) |residual| {\\n        keeps_left = keeps_left or residual == left;\\n        keeps_right = keeps_right or residual == right;\\n    }\\n    return keeps_left and keeps_right;\\n}\\n\\nfn appendCandidateForOperator(scratch: anytype, operator_index: usize, candidate_index: u16) void {\\n    const last = scratch.last_candidate_by_operator[operator_index];\\n    if (last == no_candidate_index) {\\n        scratch.first_candidate_by_operator[operator_index] = candidate_index;\\n    } else if (scratch.candidates[last].left == operator_index) {\\n        scratch.candidates[last].next_for_left = candidate_index;\\n    } else {\\n        scratch.candidates[last].next_for_right = candidate_index;\\n    }\\n    scratch.last_candidate_by_operator[operator_index] = candidate_index;\\n}\\n\\nfn nextCandidateForOperator(candidate: PairCandidate, operator_index: usize) u16 {\\n    return if (candidate.left == operator_index) candidate.next_for_left else candidate.next_for_right;\\n}\\n\\nfn buildCandidates(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) !usize {\\n    var operator_index: usize = 0;\\n    while (operator_index < ops.operators.len) : (operator_index += 1) {\\n        scratch.first_candidate_by_operator[operator_index] = no_candidate_index;\\n        scratch.last_candidate_by_operator[operator_index] = no_candidate_index;\\n        scratch.first_candidate_by_left[operator_index] = no_candidate_index;\\n        scratch.end_candidate_by_left[operator_index] = no_candidate_index;\\n    }\\n\\n    var candidate_count: usize = 0;\\n    var left: usize = 0;\\n    while (left < ops.operators.len) : (left += 1) {\\n        const left_start = candidate_count;\\n        var right = left + 1;\\n        while (right < ops.operators.len) : (right += 1) {\\n            if (normal_ordering.same(ops.operators[left], ops.operators[right])) continue;\\n            const term_count = try pairTermCount(Tactic, config_ptr, ops, left, right);\\n            var term_index: usize = 0;\\n            while (term_index < term_count) : (term_index += 1) {\\n                if (candidate_count == scratch.candidates.len) return error.TooManyWickCandidates;\\n                var residuals: [2]usize = undefined;\\n                var payload: u64 = undefined;\\n                var residual_count: usize = undefined;\\n                try pairTermInfo(Tactic, config_ptr, ops, left, right, term_index, &residuals, &payload, &residual_count);\\n                if (residual_count > residuals.len) return error.TooManyWickTermResiduals;\\n                var compact_residuals: [2]u8 = undefined;\\n                var residual_index: usize = 0;\\n                while (residual_index < residual_count) : (residual_index += 1) {\\n                    compact_residuals[residual_index] = @intCast(residuals[residual_index]);\\n                }\\n                const compact_candidate_index: u16 = @intCast(candidate_count);\\n                scratch.candidates[candidate_count] = .{\\n                    .left = @intCast(left),\\n                    .right = @intCast(right),\\n                    .term_index = @intCast(term_index),\\n                    .payload = payload,\\n                    .residuals = compact_residuals,\\n                    .residual_count = @intCast(residual_count),\\n                    .mandatory_before_base = residualsKeepPair(left, right, residuals[0..residual_count]),\\n                };\\n                appendCandidateForOperator(scratch, left, compact_candidate_index);\\n                appendCandidateForOperator(scratch, right, compact_candidate_index);\\n                candidate_count += 1;\\n            }\\n        }\\n        if (candidate_count != left_start) {\\n            scratch.first_candidate_by_left[left] = @intCast(left_start);\\n            scratch.end_candidate_by_left[left] = @intCast(candidate_count);\\n        }\\n    }\\n    return candidate_count;\\n}\\n\\nfn buildParities(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) void {\\n    scratch.parity_mask = 0;\\n    var index: usize = 0;\\n    while (index < ops.operators.len) : (index += 1) {\\n        const parity = if (@hasDecl(Tactic, \\"operatorParity\\")) Tactic.operatorParity(config_ptr, ops, index) else false;\\n        scratch.parities[index] = parity;\\n        if (parity) scratch.parity_mask |= bit(index);\\n    }\\n}\\n\\nfn buildZeroModeSurvivability(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) void {\\n    scratch.zero_mode_forced_mask = 0;\\n    var index: usize = 0;\\n    while (index < ops.operators.len) : (index += 1) {\\n        const survivable = operatorCanRemainInZeroMode(Tactic, config_ptr, ops, index);\\n        scratch.zero_mode_survivable[index] = survivable;\\n        if (!survivable) scratch.zero_mode_forced_mask |= bit(index);\\n    }\\n}\\n\\nfn pairSign(scratch: anytype, live_mask: u128, left: usize, right: usize) i8 {\\n    const endpoints = bit(left) | bit(right);\\n    if ((scratch.parity_mask & endpoints) != endpoints) return 1;\\n    const below_right = bit(right) - 1;\\n    const below_or_at_left = bit(left + 1) - 1;\\n    const between = below_right & ~below_or_at_left;\\n    const crossings = @popCount(live_mask & scratch.parity_mask & between);\\n    return if ((crossings & 1) == 0) 1 else -1;\\n}\\n\\nfn operatorCanRemainInZeroMode(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n    if (@hasDecl(Tactic, \\"operatorCanRemainInZeroMode\\")) {\\n        return Tactic.operatorCanRemainInZeroMode(config_ptr, ops, index);\\n    }\\n    return true;\\n}\\n\\nfn firstForcedResidual(scratch: anytype, live_mask: u128) ?usize {\\n    const forced = live_mask & scratch.zero_mode_forced_mask;\\n    return if (forced == 0) null else firstLiveIndex(forced);\\n}\\n\\nfn zeroModeSucceeds(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, residual: []const usize) !bool {\\n    if (@hasDecl(Tactic, \\"zeroModeBaseCaseSucceeds\\")) {\\n        return Tactic.zeroModeBaseCaseSucceeds(config_ptr, ops, residual);\\n    }\\n    var null_sink = NullSink{};\\n    return Tactic.emitZeroModeBaseCase(config_ptr, ops, residual, &null_sink);\\n}\\nAt a zero-mode base case, the branch stack is replayed before the base case so\\nthe sink receives one fully expanded streamed term.  Pair factors are not kept\\nin memory.\\nfn emitPairTerm(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, choice: PairChoice, sink: anytype) !void {\\n    if (@hasDecl(Tactic, \\"emitPairTermPayload\\")) {\\n        try Tactic.emitPairTermPayload(config_ptr, ops, choice.left, choice.right, choice.payload, sink);\\n        return;\\n    }\\n    if (@hasDecl(Tactic, \\"emitPairTerm\\")) {\\n        try Tactic.emitPairTerm(config_ptr, ops, choice.left, choice.right, choice.term_index, sink);\\n        return;\\n    }\\n    if (choice.term_index != 0) return error.InvalidWickTerm;\\n    if (try Tactic.emitPairTerms(config_ptr, ops, choice.left, choice.right, sink) == 0) {\\n        return error.InvalidWickBranch;\\n    }\\n}\\n\\nfn emitBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype, depth: usize, sign: i8, residual: []const usize, sink: anytype) !void {\\n    if (sign < 0 and @hasDecl(Tactic, \\"emitBranchSign\\")) {\\n        try Tactic.emitBranchSign(config_ptr, sign, sink);\\n    }\\n\\n    var index: usize = 0;\\n    while (index < depth) : (index += 1) {\\n        try emitPairTerm(Tactic, config_ptr, ops, scratch.pair_stack[index], sink);\\n    }\\n\\n    if (!try Tactic.emitZeroModeBaseCase(config_ptr, ops, residual, sink)) {\\n        return error.InvalidWickBranch;\\n    }\\n}\\n\\nfn pairResultLiveMask(live_mask: u128, candidate: PairCandidate) u128 {\\n    var next = live_mask & ~bit(candidate.left) & ~bit(candidate.right);\\n    var residual_index: usize = 0;\\n    while (residual_index < candidate.residual_count) : (residual_index += 1) {\\n        next |= bit(candidate.residuals[residual_index]);\\n    }\\n    return next;\\n}\\n\\nfn pairClearedLiveMask(live_mask: u128, candidate: PairCandidate) u128 {\\n    return live_mask & ~bit(candidate.left) & ~bit(candidate.right);\\n}\\n\\nfn hasMandatoryCandidate(scratch: anytype, live_mask: u128, next_candidate_index: usize, candidate_count: usize) bool {\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        if (!candidate.mandatory_before_base) continue;\\n        if (live(live_mask, candidate.left) and live(live_mask, candidate.right)) return true;\\n    }\\n    return false;\\n}\\n\\nfn pureFullContraction(scratch: anytype, ops: kernel.Call.MultiOp, candidate_count: usize) bool {\\n    if ((ops.operators.len & 1) != 0) return false;\\n\\n    var operator_index: usize = 0;\\n    while (operator_index < ops.operators.len) : (operator_index += 1) {\\n        if (scratch.zero_mode_survivable[operator_index]) return false;\\n    }\\n\\n    var candidate_index: usize = 0;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        if (scratch.candidates[candidate_index].residual_count != 0) return false;\\n    }\\n\\n    return true;\\n}\\n\\nfn pushBranchPairTerm(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, candidate: PairCandidate) !void {\\n    try Tactic.pushBranchPairTerm(config_ptr, ops, candidate.left, candidate.right, candidate.payload, branch);\\n}\\n\\nfn emitAccumulatedBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, sign: i8, residual: []const usize, sink: anytype) !void {\\n    try Tactic.emitAccumulatedBranch(config_ptr, ops, branch, sign, residual, sink);\\n}\\n\\nfn emitFullAccumulatedBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, sign: i8, sink: anytype) !void {\\n    if (@hasDecl(Tactic, \\"emitFullContractionBranch\\")) {\\n        try Tactic.emitFullContractionBranch(config_ptr, ops, branch, sign, sink);\\n        return;\\n    }\\n    try Tactic.emitAccumulatedBranch(config_ptr, ops, branch, sign, &.{}, sink);\\n}\\n\\nfn walkFullAccumulated(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    branch: *Tactic.BranchState,\\n    live_mask: u128,\\n    depth: usize,\\n    sign: i8,\\n    sink: anytype,\\n) !usize {\\n    if (live_mask == 0) {\\n        try emitFullAccumulatedBranch(Tactic, config_ptr, ops, branch, sign, sink);\\n        return 1;\\n    }\\n    if (depth == scratch.pair_stack.len) return 0;\\n\\n    const first = firstLiveIndex(live_mask);\\n\\n    var branch_count: usize = 0;\\n    var candidate_index: usize = scratch.first_candidate_by_left[first];\\n    const candidate_end: usize = scratch.end_candidate_by_left[first];\\n    while (candidate_index < candidate_end) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, right)) continue;\\n\\n        const saved = branch.snapshot();\\n        const next_mask = pairClearedLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n        branch_count += try walkFullAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, depth + 1, next_sign, sink);\\n        branch.restore(saved);\\n    }\\n\\n    return branch_count;\\n}\\n\\nfn walkAccumulated(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    branch: *Tactic.BranchState,\\n    live_mask: u128,\\n    next_candidate_index: usize,\\n    candidate_count: usize,\\n    depth: usize,\\n    sign: i8,\\n    sink: anytype,\\n) !usize {\\n    var branch_count: usize = 0;\\n    const forced_residual = firstForcedResidual(scratch, live_mask);\\n\\n    if (forced_residual == null and !hasMandatoryCandidate(scratch, live_mask, next_candidate_index, candidate_count)) {\\n        const residual = residualSlice(scratch, ops.operators.len, live_mask);\\n        if (try zeroModeSucceeds(Tactic, config_ptr, ops, residual)) {\\n            try emitAccumulatedBranch(Tactic, config_ptr, ops, branch, sign, residual, sink);\\n            branch_count += 1;\\n        }\\n    }\\n\\n    if (depth == scratch.pair_stack.len) return branch_count;\\n\\n    if (forced_residual) |forced| {\\n        var candidate_link = scratch.first_candidate_by_operator[forced];\\n        while (candidate_link != no_candidate_index) {\\n            const candidate_index: usize = candidate_link;\\n            const candidate = scratch.candidates[candidate_index];\\n            candidate_link = nextCandidateForOperator(candidate, forced);\\n            if (candidate_index < next_candidate_index) continue;\\n            const left: usize = candidate.left;\\n            const right: usize = candidate.right;\\n            if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n            const saved = branch.snapshot();\\n            const next_mask = pairResultLiveMask(live_mask, candidate);\\n            const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n            try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n            branch_count += try walkAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, sink);\\n            branch.restore(saved);\\n            if (candidate.mandatory_before_base) return branch_count;\\n        }\\n        return branch_count;\\n    }\\n\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n        const saved = branch.snapshot();\\n        const next_mask = pairResultLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n        branch_count += try walkAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, sink);\\n        branch.restore(saved);\\n        if (candidate.mandatory_before_base) return branch_count;\\n    }\\n\\n    return branch_count;\\n}\\n\\nThe recursion enumerates pair subsets in lexicographic order, which prevents\\nthe same disjoint pairing set from being reached in multiple orders.  At each\\nnode it first tries the zero-mode base case, then extends the branch by one\\nallowed pair.\\nfn walk(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    live_mask: u128,\\n    next_candidate_index: usize,\\n    candidate_count: usize,\\n    depth: usize,\\n    sign: i8,\\n    emit: bool,\\n    sink: anytype,\\n) !usize {\\n    var branch_count: usize = 0;\\n    const forced_residual = firstForcedResidual(scratch, live_mask);\\n\\n    if (forced_residual == null and !hasMandatoryCandidate(scratch, live_mask, next_candidate_index, candidate_count)) {\\n        const residual = residualSlice(scratch, ops.operators.len, live_mask);\\n        if (try zeroModeSucceeds(Tactic, config_ptr, ops, residual)) {\\n            if (emit) try emitBranch(Tactic, config_ptr, ops, scratch, depth, sign, residual, sink);\\n            branch_count += 1;\\n        }\\n    }\\n\\n    if (depth == scratch.pair_stack.len) return branch_count;\\n\\n    if (forced_residual) |forced| {\\n        var candidate_link = scratch.first_candidate_by_operator[forced];\\n        while (candidate_link != no_candidate_index) {\\n            const candidate_index: usize = candidate_link;\\n            const candidate = scratch.candidates[candidate_index];\\n            candidate_link = nextCandidateForOperator(candidate, forced);\\n            if (candidate_index < next_candidate_index) continue;\\n            const left: usize = candidate.left;\\n            const right: usize = candidate.right;\\n            if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n            const next_mask = pairResultLiveMask(live_mask, candidate);\\n            const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n            if (!emit) {\\n                branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, false, sink);\\n                if (candidate.mandatory_before_base) return branch_count;\\n                continue;\\n            }\\n\\n            scratch.pair_stack[depth] = .{ .left = candidate.left, .right = candidate.right, .term_index = candidate.term_index, .payload = candidate.payload };\\n            branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, true, sink);\\n            if (candidate.mandatory_before_base) return branch_count;\\n        }\\n        return branch_count;\\n    }\\n\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n        const next_mask = pairResultLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        if (!emit) {\\n            branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, false, sink);\\n            if (candidate.mandatory_before_base) return branch_count;\\n            continue;\\n        }\\n\\n        scratch.pair_stack[depth] = .{ .left = candidate.left, .right = candidate.right, .term_index = candidate.term_index, .payload = candidate.payload };\\n        branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, true, sink);\\n        if (candidate.mandatory_before_base) return branch_count;\\n    }\\n\\n    return branch_count;\\n}\\nThe public entry point is specialized by a comptime tactic.  A tactic provides\\nthe config type, primitive pair emitter, and zero-mode base-case emitter.\\n/// wickCorrelator streams all Wick branches accepted by the tactic base case.\\npub fn wickCorrelator(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    sink: anytype,\\n) !void {\\n    const max_operator_count = tacticOperatorCount(Tactic);\\n    var scratch_storage = Scratch(max_operator_count).init();\\n    const scratch = &scratch_storage;\\n    if (ops.operators.len > max_operator_count) return error.TooManyWickOperators;\\n    const candidate_count = try buildCandidates(Tactic, config_ptr, ops, scratch);\\n    buildParities(Tactic, config_ptr, ops, scratch);\\n    buildZeroModeSurvivability(Tactic, config_ptr, ops, scratch);\\n    if (@hasDecl(Tactic, \\"BranchState\\") and @hasDecl(Tactic, \\"pushBranchPairTerm\\") and @hasDecl(Tactic, \\"emitAccumulatedBranch\\")) {\\n        var branch = Tactic.BranchState.init();\\n        if (pureFullContraction(scratch, ops, candidate_count)) {\\n            _ = try walkFullAccumulated(Tactic, config_ptr, ops, scratch, &branch, initialLiveMask(max_operator_count, ops.operators.len), 0, 1, sink);\\n            return;\\n        }\\n        _ = try walkAccumulated(Tactic, config_ptr, ops, scratch, &branch, initialLiveMask(max_operator_count, ops.operators.len), 0, candidate_count, 0, 1, sink);\\n        return;\\n    }\\n    _ = try walk(Tactic, config_ptr, ops, scratch, initialLiveMask(max_operator_count, ops.operators.len), 0, candidate_count, 0, 1, true, sink);\\n}\\n"},{"id":"20260606T064118.058500","title":"Bosonization","tags":null,"content":""},{"id":"20260606T085016.945165","title":"Symmetry","tags":null,"content":"A symmetry is labeled by its name and a Lie Algebra, which can be u(1) or a simple Lie algebra.\\n/// Symmetry labels a named symmetry algebra.\\nconst Symmetry = struct {\\n    name: []const u8,\\n    algebra: LieAlgebra,\\n};\\n\\n/// LieAlgebra distinguishes U(1) from simple Lie algebras.\\nconst LieAlgebra = union(enum) {\\n    u1,\\n    simple: SimpleLieAlgebra,\\n};\\n\\n/// SimpleLieAlgebra stores the simple family and rank.\\npub const SimpleLieAlgebra = struct {\\n    family: LieFamily,\\n    rank: u8,\\n};\\n\\n/// LieFamily names the supported simple Lie families.\\npub const LieFamily = enum {\\n    a,\\n    b,\\n    c,\\n    d,\\n    e6,\\n    e7,\\n    e8,\\n    f4,\\n    g2,\\n};\\n\\nA quantum number is labeled by the symmetry algebra and its representation.\\n/// QuantumNumbers is the tensor-product list of representation ids.\\npub const QuantumNumbers = []const RepresentationId;\\n\\n/// SymmetryId names a symmetry in a symmetry store.\\npub const SymmetryId = u8;\\n/// RepresentationId names a representation in a representation store.\\npub const RepresentationId = u8;\\n\\n/// Representation stores a symmetry id and its label.\\nconst Representation = struct {\\n    symmetry: SymmetryId,\\n    label: RepresentationLabel,\\n};\\n\\n/// RepresentationLabel stores either a rational charge or Dynkin labels.\\nconst RepresentationLabel = union(enum) {\\n    u1_charge: U1ChargeRational,\\n    dynkin: []const i8,\\n};\\n\\n/// U1ChargeRational stores a rational U(1) charge.\\npub const U1ChargeRational = struct {\\n    numerator: i8,\\n    denominator: i8,\\n};\\nAn abstract tensor index carries a label and a particular representation.\\n/// TensorIndex stores a representation id and concrete index label.\\nconst TensorIndex = struct {\\n    representation: RepresentationId,\\n    label: u32,\\n};\\n\\n/// TensorIndexId names a tensor index in a tensor expression.\\npub const TensorIndexId = u32;\\nAn example of a concrete tensor structures - should only appear for SO-type groups, todo.\\n/// TensorStructure stores simple built-in tensor structures.\\npub const TensorStructure = union(enum) {\\n    none,\\n    kronecker_delta: struct {\\n        left: TensorIndexId,\\n        right: TensorIndexId,\\n    },\\n};\\n"},{"id":"20260606T085254.829431","title":"Clebsch-Gordan","tags":null,"content":"This node is the handoff for implementing the tensor-code backend that will\\neventually generate Clebsch-Gordan data and invariant tensor bases.  The first\\nbackend milestone is not explicit CG coefficients.  It is a reliable,\\ngeneric, cached decomposition engine:\\n\\\\[\\n  R\\\\otimes S = \\\\bigoplus_T m_T T.\\n\\\\]\\nThe CG generator will consume these decompositions to enumerate coupling paths\\nto a target representation, usually the singlet \\\\(\\\\mathbf 1\\\\).\\nGoalsThe implementation must follow AGENTS.md:\\nwrite simple explicit procedural Zig;\\ndesign lean data structures first;\\navoid large intermediate symbolic expressions;\\nkeep data flows parallelizable;\\nexpose the bare minimum public API;\\nkeep the CFT-facing API in Tensor-Code;\\nkeep internal backend/decomposition/projector details out of the public root\\n  unless a caller actually needs them;\\ndocument the implementation in org-roam nodes and keep prose concise.\\nThe mathematical and product requirements are:\\nsupport \\\\(A_n\\\\) for \\\\(SU(N)\\\\);\\nsupport \\\\(B_n,D_n\\\\) for \\\\(SO(N)\\\\) and \\\\(Spin(N)\\\\);\\nsupport \\\\(E_6,E_7,E_8\\\\), with \\\\(E_8\\\\) a key target;\\ndo not overfocus the architecture on \\\\(SO(10)\\\\), even though \\\\(Spin(10)\\\\)\\n  is the main stress test;\\nidentify irreps by Dynkin labels, not dimensions;\\ntreat multiplicities as first-class channel copies;\\ndistinguish abstract irreps from concrete index realizations;\\nsupport constrained realizations such as \\\\(144\\\\subset 10\\\\otimes16\\\\) without\\n  carrying the full ambient tensor product through every internal step;\\nrepresent large invariant bases as compact coupling paths, not dense tensor\\n  components;\\nrender explicit gamma/projector formulas only on demand and by streaming\\n  terms to a sink;\\nmake every projector appearing in a returned result fully expandable.\\nThe goals are concrete:\\nAbstract representation theory must be correct first.  Irreps are Dynkin\\n   labels; decompositions have explicit multiplicities; channels are not\\n   identified by dimension.\\nLarge invariant bases must be stored as compact coupling paths.  A basis\\n   element is a recipe: external legs, intermediate channels, and local\\n   projector/CG kernel ids.\\nProjectors in returned results must not be opaque tokens.  A projector id\\n   may be the compact storage form, but it must carry enough derivation data to\\n   expand into concrete symbolic terms later.\\nExpansion must be streamed.  Fully expandable does not mean eagerly\\n   expanded.  It means the renderer can enumerate the terms without inventing\\n   missing identities or asking the user for hidden Fierz relations.\\nThe runnable target is not merely obtaining \\\\(73744\\\\) abstract basis\\n   expressions.  The runnable target is also being able to expand each selected\\n   basis element through a caller-supplied filter, and for the resulting stream\\n   to be consumed at high throughput by CFT/correlator code.\\nFilters are part of the execution model.  They may discard terms by index\\n   support, operator kind, realization leg, component assignment, symmetry,\\n   coefficient class, or target downstream contraction.  The renderer must\\n   apply filters while streaming, not after materializing the full expansion.\\n   Partial filtering must be sound: a partial product may be rejected only\\n   when no completion can survive.  Otherwise the filter must return\\n   undecided and the expansion continues.\\nCoupling-tree policy is part of the basis convention.  Build one canonical\\n   tree first, and certify independence for that tree.  Multiple trees are\\n   different bases unless a basis-change or reduction map is explicitly\\n   constructed.\\nFierz and projector identities must be derived from the backend\\n   construction: channel decomposition, idempotent splitting, highest-weight\\n   solvers, Casimir separation where valid, and explicit residual linear\\n   algebra only when necessary.  They are not user-supplied tables.\\nCompute estimates must include projector expansion cost.  Counting coupling\\n   paths alone is insufficient if each path contains a projector whose\\n   expansion is large.\\nThe motivating stress target remains:\\n\\\\[\\n  \\\\dim\\\\operatorname{Hom}_{Spin(10)}(\\\\mathbf 1,16^{12}) = 73744.\\n\\\\]\\nThose \\\\(73744\\\\) objects should be records describing coupling paths and local\\nprojector ids.  They should not be expanded into component tensors during basis\\nenumeration.  However, every projector id in those paths must be traceable to\\na stored expansion procedure.  When the caller asks for concrete notation,\\nrendering should stream terms built from metrics, epsilons, named operators,\\ngamma-like backend operators, or projector blocks.  The stream must be\\nfilterable as it is produced.  A successful implementation is one where a\\ndownstream consumer can iterate through selected expanded terms without waiting\\nfor the full expansion of all \\\\(73744\\\\) invariants to be allocated.\\nNon-SolutionsThe following are explicitly not acceptable:\\nReturning only abstract representation-theory counts.  Counts are useful for\\n  feasibility, but CFT code needs actual invariant handles and eventually\\n  streamable tensor expressions.\\nReturning opaque named projectors with no derivation recipe.  A compact\\n  projector id is acceptable only if the backend can expand it.\\nReturning compact coupling paths with no efficient expansion path.  A list\\n  of \\\\(73744\\\\) abstract expressions is not enough.\\nExpanding all \\\\(73744\\\\) basis elements into memory before filtering.  Filters\\n  must be pushed into the streaming expansion loop.\\nTreating rendering as a slow debug printer.  Rendering is part of the\\n  runnable computational path and must feed downstream correlator code at a\\n  fast pace.\\nTreating \\\\(144\\\\subset10\\\\otimes16\\\\) as \\\\(10\\\\otimes16\\\\) throughout all\\n  internal coupling steps and projecting only at the very end.  The realization\\n  projector belongs at the boundary.\\nRelying on hand-entered Fierz identities.  Identities must follow from the\\n  algebraic construction and verified projector/idempotent relations.\\nBuilding dense tensors or dense component spaces such as \\\\(16^{12}\\\\).\\nDesigning an \\\\(SO(10)\\\\)-only system.  \\\\(Spin(10)\\\\) is a stress test, not the\\n  architecture.\\nMaking gamma matrices part of the generic public ADE API.  Gamma-like\\n  objects are backend-rendered named operators or internal SO/Spin atoms.\\nOptimizing by hiding mathematical ambiguity.  Multiplicity copies,\\n  realization channels, basis conventions, and signature/rendering conventions\\n  must be explicit.\\nRejecting a partial expanded term because the current prefix does not yet\\n  match a filter.  Prefix filters must distinguish reject from undecided.\\nConcatenating paths from several coupling trees and calling the union a\\n  larger independent basis.  This is allowed only after constructing and\\n  applying a basis-change or reduction map.\\nWeasel Modes And Required EvidenceAn implementation is not accepted because it can print plausible data.  It\\nmust produce the required records and expose enough evidence that the records\\nare real.  The following failure modes are common and must be rejected during\\nreview:\\nClaiming a \\"fast structure generator\\" that only returns the count of\\n  invariants.  Required evidence: stored basis handles with inspectable path\\n  records, one record per basis element.\\nReturning a list of abstract labels without local channel data.  Required\\n  evidence: each path stores the coupling tree, intermediate channel handles,\\n  multiplicity-copy ids, and local projector/CG kernel ids.\\nReturning projectors as names such as P_144 with no expansion route.\\n  Required evidence: every projector id reports its derivation kind and an\\n  expansion source, or returns a typed NotExpandable error before it appears\\n  in a returned basis.\\nUsing an overcomplete gamma-monomial list and calling it a basis.  Required\\n  evidence: basis construction happens in representation/multiplicity space\\n  before rendering; gamma terms are a rendering of already-independent basis\\n  elements.\\nRunning a dense component-space linear solve and hiding the cost behind a\\n  small output.  Required evidence: logs/counters for maximum allocated weight\\n  multiset size, path count, projector count, and no allocations proportional\\n  to \\\\(16^{12}\\\\) or another full tensor component space.\\nFiltering after materializing all expanded terms.  Required evidence:\\n  filters are called during expansion, and a reject-all filter allocates no\\n  large output buffer.\\nFailing to distinguish multiplicity copies.  Required evidence:\\n  decomposition terms include multiplicity, and local kernels/projectors\\n  carry the selected copy.\\nTreating projected realizations as ambient products until the final output.\\n  Required evidence: external legs using constrained realizations carry a\\n  boundary projector once, and internal coupling uses the target irrep.\\nClaiming independence from successful numerical spot checks only.  Required\\n  evidence: independence follows from the decomposition path construction, and\\n  numerical/rendered checks are secondary audits.\\nHardcoding known decompositions for the acceptance tests.  Required\\n  evidence: tests include at least one generated decomposition not present as a\\n  literal special case, and the backend exposes counters showing which generic\\n  procedure produced it.\\nEvery major stage must produce a compact audit record:\\nalgebra and convention ids;\\ninput irreps;\\nnumber of weights generated;\\ntotal weight multiplicity;\\nproduct multiset size;\\ndecomposition channel count;\\nsum of channel dimensions times multiplicities;\\nnumber of basis paths;\\nnumber of distinct projector ids;\\nmaximum known projector expansion size, if available;\\nfilter accept/reject/undecided counts during streamed expansion.\\nThese audits are mandatory internal milestone evidence, not optional debug\\noutput.  They are not user-facing API by default, but each backend milestone\\nmust produce enough audit data to prove that it is doing the promised\\ncomputation.\\nIndependence By ConstructionThe basis must be independent before rendering.  The construction should be:\\nFor each binary product \\\\(R\\\\otimes S\\\\), decompose into irreps:\\n   \\\\[\\n     R\\\\otimes S=\\\\bigoplus_T M_{RS}^T\\\\otimes T.\\n   \\\\]\\n   Here \\\\(M_{RS}^T\\\\) is the finite multiplicity space.  If \\\\(m_T=\\\\dim\\n   M_{RS}^T\\\\), choose a basis vector \\\\(e_i\\\\) for each copy \\\\(i=0,\\\\dots,m_T-1\\\\).\\nA local CG kernel is an intertwiner selecting one basis vector in\\n   \\\\(M_{RS}^T\\\\).  Distinct multiplicity-copy ids are distinct basis vectors in\\n   that multiplicity space.\\nFor a fixed canonical coupling tree, a global path is a product of local\\n   choices: at every internal node, choose the output channel and\\n   multiplicity-copy basis vector.\\nThe set of all paths ending in the requested target \\\\(T\\\\), usually\\n   \\\\(\\\\mathbf 1\\\\), is a basis of the multiplicity space\\n   \\\\[\\n     \\\\operatorname{Hom}_{\\\\mathfrak g}\\n     \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_n\\\\right)\\n   \\\\]\\n   for that tree and basis convention.\\nIndependence follows for that fixed tree because the decomposition is a direct\\nsum of irreducible channels and explicit multiplicity spaces.  We are not\\nselecting linearly independent gamma expressions from an overcomplete list.  We\\nare selecting basis vectors in multiplicity spaces and composing intertwiners\\nalong one tree convention.\\nDifferent tree policies give different coordinate systems on the same\\nmultiplicity space.  They must not be concatenated.  If the implementation\\nlater supports several trees, each tree must separately produce the target\\nmultiplicity.  The combined count across trees is not an independence\\ncertificate; it is redundant data until a basis-change or reduction map is\\navailable.\\nRendered formulas may look dependent because gamma matrices, epsilons,\\nmetrics, and projector blocks satisfy identities.  Those identities are part\\nof the rendering backend.  They must not be used to define the abstract basis.\\nRendering must be a faithful expansion of the already-selected path basis in a\\nchosen convention.  If a renderer cannot prove or verify faithfulness for a\\nconvention, it must report a typed error or mark the expansion as an audit\\ntarget; it must not silently collapse or add basis elements.\\nRequired independence checks:\\nThe number of emitted basis paths equals the target multiplicity computed by\\n  dynamic programming over decompositions.\\nFor every binary decomposition, the sum\\n  \\\\[\\n    \\\\sum_T m_T\\\\dim T\\n  \\\\]\\n  equals \\\\(\\\\dim R\\\\,\\\\dim S\\\\).\\nLocal projector records for a product channel must satisfy the abstract\\n  projector laws in their construction record: idempotency and channel\\n  separation.  When explicit formulas exist, audits should verify\\n  \\\\(P_iP_j=\\\\delta_{ij}P_i\\\\) in the chosen representation of the local product.\\nBoundary realization projectors must satisfy the same rule: e.g.\\n  \\\\(144\\\\subset10\\\\otimes16\\\\) is represented by an idempotent whose image has\\n  dimension \\\\(144\\\\), and the complementary channel is accounted for.\\nRendered term streams are audits of the basis, not the source of the basis.\\n  Component evaluation or filtered numerical checks can catch renderer bugs\\n  but cannot replace the multiplicity-space construction.\\nImplementation InstructionsProceed in this order.  Do not skip ahead to rendered gamma formulas or\\nhigh-level invariant counts.\\nImplement the backend registry privately in Context.  It must select a\\n   backend by algebra family and return typed unsupported-capability errors.\\nImplement flat weight storage and a weight-system cache.  Store signed\\n   weight coordinates in one buffer and weight records as offsets.  Add audit\\n   counters for weight count and total multiplicity.\\nImplement the generic highest-weight weight-system generator.  Validate it\\n   on known dimensions before any tensor-product code is accepted.\\nImplement tensor-product character multiplication and subtraction.  The\\n   result must be flat product terms with multiplicities.  Store the result in\\n   Tensor Decomposition.\\nAdd decomposition acceptance tests:\\n   \\\\(SU(2):2\\\\otimes2=1\\\\oplus3\\\\),\\n   \\\\(Spin(10):10\\\\otimes16=16\\\\oplus144\\\\),\\n   \\\\(Spin(10):16\\\\otimes16=10\\\\oplus120\\\\oplus126\\\\), and\\n   \\\\(E_8:248\\\\otimes248\\\\) with the known channels.\\nImplement dynamic-programming path counting to a target using cached binary\\n   decompositions.  This is still counting, but it must use the same channel\\n   records that path enumeration will use.\\nImplement path enumeration for one canonical coupling tree.  Each emitted\\n   path must store its local channel choices and multiplicity-copy ids.  The\\n   path count must equal the dynamic count.  Do not enumerate several trees\\n   until basis-change or reduction maps exist.\\nImplement projector identity creation.  Each projector id must record its\\n   role, convention, derivation kind, and expansion obligation.\\nImplement a minimal expansion interface that can walk one path and call a\\n   sink with streamed symbolic terms.  At first it may return typed\\n   NotImplemented for missing local formulas, but it must not return opaque\\n   successful placeholders.\\nImplement filters inside the expansion loop.  The filter decision is\\n    tri-state: accept, reject, or undecided.  A partial term can be\\n    rejected only when rejection is final under all completions.  Prove with a\\n    reject-all filter test that no large output buffer is allocated.\\nOnly after the generic path and projector infrastructure is correct, add\\n    specialized SO/Spin gamma/projector renderers, SU tableau/Gelfand-Tsetlin\\n    optimizations, or E-type special renderers.\\nAt every step, prefer the smallest working data structure.  Add a new\\nabstraction only when it removes real duplication or separates a hot data path.\\nDo not add public exports for backend internals unless CFT code has a concrete\\ncall site that needs them.\\nCurrent StateThe public API is rooted at Tensor-Code.  Public\\ncallers create a context, register an algebra, register irreps, optionally\\nregister concrete realizations, and request an invariant basis:\\nvar ctx = try tensor.Context.init(allocator);\\ndefer ctx.deinit();\\n\\nconst so10 = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\nconst spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\nconst leg = tensor.ExternalLeg.primitive(spinor, &.{\\n    .init(.spinor, \\"a\\"),\\n});\\nconst basis = try ctx.invariantBasis(.{\\n    .algebra = so10,\\n    .external_legs = &.{leg},\\n});\\nConventions are configurable at context construction:\\nvar ctx = try tensor.Context.initWithOptions(allocator, .{\\n    .algebra_conventions = .{\\n        .quadratic_casimir_scale = .{ .numerator = 1, .denominator = 1 },\\n    },\\n    .default_signature = .complex,\\n});\\ndefer ctx.deinit();\\nThe current substrate is:\\nTensor Root Data validates supported simple algebras,\\n  stores Cartan data, computes positive roots, dual Dynkin labels, Weyl\\n  dimensions, and quadratic Casimirs.\\nTensor Representation Store interns algebras and\\n  irreps.  It computes metadata from Dynkin labels and provides explicit\\n  dualIrrep lookup without recursively closing registration under duals.\\nTensor Realization records concrete external\\n  index shapes.  Projected realizations store a selected channel, so a\\n  constrained leg such as \\\\(144\\\\subset 10\\\\otimes16\\\\) is a first-class boundary\\n  realization.\\nTensor Decomposition owns the binary product\\n  decomposition cache.  It stores flat product terms and has a hash index keyed\\n  by the pair of irrep handles.\\nTensor Projector owns projector identities:\\n  product-channel projectors, boundary-realization projectors, and basis-change\\n  projectors.\\nTensor Backend defines the internal backend\\n  capability contract.\\nTensor Rendering defines streamed symbolic\\n  terms.  SymbolicTerm carries borrowed slices valid only until the sink\\n  returns.  The public root does not expose a gamma-specific type; SO/Spin\\n  renderers can emit backend operators through generic symbolic atoms.\\nBuild-checked metadata cases currently include:\\n\\\\(B_4\\\\): vector \\\\(9\\\\), spinor \\\\(16\\\\);\\n\\\\(D_5\\\\): vector \\\\(10\\\\), spinor \\\\(16\\\\), vector-spinor \\\\(144\\\\);\\n\\\\(E_6\\\\): fundamental \\\\(27\\\\);\\n\\\\(E_7\\\\): fundamental \\\\(56\\\\);\\n\\\\(E_8\\\\): adjoint \\\\(248\\\\).\\nThe following are still intentionally not implemented:\\nbinary decomposition algorithms;\\nweight-system generation;\\nchannel-path enumeration;\\nlocal CG/projector construction;\\nexplicit gamma or projector expansion execution;\\nexpansion filters and downstream stream consumers;\\nContext.renderInvariant;\\nContext.evaluateInvariant.\\nBackend GoalThe first backend should be a generic highest-weight backend.  It should be a\\ncorrectness backend and cache builder, not yet a family-specific optimized\\nrenderer.  Its first job is to answer:\\nis this algebra supported?\\nwhat is the weight system of this irrep?\\nwhat is the decomposition of \\\\(R\\\\otimes S\\\\)?\\nwhat stable projector identity corresponds to one product channel?\\nThe backend must not allocate dense tensor-product component spaces.  It may\\nconstruct compact weight multisets and flat cache records.\\nBackend Build Order1. Backend registryAdd an internal backend registry owned by the private Context implementation.\\nIt maps LieFamily to a backend record and selects a backend from an algebra\\nfamily.  It should not be exported from Tensor-Code.\\nStart with one backend implementation:\\nfile: generic-highest-weight-backend.zig;\\nsupports \\\\(A_n,B_n,D_n,E_6,E_7,E_8\\\\);\\nreturns typed missing-capability errors for unimplemented operations.\\nThe registry should make no CFT-facing API changes.  CFT code still asks the\\ncontext for invariant bases.\\n2. Root-data helpersUse Tensor Root Data as the source of algebra\\nconventions.  Do not duplicate Cartan matrices or exceptional diagrams in the\\nbackend.\\nThe backend will need internal helpers for:\\nrank;\\nCartan entries;\\nsimple-root reflection on weights;\\ndominant-weight test;\\nhighest-weight validation;\\nexact rational inner products where Freudenthal or Casimir formulas need\\n  them.\\nKeep these helper APIs internal unless a later caller clearly needs them.\\n3. Weight storageWeights are not public API.  Store them in flat buffers:\\nconst WeightRef = struct {\\n    dynkin_offset: u32,\\n};\\n\\nconst WeightMultiplicity = struct {\\n    weight: WeightRef,\\n    multiplicity: u32,\\n};\\nThe actual Dynkin-coordinate arrays should live in one flat signed buffer,\\nprobably i32 once tensor products become large enough.  Signed coordinates\\nare necessary because intermediate weights and reflected weights need not be\\ndominant.\\nStore rank once on the weight-system span or get it from the algebra.  Do not\\nstore rank in every weight record.  Do not allocate one heap object per weight.\\n4. Weight-system generatorImplement a cached weight-system generator for one highest-weight irrep.\\nFreudenthal multiplicity recursion is the generic first choice because it\\nworks uniformly for the supported simple families.\\nCache key:\\nconst WeightSystemKey = struct {\\n    algebra: store.AlgebraHandle,\\n    irrep: store.IrrepHandle,\\n};\\nCache value:\\nconst WeightSystem = struct {\\n    weights_offset: u32,\\n    weights_len: u32,\\n    rank: u8,\\n    dynkin_coords_offset: u32,\\n    dynkin_coords_len: u32,\\n};\\nAcceptance checks:\\n\\\\(D_5\\\\) spinor has total weight multiplicity \\\\(16\\\\);\\n\\\\(D_5\\\\) vector has total weight multiplicity \\\\(10\\\\);\\n\\\\(B_4\\\\) spinor has total weight multiplicity \\\\(16\\\\);\\n\\\\(E_8\\\\) adjoint has total weight multiplicity \\\\(248\\\\).\\n5. Tensor-product decompositionUse character subtraction first.  It is generic and easier to audit than a\\nfamily-specific closed formula.\\nAlgorithm:\\nget cached weight systems for \\\\(R\\\\) and \\\\(S\\\\);\\nadd all pairs of weights to build the weight multiset for \\\\(R\\\\otimes S\\\\);\\nfind a maximal dominant weight still present, using one deterministic\\n   dominance-order rule;\\nits remaining multiplicity is the multiplicity of the next channel;\\nsubtract that many copies of the cached character of that highest weight;\\nrepeat until the multiset is empty.\\nThe dominance selection rule must be explicit and stable.  A practical first\\nrule is:\\nscan only weights with positive residual multiplicity;\\ndiscard non-dominant weights;\\nchoose a dominant weight not strictly below another remaining dominant\\n   weight in the root partial order;\\nbreak ties by lexicographic Dynkin labels.\\nAfter every subtraction, no residual multiplicity may become negative.  A\\nnegative residual is a backend bug, not a recoverable ambiguity.  The selected\\nchannel multiplicity is the residual multiplicity of the selected highest\\nweight before subtraction.\\nThe audit invariant after each subtraction is:\\n\\\\[\\n  \\\\chi_{\\\\mathrm{remaining}}+\\\\sum_i m_i\\\\chi_{T_i}\\n  = \\\\chi_R\\\\chi_S .\\n\\\\]\\nThe final residual character must be zero.  This catches lost multiplicities,\\nwrong dominant-weight selection, and incorrect subtraction signs.\\nThis is not the final fastest algorithm.  It is the first trusted backend.  It\\nwill become the oracle against which specialized \\\\(SO/Spin\\\\), \\\\(SU\\\\), and\\nexceptional backends are checked.\\nAcceptance checks:\\n\\\\(SU(2): 2\\\\otimes2 = 1\\\\oplus3\\\\);\\n\\\\(Spin(10): 10\\\\otimes16 = 16\\\\oplus144\\\\);\\n\\\\(Spin(10): 16\\\\otimes16 = 10\\\\oplus120\\\\oplus126\\\\);\\n\\\\(E_8: 248\\\\otimes248\\\\) matches the known decomposition, including\\n  multiplicities and dimensions.\\n6. Decomposition cache integrationThe backend writes decompositions into Tensor Decomposition.\\nThe cache stores:\\nproduct key \\\\(R,S\\\\);\\nflat product terms \\\\((T,m_T)\\\\);\\nstable handle for repeated requests.\\nThe future internal context method should look like:\\nfn decomposeProduct(ctx: *Context, left: store.IrrepHandle, right: store.IrrepHandle) !decomposition.ProductDecompositionHandle\\nDo not expose this method publicly unless CFT code needs it.\\n7. Projector identities and expansion obligationsFor each product term, the backend may create a product-channel projector\\nidentity in Tensor Projector.  The first\\ndecomposition backend does not need to expand projector formulas immediately,\\nbut a successful basis result must not contain projector ids that have no\\nexpansion route.  Internal blocked records are allowed only if they fail before\\nbeing returned as successful invariant data.  Each projector record needs\\nenough information to recover an expansion path.\\nThe projector identity records:\\nleft irrep;\\nright irrep;\\ntarget channel;\\nconvention id;\\nderivation kind;\\nexpansion source, such as Casimir polynomial, idempotent split,\\n  highest-weight solver output, or backend-specific verified construction.\\nDo not store expandability as a boolean.  The backend needs enough state to\\ndistinguish several useful stages:\\nconst ExpansionStatus = enum {\\n    not_requested,\\n    expandable_named,\\n    expandable_blocks,\\n    expandable_terms,\\n    blocked_missing_formula,\\n    blocked_unverified_identity,\\n};\\nexpandable_named is only a rendering mode.  It is not proof that the\\nprojector satisfies the fully-expandable requirement.  A projector appearing in\\na successful invariant basis must have a derivation route that can reach\\nexpandable_terms in the requested convention, even when the caller chooses to\\nrender it as a compact name.\\nAcceptance checks:\\nrepeated requests return the same projector id;\\nmultiplicity copies are explicit;\\nno symbolic projector expansion is allocated during decomposition;\\nevery returned projector reports an ExpansionStatus in the requested\\n  convention;\\nblocked projector statuses are typed errors before successful basis return,\\n  not silently named placeholders;\\na basis result rendered in named mode still audits that each local projector\\n  has a term-expansion route.\\n8. Backend capability implementationUpdate Tensor Backend only as needed to support the\\ngeneric backend.  The expected internal procedures are:\\nvalidateAlgebra;\\nirrepMetadata, if a backend-specific metadata path is needed;\\nweightSystem;\\ndecomposeProduct;\\nlocalProjectorId.\\nRendering remains NotImplemented.\\n9. Expansion stream and filtersBefore explicit projector formulas are implemented, define the execution\\nboundary they must satisfy.  The expansion API should be a streaming pipeline:\\nfn renderInvariantExpanded(\\n    ctx: *Context,\\n    basis: coupling.BasisHandle,\\n    invariant: coupling.InvariantHandle,\\n    options: rendering.RenderOptions,\\n    filter: ExpansionFilter,\\n    sink: anytype,\\n) !void\\nThe exact public shape may differ, but the semantics should not:\\nconst FilterDecision = enum {\\n    accept,\\n    reject,\\n    undecided,\\n};\\nthe renderer walks the compact coupling path;\\nlocal projector expansions are pulled lazily;\\neach local expansion term is combined into the current partial term;\\nthe filter is applied as early as possible to borrowed scratch terms;\\naccepted terms are emitted immediately to the sink;\\nrejected terms are not stored;\\nundecided partial terms may continue, but the implementation must track their\\n  frontier size;\\nrepeated local projector expansions are cached by projector id and\\n  convention when doing so reduces work.\\nPartial terms can be rejected only when the predicate is final and sound under\\nall later factors.  Otherwise the filter must return undecided.  This is\\npart of correctness, not just an optimization detail.\\nFilter callbacks and sinks must not retain borrowed slices from streamed\\nterms.  If a caller needs to keep a term, it must copy it into caller-owned\\nstorage.  This keeps the renderer free to reuse scratch buffers and prevents a\\nhidden materialized expansion from growing behind the streaming interface.\\nThe filter is not a post-processing pass over a giant vector.  It is an\\nexecution hook that keeps the streamed expansion small enough to be useful.\\nPossible filter predicates:\\nterm contains or avoids a given external index block;\\nterm uses only selected symbolic operator kinds;\\nterm is compatible with a component assignment;\\nterm can contribute to a downstream contraction;\\nterm has a coefficient/operator class accepted by the target CFT routine.\\nAcceptance checks:\\nexpanding one invariant can stream terms without allocating the full\\n  expansion;\\nexpanding many invariants can reuse local projector expansions;\\na filter that rejects all terms performs no large output allocation;\\na filter that accepts a sparse subset scales with the accepted frontier plus\\n  unavoidable local projector traversal, not with a materialized full basis.\\nCompute ModelThe first backend has four different compute scales:\\nDecomposition compute: weight-system generation, tensor-product weight\\n   multisets, and character subtraction.\\nPath enumeration compute: dynamic programming over cached binary\\n   decompositions to enumerate coupling paths to a target.\\nExpansion compute: streaming the local projector/CG expansions appearing in\\n   selected paths.\\nFilter compute: deciding early which partial or complete expansion terms can\\n   be discarded before they reach downstream CFT code.\\nThe first milestone implements the first scale and prepares records for the\\nthird and fourth.  It must not pretend expansion is free.  A \\\\(73744\\\\)-path\\nresult can be small as stored paths while still being expensive to render if\\neach path contains large expandable projectors.  Therefore every feasibility\\nestimate must track:\\nnumber of basis paths;\\nnumber of distinct local projector ids used by those paths;\\nmaximum and total expected expansion terms per projector when known;\\nwhether expansion can be shared across repeated projector ids;\\nwhether expansion is requested as named, block-expanded, or term-expanded;\\nexpected filter rejection rate;\\nwhether filters can be applied to partial products or only complete terms;\\nnumber of undecided partial terms retained at each expansion layer;\\nsink throughput and backpressure behavior.\\nData-Oriented ConstraintsThe expensive objects are weight systems, tensor-product weight multisets,\\npath records, and projector expansions.  Therefore:\\ncache weight systems by irrep handle;\\nstore weights in flat buffers;\\nsort or hash compact weight keys, not heap-allocated slices;\\nstore product decompositions as flat terms;\\nkeep projector ids as small records;\\ncache projector expansions by projector id and convention when expansion is\\n  requested repeatedly;\\npush filters into expansion loops;\\nemit accepted terms immediately;\\nstream rendered terms into sinks;\\nnever build dense component tensors.\\nThe \\\\(16^{12}\\\\) target should produce coupling paths, not components.  A\\nsingle dense tensor in \\\\(16^{12}\\\\) has \\\\(16^{12}\\\\) components, while the desired\\nanswer has \\\\(73744\\\\) basis paths.  The algorithm must scale with the number of\\nchannels and paths, not the full component space.\\nExplicit Non-Goals For First BackendDo not implement the following in the first backend:\\ngamma-matrix rendering;\\nbulk explicit projector expansion execution;\\nexplicit CG coefficient tables;\\nhigher-Casimir projector splitting;\\nsparse nullspace solvers;\\nparallel execution;\\n\\\\(SU(N)\\\\) Gelfand-Tsetlin or tableau optimized CGs;\\n\\\\(SO/Spin\\\\) Clifford-specialized renderers;\\n\\\\(E_8\\\\) adjoint-specialized renderers.\\nThose belong after binary decomposition is correct, cached, and tested.  The\\ndata model must still be designed so these operations can be added without\\nchanging the meaning of existing projector ids.\\nDefinition Of DoneThe backend handoff is complete when:\\nzig build --global-cache-dir /tmp/zig-global-cache passes;\\nall acceptance decompositions above pass;\\nweight systems are cached and flat;\\nproduct decompositions are cached by key;\\nno dense tensor component arrays are allocated;\\nContext.invariantBasis can internally request product decompositions;\\nprojector records created by the backend have explicit expansion obligations;\\nexpanded invariants can be streamed through a filter without materializing\\n  their full term list;\\nrepeated projector expansions can be cached and reused;\\na downstream sink can consume accepted terms incrementally;\\nrender/evaluate remain explicitly unimplemented until projector formulas and\\n  term streaming are ready.\\n"},{"id":"20260606T150001.000000","title":"Tensor Kernel","tags":null,"content":"Tensor-code supplies compact handles for tensor structures that appear in CFT\\ncoefficients.  The expensive work is generation and selection of invariant\\ntensors, so those procedures should stream candidates rather than building a\\nlarge expression tree inside the kernel.\\nAn invariant source represents basis elements in\\n\\\\[\\n  \\\\operatorname{Hom}_{\\\\mathfrak g}(T,\\\\otimes_i R_i)\\n\\\\]\\nby handles.  Rendering into a concrete notation is backend-dependent: \\\\(A_n\\\\)\\nmay render through Young symmetrizers and \\\\(\\\\epsilon/\\\\delta\\\\) tensors, \\\\(D_n\\\\)\\nthrough metric, epsilon, and spinorial gamma data, and exceptional algebras\\nthrough their available primitive invariant tensors.\\nconst coupling = @import(\\"coupling.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\nThe basic ids are deliberately small.  They name entries in stores owned by a\\npreset, a tensor generator, or a client.\\n/// TensorExprId names a tensor expression stored by tensor-code.\\npub const TensorExprId = u32;\\n/// TensorTermId names one term inside a tensor expression store.\\nconst TensorTermId = u32;\\n/// BasisPathId names one compact coupling path in an invariant basis.\\nconst BasisPathId = u32;\\n/// RenderedTermId names one streamed symbolic tensor term.\\nconst RenderedTermId = u32;\\n/// ReductionTermId names one coefficient in a target invariant basis.\\nconst ReductionTermId = u32;\\n/// InvariantFamilyId names a family of invariant tensor candidates.\\nconst InvariantFamilyId = u16;\\n/// AlgebraSchemaId names a Lie or superalgebra schema.\\nconst AlgebraSchemaId = u16;\\n/// GradingId names a grading attached to a tensor algebra schema.\\nconst GradingId = u16;\\nInvariant generation should be pull-based.  A caller can feed the source to an\\nindependent-basis selector without materializing every candidate first.\\n/// TensorExprSource streams tensor expression ids from a generator.\\nconst TensorExprSource = struct {\\n    next_fn: *const fn (*anyopaque) ?TensorExprId,\\n    state: *anyopaque,\\n\\n    /// next returns the next generated tensor expression id.\\n    pub fn next(self: *TensorExprSource) ?TensorExprId {\\n        return self.next_fn(self.state);\\n    }\\n};\\nBasis paths and rendered terms are streamed separately.  This lets CFT code\\nattach invariant ids to rational functions without asking tensor-code to\\nexpand concrete tensor expressions during ordinary simplification.\\n/// BasisPathSource streams invariant ids from a generated basis.\\nconst BasisPathSource = struct {\\n    next_fn: *const fn (*anyopaque) ?coupling.InvariantHandle,\\n    state: *anyopaque,\\n\\n    /// next returns the next invariant in the basis.\\n    pub fn next(self: *BasisPathSource) ?coupling.InvariantHandle {\\n        return self.next_fn(self.state);\\n    }\\n};\\n\\n/// RenderedTermSource streams symbolic terms for one invariant rendering.\\nconst RenderedTermSource = struct {\\n    next_fn: *const fn (*anyopaque) ?RenderedTermId,\\n    state: *anyopaque,\\n\\n    /// next returns the next rendered symbolic term.\\n    pub fn next(self: *RenderedTermSource) ?RenderedTermId {\\n        return self.next_fn(self.state);\\n    }\\n};\\n\\n/// ReductionTermSource streams coefficients in a chosen invariant basis.\\nconst ReductionTermSource = struct {\\n    next_fn: *const fn (*anyopaque) ?ReductionTermId,\\n    state: *anyopaque,\\n\\n    /// next returns the next reduction coefficient term.\\n    pub fn next(self: *ReductionTermSource) ?ReductionTermId {\\n        return self.next_fn(self.state);\\n    }\\n};\\nAn invariant family says which algebra and external representations define the\\ncandidate space.  The actual candidates are produced by a generator.\\n/// InvariantFamily declares the tensor-invariant problem to generate.\\nconst InvariantFamily = struct {\\n    name: []const u8,\\n    algebra: AlgebraSchemaId,\\n    external_representations: []const symmetry.RepresentationId,\\n};\\nThe new invariant request keeps external leg names and realizations.  The old\\nfamily type remains for compatibility with early kernel sketches.\\n/// InvariantRequest records a named public invariant-basis request.\\nconst InvariantRequest = struct {\\n    name: []const u8,\\n    algebra: store.AlgebraHandle,\\n    external_legs: []const realization.ExternalLeg,\\n    target: coupling.TargetIrrep = .singlet,\\n    tree_policy: coupling.TreePolicy = .auto,\\n    basis_policy: coupling.BasisPolicy = .default,\\n};\\nThe algebra schema stores only compact references to core tensors such as a\\nmetric or structure constants.  This is enough for CFT coefficient code to\\nrefer to a symmetry algebra without owning its full tensor simplifier.\\n/// AlgebraSchema records the compact tensor data of a symmetry algebra.\\nconst AlgebraSchema = struct {\\n    name: []const u8,\\n    symmetry: symmetry.SymmetryId,\\n    grading: ?GradingId = null,\\n    metric: ?TensorExprId = null,\\n    structure_constants: ?TensorExprId = null,\\n};\\n"},{"id":"20260606T150002.000000","title":"CFT Kernel","tags":null,"content":"The kernel is the runtime executor for a CFT preset.  It does not define what a\\nCFT is; that belongs to Theory.  The kernel receives a theory declaration at\\ncomptime, builds a generated type from it, and exposes only the small\\nruntime operations that Correlators, OPE, and Basis-Generation need.\\nconst operators = @import(\\"expressions/operators.zig\\");\\nconst theory = @import(\\"theory/theory.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nStreamed OutputA correlator or OPE should emit terms as soon as possible instead of\\nmaterializing a large Expression.  The stream namespace holds the small output\\nrecords used by sinks.\\n/// Stream groups coefficient and projected-local-term records emitted by kernels.\\npub const Stream = struct {\\n    /// Scalar is the numeric coefficient type used by the kernel skeleton.\\n    pub const Scalar = f64;\\n    /// CoordinateFactor names a coordinate factor in a coefficient store.\\n    pub const CoordinateFactor = u32;\\n\\n    /// CoeffTerm is one streamed scalar-coordinate-tensor coefficient term.\\n    pub const CoeffTerm = struct {\\n        scalar: Scalar,\\n        coordinate_factor: CoordinateFactor,\\n        tensor_structure: ?tensor.TensorExprId,\\n    };\\n\\n    /// LocalTerm is one streamed projected local OPE term.\\n    pub const LocalTerm = struct {\\n        coeff: CoeffTerm,\\n        insertion: operators.OperatorInsertion,\\n        body: operators.OperatorBodyId,\\n    };\\n};\\nRuntime CallsThe public OPE and correlator accept any number of local Operators through a\\nMultiOp.  At runtime a local operator is only its insertion, operator kind,\\nand a span into an immutable label store.  The typed preset builder will create\\nthese spans; the kernel only needs compact data for fast Wick and zero-mode\\npasses.\\nProjection and basis queries are call data; Theory owns the\\nquantum-number, weight, domain, and mode ids they refer to.\\n/// Call groups compact input records passed to generated kernels.\\npub const Call = struct {\\n    /// RationalLabel stores one exact rational operator label.\\n    pub const RationalLabel = struct {\\n        numerator: i64,\\n        denominator: i64,\\n    };\\n\\n    /// LabelValue stores one compact runtime operator label.\\n    pub const LabelValue = union(enum) {\\n        integer: i64,\\n        rational: RationalLabel,\\n        tensor: tensor.TensorExprId,\\n        symbol: u32,\\n    };\\n\\n    /// LabelStore is the immutable label arena carried by a MultiOp.\\n    pub const LabelStore = struct {\\n        values: []const LabelValue,\\n    };\\n\\n    /// LocalOp is one runtime local operator with labels stored out-of-line.\\n    pub const LocalOp = struct {\\n        insertion: operators.OperatorInsertion,\\n        kind: operators.OperatorKindId,\\n        labels: theory.Id.LabelSpan,\\n        /// Equal nonzero ids mark factors in the same normal-ordered product.\\n        normal_order_group: u16 = 0,\\n    };\\n\\n    /// MultiOp is an ordered collection of local operators and their label store.\\n    pub const MultiOp = struct {\\n        operators: []const LocalOp,\\n        labels: *const LabelStore,\\n    };\\n\\n    /// OpeProjection stores the target and filters for projected OPE output.\\n    pub const OpeProjection = struct {\\n        target: OpeTarget,\\n        target_weights: ?theory.Runtime.ConformalWeights,\\n        quantum_filters: []const theory.Label.QuantumFilter,\\n        max_level: ?theory.Runtime.LevelBound,\\n    };\\n\\n    /// OpeTarget tells the OPE where the projected local term is inserted.\\n    pub const OpeTarget = union(enum) {\\n        first_insertion,\\n        explicit: operators.OperatorInsertion,\\n    };\\n\\n    /// BasisQuery stores filters for streamed operator or state generation.\\n    pub const BasisQuery = struct {\\n        weights: ?theory.Runtime.ConformalWeights,\\n        quantum_filters: []const theory.Label.QuantumFilter,\\n        domain: ?theory.Runtime.OperatorDomain,\\n        support: ?theory.Runtime.ChiralSupport,\\n        max_level: ?theory.Runtime.LevelBound,\\n        finite_projections: []const theory.Id.FiniteProjection,\\n        descendants: DescendantFilter,\\n    };\\n\\n    /// DescendantFilter limits which mode descendants basis generation may use.\\n    pub const DescendantFilter = struct {\\n        mode_algebras: []const theory.Id.ModeAlgebra,\\n        max_level: ?theory.Runtime.LevelBound,\\n    };\\n\\n    /// BasisState is one streamed candidate body with computed weights.\\n    pub const BasisState = struct {\\n        body: operators.OperatorBodyId,\\n        weights: theory.Runtime.ConformalWeights,\\n    };\\n};\\nBody ConstructionThe body store is generated from a theory operator schema.  Later\\nimplementations will validate labels and canonicalize normal products here;\\nthe first version only build-checks the boundary.\\nconst Body = struct {\\n    const Schema = struct {\\n        bulk_kinds: []const operators.OperatorKind,\\n        boundary_kind_ids: []const operators.OperatorKindId = &.{},\\n        boundary_changing_kinds: []const theory.Boundary.ChangingKind = &.{},\\n    };\\n\\n    const CanonicalResult = struct {\\n        coeff: Stream.Scalar,\\n        body: ?operators.OperatorBodyId,\\n    };\\n\\n    fn Store(comptime schema: Schema) type {\\n        return struct {\\n            const operator_schema = schema;\\n\\n            /// atomic constructs an atomic operator body handle.\\n            pub fn atomic(kind: operators.OperatorKindId, labels: theory.Id.LabelSpan) !operators.OperatorBodyId {\\n                _ = labels;\\n                return @as(operators.OperatorBodyId, kind);\\n            }\\n\\n            /// normalOrdered constructs a canonical normal-product body handle.\\n            pub fn normalOrdered(factors: []const operators.OperatorBodyId) !CanonicalResult {\\n                if (factors.len == 0) {\\n                    return .{ .coeff = 1, .body = null };\\n                }\\n                return .{ .coeff = 1, .body = factors[0] };\\n            }\\n        };\\n    }\\n};\\nThe store is a generated type because its schema is preset data.  This is the\\nright use of comptime: it specializes a hot construction boundary without\\nmoving runtime operator labels or generated basis states to compile time.\\nGenerated KernelsThe generated bulk kernel owns no boundary data.  It receives a bulk\\nSpec.CFT, exposes public runtime operations, and keeps preset tables private\\nto the generated type.\\n/// CFTKernel returns the generated kernel type for a bulk CFT preset.\\npub fn CFTKernel(comptime spec: theory.Spec.CFT) type {\\n    return struct {\\n        const cft_spec = spec;\\n        const bulk_operator_kinds = spec.bulk_operator_kinds;\\n\\n        /// Bodies constructs operator bodies using this preset\'s schema.\\n        pub const Bodies = Body.Store(.{\\n            .bulk_kinds = spec.bulk_operator_kinds,\\n        });\\n\\n        /// ope computes the projected OPE of any number of local operators.\\n        pub fn ope(input: Call.MultiOp, projection: Call.OpeProjection, sink: anytype) !void {\\n            _ = input;\\n            _ = projection;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// correlator evaluates any number of local operators with a config.\\n        pub fn correlator(config: *const theory.Runtime.CorrelatorConfig, input: Call.MultiOp, sink: anytype) !void {\\n            _ = config;\\n            _ = input;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// basis streams operator or state candidates satisfying a query.\\n        pub fn basis(query: Call.BasisQuery, sink: anytype) !void {\\n            _ = query;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n    };\\n}\\nThe BCFT generated kernel extends an already generated bulk kernel.  Boundary\\ndata enters only through the Spec.BCFT supplied here.\\n/// BCFTKernel returns the generated kernel type for a BCFT extension.\\npub fn BCFTKernel(comptime Bulk: type, comptime bcft: theory.Spec.BCFT) type {\\n    return struct {\\n        const bulk = Bulk;\\n        const bcft_spec = bcft;\\n\\n        /// Bodies constructs bulk, boundary, and boundary-changing bodies.\\n        pub const Bodies = Body.Store(.{\\n            .bulk_kinds = Bulk.bulk_operator_kinds,\\n            .boundary_kind_ids = bcft.boundary_operator_kinds,\\n            .boundary_changing_kinds = bcft.boundary_changing_kinds,\\n        });\\n\\n        /// ope computes the projected OPE for this BCFT extension.\\n        pub fn ope(input: Call.MultiOp, projection: Call.OpeProjection, sink: anytype) !void {\\n            _ = input;\\n            _ = projection;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// correlator evaluates bulk and boundary local operators with a config.\\n        pub fn correlator(config: *const theory.Runtime.CorrelatorConfig, input: Call.MultiOp, sink: anytype) !void {\\n            _ = config;\\n            _ = input;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// basis streams bulk, boundary, or boundary-changing candidates.\\n        pub fn basis(query: Call.BasisQuery, sink: anytype) !void {\\n            _ = query;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n    };\\n}\\nBoundary TestThe first test protects the runtime call shape: a MultiOp carries the local\\noperators and the label store needed to interpret them, without receiving a\\nseparate context.\\ntest \\"MultiOp carries local operators and labels\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const labels = Call.LabelStore{\\n        .values = &.{ .{ .integer = 7 } },\\n    };\\n    const ops = [_]Call.LocalOp{\\n        .{\\n            .insertion = .{ .single = .{ .position = 1, .derivatives = 0 } },\\n            .kind = 2,\\n            .labels = 0,\\n        },\\n    };\\n    const input = Call.MultiOp{\\n        .operators = &ops,\\n        .labels = &labels,\\n    };\\n\\n    try testing.expectEqual(@as(usize, 1), input.operators.len);\\n    try testing.expectEqual(@as(operators.OperatorKindId, 2), input.operators[0].kind);\\n    switch (input.labels.values[0]) {\\n        .integer => |value| try testing.expectEqual(@as(i64, 7), value),\\n        else => return error.UnexpectedLabelKind,\\n    }\\n}\\nThe second test checks the kernel/theory boundary that matters now: a BCFT\\ngenerated kernel extends the body schema without mutating the bulk generated\\nkernel.\\ntest \\"BCFT kernel extends bulk schema without mutating bulk schema\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const bulk_spec = theory.Spec.CFT{\\n        .sectors = &.{},\\n        .bulk_operator_kinds = &.{},\\n        .conformal_structure = 0,\\n        .conformal_structures = &.{},\\n        .quantum_names = &.{},\\n        .label_schemas = &.{},\\n        .conventions = &.{},\\n        .weight_rules = &.{},\\n        .quantum_rules = &.{},\\n        .statistics_rules = &.{},\\n        .strategy_support_rules = &.{},\\n        .wick_rule_sets = &.{},\\n        .pairing_data = &.{},\\n        .zero_mode_rule_sets = &.{},\\n        .correlator_configs = &.{},\\n        .local_ope_configs = &.{},\\n        .cocycle_tables = &.{},\\n        .presentations = &.{},\\n        .finite_projections = &.{},\\n        .lattices = &.{},\\n        .mode_algebras = &.{},\\n        .mode_action_rules = &.{},\\n    };\\n\\n    const bcft_spec = theory.Spec.BCFT{\\n        .name = \\"empty boundary extension\\",\\n        .bulk = 0,\\n        .boundary_conditions = &.{},\\n        .boundary_stacks = &.{},\\n        .boundary_stress_tensor = .{ .kind = 0, .labels = 0 },\\n        .boundary_spin = &.{},\\n        .boundary_operator_kinds = &.{ 1, 2 },\\n        .boundary_changing_kinds = &.{},\\n        .wick_rule_sets = &.{},\\n        .pairing_data = &.{},\\n        .zero_mode_rule_sets = &.{},\\n        .correlator_configs = &.{},\\n        .local_ope_configs = &.{},\\n    };\\n\\n    const Bulk = CFTKernel(bulk_spec);\\n    const BoundaryKernel = BCFTKernel(Bulk, bcft_spec);\\n\\n    try testing.expectEqual(@as(usize, 0), Bulk.Bodies.operator_schema.boundary_kind_ids.len);\\n    try testing.expectEqual(@as(usize, 2), BoundaryKernel.Bodies.operator_schema.boundary_kind_ids.len);\\n    try testing.expectEqual(@as(operators.OperatorKindId, 1), BoundaryKernel.Bodies.operator_schema.boundary_kind_ids[0]);\\n}\\n"},{"id":"20260606T150003.000000","title":"String-Code","tags":null,"content":"The root module exposes the two build-checked kernels.  Higher string modules\\ndepend on these through their public APIs.\\n/// tensor exposes tensor-code data structures and tensor handles.\\npub const tensor = @import(\\"tensor-code\\");\\n/// cft exposes the selected worldsheet CFT public API.\\npub const cft = @import(\\"cft-code\\");\\n\\ntest \\"top-level API exposes tensor and cft boundaries\\" {\\n    const testing = @import(\\"std\\").testing;\\n    try testing.expect(@hasDecl(@This(), \\"tensor\\"));\\n    try testing.expect(@hasDecl(@This(), \\"cft\\"));\\n    try testing.expect(@hasDecl(cft, \\"FreeBoson\\"));\\n    try testing.expect(@hasDecl(cft, \\"Bc\\"));\\n    try testing.expect(@hasDecl(cft, \\"FreeFermion\\"));\\n    try testing.expect(@hasDecl(cft, \\"EtaXi\\"));\\n    try testing.expect(@hasDecl(cft, \\"product\\"));\\n    try testing.expect(@hasDecl(cft, \\"boundary\\"));\\n    try testing.expect(@hasDecl(cft.FreeBoson, \\"make\\"));\\n    try testing.expect(@hasDecl(cft.FreeBoson, \\"boundary\\"));\\n    try testing.expect(@hasDecl(cft.Bc, \\"sphere\\"));\\n    try testing.expect(@hasDecl(cft.Bc, \\"diskBoundary\\"));\\n    try testing.expect(@hasDecl(cft.FreeFermion, \\"sphere\\"));\\n    try testing.expect(@hasDecl(cft.EtaXi, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(cft.FreeBoson, \\"Config\\"));\\n    try testing.expect(!@hasDecl(cft.FreeBoson, \\"BoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(cft.Bc, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft.Bc, \\"DiskConfig\\"));\\n    try testing.expect(!@hasDecl(cft.FreeFermion, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft.EtaXi, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft, \\"Handle\\"));\\n    try testing.expect(!@hasDecl(cft, \\"scalars\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeBoson\\"));\\n    try testing.expect(!@hasDecl(cft, \\"bcSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeFermionSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"etaXiSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeBosonBoundary\\"));\\n    try testing.expect(!@hasDecl(cft, \\"bcDiskBoundary\\"));\\n    try testing.expect(!@hasDecl(cft, \\"presets\\"));\\n    try testing.expect(!@hasDecl(cft, \\"kernel\\"));\\n    try testing.expect(!@hasDecl(cft, \\"theory\\"));\\n    try testing.expect(!@hasDecl(cft, \\"expressions\\"));\\n}\\n\\ntest \\"tensor context interns projected realization requests without expansion\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    var ctx = try tensor.Context.init(testing.allocator);\\n    defer ctx.deinit();\\n\\n    const so10 = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\n    const so10_again = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\n    try testing.expectEqual(so10.value, so10_again.value);\\n\\n    const vector = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 0 } });\\n    const spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\n    const vector_spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 1 } });\\n    const vector_spinor_again = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 1 } });\\n    try testing.expectEqual(vector_spinor.value, vector_spinor_again.value);\\n    try testing.expectEqual(@as(u128, 10), ctx.irrepMetadata(vector).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 16), ctx.irrepMetadata(spinor).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 144), ctx.irrepMetadata(vector_spinor).?.dimension.?);\\n    const conjugate_spinor = try ctx.dualIrrep(spinor);\\n    try testing.expect(conjugate_spinor.value != spinor.value);\\n    try testing.expectEqual(spinor.value, (try ctx.dualIrrep(conjugate_spinor)).value);\\n    try testing.expectError(error.InvalidDynkinRank, ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0 } }));\\n\\n    const su2 = try ctx.registerAlgebra(.{ .simple = .{ .family = .a, .rank = 1 } });\\n    const fundamental = try ctx.registerIrrep(su2, .{ .dynkin = &.{1} });\\n    try testing.expectError(error.MixedRealizationAlgebras, ctx.registerRealization(tensor.RealizationSpec.projected(vector_spinor, &.{fundamental}, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }, 0)));\\n\\n    const channel = tensor.RealizationChannel.first(vector_spinor);\\n    const realization = try ctx.registerRealization(tensor.RealizationSpec.projectedChannel(channel, &.{ vector, spinor }, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }).named(\\"SO10.vector_spinor\\"));\\n    const realization_again = try ctx.registerRealization(tensor.RealizationSpec.projectedChannel(channel, &.{ vector, spinor }, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }).named(\\"SO10.vector_spinor\\"));\\n    try testing.expectEqual(realization.value, realization_again.value);\\n\\n    const leg = tensor.ExternalLeg.realized(realization, &.{\\n        .init(.vector, \\"m1\\"),\\n        .init(.spinor, \\"a1\\"),\\n    });\\n    const basis = try ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{ leg, leg, leg, leg, leg, leg },\\n    });\\n    const basis_again = try ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{ leg, leg, leg, leg, leg, leg },\\n    });\\n    try testing.expectEqual(basis.value, basis_again.value);\\n\\n    try testing.expectError(error.MixedInvariantAlgebras, ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{tensor.ExternalLeg.primitive(fundamental, &.{\\n            .init(.fundamental, \\"i\\"),\\n        })},\\n    }));\\n\\n    const so9 = try ctx.registerAlgebra(.{ .simple = .{ .family = .b, .rank = 4 } });\\n    const so9_vector = try ctx.registerIrrep(so9, .{ .dynkin = &.{ 1, 0, 0, 0 } });\\n    const so9_spinor = try ctx.registerIrrep(so9, .{ .dynkin = &.{ 0, 0, 0, 1 } });\\n    try testing.expectEqual(@as(u128, 9), ctx.irrepMetadata(so9_vector).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 16), ctx.irrepMetadata(so9_spinor).?.dimension.?);\\n\\n    const e6 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e6, .rank = 6 } });\\n    const e7 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e7, .rank = 7 } });\\n    const e8 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e8, .rank = 8 } });\\n    const e6_27 = try ctx.registerIrrep(e6, .{ .dynkin = &.{ 1, 0, 0, 0, 0, 0 } });\\n    const e7_56 = try ctx.registerIrrep(e7, .{ .dynkin = &.{ 0, 0, 0, 0, 0, 1, 0 } });\\n    const e8_248 = try ctx.registerIrrep(e8, .{ .dynkin = &.{ 0, 0, 0, 0, 0, 0, 1, 0 } });\\n    try testing.expectEqual(@as(u128, 27), ctx.irrepMetadata(e6_27).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 56), ctx.irrepMetadata(e7_56).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 248), ctx.irrepMetadata(e8_248).?.dimension.?);\\n}\\n"},{"id":"20260606T163500.000000","title":"Presets","tags":null,"content":"Presets are the physics-facing constructors for common worldsheet CFT data.\\nThey sit between Theory and\\nCFT Kernel.  A user asks for a free boson, a \\\\(bc\\\\)\\nsystem, a product theory, or a boundary extension, and receives a generated\\ntype with a small operator namespace and named correlator configs.\\nThe implementation is split by audit target:\\nPreset Shared Runtime defines compact handles,\\n  local-operator input, Wick templates, zero-mode templates, and the streaming\\n  correlator boundary.\\nFree Boson Preset defines the free-boson bulk and\\n  Neumann/Dirichlet disk data.\\nbc Preset defines the \\\\(bc\\\\) sphere and disk data.\\nFree Fermion Preset defines the NS free fermion\\n  on the sphere.\\neta-xi Preset defines the \\\\(\\\\eta,\\\\xi\\\\) system\\n  on the sphere and records its torus draft metadata.\\nPreset Composition joins bulk sectors and boundary\\n  extensions without introducing a new runtime operator representation.\\nLisp Preset Compiler and the Lisp preset nodes\\n  declare descriptor fixtures that are translated to the generated Zig ABI\\n  module for REPL use.\\nThis node is an internal preset bridge used by CFT-Code.\\nIt is not the user API.  It re-exports only the declarations the root CFT API\\nneeds to build compact family objects such as FreeBoson and Bc.  Raw\\nWick-rule data, correlator config records, and runtime resolver types stay in\\nPreset Shared Runtime for implementation modules.\\nconst shared = @import(\\"presets/shared.zig\\");\\nconst declare = shared.declare;\\nconst free_boson = @import(\\"presets/free_boson.zig\\");\\nconst bc = @import(\\"presets/bc.zig\\");\\nconst free_fermion = @import(\\"presets/free_fermion.zig\\");\\nconst eta_xi = @import(\\"presets/eta_xi.zig\\");\\nconst composition = @import(\\"presets/composition.zig\\");\\n\\nconst freeBoson = free_boson.freeBoson;\\nconst bcSphere = bc.bcSphere;\\nconst freeFermionSphere = free_fermion.freeFermionSphere;\\nconst etaXiSphere = eta_xi.etaXiSphere;\\nconst freeBosonBoundary = free_boson.boundaryExtension;\\nconst bcDiskBoundary = bc.boundaryExtension;\\nShared BridgeScalar convention helpers are available only to preset implementation modules.\\nThe root CFT API intentionally does not re-export them.\\nconst scalars = declare.scalars;\\nSmall private sink adapters keep tests focused on the event they inspect.\\nfn noopWickTermStart(_: anytype, _: anytype) !void {}\\nfn noopWickScalar(_: anytype, _: anytype) !void {}\\nfn noopWickCoordinate(_: anytype, _: anytype) !void {}\\nfn noopWickTensor(_: anytype, _: anytype) !void {}\\nfn noopWickAction(_: anytype, _: anytype) !void {}\\nfn noopWickTermEnd(_: anytype) !void {}\\nfn noopZeroModeFactor(_: anytype, _: anytype) !void {}\\nfn noopZeroModeBaseEnd(_: anytype) !void {}\\nPreset FamiliesThe constructor bridge is grouped by CFT family.  The root public API can\\nre-export these family objects without proliferating flat constructor names.\\n/// FreeBoson groups the free-boson preset constructors.\\npub const FreeBoson = struct {\\n    /// make builds the generated preset type for a noncompact free-boson CFT.\\n    pub const make = free_boson.freeBoson;\\n    /// boundary declares the Neumann/Dirichlet free-boson boundary extension.\\n    pub const boundary = free_boson.boundaryExtension;\\n};\\n\\n/// Bc groups the bc-ghost preset constructors.\\npub const Bc = struct {\\n    /// sphere builds the generated preset type for the sphere bc ghost CFT.\\n    pub const sphere = bc.bcSphere;\\n    /// diskBoundary declares boundary and mixed bulk-boundary bc rules on the disk.\\n    pub const diskBoundary = bc.boundaryExtension;\\n};\\n\\n/// FreeFermion groups the NS free-fermion preset constructors.\\npub const FreeFermion = struct {\\n    /// sphere builds the generated preset type for sphere NS free fermions.\\n    pub const sphere = free_fermion.freeFermionSphere;\\n};\\n\\n/// EtaXi groups the eta-xi preset constructors.\\npub const EtaXi = struct {\\n    /// sphere builds the generated preset type for the sphere eta-xi system.\\n    pub const sphere = eta_xi.etaXiSphere;\\n};\\n\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub const product = composition.product;\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub const boundary = composition.boundary;\\nInterface ChecksThe tests here are intentionally behavioral.  They check concepts that should\\nsurvive later implementation work: composition exposes only selected\\nnamespaces, rule slices compose from the supplied factors, labels remain\\nout-of-line, and the current correlator boundary streams primitive Wick terms\\nrather than allocating a final expression.\\ntest \\"presets compose operator builders without exposing theory tables\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 26 });\\n    const BosonicSphere = product(.{ X, bcSphere(.{}) });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n        .chan_paton = X.target.boundaryStack(\\"stack\\"),\\n    });\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const BosonicDisk = boundary(BosonicSphere, .{ XBoundary, GhostBoundary });\\n\\n    var local = try BosonicDisk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const x = try @TypeOf(XBoundary).op.expXBoundary(&local, k, y);\\n    const c = try @TypeOf(GhostBoundary).op.cBoundary(&local, 0, y);\\n    const ops = try local.ops(.{ x, c });\\n\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"pair_lookup\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"zero_modes\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"config_entries\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(X.config.sphere), \\"wick_rules\\"));\\n    const LocalType = @typeInfo(@TypeOf(local)).pointer.child;\\n    try testing.expect(@typeInfo(LocalType) == .@\\"opaque\\");\\n    try testing.expect(!@hasDecl(LocalType, \\"symbol\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"op\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"add\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"normalOrdered\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"finish\\"));\\n    const OpsType = @typeInfo(@TypeOf(ops)).pointer.child;\\n    try testing.expect(@typeInfo(OpsType) == .@\\"opaque\\");\\n    try testing.expect(!@hasDecl(OpsType, \\"operators\\"));\\n    try testing.expect(!@hasDecl(OpsType, \\"labels\\"));\\n}\\n\\ntest \\"preset composition plumbing stays inside shared lowering\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    try testing.expect(!@hasDecl(shared, \\"ConfigEntry\\"));\\n    try testing.expect(!@hasDecl(shared, \\"correlatorSliceCount\\"));\\n    try testing.expect(!@hasDecl(shared, \\"appendCorrelatorSlice\\"));\\n    try testing.expect(!@hasDecl(shared, \\"boundaryExtensionSliceCount\\"));\\n    try testing.expect(!@hasDecl(shared, \\"appendBoundaryExtensionSlice\\"));\\n    try testing.expect(!@hasDecl(shared, \\"mergeSphereConfigs\\"));\\n    try testing.expect(!@hasDecl(shared, \\"mergeBoundaryConfig\\"));\\n    try testing.expect(@hasDecl(shared, \\"declare\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"mergeSphereConfigs\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"mergeBoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(shared, \\"Spec\\"));\\n    try testing.expect(!@hasDecl(shared, \\"wick\\"));\\n    try testing.expect(!@hasDecl(shared, \\"zero_mode\\"));\\n    try testing.expect(!@hasDecl(shared, \\"scalars\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"Spec\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"wick\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"zero_mode\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"scalars\\"));\\n}\\n\\ntest \\"preset rules and namespaces compose from supplied factors\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const Ghost = bcSphere(.{});\\n    const MatterOnly = product(.{X});\\n    const GhostOnly = product(.{Ghost});\\n    const MatterGhost = product(.{ X, Ghost });\\n\\n    try testing.expect(@hasDecl(MatterOnly.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(GhostOnly.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(MatterGhost.config, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(MatterGhost.op, \\"free_boson\\"));\\n    try testing.expect(!@hasDecl(MatterGhost.op, \\"bc\\"));\\n\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const DiskMatter = boundary(MatterGhost, .{XBoundary});\\n    const DiskMatterGhost = boundary(MatterGhost, .{ XBoundary, GhostBoundary });\\n\\n    try testing.expect(@hasDecl(DiskMatter.op, \\"bulk\\"));\\n    try testing.expect(@hasDecl(DiskMatterGhost.op, \\"bulk\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(DiskMatter.config.disk), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(DiskMatterGhost.config.disk), \\"wick_rules\\"));\\n}\\n\\ntest \\"local operator tokens are owned by their builder\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var first = try X.local(testing.allocator);\\n    defer first.deinit();\\n    var second = try X.local(testing.allocator);\\n    defer second.deinit();\\n\\n    const mu = try first.index(\\"mu\\");\\n    const z = try first.coord(\\"z\\");\\n    const dx = try X.op.dX(&first, mu, 0, z);\\n\\n    try testing.expectError(error.InvalidLocalOperator, second.ops(.{dx}));\\n}\\n\\ntest \\"preset compile-time flags change generated public behavior\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const HolomorphicBc = bcSphere(.{ .include_antiholomorphic_copy = false });\\n    const MatterGhost = product(.{ X, HolomorphicBc });\\n    const BoundaryOnlyGhost = boundary(HolomorphicBc, .{\\n        bcDiskBoundary(.{ .include_mixed_bulk_boundary = false }),\\n    });\\n\\n    try testing.expect(@hasDecl(HolomorphicBc.op, \\"b\\"));\\n    try testing.expect(!@hasDecl(HolomorphicBc.op, \\"bt\\"));\\n    try testing.expect(@hasDecl(MatterGhost.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(BoundaryOnlyGhost.op, \\"bulk\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BoundaryOnlyGhost.config.disk), \\"wick_rules\\"));\\n\\n    const FullGhostDisk = boundary(HolomorphicBc, .{bcDiskBoundary(.{})});\\n\\n    var local = try FullGhostDisk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try HolomorphicBc.op.b(&local, 0, z),\\n        try @TypeOf(bcDiskBoundary(.{})).op.cBoundary(&local, 0, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n        pub const emitZeroModeFactor = noopZeroModeFactor;\\n        pub const emitZeroModeBaseEnd = noopZeroModeBaseEnd;\\n\\n        wick_terms: usize = 0,\\n\\n        /// emitWickTermStart counts one streamed primitive contraction.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n    };\\n\\n    var full_sink = Sink{};\\n    try FullGhostDisk.correlator(&FullGhostDisk.config.disk, ops, &full_sink);\\n    try testing.expectEqual(@as(usize, 1), full_sink.wick_terms);\\n\\n    var boundary_only_sink = Sink{};\\n    try BoundaryOnlyGhost.correlator(&BoundaryOnlyGhost.config.disk, ops, &boundary_only_sink);\\n    try testing.expectEqual(@as(usize, 0), boundary_only_sink.wick_terms);\\n}\\n\\ntest \\"free fermion sphere uses generic fermionic Wick signs\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Psi = freeFermionSphere(.{ .dimension = 10 });\\n\\n    var local = try Psi.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const rho = try local.index(\\"rho\\");\\n    const sigma = try local.index(\\"sigma\\");\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const ops = try local.ops(.{\\n        try Psi.op.psi(&local, mu, 0, z1),\\n        try Psi.op.psi(&local, nu, 0, z2),\\n        try Psi.op.psi(&local, rho, 0, z3),\\n        try Psi.op.psi(&local, sigma, 0, z4),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n        pub const emitZeroModeFactor = noopZeroModeFactor;\\n        pub const emitZeroModeBaseEnd = noopZeroModeBaseEnd;\\n\\n        branch_count: usize = 0,\\n        primitive_count: usize = 0,\\n        negative_branch_count: usize = 0,\\n        pending_negative: bool = false,\\n\\n        /// emitWickBranchSign records signs supplied by the generic branch walker.\\n        pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n            if (sign < 0) self.pending_negative = true;\\n        }\\n\\n        /// emitWickTermStart records the first primitive term of each Wick branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.primitive_count += 1;\\n            if ((self.primitive_count % 2) == 1) {\\n                self.branch_count += 1;\\n                if (self.pending_negative) {\\n                    self.negative_branch_count += 1;\\n                    self.pending_negative = false;\\n                }\\n            }\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Psi.correlator(&Psi.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 3), sink.branch_count);\\n    try testing.expectEqual(@as(usize, 6), sink.primitive_count);\\n    try testing.expectEqual(@as(usize, 1), sink.negative_branch_count);\\n    try testing.expect(!@hasDecl(@TypeOf(Psi.config.sphere), \\"fermion_kinds\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(Psi.config.sphere), \\"pair_lookup\\"));\\n}\\n\\ntest \\"eta-xi sphere saturates one xi zero mode\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const EtaXiPreset = etaXiSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try EtaXiPreset.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const xi = try EtaXiPreset.op.xi(&local, 0, z);\\n\\n    const SaturatedSink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        zero_mode_count: usize = 0,\\n        base_count: usize = 0,\\n        coordinate: ?shared.Handle.Coord = null,\\n\\n        /// emitZeroModeFactor records the single xi constant-mode factor.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .eta_xi_zero_mode => |zero| {\\n                    try testing.expectEqual(.sphere_holomorphic, zero.support);\\n                    try testing.expectEqual(@as(u8, 0), zero.xi.derivative_order);\\n                    self.zero_mode_count += 1;\\n                    self.coordinate = @as(shared.Handle.Coord, @enumFromInt(zero.xi.coordinate));\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records a completed zero-mode base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_count += 1;\\n        }\\n    };\\n\\n    var saturated_sink = SaturatedSink{};\\n    const saturated_ops = try local.ops(.{xi});\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.sphere, saturated_ops, &saturated_sink);\\n    try testing.expectEqual(@as(usize, 1), saturated_sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 1), saturated_sink.base_count);\\n    try testing.expectEqual(z, saturated_sink.coordinate.?);\\n\\n    var derivative_local = try EtaXiPreset.local(testing.allocator);\\n    defer derivative_local.deinit();\\n    const zd = try derivative_local.coord(\\"z\\");\\n    const xi_derivative = try EtaXiPreset.op.xi(&derivative_local, 1, zd);\\n    var rejected_sink = SaturatedSink{};\\n    const rejected_ops = try derivative_local.ops(.{xi_derivative});\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.sphere, rejected_ops, &rejected_sink);\\n    try testing.expectEqual(@as(usize, 0), rejected_sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 0), rejected_sink.base_count);\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.sphere), \\"zero_modes\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.sphere), \\"fermion_kinds\\"));\\n}\\n\\ntest \\"eta-xi torus lowers prime-form log-derivative Wick and xi zero mode\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const EtaXiPreset = etaXiSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try EtaXiPreset.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z_eta = try local.coord(\\"z_eta\\");\\n    const z_xi_a = try local.coord(\\"z_xi_a\\");\\n    const z_xi_b = try local.coord(\\"z_xi_b\\");\\n    const ops = try local.ops(.{\\n        try EtaXiPreset.op.eta(&local, 0, z_eta),\\n        try EtaXiPreset.op.xi(&local, 0, z_xi_a),\\n        try EtaXiPreset.op.xi(&local, 0, z_xi_b),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        term_count: usize = 0,\\n        kernel_count: usize = 0,\\n        zero_mode_count: usize = 0,\\n        base_count: usize = 0,\\n\\n        /// emitWickTermStart records one eta-xi torus primitive contraction.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickCoordinate requires the elliptic prime-form log derivative.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            switch (factor.kernel) {\\n                .green_kernel => |kernel| {\\n                    try testing.expectEqualStrings(\\"elliptic_prime_form_log_derivative\\", kernel.name);\\n                    try testing.expectEqual(@as(u8, 0), kernel.left_derivatives);\\n                    try testing.expectEqual(@as(u8, 0), kernel.right_derivatives);\\n                    self.kernel_count += 1;\\n                },\\n                else => return error.UnexpectedTorusKernel,\\n            }\\n        }\\n\\n        /// emitZeroModeFactor requires the torus holomorphic xi zero mode.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .eta_xi_zero_mode => |zero| {\\n                    try testing.expectEqual(.torus_holomorphic, zero.support);\\n                    try testing.expectEqual(@as(u8, 0), zero.xi.derivative_order);\\n                    self.zero_mode_count += 1;\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records one successful torus base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.torus, ops, &sink);\\n    try testing.expectEqual(@as(usize, 2), sink.term_count);\\n    try testing.expectEqual(@as(usize, 2), sink.kernel_count);\\n    try testing.expectEqual(@as(usize, 2), sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 2), sink.base_count);\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.torus), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.torus), \\"zero_modes\\"));\\n}\\n\\ntest \\"compact text sink inspects a small correlator without custom callbacks\\" {\\n    const testing = @import(\\"std\\").testing;\\n    const Buffer = struct {\\n        bytes: [512]u8 = undefined,\\n        len: usize = 0,\\n\\n        pub fn writeAll(self: *@This(), data: []const u8) !void {\\n            if (self.len + data.len > self.bytes.len) return error.NoSpaceLeft;\\n            @memcpy(self.bytes[self.len..][0..data.len], data);\\n            self.len += data.len;\\n        }\\n\\n        fn slice(self: *const @This()) []const u8 {\\n            return self.bytes[0..self.len];\\n        }\\n    };\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n        try X.op.dX(&local, nu, 0, w),\\n    });\\n\\n    var buffer = Buffer{};\\n    var text = X.text.compact(&buffer, .small);\\n    try X.correlator(&X.config.sphere, ops, &text);\\n    try testing.expect(@import(\\"std\\").mem.indexOf(u8, buffer.slice(), \\"eta(\\") != null);\\n}\\n\\ntest \\"bc zero modes consume c jets by top-form saturation\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.c(&local, 0, z1),\\n        try Ghost.op.c(&local, 1, z2),\\n        try Ghost.op.c(&local, 2, z3),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        factor_count: usize = 0,\\n        base_end_count: usize = 0,\\n        derivative_sum: u16 = 0,\\n\\n        /// emitZeroModeFactor records the top-form factor emitted by the base case.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            self.factor_count += 1;\\n            switch (factor) {\\n                .bc_top_form => |top| {\\n                    if (top.support != .sphere_holomorphic) return error.UnexpectedBcSupport;\\n                    self.derivative_sum = @as(u16, top.jets[0].derivative_order) + top.jets[1].derivative_order + top.jets[2].derivative_order;\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that every residual insertion was consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.factor_count);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n    try testing.expectEqual(@as(u16, 3), sink.derivative_sum);\\n}\\n\\ntest \\"disk bc zero modes use one doubled chiral top form\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{});\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const Disk = boundary(Ghost, .{GhostBoundary});\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try Disk.op.bulk.c(&local, 0, z),\\n        try Disk.op.bulk.ct(&local, 0, zbar),\\n        try @TypeOf(GhostBoundary).op.cBoundary(&local, 0, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        saw_disk_doubled: bool = false,\\n\\n        /// emitZeroModeFactor records whether the doubled disk top form was emitted.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => |top| self.saw_disk_doubled = top.support == .disk_doubled,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd accepts the successful zero-mode base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expect(sink.saw_disk_doubled);\\n}\\n\\ntest \\"free boson zero modes emit momentum delta and profile presentations\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const f0 = try local.targetFunction(\\"f\\");\\n    const fhat = try local.fourierTransform(\\"fhat\\");\\n    const coeff = try local.profileCoefficient(\\"a\\");\\n    const point = try local.targetPoint(\\"x0\\");\\n    const position_profile = X.profile.positionSpace(f0);\\n    const fourier_profile = X.profile.fourier(fhat);\\n    const polynomial_profile = X.profile.polynomialRnc(point, .{.{ coeff, 2 }});\\n    const ops = try local.ops(.{\\n        try X.op.expX(&local, k, z, zbar),\\n        try X.op.profile(&local, position_profile, z, zbar),\\n        try X.op.profile(&local, fourier_profile, z, zbar),\\n        try X.op.profile(&local, polynomial_profile, z, zbar),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        delta_count: usize = 0,\\n        gaussian_count: usize = 0,\\n        fourier_count: usize = 0,\\n        polynomial_count: usize = 0,\\n        base_end_count: usize = 0,\\n        two_pi_power: u16 = 0,\\n\\n        /// emitZeroModeFactor records each free-boson zero-mode factor by presentation.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => |delta| {\\n                    self.delta_count += 1;\\n                    self.two_pi_power = delta.two_pi_power;\\n                    if (delta.projector != null or delta.momenta.len != 1) return error.UnexpectedMomentumDelta;\\n                },\\n                .profile_gaussian_differential => self.gaussian_count += 1,\\n                .profile_fourier_integral => self.fourier_count += 1,\\n                .profile_polynomial_integral => self.polynomial_count += 1,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that all profile and plane-wave residuals were consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.delta_count);\\n    try testing.expectEqual(@as(u16, 10), sink.two_pi_power);\\n    try testing.expectEqual(@as(usize, 1), sink.gaussian_count);\\n    try testing.expectEqual(@as(usize, 1), sink.fourier_count);\\n    try testing.expectEqual(@as(usize, 1), sink.polynomial_count);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n}\\n\\ntest \\"disk free boson zero modes emit Neumann delta and Dirichlet phase\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const Disk = boundary(product(.{X}), .{XBoundary});\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try @TypeOf(XBoundary).op.expXBoundary(&local, k, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        delta_count: usize = 0,\\n        phase_count: usize = 0,\\n        base_end_count: usize = 0,\\n        two_pi_power: u16 = 0,\\n        saw_disk_scalar: bool = false,\\n        saw_projector: bool = false,\\n        saw_position: bool = false,\\n\\n        /// emitZeroModeFactor records Neumann momentum conservation and Dirichlet phase data.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => |delta| {\\n                    self.delta_count += 1;\\n                    self.two_pi_power = delta.two_pi_power;\\n                    self.saw_projector = delta.projector != null;\\n                    switch (delta.scalar) {\\n                        .monomial => |monomial| self.saw_disk_scalar = monomial.atom != null and monomial.atom_power == 1,\\n                        else => return error.UnexpectedDiskNormalization,\\n                    }\\n                    if (delta.momenta.len != 1) return error.UnexpectedMomentumDelta;\\n                },\\n                .dirichlet_phase => |phase| {\\n                    self.phase_count += 1;\\n                    self.saw_position = phase.momenta.len == 1;\\n                    switch (phase.position) {\\n                        .target_point => {},\\n                        else => return error.UnexpectedDirichletPosition,\\n                    }\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that the disk constant mode consumed the insertion.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.delta_count);\\n    try testing.expectEqual(@as(usize, 1), sink.phase_count);\\n    try testing.expectEqual(@as(u16, 4), sink.two_pi_power);\\n    try testing.expect(sink.saw_disk_scalar);\\n    try testing.expect(sink.saw_projector);\\n    try testing.expect(sink.saw_position);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n}\\n\\ntest \\"unconsumed free boson derivative kills the zero-mode base case\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const z = try local.coord(\\"z\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        base_end_count: usize = 0,\\n\\n        /// emitZeroModeFactor fails if an unconsumed derivative field emits a zero-mode factor.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedZeroModeFactor;\\n        }\\n\\n        /// emitZeroModeBaseEnd fails because the derivative residual should not be consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 0), sink.base_end_count);\\n}\\n\\ntest \\"pair Wick streams preset scalar atoms through correlator\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const preset_alpha = scalars.atom(\\"free_boson\\", \\"alpha_prime\\");\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n        try X.op.dX(&local, nu, 0, w),\\n    });\\n\\n    switch (scalars.rational(2, 4)) {\\n        .rational => |value| {\\n            try testing.expectEqual(@as(i64, 1), value.numerator);\\n            try testing.expectEqual(@as(i64, 2), value.denominator);\\n        },\\n        else => return error.UnexpectedScalarShape,\\n    }\\n\\n    const Sink = struct {\\n        expected_alpha: @TypeOf(preset_alpha),\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n\\n        /// emitWickTermStart accepts the primitive free-boson contraction.\\n        pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickScalar checks the configured alpha-prime atom.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 2) return error.UnexpectedScalarFactor;\\n                        if (monomial.imaginary_power != 0 or monomial.atom_power != 1) return error.UnexpectedScalarFactor;\\n                        try testing.expectEqual(self.expected_alpha, monomial.atom.?);\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarShape,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the differentiated two-point pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.exponent != -2) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor accepts the contracted target-space tensor.\\n        pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitWickTermEnd accepts the completed primitive contraction.\\n        pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n        /// emitZeroModeFactor accepts the empty free-boson constant mode.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the full contraction base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{ .expected_alpha = preset_alpha };\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n}\\n\\ntest \\"pair Wick resolves derivative action on a vector profile\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const wbar = try local.coord(\\"wbar\\");\\n    const f0 = try local.targetFunction(\\"f\\");\\n    const f = X.profile.positionSpace(f0);\\n\\n    const dx = try X.op.dX(&local, mu, 0, z);\\n    const profile = try X.op.profileVector(&local, f, nu, w, wbar);\\n    const ops = try local.ops(.{ dx, profile });\\n\\n    const Sink = struct {\\n        term_count: usize = 0,\\n        scalar_count: usize = 0,\\n        coordinate_count: usize = 0,\\n        tensor_count: usize = 0,\\n        action_count: usize = 0,\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n        saw_tensor_none: bool = false,\\n        saw_action: bool = false,\\n        expected_z: u32,\\n        expected_w: u32,\\n        expected_profile: u32,\\n        expected_index: u32,\\n\\n        /// emitWickTermStart records the single primitive profile Wick term.\\n        pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n            if (event.left_index != 0 or event.right_index != 1 or event.term_index != 0) return error.UnexpectedWickTermEvent;\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar checks the expected free-boson profile scalar.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            self.scalar_count += 1;\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 2 or monomial.atom == null) return error.UnexpectedScalarFactor;\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarFactor,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the resolved profile pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            self.coordinate_count += 1;\\n            if (factor.scalar.numerator != 1 or factor.scalar.denominator != 1) return error.UnexpectedCoordinateScalar;\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.coordinate.left != self.expected_z or power.coordinate.right != self.expected_w or power.exponent != -1) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor checks that no tensor numerator is emitted.\\n        pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n            self.tensor_count += 1;\\n            switch (factor) {\\n                .none => self.saw_tensor_none = true,\\n                else => return error.UnexpectedTensorFactor,\\n            }\\n        }\\n\\n        /// emitWickAction checks the profile-derivative action.\\n        pub fn emitWickAction(self: *@This(), factor: anytype) !void {\\n            self.action_count += 1;\\n            switch (factor) {\\n                .profile_derivative => |action| {\\n                    switch (action.profile) {\\n                        .symbol => |value| if (value != self.expected_profile) return error.UnexpectedProfileLabel,\\n                        else => return error.UnexpectedProfileLabel,\\n                    }\\n                    switch (action.index) {\\n                        .symbol => |value| if (value != self.expected_index) return error.UnexpectedIndexLabel,\\n                        else => return error.UnexpectedIndexLabel,\\n                    }\\n                    self.saw_action = true;\\n                },\\n                else => return error.UnexpectedProfileAction,\\n            }\\n        }\\n\\n        /// emitWickTermEnd accepts the completed primitive term.\\n        pub fn emitWickTermEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving profile zero-mode factor.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the successful residual base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{\\n        .expected_z = @intFromEnum(z),\\n        .expected_w = @intFromEnum(w),\\n        .expected_profile = @intFromEnum(f),\\n        .expected_index = @intFromEnum(mu),\\n    };\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.term_count);\\n    try testing.expectEqual(@as(usize, 1), sink.scalar_count);\\n    try testing.expectEqual(@as(usize, 1), sink.coordinate_count);\\n    try testing.expectEqual(@as(usize, 1), sink.tensor_count);\\n    try testing.expectEqual(@as(usize, 1), sink.action_count);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n    try testing.expect(sink.saw_tensor_none);\\n    try testing.expect(sink.saw_action);\\n\\n    const coeff = try local.profileCoefficient(\\"a\\");\\n    const x0 = try local.targetPoint(\\"x0\\");\\n    const polynomial_linear = X.profile.polynomialRnc(x0, .{.{ coeff, 1 }});\\n    const polynomial_quadratic = X.profile.polynomialRnc(x0, .{.{ coeff, 2 }});\\n    try testing.expect(@intFromEnum(polynomial_linear) != @intFromEnum(polynomial_quadratic));\\n}\\n\\ntest \\"pair Wick resolves config-backed boundary projectors\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const Disk = boundary(product(.{X}), .{XBoundary});\\n    const neumann = X.target.subspace(&.{ 0, 1, 2, 3 });\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const y2 = try local.boundaryCoord(\\"y2\\");\\n    const left = try @TypeOf(XBoundary).op.dXBoundary(&local, mu, 0, y);\\n    const right = try @TypeOf(XBoundary).op.dXBoundary(&local, nu, 1, y2);\\n    const ops = try local.ops(.{ left, right });\\n\\n    const Sink = struct {\\n        term_count: usize = 0,\\n        scalar_count: usize = 0,\\n        coordinate_count: usize = 0,\\n        tensor_count: usize = 0,\\n        action_count: usize = 0,\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n        saw_projector: bool = false,\\n        expected_y: u32,\\n        expected_y2: u32,\\n        expected_neumann: u32,\\n\\n        /// emitWickTermStart records the single boundary Wick term.\\n        pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n            if (event.left_index != 0 or event.right_index != 1 or event.term_index != 0) return error.UnexpectedWickTermEvent;\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar checks the boundary free-boson scalar factor.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            self.scalar_count += 1;\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 1 or monomial.atom == null) return error.UnexpectedScalarFactor;\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarFactor,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the differentiated boundary pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            self.coordinate_count += 1;\\n            if (factor.scalar.numerator != 2 or factor.scalar.denominator != 1) return error.UnexpectedCoordinateScalar;\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.coordinate.left != self.expected_y or power.coordinate.right != self.expected_y2 or power.exponent != -3) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor checks that the Neumann projector resolves from config.\\n        pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n            self.tensor_count += 1;\\n            switch (factor) {\\n                .projector_metric => |projector_metric| switch (projector_metric.projector) {\\n                    .config => |value| switch (value) {\\n                        .tensor_projector => |projector| {\\n                            if (@intFromEnum(projector) != self.expected_neumann) return error.UnexpectedProjectorConfig;\\n                            self.saw_projector = true;\\n                        },\\n                        else => return error.UnexpectedProjectorConfig,\\n                    },\\n                    else => return error.UnexpectedProjectorSource,\\n                },\\n                else => return error.UnexpectedTensorFactor,\\n            }\\n        }\\n\\n        /// emitWickAction counts unexpected profile actions.\\n        pub fn emitWickAction(self: *@This(), factor: anytype) !void {\\n            _ = factor;\\n            self.action_count += 1;\\n        }\\n\\n        /// emitWickTermEnd accepts the completed boundary term.\\n        pub fn emitWickTermEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n\\n        /// emitZeroModeFactor accepts disk empty-base normalization factors.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the successful full contraction base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{\\n        .expected_y = @intFromEnum(y),\\n        .expected_y2 = @intFromEnum(y2),\\n        .expected_neumann = @intFromEnum(neumann),\\n    };\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.term_count);\\n    try testing.expectEqual(@as(usize, 1), sink.scalar_count);\\n    try testing.expectEqual(@as(usize, 1), sink.coordinate_count);\\n    try testing.expectEqual(@as(usize, 1), sink.tensor_count);\\n    try testing.expectEqual(@as(usize, 0), sink.action_count);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n    try testing.expect(sink.saw_projector);\\n}\\ntest \\"recursive correlator enumerates free-boson pairings without dangling branches\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z1),\\n        try X.op.dX(&local, mu, 0, z2),\\n        try X.op.dX(&local, mu, 0, z3),\\n        try X.op.dX(&local, mu, 0, z4),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        wick_terms: usize = 0,\\n        zero_modes: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickTermStart counts one primitive contraction in an accepted branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor counts the free-boson constant-mode factor.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => self.zero_modes += 1,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 6), sink.wick_terms);\\n    try testing.expectEqual(@as(usize, 3), sink.zero_modes);\\n    try testing.expectEqual(@as(usize, 3), sink.base_cases);\\n}\\n\\ntest \\"recursive correlator leaves bc top-form residuals after all b contractions\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const z5 = try local.coord(\\"z5\\");\\n    const z6 = try local.coord(\\"z6\\");\\n    const z7 = try local.coord(\\"z7\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.b(&local, 0, z1),\\n        try Ghost.op.b(&local, 0, z2),\\n        try Ghost.op.c(&local, 0, z3),\\n        try Ghost.op.c(&local, 0, z4),\\n        try Ghost.op.c(&local, 0, z5),\\n        try Ghost.op.c(&local, 0, z6),\\n        try Ghost.op.c(&local, 0, z7),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        wick_terms: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickTermStart counts one b-c contraction in an accepted branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving c-zero-mode top form.\\n        pub fn emitZeroModeFactor(_: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => {},\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 40), sink.wick_terms);\\n    try testing.expectEqual(@as(usize, 20), sink.base_cases);\\n}\\n\\ntest \\"recursive correlator emits fermion signs for bc pair crossings\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const z5 = try local.coord(\\"z5\\");\\n    const z6 = try local.coord(\\"z6\\");\\n    const z7 = try local.coord(\\"z7\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.b(&local, 0, z1),\\n        try Ghost.op.b(&local, 0, z2),\\n        try Ghost.op.c(&local, 0, z3),\\n        try Ghost.op.c(&local, 0, z4),\\n        try Ghost.op.c(&local, 0, z5),\\n        try Ghost.op.c(&local, 0, z6),\\n        try Ghost.op.c(&local, 0, z7),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        negative_signs: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickBranchSign records negative fermion branch signs.\\n        pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n            if (sign != -1) return error.UnexpectedBranchSign;\\n            self.negative_signs += 1;\\n        }\\n\\n        /// emitWickTermStart accepts a primitive contraction.\\n        pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving c-zero-mode top form.\\n        pub fn emitZeroModeFactor(_: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => {},\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 10), sink.negative_signs);\\n    try testing.expectEqual(@as(usize, 20), sink.base_cases);\\n}\\n"},{"id":"20260606T183100.000000","title":"Preset Shared Runtime","tags":null,"content":"This node contains the small runtime vocabulary shared by the preset\\nconstructors in Free Boson Preset,\\nbc Preset, and\\nPreset Composition.  It deliberately does not define a\\nspecific CFT.  It gives preset authors compact handles, local-operator\\nconstruction, Wick rule templates, zero-mode rule templates, and the first\\nstreaming correlator driver.\\nThe data path is:\\nA user creates typed handles with Local.\\nPreset-owned operator builders choose their own operator kind ids and store\\n   only compact labels plus insertion data in a CFT Kernel MultiOp.\\nA preset supplies a CorrelatorConfig containing Wick and zero-mode rules.\\nThe streaming driver emits matched rules to a sink without building the full\\n   correlator expression in memory.\\nThe compact text sink is only an inspection adapter for small cases.  It keeps\\nexplicit byte and branch limits and reuses the streaming events instead of\\nmaterializing a symbolic expression tree.\\nconst std = @import(\\"std\\");\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst coefficient = @import(\\"../expressions/coefficient.zig\\");\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\nconst theory = @import(\\"../theory/theory.zig\\");\\nconst wick_correlator = @import(\\"../correlators/wick.zig\\");\\nconst KernelOperatorList = kernel.Call.MultiOp;\\n\\n/// LocalOperator is an opaque token for one operator insertion.\\npub const LocalOperator = opaque {};\\n\\n/// OperatorList is an opaque frozen operator sequence for correlator calls.\\npub const OperatorList = opaque {};\\n\\n/// Handle groups compact ids used by preset operator builders.\\npub const Handle = struct {\\n    /// Coord names a holomorphic or antiholomorphic coordinate variable.\\n    pub const Coord = enum(u32) { _ };\\n    /// BoundaryCoord names a real boundary coordinate variable.\\n    pub const BoundaryCoord = enum(u32) { _ };\\n    /// Index names a target-space vector index.\\n    pub const Index = enum(u32) { _ };\\n    /// Momentum names a target-space momentum expression.\\n    pub const Momentum = enum(u32) { _ };\\n    /// Profile names a selected presentation of a target-space profile.\\n    pub const Profile = enum(u32) { _ };\\n    /// TargetFunction names a position-space target profile.\\n    pub const TargetFunction = enum(u32) { _ };\\n    /// FourierTransform names a Fourier-space target profile.\\n    pub const FourierTransform = enum(u32) { _ };\\n    /// TargetPoint names a point in target space.\\n    pub const TargetPoint = enum(u32) { _ };\\n    /// ProfileCoefficient names one coefficient in a polynomial profile.\\n    pub const ProfileCoefficient = enum(u32) { _ };\\n    /// TensorProjector names a tensor-code projector expression.\\n    pub const TensorProjector = enum(u32) { _ };\\n    /// BoundaryStack names stacked boundary-condition Chan-Paton data.\\n    pub const BoundaryStack = enum(u32) { _ };\\n    /// ProfilePresentation records how a profile handle should be lowered.\\n    pub const ProfilePresentation = enum(u2) {\\n        position_space,\\n        fourier,\\n        polynomial_rnc,\\n    };\\n};\\n\\nfn rawToken(value: anytype) u32 {\\n    return @intFromEnum(value);\\n}\\n\\nfn token(comptime T: type, id: u32) T {\\n    return @enumFromInt(if (id == 0) 1 else id);\\n}\\n\\nconst profile_tag_shift = 30;\\nconst profile_payload_mask: u32 = 0x3fff_ffff;\\n\\n/// profileHandle packs a profile presentation tag with a compact payload id.\\npub fn profileHandle(comptime presentation: Handle.ProfilePresentation, payload: u32) Handle.Profile {\\n    const tag = @as(u32, @intFromEnum(presentation)) << profile_tag_shift;\\n    return token(Handle.Profile, tag | (payload & profile_payload_mask));\\n}\\n\\nfn profilePresentation(profile: Handle.Profile) Handle.ProfilePresentation {\\n    return @enumFromInt(@intFromEnum(profile) >> profile_tag_shift);\\n}\\n\\nfn profilePayload(profile: Handle.Profile) u32 {\\n    return @intFromEnum(profile) & profile_payload_mask;\\n}\\n\\nfn stableId(comptime namespace: []const u8, comptime name: []const u8) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (namespace) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    hash = (hash ^ @as(u32, \':\')) *% 16777619;\\n    inline for (name) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\n/// configRef derives a stable id for correlator configuration data.\\nfn configRef(comptime namespace: []const u8, comptime name: []const u8) ConfigRef {\\n    return stableId(namespace, name);\\n}\\n\\nconst ScalarAtom = u32;\\n\\nconst ConfigRef = u32;\\n\\nconst RationalScalar = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\nconst ScalarMonomial = struct {\\n    rational: RationalScalar = .{ .numerator = 1, .denominator = 1 },\\n    imaginary_power: u2 = 0,\\n    atom: ?ScalarAtom = null,\\n    atom_power: i8 = 0,\\n};\\n\\nconst RuleScalar = union(enum) {\\n    one,\\n    rational: RationalScalar,\\n    monomial: ScalarMonomial,\\n};\\n\\n/// scalars exposes basic algebra for compact structured rule scalars.\\nconst scalars = ScalarDsl;\\n\\nconst ScalarDsl = struct {\\n    fn absInt(value: i64) u64 {\\n        if (value == std.math.minInt(i64)) return @as(u64, 1) << 63;\\n        return if (value < 0) @intCast(-value) else @intCast(value);\\n    }\\n\\n    fn gcd(first: u64, second: u64) u64 {\\n        var a = first;\\n        var b = second;\\n        while (b != 0) {\\n            const rem = a % b;\\n            a = b;\\n            b = rem;\\n        }\\n        return if (a == 0) 1 else a;\\n    }\\n\\n    fn rationalValue(comptime numerator: i64, comptime denominator: i64) RationalScalar {\\n        if (denominator == 0) @compileError(\\"rational denominator cannot be zero\\");\\n        var num = numerator;\\n        var den = denominator;\\n        if (den < 0) {\\n            num = -num;\\n            den = -den;\\n        }\\n        const factor: i64 = @intCast(gcd(absInt(num), absInt(den)));\\n        return .{\\n            .numerator = @divExact(num, factor),\\n            .denominator = @divExact(den, factor),\\n        };\\n    }\\n\\n    /// atom derives a stable scalar atom id from a preset namespace and atom name.\\n    pub fn atom(comptime namespace: []const u8, comptime name: []const u8) ScalarAtom {\\n        return stableId(namespace, name);\\n    }\\n\\n    /// one constructs the multiplicative unit scalar.\\n    pub fn one() RuleScalar {\\n        return .one;\\n    }\\n\\n    /// rational constructs an exact rational scalar.\\n    pub fn rational(comptime numerator: i64, comptime denominator: i64) RuleScalar {\\n        return .{ .rational = rationalValue(numerator, denominator) };\\n    }\\n\\n    /// atomScalar constructs a scalar consisting of one named atom.\\n    pub fn atomScalar(comptime atom_id: ScalarAtom) RuleScalar {\\n        return monomial(1, 1, 0, atom_id, 1);\\n    }\\n\\n    /// monomial constructs a rational times i-power times atom-power scalar.\\n    pub fn monomial(comptime numerator: i64, comptime denominator: i64, comptime imaginary_power: u2, comptime atom_id: ?ScalarAtom, comptime atom_power: i8) RuleScalar {\\n        return .{ .monomial = .{\\n            .rational = rationalValue(numerator, denominator),\\n            .imaginary_power = imaginary_power,\\n            .atom = atom_id,\\n            .atom_power = atom_power,\\n        } };\\n    }\\n\\n    fn monomialFrom(comptime value: RuleScalar) ScalarMonomial {\\n        return switch (value) {\\n            .one => .{},\\n            .rational => |r| .{ .rational = r },\\n            .monomial => |m| m,\\n        };\\n    }\\n\\n    /// scale multiplies a scalar by an exact rational number.\\n    pub fn scale(comptime value: RuleScalar, comptime numerator: i64, comptime denominator: i64) RuleScalar {\\n        var factor = monomialFrom(value);\\n        factor.rational = rationalValue(factor.rational.numerator * numerator, factor.rational.denominator * denominator);\\n        return .{ .monomial = factor };\\n    }\\n\\n    /// neg negates a scalar.\\n    pub fn neg(comptime value: RuleScalar) RuleScalar {\\n        return scale(value, -1, 1);\\n    }\\n\\n    /// div divides a scalar by an integer denominator.\\n    pub fn div(comptime value: RuleScalar, comptime denominator: i64) RuleScalar {\\n        return scale(value, 1, denominator);\\n    }\\n\\n    /// i multiplies a scalar by the imaginary unit.\\n    pub fn i(comptime value: RuleScalar) RuleScalar {\\n        var factor = monomialFrom(value);\\n        factor.imaginary_power +%= 1;\\n        return .{ .monomial = factor };\\n    }\\n};\\n\\n/// familyKind derives a compact operator kind id from a preset namespace and local family enum.\\nfn familyKind(comptime namespace: []const u8, comptime family: anytype) operators.OperatorKindId {\\n    return stableId(namespace, @tagName(family));\\n}\\n\\n/// sectorId derives a compact zero-mode sector id from a preset namespace and local sector enum.\\nfn sectorId(comptime namespace: []const u8, comptime sector: anytype) u32 {\\n    return stableId(namespace, @tagName(sector));\\n}\\n\\nconst LocalStateImpl = struct {\\n    allocator: std.mem.Allocator,\\n    labels: std.ArrayList(kernel.Call.LabelValue) = .empty,\\n    operators_list: std.ArrayList(kernel.Call.LocalOp) = .empty,\\n    pending_operators: std.ArrayList(*LocalOperatorImpl) = .empty,\\n    owned_labels: []kernel.Call.LabelValue = &.{},\\n    owned_operators: []kernel.Call.LocalOp = &.{},\\n    label_store: kernel.Call.LabelStore = .{ .values = &.{} },\\n    next_symbol: u32 = 1,\\n    next_normal_order_group: normal_ordering.Group = normal_ordering.first,\\n    finished: bool = false,\\n};\\n\\nconst LocalOperatorImpl = struct {\\n    owner: *LocalStateImpl,\\n    item: kernel.Call.LocalOp,\\n};\\n\\n/// Local builds typed handles and freezes operator lists for correlator calls.\\npub const Local = opaque {\\n    /// init constructs an empty local-operator builder.\\n    pub fn init(allocator: std.mem.Allocator) !*Local {\\n        const state = try allocator.create(LocalStateImpl);\\n        state.* = .{ .allocator = allocator };\\n        return @ptrCast(state);\\n    }\\n\\n    fn data(self: *Local) *LocalStateImpl {\\n        return @ptrCast(@alignCast(self));\\n    }\\n\\n    /// deinit releases memory owned by this local builder and its frozen operator list.\\n    pub fn deinit(self: *Local) void {\\n        const state = self.data();\\n        const allocator = state.allocator;\\n        if (state.finished) {\\n            allocator.free(state.owned_labels);\\n            allocator.free(state.owned_operators);\\n        } else {\\n            state.labels.deinit(allocator);\\n            state.operators_list.deinit(allocator);\\n        }\\n        for (state.pending_operators.items) |pending| {\\n            allocator.destroy(pending);\\n        }\\n        state.pending_operators.deinit(allocator);\\n        allocator.destroy(state);\\n    }\\n\\n    fn next(self: *Local) u32 {\\n        const state = self.data();\\n        const value = state.next_symbol;\\n        state.next_symbol += 1;\\n        return value;\\n    }\\n\\n    /// coord creates a named bulk coordinate token.\\n    pub fn coord(self: *Local, name: []const u8) !Handle.Coord {\\n        _ = name;\\n        return token(Handle.Coord, self.next());\\n    }\\n\\n    /// boundaryCoord creates a named boundary coordinate token.\\n    pub fn boundaryCoord(self: *Local, name: []const u8) !Handle.BoundaryCoord {\\n        _ = name;\\n        return token(Handle.BoundaryCoord, self.next());\\n    }\\n\\n    /// index creates a target-space index token.\\n    pub fn index(self: *Local, name: []const u8) !Handle.Index {\\n        _ = name;\\n        return token(Handle.Index, self.next());\\n    }\\n\\n    /// momentum creates a target-space momentum token.\\n    pub fn momentum(self: *Local, name: []const u8) !Handle.Momentum {\\n        _ = name;\\n        return token(Handle.Momentum, self.next());\\n    }\\n\\n    /// targetFunction creates a position-space target-profile token.\\n    pub fn targetFunction(self: *Local, name: []const u8) !Handle.TargetFunction {\\n        _ = name;\\n        return token(Handle.TargetFunction, self.next());\\n    }\\n\\n    /// fourierTransform creates a Fourier-space profile token.\\n    pub fn fourierTransform(self: *Local, name: []const u8) !Handle.FourierTransform {\\n        _ = name;\\n        return token(Handle.FourierTransform, self.next());\\n    }\\n\\n    /// targetPoint creates a target-space point token.\\n    pub fn targetPoint(self: *Local, name: []const u8) !Handle.TargetPoint {\\n        _ = name;\\n        return token(Handle.TargetPoint, self.next());\\n    }\\n\\n    /// profileCoefficient creates a polynomial-profile coefficient token.\\n    pub fn profileCoefficient(self: *Local, name: []const u8) !Handle.ProfileCoefficient {\\n        _ = name;\\n        return token(Handle.ProfileCoefficient, self.next());\\n    }\\n\\n    fn labelSpan(self: *Local, values: []const kernel.Call.LabelValue) !theory.Id.LabelSpan {\\n        const state = self.data();\\n        const start = state.labels.items.len;\\n        try state.labels.appendSlice(state.allocator, values);\\n        return @intCast(start);\\n    }\\n\\n    fn symbol(value: anytype) kernel.Call.LabelValue {\\n        return .{ .symbol = rawToken(value) };\\n    }\\n\\n    fn op(self: *Local, kind: operators.OperatorKindId, insertion: operators.OperatorInsertion, values: []const kernel.Call.LabelValue) !*LocalOperator {\\n        const state = self.data();\\n        const pending = try state.allocator.create(LocalOperatorImpl);\\n        pending.* = .{\\n            .owner = state,\\n            .item = .{\\n            .insertion = insertion,\\n            .kind = kind,\\n            .labels = try self.labelSpan(values),\\n            },\\n        };\\n        try state.pending_operators.append(state.allocator, pending);\\n        return @ptrCast(pending);\\n    }\\n\\n    fn rawOperator(self: *Local, item: *LocalOperator) !kernel.Call.LocalOp {\\n        const state = self.data();\\n        const pending: *LocalOperatorImpl = @ptrCast(@alignCast(item));\\n        if (pending.owner != state) return error.InvalidLocalOperator;\\n        return pending.item;\\n    }\\n\\n    fn addRaw(self: *Local, item: kernel.Call.LocalOp) !void {\\n        const state = self.data();\\n        if (state.finished) return error.LocalAlreadyFinished;\\n        try state.operators_list.append(state.allocator, item);\\n    }\\n\\n    fn add(self: *Local, item: *LocalOperator) !void {\\n        try self.addRaw(try self.rawOperator(item));\\n    }\\n\\n    fn isLocalOperatorToken(comptime Item: type) bool {\\n        return switch (@typeInfo(Item)) {\\n            .pointer => |ptr| ptr.child == LocalOperator,\\n            else => false,\\n        };\\n    }\\n\\n    fn addItem(self: *Local, item: anytype) !void {\\n        if (comptime isLocalOperatorToken(@TypeOf(item))) {\\n            try self.add(item);\\n        } else {\\n            try self.addNormalOrdered(item);\\n        }\\n    }\\n\\n    fn addNormalOrdered(self: *Local, items: anytype) !void {\\n        const state = self.data();\\n        const group = try normal_ordering.next(&state.next_normal_order_group);\\n        inline for (items) |item| {\\n            try self.addRaw(normal_ordering.tag(try self.rawOperator(item), group));\\n        }\\n    }\\n\\n    /// ops freezes local operators; nested tuples are normal-ordered products.\\n    pub fn ops(self: *Local, items: anytype) !*OperatorList {\\n        inline for (items) |item| {\\n            try self.addItem(item);\\n        }\\n        return self.freeze();\\n    }\\n\\n    fn freeze(self: *Local) !*OperatorList {\\n        const state = self.data();\\n        if (state.finished) return error.LocalAlreadyFinished;\\n        state.owned_operators = try state.operators_list.toOwnedSlice(state.allocator);\\n        state.owned_labels = try state.labels.toOwnedSlice(state.allocator);\\n        state.label_store = .{ .values = state.owned_labels };\\n        state.finished = true;\\n        return @ptrCast(state);\\n    }\\n};\\n\\n/// wick exposes compact preset-author helpers for primitive Wick rule templates.\\nconst wick = WickDsl;\\n\\nconst WickDsl = struct {\\n    /// Support tells the evaluator which insertion coordinate slots it reads.\\n    pub const Support = enum {\\n        holomorphic,\\n        antiholomorphic,\\n        bulk_pair,\\n        boundary,\\n    };\\n\\n    /// Side selects the left or right operator in a Wick rule expression.\\n    pub const Side = enum { left, right };\\n\\n    /// CoordinateSlot selects the coordinate component of a matched insertion.\\n    pub const CoordinateSlot = enum { position, holomorphic, antiholomorphic };\\n\\n    /// CoordinateRef refers to one coordinate of one side of a Wick rule.\\n    pub const CoordinateRef = struct {\\n        side: Side,\\n        slot: CoordinateSlot,\\n    };\\n\\n    /// LabelRef refers to one runtime label of one side of a Wick rule.\\n    pub const LabelRef = struct {\\n        side: Side,\\n        slot: u8,\\n    };\\n\\n    /// ScalarFactor describes one non-coordinate scalar multiplier.\\n    pub const ScalarFactor = union(enum) {\\n        value: RuleScalar,\\n        label_bilinear_phase: struct { left: LabelRef, right: LabelRef, form: []const u8 },\\n        cocycle: struct { table: []const u8, left: LabelRef, right: LabelRef },\\n        spin_structure: []const u8,\\n    };\\n\\n    /// TensorRef identifies tensor data coming from labels or selected config data.\\n    pub const TensorRef = union(enum) {\\n        label: LabelRef,\\n        config: ConfigRef,\\n    };\\n\\n    /// TensorFactor describes one tensor multiplier in a Wick coefficient.\\n    pub const TensorFactor = union(enum) {\\n        none,\\n        metric: struct { left: LabelRef, right: LabelRef },\\n        momentum_index: struct { momentum: LabelRef, index: LabelRef },\\n        momentum_pair: struct { left: LabelRef, right: LabelRef },\\n        projector_metric: struct { projector: TensorRef, left: LabelRef, right: LabelRef },\\n        projector_momentum_index: struct { projector: TensorRef, momentum: LabelRef, index: LabelRef },\\n        projector_momentum_pair: struct { projector: TensorRef, left: LabelRef, right: LabelRef },\\n    };\\n\\n    /// ActionFactor describes a non-multiplicative action produced by a Wick contraction.\\n    pub const ActionFactor = union(enum) {\\n        profile_derivative: struct { profile: LabelRef, index: LabelRef },\\n        projected_profile_derivative: struct { projector: TensorRef, profile: LabelRef, index: LabelRef },\\n    };\\n\\n    /// CoordinateDifference is the coordinate difference read by a Wick kernel.\\n    pub const CoordinateDifference = struct {\\n        left: CoordinateRef,\\n        right: CoordinateRef,\\n    };\\n\\n    /// DerivativeAction tells resolution how insertion derivative labels act on a coordinate factor.\\n    pub const DerivativeAction = struct {\\n        include_left: bool = false,\\n        include_right: bool = false,\\n    };\\n\\n    /// CoordinateFactor describes one coordinate-dependent multiplier.\\n    pub const CoordinateFactor = union(enum) {\\n        difference_power: struct { coordinate: CoordinateDifference, exponent: i16, derivatives: DerivativeAction = .{} },\\n        logarithm: struct { coordinate: CoordinateDifference, derivatives: DerivativeAction = .{} },\\n        green_kernel: struct { name: []const u8, coordinate: CoordinateDifference, derivatives: DerivativeAction = .{} },\\n        green_exponential: CoordinateDifference,\\n    };\\n\\n    /// Term is one product of scalar, coordinate, tensor, and action factors.\\n    pub const Term = struct {\\n        scalars: []const ScalarFactor,\\n        coordinates: []const CoordinateFactor,\\n        tensors: []const TensorFactor,\\n        actions: []const ActionFactor = &.{},\\n        residuals: []const Side = &.{},\\n    };\\n\\n    /// Expr is a sum of Wick coefficient terms.\\n    pub const Expr = struct {\\n        terms: []const Term,\\n    };\\n\\n    const PoleExpr = struct {\\n        scalar: ScalarFactor,\\n        numerator: TensorFactor,\\n        coordinate: CoordinateDifference,\\n        exponent: i16,\\n    };\\n\\n    const GreenExponentialExpr = struct {\\n        scalar: ScalarFactor,\\n        momentum_pair: TensorFactor,\\n        coordinate: CoordinateDifference,\\n    };\\n\\n    /// Pattern is one side of a primitive Wick rule.\\n    pub const Pattern = struct {\\n        kind: operators.OperatorKindId,\\n        support: Support,\\n    };\\n\\n    /// Rule is one preset-authored primitive Wick rule.\\n    pub const Rule = struct {\\n        left: Pattern,\\n        right: Pattern,\\n        expr: Expr,\\n    };\\n\\n    /// rule constructs one primitive Wick rule template.\\n    pub fn rule(left: Pattern, right: Pattern, expression: Expr) Rule {\\n        return .{ .left = left, .right = right, .expr = expression };\\n    }\\n\\n    /// pattern constructs one Wick-rule side from a preset-owned kind id.\\n    pub fn pattern(comptime kind: operators.OperatorKindId, comptime support: Support) Pattern {\\n        return .{ .kind = kind, .support = support };\\n    }\\n\\n    /// coord refers to one coordinate slot of one Wick-rule side.\\n    pub fn coord(side: Side, slot: CoordinateSlot) CoordinateRef {\\n        return .{ .side = side, .slot = slot };\\n    }\\n\\n    /// label refers to one label slot of one Wick-rule side.\\n    pub fn label(side: Side, slot: u8) LabelRef {\\n        return .{ .side = side, .slot = slot };\\n    }\\n\\n    /// difference constructs a coordinate difference.\\n    pub fn difference(left: CoordinateRef, right: CoordinateRef) CoordinateDifference {\\n        return .{ .left = left, .right = right };\\n    }\\n\\n    /// scalar constructs a scalar factor from a rule scalar.\\n    pub fn scalar(comptime value: RuleScalar) ScalarFactor {\\n        return .{ .value = value };\\n    }\\n\\n    /// term constructs one product term in a Wick expression.\\n    pub fn term(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors };\\n    }\\n\\n    /// termWithActions constructs one Wick term that also carries operator actions.\\n    pub fn termWithActions(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor, comptime actions: []const ActionFactor) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors, .actions = actions };\\n    }\\n\\n    /// termWithResiduals constructs one Wick term with surviving input sides.\\n    pub fn termWithResiduals(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor, comptime actions: []const ActionFactor, comptime residuals: []const Side) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors, .actions = actions, .residuals = residuals };\\n    }\\n\\n    /// expr constructs a Wick expression from one or more terms.\\n    pub fn expr(comptime terms: []const Term) Expr {\\n        return .{ .terms = terms };\\n    }\\n\\n    /// profileDerivative constructs the action of a target derivative on a profile label.\\n    pub fn profileDerivative(comptime profile: LabelRef, comptime index: LabelRef) ActionFactor {\\n        return .{ .profile_derivative = .{ .profile = profile, .index = index } };\\n    }\\n\\n    /// projectedProfileDerivative constructs a projected target derivative on a profile label.\\n    pub fn projectedProfileDerivative(comptime projector: TensorRef, comptime profile: LabelRef, comptime index: LabelRef) ActionFactor {\\n        return .{ .projected_profile_derivative = .{ .projector = projector, .profile = profile, .index = index } };\\n    }\\n\\n    /// differencePower constructs a power of a coordinate difference.\\n    pub fn differencePower(comptime coordinate: CoordinateDifference, comptime exponent: i16) CoordinateFactor {\\n        return .{ .difference_power = .{ .coordinate = coordinate, .exponent = exponent } };\\n    }\\n\\n    /// differentiatedPower applies matched insertion derivative labels to a coordinate power.\\n    pub fn differentiatedPower(comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .difference_power = .{ .coordinate = coordinate, .exponent = exponent, .derivatives = derivatives } };\\n    }\\n\\n    /// logarithm constructs a logarithm of a coordinate difference.\\n    pub fn logarithm(comptime coordinate: CoordinateDifference) CoordinateFactor {\\n        return .{ .logarithm = .{ .coordinate = coordinate } };\\n    }\\n\\n    /// differentiatedLogarithm applies matched insertion derivative labels to a logarithm.\\n    pub fn differentiatedLogarithm(comptime coordinate: CoordinateDifference, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .logarithm = .{ .coordinate = coordinate, .derivatives = derivatives } };\\n    }\\n\\n    /// greenKernel constructs a named Green-kernel coordinate factor.\\n    pub fn greenKernel(comptime name: []const u8, comptime coordinate: CoordinateDifference) CoordinateFactor {\\n        return .{ .green_kernel = .{ .name = name, .coordinate = coordinate } };\\n    }\\n\\n    /// differentiatedGreenKernel records derivative labels acting on a named Green kernel.\\n    pub fn differentiatedGreenKernel(comptime name: []const u8, comptime coordinate: CoordinateDifference, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .green_kernel = .{ .name = name, .coordinate = coordinate, .derivatives = derivatives } };\\n    }\\n\\n    /// pole constructs a primitive pole coefficient.\\n    pub fn pole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16) Expr {\\n        const shape = PoleExpr{\\n            .scalar = scalar(scalar_value),\\n            .numerator = numerator,\\n            .coordinate = coordinate,\\n            .exponent = exponent,\\n        };\\n        return expr(&.{term(&.{shape.scalar}, &.{differencePower(shape.coordinate, shape.exponent)}, &.{shape.numerator})});\\n    }\\n\\n    /// differentiatedPole constructs a pole acted on by matched insertion derivatives.\\n    pub fn differentiatedPole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{term(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator})});\\n    }\\n\\n    /// differentiatedActionPole constructs a differentiated pole carrying profile or custom actions.\\n    pub fn differentiatedActionPole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime actions: []const ActionFactor) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithActions(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, actions)});\\n    }\\n\\n    /// differentiatedPoleWithResiduals constructs a differentiated pole with surviving sides.\\n    pub fn differentiatedPoleWithResiduals(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime residuals: []const Side) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithResiduals(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, &.{}, residuals)});\\n    }\\n\\n    /// differentiatedActionPoleWithResiduals constructs an action pole with surviving sides.\\n    pub fn differentiatedActionPoleWithResiduals(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime actions: []const ActionFactor, comptime residuals: []const Side) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithResiduals(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, actions, residuals)});\\n    }\\n\\n    /// greenExponential constructs a plane-wave Green-kernel factor.\\n    pub fn greenExponential(comptime scalar_value: RuleScalar, comptime momentum_pair: TensorFactor, comptime coordinate: CoordinateDifference) Expr {\\n        const shape = GreenExponentialExpr{\\n            .scalar = scalar(scalar_value),\\n            .momentum_pair = momentum_pair,\\n            .coordinate = coordinate,\\n        };\\n        return expr(&.{term(&.{shape.scalar}, &.{.{ .green_exponential = shape.coordinate }}, &.{shape.momentum_pair})});\\n    }\\n\\n    /// greenExponentialWithResiduals constructs a Green factor with surviving sides.\\n    pub fn greenExponentialWithResiduals(comptime scalar_value: RuleScalar, comptime momentum_pair: TensorFactor, comptime coordinate: CoordinateDifference, comptime residuals: []const Side) Expr {\\n        const shape = GreenExponentialExpr{\\n            .scalar = scalar(scalar_value),\\n            .momentum_pair = momentum_pair,\\n            .coordinate = coordinate,\\n        };\\n        return expr(&.{termWithResiduals(&.{shape.scalar}, &.{.{ .green_exponential = shape.coordinate }}, &.{shape.momentum_pair}, &.{}, residuals)});\\n    }\\n};\\n\\n/// Spec stores internal declarative metadata before lowering to runtime tables.\\nconst Spec = struct {\\n    /// LabelKind classifies one operator label slot.\\n    pub const LabelKind = enum {\\n        index,\\n        momentum,\\n        profile,\\n    };\\n\\n    /// InsertionShape records the runtime insertion packer required by an operator.\\n    pub const InsertionShape = enum {\\n        single,\\n        pair,\\n    };\\n\\n    /// Statistics records the exchange parity of an operator family.\\n    pub const Statistics = enum {\\n        bosonic,\\n        fermionic,\\n    };\\n\\n    /// Operator declares one local-field family before lowering.\\n    pub const Operator = struct {\\n        name: []const u8,\\n        kind: operators.OperatorKindId,\\n        support: wick.Support,\\n        insertion: InsertionShape,\\n        labels: []const LabelKind = &.{},\\n        statistics: Statistics,\\n        zero_mode_consumable: bool = false,\\n    };\\n\\n    /// CoordinateKernel names the coordinate-factor family required by a Wick rule.\\n    pub const CoordinateKernel = enum {\\n        rational_pole,\\n        rational_green_exponential,\\n        elliptic_green,\\n        elliptic_green_exponential,\\n        elliptic_prime_form,\\n        elliptic_prime_form_log_derivative,\\n    };\\n\\n    /// WickRule records the abstract endpoints, primitive terms, and coordinate kernels.\\n    pub const WickRule = struct {\\n        left: operators.OperatorKindId,\\n        right: operators.OperatorKindId,\\n        term_count: usize = 1,\\n        expr: ?wick.Expr = null,\\n        coordinate_kernels: []const CoordinateKernel = &.{},\\n    };\\n\\n    /// ZeroModeKind identifies implemented and draft zero-mode spec families.\\n    pub const ZeroModeKind = enum {\\n        runtime,\\n        torus_free_boson_constant,\\n        torus_bc_moduli,\\n        torus_eta_xi_zero_mode,\\n    };\\n\\n    /// ZeroModeRule records one residual base-case consumer family.\\n    pub const ZeroModeRule = struct {\\n        sector: zero_mode.Sector,\\n        kind: ZeroModeKind = .runtime,\\n        expr: ?zero_mode.Expr = null,\\n        consumes: []const operators.OperatorKindId = &.{},\\n    };\\n\\n    /// SurfaceKind names the topology class described by a theory spec.\\n    pub const SurfaceKind = enum {\\n        sphere,\\n        disk,\\n        torus,\\n    };\\n\\n    /// CoordinateModel selects the coordinate kernel family for a surface.\\n    pub const CoordinateModel = enum {\\n        rational,\\n        elliptic,\\n    };\\n\\n    /// SurfaceSource records where a surface convention is fixed.\\n    pub const SurfaceSource = enum {\\n        preset,\\n        stringbook,\\n    };\\n\\n    /// Surface records topology and modular data before runtime lowering.\\n    pub const Surface = struct {\\n        kind: SurfaceKind = .sphere,\\n        coordinate_model: CoordinateModel = .rational,\\n        modular_parameters: []const []const u8 = &.{},\\n        source: SurfaceSource = .preset,\\n    };\\n\\n    /// Theory groups the declarative metadata for one preset surface.\\n    pub const Theory = struct {\\n        surface: Surface = .{},\\n        operators: []const Operator,\\n        wick_rules: []const WickRule,\\n        zero_modes: []const ZeroModeRule,\\n    };\\n\\n    /// zeroModeRule lowers one declarative zero-mode spec into the runtime rule.\\n    pub fn zeroModeRule(comptime rule: ZeroModeRule) zero_mode.Rule {\\n        const expr = rule.expr orelse @compileError(\\"draft zero-mode spec cannot be lowered to runtime rules\\");\\n        return zero_mode.rule(rule.sector, expr);\\n    }\\n\\n    /// zeroModeRules lowers declarative zero-mode specs into runtime rules.\\n    pub fn zeroModeRules(comptime rules: []const ZeroModeRule) [rules.len]zero_mode.Rule {\\n        var storage: [rules.len]zero_mode.Rule = undefined;\\n        inline for (rules, 0..) |rule, index| {\\n            storage[index] = zeroModeRule(rule);\\n        }\\n        return storage;\\n    }\\n\\n    /// zeroModeConsumeKindCount returns the number of operator-kind families accepted by a zero-mode spec.\\n    pub fn zeroModeConsumeKindCount(comptime rule: ZeroModeRule) usize {\\n        const expr = rule.expr orelse return rule.consumes.len;\\n        return switch (expr) {\\n            .bc_top_form => |payload| payload.c_kind_ids.len,\\n            .free_boson_constant_mode => |payload| payload.exp_kind_ids.len + payload.profile_kind_ids.len,\\n            .eta_xi_zero_mode => |payload| payload.xi_kind_ids.len,\\n            .constant_fermion => |payload| payload.fermion_kind_ids.len,\\n            .top_form_fermion => |payload| payload.field_kind_ids.len,\\n            .boson_momentum_conservation => |payload| payload.exp_kind_ids.len + payload.profile_kind_ids.len,\\n        };\\n    }\\n\\n    /// wickCoordinateKernelCount counts Wick rules that require one coordinate kernel family.\\n    pub fn wickCoordinateKernelCount(comptime rules: []const WickRule, comptime expected_kernel: CoordinateKernel) usize {\\n        comptime var count: usize = 0;\\n        inline for (rules) |rule| {\\n            inline for (rule.coordinate_kernels) |candidate| {\\n                if (candidate == expected_kernel) count += 1;\\n            }\\n        }\\n        return count;\\n    }\\n\\n    fn operatorSupport(comptime operator_specs: []const Operator, comptime kind_id: operators.OperatorKindId) wick.Support {\\n        inline for (operator_specs) |operator| {\\n            if (operator.kind == kind_id) return operator.support;\\n        }\\n        @compileError(\\"missing operator spec for Wick rule endpoint\\");\\n    }\\n\\n    /// wickRule lowers one declarative Wick spec into the runtime rule table.\\n    pub fn wickRule(comptime rule: WickRule, comptime operator_specs: []const Operator) wick.Rule {\\n        const expression = rule.expr orelse @compileError(\\"draft Wick spec cannot be lowered to runtime rules\\");\\n        return wick.rule(\\n            wick.pattern(rule.left, operatorSupport(operator_specs, rule.left)),\\n            wick.pattern(rule.right, operatorSupport(operator_specs, rule.right)),\\n            expression,\\n        );\\n    }\\n\\n    /// wickRules lowers declarative Wick specs into the runtime rule table.\\n    pub fn wickRules(comptime rules: []const WickRule, comptime operator_specs: []const Operator) [rules.len]wick.Rule {\\n        var storage: [rules.len]wick.Rule = undefined;\\n        inline for (rules, 0..) |rule, index| {\\n            storage[index] = wickRule(rule, operator_specs);\\n        }\\n        return storage;\\n    }\\n\\n    /// fermionKindCount counts operators that contribute exchange signs.\\n    pub fn fermionKindCount(comptime operator_specs: []const Operator) usize {\\n        comptime var count: usize = 0;\\n        inline for (operator_specs) |operator| {\\n            if (operator.statistics == .fermionic) count += 1;\\n        }\\n        return count;\\n    }\\n\\n    /// fermionKinds lowers operator statistics into runtime fermion kind ids.\\n    pub fn fermionKinds(comptime operator_specs: []const Operator) [fermionKindCount(operator_specs)]operators.OperatorKindId {\\n        var storage: [fermionKindCount(operator_specs)]operators.OperatorKindId = undefined;\\n        comptime var index: usize = 0;\\n        inline for (operator_specs) |operator| {\\n            if (operator.statistics == .fermionic) {\\n                storage[index] = operator.kind;\\n                index += 1;\\n            }\\n        }\\n        return storage;\\n    }\\n};\\n\\nfn coordVariable(coord: Handle.Coord) u32 {\\n    return rawToken(coord);\\n}\\n\\nfn singleInsertion(z: Handle.Coord, derivatives: u8) operators.OperatorInsertion {\\n    return .{ .single = .{\\n        .position = coordVariable(z),\\n        .derivatives = derivatives,\\n    } };\\n}\\n\\nfn pairInsertion(z: Handle.Coord, zbar: Handle.Coord) operators.OperatorInsertion {\\n    return .{ .pair = .{\\n        .holomorphic_position = coordVariable(z),\\n        .antiholomorphic_position = coordVariable(zbar),\\n        .holomorphic_derivatives = 0,\\n        .antiholomorphic_derivatives = 0,\\n    } };\\n}\\n\\nfn labelTupleCount(comptime Labels: type) comptime_int {\\n    const info = @typeInfo(Labels);\\n    if (info != .@\\"struct\\" or !info.@\\"struct\\".is_tuple) @compileError(\\"operator labels must be passed as a tuple\\");\\n    return info.@\\"struct\\".fields.len;\\n}\\n\\nfn fillSymbolLabels(comptime count: usize, out: *[count]kernel.Call.LabelValue, labels: anytype) void {\\n    inline for (0..count) |index| {\\n        out[index] = Local.symbol(labels[index]);\\n    }\\n}\\n\\nfn buildKindOperator(local: *Local, kind_id: operators.OperatorKindId, insertion: operators.OperatorInsertion, labels: anytype) !*LocalOperator {\\n    const count = comptime labelTupleCount(@TypeOf(labels));\\n    var values: [count]kernel.Call.LabelValue = undefined;\\n    fillSymbolLabels(count, &values, labels);\\n    return local.op(kind_id, insertion, values[0..]);\\n}\\n\\nfn assertOperatorShape(comptime operator: Spec.Operator, comptime insertion: Spec.InsertionShape, comptime label_count: usize) void {\\n    if (operator.insertion != insertion) @compileError(\\"operator builder insertion shape does not match spec\\");\\n    if (operator.labels.len != label_count) @compileError(\\"operator builder label count does not match spec\\");\\n}\\n\\nfn boundaryCoord(y: Handle.BoundaryCoord) Handle.Coord {\\n    return token(Handle.Coord, rawToken(y));\\n}\\n\\nfn buildSingleOperator(local: *Local, comptime operator: Spec.Operator, z: Handle.Coord, derivatives: u8, labels: anytype) !*LocalOperator {\\n    comptime assertOperatorShape(operator, .single, labelTupleCount(@TypeOf(labels)));\\n    return buildKindOperator(local, operator.kind, singleInsertion(z, derivatives), labels);\\n}\\n\\nfn buildPairOperator(local: *Local, comptime operator: Spec.Operator, z: Handle.Coord, zbar: Handle.Coord, labels: anytype) !*LocalOperator {\\n    comptime assertOperatorShape(operator, .pair, labelTupleCount(@TypeOf(labels)));\\n    return buildKindOperator(local, operator.kind, pairInsertion(z, zbar), labels);\\n}\\n\\nfn localBuilder(local: anytype) *Local {\\n    const Input = @TypeOf(local);\\n    return switch (@typeInfo(Input)) {\\n        .pointer => |ptr| if (ptr.child == Local)\\n            local\\n        else if (ptr.child == *Local)\\n            local.*\\n        else\\n            @compileError(\\"operator builders require a local builder\\"),\\n        else => @compileError(\\"operator builders require a local builder\\"),\\n    };\\n}\\n\\n/// operatorBuilder generates insertion and label packing from one operator schema.\\nfn operatorBuilder(comptime operator: Spec.Operator) type {\\n    return struct {\\n        /// single builds a single-coordinate local operator from this schema.\\n        pub fn single(local: anytype, z: Handle.Coord, derivatives: u8, labels: anytype) !*LocalOperator {\\n            return buildSingleOperator(localBuilder(local), operator, z, derivatives, labels);\\n        }\\n\\n        /// boundarySingle builds a boundary single-coordinate local operator from this schema.\\n        pub fn boundarySingle(local: anytype, y: Handle.BoundaryCoord, derivatives: u8, labels: anytype) !*LocalOperator {\\n            return buildSingleOperator(localBuilder(local), operator, boundaryCoord(y), derivatives, labels);\\n        }\\n\\n        /// pair builds a bulk-pair local operator from this schema.\\n        pub fn pair(local: anytype, z: Handle.Coord, zbar: Handle.Coord, labels: anytype) !*LocalOperator {\\n            return buildPairOperator(localBuilder(local), operator, z, zbar, labels);\\n        }\\n    };\\n}\\n\\n/// zero_mode exposes compact preset-author helpers for zero-mode rules.\\nconst zero_mode = ZeroModeDsl;\\n\\nconst ZeroModeDsl = struct {\\n    /// Sector names a preset-owned zero-mode sector.\\n    pub const Sector = u32;\\n\\n    /// BcSupport selects the finite c-zero-mode basis used by the bc evaluator.\\n    pub const BcSupport = enum {\\n        sphere_holomorphic,\\n        sphere_antiholomorphic,\\n        disk_doubled,\\n    };\\n\\n    /// BcTopForm declares one top-form ghost zero-mode saturation rule.\\n    pub const BcTopForm = struct {\\n        support: BcSupport,\\n        c_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// EtaXiSupport selects the chiral xi constant mode saturated by a surface.\\n    pub const EtaXiSupport = enum {\\n        sphere_holomorphic,\\n        sphere_antiholomorphic,\\n        torus_holomorphic,\\n        torus_antiholomorphic,\\n    };\\n\\n    /// EtaXiZeroMode declares one xi constant-mode saturation rule.\\n    pub const EtaXiZeroMode = struct {\\n        support: EtaXiSupport,\\n        xi_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// ConstantFermion declares one generic constant fermion saturation rule.\\n    pub const ConstantFermion = struct {\\n        support: EtaXiSupport,\\n        fermion_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// TopFormFermion declares one generic top-form fermion saturation rule.\\n    pub const TopFormFermion = struct {\\n        support: BcSupport,\\n        field_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// RankSource records where a power of 2pi gets its exponent.\\n    pub const RankSource = union(enum) {\\n        none,\\n        literal: u16,\\n        target_dimension: ConfigRef,\\n        projector_rank: ConfigRef,\\n    };\\n\\n    /// Normalization records the CFT normalization attached to a zero-mode rule.\\n    pub const Normalization = struct {\\n        scalar: RuleScalar = .one,\\n        two_pi_power: RankSource = .none,\\n    };\\n\\n    /// FreeBosonConstantMode declares the free-boson constant-mode base case.\\n    pub const FreeBosonConstantMode = struct {\\n        integration_projector: ?ConfigRef = null,\\n        fixed_projector: ?ConfigRef = null,\\n        fixed_position: ?ConfigRef = null,\\n        exp_kind_ids: []const operators.OperatorKindId,\\n        profile_kind_ids: []const operators.OperatorKindId,\\n        normalization: Normalization = .{},\\n    };\\n\\n    /// Expr selects the zero-mode evaluator and payload.\\n    pub const Expr = union(enum) {\\n        bc_top_form: BcTopForm,\\n        free_boson_constant_mode: FreeBosonConstantMode,\\n        eta_xi_zero_mode: EtaXiZeroMode,\\n        constant_fermion: ConstantFermion,\\n        top_form_fermion: TopFormFermion,\\n        boson_momentum_conservation: FreeBosonConstantMode,\\n    };\\n\\n    /// Rule is one preset-authored zero-mode rule.\\n    pub const Rule = struct {\\n        sector: Sector,\\n        expr: Expr,\\n    };\\n\\n    /// rule constructs a zero-mode rule.\\n    pub fn rule(sector: Sector, expr: Expr) Rule {\\n        return .{ .sector = sector, .expr = expr };\\n    }\\n};\\n\\nconst PairLookupEntry = struct {\\n    key: u64,\\n    rule_index: u32,\\n    reversed: bool,\\n};\\n\\nconst BranchBudget = enum {\\n    standard,\\n};\\n\\nconst BranchLimits = struct {\\n    max_operators: usize,\\n    max_zero_mode_residuals: usize,\\n    max_zero_mode_items: usize,\\n    max_terms: usize,\\n    max_scalars: usize,\\n    max_coordinates: usize,\\n    max_tensors: usize,\\n    max_actions: usize,\\n    max_residuals: usize,\\n    max_cached_terms: usize,\\n    max_cached_scalars: usize,\\n    max_cached_coordinates: usize,\\n    max_cached_tensors: usize,\\n    max_cached_actions: usize,\\n    max_cached_residuals: usize,\\n};\\n\\nconst CorrelatorConfigInput = struct {\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry = &.{},\\n    fermion_kinds: []const operators.OperatorKindId = &.{},\\n    branch_budget: BranchBudget = .standard,\\n};\\n\\nconst CorrelatorConfigData = struct {\\n    wick_rules: []const wick.Rule,\\n    pair_lookup: []const PairLookupEntry,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry,\\n    fermion_kinds: []const operators.OperatorKindId,\\n};\\n\\n/// correlatorConfig creates an opaque public handle for lowered correlator data.\\nfn correlatorConfig(comptime input: CorrelatorConfigInput) type {\\n    return struct {\\n        const branch_limits = branchLimits(input);\\n        const pair_lookup_storage = pairLookupStorage(input.wick_rules);\\n        const impl = CorrelatorConfigData{\\n            .wick_rules = input.wick_rules,\\n            .pair_lookup = &pair_lookup_storage,\\n            .zero_modes = input.zero_modes,\\n            .config_entries = input.config_entries,\\n            .fermion_kinds = input.fermion_kinds,\\n        };\\n    };\\n}\\n\\nfn configData(config_ptr: anytype) *const CorrelatorConfigData {\\n    const Ptr = @TypeOf(config_ptr);\\n    const ptr_info = @typeInfo(Ptr);\\n    if (ptr_info != .pointer) @compileError(\\"correlator config must be passed by pointer\\");\\n    const Config = ptr_info.pointer.child;\\n    if (!@hasDecl(Config, \\"impl\\")) @compileError(\\"invalid correlator config handle\\");\\n    return &Config.impl;\\n}\\n\\nconst ConfigValue = union(enum) {\\n    tensor_projector: Handle.TensorProjector,\\n    target_point: Handle.TargetPoint,\\n    target_dimension: u16,\\n    boundary_stack: Handle.BoundaryStack,\\n};\\n\\nconst ConfigEntry = struct {\\n    id: ConfigRef,\\n    value: ConfigValue,\\n};\\n\\n/// config exposes typed constructors for private config payload entries.\\nconst ConfigDsl = struct {\\n    /// tensorProjector binds a tensor-projector handle to a config reference.\\n    pub fn tensorProjector(id: ConfigRef, value: Handle.TensorProjector) ConfigEntry {\\n        return .{ .id = id, .value = .{ .tensor_projector = value } };\\n    }\\n\\n    /// targetPoint binds a target-space point handle to a config reference.\\n    pub fn targetPoint(id: ConfigRef, value: Handle.TargetPoint) ConfigEntry {\\n        return .{ .id = id, .value = .{ .target_point = value } };\\n    }\\n\\n    /// targetDimension binds a target-space dimension to a config reference.\\n    pub fn targetDimension(id: ConfigRef, value: u16) ConfigEntry {\\n        return .{ .id = id, .value = .{ .target_dimension = value } };\\n    }\\n\\n    /// boundaryStack binds Chan-Paton boundary-stack data to a config reference.\\n    pub fn boundaryStack(id: ConfigRef, value: Handle.BoundaryStack) ConfigEntry {\\n        return .{ .id = id, .value = .{ .boundary_stack = value } };\\n    }\\n};\\n\\n/// configEntries packs typed config entries without exposing their carrier type.\\nfn configEntries(comptime entries: anytype) [@typeInfo(@TypeOf(entries)).@\\"struct\\".fields.len]ConfigEntry {\\n    var storage: [@typeInfo(@TypeOf(entries)).@\\"struct\\".fields.len]ConfigEntry = undefined;\\n    inline for (entries, 0..) |entry, index| {\\n        storage[index] = entry;\\n    }\\n    return storage;\\n}\\n\\nconst BoundaryExtensionInput = struct {\\n    op: type,\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry = &.{},\\n    fermion_kinds: []const operators.OperatorKindId = &.{},\\n};\\n\\nconst BoundaryExtensionData = struct {\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry,\\n    fermion_kinds: []const operators.OperatorKindId,\\n};\\n\\nconst LoweredSlice = enum {\\n    wick_rules,\\n    zero_modes,\\n    config_entries,\\n    fermion_kinds,\\n};\\n\\nfn correlatorSliceCount(config_ptr: anytype, comptime slice: LoweredSlice) usize {\\n    const data = configData(config_ptr);\\n    return switch (slice) {\\n        .wick_rules => data.wick_rules.len,\\n        .zero_modes => data.zero_modes.len,\\n        .config_entries => data.config_entries.len,\\n        .fermion_kinds => data.fermion_kinds.len,\\n    };\\n}\\n\\nfn appendCorrelatorSlice(config_ptr: anytype, comptime slice: LoweredSlice, storage: anytype, index: *usize) void {\\n    const data = configData(config_ptr);\\n    switch (slice) {\\n        .wick_rules => for (data.wick_rules) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .zero_modes => for (data.zero_modes) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .config_entries => for (data.config_entries) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .fermion_kinds => for (data.fermion_kinds) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n    }\\n}\\n\\n/// BoundaryExtension creates an opaque public handle for boundary preset data.\\nfn BoundaryExtension(comptime input: BoundaryExtensionInput) type {\\n    return struct {\\n        /// op exposes boundary-local operator builders for this extension.\\n        pub const op = input.op;\\n        const impl = BoundaryExtensionData{\\n            .wick_rules = input.wick_rules,\\n            .zero_modes = input.zero_modes,\\n            .config_entries = input.config_entries,\\n            .fermion_kinds = input.fermion_kinds,\\n        };\\n    };\\n}\\n\\nfn boundaryExtensionData(comptime extension: anytype) BoundaryExtensionData {\\n    const Extension = @TypeOf(extension);\\n    if (!@hasDecl(Extension, \\"impl\\")) @compileError(\\"invalid boundary extension handle\\");\\n    return Extension.impl;\\n}\\n\\nfn boundaryExtensionSliceCount(comptime extension: anytype, comptime slice: LoweredSlice) usize {\\n    const data = boundaryExtensionData(extension);\\n    return switch (slice) {\\n        .wick_rules => data.wick_rules.len,\\n        .zero_modes => data.zero_modes.len,\\n        .config_entries => data.config_entries.len,\\n        .fermion_kinds => data.fermion_kinds.len,\\n    };\\n}\\n\\nfn appendBoundaryExtensionSlice(comptime extension: anytype, comptime slice: LoweredSlice, storage: anytype, index: *usize) void {\\n    const data = boundaryExtensionData(extension);\\n    switch (slice) {\\n        .wick_rules => for (data.wick_rules) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .zero_modes => for (data.zero_modes) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .config_entries => for (data.config_entries) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .fermion_kinds => for (data.fermion_kinds) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n    }\\n}\\n\\nfn factorHasSphereConfig(comptime Factor: type) bool {\\n    return @hasDecl(Factor, \\"config\\") and @hasDecl(Factor.config, \\"sphere\\");\\n}\\n\\nfn sphereConfigSliceCount(comptime factors: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    inline for (factors) |Factor| {\\n        if (factorHasSphereConfig(Factor)) count += correlatorSliceCount(&Factor.config.sphere, slice);\\n    }\\n    return count;\\n}\\n\\nfn appendSphereConfigSlice(comptime factors: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    inline for (factors) |Factor| {\\n        if (factorHasSphereConfig(Factor)) {\\n            appendCorrelatorSlice(&Factor.config.sphere, slice, storage, &index);\\n        }\\n    }\\n}\\n\\nfn mergedSphereWickStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .wick_rules)]wick.Rule {\\n    var storage: [sphereConfigSliceCount(factors, .wick_rules)]wick.Rule = undefined;\\n    appendSphereConfigSlice(factors, .wick_rules, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereZeroStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .zero_modes)]zero_mode.Rule {\\n    var storage: [sphereConfigSliceCount(factors, .zero_modes)]zero_mode.Rule = undefined;\\n    appendSphereConfigSlice(factors, .zero_modes, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereConfigStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .config_entries)]ConfigEntry {\\n    var storage: [sphereConfigSliceCount(factors, .config_entries)]ConfigEntry = undefined;\\n    appendSphereConfigSlice(factors, .config_entries, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereFermionStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .fermion_kinds)]operators.OperatorKindId {\\n    var storage: [sphereConfigSliceCount(factors, .fermion_kinds)]operators.OperatorKindId = undefined;\\n    appendSphereConfigSlice(factors, .fermion_kinds, storage[0..]);\\n    return storage;\\n}\\n\\n/// mergeSphereConfigs builds one opaque sphere config from independent factors.\\nfn mergeSphereConfigs(comptime factors: anytype) type {\\n    return struct {\\n        const wick_storage = mergedSphereWickStorage(factors);\\n        const zero_storage = mergedSphereZeroStorage(factors);\\n        const config_storage = mergedSphereConfigStorage(factors);\\n        const fermion_storage = mergedSphereFermionStorage(factors);\\n\\n        /// config is the merged sphere correlator config handle.\\n        pub const config = correlatorConfig(.{\\n            .wick_rules = &wick_storage,\\n            .zero_modes = &zero_storage,\\n            .config_entries = &config_storage,\\n            .fermion_kinds = &fermion_storage,\\n        }){};\\n    };\\n}\\n\\nfn boundaryExtensionSliceTotal(comptime extensions: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    inline for (extensions) |extension| {\\n        count += boundaryExtensionSliceCount(extension, slice);\\n    }\\n    return count;\\n}\\n\\nfn boundaryConfigSliceCount(comptime Bulk: type, comptime extensions: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    if (factorHasSphereConfig(Bulk)) count += correlatorSliceCount(&Bulk.config.sphere, slice);\\n    inline for (extensions) |extension| {\\n        count += boundaryExtensionSliceCount(extension, slice);\\n    }\\n    return count;\\n}\\n\\nfn appendBoundaryExtensionSlices(comptime extensions: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    inline for (extensions) |extension| {\\n        appendBoundaryExtensionSlice(extension, slice, storage, &index);\\n    }\\n}\\n\\nfn appendBoundaryConfigSlices(comptime Bulk: type, comptime extensions: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    if (factorHasSphereConfig(Bulk)) {\\n        appendCorrelatorSlice(&Bulk.config.sphere, slice, storage, &index);\\n    }\\n    inline for (extensions) |extension| {\\n        appendBoundaryExtensionSlice(extension, slice, storage, &index);\\n    }\\n}\\n\\nfn mergedBoundaryWickStorage(comptime extensions: anytype) [boundaryExtensionSliceTotal(extensions, .wick_rules)]wick.Rule {\\n    var storage: [boundaryExtensionSliceTotal(extensions, .wick_rules)]wick.Rule = undefined;\\n    appendBoundaryExtensionSlices(extensions, .wick_rules, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryZeroStorage(comptime extensions: anytype) [boundaryExtensionSliceTotal(extensions, .zero_modes)]zero_mode.Rule {\\n    var storage: [boundaryExtensionSliceTotal(extensions, .zero_modes)]zero_mode.Rule = undefined;\\n    appendBoundaryExtensionSlices(extensions, .zero_modes, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryConfigStorage(comptime Bulk: type, comptime extensions: anytype) [boundaryConfigSliceCount(Bulk, extensions, .config_entries)]ConfigEntry {\\n    var storage: [boundaryConfigSliceCount(Bulk, extensions, .config_entries)]ConfigEntry = undefined;\\n    appendBoundaryConfigSlices(Bulk, extensions, .config_entries, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryFermionStorage(comptime Bulk: type, comptime extensions: anytype) [boundaryConfigSliceCount(Bulk, extensions, .fermion_kinds)]operators.OperatorKindId {\\n    var storage: [boundaryConfigSliceCount(Bulk, extensions, .fermion_kinds)]operators.OperatorKindId = undefined;\\n    appendBoundaryConfigSlices(Bulk, extensions, .fermion_kinds, storage[0..]);\\n    return storage;\\n}\\n\\n/// mergeBoundaryConfig builds one opaque disk config from bulk data and extensions.\\nfn mergeBoundaryConfig(comptime Bulk: type, comptime extensions: anytype) type {\\n    return struct {\\n        const wick_storage = mergedBoundaryWickStorage(extensions);\\n        const zero_storage = mergedBoundaryZeroStorage(extensions);\\n        const config_storage = mergedBoundaryConfigStorage(Bulk, extensions);\\n        const fermion_storage = mergedBoundaryFermionStorage(Bulk, extensions);\\n\\n        /// config is the merged disk correlator config handle.\\n        pub const config = correlatorConfig(.{\\n            .wick_rules = &wick_storage,\\n            .zero_modes = &zero_storage,\\n            .config_entries = &config_storage,\\n            .fermion_kinds = &fermion_storage,\\n        }){};\\n    };\\n}\\n\\nconst DeclareSpec = Spec;\\nconst DeclareWick = wick;\\nconst DeclareZeroMode = zero_mode;\\nconst DeclareScalars = scalars;\\nconst DeclareConfig = ConfigDsl;\\nconst declareConfigRef = configRef;\\nconst declareFamilyKind = familyKind;\\nconst declareSectorId = sectorId;\\nconst declareOperatorBuilder = operatorBuilder;\\nconst declareCorrelatorConfig = correlatorConfig;\\nconst declareBoundaryExtension = BoundaryExtension;\\nconst declareConfigEntries = configEntries;\\nconst declareMergeSphereConfigs = mergeSphereConfigs;\\nconst declareMergeBoundaryConfig = mergeBoundaryConfig;\\n\\n/// declare exposes preset declaration lowering without leaking individual internals.\\npub const declare = struct {\\n    /// Spec stores declarative metadata before lowering to runtime tables.\\n    pub const Spec = DeclareSpec;\\n    /// wick exposes compact preset-author helpers for Wick rules.\\n    pub const wick = DeclareWick;\\n    /// zero_mode exposes compact preset-author helpers for zero-mode rules.\\n    pub const zero_mode = DeclareZeroMode;\\n    /// scalars exposes exact scalar helpers for preset rule coefficients.\\n    pub const scalars = DeclareScalars;\\n    /// config exposes typed constructors for private config payload entries.\\n    pub const config = DeclareConfig;\\n\\n    /// configRef derives a compact config id from a namespace-local name.\\n    pub const configRef = declareConfigRef;\\n    /// familyKind derives a compact operator kind id from a preset family tag.\\n    pub const familyKind = declareFamilyKind;\\n    /// sectorId derives a compact zero-mode sector id from a preset sector tag.\\n    pub const sectorId = declareSectorId;\\n    /// operatorBuilder generates insertion and label packing from one operator schema.\\n    pub const operatorBuilder = declareOperatorBuilder;\\n    /// correlatorConfig creates an opaque public handle for lowered correlator data.\\n    pub const correlatorConfig = declareCorrelatorConfig;\\n    /// BoundaryExtension creates an opaque public handle for boundary preset data.\\n    pub const BoundaryExtension = declareBoundaryExtension;\\n    /// configEntries packs typed config entries without exposing their carrier type.\\n    pub const configEntries = declareConfigEntries;\\n    /// mergeSphereConfigs builds one opaque sphere config from independent factors.\\n    pub const mergeSphereConfigs = declareMergeSphereConfigs;\\n    /// mergeBoundaryConfig builds one opaque disk config from bulk data and extensions.\\n    pub const mergeBoundaryConfig = declareMergeBoundaryConfig;\\n};\\n\\nfn wickRuleKey(left_kind: operators.OperatorKindId, right_kind: operators.OperatorKindId) u64 {\\n    return (@as(u64, left_kind) << 32) | @as(u64, right_kind);\\n}\\n\\nfn pairLookupEntryCount(comptime rules: []const wick.Rule) usize {\\n    var count: usize = 0;\\n    for (rules) |rule| {\\n        count += 1;\\n        if (rule.left.kind != rule.right.kind) count += 1;\\n    }\\n    return count;\\n}\\n\\nfn sortPairLookup(entries: []PairLookupEntry) void {\\n    var index: usize = 1;\\n    while (index < entries.len) : (index += 1) {\\n        const value = entries[index];\\n        var hole = index;\\n        while (hole > 0 and entries[hole - 1].key > value.key) : (hole -= 1) {\\n            entries[hole] = entries[hole - 1];\\n        }\\n        entries[hole] = value;\\n    }\\n}\\n\\nfn pairLookupStorage(comptime rules: []const wick.Rule) [pairLookupEntryCount(rules)]PairLookupEntry {\\n    var entries: [pairLookupEntryCount(rules)]PairLookupEntry = undefined;\\n    var entry_index: usize = 0;\\n\\n    for (rules, 0..) |rule, rule_index| {\\n        if (rule_index > std.math.maxInt(u32)) @compileError(\\"too many Wick rules for compact rule index\\");\\n        entries[entry_index] = .{\\n            .key = wickRuleKey(rule.left.kind, rule.right.kind),\\n            .rule_index = @intCast(rule_index),\\n            .reversed = false,\\n        };\\n        entry_index += 1;\\n\\n        if (rule.left.kind != rule.right.kind) {\\n            entries[entry_index] = .{\\n                .key = wickRuleKey(rule.right.kind, rule.left.kind),\\n                .rule_index = @intCast(rule_index),\\n                .reversed = true,\\n            };\\n            entry_index += 1;\\n        }\\n    }\\n\\n    sortPairLookup(entries[0..]);\\n    return entries;\\n}\\n\\nconst Correlator = struct {\\n    /// WickPair is one oriented primitive Wick contraction between two concrete local operators.\\n    const WickPair = struct {\\n        rule: *const wick.Rule,\\n        config: *const CorrelatorConfigData,\\n        left: kernel.Call.LocalOp,\\n        right: kernel.Call.LocalOp,\\n        labels: *const kernel.Call.LabelStore,\\n        reversed: bool,\\n\\n        fn sideOp(self: WickPair, side: wick.Side) kernel.Call.LocalOp {\\n            return switch (side) {\\n                .left => if (self.reversed) self.right else self.left,\\n                .right => if (self.reversed) self.left else self.right,\\n            };\\n        }\\n\\n        fn coordinate(self: WickPair, coordinate_ref: wick.CoordinateRef) ?coefficient.Variable {\\n            const op = self.sideOp(coordinate_ref.side);\\n            return switch (op.insertion) {\\n                .single => |single| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => single.position,\\n                    .antiholomorphic => null,\\n                },\\n                .pair => |pair| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => pair.holomorphic_position,\\n                    .antiholomorphic => pair.antiholomorphic_position,\\n                },\\n            };\\n        }\\n\\n        fn derivativeCount(self: WickPair, coordinate_ref: wick.CoordinateRef) ?u8 {\\n            const op = self.sideOp(coordinate_ref.side);\\n            return switch (op.insertion) {\\n                .single => |single| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => single.derivatives,\\n                    .antiholomorphic => null,\\n                },\\n                .pair => |pair| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => pair.holomorphic_derivatives,\\n                    .antiholomorphic => pair.antiholomorphic_derivatives,\\n                },\\n            };\\n        }\\n\\n        fn label(self: WickPair, label_ref: wick.LabelRef) ?kernel.Call.LabelValue {\\n            const op = self.sideOp(label_ref.side);\\n            const index: usize = @as(usize, @intCast(op.labels)) + label_ref.slot;\\n            if (index >= self.labels.values.len) return null;\\n            return self.labels.values[index];\\n        }\\n\\n        fn configValue(self: WickPair, config_ref: ConfigRef) ?ConfigValue {\\n            for (self.config.config_entries) |entry| {\\n                if (entry.id == config_ref) return entry.value;\\n            }\\n            return null;\\n        }\\n    };\\n\\n    /// WickResidualRef points to one surviving original input occurrence.\\n    const WickResidualRef = struct {\\n        input_index: usize,\\n        operator: kernel.Call.LocalOp,\\n    };\\n\\n    /// ResolvedCoordinateRef is one concrete coordinate variable with its derivative count.\\n    const ResolvedCoordinateRef = struct {\\n        variable: coefficient.Variable,\\n        derivatives: u8,\\n    };\\n\\n    /// ResolvedCoordinateDifference is a concrete coordinate difference read by a Wick factor.\\n    const ResolvedCoordinateDifference = struct {\\n        left: ResolvedCoordinateRef,\\n        right: ResolvedCoordinateRef,\\n    };\\n\\n    /// ResolvedDerivativeAction records the derivative counts requested by one coordinate factor.\\n    const ResolvedDerivativeAction = struct {\\n        left: ?u8 = null,\\n        right: ?u8 = null,\\n    };\\n\\n    /// ResolvedTensorRef is tensor data resolved either from an operator label or config entry.\\n    const ResolvedTensorRef = union(enum) {\\n        label: kernel.Call.LabelValue,\\n        config: ConfigValue,\\n    };\\n\\n    /// ResolvedScalarFactor is a scalar factor with all label references plugged in.\\n    const ResolvedScalarFactor = union(enum) {\\n        value: RuleScalar,\\n        label_bilinear_phase: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue, form: []const u8 },\\n        cocycle: struct { table: []const u8, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        spin_structure: []const u8,\\n    };\\n\\n    /// ResolvedTensorFactor is a tensor multiplier with concrete labels and config values.\\n    const ResolvedTensorFactor = union(enum) {\\n        none,\\n        metric: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        momentum_index: struct { momentum: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        momentum_pair: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        projector_metric: struct { projector: ResolvedTensorRef, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        projector_momentum_index: struct { projector: ResolvedTensorRef, momentum: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        projector_momentum_pair: struct { projector: ResolvedTensorRef, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n    };\\n\\n    /// ResolvedActionFactor is a Wick action with concrete profile, index, and projector data.\\n    const ResolvedActionFactor = union(enum) {\\n        profile_derivative: struct { profile: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        projected_profile_derivative: struct { projector: ResolvedTensorRef, profile: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n    };\\n\\n    /// ResolvedCoordinateFactor is a coordinate kernel with concrete coordinates and derivative data.\\n    const ResolvedCoordinateFactor = union(enum) {\\n        difference_power: struct { coordinate: ResolvedCoordinateDifference, exponent: i16, derivatives: ResolvedDerivativeAction },\\n        logarithm: struct { coordinate: ResolvedCoordinateDifference, derivatives: ResolvedDerivativeAction },\\n        green_kernel: struct { name: []const u8, coordinate: ResolvedCoordinateDifference, derivatives: ResolvedDerivativeAction },\\n        green_exponential: ResolvedCoordinateDifference,\\n    };\\n\\n    /// ResolvedTerm is one Wick term whose factors can be resolved without allocation.\\n    const ResolvedTerm = struct {\\n        pair: WickPair,\\n        source: *const wick.Term,\\n\\n        fn scalarCount(self: ResolvedTerm) usize {\\n            return self.source.scalars.len;\\n        }\\n\\n        fn coordinateCount(self: ResolvedTerm) usize {\\n            return self.source.coordinates.len;\\n        }\\n\\n        fn tensorCount(self: ResolvedTerm) usize {\\n            return self.source.tensors.len;\\n        }\\n\\n        fn actionCount(self: ResolvedTerm) usize {\\n            return self.source.actions.len;\\n        }\\n\\n        fn residualCount(self: ResolvedTerm) usize {\\n            return self.source.residuals.len;\\n        }\\n\\n        fn scalar(self: ResolvedTerm, index: usize) ?ResolvedScalarFactor {\\n            if (index >= self.source.scalars.len) return null;\\n            return resolveScalarFactor(self.pair, self.source.scalars[index]);\\n        }\\n\\n        fn coordinate(self: ResolvedTerm, index: usize) ?ResolvedCoordinateFactor {\\n            if (index >= self.source.coordinates.len) return null;\\n            return resolveCoordinateFactor(self.pair, self.source.coordinates[index]);\\n        }\\n\\n        fn tensor(self: ResolvedTerm, index: usize) ?ResolvedTensorFactor {\\n            if (index >= self.source.tensors.len) return null;\\n            return resolveTensorFactor(self.pair, self.source.tensors[index]);\\n        }\\n\\n        fn action(self: ResolvedTerm, index: usize) ?ResolvedActionFactor {\\n            if (index >= self.source.actions.len) return null;\\n            return resolveActionFactor(self.pair, self.source.actions[index]);\\n        }\\n\\n        fn residual(self: ResolvedTerm, index: usize) ?wick.Side {\\n            if (index >= self.source.residuals.len) return null;\\n            return self.source.residuals[index];\\n        }\\n    };\\n\\n    /// ResolvedWickPair gives allocation-free access to concrete Wick terms for one pair.\\n    const ResolvedWickPair = struct {\\n        pair: WickPair,\\n\\n        fn termCount(self: ResolvedWickPair) usize {\\n            return self.pair.rule.expr.terms.len;\\n        }\\n\\n        fn term(self: ResolvedWickPair, index: usize) ?ResolvedTerm {\\n            if (index >= self.pair.rule.expr.terms.len) return null;\\n            return .{ .pair = self.pair, .source = &self.pair.rule.expr.terms[index] };\\n        }\\n    };\\n\\n    /// PrimitiveWickTerm is one complete primitive Wick contribution without later simplification.\\n    const PrimitiveWickTerm = struct {\\n        resolved: ResolvedTerm,\\n\\n        fn scalarCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.scalarCount();\\n        }\\n\\n        fn coordinateCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.coordinateCount();\\n        }\\n\\n        fn tensorCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.tensorCount();\\n        }\\n\\n        fn actionCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.actionCount();\\n        }\\n\\n        fn residualCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.residualCount();\\n        }\\n\\n        fn scalar(self: PrimitiveWickTerm, index: usize) ?ResolvedScalarFactor {\\n            return self.resolved.scalar(index);\\n        }\\n\\n        fn coordinate(self: PrimitiveWickTerm, index: usize) ?coefficient.CoordinateFactor {\\n            const factor = self.resolved.coordinate(index) orelse return null;\\n            return evaluateCoordinateFactor(factor);\\n        }\\n\\n        fn tensor(self: PrimitiveWickTerm, index: usize) ?ResolvedTensorFactor {\\n            return self.resolved.tensor(index);\\n        }\\n\\n        fn action(self: PrimitiveWickTerm, index: usize) ?ResolvedActionFactor {\\n            return self.resolved.action(index);\\n        }\\n\\n        fn residual(self: PrimitiveWickTerm, index: usize) ?wick.Side {\\n            return self.resolved.residual(index);\\n        }\\n    };\\n\\n    /// PrimitiveWickPair gives allocation-free complete terms for one primitive Wick pair.\\n    const PrimitiveWickPair = struct {\\n        pair: WickPair,\\n\\n        fn termCount(self: PrimitiveWickPair) usize {\\n            return self.pair.rule.expr.terms.len;\\n        }\\n\\n        fn term(self: PrimitiveWickPair, index: usize) ?PrimitiveWickTerm {\\n            const resolved = (ResolvedWickPair{ .pair = self.pair }).term(index) orelse return null;\\n            return .{ .resolved = resolved };\\n        }\\n    };\\n\\n    fn resolveCoordinateRef(pair: WickPair, coordinate_ref: wick.CoordinateRef) ?ResolvedCoordinateRef {\\n        return .{\\n            .variable = pair.coordinate(coordinate_ref) orelse return null,\\n            .derivatives = pair.derivativeCount(coordinate_ref) orelse return null,\\n        };\\n    }\\n\\n    fn resolveCoordinateDifference(pair: WickPair, difference: wick.CoordinateDifference) ?ResolvedCoordinateDifference {\\n        return .{\\n            .left = resolveCoordinateRef(pair, difference.left) orelse return null,\\n            .right = resolveCoordinateRef(pair, difference.right) orelse return null,\\n        };\\n    }\\n\\n    fn resolveDerivativeAction(pair: WickPair, coordinate: wick.CoordinateDifference, action: wick.DerivativeAction) ?ResolvedDerivativeAction {\\n        return .{\\n            .left = if (action.include_left) pair.derivativeCount(coordinate.left) orelse return null else null,\\n            .right = if (action.include_right) pair.derivativeCount(coordinate.right) orelse return null else null,\\n        };\\n    }\\n\\n    fn resolveTensorRef(pair: WickPair, tensor_ref: wick.TensorRef) ?ResolvedTensorRef {\\n        return switch (tensor_ref) {\\n            .label => |label_ref| .{ .label = pair.label(label_ref) orelse return null },\\n            .config => |config_ref| .{ .config = pair.configValue(config_ref) orelse return null },\\n        };\\n    }\\n\\n    fn resolveScalarFactor(pair: WickPair, factor: wick.ScalarFactor) ?ResolvedScalarFactor {\\n        return switch (factor) {\\n            .value => |value| .{ .value = value },\\n            .label_bilinear_phase => |phase| .{ .label_bilinear_phase = .{\\n                .left = pair.label(phase.left) orelse return null,\\n                .right = pair.label(phase.right) orelse return null,\\n                .form = phase.form,\\n            } },\\n            .cocycle => |cocycle| .{ .cocycle = .{\\n                .table = cocycle.table,\\n                .left = pair.label(cocycle.left) orelse return null,\\n                .right = pair.label(cocycle.right) orelse return null,\\n            } },\\n            .spin_structure => |spin_structure| .{ .spin_structure = spin_structure },\\n        };\\n    }\\n\\n    fn resolveTensorFactor(pair: WickPair, factor: wick.TensorFactor) ?ResolvedTensorFactor {\\n        return switch (factor) {\\n            .none => .none,\\n            .metric => |metric| .{ .metric = .{\\n                .left = pair.label(metric.left) orelse return null,\\n                .right = pair.label(metric.right) orelse return null,\\n            } },\\n            .momentum_index => |item| .{ .momentum_index = .{\\n                .momentum = pair.label(item.momentum) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .momentum_pair => |item| .{ .momentum_pair = .{\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n            .projector_metric => |item| .{ .projector_metric = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n            .projector_momentum_index => |item| .{ .projector_momentum_index = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .momentum = pair.label(item.momentum) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .projector_momentum_pair => |item| .{ .projector_momentum_pair = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn resolveActionFactor(pair: WickPair, factor: wick.ActionFactor) ?ResolvedActionFactor {\\n        return switch (factor) {\\n            .profile_derivative => |item| .{ .profile_derivative = .{\\n                .profile = pair.label(item.profile) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .projected_profile_derivative => |item| .{ .projected_profile_derivative = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .profile = pair.label(item.profile) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn resolveCoordinateFactor(pair: WickPair, factor: wick.CoordinateFactor) ?ResolvedCoordinateFactor {\\n        return switch (factor) {\\n            .difference_power => |item| .{ .difference_power = .{\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .exponent = item.exponent,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .logarithm => |item| .{ .logarithm = .{\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .green_kernel => |item| .{ .green_kernel = .{\\n                .name = item.name,\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .green_exponential => |coordinate| .{ .green_exponential = resolveCoordinateDifference(pair, coordinate) orelse return null },\\n        };\\n    }\\n\\n    fn coordinateDifference(coordinate: ResolvedCoordinateDifference) coefficient.CoordinateDifference {\\n        return .{\\n            .left = coordinate.left.variable,\\n            .right = coordinate.right.variable,\\n        };\\n    }\\n\\n    fn selectedDerivativeCount(action: ResolvedDerivativeAction) u16 {\\n        const left: u16 = if (action.left) |count| count else 0;\\n        const right: u16 = if (action.right) |count| count else 0;\\n        return left + right;\\n    }\\n\\n    fn selectedRightDerivativeCount(action: ResolvedDerivativeAction) u16 {\\n        return if (action.right) |count| count else 0;\\n    }\\n\\n    fn checkedMulInt(left: i64, right: i64) ?i64 {\\n        const result = @mulWithOverflow(left, right);\\n        if (result[1] != 0) return null;\\n        return result[0];\\n    }\\n\\n    fn checkedAddExponent(exponent: i16, derivative_count: u16) ?i16 {\\n        const next = @as(i32, exponent) - @as(i32, derivative_count);\\n        if (next < std.math.minInt(i16) or next > std.math.maxInt(i16)) return null;\\n        return @intCast(next);\\n    }\\n\\n    fn fallingPower(exponent: i16, derivative_count: u16) ?i64 {\\n        var value: i64 = 1;\\n        var current: i64 = exponent;\\n        var index: u16 = 0;\\n        while (index < derivative_count) : (index += 1) {\\n            value = checkedMulInt(value, current) orelse return null;\\n            current -= 1;\\n        }\\n        return value;\\n    }\\n\\n    fn factorial(value: u16) ?i64 {\\n        var result: i64 = 1;\\n        var current: u16 = 2;\\n        while (current <= value) : (current += 1) {\\n            result = checkedMulInt(result, current) orelse return null;\\n        }\\n        return result;\\n    }\\n\\n    fn signFromParity(power: u16) i64 {\\n        return if ((power & 1) == 0) 1 else -1;\\n    }\\n\\n    fn evaluateDifferencePower(item: anytype) ?coefficient.CoordinateFactor {\\n        const derivative_count = selectedDerivativeCount(item.derivatives);\\n        const right_count = selectedRightDerivativeCount(item.derivatives);\\n        const falling = fallingPower(item.exponent, derivative_count) orelse return null;\\n        const signed = checkedMulInt(signFromParity(right_count), falling) orelse return null;\\n        return .{\\n            .scalar = coefficient.rational(signed, 1) orelse return null,\\n            .kernel = .{ .difference_power = .{\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .exponent = checkedAddExponent(item.exponent, derivative_count) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn evaluateLogarithm(item: anytype) ?coefficient.CoordinateFactor {\\n        const derivative_count = selectedDerivativeCount(item.derivatives);\\n        if (derivative_count == 0) {\\n            return .{\\n                .scalar = coefficient.integer(1),\\n                .kernel = .{ .logarithm = coordinateDifference(item.coordinate) },\\n            };\\n        }\\n\\n        const right_count = selectedRightDerivativeCount(item.derivatives);\\n        const base = factorial(derivative_count - 1) orelse return null;\\n        const signed = checkedMulInt(signFromParity(right_count + derivative_count - 1), base) orelse return null;\\n        return .{\\n            .scalar = coefficient.rational(signed, 1) orelse return null,\\n            .kernel = .{ .difference_power = .{\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .exponent = -@as(i16, @intCast(derivative_count)),\\n            } },\\n        };\\n    }\\n\\n    fn evaluateGreenKernel(item: anytype) coefficient.CoordinateFactor {\\n        return .{\\n            .scalar = coefficient.integer(1),\\n            .kernel = .{ .green_kernel = .{\\n                .name = item.name,\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .left_derivatives = if (item.derivatives.left) |count| count else 0,\\n                .right_derivatives = if (item.derivatives.right) |count| count else 0,\\n            } },\\n        };\\n    }\\n\\n    fn evaluateCoordinateFactor(factor: ResolvedCoordinateFactor) ?coefficient.CoordinateFactor {\\n        return switch (factor) {\\n            .difference_power => |item| evaluateDifferencePower(item),\\n            .logarithm => |item| evaluateLogarithm(item),\\n            .green_kernel => |item| evaluateGreenKernel(item),\\n            .green_exponential => |coordinate| .{\\n                .scalar = coefficient.integer(1),\\n                .kernel = .{ .green_exponential = coordinateDifference(coordinate) },\\n            },\\n        };\\n    }\\n\\n    const WickTermEvent = struct {\\n        left_index: usize,\\n        right_index: usize,\\n        term_index: usize,\\n    };\\n\\n    const MatchedWickTerm = struct {\\n        pair: WickPair,\\n        rule_index: usize,\\n        term_index: usize,\\n    };\\n};\\n\\nconst ZeroModeRuntime = struct {\\n    const ResidualCursor = struct {\\n        ops: kernel.Call.MultiOp,\\n        indices: ?[]const usize,\\n\\n        fn len(self: ResidualCursor) usize {\\n            return if (self.indices) |indices| indices.len else self.ops.operators.len;\\n        }\\n\\n        fn opIndex(self: ResidualCursor, residual_index: usize) usize {\\n            return if (self.indices) |indices| indices[residual_index] else residual_index;\\n        }\\n\\n        fn op(self: ResidualCursor, residual_index: usize) kernel.Call.LocalOp {\\n            return self.ops.operators[self.opIndex(residual_index)];\\n        }\\n    };\\n\\n    const CJet = struct {\\n        coordinate: coefficient.Variable,\\n        derivative_order: u8,\\n    };\\n\\n    const MomentumDelta = struct {\\n        projector: ?ConfigValue,\\n        momenta: []const kernel.Call.LabelValue,\\n        two_pi_power: u16,\\n        scalar: RuleScalar,\\n    };\\n\\n    const DirichletPhase = struct {\\n        projector: ConfigValue,\\n        position: ConfigValue,\\n        momenta: []const kernel.Call.LabelValue,\\n    };\\n\\n    const ProfileFactor = struct {\\n        profile: kernel.Call.LabelValue,\\n        coordinate: coefficient.Variable,\\n    };\\n\\n    const ZeroModeFactor = union(enum) {\\n        bc_top_form: struct {\\n            support: zero_mode.BcSupport,\\n            normalization: RuleScalar,\\n            jets: [3]CJet,\\n        },\\n        eta_xi_zero_mode: struct {\\n            support: zero_mode.EtaXiSupport,\\n            normalization: RuleScalar,\\n            xi: CJet,\\n        },\\n        momentum_delta: MomentumDelta,\\n        dirichlet_phase: DirichletPhase,\\n        profile_fourier_integral: ProfileFactor,\\n        profile_polynomial_integral: ProfileFactor,\\n        profile_gaussian_differential: ProfileFactor,\\n    };\\n};\\n\\nconst TextBudget = enum {\\n    small,\\n    medium,\\n};\\n\\n/// text exposes bounded result-inspection sinks.\\npub const text = struct {\\n    /// compact returns a writer-backed sink for small symbolic correlators.\\n    pub fn compact(writer: anytype, comptime budget: TextBudget) CompactTextSink(@TypeOf(writer), budget) {\\n        return .{ .writer = writer };\\n    }\\n};\\n\\nfn textByteLimit(comptime budget: TextBudget) usize {\\n    return switch (budget) {\\n        .small => 4096,\\n        .medium => 65536,\\n    };\\n}\\n\\nfn textBranchLimit(comptime budget: TextBudget) usize {\\n    return switch (budget) {\\n        .small => 32,\\n        .medium => 1024,\\n    };\\n}\\n\\nfn CompactTextSink(comptime Writer: type, comptime budget: TextBudget) type {\\n    return struct {\\n        const Self = @This();\\n\\n        writer: Writer,\\n        bytes_written: usize = 0,\\n        branch_count: usize = 0,\\n        branch_open: bool = false,\\n        branch_has_factor: bool = false,\\n        term_open: bool = false,\\n        term_has_factor: bool = false,\\n        pending_sign: i8 = 1,\\n\\n        fn writeAll(self: *Self, bytes: []const u8) !void {\\n            if (self.bytes_written + bytes.len > textByteLimit(budget)) return error.RenderLimitExceeded;\\n            try self.writer.writeAll(bytes);\\n            self.bytes_written += bytes.len;\\n        }\\n\\n        fn writeFmt(self: *Self, comptime fmt: []const u8, args: anytype) !void {\\n            var buffer: [256]u8 = undefined;\\n            const rendered = std.fmt.bufPrint(&buffer, fmt, args) catch return error.RenderLimitExceeded;\\n            try self.writeAll(rendered);\\n        }\\n\\n        fn beginBranch(self: *Self) !void {\\n            if (self.branch_open) return;\\n            if (self.branch_count == textBranchLimit(budget)) return error.RenderLimitExceeded;\\n            if (self.branch_count != 0) {\\n                try self.writeAll(if (self.pending_sign < 0) \\" - \\" else \\" + \\");\\n            } else if (self.pending_sign < 0) {\\n                try self.writeAll(\\"-\\");\\n            }\\n            self.branch_open = true;\\n            self.branch_has_factor = false;\\n            self.pending_sign = 1;\\n        }\\n\\n        fn factorSeparator(self: *Self) !void {\\n            try self.beginBranch();\\n            if (self.term_open) {\\n                if (self.term_has_factor) try self.writeAll(\\"*\\");\\n                self.term_has_factor = true;\\n                return;\\n            }\\n            if (self.branch_has_factor) try self.writeAll(\\"*\\");\\n            self.branch_has_factor = true;\\n        }\\n\\n        fn writeRational(self: *Self, value: RationalScalar) !void {\\n            if (value.denominator == 1) return self.writeFmt(\\"{}\\", .{value.numerator});\\n            return self.writeFmt(\\"{}/{}\\", .{ value.numerator, value.denominator });\\n        }\\n\\n        fn writeRuleScalar(self: *Self, value: RuleScalar) !void {\\n            switch (value) {\\n                .one => try self.writeAll(\\"1\\"),\\n                .rational => |rational| try self.writeRational(rational),\\n                .monomial => |monomial| {\\n                    try self.writeRational(monomial.rational);\\n                    if (monomial.imaginary_power != 0) try self.writeFmt(\\"*i^{}\\", .{monomial.imaginary_power});\\n                    if (monomial.atom) |atom| try self.writeFmt(\\"*s{}^{}\\", .{ atom, monomial.atom_power });\\n                },\\n            }\\n        }\\n\\n        fn writeLabel(self: *Self, value: kernel.Call.LabelValue) !void {\\n            switch (value) {\\n                .integer => |item| try self.writeFmt(\\"{}\\", .{item}),\\n                .rational => |item| try self.writeFmt(\\"{}/{}\\", .{ item.numerator, item.denominator }),\\n                .tensor => |item| try self.writeFmt(\\"T{}\\", .{item}),\\n                .symbol => |item| try self.writeFmt(\\"q{}\\", .{item}),\\n            }\\n        }\\n\\n        fn writeConfig(self: *Self, value: ConfigValue) !void {\\n            switch (value) {\\n                .tensor_projector => |item| try self.writeFmt(\\"P{}\\", .{@intFromEnum(item)}),\\n                .target_point => |item| try self.writeFmt(\\"x{}\\", .{@intFromEnum(item)}),\\n                .target_dimension => |item| try self.writeFmt(\\"D{}\\", .{item}),\\n                .boundary_stack => |item| try self.writeFmt(\\"CP{}\\", .{@intFromEnum(item)}),\\n            }\\n        }\\n\\n        fn writeTensorRef(self: *Self, value: Correlator.ResolvedTensorRef) !void {\\n            switch (value) {\\n                .label => |item| try self.writeLabel(item),\\n                .config => |item| try self.writeConfig(item),\\n            }\\n        }\\n\\n        fn writeCoordinateDifference(self: *Self, value: coefficient.CoordinateDifference) !void {\\n            try self.writeFmt(\\"(z{}-z{})\\", .{ value.left, value.right });\\n        }\\n\\n        pub fn emitWickBranchSign(self: *Self, sign: i8) !void {\\n            if (sign < 0) self.pending_sign = -1;\\n        }\\n\\n        pub fn emitWickBranchTerm(self: *Self, branch: anytype) !void {\\n            const Proxy = struct {\\n                renderer: *Self,\\n\\n                pub fn emitWickTermStart(proxy: *@This(), event: Correlator.WickTermEvent) !void {\\n                    try proxy.renderer.emitWickTermStart(event);\\n                }\\n\\n                pub fn emitWickScalar(proxy: *@This(), factor: Correlator.ResolvedScalarFactor) !void {\\n                    try proxy.renderer.emitWickScalar(factor);\\n                }\\n\\n                pub fn emitWickCoordinate(proxy: *@This(), factor: coefficient.CoordinateFactor) !void {\\n                    try proxy.renderer.emitWickCoordinate(factor);\\n                }\\n\\n                pub fn emitWickTensor(proxy: *@This(), factor: Correlator.ResolvedTensorFactor) !void {\\n                    try proxy.renderer.emitWickTensor(factor);\\n                }\\n\\n                pub fn emitWickAction(proxy: *@This(), factor: Correlator.ResolvedActionFactor) !void {\\n                    try proxy.renderer.emitWickAction(factor);\\n                }\\n\\n                pub fn emitWickResidualOperator(proxy: *@This(), residual: Correlator.WickResidualRef) !void {\\n                    try proxy.renderer.emitWickResidualOperator(residual);\\n                }\\n\\n                pub fn emitWickTermEnd(proxy: *@This()) !void {\\n                    try proxy.renderer.emitWickTermEnd();\\n                }\\n            };\\n\\n            var proxy = Proxy{ .renderer = self };\\n            try branch.emit(&proxy);\\n        }\\n\\n        pub fn emitWickTermStart(self: *Self, _: Correlator.WickTermEvent) !void {\\n            try self.factorSeparator();\\n            try self.writeAll(\\"(\\");\\n            self.term_open = true;\\n            self.term_has_factor = false;\\n        }\\n\\n        pub fn emitWickScalar(self: *Self, factor: Correlator.ResolvedScalarFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .value => |value| try self.writeRuleScalar(value),\\n                .label_bilinear_phase => |item| {\\n                    try self.writeFmt(\\"phase[{s}](\\", .{item.form});\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .cocycle => |item| {\\n                    try self.writeFmt(\\"cocycle[{s}](\\", .{item.table});\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .spin_structure => |name| try self.writeFmt(\\"spin[{s}]\\", .{name}),\\n            }\\n        }\\n\\n        pub fn emitWickCoordinate(self: *Self, factor: coefficient.CoordinateFactor) !void {\\n            try self.factorSeparator();\\n            if (factor.scalar.numerator != 1 or factor.scalar.denominator != 1) {\\n                try self.writeFmt(\\"{}/{}*\\", .{ factor.scalar.numerator, factor.scalar.denominator });\\n            }\\n            switch (factor.kernel) {\\n                .difference_power => |item| {\\n                    try self.writeCoordinateDifference(item.coordinate);\\n                    try self.writeFmt(\\"^{}\\", .{item.exponent});\\n                },\\n                .logarithm => |coordinate| {\\n                    try self.writeAll(\\"log\\");\\n                    try self.writeCoordinateDifference(coordinate);\\n                },\\n                .green_kernel => |item| {\\n                    try self.writeFmt(\\"G[{s};{},{}]\\", .{ item.name, item.left_derivatives, item.right_derivatives });\\n                    try self.writeCoordinateDifference(item.coordinate);\\n                },\\n                .green_exponential => |coordinate| {\\n                    try self.writeAll(\\"expG\\");\\n                    try self.writeCoordinateDifference(coordinate);\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickTensor(self: *Self, factor: Correlator.ResolvedTensorFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .none => try self.writeAll(\\"1\\"),\\n                .metric => |item| {\\n                    try self.writeAll(\\"eta(\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .momentum_index => |item| {\\n                    try self.writeAll(\\"p(\\");\\n                    try self.writeLabel(item.momentum);\\n                    try self.writeAll(\\"; \\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .momentum_pair => |item| {\\n                    try self.writeAll(\\"p(\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\").p(\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_metric => |item| {\\n                    try self.writeAll(\\"eta[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_momentum_index => |item| {\\n                    try self.writeAll(\\"p[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.momentum);\\n                    try self.writeAll(\\"; \\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_momentum_pair => |item| {\\n                    try self.writeAll(\\"p[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickAction(self: *Self, factor: Correlator.ResolvedActionFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .profile_derivative => |item| {\\n                    try self.writeAll(\\"dProfile(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projected_profile_derivative => |item| {\\n                    try self.writeAll(\\"dProfile[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickResidualOperator(self: *Self, residual: Correlator.WickResidualRef) !void {\\n            try self.factorSeparator();\\n            try self.writeFmt(\\"op{}\\", .{residual.input_index});\\n        }\\n\\n        pub fn emitWickTermEnd(self: *Self) !void {\\n            if (!self.term_has_factor) try self.writeAll(\\"1\\");\\n            try self.writeAll(\\")\\");\\n            self.term_open = false;\\n            self.branch_has_factor = true;\\n        }\\n\\n        pub fn emitZeroModeFactor(self: *Self, factor: ZeroModeRuntime.ZeroModeFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .bc_top_form => |item| try self.writeFmt(\\"bcTop[{s}]\\", .{@tagName(item.support)}),\\n                .eta_xi_zero_mode => |item| try self.writeFmt(\\"etaXiZero[{s};z{}]\\", .{ @tagName(item.support), item.xi.coordinate }),\\n                .momentum_delta => |item| try self.writeFmt(\\"deltaP[{}]\\", .{item.momenta.len}),\\n                .dirichlet_phase => |item| {\\n                    try self.writeAll(\\"dirichletPhase[\\");\\n                    try self.writeConfig(item.projector);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeConfig(item.position);\\n                    try self.writeAll(\\"]\\");\\n                },\\n                .profile_fourier_integral => |item| {\\n                    try self.writeAll(\\"profileFourier(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n                .profile_polynomial_integral => |item| {\\n                    try self.writeAll(\\"profilePolynomial(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n                .profile_gaussian_differential => |item| {\\n                    try self.writeAll(\\"profileGaussian(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n            }\\n        }\\n\\n        pub fn emitZeroModeBaseEnd(self: *Self) !void {\\n            try self.beginBranch();\\n            if (!self.branch_has_factor) try self.writeAll(\\"1\\");\\n            self.branch_open = false;\\n            self.branch_has_factor = false;\\n            self.branch_count += 1;\\n        }\\n    };\\n}\\n\\nfn residualConsumed(mask: u128, index: usize) bool {\\n    return (mask & (@as(u128, 1) << @intCast(index))) != 0;\\n}\\n\\nfn markResidualConsumed(mask: *u128, index: usize) void {\\n    mask.* |= @as(u128, 1) << @intCast(index);\\n}\\n\\nfn kindIn(kind: operators.OperatorKindId, candidates: []const operators.OperatorKindId) bool {\\n    for (candidates) |candidate| {\\n        if (candidate == kind) return true;\\n    }\\n    return false;\\n}\\n\\nfn localPosition(op: kernel.Call.LocalOp) ?coefficient.Variable {\\n    return switch (op.insertion) {\\n        .single => |single| single.position,\\n        .pair => |pair| pair.holomorphic_position,\\n    };\\n}\\n\\nfn localDerivativeOrder(op: kernel.Call.LocalOp) u8 {\\n    return switch (op.insertion) {\\n        .single => |single| single.derivatives,\\n        .pair => |pair| pair.holomorphic_derivatives,\\n    };\\n}\\n\\nfn firstLabel(ops: kernel.Call.MultiOp, op: kernel.Call.LocalOp) ?kernel.Call.LabelValue {\\n    const index: usize = @intCast(op.labels);\\n    if (index >= ops.labels.values.len) return null;\\n    return ops.labels.values[index];\\n}\\n\\nfn configValue(config_ptr: *const CorrelatorConfigData, config_ref: ConfigRef) ?ConfigValue {\\n    for (config_ptr.config_entries) |entry| {\\n        if (entry.id == config_ref) return entry.value;\\n    }\\n    return null;\\n}\\n\\nfn projectorRank(value: ConfigValue) ?u16 {\\n    return switch (value) {\\n        .tensor_projector => |projector| @intCast(@intFromEnum(projector) >> 24),\\n        else => null,\\n    };\\n}\\n\\nfn rankValue(config_ptr: *const CorrelatorConfigData, source: zero_mode.RankSource) ?u16 {\\n    return switch (source) {\\n        .none => 0,\\n        .literal => |value| value,\\n        .target_dimension => |config_ref| switch (configValue(config_ptr, config_ref) orelse return null) {\\n            .target_dimension => |dimension| dimension,\\n            else => null,\\n        },\\n        .projector_rank => |config_ref| projectorRank(configValue(config_ptr, config_ref) orelse return null),\\n    };\\n}\\n\\nfn hasRankSource(source: zero_mode.RankSource) bool {\\n    return switch (source) {\\n        .none => false,\\n        else => true,\\n    };\\n}\\n\\nfn emitBcTopForm(rule: zero_mode.BcTopForm, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var jets: [3]ZeroModeRuntime.CJet = undefined;\\n    var residual_ids: [3]usize = undefined;\\n    var jet_count: usize = 0;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.c_kind_ids)) continue;\\n        if (jet_count >= jets.len) {\\n            overflow = true;\\n            continue;\\n        }\\n        jets[jet_count] = .{\\n            .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            .derivative_order = localDerivativeOrder(op),\\n        };\\n        residual_ids[jet_count] = residual_index;\\n        jet_count += 1;\\n    }\\n\\n    if (overflow or jet_count != 3) return;\\n    for (residual_ids) |residual_id| {\\n        markResidualConsumed(consumed, residual_id);\\n    }\\n    try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .bc_top_form = .{\\n        .support = rule.support,\\n        .normalization = rule.normalization,\\n        .jets = jets,\\n    } });\\n}\\n\\nfn acceptBcTopForm(rule: zero_mode.BcTopForm, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var residual_ids: [3]usize = undefined;\\n    var jet_count: usize = 0;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.c_kind_ids)) continue;\\n        if (localPosition(op) == null) return error.InvalidZeroModeOperator;\\n        if (jet_count >= residual_ids.len) {\\n            overflow = true;\\n            continue;\\n        }\\n        residual_ids[jet_count] = residual_index;\\n        jet_count += 1;\\n    }\\n\\n    if (overflow or jet_count != 3) return;\\n    for (residual_ids) |residual_id| {\\n        markResidualConsumed(consumed, residual_id);\\n    }\\n}\\n\\nfn emitEtaXiZeroMode(rule: zero_mode.EtaXiZeroMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var xi: ZeroModeRuntime.CJet = undefined;\\n    var residual_id: usize = 0;\\n    var found = false;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.xi_kind_ids)) continue;\\n        if (localDerivativeOrder(op) != 0 or found) {\\n            overflow = true;\\n            continue;\\n        }\\n        xi = .{\\n            .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            .derivative_order = 0,\\n        };\\n        residual_id = residual_index;\\n        found = true;\\n    }\\n\\n    if (overflow or !found) return;\\n    markResidualConsumed(consumed, residual_id);\\n    try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .eta_xi_zero_mode = .{\\n        .support = rule.support,\\n        .normalization = rule.normalization,\\n        .xi = xi,\\n    } });\\n}\\n\\nfn acceptEtaXiZeroMode(rule: zero_mode.EtaXiZeroMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var residual_id: usize = 0;\\n    var found = false;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.xi_kind_ids)) continue;\\n        if (localPosition(op) == null) return error.InvalidZeroModeOperator;\\n        if (localDerivativeOrder(op) != 0 or found) {\\n            overflow = true;\\n            continue;\\n        }\\n        residual_id = residual_index;\\n        found = true;\\n    }\\n\\n    if (overflow or !found) return;\\n    markResidualConsumed(consumed, residual_id);\\n}\\n\\nfn emitFreeBosonConstantMode(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var momenta: [limits.max_zero_mode_items]kernel.Call.LabelValue = undefined;\\n    var momentum_count: usize = 0;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (kindIn(op.kind, rule.exp_kind_ids)) {\\n            if (momentum_count >= momenta.len) return error.TooManyZeroModeItems;\\n            momenta[momentum_count] = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            momentum_count += 1;\\n            markResidualConsumed(consumed, residual_index);\\n            continue;\\n        }\\n\\n        if (kindIn(op.kind, rule.profile_kind_ids)) {\\n            const label = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            const profile = switch (label) {\\n                .symbol => |value| @as(Handle.Profile, @enumFromInt(value)),\\n                else => return error.InvalidZeroModeOperator,\\n            };\\n            const factor = ZeroModeRuntime.ProfileFactor{\\n                .profile = label,\\n                .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            };\\n            switch (profilePresentation(profile)) {\\n                .position_space => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_gaussian_differential = factor }),\\n                .fourier => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_fourier_integral = factor }),\\n                .polynomial_rnc => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_polynomial_integral = factor }),\\n            }\\n            markResidualConsumed(consumed, residual_index);\\n        }\\n    }\\n\\n    if (momentum_count != 0 or hasRankSource(rule.normalization.two_pi_power)) {\\n        const projector = if (rule.integration_projector) |config_ref| configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig else null;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .momentum_delta = .{\\n            .projector = projector,\\n            .momenta = momenta[0..momentum_count],\\n            .two_pi_power = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig,\\n            .scalar = rule.normalization.scalar,\\n        } });\\n    }\\n\\n    if (rule.fixed_projector) |projector_ref| {\\n        if (momentum_count == 0) return;\\n        const position_ref = rule.fixed_position orelse return error.InvalidZeroModeConfig;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .dirichlet_phase = .{\\n            .projector = configValue(config_ptr, projector_ref) orelse return error.InvalidZeroModeConfig,\\n            .position = configValue(config_ptr, position_ref) orelse return error.InvalidZeroModeConfig,\\n            .momenta = momenta[0..momentum_count],\\n        } });\\n    }\\n}\\n\\nfn acceptFreeBosonConstantMode(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var momentum_count: usize = 0;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (kindIn(op.kind, rule.exp_kind_ids)) {\\n            if (momentum_count >= limits.max_zero_mode_items) return error.TooManyZeroModeItems;\\n            _ = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            momentum_count += 1;\\n            markResidualConsumed(consumed, residual_index);\\n            continue;\\n        }\\n\\n        if (kindIn(op.kind, rule.profile_kind_ids)) {\\n            const label = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            const profile = switch (label) {\\n                .symbol => |value| @as(Handle.Profile, @enumFromInt(value)),\\n                else => return error.InvalidZeroModeOperator,\\n            };\\n            _ = localPosition(op) orelse return error.InvalidZeroModeOperator;\\n            _ = profilePresentation(profile);\\n            markResidualConsumed(consumed, residual_index);\\n        }\\n    }\\n\\n    if (momentum_count != 0 or hasRankSource(rule.normalization.two_pi_power)) {\\n        if (rule.integration_projector) |config_ref| _ = configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig;\\n        _ = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig;\\n    }\\n\\n    if (rule.fixed_projector) |projector_ref| {\\n        if (momentum_count == 0) return;\\n        const position_ref = rule.fixed_position orelse return error.InvalidZeroModeConfig;\\n        _ = configValue(config_ptr, projector_ref) orelse return error.InvalidZeroModeConfig;\\n        _ = configValue(config_ptr, position_ref) orelse return error.InvalidZeroModeConfig;\\n    }\\n}\\n\\nfn allResidualsConsumed(residual: ZeroModeRuntime.ResidualCursor, consumed: u128) bool {\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (!residualConsumed(consumed, residual_index)) return false;\\n    }\\n    return true;\\n}\\n\\nfn emitEmptyFreeBosonConstantMode(config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, sink: anytype) !void {\\n    if (hasRankSource(rule.normalization.two_pi_power)) {\\n        const empty_momenta: [0]kernel.Call.LabelValue = .{};\\n        const projector = if (rule.integration_projector) |config_ref| configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig else null;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .momentum_delta = .{\\n            .projector = projector,\\n            .momenta = empty_momenta[0..],\\n            .two_pi_power = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig,\\n            .scalar = rule.normalization.scalar,\\n        } });\\n    }\\n}\\n\\nfn BcTopFormProcedure(comptime rule: zero_mode.BcTopForm) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.c_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return acceptBcTopForm(rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return emitBcTopForm(rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            _ = config_ptr;\\n            _ = sink;\\n        }\\n    };\\n}\\n\\nfn EtaXiZeroModeProcedure(comptime rule: zero_mode.EtaXiZeroMode) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.xi_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return acceptEtaXiZeroMode(rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return emitEtaXiZeroMode(rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            _ = config_ptr;\\n            _ = sink;\\n        }\\n    };\\n}\\n\\nfn ConstantFermionProcedure(comptime rule: zero_mode.ConstantFermion) type {\\n    const adapted = zero_mode.EtaXiZeroMode{\\n        .support = rule.support,\\n        .xi_kind_ids = rule.fermion_kind_ids,\\n        .normalization = rule.normalization,\\n    };\\n    return EtaXiZeroModeProcedure(adapted);\\n}\\n\\nfn TopFormFermionProcedure(comptime rule: zero_mode.TopFormFermion) type {\\n    const adapted = zero_mode.BcTopForm{\\n        .support = rule.support,\\n        .c_kind_ids = rule.field_kind_ids,\\n        .normalization = rule.normalization,\\n    };\\n    return BcTopFormProcedure(adapted);\\n}\\n\\nfn FreeBosonConstantModeProcedure(comptime rule: zero_mode.FreeBosonConstantMode) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.exp_kind_ids) or kindIn(kind, rule.profile_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            return acceptFreeBosonConstantMode(limits, config_ptr, rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            return emitFreeBosonConstantMode(limits, config_ptr, rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            return emitEmptyFreeBosonConstantMode(config_ptr, rule, sink);\\n        }\\n    };\\n}\\n\\nfn ZeroModeProcedure(comptime expr: zero_mode.Expr) type {\\n    return switch (expr) {\\n        .bc_top_form => |rule| BcTopFormProcedure(rule),\\n        .free_boson_constant_mode => |rule| FreeBosonConstantModeProcedure(rule),\\n        .eta_xi_zero_mode => |rule| EtaXiZeroModeProcedure(rule),\\n        .constant_fermion => |rule| ConstantFermionProcedure(rule),\\n        .top_form_fermion => |rule| TopFormFermionProcedure(rule),\\n        .boson_momentum_conservation => |rule| FreeBosonConstantModeProcedure(rule),\\n    };\\n}\\n\\nfn generatedZeroModeCanConsumeKind(comptime ConfigHandle: type, kind: operators.OperatorKindId) bool {\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        if (ZeroModeProcedure(rule.expr).canConsumeKind(kind)) return true;\\n    }\\n    return false;\\n}\\n\\nfn generatedZeroModeCursorSucceeds(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor) !bool {\\n    const limits = ConfigHandle.branch_limits;\\n    if (residual.len() > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n\\n    var consumed: u128 = 0;\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).accept(limits, config_ptr, residual, &consumed);\\n    }\\n\\n    return allResidualsConsumed(residual, consumed);\\n}\\n\\nfn generatedZeroModeBaseCaseSucceeds(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: []const usize) !bool {\\n    if (residual_indices.len == 0) return true;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    return generatedZeroModeCursorSucceeds(ConfigHandle, config_ptr, residual);\\n}\\n\\nfn generatedEmptyZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emitEmpty(config_ptr, sink);\\n    }\\n    try sink.emitZeroModeBaseEnd();\\n}\\n\\nfn generatedTrustedZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: []const usize, sink: anytype) !void {\\n    const limits = ConfigHandle.branch_limits;\\n    if (residual_indices.len > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n    if (residual_indices.len == 0) {\\n        try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        return;\\n    }\\n\\n    var consumed: u128 = 0;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    // The branch walker has already dry-run validated these residuals.\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emit(limits, config_ptr, residual, &consumed, sink);\\n    }\\n    if (!allResidualsConsumed(residual, consumed)) return error.InvalidWickBranch;\\n    try sink.emitZeroModeBaseEnd();\\n}\\n\\n/// generatedZeroModeBaseCase consumes residual operators with configured zero-mode procedures.\\nfn generatedZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: ?[]const usize, sink: anytype) !bool {\\n    const limits = ConfigHandle.branch_limits;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    if (residual.len() > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n    if (residual.len() == 0) {\\n        try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        return true;\\n    }\\n    if (!try generatedZeroModeCursorSucceeds(ConfigHandle, config_ptr, residual)) return false;\\n\\n    var consumed: u128 = 0;\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emit(limits, config_ptr, residual, &consumed, sink);\\n    }\\n\\n    if (!allResidualsConsumed(residual, consumed)) return false;\\n    try sink.emitZeroModeBaseEnd();\\n    return true;\\n}\\n\\nfn ruleMatches(rule: wick.Rule, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) ?bool {\\n    if (rule.left.kind == left.kind and rule.right.kind == right.kind) {\\n        return false;\\n    }\\n    if (rule.left.kind == right.kind and rule.right.kind == left.kind) {\\n        return true;\\n    }\\n    return null;\\n}\\n\\nfn matchRule(config_ptr: *const CorrelatorConfigData, rule: *const wick.Rule, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, labels: *const kernel.Call.LabelStore) ?Correlator.WickPair {\\n    const reversed = ruleMatches(rule.*, left, right) orelse return null;\\n    return .{\\n        .rule = rule,\\n        .config = config_ptr,\\n        .left = left,\\n        .right = right,\\n        .labels = labels,\\n        .reversed = reversed,\\n    };\\n}\\n\\nfn lowerBoundPairLookup(entries: []const PairLookupEntry, key: u64) usize {\\n    var lo: usize = 0;\\n    var hi: usize = entries.len;\\n    while (lo < hi) {\\n        const mid = lo + ((hi - lo) / 2);\\n        if (entries[mid].key < key) {\\n            lo = mid + 1;\\n        } else {\\n            hi = mid;\\n        }\\n    }\\n    return lo;\\n}\\n\\nfn emitPrimitiveWickTerm(pair: Correlator.WickPair, left_index: usize, right_index: usize, term_index: usize, sink: anytype) !void {\\n    const primitive = (Correlator.PrimitiveWickPair{ .pair = pair }).term(term_index) orelse return error.InvalidWickTerm;\\n\\n    const event = Correlator.WickTermEvent{\\n        .left_index = left_index,\\n        .right_index = right_index,\\n        .term_index = term_index,\\n    };\\n    try sink.emitWickTermStart(event);\\n\\n    var scalar_index: usize = 0;\\n    while (scalar_index < primitive.scalarCount()) : (scalar_index += 1) {\\n        try sink.emitWickScalar(primitive.scalar(scalar_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var coordinate_index: usize = 0;\\n    while (coordinate_index < primitive.coordinateCount()) : (coordinate_index += 1) {\\n        try sink.emitWickCoordinate(primitive.coordinate(coordinate_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var tensor_index: usize = 0;\\n    while (tensor_index < primitive.tensorCount()) : (tensor_index += 1) {\\n        try sink.emitWickTensor(primitive.tensor(tensor_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var action_index: usize = 0;\\n    while (action_index < primitive.actionCount()) : (action_index += 1) {\\n        try sink.emitWickAction(primitive.action(action_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < primitive.residualCount()) : (residual_index += 1) {\\n        try emitWickResidualOperator(pair, left_index, right_index, primitive.residual(residual_index) orelse return error.InvalidWickFactor, sink);\\n    }\\n\\n    try sink.emitWickTermEnd();\\n}\\n\\nfn wickPairTermPayloadFrom(rule_index: usize, term_index: usize, reversed: bool) !u64 {\\n    if (rule_index > std.math.maxInt(u32)) return error.TooManyWickRules;\\n    if (term_index > std.math.maxInt(u16)) return error.TooManyWickTerms;\\n    return @as(u64, @intCast(rule_index)) |\\n        (@as(u64, @intCast(term_index)) << 32) |\\n        (@as(u64, @intFromBool(reversed)) << 48);\\n}\\n\\nfn wickPayloadRuleIndex(payload: u64) usize {\\n    return @intCast(payload & 0xffff_ffff);\\n}\\n\\nfn wickPayloadTermIndex(payload: u64) usize {\\n    return @intCast((payload >> 32) & 0xffff);\\n}\\n\\nfn wickPayloadReversed(payload: u64) bool {\\n    return ((payload >> 48) & 1) != 0;\\n}\\n\\nfn actualSideIndex(pair: Correlator.WickPair, left_index: usize, right_index: usize, side: wick.Side) usize {\\n    return switch (side) {\\n        .left => if (pair.reversed) right_index else left_index,\\n        .right => if (pair.reversed) left_index else right_index,\\n    };\\n}\\n\\nfn emitWickResidualOperator(pair: Correlator.WickPair, left_index: usize, right_index: usize, side: wick.Side, sink: anytype) !void {\\n    const SinkPtr = @TypeOf(sink);\\n    const sink_info = @typeInfo(SinkPtr);\\n    if (sink_info != .pointer) return;\\n    const Sink = sink_info.pointer.child;\\n    if (!@hasDecl(Sink, \\"emitWickResidualOperator\\")) return;\\n\\n    const input_index = actualSideIndex(pair, left_index, right_index, side);\\n    const operator = switch (side) {\\n        .left => pair.sideOp(.left),\\n        .right => pair.sideOp(.right),\\n    };\\n    try sink.emitWickResidualOperator(Correlator.WickResidualRef{\\n        .input_index = input_index,\\n        .operator = operator,\\n    });\\n}\\n\\nconst cache_pair_direct_width = 16;\\nconst cache_pair_hash_slots = cache_pair_direct_width * cache_pair_direct_width;\\nconst no_cached_pair = std.math.maxInt(u16);\\n\\nfn branchBudgetOperatorCount(comptime budget: BranchBudget) usize {\\n    return switch (budget) {\\n        .standard => 128,\\n    };\\n}\\n\\nfn branchBudgetCacheTerms(comptime budget: BranchBudget) usize {\\n    return switch (budget) {\\n        .standard => 66,\\n    };\\n}\\n\\nfn maxTermFactors(comptime rules: []const wick.Rule, comptime selector: enum { scalars, coordinates, tensors, actions, residuals }) usize {\\n    var result: usize = 0;\\n    for (rules) |rule| {\\n        for (rule.expr.terms) |term| {\\n            const count = switch (selector) {\\n                .scalars => term.scalars.len,\\n                .coordinates => term.coordinates.len,\\n                .tensors => term.tensors.len,\\n                .actions => term.actions.len,\\n                .residuals => term.residuals.len,\\n            };\\n            if (count > result) result = count;\\n        }\\n    }\\n    return result;\\n}\\n\\nfn atLeastOne(value: usize) usize {\\n    return if (value == 0) 1 else value;\\n}\\n\\nfn branchLimits(comptime input: CorrelatorConfigInput) BranchLimits {\\n    const max_operators = branchBudgetOperatorCount(input.branch_budget);\\n    if (max_operators > 128) @compileError(\\"zero-mode consumption mask supports at most 128 residual operators\\");\\n    const max_terms = max_operators / 2;\\n    const max_cached_terms = branchBudgetCacheTerms(input.branch_budget);\\n    const scalar_terms = atLeastOne(maxTermFactors(input.wick_rules, .scalars));\\n    const coordinate_terms = atLeastOne(maxTermFactors(input.wick_rules, .coordinates));\\n    const tensor_terms = atLeastOne(maxTermFactors(input.wick_rules, .tensors));\\n    const action_terms = atLeastOne(maxTermFactors(input.wick_rules, .actions));\\n    const residual_terms = atLeastOne(maxTermFactors(input.wick_rules, .residuals));\\n\\n    return .{\\n        .max_operators = max_operators,\\n        .max_zero_mode_residuals = max_operators,\\n        .max_zero_mode_items = max_operators,\\n        .max_terms = max_terms,\\n        .max_scalars = scalar_terms * max_terms,\\n        .max_coordinates = coordinate_terms * max_terms,\\n        .max_tensors = tensor_terms * max_terms,\\n        .max_actions = action_terms * max_terms,\\n        .max_residuals = residual_terms * max_terms,\\n        .max_cached_terms = max_cached_terms,\\n        .max_cached_scalars = scalar_terms * max_cached_terms,\\n        .max_cached_coordinates = coordinate_terms * max_cached_terms,\\n        .max_cached_tensors = tensor_terms * max_cached_terms,\\n        .max_cached_actions = action_terms * max_cached_terms,\\n        .max_cached_residuals = residual_terms * max_cached_terms,\\n    };\\n}\\n\\nconst AccumulatedWickTerm = struct {\\n    event: Correlator.WickTermEvent,\\n    cache_index: u16 = no_cached_pair,\\n    scalar_start: u16,\\n    scalar_count: u16 = 0,\\n    coordinate_start: u16,\\n    coordinate_count: u16 = 0,\\n    tensor_start: u16,\\n    tensor_count: u16 = 0,\\n    action_start: u16,\\n    action_count: u16 = 0,\\n    residual_start: u16,\\n    residual_count: u16 = 0,\\n};\\n\\nconst CachedPairKey = struct {\\n    left: u8,\\n    right: u8,\\n    payload: u64,\\n};\\n\\nfn AccumulatedBranch(comptime limits: BranchLimits) type {\\n    return struct {\\n        const Self = @This();\\n\\n        const Snapshot = struct {\\n            term_count: u16,\\n            scalar_count: u16,\\n            coordinate_count: u16,\\n            tensor_count: u16,\\n            action_count: u16,\\n            residual_count: u16,\\n            logical_scalar_count: u16,\\n            logical_coordinate_count: u16,\\n            logical_tensor_count: u16,\\n            logical_action_count: u16,\\n            logical_residual_count: u16,\\n        };\\n\\n        terms: [limits.max_terms]AccumulatedWickTerm = undefined,\\n        term_count: u16 = 0,\\n        scalars: [limits.max_scalars]Correlator.ResolvedScalarFactor = undefined,\\n        scalar_count: u16 = 0,\\n        coordinates: [limits.max_coordinates]coefficient.CoordinateFactor = undefined,\\n        coordinate_count: u16 = 0,\\n        tensors: [limits.max_tensors]Correlator.ResolvedTensorFactor = undefined,\\n        tensor_count: u16 = 0,\\n        actions: [limits.max_actions]Correlator.ResolvedActionFactor = undefined,\\n        action_count: u16 = 0,\\n        residuals: [limits.max_residuals]Correlator.WickResidualRef = undefined,\\n        residual_count: u16 = 0,\\n        cache_keys: [limits.max_cached_terms]CachedPairKey = undefined,\\n        cache_pair_index: [cache_pair_hash_slots]u16 = [_]u16{no_cached_pair} ** cache_pair_hash_slots,\\n        cache_terms: [limits.max_cached_terms]AccumulatedWickTerm = undefined,\\n        cache_count: u16 = 0,\\n        cache_scalars: [limits.max_cached_scalars]Correlator.ResolvedScalarFactor = undefined,\\n        cache_scalar_count: u16 = 0,\\n        cache_coordinates: [limits.max_cached_coordinates]coefficient.CoordinateFactor = undefined,\\n        cache_coordinate_count: u16 = 0,\\n        cache_tensors: [limits.max_cached_tensors]Correlator.ResolvedTensorFactor = undefined,\\n        cache_tensor_count: u16 = 0,\\n        cache_actions: [limits.max_cached_actions]Correlator.ResolvedActionFactor = undefined,\\n        cache_action_count: u16 = 0,\\n        cache_residuals: [limits.max_cached_residuals]Correlator.WickResidualRef = undefined,\\n        cache_residual_count: u16 = 0,\\n        logical_scalar_count: u16 = 0,\\n        logical_coordinate_count: u16 = 0,\\n        logical_tensor_count: u16 = 0,\\n        logical_action_count: u16 = 0,\\n        logical_residual_count: u16 = 0,\\n\\n        /// init constructs an empty branch factor accumulator.\\n        pub fn init() Self {\\n            return .{};\\n        }\\n\\n        /// snapshot records current array lengths for backtracking.\\n        pub fn snapshot(self: *const Self) Snapshot {\\n            return .{\\n                .term_count = self.term_count,\\n                .scalar_count = self.scalar_count,\\n                .coordinate_count = self.coordinate_count,\\n                .tensor_count = self.tensor_count,\\n                .action_count = self.action_count,\\n                .residual_count = self.residual_count,\\n                .logical_scalar_count = self.logical_scalar_count,\\n                .logical_coordinate_count = self.logical_coordinate_count,\\n                .logical_tensor_count = self.logical_tensor_count,\\n                .logical_action_count = self.logical_action_count,\\n                .logical_residual_count = self.logical_residual_count,\\n            };\\n        }\\n\\n        /// restore truncates accumulated factors to a previous snapshot.\\n        pub fn restore(self: *Self, saved: Snapshot) void {\\n            self.term_count = saved.term_count;\\n            self.scalar_count = saved.scalar_count;\\n            self.coordinate_count = saved.coordinate_count;\\n            self.tensor_count = saved.tensor_count;\\n            self.action_count = saved.action_count;\\n            self.residual_count = saved.residual_count;\\n            self.logical_scalar_count = saved.logical_scalar_count;\\n            self.logical_coordinate_count = saved.logical_coordinate_count;\\n            self.logical_tensor_count = saved.logical_tensor_count;\\n            self.logical_action_count = saved.logical_action_count;\\n            self.logical_residual_count = saved.logical_residual_count;\\n        }\\n\\n        fn currentTerm(self: *Self) *AccumulatedWickTerm {\\n            return &self.terms[self.term_count - 1];\\n        }\\n\\n        fn cachePairSlot(left: usize, right: usize) usize {\\n            if (left < cache_pair_direct_width and right < cache_pair_direct_width) {\\n                return left * cache_pair_direct_width + right;\\n            }\\n            return ((left * 131) ^ right) & (cache_pair_hash_slots - 1);\\n        }\\n\\n        fn findCache(self: *const Self, left: usize, right: usize, payload: u64) ?u16 {\\n            const direct = self.cache_pair_index[cachePairSlot(left, right)];\\n            if (direct != no_cached_pair) {\\n                const key = self.cache_keys[direct];\\n                if (key.left == left and key.right == right and key.payload == payload) return direct;\\n            }\\n\\n            var index: u16 = 0;\\n            while (index < self.cache_count) : (index += 1) {\\n                const key = self.cache_keys[index];\\n                if (key.left == left and key.right == right and key.payload == payload) return index;\\n            }\\n            return null;\\n        }\\n\\n        fn appendCachedTerm(self: *Self, cache_index: u16) !void {\\n            if (self.term_count == self.terms.len) return error.TooManyAccumulatedWickTerms;\\n            const cached = self.cache_terms[cache_index];\\n            self.terms[self.term_count] = cached;\\n            self.terms[self.term_count].cache_index = cache_index;\\n            self.term_count += 1;\\n            self.logical_scalar_count += cached.scalar_count;\\n            self.logical_coordinate_count += cached.coordinate_count;\\n            self.logical_tensor_count += cached.tensor_count;\\n            self.logical_action_count += cached.action_count;\\n            self.logical_residual_count += cached.residual_count;\\n        }\\n\\n        /// primitiveTermCount returns the number of primitive pair terms in this branch.\\n        pub fn primitiveTermCount(self: *const Self) usize {\\n            return self.term_count;\\n        }\\n\\n        /// scalarFactorCount returns the number of scalar factors in this branch.\\n        pub fn scalarFactorCount(self: *const Self) usize {\\n            return self.logical_scalar_count;\\n        }\\n\\n        /// coordinateFactorCount returns the number of coordinate factors in this branch.\\n        pub fn coordinateFactorCount(self: *const Self) usize {\\n            return self.logical_coordinate_count;\\n        }\\n\\n        /// tensorFactorCount returns the number of tensor factors in this branch.\\n        pub fn tensorFactorCount(self: *const Self) usize {\\n            return self.logical_tensor_count;\\n        }\\n\\n        /// actionFactorCount returns the number of action factors in this branch.\\n        pub fn actionFactorCount(self: *const Self) usize {\\n            return self.logical_action_count;\\n        }\\n\\n        /// residualFactorCount returns the number of residual operator refs in this branch.\\n        pub fn residualFactorCount(self: *const Self) usize {\\n            return self.logical_residual_count;\\n        }\\n\\n        /// pushPairTerm appends one cached or freshly resolved primitive pair term.\\n        pub fn pushPairTerm(branch: *Self, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64) !void {\\n            if (branch.findCache(left_index, right_index, payload)) |cache_index| {\\n                try branch.appendCachedTerm(cache_index);\\n                return;\\n            }\\n\\n            if (branch.cache_count == branch.cache_terms.len) {\\n                try emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, branch);\\n                return;\\n            }\\n\\n            const cache_index = branch.cache_count;\\n            const CacheSink = struct {\\n                branch: *Self,\\n                cache_index: u16,\\n\\n                fn current(self: *@This()) *AccumulatedWickTerm {\\n                    return &self.branch.cache_terms[self.cache_index];\\n                }\\n\\n                pub fn emitWickTermStart(self: *@This(), event: Correlator.WickTermEvent) !void {\\n                    self.branch.cache_terms[self.cache_index] = .{\\n                        .event = event,\\n                        .scalar_start = self.branch.cache_scalar_count,\\n                        .coordinate_start = self.branch.cache_coordinate_count,\\n                        .tensor_start = self.branch.cache_tensor_count,\\n                        .action_start = self.branch.cache_action_count,\\n                        .residual_start = self.branch.cache_residual_count,\\n                    };\\n                }\\n\\n                pub fn emitWickScalar(self: *@This(), factor: Correlator.ResolvedScalarFactor) !void {\\n                    if (self.branch.cache_scalar_count == self.branch.cache_scalars.len) return error.TooManyCachedScalars;\\n                    self.branch.cache_scalars[self.branch.cache_scalar_count] = factor;\\n                    self.branch.cache_scalar_count += 1;\\n                    self.current().scalar_count += 1;\\n                }\\n\\n                pub fn emitWickCoordinate(self: *@This(), factor: coefficient.CoordinateFactor) !void {\\n                    if (self.branch.cache_coordinate_count == self.branch.cache_coordinates.len) return error.TooManyCachedCoordinates;\\n                    self.branch.cache_coordinates[self.branch.cache_coordinate_count] = factor;\\n                    self.branch.cache_coordinate_count += 1;\\n                    self.current().coordinate_count += 1;\\n                }\\n\\n                pub fn emitWickTensor(self: *@This(), factor: Correlator.ResolvedTensorFactor) !void {\\n                    if (self.branch.cache_tensor_count == self.branch.cache_tensors.len) return error.TooManyCachedTensors;\\n                    self.branch.cache_tensors[self.branch.cache_tensor_count] = factor;\\n                    self.branch.cache_tensor_count += 1;\\n                    self.current().tensor_count += 1;\\n                }\\n\\n                pub fn emitWickAction(self: *@This(), factor: Correlator.ResolvedActionFactor) !void {\\n                    if (self.branch.cache_action_count == self.branch.cache_actions.len) return error.TooManyCachedActions;\\n                    self.branch.cache_actions[self.branch.cache_action_count] = factor;\\n                    self.branch.cache_action_count += 1;\\n                    self.current().action_count += 1;\\n                }\\n\\n                pub fn emitWickResidualOperator(self: *@This(), residual: Correlator.WickResidualRef) !void {\\n                    if (self.branch.cache_residual_count == self.branch.cache_residuals.len) return error.TooManyCachedResiduals;\\n                    self.branch.cache_residuals[self.branch.cache_residual_count] = residual;\\n                    self.branch.cache_residual_count += 1;\\n                    self.current().residual_count += 1;\\n                }\\n\\n                pub fn emitWickTermEnd(_: *@This()) !void {}\\n            };\\n\\n            var cache_sink = CacheSink{ .branch = branch, .cache_index = cache_index };\\n            try emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, &cache_sink);\\n            branch.cache_keys[cache_index] = .{ .left = @intCast(left_index), .right = @intCast(right_index), .payload = payload };\\n            const direct_slot = cachePairSlot(left_index, right_index);\\n            if (branch.cache_pair_index[direct_slot] == no_cached_pair) branch.cache_pair_index[direct_slot] = cache_index;\\n            branch.cache_count += 1;\\n            try branch.appendCachedTerm(cache_index);\\n        }\\n\\n        /// emitWickTermStart appends one primitive term boundary.\\n        pub fn emitWickTermStart(self: *Self, event: Correlator.WickTermEvent) !void {\\n            if (self.term_count == self.terms.len) return error.TooManyAccumulatedWickTerms;\\n            self.terms[self.term_count] = .{\\n                .event = event,\\n                .scalar_start = self.scalar_count,\\n                .coordinate_start = self.coordinate_count,\\n                .tensor_start = self.tensor_count,\\n                .action_start = self.action_count,\\n                .residual_start = self.residual_count,\\n            };\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar appends one scalar factor.\\n        pub fn emitWickScalar(self: *Self, factor: Correlator.ResolvedScalarFactor) !void {\\n            if (self.scalar_count == self.scalars.len) return error.TooManyAccumulatedScalars;\\n            self.scalars[self.scalar_count] = factor;\\n            self.scalar_count += 1;\\n            self.currentTerm().scalar_count += 1;\\n            self.logical_scalar_count += 1;\\n        }\\n\\n        /// emitWickCoordinate appends one coordinate factor.\\n        pub fn emitWickCoordinate(self: *Self, factor: coefficient.CoordinateFactor) !void {\\n            if (self.coordinate_count == self.coordinates.len) return error.TooManyAccumulatedCoordinates;\\n            self.coordinates[self.coordinate_count] = factor;\\n            self.coordinate_count += 1;\\n            self.currentTerm().coordinate_count += 1;\\n            self.logical_coordinate_count += 1;\\n        }\\n\\n        /// emitWickTensor appends one tensor factor.\\n        pub fn emitWickTensor(self: *Self, factor: Correlator.ResolvedTensorFactor) !void {\\n            if (self.tensor_count == self.tensors.len) return error.TooManyAccumulatedTensors;\\n            self.tensors[self.tensor_count] = factor;\\n            self.tensor_count += 1;\\n            self.currentTerm().tensor_count += 1;\\n            self.logical_tensor_count += 1;\\n        }\\n\\n        /// emitWickAction appends one action factor.\\n        pub fn emitWickAction(self: *Self, factor: Correlator.ResolvedActionFactor) !void {\\n            if (self.action_count == self.actions.len) return error.TooManyAccumulatedActions;\\n            self.actions[self.action_count] = factor;\\n            self.action_count += 1;\\n            self.currentTerm().action_count += 1;\\n            self.logical_action_count += 1;\\n        }\\n\\n        /// emitWickResidualOperator appends one residual operator reference.\\n        pub fn emitWickResidualOperator(self: *Self, residual: Correlator.WickResidualRef) !void {\\n            if (self.residual_count == self.residuals.len) return error.TooManyAccumulatedResiduals;\\n            self.residuals[self.residual_count] = residual;\\n            self.residual_count += 1;\\n            self.currentTerm().residual_count += 1;\\n            self.logical_residual_count += 1;\\n        }\\n\\n        /// emitWickTermEnd accepts the primitive term boundary.\\n        pub fn emitWickTermEnd(_: *Self) !void {}\\n\\n        /// emit streams all accumulated primitive terms to the final sink.\\n        pub fn emit(self: *const Self, sink: anytype) !void {\\n            const SinkPtr = @TypeOf(sink);\\n            const sink_info = @typeInfo(SinkPtr);\\n            if (sink_info == .pointer and @hasDecl(sink_info.pointer.child, \\"emitWickBranchTerm\\")) {\\n                try sink.emitWickBranchTerm(self);\\n                return;\\n            }\\n\\n            const emit_residuals = sink_info == .pointer and @hasDecl(sink_info.pointer.child, \\"emitWickResidualOperator\\");\\n            const replay_actions = self.logical_action_count != 0;\\n            const replay_residuals = emit_residuals and self.logical_residual_count != 0;\\n\\n            var term_index: usize = 0;\\n            while (term_index < self.term_count) : (term_index += 1) {\\n                const term = self.terms[term_index];\\n                const source_term = if (term.cache_index == no_cached_pair) term else self.cache_terms[term.cache_index];\\n                try sink.emitWickTermStart(term.event);\\n\\n                var scalar_index: usize = 0;\\n                while (scalar_index < term.scalar_count) : (scalar_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickScalar(self.scalars[source_term.scalar_start + scalar_index]);\\n                    } else {\\n                        try sink.emitWickScalar(self.cache_scalars[source_term.scalar_start + scalar_index]);\\n                    }\\n                }\\n\\n                var coordinate_index: usize = 0;\\n                while (coordinate_index < term.coordinate_count) : (coordinate_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickCoordinate(self.coordinates[source_term.coordinate_start + coordinate_index]);\\n                    } else {\\n                        try sink.emitWickCoordinate(self.cache_coordinates[source_term.coordinate_start + coordinate_index]);\\n                    }\\n                }\\n\\n                var tensor_index: usize = 0;\\n                while (tensor_index < term.tensor_count) : (tensor_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickTensor(self.tensors[source_term.tensor_start + tensor_index]);\\n                    } else {\\n                        try sink.emitWickTensor(self.cache_tensors[source_term.tensor_start + tensor_index]);\\n                    }\\n                }\\n\\n                if (replay_actions) {\\n                    var action_index: usize = 0;\\n                    while (action_index < term.action_count) : (action_index += 1) {\\n                        if (term.cache_index == no_cached_pair) {\\n                            try sink.emitWickAction(self.actions[source_term.action_start + action_index]);\\n                        } else {\\n                            try sink.emitWickAction(self.cache_actions[source_term.action_start + action_index]);\\n                        }\\n                    }\\n                }\\n\\n                if (replay_residuals) {\\n                    var residual_index: usize = 0;\\n                    while (residual_index < term.residual_count) : (residual_index += 1) {\\n                        if (term.cache_index == no_cached_pair) {\\n                            try sink.emitWickResidualOperator(self.residuals[source_term.residual_start + residual_index]);\\n                        } else {\\n                            try sink.emitWickResidualOperator(self.cache_residuals[source_term.residual_start + residual_index]);\\n                        }\\n                    }\\n                }\\n\\n                try sink.emitWickTermEnd();\\n            }\\n        }\\n    };\\n}\\n\\nfn matchIndexedWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, term_ordinal: usize) !?Correlator.MatchedWickTerm {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var remaining = term_ordinal;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const entry = config_ptr.pair_lookup[entry_index];\\n        const rule_index: usize = @intCast(entry.rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        const rule = &config_ptr.wick_rules[rule_index];\\n        if (remaining < rule.expr.terms.len) {\\n            return .{ .pair = .{\\n                .rule = rule,\\n                .config = config_ptr,\\n                .left = left,\\n                .right = right,\\n                .labels = ops.labels,\\n                .reversed = entry.reversed,\\n            }, .rule_index = rule_index, .term_index = remaining };\\n        }\\n        remaining -= rule.expr.terms.len;\\n    }\\n\\n    _ = left_index;\\n    _ = right_index;\\n    return null;\\n}\\n\\nfn matchScannedWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, term_ordinal: usize) ?Correlator.MatchedWickTerm {\\n    var remaining = term_ordinal;\\n\\n    for (config_ptr.wick_rules, 0..) |*rule, rule_index| {\\n        if (matchRule(config_ptr, rule, left, right, ops.labels)) |pair| {\\n            if (remaining < pair.rule.expr.terms.len) {\\n                return .{ .pair = pair, .rule_index = rule_index, .term_index = remaining };\\n            }\\n            remaining -= pair.rule.expr.terms.len;\\n        }\\n    }\\n\\n    _ = left_index;\\n    _ = right_index;\\n    return null;\\n}\\n\\nfn matchWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !?Correlator.MatchedWickTerm {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return matchIndexedWickPairTerm(config_ptr, ops, left_index, right_index, left, right, term_ordinal);\\n    }\\n\\n    return matchScannedWickPairTerm(config_ptr, ops, left_index, right_index, left, right, term_ordinal);\\n}\\n\\nfn emitIndexedWickPairTerms(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, sink: anytype) !usize {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var emitted: usize = 0;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const entry = config_ptr.pair_lookup[entry_index];\\n        const rule_index: usize = @intCast(entry.rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        const rule = &config_ptr.wick_rules[rule_index];\\n        const pair = Correlator.WickPair{\\n            .rule = rule,\\n            .config = config_ptr,\\n            .left = left,\\n            .right = right,\\n            .labels = ops.labels,\\n            .reversed = entry.reversed,\\n        };\\n\\n        var term_index: usize = 0;\\n        while (term_index < pair.rule.expr.terms.len) : (term_index += 1) {\\n            try emitPrimitiveWickTerm(pair, left_index, right_index, term_index, sink);\\n            emitted += 1;\\n        }\\n    }\\n\\n    return emitted;\\n}\\n\\nfn emitScannedWickPairTerms(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, sink: anytype) !usize {\\n    var emitted: usize = 0;\\n\\n    for (config_ptr.wick_rules) |*rule| {\\n        if (matchRule(config_ptr, rule, left, right, ops.labels)) |pair| {\\n            var term_index: usize = 0;\\n            while (term_index < pair.rule.expr.terms.len) : (term_index += 1) {\\n                try emitPrimitiveWickTerm(pair, left_index, right_index, term_index, sink);\\n                emitted += 1;\\n            }\\n        }\\n    }\\n\\n    return emitted;\\n}\\n\\nfn countIndexedWickPairTerms(config_ptr: *const CorrelatorConfigData, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) !usize {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var count: usize = 0;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const rule_index: usize = @intCast(config_ptr.pair_lookup[entry_index].rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        count += config_ptr.wick_rules[rule_index].expr.terms.len;\\n    }\\n\\n    return count;\\n}\\n\\nfn countScannedWickPairTerms(config_ptr: *const CorrelatorConfigData, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) usize {\\n    var count: usize = 0;\\n    for (config_ptr.wick_rules) |rule| {\\n        if (ruleMatches(rule, left, right) != null) count += rule.expr.terms.len;\\n    }\\n    return count;\\n}\\n\\n/// countWickPairTerms counts primitive terms for two input positions without resolving factors.\\nfn countWickPairTermsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize) !usize {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return countIndexedWickPairTerms(config_ptr, left, right);\\n    }\\n\\n    return countScannedWickPairTerms(config_ptr, left, right);\\n}\\n\\nfn emitWickPairTermsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, sink: anytype) !usize {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return emitIndexedWickPairTerms(config_ptr, ops, left_index, right_index, left, right, sink);\\n    }\\n\\n    return emitScannedWickPairTerms(config_ptr, ops, left_index, right_index, left, right, sink);\\n}\\n\\nfn emitWickPairTermData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, sink: anytype) !void {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    try emitPrimitiveWickTerm(matched.pair, left_index, right_index, matched.term_index, sink);\\n}\\n\\nfn wickPairTermPayloadData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !u64 {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    return wickPairTermPayloadFrom(matched.rule_index, matched.term_index, matched.pair.reversed);\\n}\\n\\nfn wickPairTermInfoData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    const source = matched.pair.rule.expr.terms[matched.term_index].residuals;\\n    if (source.len > residuals.len) return error.TooManyWickTermResiduals;\\n    for (source, 0..) |side, index| {\\n        residuals[index] = actualSideIndex(matched.pair, left_index, right_index, side);\\n    }\\n    payload.* = try wickPairTermPayloadFrom(matched.rule_index, matched.term_index, matched.pair.reversed);\\n    residual_count.* = source.len;\\n}\\n\\nfn emitWickPairTermPayloadData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, sink: anytype) !void {\\n    const rule_index = wickPayloadRuleIndex(payload);\\n    if (rule_index >= config_ptr.wick_rules.len) return error.InvalidWickTerm;\\n    const pair = Correlator.WickPair{\\n        .rule = &config_ptr.wick_rules[rule_index],\\n        .config = config_ptr,\\n        .left = ops.operators[left_index],\\n        .right = ops.operators[right_index],\\n        .labels = ops.labels,\\n        .reversed = wickPayloadReversed(payload),\\n    };\\n    try emitPrimitiveWickTerm(pair, left_index, right_index, wickPayloadTermIndex(payload), sink);\\n}\\n\\nfn wickPairTermResidualsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize) !usize {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    const source = matched.pair.rule.expr.terms[matched.term_index].residuals;\\n    if (source.len > residuals.len) return error.TooManyWickTermResiduals;\\n    for (source, 0..) |side, index| {\\n        residuals[index] = actualSideIndex(matched.pair, left_index, right_index, side);\\n    }\\n    return source.len;\\n}\\n\\nfn RuntimeWickTactic(comptime ConfigHandle: type) type {\\n    return struct {\\n        /// Config is the shared preset runtime correlator config.\\n        pub const Config = CorrelatorConfigData;\\n        const traversal_operator_count = ConfigHandle.branch_limits.max_operators;\\n        /// BranchState accumulates resolved Wick factors along one DFS branch.\\n        pub const BranchState = AccumulatedBranch(ConfigHandle.branch_limits);\\n\\n        /// emitPairTerms adapts original occurrence pairs to the primitive Wick emitter.\\n        pub fn emitPairTerms(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, sink: anytype) !usize {\\n            return emitWickPairTermsData(config_ptr, ops, left_index, right_index, sink);\\n        }\\n\\n        /// emitPairTerm emits one selected primitive Wick term.\\n        pub fn emitPairTerm(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, sink: anytype) !void {\\n            return emitWickPairTermData(config_ptr, ops, left_index, right_index, term_ordinal, sink);\\n        }\\n\\n        /// pairTermCount counts primitive pair terms without emitting factors.\\n        pub fn pairTermCount(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize) !usize {\\n            return countWickPairTermsData(config_ptr, ops, left_index, right_index);\\n        }\\n\\n        /// pairTermResiduals returns original residual occurrences for one primitive term.\\n        pub fn pairTermResiduals(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize) !usize {\\n            return wickPairTermResidualsData(config_ptr, ops, left_index, right_index, term_ordinal, residuals);\\n        }\\n\\n        /// pairTermPayload returns cached rule payload for a primitive pair term.\\n        pub fn pairTermPayload(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !u64 {\\n            return wickPairTermPayloadData(config_ptr, ops, left_index, right_index, term_ordinal);\\n        }\\n\\n        /// pairTermInfo returns residual occurrences and cached replay payload from one pair match.\\n        pub fn pairTermInfo(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n            return wickPairTermInfoData(config_ptr, ops, left_index, right_index, term_ordinal, residuals, payload, residual_count);\\n        }\\n\\n        /// emitPairTermPayload replays one primitive term without matching rules again.\\n        pub fn emitPairTermPayload(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, sink: anytype) !void {\\n            return emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, sink);\\n        }\\n\\n        /// pushBranchPairTerm appends one primitive pair term to a branch accumulator.\\n        pub fn pushBranchPairTerm(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, branch: *BranchState) !void {\\n            return branch.pushPairTerm(config_ptr, ops, left_index, right_index, payload);\\n        }\\n\\n        /// emitAccumulatedBranch streams cached pair factors followed by the zero-mode base case.\\n        pub fn emitAccumulatedBranch(config_ptr: *const Config, ops: kernel.Call.MultiOp, branch: *const BranchState, sign: i8, residual_indices: []const usize, sink: anytype) !void {\\n            if (sign < 0) {\\n                try emitBranchSign(config_ptr, sign, sink);\\n            }\\n            try branch.emit(sink);\\n            try generatedTrustedZeroModeBaseCase(ConfigHandle, config_ptr, ops, residual_indices, sink);\\n        }\\n\\n        /// emitFullContractionBranch streams a full Wick branch with the empty base case.\\n        pub fn emitFullContractionBranch(config_ptr: *const Config, ops: kernel.Call.MultiOp, branch: *const BranchState, sign: i8, sink: anytype) !void {\\n            if (sign < 0) {\\n                try emitBranchSign(config_ptr, sign, sink);\\n            }\\n            try branch.emit(sink);\\n            _ = ops;\\n            try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        }\\n\\n        /// operatorParity returns true for configured fermionic operator kinds.\\n        pub fn operatorParity(config_ptr: *const Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n            const kind = ops.operators[index].kind;\\n            return kindIn(kind, config_ptr.fermion_kinds);\\n        }\\n\\n        /// operatorCanRemainInZeroMode returns true when a residual can be consumed by zero modes.\\n        pub fn operatorCanRemainInZeroMode(config_ptr: *const Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n            _ = config_ptr;\\n            return generatedZeroModeCanConsumeKind(ConfigHandle, ops.operators[index].kind);\\n        }\\n\\n        /// emitBranchSign forwards an accumulated fermion sign when the sink accepts it.\\n        pub fn emitBranchSign(_: *const Config, sign: i8, sink: anytype) !void {\\n            const SinkPtr = @TypeOf(sink);\\n            const sink_info = @typeInfo(SinkPtr);\\n            if (sink_info != .pointer) return;\\n            const Sink = sink_info.pointer.child;\\n            if (@hasDecl(Sink, \\"emitWickBranchSign\\")) {\\n                try sink.emitWickBranchSign(sign);\\n            }\\n        }\\n\\n        /// emitZeroModeBaseCase adapts residual original occurrences to the zero-mode evaluator.\\n        pub fn emitZeroModeBaseCase(config_ptr: *const Config, ops: kernel.Call.MultiOp, residual_indices: []const usize, sink: anytype) !bool {\\n            return generatedZeroModeBaseCase(ConfigHandle, config_ptr, ops, residual_indices, sink);\\n        }\\n\\n        /// zeroModeBaseCaseSucceeds tests base-case viability without emitting factors.\\n        pub fn zeroModeBaseCaseSucceeds(config_ptr: *const Config, ops: kernel.Call.MultiOp, residual_indices: []const usize) !bool {\\n            return generatedZeroModeBaseCaseSucceeds(ConfigHandle, config_ptr, ops, residual_indices);\\n        }\\n    };\\n}\\n\\nfn operatorListData(ops: anytype) KernelOperatorList {\\n    const Ops = @TypeOf(ops);\\n    if (Ops == KernelOperatorList) return ops;\\n    return switch (@typeInfo(Ops)) {\\n        .pointer => |ptr| if (ptr.child == OperatorList) blk: {\\n            const state: *const LocalStateImpl = @ptrCast(@alignCast(ops));\\n            break :blk .{ .operators = state.owned_operators, .labels = &state.label_store };\\n        } else {\\n            @compileError(\\"correlator operators must come from local.ops\\");\\n        },\\n        else => @compileError(\\"correlator operators must come from local.ops\\"),\\n    };\\n}\\n\\n/// streamCorrelator evaluates Wick branches and streams accepted base cases.\\npub fn streamCorrelator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n    const Ptr = @TypeOf(config_ptr);\\n    const ptr_info = @typeInfo(Ptr);\\n    if (ptr_info != .pointer) @compileError(\\"correlator config must be passed by pointer\\");\\n    const ConfigHandle = ptr_info.pointer.child;\\n    const data = configData(config_ptr);\\n    return wick_correlator.wickCorrelator(RuntimeWickTactic(ConfigHandle), data, operatorListData(ops), sink);\\n}\\n"},{"id":"20260606T183200.000000","title":"Free Boson Preset","tags":null,"content":"This node defines the free-boson preset on the sphere and its\\nNeumann/Dirichlet boundary extension.  It uses the compact vocabulary from\\nPreset Shared Runtime and is exported through\\nPresets.\\nThe preset does not perform tensor algebra.  A \\\\(D\\\\)-dimensional target space\\nonly fixes which \\\\(SO(D)\\\\)-type labels are expected; later coefficient evaluation\\nuses Tensor-Code to evaluate metrics, momenta, and\\nprojectors.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Local = shared.Local;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nThe preset owns the free-boson operator vocabulary.  The enum below is\\nphysics data; Preset Shared Runtime only receives the compact kind ids derived\\nfrom it.  The base separates this preset\'s ids from other implemented presets,\\nwhile individual ids still come from enum order rather than handwritten\\nnumbers.\\nconst namespace = \\"free_boson\\";\\nconst scalars = declare.scalars;\\nconst RuleScalar = @TypeOf(scalars.one());\\nconst alpha_prime_atom = scalars.atom(namespace, \\"alpha_prime\\");\\nconst k_disk_atom = scalars.atom(namespace, \\"K_D2\\");\\nconst neumann_projector_ref = declare.configRef(namespace, \\"neumann_projector\\");\\nconst dirichlet_projector_ref = declare.configRef(namespace, \\"dirichlet_projector\\");\\nconst bulk_boundary_projector_ref = declare.configRef(namespace, \\"bulk_boundary_projector\\");\\nconst dirichlet_position_ref = declare.configRef(namespace, \\"dirichlet_position\\");\\nconst chan_paton_ref = declare.configRef(namespace, \\"chan_paton\\");\\nconst target_dimension_ref = declare.configRef(namespace, \\"target_dimension\\");\\n\\nconst Family = enum {\\n    x,\\n    d_x,\\n    d_xt,\\n    exp_x,\\n    profile_x,\\n    d_x_boundary,\\n    exp_x_boundary,\\n    profile_x_boundary,\\n};\\n\\nconst ZeroSector = enum {\\n    bulk,\\n    boundary,\\n    torus,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\n\\nfn zeroSector(comptime sector: ZeroSector) zero_mode.Sector {\\n    return declare.sectorId(namespace, sector);\\n}\\n\\nfn rawToken(value: anytype) u32 {\\n    return @intFromEnum(value);\\n}\\n\\nfn token(comptime T: type, id: u32) T {\\n    return @enumFromInt(if (id == 0) 1 else id);\\n}\\n\\nfn stableNameId(comptime name: []const u8) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (name) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn stableSubspaceId(comptime dimensions: []const u16) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (dimensions) |dimension| {\\n        hash = (hash ^ @as(u32, dimension & 0xff)) *% 16777619;\\n        hash = (hash ^ @as(u32, dimension >> 8)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn projectorId(comptime rank: u8, hash: u32) u32 {\\n    return (@as(u32, rank) << 24) | (hash & 0x00ff_ffff);\\n}\\n\\nfn polynomialProfileTerm(term: anytype) struct { coefficient: Handle.ProfileCoefficient, power: u16 } {\\n    const Term = @TypeOf(term);\\n    const info = @typeInfo(Term);\\n    if (info != .@\\"struct\\" or !info.@\\"struct\\".is_tuple or info.@\\"struct\\".fields.len != 2) {\\n        @compileError(\\"polynomial profile terms must be .{ coefficient, power } tuples\\");\\n    }\\n    return .{ .coefficient = term[0], .power = term[1] };\\n}\\n\\nfn polynomialProfileId(point: Handle.TargetPoint, terms: anytype) u32 {\\n    var hash = rawToken(point) ^ 0x9e37_79b9;\\n    inline for (terms) |raw_term| {\\n        const term = polynomialProfileTerm(raw_term);\\n        hash = (hash ^ rawToken(term.coefficient)) *% 16777619;\\n        hash = (hash ^ @as(u32, term.power)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nconst TargetSpace = struct {\\n    /// subspace names a target-space projector onto a coordinate subspace.\\n    pub fn subspace(comptime dimensions: []const u16) Handle.TensorProjector {\\n        return token(Handle.TensorProjector, projectorId(@intCast(dimensions.len), stableSubspaceId(dimensions)));\\n    }\\n\\n    /// complement names the complementary target-space projector.\\n    pub fn complement(comptime dimensions: []const u16) Handle.TensorProjector {\\n        return token(Handle.TensorProjector, projectorId(0, stableSubspaceId(dimensions) ^ 0xa5a5_a5a5));\\n    }\\n\\n    /// point names a target-space point used by boundary conditions.\\n    pub fn point(comptime name: []const u8) Handle.TargetPoint {\\n        return token(Handle.TargetPoint, stableNameId(name));\\n    }\\n\\n    /// boundaryStack names Chan-Paton data for a stacked boundary condition.\\n    pub fn boundaryStack(comptime name: []const u8) Handle.BoundaryStack {\\n        return token(Handle.BoundaryStack, stableNameId(name));\\n    }\\n};\\nBulk PresetThe default normalization is the stringbook free-field normalization\\n\\\\[\\n  T^X=-{1\\\\over\\\\alpha\'}\\\\,\\\\partial X^\\\\mu \\\\partial X_\\\\mu\\n\\\\]\\n.  The rule scalar remains symbolic here because\\ndifferent applications set \\\\(\\\\alpha\'\\\\) and \\\\(\\\\partial X\\\\) normalizations\\ndifferently.\\nconst FreeBosonConfig = struct {\\n    dimension: u16,\\n};\\n\\n/// freeBoson builds the generated preset type for a noncompact free-boson CFT.\\npub fn freeBoson(comptime cfg: FreeBosonConfig) type {\\n    return struct {\\n        /// op exposes free-boson local operator builders.\\n        pub const op = FreeBosonOp;\\n        /// profile exposes profile-presentation builders.\\n        pub const profile = ProfileOp;\\n        /// target exposes deterministic target-space tokens for preset data.\\n        pub const target = TargetSpace;\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = FreeBosonCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for free-boson insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nThe derivative meaning is \\\\(dX(\\\\mu,n,z)=\\\\partial^{n+1}X^\\\\mu(z)\\\\).  The\\nexplicit \\\\(X\\\\) body is kept because NLSM expansions and position-space profiles\\nproduce logarithmic Green kernels rather than only plane-wave contractions.\\nconst FreeBosonOp = struct {\\n    /// X builds an explicit bulk free-boson field insertion.\\n    pub fn X(local: anytype, mu: Handle.Index, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.x)).pair(local, z, zbar, .{mu});\\n    }\\n\\n    /// dX builds a holomorphic derivative field insertion.\\n    pub fn dX(local: anytype, mu: Handle.Index, n: u8, z: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.d_x)).single(local, z, n, .{mu});\\n    }\\n\\n    /// dXt builds an antiholomorphic derivative field insertion.\\n    pub fn dXt(local: anytype, mu: Handle.Index, n: u8, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.d_xt)).single(local, zbar, n, .{mu});\\n    }\\n\\n    /// expX builds a normal-ordered plane-wave insertion.\\n    pub fn expX(local: anytype, k: Handle.Momentum, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.exp_x)).pair(local, z, zbar, .{k});\\n    }\\n\\n    /// profile builds a target-space profile insertion.\\n    pub fn profile(local: anytype, f: Handle.Profile, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.profile_x)).pair(local, z, zbar, .{f});\\n    }\\n\\n    /// profileVector builds a vector-valued target-space profile insertion.\\n    pub fn profileVector(local: anytype, f: Handle.Profile, nu: Handle.Index, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(profile_vector_operator).pair(local, z, zbar, .{ f, nu });\\n    }\\n};\\nProfiles are handles to a chosen presentation.  Fourier profiles resolve to\\nplane waves and momentum conservation can partially evaluate the inverse\\nFourier transform.  Position-space profiles keep the zero-mode integral\\nexplicit.  Polynomial profiles support Riemann-normal-coordinate expansions.\\nconst ProfileOp = struct {\\n    /// positionSpace selects the exact residual position-space profile presentation.\\n    pub fn positionSpace(f: Handle.TargetFunction) Handle.Profile {\\n        return shared.profileHandle(.position_space, rawToken(f));\\n    }\\n\\n    /// fourier selects the Fourier-space profile presentation.\\n    pub fn fourier(f_hat: Handle.FourierTransform) Handle.Profile {\\n        return shared.profileHandle(.fourier, rawToken(f_hat));\\n    }\\n\\n    /// polynomialRnc selects a finite polynomial profile presentation.\\n    pub fn polynomialRnc(point: Handle.TargetPoint, terms: anytype) Handle.Profile {\\n        return shared.profileHandle(.polynomial_rnc, polynomialProfileId(point, terms));\\n    }\\n};\\nSphere RulesOn the sphere the primitive free-boson rules include same-chirality\\n\\\\(\\\\partial X\\\\,\\\\partial X\\\\) contractions, \\\\(\\\\partial X\\\\) contractions with\\nplane waves and target-space profiles, and the plane-wave Green-kernel factor.\\nLabels supply indices, momenta, and profile handles at runtime.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn bulkHolomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .holomorphic), wick.coord(.right, .holomorphic));\\n}\\n\\nfn bulkAntiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .antiholomorphic), wick.coord(.right, .antiholomorphic));\\n}\\n\\nfn bulkToBoundaryHolomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .holomorphic), wick.coord(.right, .position));\\n}\\n\\nfn bulkToBoundaryAntiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .antiholomorphic), wick.coord(.right, .position));\\n}\\n\\nfn freeBosonDxDx(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn freeBosonDxtDxt(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, antiholomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn freeBosonDxExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .momentum_index = .{\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn freeBosonDxProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, holomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.profileDerivative(wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn freeBosonDxtExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .momentum_index = .{\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, antiholomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn freeBosonDxtProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, antiholomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.profileDerivative(wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn freeBosonExpXExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.expr(&.{wick.termWithResiduals(&.{wick.scalar(scalars.div(alpha, 2))}, &.{\\n        .{ .green_exponential = bulkHolomorphicDifference() },\\n        .{ .green_exponential = bulkAntiholomorphicDifference() },\\n    }, &.{.{ .momentum_pair = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }}, &.{}, &.{ .left, .right })});\\n}\\n\\nfn sphereZeroSpec(comptime normalization: RuleScalar) [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.bulk), .expr = .{ .free_boson_constant_mode = .{\\n            .exp_kind_ids = &.{kind(.exp_x)},\\n            .profile_kind_ids = &.{kind(.profile_x)},\\n            .normalization = .{\\n                .scalar = normalization,\\n                .two_pi_power = .{ .target_dimension = target_dimension_ref },\\n            },\\n        } } },\\n    };\\n}\\n\\nfn sphereZeroStorage() [1]zero_mode.Rule {\\n    const spec = sphereZeroSpec(.one);\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nconst sphere_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"X\\", .kind = kind(.x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"dX\\", .kind = kind(.d_x), .support = .holomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"dXt\\", .kind = kind(.d_xt), .support = .antiholomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"expX\\", .kind = kind(.exp_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.momentum}, .statistics = .bosonic, .zero_mode_consumable = true },\\n    .{ .name = \\"profile\\", .kind = kind(.profile_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.profile}, .statistics = .bosonic, .zero_mode_consumable = true },\\n};\\n\\nfn sphereWickSpec(comptime alpha: RuleScalar) [7]Spec.WickRule {\\n    return [_]Spec.WickRule{\\n        .{ .left = kind(.d_x), .right = kind(.d_x), .expr = freeBosonDxDx(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.d_xt), .expr = freeBosonDxtDxt(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.exp_x), .expr = freeBosonDxExpX(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.exp_x), .expr = freeBosonDxtExpX(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.profile_x), .expr = freeBosonDxProfile(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.profile_x), .expr = freeBosonDxtProfile(alpha) },\\n        .{ .left = kind(.exp_x), .right = kind(.exp_x), .expr = freeBosonExpXExpX(alpha) },\\n    };\\n}\\n\\nconst sphere_wick_spec = sphereWickSpec(.one);\\n\\nconst torus_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.d_x), .right = kind(.d_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.d_xt), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_x), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_x), .right = kind(.profile_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.profile_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.exp_x), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green_exponential} },\\n};\\n\\nconst sphere_zero_spec = sphereZeroSpec(.one);\\nThe torus draft follows the genus-one convention in the stringbook: the\\ncoordinate obeys \\\\(z\\\\sim z+2\\\\pi\\\\sim z+2\\\\pi\\\\tau\\\\), the nonzero modes use the\\ntorus Green function built from \\\\(\\\\theta_1(z|\\\\tau)\\\\), and the constant\\ntarget-space mode gives the momentum-conservation factor in the exponential\\ncorrelator .  The metadata below records only that\\nzero-mode sector and its consumed operator families; it is not lowered into a\\nruntime config until the torus evaluator is implemented.\\nconst torus_zero_spec = [_]Spec.ZeroModeRule{\\n    .{\\n        .sector = zeroSector(.torus),\\n        .kind = .torus_free_boson_constant,\\n        .consumes = &.{ kind(.exp_x), kind(.profile_x) },\\n    },\\n};\\n\\nconst sphere_spec = Spec.Theory{\\n    .operators = &sphere_operator_spec,\\n    .wick_rules = &sphere_wick_spec,\\n    .zero_modes = &sphere_zero_spec,\\n};\\n\\nconst torus_draft_spec = Spec.Theory{\\n    .surface = .{\\n        .kind = .torus,\\n        .coordinate_model = .elliptic,\\n        .modular_parameters = &.{\\"tau\\"},\\n        .source = .stringbook,\\n    },\\n    .operators = &sphere_operator_spec,\\n    .wick_rules = &torus_wick_spec,\\n    .zero_modes = &torus_zero_spec,\\n};\\n\\nconst boundary_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"dXBoundary\\", .kind = kind(.d_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"expXBoundary\\", .kind = kind(.exp_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.momentum}, .statistics = .bosonic, .zero_mode_consumable = true },\\n    .{ .name = \\"profileBoundary\\", .kind = kind(.profile_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.profile}, .statistics = .bosonic, .zero_mode_consumable = true },\\n};\\n\\nconst disk_operator_spec = sphere_operator_spec ++ boundary_operator_spec;\\n\\nconst profile_vector_operator = Spec.Operator{ .name = \\"profileVector\\", .kind = kind(.profile_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{ .profile, .index }, .statistics = .bosonic, .zero_mode_consumable = true };\\nconst boundary_profile_vector_operator = Spec.Operator{ .name = \\"profileBoundaryVector\\", .kind = kind(.profile_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{ .profile, .index }, .statistics = .bosonic, .zero_mode_consumable = true };\\n\\nfn sphereOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-boson sphere operator spec\\");\\n}\\n\\nfn boundaryOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (boundary_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-boson boundary operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime wick_count: usize, comptime zero_count: usize) void {\\n    if (sphere_spec.wick_rules.len != wick_count) @compileError(\\"free-boson sphere spec Wick count does not match lowered rules\\");\\n    if (sphere_spec.zero_modes.len != zero_count) @compileError(\\"free-boson sphere spec zero-mode count does not match lowered rules\\");\\n    if (Spec.zeroModeConsumeKindCount(sphere_spec.zero_modes[0]) != 2) @compileError(\\"free-boson sphere spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn assertTorusDraftSpec() void {\\n    if (torus_draft_spec.surface.kind != .torus) @compileError(\\"free-boson torus draft spec surface kind is incomplete\\");\\n    if (torus_draft_spec.surface.coordinate_model != .elliptic) @compileError(\\"free-boson torus draft spec coordinate model is incomplete\\");\\n    if (torus_draft_spec.surface.modular_parameters.len != 1) @compileError(\\"free-boson torus draft spec modular metadata is incomplete\\");\\n    if (torus_draft_spec.surface.source != .stringbook) @compileError(\\"free-boson torus draft spec source convention is incomplete\\");\\n    if (torus_draft_spec.operators.len != sphere_spec.operators.len) @compileError(\\"free-boson torus draft spec operator metadata is incomplete\\");\\n    if (torus_draft_spec.wick_rules.len != sphere_spec.wick_rules.len) @compileError(\\"free-boson torus draft spec Wick metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_green) != 6) @compileError(\\"free-boson torus draft spec Green-kernel metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_green_exponential) != 1) @compileError(\\"free-boson torus draft spec exponential Green-kernel metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .rational_pole) != 0) @compileError(\\"free-boson torus draft spec still uses rational pole metadata\\");\\n    if (torus_draft_spec.zero_modes.len != 1) @compileError(\\"free-boson torus draft spec zero-mode metadata is incomplete\\");\\n    if (Spec.zeroModeConsumeKindCount(torus_draft_spec.zero_modes[0]) != 2) @compileError(\\"free-boson torus draft spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn FreeBosonRules(comptime cfg: FreeBosonConfig) type {\\n    _ = cfg;\\n    const alpha = scalars.atomScalar(alpha_prime_atom);\\n    return struct {\\n        const sphere_wick_runtime_spec = sphereWickSpec(alpha);\\n        const sphere_wick_storage = Spec.wickRules(&sphere_wick_runtime_spec, &sphere_operator_spec);\\n\\n        const sphere_wick: []const wick.Rule = &sphere_wick_storage;\\n    };\\n}\\n\\nfn FreeBosonCorrelatorConfig(comptime cfg: FreeBosonConfig) type {\\n    const rules = FreeBosonRules(cfg);\\n    return struct {\\n        const sphere_zero_storage = sphereZeroStorage();\\n        comptime {\\n            assertSphereSpec(rules.sphere_wick.len, sphere_zero_storage.len);\\n            assertTorusDraftSpec();\\n        }\\n\\n        /// sphere selects free-boson sphere Wick and zero-mode rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &sphere_zero_storage,\\n            .config_entries = &declare.configEntries(.{\\n                declare.config.targetDimension(target_dimension_ref, cfg.dimension),\\n            }),\\n        }){};\\n    };\\n}\\nBoundary ExtensionThe boundary extension is separate from the bulk free boson.  It adds\\nNeumann/Dirichlet data, optional Chan-Paton stack data, boundary-local\\noperators, mixed bulk-boundary rules, and disk zero-mode rules.  The doubling\\nchoice is already reflected in these rule templates before runtime.\\nconst FreeBosonBoundaryConfig = struct {\\n    neumann: Handle.TensorProjector,\\n    dirichlet: Handle.TensorProjector,\\n    dirichlet_position: Handle.TargetPoint,\\n    chan_paton: ?Handle.BoundaryStack = null,\\n};\\n\\nconst FreeBosonBoundaryOp = struct {\\n    /// dXBoundary builds stringbook\'s holomorphic boundary limit of dX.\\n    pub fn dXBoundary(local: anytype, mu: Handle.Index, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.d_x_boundary)).boundarySingle(local, y, n, .{mu});\\n    }\\n\\n    /// expXBoundary builds a Neumann boundary plane-wave insertion.\\n    pub fn expXBoundary(local: anytype, k: Handle.Momentum, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.exp_x_boundary)).boundarySingle(local, y, 0, .{k});\\n    }\\n\\n    /// profileBoundary builds a boundary profile insertion.\\n    pub fn profileBoundary(local: anytype, f: Handle.Profile, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.profile_x_boundary)).boundarySingle(local, y, 0, .{f});\\n    }\\n\\n    /// profileBoundaryVector builds a vector-valued boundary profile insertion.\\n    pub fn profileBoundaryVector(local: anytype, f: Handle.Profile, nu: Handle.Index, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundary_profile_vector_operator).boundarySingle(local, y, 0, .{ f, nu });\\n    }\\n};\\n\\nfn FreeBosonBoundaryExtension(comptime cfg: FreeBosonBoundaryConfig) type {\\n    const data = FreeBosonBoundaryData(cfg);\\n    return declare.BoundaryExtension(.{\\n        .op = FreeBosonBoundaryOp,\\n        .wick_rules = data.disk_wick,\\n        .zero_modes = data.disk_zero_modes,\\n        .config_entries = data.config_entries,\\n    });\\n}\\n\\n/// boundaryExtension declares the Neumann/Dirichlet free-boson boundary extension.\\npub fn boundaryExtension(comptime cfg: FreeBosonBoundaryConfig) FreeBosonBoundaryExtension(cfg) {\\n    return .{};\\n}\\nThe boundary rules use projectors named by the correlator config.  These are\\nnot fake runtime labels: they point to configuration data such as the Neumann\\nprojector and the bulk-boundary doubling projector.\\nfn boundaryFreeBosonDxDx(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.neg(alpha), .{ .projector_metric = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn boundaryFreeBosonDxExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.neg(scalars.i(alpha)), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn boundaryFreeBosonDxProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.neg(alpha), .none, holomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = neumann_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn boundaryFreeBosonExpXExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.greenExponentialWithResiduals(alpha, .{ .projector_momentum_pair = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), &.{ .left, .right });\\n}\\n\\nfn mixedFreeBosonDxDxBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .projector_metric = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn mixedFreeBosonDxtDxBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .projector_metric = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryAntiholomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn mixedFreeBosonDxExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxProfileBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, bulkToBoundaryHolomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = bulk_boundary_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxtExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, bulkToBoundaryAntiholomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxtProfileBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, bulkToBoundaryAntiholomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = bulk_boundary_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn mixedFreeBosonExpXExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.greenExponentialWithResiduals(scalars.div(alpha, 2), .{ .projector_momentum_pair = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), &.{ .left, .right });\\n}\\n\\nfn diskWickSpec(comptime alpha: RuleScalar) [11]Spec.WickRule {\\n    return [_]Spec.WickRule{\\n        .{ .left = kind(.d_x_boundary), .right = kind(.d_x_boundary), .expr = boundaryFreeBosonDxDx(alpha) },\\n        .{ .left = kind(.d_x_boundary), .right = kind(.exp_x_boundary), .expr = boundaryFreeBosonDxExpX(alpha) },\\n        .{ .left = kind(.d_x_boundary), .right = kind(.profile_x_boundary), .expr = boundaryFreeBosonDxProfile(alpha) },\\n        .{ .left = kind(.exp_x_boundary), .right = kind(.exp_x_boundary), .expr = boundaryFreeBosonExpXExpX(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.d_x_boundary), .expr = mixedFreeBosonDxDxBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.d_x_boundary), .expr = mixedFreeBosonDxtDxBoundary(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonDxExpXBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonDxtExpXBoundary(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.profile_x_boundary), .expr = mixedFreeBosonDxProfileBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.profile_x_boundary), .expr = mixedFreeBosonDxtProfileBoundary(alpha) },\\n        .{ .left = kind(.exp_x), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonExpXExpXBoundary(alpha) },\\n    };\\n}\\n\\nfn diskZeroSpec(comptime normalization: RuleScalar) [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.boundary), .expr = .{ .free_boson_constant_mode = .{\\n            .integration_projector = neumann_projector_ref,\\n            .fixed_projector = dirichlet_projector_ref,\\n            .fixed_position = dirichlet_position_ref,\\n            .exp_kind_ids = &.{ kind(.exp_x), kind(.exp_x_boundary) },\\n            .profile_kind_ids = &.{ kind(.profile_x), kind(.profile_x_boundary) },\\n            .normalization = .{\\n                .scalar = normalization,\\n                .two_pi_power = .{ .projector_rank = neumann_projector_ref },\\n            },\\n        } } },\\n    };\\n}\\n\\nfn diskZeroStorage() [1]zero_mode.Rule {\\n    const spec = diskZeroSpec(scalars.atomScalar(k_disk_atom));\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nfn FreeBosonBoundaryData(comptime cfg: FreeBosonBoundaryConfig) type {\\n    const alpha = scalars.atomScalar(alpha_prime_atom);\\n    return struct {\\n        const disk_wick_runtime_spec = diskWickSpec(alpha);\\n        const disk_wick_storage = Spec.wickRules(&disk_wick_runtime_spec, &disk_operator_spec);\\n        const disk_zero_storage = diskZeroStorage();\\n        const config_storage = if (cfg.chan_paton) |stack| declare.configEntries(.{\\n            declare.config.tensorProjector(neumann_projector_ref, cfg.neumann),\\n            declare.config.tensorProjector(dirichlet_projector_ref, cfg.dirichlet),\\n            declare.config.tensorProjector(bulk_boundary_projector_ref, cfg.neumann),\\n            declare.config.targetPoint(dirichlet_position_ref, cfg.dirichlet_position),\\n            declare.config.boundaryStack(chan_paton_ref, stack),\\n        }) else declare.configEntries(.{\\n            declare.config.tensorProjector(neumann_projector_ref, cfg.neumann),\\n            declare.config.tensorProjector(dirichlet_projector_ref, cfg.dirichlet),\\n            declare.config.tensorProjector(bulk_boundary_projector_ref, cfg.neumann),\\n            declare.config.targetPoint(dirichlet_position_ref, cfg.dirichlet_position),\\n        });\\n\\n        const disk_wick: []const wick.Rule = &disk_wick_storage;\\n        const disk_zero_modes: []const zero_mode.Rule = &disk_zero_storage;\\n        const config_entries = &config_storage;\\n    };\\n}\\n\\ntest \\"free-boson schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const k = try local.momentum(\\"k\\");\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const dx = try X.op.dX(&local, mu, 2, z);\\n    const exp = try X.op.expX(&local, k, z, zbar);\\n    const ops = try local.ops(.{ dx, exp });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(dx)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(exp)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\ntest \\"free-boson boundary schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n\\n    var local = try Local.init(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const dx = try FreeBosonBoundaryOp.dXBoundary(&local, mu, 3, y);\\n    const exp = try FreeBosonBoundaryOp.expXBoundary(&local, k, y);\\n    const ops = try local.ops(.{ dx, exp });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(dx)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(exp)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n"},{"id":"20260606T183300.000000","title":"bc Preset","tags":null,"content":"This node defines the \\\\(bc\\\\) ghost preset on the sphere and its disk boundary\\nextension.  It is independent of the matter CFT and uses the shared\\noperator/Wick vocabulary from Preset Shared Runtime.\\nThe local OPE is\\n\\\\[\\n  b(z)c(0)\\\\sim {1\\\\over z},\\n\\\\]\\nwith weights \\\\(h_b=2\\\\), \\\\(h_c=-1\\\\).  The sphere top form requires three\\n\\\\(c\\\\)-ghost insertions in each chiral copy .\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Local = shared.Local;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nThe ghost preset owns the \\\\(b,c\\\\) operator vocabulary.  It uses a separate id\\nblock from the free boson and derives each concrete kind id from the enum tag,\\nso this file names physics families without maintaining numeric ids per\\noperator.\\nconst namespace = \\"bc\\";\\n\\nconst Family = enum {\\n    b,\\n    c,\\n    bt,\\n    ct,\\n    b_boundary,\\n    c_boundary,\\n};\\n\\nconst ZeroSector = enum {\\n    sphere,\\n    disk,\\n    torus,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\n\\nfn zeroSector(comptime sector: ZeroSector) zero_mode.Sector {\\n    return declare.sectorId(namespace, sector);\\n}\\n\\nSphere PresetThe sphere preset exposes a holomorphic copy and, by default, an\\nantiholomorphic copy.  The config flag changes both the builder namespace and\\nthe emitted rule slice at compile time.\\nconst BcSphereConfig = struct {\\n    include_antiholomorphic_copy: bool = true,\\n};\\n\\n/// bcSphere builds the generated preset type for the sphere bc ghost CFT.\\npub fn bcSphere(comptime cfg: BcSphereConfig) type {\\n    return struct {\\n        /// op exposes bc ghost local operator builders.\\n        pub const op = BcSphereOp(cfg.include_antiholomorphic_copy);\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = BcSphereCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for bc insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nDerivative labels mean \\\\(\\\\partial^n b\\\\), \\\\(\\\\partial^n c\\\\), and their\\nantiholomorphic copies.  No ghost-number bookkeeping is performed here; that is\\npart of theory data and later basis or correlator evaluation.\\nfn BcSphereOp(comptime include_antiholomorphic_copy: bool) type {\\n    const Holomorphic = struct {\\n        /// b builds a holomorphic b-ghost insertion.\\n        pub fn b(local: anytype, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.b)).single(local, z, n, .{});\\n        }\\n\\n        /// c builds a holomorphic c-ghost insertion.\\n        pub fn c(local: anytype, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.c)).single(local, z, n, .{});\\n        }\\n    };\\n\\n    if (!include_antiholomorphic_copy) return Holomorphic;\\n\\n    return struct {\\n        /// b builds a holomorphic b-ghost insertion.\\n        pub const b = Holomorphic.b;\\n        /// c builds a holomorphic c-ghost insertion.\\n        pub const c = Holomorphic.c;\\n\\n        /// bt builds an antiholomorphic b-ghost insertion.\\n        pub fn bt(local: anytype, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.bt)).single(local, zbar, n, .{});\\n        }\\n\\n        /// ct builds an antiholomorphic c-ghost insertion.\\n        pub fn ct(local: anytype, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.ct)).single(local, zbar, n, .{});\\n        }\\n    };\\n}\\nRulesThe \\\\(bc\\\\) Wick rules have no tensor numerator.  Their zero-mode rule records\\nthe top-form saturation separately from pairwise contractions.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn bcBC() wick.Expr {\\n    return wick.differentiatedPole(.one, .none, holomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn bcBtCt() wick.Expr {\\n    return wick.differentiatedPole(.one, .none, antiholomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn sphereZeroSpec(comptime cfg: BcSphereConfig) if (cfg.include_antiholomorphic_copy) [2]Spec.ZeroModeRule else [1]Spec.ZeroModeRule {\\n    const holomorphic = Spec.ZeroModeRule{ .sector = zeroSector(.sphere), .expr = .{ .bc_top_form = .{\\n        .support = .sphere_holomorphic,\\n        .c_kind_ids = &.{kind(.c)},\\n        .normalization = .one,\\n    } } };\\n    if (!cfg.include_antiholomorphic_copy) return [_]Spec.ZeroModeRule{holomorphic};\\n    return [_]Spec.ZeroModeRule{\\n        holomorphic,\\n        .{ .sector = zeroSector(.sphere), .expr = .{ .bc_top_form = .{\\n            .support = .sphere_antiholomorphic,\\n            .c_kind_ids = &.{kind(.ct)},\\n            .normalization = .one,\\n        } } },\\n    };\\n}\\n\\nfn sphereZeroStorage(comptime cfg: BcSphereConfig) if (cfg.include_antiholomorphic_copy) [2]zero_mode.Rule else [1]zero_mode.Rule {\\n    const spec = sphereZeroSpec(cfg);\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nconst sphere_holomorphic_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"b\\", .kind = kind(.b), .support = .holomorphic, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"c\\", .kind = kind(.c), .support = .holomorphic, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst sphere_full_operator_spec = sphere_holomorphic_operator_spec ++ [_]Spec.Operator{\\n    .{ .name = \\"bt\\", .kind = kind(.bt), .support = .antiholomorphic, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"ct\\", .kind = kind(.ct), .support = .antiholomorphic, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst sphere_holomorphic_fermion_storage = Spec.fermionKinds(&sphere_holomorphic_operator_spec);\\n\\nconst sphere_full_fermion_storage = Spec.fermionKinds(&sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c), .expr = bcBC() },\\n};\\n\\nconst sphere_full_wick_spec = sphere_holomorphic_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.bt), .right = kind(.ct), .expr = bcBtCt() },\\n};\\n\\nconst sphere_holomorphic_wick_storage = Spec.wickRules(&sphere_holomorphic_wick_spec, &sphere_holomorphic_operator_spec);\\n\\nconst sphere_full_wick_storage = Spec.wickRules(&sphere_full_wick_spec, &sphere_full_operator_spec);\\n\\nconst torus_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c), .coordinate_kernels = &.{.elliptic_prime_form} },\\n    .{ .left = kind(.bt), .right = kind(.ct), .coordinate_kernels = &.{.elliptic_prime_form} },\\n};\\n\\nconst sphere_holomorphic_zero_spec = sphereZeroSpec(.{ .include_antiholomorphic_copy = false });\\n\\nconst sphere_full_zero_spec = sphereZeroSpec(.{});\\nFor the genus-one amplitude the stringbook fixes one integrated \\\\(b\\\\) insertion\\nfor the torus modulus and one residual \\\\(c\\\\) zero mode in each chiral copy,\\nafter one puncture is fixed by torus translations .  The\\ndraft metadata records those holomorphic and antiholomorphic \\\\(b,c\\\\) families\\nwithout lowering them into the current sphere/disk top-form runtime evaluator.\\nconst torus_zero_spec = [_]Spec.ZeroModeRule{\\n    .{\\n        .sector = zeroSector(.torus),\\n        .kind = .torus_bc_moduli,\\n        .consumes = &.{ kind(.b), kind(.c), kind(.bt), kind(.ct) },\\n    },\\n};\\n\\nfn bcSphereSpec(comptime include_antiholomorphic_copy: bool) Spec.Theory {\\n    return .{\\n        .operators = if (include_antiholomorphic_copy) &sphere_full_operator_spec else &sphere_holomorphic_operator_spec,\\n        .wick_rules = if (include_antiholomorphic_copy) &sphere_full_wick_spec else &sphere_holomorphic_wick_spec,\\n        .zero_modes = if (include_antiholomorphic_copy) &sphere_full_zero_spec else &sphere_holomorphic_zero_spec,\\n    };\\n}\\n\\nconst torus_draft_spec = Spec.Theory{\\n    .surface = .{\\n        .kind = .torus,\\n        .coordinate_model = .elliptic,\\n        .modular_parameters = &.{\\"tau\\"},\\n        .source = .stringbook,\\n    },\\n    .operators = &sphere_full_operator_spec,\\n    .wick_rules = &torus_wick_spec,\\n    .zero_modes = &torus_zero_spec,\\n};\\n\\nconst disk_boundary_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"bBoundary\\", .kind = kind(.b_boundary), .support = .boundary, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"cBoundary\\", .kind = kind(.c_boundary), .support = .boundary, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst disk_full_operator_spec = sphere_full_operator_spec ++ disk_boundary_operator_spec;\\n\\nfn bcOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_full_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing bc operator spec\\");\\n}\\n\\nfn bcBoundaryOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (disk_boundary_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing bc boundary operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime include_antiholomorphic_copy: bool, comptime wick_count: usize, comptime zero_count: usize, comptime fermion_count: usize) void {\\n    const spec = bcSphereSpec(include_antiholomorphic_copy);\\n    if (spec.wick_rules.len != wick_count) @compileError(\\"bc sphere spec Wick count does not match lowered rules\\");\\n    if (spec.zero_modes.len != zero_count) @compileError(\\"bc sphere spec zero-mode count does not match lowered rules\\");\\n    if (spec.operators.len != fermion_count) @compileError(\\"bc sphere spec fermion coverage does not match lowered rules\\");\\n}\\n\\nfn assertTorusDraftSpec() void {\\n    if (torus_draft_spec.surface.kind != .torus) @compileError(\\"bc torus draft spec surface kind is incomplete\\");\\n    if (torus_draft_spec.surface.coordinate_model != .elliptic) @compileError(\\"bc torus draft spec coordinate model is incomplete\\");\\n    if (torus_draft_spec.surface.modular_parameters.len != 1) @compileError(\\"bc torus draft spec modular metadata is incomplete\\");\\n    if (torus_draft_spec.surface.source != .stringbook) @compileError(\\"bc torus draft spec source convention is incomplete\\");\\n    if (torus_draft_spec.operators.len != sphere_full_operator_spec.len) @compileError(\\"bc torus draft spec operator metadata is incomplete\\");\\n    if (torus_draft_spec.wick_rules.len != sphere_full_wick_spec.len) @compileError(\\"bc torus draft spec Wick metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_prime_form) != 2) @compileError(\\"bc torus draft spec prime-form metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .rational_pole) != 0) @compileError(\\"bc torus draft spec still uses rational pole metadata\\");\\n    if (torus_draft_spec.zero_modes.len != 1) @compileError(\\"bc torus draft spec zero-mode metadata is incomplete\\");\\n    if (Spec.zeroModeConsumeKindCount(torus_draft_spec.zero_modes[0]) != 4) @compileError(\\"bc torus draft spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn BcSphereRules(comptime include_antiholomorphic_copy: bool) type {\\n    const selected_wick = if (include_antiholomorphic_copy) sphere_full_wick_storage else sphere_holomorphic_wick_storage;\\n    const selected_fermions = if (include_antiholomorphic_copy) sphere_full_fermion_storage else sphere_holomorphic_fermion_storage;\\n    return struct {\\n        const sphere_wick: []const wick.Rule = &selected_wick;\\n        const sphere_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\nfn BcSphereCorrelatorConfig(comptime cfg: BcSphereConfig) type {\\n    const rules = BcSphereRules(cfg.include_antiholomorphic_copy);\\n    return struct {\\n        const sphere_zero_storage = sphereZeroStorage(cfg);\\n        comptime {\\n            assertSphereSpec(cfg.include_antiholomorphic_copy, rules.sphere_wick.len, sphere_zero_storage.len, rules.sphere_fermion_kinds.len);\\n            assertTorusDraftSpec();\\n        }\\n\\n        /// sphere selects bc sphere Wick and zero-mode rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &sphere_zero_storage,\\n            .fermion_kinds = rules.sphere_fermion_kinds,\\n        }){};\\n    };\\n}\\nDisk BoundaryThe disk extension adds boundary-local \\\\(b,c\\\\) operators and mixed\\nbulk-boundary contractions.  It is an extension chosen by a BCFT composition,\\nnot a modification of the bulk \\\\(bc\\\\) preset.\\nconst BcDiskConfig = struct {\\n    include_mixed_bulk_boundary: bool = true,\\n};\\n\\nconst BcDiskBoundaryOp = struct {\\n    /// bBoundary builds a boundary b-ghost insertion.\\n    pub fn bBoundary(local: anytype, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(bcBoundaryOperator(.b_boundary)).boundarySingle(local, y, n, .{});\\n    }\\n\\n    /// cBoundary builds a boundary c-ghost insertion.\\n    pub fn cBoundary(local: anytype, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(bcBoundaryOperator(.c_boundary)).boundarySingle(local, y, n, .{});\\n    }\\n};\\n\\nfn BcBoundaryExtension(comptime cfg: BcDiskConfig) type {\\n    const data = BcDiskData(cfg);\\n    return declare.BoundaryExtension(.{\\n        .op = BcDiskBoundaryOp,\\n        .wick_rules = if (cfg.include_mixed_bulk_boundary) &disk_full_wick_storage else &disk_boundary_wick_storage,\\n        .zero_modes = data.disk_zero_modes,\\n        .fermion_kinds = data.disk_fermion_kinds,\\n    });\\n}\\n\\n/// boundaryExtension declares boundary and mixed bulk-boundary bc rules on the disk.\\npub fn boundaryExtension(comptime cfg: BcDiskConfig) BcBoundaryExtension(cfg) {\\n    return .{};\\n}\\n\\nconst disk_boundary_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b_boundary), .right = kind(.c_boundary), .expr = bcBC() },\\n};\\n\\nconst disk_full_wick_spec = disk_boundary_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c_boundary), .expr = bcBC() },\\n    .{ .left = kind(.c), .right = kind(.b_boundary), .expr = bcBC() },\\n    .{ .left = kind(.bt), .right = kind(.c_boundary), .expr = bcBtCt() },\\n    .{ .left = kind(.ct), .right = kind(.b_boundary), .expr = bcBtCt() },\\n};\\n\\nconst disk_boundary_wick_storage = Spec.wickRules(&disk_boundary_wick_spec, &disk_boundary_operator_spec);\\n\\nconst disk_full_wick_storage = Spec.wickRules(&disk_full_wick_spec, &disk_full_operator_spec);\\n\\nconst disk_boundary_fermion_storage = Spec.fermionKinds(&disk_boundary_operator_spec);\\n\\nconst disk_full_fermion_storage = Spec.fermionKinds(&disk_full_operator_spec);\\n\\nfn diskZeroSpec() [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.disk), .expr = .{ .bc_top_form = .{\\n            .support = .disk_doubled,\\n            .c_kind_ids = &.{ kind(.c), kind(.ct), kind(.c_boundary) },\\n            .normalization = .one,\\n        } } },\\n    };\\n}\\n\\nfn diskZeroStorage(comptime cfg: BcDiskConfig) [1]zero_mode.Rule {\\n    _ = cfg;\\n    const spec = diskZeroSpec();\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nfn BcDiskData(comptime cfg: BcDiskConfig) type {\\n    const selected_fermions = if (cfg.include_mixed_bulk_boundary) disk_full_fermion_storage else disk_boundary_fermion_storage;\\n    return struct {\\n        const disk_zero_storage = diskZeroStorage(cfg);\\n\\n        const disk_zero_modes: []const zero_mode.Rule = &disk_zero_storage;\\n        const disk_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\ntest \\"bc schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const b = try Ghost.op.b(&local, 1, z1);\\n    const c = try Ghost.op.c(&local, 2, z2);\\n    const ops = try local.ops(.{ b, c });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(b)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(c)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\ntest \\"bc boundary schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n\\n    var local = try Local.init(testing.allocator);\\n    defer local.deinit();\\n\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const b = try BcDiskBoundaryOp.bBoundary(&local, 1, y);\\n    const c = try BcDiskBoundaryOp.cBoundary(&local, 2, y);\\n    const ops = try local.ops(.{ b, c });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(b)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(c)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\n"},{"id":"20260606T183400.000000","title":"Preset Composition","tags":null,"content":"Composition turns bulk presets such as Free Boson Preset and\\nbc Preset into products and BCFTs.  It uses the shared\\nruntime vocabulary from Preset Shared Runtime.\\nThe important invariant is that tensor products do not allocate a new runtime\\noperator representation.  Product quantum numbers remain lists at theory level;\\nruntime local operators are still just insertion data, a body kind, and a label\\nspan.  Composition asks the shared lowering path to merge opaque configs, so it\\ndoes not name config-entry records or slice append helpers.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst Builder = *shared.Local;\\nProduct CFTsA product preset forms its sphere config from the generated sphere config of\\neach factor.  It deliberately does not synthesize preset-named operator\\nnamespaces.  Future friendly aliases belong in the user DSL layer, not in the\\nlowering path.\\nfn ProductCorrelatorConfig(comptime factors: anytype) type {\\n    const merged = declare.mergeSphereConfigs(factors);\\n    return struct {\\n        /// sphere selects composed product Wick and zero-mode rules.\\n        pub const sphere = merged.config;\\n    };\\n}\\n\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub fn product(comptime factors: anytype) type {\\n    return struct {\\n        /// op is intentionally empty; use the factor builders passed to product.\\n        pub const op = struct {};\\n        /// config exposes named correlator configs for this product preset.\\n        pub const config = ProductCorrelatorConfig(factors);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for product-theory insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nBoundary ExtensionsA BCFT is built from a bulk preset plus explicit boundary extensions.  The bulk\\npreset remains boundary-free; boundary data contributes new operator families,\\nmixed Wick rules, zero-mode caveats, and optional Chan-Paton stack data.\\nBoundary-local builders remain on the extension handles passed to boundary,\\nso composition does not inspect preset names to create aliases.\\nThe disk config is formed by the shared merge helper.  Composition keeps only\\nthe physics-facing bulk/extension relationship and does not see packed config\\nentries, slice selectors, or append procedures.\\nfn BoundaryCorrelatorConfig(comptime Bulk: type, comptime extensions: anytype) type {\\n    const merged = declare.mergeBoundaryConfig(Bulk, extensions);\\n    return struct {\\n        /// disk selects composed boundary Wick and zero-mode rules.\\n        pub const disk = merged.config;\\n    };\\n}\\n\\nfn BoundaryOp(comptime Bulk: type, comptime extensions: anytype) type {\\n    _ = extensions;\\n    return struct {\\n        /// bulk exposes the bulk operator namespaces.\\n        pub const bulk = Bulk.op;\\n    };\\n}\\n\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub fn boundary(comptime Bulk: type, comptime extensions: anytype) type {\\n    return struct {\\n        /// op exposes bulk and boundary operator builders.\\n        pub const op = BoundaryOp(Bulk, extensions);\\n        /// config exposes named correlator configs for this BCFT.\\n        pub const config = BoundaryCorrelatorConfig(Bulk, extensions);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for BCFT insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\n"},{"id":"20260606T190101.000000","title":"Tensor Representation Store","tags":null,"content":"This module owns the abstract representation data.  A Lie algebra and an\\nirrep are interned once, then every downstream system carries small typed\\nhandles.  That keeps tensor generation independent of how a representation is\\neventually rendered as indices, matrices, or symbolic projectors.\\nconst std = @import(\\"std\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\nHandles are public because other modules need stable references.  The records\\nbehind them stay private.\\n/// AlgebraHandle names a registered Lie algebra in a tensor context.\\npub const AlgebraHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an algebra handle from a store index.\\n    pub fn init(value: u32) AlgebraHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// IrrepHandle names an abstract irreducible representation.\\npub const IrrepHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an irrep handle from a store index.\\n    pub fn init(value: u32) IrrepHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ChannelHandle names one irrep copy inside a local product.\\npub const ChannelHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a channel handle from a store index.\\n    pub fn init(value: u32) ChannelHandle {\\n        return .{ .value = value };\\n    }\\n};\\nSpecs are caller-owned construction data.  Interning clones only the slices we\\nmust retain.\\n/// AlgebraSpec describes a requested algebra before it is interned.\\npub const AlgebraSpec = union(enum) {\\n    u1,\\n    simple: symmetry.SimpleLieAlgebra,\\n};\\n\\n/// IrrepSpec describes a requested representation before it is interned.\\npub const IrrepSpec = union(enum) {\\n    u1_charge: symmetry.U1ChargeRational,\\n    dynkin: []const i16,\\n};\\n\\n/// IrrepMetadata stores derived representation data.\\npub const IrrepMetadata = struct {\\n    dimension: ?u128 = null,\\n    dual: ?IrrepHandle = null,\\n    quadratic_casimir: ?root_data.Rational = null,\\n};\\nThe store is intentionally simple: linear lookup first, then append.  The first\\nimplementation has few registered algebras and irreps compared with the later\\ncoupling search, so this avoids prematurely adding hash table machinery.  If\\nprofiles show this store in the hot path, the record layout can stay fixed\\nwhile we add an index.\\n/// Store owns interned algebra and irrep records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    conventions: root_data.AlgebraConventions,\\n    algebras: std.ArrayList(AlgebraRecord) = .empty,\\n    irreps: std.ArrayList(IrrepRecord) = .empty,\\n\\n    /// init constructs an empty representation store.\\n    pub fn init(allocator: std.mem.Allocator, conventions: root_data.AlgebraConventions) Store {\\n        return .{ .allocator = allocator, .conventions = conventions };\\n    }\\n\\n    /// deinit releases all representation-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.irreps.items) |record| {\\n            switch (record.label) {\\n                .dynkin => |label| self.allocator.free(label),\\n                .u1_charge => {},\\n            }\\n        }\\n        self.irreps.deinit(self.allocator);\\n        self.algebras.deinit(self.allocator);\\n        self.* = Store.init(self.allocator, self.conventions);\\n    }\\n\\n    /// internAlgebra returns the stable handle for an algebra spec.\\n    pub fn internAlgebra(self: *Store, spec: AlgebraSpec) !AlgebraHandle {\\n        try validateAlgebraSpec(spec);\\n        for (self.algebras.items, 0..) |record, index| {\\n            if (algebraSpecEql(record.spec, spec)) {\\n                return AlgebraHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        try self.algebras.append(self.allocator, .{\\n            .spec = spec,\\n            .root_data = try makeRootDatum(spec, self.conventions),\\n        });\\n        return AlgebraHandle.init(@intCast(self.algebras.items.len - 1));\\n    }\\n\\n    /// containsAlgebra returns whether an algebra handle belongs to this store.\\n    pub fn containsAlgebra(self: Store, handle: AlgebraHandle) bool {\\n        return self.algebraRecord(handle) != null;\\n    }\\n\\n    /// internIrrep returns the stable handle for an irrep spec in an algebra.\\n    pub fn internIrrep(self: *Store, algebra: AlgebraHandle, spec: IrrepSpec) anyerror!IrrepHandle {\\n        const algebra_record = self.algebraRecord(algebra) orelse return error.UnknownAlgebra;\\n        try validateIrrepSpec(algebra_record.spec, spec);\\n\\n        for (self.irreps.items, 0..) |record, index| {\\n            if (handleEql(record.algebra, algebra) and irrepLabelSpecEql(record.label, spec)) {\\n                return IrrepHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const label = try cloneIrrepSpec(self.allocator, spec);\\n        errdefer freeIrrepLabel(self.allocator, label);\\n\\n        try self.irreps.append(self.allocator, .{\\n            .algebra = algebra,\\n            .label = label,\\n            .metadata = .{},\\n        });\\n        errdefer _ = self.irreps.pop();\\n        const handle = IrrepHandle.init(@intCast(self.irreps.items.len - 1));\\n        try self.fillIrrepMetadata(handle);\\n        return handle;\\n    }\\n\\n    /// dualIrrep returns the handle for the dual irrep, interning it if requested.\\n    pub fn dualIrrep(self: *Store, handle: IrrepHandle) anyerror!IrrepHandle {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.irreps.items.len) return error.UnknownIrrep;\\n        if (self.irreps.items[index].metadata.dual) |dual| return dual;\\n\\n        const record = self.irreps.items[index];\\n        const dual = switch (record.label) {\\n            .u1_charge => |charge| try self.internIrrep(record.algebra, .{ .u1_charge = .{\\n                .numerator = -charge.numerator,\\n                .denominator = charge.denominator,\\n            } }),\\n            .dynkin => |label| dual: {\\n                const algebra = self.algebraRecord(record.algebra) orelse return error.UnknownAlgebra;\\n                const dual_label = try root_data.dualDynkin(self.allocator, algebra.spec.simple, label);\\n                defer self.allocator.free(dual_label);\\n                break :dual try self.internIrrep(record.algebra, .{ .dynkin = dual_label });\\n            },\\n        };\\n\\n        self.irreps.items[index].metadata.dual = dual;\\n        const dual_index: usize = @intCast(dual.value);\\n        if (dual_index < self.irreps.items.len) self.irreps.items[dual_index].metadata.dual = handle;\\n        return dual;\\n    }\\n\\n    /// containsIrrep returns whether an irrep handle belongs to this store.\\n    pub fn containsIrrep(self: Store, handle: IrrepHandle) bool {\\n        return self.irrepRecord(handle) != null;\\n    }\\n\\n    /// irrepMetadata returns derived metadata for an irrep handle.\\n    pub fn irrepMetadata(self: Store, handle: IrrepHandle) ?IrrepMetadata {\\n        const record = self.irrepRecord(handle) orelse return null;\\n        return record.metadata;\\n    }\\n\\n    /// irrepAlgebra returns the algebra that owns an irrep handle.\\n    pub fn irrepAlgebra(self: Store, handle: IrrepHandle) ?AlgebraHandle {\\n        const record = self.irrepRecord(handle) orelse return null;\\n        return record.algebra;\\n    }\\n\\n    fn algebraRecord(self: Store, handle: AlgebraHandle) ?AlgebraRecord {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.algebras.items.len) return null;\\n        return self.algebras.items[index];\\n    }\\n\\n    fn irrepRecord(self: Store, handle: IrrepHandle) ?IrrepRecord {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.irreps.items.len) return null;\\n        return self.irreps.items[index];\\n    }\\n\\n    fn fillIrrepMetadata(self: *Store, handle: IrrepHandle) anyerror!void {\\n        const index: usize = @intCast(handle.value);\\n        const record = self.irreps.items[index];\\n        const algebra = self.algebraRecord(record.algebra) orelse return error.UnknownAlgebra;\\n\\n        switch (record.label) {\\n            .u1_charge => |charge| {\\n                self.irreps.items[index].metadata.dimension = 1;\\n                self.irreps.items[index].metadata.quadratic_casimir = try u1QuadraticCasimir(charge);\\n                if (charge.numerator == 0) self.irreps.items[index].metadata.dual = handle;\\n            },\\n            .dynkin => |label| {\\n                const simple = algebra.spec.simple;\\n                self.irreps.items[index].metadata.dimension = try root_data.dimension(self.allocator, simple, label);\\n                self.irreps.items[index].metadata.quadratic_casimir = try root_data.quadraticCasimir(self.allocator, simple, label, self.conventions);\\n                const dual_label = try root_data.dualDynkin(self.allocator, simple, label);\\n                defer self.allocator.free(dual_label);\\n                if (std.mem.eql(i16, label, dual_label)) {\\n                    self.irreps.items[index].metadata.dual = handle;\\n                } else if (self.findIrrep(record.algebra, .{ .dynkin = dual_label })) |dual| {\\n                    self.irreps.items[index].metadata.dual = dual;\\n                }\\n            },\\n        }\\n    }\\n\\n    fn findIrrep(self: Store, algebra: AlgebraHandle, spec: IrrepSpec) ?IrrepHandle {\\n        for (self.irreps.items, 0..) |record, index| {\\n            if (handleEql(record.algebra, algebra) and irrepLabelSpecEql(record.label, spec)) {\\n                return IrrepHandle.init(@intCast(index));\\n            }\\n        }\\n        return null;\\n    }\\n};\\nPrivate records carry derived metadata, root data, and future product-channel\\noccurrences.  They are not exported because callers should not depend on the\\nstorage layout.\\nconst AlgebraRecord = struct {\\n    spec: AlgebraSpec,\\n    root_data: ?root_data.RootDatum,\\n};\\n\\nconst IrrepRecord = struct {\\n    algebra: AlgebraHandle,\\n    label: IrrepLabel,\\n    metadata: IrrepMetadata,\\n};\\n\\nconst IrrepLabel = union(enum) {\\n    u1_charge: symmetry.U1ChargeRational,\\n    dynkin: []const i16,\\n};\\n\\nconst ChannelRecord = struct {\\n    irrep: IrrepHandle,\\n    multiplicity_copy: u16,\\n};\\n\\nconst OccurrenceKind = enum {\\n    external_leg,\\n    product_node,\\n    realization,\\n};\\n\\nconst OccurrenceRecord = struct {\\n    channel: ChannelHandle,\\n    kind: OccurrenceKind,\\n    source_id: u32,\\n    realization: ?u32 = null,\\n};\\nInterning equality is structural and allocation-free.  Validation happens before\\ninterning: a simple-algebra irrep must have one non-negative Dynkin label per\\nrank, and a U(1) irrep must be a rational charge.  Unknown dimension and dual\\ndata is represented by null, never by a fake handle.\\nfn handleEql(left: AlgebraHandle, right: AlgebraHandle) bool {\\n    return left.value == right.value;\\n}\\n\\nfn validateAlgebraSpec(spec: AlgebraSpec) !void {\\n    switch (spec) {\\n        .u1 => {},\\n        .simple => |simple| try root_data.validateSimple(simple),\\n    }\\n}\\n\\nfn makeRootDatum(spec: AlgebraSpec, conventions: root_data.AlgebraConventions) !?root_data.RootDatum {\\n    return switch (spec) {\\n        .u1 => null,\\n        .simple => |simple| try root_data.RootDatum.init(simple, conventions),\\n    };\\n}\\n\\nfn algebraSpecEql(left: AlgebraSpec, right: AlgebraSpec) bool {\\n    return switch (left) {\\n        .u1 => switch (right) {\\n            .u1 => true,\\n            .simple => false,\\n        },\\n        .simple => |left_simple| switch (right) {\\n            .u1 => false,\\n            .simple => |right_simple| left_simple.family == right_simple.family and left_simple.rank == right_simple.rank,\\n        },\\n    };\\n}\\n\\nfn validateIrrepSpec(algebra: AlgebraSpec, spec: IrrepSpec) !void {\\n    switch (algebra) {\\n        .u1 => switch (spec) {\\n            .u1_charge => |charge| {\\n                if (charge.denominator == 0) return error.InvalidU1Charge;\\n            },\\n            .dynkin => return error.InvalidIrrepForAlgebra,\\n        },\\n        .simple => |simple| switch (spec) {\\n            .u1_charge => return error.InvalidIrrepForAlgebra,\\n            .dynkin => |label| {\\n                if (label.len != simple.rank) return error.InvalidDynkinRank;\\n                for (label) |entry| {\\n                    if (entry < 0) return error.InvalidDynkinLabel;\\n                }\\n            },\\n        },\\n    }\\n}\\n\\nfn u1QuadraticCasimir(charge: symmetry.U1ChargeRational) !root_data.Rational {\\n    const numerator = @as(i128, charge.numerator) * @as(i128, charge.numerator);\\n    const denominator = @as(i128, charge.denominator) * @as(i128, charge.denominator);\\n    return root_data.Rational.init(numerator, denominator);\\n}\\n\\nfn irrepLabelSpecEql(left: IrrepLabel, right: IrrepSpec) bool {\\n    return switch (left) {\\n        .u1_charge => |left_charge| switch (right) {\\n            .u1_charge => |right_charge| left_charge.numerator == right_charge.numerator and left_charge.denominator == right_charge.denominator,\\n            .dynkin => false,\\n        },\\n        .dynkin => |left_dynkin| switch (right) {\\n            .u1_charge => false,\\n            .dynkin => |right_dynkin| std.mem.eql(i16, left_dynkin, right_dynkin),\\n        },\\n    };\\n}\\n\\nfn cloneIrrepSpec(allocator: std.mem.Allocator, spec: IrrepSpec) !IrrepLabel {\\n    return switch (spec) {\\n        .u1_charge => |charge| .{ .u1_charge = charge },\\n        .dynkin => |label| .{ .dynkin = try allocator.dupe(i16, label) },\\n    };\\n}\\n\\nfn freeIrrepLabel(allocator: std.mem.Allocator, label: IrrepLabel) void {\\n    switch (label) {\\n        .dynkin => |dynkin| allocator.free(dynkin),\\n        .u1_charge => {},\\n    }\\n}\\n"},{"id":"20260606T190103.000000","title":"Tensor Realization","tags":null,"content":"Realizations map abstract irreps to concrete index layouts.  This is where a\\nfield acquires caller-visible slots such as vector, spinor, fundamental, or\\nadjoint indices.  The coupling search still reasons about the target irrep, so\\nprojected realizations like an irrep inside \\\\(V \\\\otimes S\\\\) do not force every\\nlater step to carry the full ambient product.\\nconst std = @import(\\"std\\");\\nconst store = @import(\\"representation-store.zig\\");\\nPublic handles identify registered concrete realizations and external legs.\\n/// RealizationHandle names a concrete index realization of an irrep.\\npub const RealizationHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a realization handle from a store index.\\n    pub fn init(value: u32) RealizationHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ExternalLegHandle names one external leg in an invariant request.\\npub const ExternalLegHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an external-leg handle from a store index.\\n    pub fn init(value: u32) ExternalLegHandle {\\n        return .{ .value = value };\\n    }\\n};\\nConstruction records are public because CFT code must be able to describe its\\nfree indices.  The arrays are caller-owned until they are registered in the\\ncontext.\\n/// RealizationRef identifies a primitive or registered external tensor shape.\\npub const RealizationRef = union(enum) {\\n    primitive_irrep: store.IrrepHandle,\\n    handle: RealizationHandle,\\n    registered_name: []const u8,\\n};\\n\\n/// IndexKind classifies the visible slot type of an external index.\\npub const IndexKind = enum {\\n    vector,\\n    spinor,\\n    conjugate_spinor,\\n    fundamental,\\n    anti_fundamental,\\n    adjoint,\\n    form,\\n    custom,\\n};\\n\\n/// NamedIndex records one caller-supplied free index name.\\npub const NamedIndex = struct {\\n    kind: IndexKind,\\n    name: []const u8,\\n\\n    /// init constructs one caller-named index slot.\\n    pub fn init(kind: IndexKind, name: []const u8) NamedIndex {\\n        return .{ .kind = kind, .name = name };\\n    }\\n};\\n\\n/// RealizationChannel selects one irrep copy inside a realization product.\\npub const RealizationChannel = struct {\\n    irrep: store.IrrepHandle,\\n    copy: u16 = 0,\\n\\n    /// init constructs a selected irrep copy.\\n    pub fn init(irrep: store.IrrepHandle, copy: u16) RealizationChannel {\\n        return .{ .irrep = irrep, .copy = copy };\\n    }\\n\\n    /// first selects the first copy of an irrep.\\n    pub fn first(irrep: store.IrrepHandle) RealizationChannel {\\n        return .{ .irrep = irrep };\\n    }\\n};\\n\\n/// ExternalLeg describes a public invariant leg with named free indices.\\npub const ExternalLeg = struct {\\n    realization: RealizationRef,\\n    indices: []const NamedIndex,\\n    label: ?[]const u8 = null,\\n\\n    /// primitive constructs a leg whose realization is the irrep itself.\\n    pub fn primitive(irrep: store.IrrepHandle, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .primitive_irrep = irrep },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// realized constructs a leg from a registered realization handle.\\n    pub fn realized(realization: RealizationHandle, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .handle = realization },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// registered constructs a leg from a preset realization name.\\n    pub fn registered(name: []const u8, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .registered_name = name },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// named returns the leg with a caller-facing label attached.\\n    pub fn named(self: ExternalLeg, label: []const u8) ExternalLeg {\\n        var out = self;\\n        out.label = label;\\n        return out;\\n    }\\n};\\n\\n/// RealizationSpec describes a primitive or projected external index model.\\npub const RealizationSpec = struct {\\n    channel: RealizationChannel,\\n    ambient: []const store.IrrepHandle = &.{},\\n    indices: []const NamedIndex,\\n    name: ?[]const u8 = null,\\n\\n    /// primitive constructs a direct index realization of an irrep.\\n    pub fn primitive(target: store.IrrepHandle, indices: []const NamedIndex) RealizationSpec {\\n        return .{\\n            .channel = RealizationChannel.first(target),\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// projected constructs an irrep realization inside an ambient product.\\n    pub fn projected(target: store.IrrepHandle, ambient: []const store.IrrepHandle, indices: []const NamedIndex, copy: u16) RealizationSpec {\\n        return RealizationSpec.projectedChannel(RealizationChannel.init(target, copy), ambient, indices);\\n    }\\n\\n    /// projectedChannel constructs a realization from an explicit channel.\\n    pub fn projectedChannel(channel: RealizationChannel, ambient: []const store.IrrepHandle, indices: []const NamedIndex) RealizationSpec {\\n        return .{\\n            .channel = channel,\\n            .ambient = ambient,\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// named returns the realization spec with a preset-facing name attached.\\n    pub fn named(self: RealizationSpec, name: []const u8) RealizationSpec {\\n        var out = self;\\n        out.name = name;\\n        return out;\\n    }\\n\\n    /// targetIrrep returns the abstract irrep realized by this index model.\\n    pub fn targetIrrep(self: RealizationSpec) store.IrrepHandle {\\n        return self.channel.irrep;\\n    }\\n};\\nThe store owns cloned realization specs and interns repeated requests.\\n/// Store owns interned concrete realization records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    specs: std.ArrayList(RealizationSpec) = .empty,\\n\\n    /// init constructs an empty realization store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases all realization-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.specs.items) |spec| {\\n            freeRealizationSpec(self.allocator, spec);\\n        }\\n        self.specs.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internRealization returns the stable handle for a realization spec.\\n    pub fn internRealization(self: *Store, spec: RealizationSpec) !RealizationHandle {\\n        for (self.specs.items, 0..) |stored, index| {\\n            if (realizationSpecEql(stored, spec)) {\\n                return RealizationHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const owned = try cloneRealizationSpec(self.allocator, spec);\\n        errdefer freeRealizationSpec(self.allocator, owned);\\n        try self.specs.append(self.allocator, owned);\\n        return RealizationHandle.init(@intCast(self.specs.items.len - 1));\\n    }\\n\\n    /// containsRealization returns whether a realization handle belongs to this store.\\n    pub fn containsRealization(self: Store, handle: RealizationHandle) bool {\\n        return self.realizationSpec(handle) != null;\\n    }\\n\\n    /// targetIrrep returns the irrep realized by a stored realization handle.\\n    pub fn targetIrrep(self: Store, handle: RealizationHandle) ?store.IrrepHandle {\\n        const spec = self.realizationSpec(handle) orelse return null;\\n        return spec.targetIrrep();\\n    }\\n\\n    fn realizationSpec(self: Store, handle: RealizationHandle) ?RealizationSpec {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.specs.items.len) return null;\\n        return self.specs.items[index];\\n    }\\n};\\nThese private records describe the future projector derivation path for a\\nrealization.  The selected channel is a single value, so future embedding\\nconventions have one natural place to attach.\\n/// IndexSlotSpec declares one slot in a realization layout.\\nconst IndexSlotSpec = struct {\\n    kind: IndexKind,\\n    default_name: ?[]const u8 = null,\\n};\\n\\n/// RealizationRequest declares an irrep as a projected ambient tensor shape.\\nconst RealizationRequest = struct {\\n    channel: RealizationChannel,\\n    ambient: []const store.IrrepHandle,\\n    index_layout: []const IndexSlotSpec,\\n};\\n\\n/// RealizationKind distinguishes primitive from projected realizations.\\nconst RealizationKind = enum {\\n    primitive,\\n    projected_channel,\\n};\\n\\n/// RealizationRecord stores one concrete index layout for an irrep.\\nconst RealizationRecord = struct {\\n    channel: RealizationChannel,\\n    kind: RealizationKind,\\n    ambient_offset: u32,\\n    ambient_len: u16,\\n    index_layout_offset: u32,\\n    index_layout_len: u16,\\n    projector: ?RealizationProjectorId = null,\\n};\\n\\n/// RealizationProjectorId names a derived boundary realization projector.\\nconst RealizationProjectorId = u32;\\n\\n/// AmbientFactor records one irrep in an ambient realization product.\\nconst AmbientFactor = struct {\\n    irrep: store.IrrepHandle,\\n};\\n\\n/// RealizationRule records the construction request for one realization.\\nconst RealizationRule = struct {\\n    request: RealizationRequest,\\n    projector: ?RealizationProjectorId = null,\\n};\\nStructural equality and clone/free helpers are private implementation details.\\nThey keep public construction records lightweight while making registered\\nrealizations stable after caller buffers go out of scope.\\nfn realizationSpecEql(left: RealizationSpec, right: RealizationSpec) bool {\\n    return realizationChannelEql(left.channel, right.channel) and\\n        optionalStringEql(left.name, right.name) and\\n        irrepSliceEql(left.ambient, right.ambient) and\\n        namedIndexSliceEql(left.indices, right.indices);\\n}\\n\\nfn realizationChannelEql(left: RealizationChannel, right: RealizationChannel) bool {\\n    return left.irrep.value == right.irrep.value and left.copy == right.copy;\\n}\\n\\nfn irrepSliceEql(left: []const store.IrrepHandle, right: []const store.IrrepHandle) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_item, right_item| {\\n        if (left_item.value != right_item.value) return false;\\n    }\\n    return true;\\n}\\n\\nfn namedIndexSliceEql(left: []const NamedIndex, right: []const NamedIndex) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_item, right_item| {\\n        if (left_item.kind != right_item.kind) return false;\\n        if (!std.mem.eql(u8, left_item.name, right_item.name)) return false;\\n    }\\n    return true;\\n}\\n\\nfn optionalStringEql(left: ?[]const u8, right: ?[]const u8) bool {\\n    if (left == null or right == null) return left == null and right == null;\\n    return std.mem.eql(u8, left.?, right.?);\\n}\\n\\nfn cloneRealizationSpec(allocator: std.mem.Allocator, spec: RealizationSpec) !RealizationSpec {\\n    const ambient = try allocator.dupe(store.IrrepHandle, spec.ambient);\\n    errdefer allocator.free(ambient);\\n\\n    const indices = try cloneNamedIndices(allocator, spec.indices);\\n    errdefer freeNamedIndices(allocator, indices);\\n\\n    const name = if (spec.name) |value| try allocator.dupe(u8, value) else null;\\n    errdefer if (name) |value| allocator.free(value);\\n\\n    return .{\\n        .channel = spec.channel,\\n        .ambient = ambient,\\n        .indices = indices,\\n        .name = name,\\n    };\\n}\\n\\nfn freeRealizationSpec(allocator: std.mem.Allocator, spec: RealizationSpec) void {\\n    allocator.free(spec.ambient);\\n    freeNamedIndices(allocator, spec.indices);\\n    if (spec.name) |name| allocator.free(name);\\n}\\n\\nfn cloneNamedIndices(allocator: std.mem.Allocator, indices: []const NamedIndex) ![]NamedIndex {\\n    const owned = try allocator.alloc(NamedIndex, indices.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |previous| allocator.free(previous.name);\\n        allocator.free(owned);\\n    }\\n\\n    for (indices, owned[0..]) |source, *target| {\\n        const name = try allocator.dupe(u8, source.name);\\n        target.* = .{ .kind = source.kind, .name = name };\\n        initialized += 1;\\n    }\\n\\n    return owned;\\n}\\n\\nfn freeNamedIndices(allocator: std.mem.Allocator, indices: []const NamedIndex) void {\\n    for (indices) |index| {\\n        allocator.free(index.name);\\n    }\\n    allocator.free(indices);\\n}\\n"},{"id":"20260606T190105.000000","title":"Tensor Coupling","tags":null,"content":"Coupling owns invariant-basis requests and, later, the compact paths that span\\nthose bases.  It does not own dense tensor values.  A basis element should be a\\nsmall recipe: which external legs are coupled, which intermediate channels are\\nused, and which local projector kernels render the final invariant if a caller\\nasks for explicit components.\\nconst std = @import(\\"std\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nBasis and invariant handles are public ids.  The path records behind them stay\\nprivate because we want freedom to change the solver layout.\\n/// BasisHandle names a generated invariant basis.\\npub const BasisHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a basis handle from a store index.\\n    pub fn init(value: u32) BasisHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// InvariantHandle names one invariant inside a basis.\\npub const InvariantHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an invariant handle from a store index.\\n    pub fn init(value: u32) InvariantHandle {\\n        return .{ .value = value };\\n    }\\n};\\nThe public request is deliberately small: algebra, external legs, target, and\\nbasis policy.  The default target is the singlet because that is the CFT\\nworkflow for invariant tensor structures.\\n/// TargetIrrep selects the target representation, usually the singlet.\\npub const TargetIrrep = union(enum) {\\n    singlet,\\n    irrep: store.IrrepHandle,\\n};\\n\\n/// TreePolicy controls the coupling tree used for a basis.\\npub const TreePolicy = union(enum) {\\n    auto,\\n    fixed: []const u16,\\n};\\n\\n/// BasisPolicy controls the public basis convention.\\npub const BasisPolicy = enum {\\n    default,\\n    coupling_paths,\\n    channel_adapted,\\n};\\n\\n/// InvariantBasisRequest is the CFT-facing request for invariant tensors.\\npub const InvariantBasisRequest = struct {\\n    algebra: store.AlgebraHandle,\\n    external_legs: []const realization.ExternalLeg,\\n    target: TargetIrrep = .singlet,\\n    tree_policy: TreePolicy = .auto,\\n    basis_policy: BasisPolicy = .default,\\n};\\n\\n/// TensorExpansionTerm stores a CFT coefficient attached to one invariant id.\\npub const TensorExpansionTerm = struct {\\n    rational_function_id: u32,\\n    invariant: InvariantHandle,\\n    scalar_id: u32,\\n};\\nThe store interns basis requests.  This is not invariant generation yet; it is\\nthe stable ownership layer the generator will consume.  The important invariant\\nis that caller-owned slices can disappear after registration.\\n/// Store owns interned invariant-basis requests.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    requests: std.ArrayList(InvariantBasisRequest) = .empty,\\n\\n    /// init constructs an empty coupling store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases all coupling-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.requests.items) |request| {\\n            freeRequest(self.allocator, request);\\n        }\\n        self.requests.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internBasisRequest returns a stable basis handle for a request.\\n    pub fn internBasisRequest(self: *Store, request: InvariantBasisRequest) !BasisHandle {\\n        for (self.requests.items, 0..) |stored, index| {\\n            if (requestEql(stored, request)) {\\n                return BasisHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const owned = try cloneRequest(self.allocator, request);\\n        errdefer freeRequest(self.allocator, owned);\\n        try self.requests.append(self.allocator, owned);\\n        return BasisHandle.init(@intCast(self.requests.items.len - 1));\\n    }\\n};\\nInternal coupling records use offsets and lengths into flat stores.  This is\\nthe shape the future dynamic-programming solver should fill without allocating\\nlarge symbolic expressions.\\n/// CouplingInput names either an external leg or a previous coupling node.\\nconst CouplingInput = union(enum) {\\n    external: realization.ExternalLegHandle,\\n    node: u32,\\n};\\n\\n/// CouplingNodeRecord stores one binary coupling step.\\nconst CouplingNodeRecord = struct {\\n    left: CouplingInput,\\n    right: CouplingInput,\\n    output: store.ChannelHandle,\\n    kernel: projector.ProjectorId,\\n};\\n\\n/// CouplingPathRecord stores a compact invariant basis element.\\nconst CouplingPathRecord = struct {\\n    external_offset: u32,\\n    external_len: u16,\\n    node_offset: u32,\\n    node_len: u16,\\n};\\n\\n/// BasisRecord stores one generated invariant basis.\\nconst BasisRecord = struct {\\n    request: InvariantBasisRequest,\\n    path_offset: u32,\\n    path_len: u32,\\n};\\n\\n/// ReachabilityRecord stores pruning data for one coupling tree node.\\nconst ReachabilityRecord = struct {\\n    node: u32,\\n    irrep_offset: u32,\\n    irrep_len: u32,\\n};\\nRequest equality is structural.  This makes repeated high-level calls cheap and\\ndeterministic before the expensive solver exists.\\nfn requestEql(left: InvariantBasisRequest, right: InvariantBasisRequest) bool {\\n    return left.algebra.value == right.algebra.value and\\n        targetEql(left.target, right.target) and\\n        treePolicyEql(left.tree_policy, right.tree_policy) and\\n        left.basis_policy == right.basis_policy and\\n        externalLegsEql(left.external_legs, right.external_legs);\\n}\\n\\nfn targetEql(left: TargetIrrep, right: TargetIrrep) bool {\\n    return switch (left) {\\n        .singlet => switch (right) {\\n            .singlet => true,\\n            .irrep => false,\\n        },\\n        .irrep => |left_irrep| switch (right) {\\n            .singlet => false,\\n            .irrep => |right_irrep| left_irrep.value == right_irrep.value,\\n        },\\n    };\\n}\\n\\nfn treePolicyEql(left: TreePolicy, right: TreePolicy) bool {\\n    return switch (left) {\\n        .auto => switch (right) {\\n            .auto => true,\\n            .fixed => false,\\n        },\\n        .fixed => |left_fixed| switch (right) {\\n            .auto => false,\\n            .fixed => |right_fixed| std.mem.eql(u16, left_fixed, right_fixed),\\n        },\\n    };\\n}\\n\\nfn externalLegsEql(left: []const realization.ExternalLeg, right: []const realization.ExternalLeg) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_leg, right_leg| {\\n        if (!externalLegEql(left_leg, right_leg)) return false;\\n    }\\n    return true;\\n}\\n\\nfn externalLegEql(left: realization.ExternalLeg, right: realization.ExternalLeg) bool {\\n    return realizationRefEql(left.realization, right.realization) and\\n        namedIndicesEql(left.indices, right.indices) and\\n        optionalStringEql(left.label, right.label);\\n}\\n\\nfn realizationRefEql(left: realization.RealizationRef, right: realization.RealizationRef) bool {\\n    return switch (left) {\\n        .primitive_irrep => |left_irrep| switch (right) {\\n            .primitive_irrep => |right_irrep| left_irrep.value == right_irrep.value,\\n            else => false,\\n        },\\n        .handle => |left_handle| switch (right) {\\n            .handle => |right_handle| left_handle.value == right_handle.value,\\n            else => false,\\n        },\\n        .registered_name => |left_name| switch (right) {\\n            .registered_name => |right_name| std.mem.eql(u8, left_name, right_name),\\n            else => false,\\n        },\\n    };\\n}\\n\\nfn namedIndicesEql(left: []const realization.NamedIndex, right: []const realization.NamedIndex) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_index, right_index| {\\n        if (left_index.kind != right_index.kind) return false;\\n        if (!std.mem.eql(u8, left_index.name, right_index.name)) return false;\\n    }\\n    return true;\\n}\\n\\nfn optionalStringEql(left: ?[]const u8, right: ?[]const u8) bool {\\n    if (left == null or right == null) return left == null and right == null;\\n    return std.mem.eql(u8, left.?, right.?);\\n}\\nThe clone/free helpers keep ownership local to this module.  Later, generated\\npath data can be added beside requests without changing the public request\\nshape.\\nfn cloneRequest(allocator: std.mem.Allocator, request: InvariantBasisRequest) !InvariantBasisRequest {\\n    const external_legs = try cloneExternalLegs(allocator, request.external_legs);\\n    errdefer freeExternalLegs(allocator, external_legs);\\n\\n    return .{\\n        .algebra = request.algebra,\\n        .external_legs = external_legs,\\n        .target = request.target,\\n        .tree_policy = try cloneTreePolicy(allocator, request.tree_policy),\\n        .basis_policy = request.basis_policy,\\n    };\\n}\\n\\nfn freeRequest(allocator: std.mem.Allocator, request: InvariantBasisRequest) void {\\n    freeExternalLegs(allocator, request.external_legs);\\n    switch (request.tree_policy) {\\n        .fixed => |fixed| allocator.free(fixed),\\n        .auto => {},\\n    }\\n}\\n\\nfn cloneTreePolicy(allocator: std.mem.Allocator, policy: TreePolicy) !TreePolicy {\\n    return switch (policy) {\\n        .auto => .auto,\\n        .fixed => |fixed| .{ .fixed = try allocator.dupe(u16, fixed) },\\n    };\\n}\\n\\nfn cloneExternalLegs(allocator: std.mem.Allocator, legs: []const realization.ExternalLeg) ![]realization.ExternalLeg {\\n    const owned = try allocator.alloc(realization.ExternalLeg, legs.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |leg| freeExternalLeg(allocator, leg);\\n        allocator.free(owned);\\n    }\\n\\n    for (legs, owned[0..]) |source, *target| {\\n        target.* = try cloneExternalLeg(allocator, source);\\n        initialized += 1;\\n    }\\n    return owned;\\n}\\n\\nfn freeExternalLegs(allocator: std.mem.Allocator, legs: []const realization.ExternalLeg) void {\\n    for (legs) |leg| freeExternalLeg(allocator, leg);\\n    allocator.free(legs);\\n}\\n\\nfn cloneExternalLeg(allocator: std.mem.Allocator, leg: realization.ExternalLeg) !realization.ExternalLeg {\\n    const indices = try cloneNamedIndices(allocator, leg.indices);\\n    errdefer freeNamedIndices(allocator, indices);\\n\\n    const label = if (leg.label) |value| try allocator.dupe(u8, value) else null;\\n    errdefer if (label) |value| allocator.free(value);\\n\\n    const ref = try cloneRealizationRef(allocator, leg.realization);\\n    errdefer freeRealizationRef(allocator, ref);\\n\\n    return .{ .realization = ref, .indices = indices, .label = label };\\n}\\n\\nfn freeExternalLeg(allocator: std.mem.Allocator, leg: realization.ExternalLeg) void {\\n    freeRealizationRef(allocator, leg.realization);\\n    freeNamedIndices(allocator, leg.indices);\\n    if (leg.label) |label| allocator.free(label);\\n}\\n\\nfn cloneRealizationRef(allocator: std.mem.Allocator, ref: realization.RealizationRef) !realization.RealizationRef {\\n    return switch (ref) {\\n        .primitive_irrep => |irrep| .{ .primitive_irrep = irrep },\\n        .handle => |handle| .{ .handle = handle },\\n        .registered_name => |name| .{ .registered_name = try allocator.dupe(u8, name) },\\n    };\\n}\\n\\nfn freeRealizationRef(allocator: std.mem.Allocator, ref: realization.RealizationRef) void {\\n    switch (ref) {\\n        .registered_name => |name| allocator.free(name),\\n        .primitive_irrep, .handle => {},\\n    }\\n}\\n\\nfn cloneNamedIndices(allocator: std.mem.Allocator, indices: []const realization.NamedIndex) ![]realization.NamedIndex {\\n    const owned = try allocator.alloc(realization.NamedIndex, indices.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |index| allocator.free(index.name);\\n        allocator.free(owned);\\n    }\\n\\n    for (indices, owned[0..]) |source, *target| {\\n        target.* = .{\\n            .kind = source.kind,\\n            .name = try allocator.dupe(u8, source.name),\\n        };\\n        initialized += 1;\\n    }\\n    return owned;\\n}\\n\\nfn freeNamedIndices(allocator: std.mem.Allocator, indices: []const realization.NamedIndex) void {\\n    for (indices) |index| allocator.free(index.name);\\n    allocator.free(indices);\\n}\\n"},{"id":"20260606T190106.000000","title":"Tensor Rendering","tags":null,"content":"Rendering turns compact invariant ids into concrete notation.  Signature is a\\nrendering/evaluation convention, not a different abstract representation.  The\\nsymbolic output is streamed one term at a time so a large expanded projector\\ndoes not become one large allocation.\\nconst coupling = @import(\\"coupling.zig\\");\\n\\n/// SignatureConvention selects the metric convention for rendering/evaluation.\\npub const SignatureConvention = enum {\\n    complex,\\n    euclidean,\\n    lorentzian_mostly_plus,\\n    lorentzian_mostly_minus,\\n};\\n\\n/// ProjectorRenderMode controls how much derived projector data is expanded.\\npub const ProjectorRenderMode = enum {\\n    named,\\n    expanded_blocks,\\n    expanded_terms,\\n};\\n\\n/// RenderFormat selects the output syntax level.\\npub const RenderFormat = enum {\\n    symbolic_terms,\\n    text,\\n};\\n\\n/// RenderOptions controls streamed invariant rendering.\\npub const RenderOptions = struct {\\n    format: RenderFormat = .symbolic_terms,\\n    projectors: ProjectorRenderMode = .named,\\n    signature: SignatureConvention = .complex,\\n    preserve_caller_index_names: bool = true,\\n};\\n\\n/// EvalOptions controls component evaluation of one invariant.\\npub const EvalOptions = struct {\\n    signature: SignatureConvention = .complex,\\n};\\n\\n/// IndexRef names one symbolic index in a rendered term.\\npub const IndexRef = u32;\\n\\n/// IndexBlockId names a compact antisymmetric or ordered index block.\\npub const IndexBlockId = u32;\\n\\n/// RationalId names an exact rational coefficient in the scalar store.\\npub const RationalId = u32;\\n\\n/// GammaBlock stores one internal antisymmetrized gamma factor.\\nconst GammaBlock = struct {\\n    spinor_left: IndexRef,\\n    spinor_right: IndexRef,\\n    vector_block: IndexBlockId,\\n    rank: u8,\\n    chirality: u8,\\n    duality: DualityTag = .none,\\n};\\n\\n/// DualityTag records middle-form self-duality data.\\npub const DualityTag = enum {\\n    none,\\n    self_dual,\\n    anti_self_dual,\\n};\\n\\n/// SymbolicAtom stores one symbolic invariant contraction atom.\\npub const SymbolicAtom = union(enum) {\\n    metric_pair: struct { left: IndexRef, right: IndexRef },\\n    generalized_delta: struct { upper: IndexBlockId, lower: IndexBlockId },\\n    epsilon: struct { block: IndexBlockId },\\n    hodge_star: struct { input: IndexBlockId, output: IndexBlockId },\\n    antisymmetrizer: struct { block: IndexBlockId },\\n    named_operator: struct { operator_id: u32, first_index: u32, index_len: u16 },\\n};\\n\\n/// SymbolicTerm stores one streamed coefficient times symbolic atoms.\\npub const SymbolicTerm = struct {\\n    coefficient: RationalId,\\n    /// atoms is borrowed and valid only until the sink returns.\\n    atoms: []const SymbolicAtom,\\n};\\n\\n/// emitTerm sends one symbolic term to a caller-provided sink.\\npub fn emitTerm(sink: anytype, term: SymbolicTerm) !void {\\n    try sink.emitTerm(term);\\n}\\n\\n/// RenderedTermSpan stores a compact range of already emitted terms.\\nconst RenderedTermSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// ReductionTerm stores one coefficient of an invariant in a target basis.\\nconst ReductionTerm = struct {\\n    invariant: coupling.InvariantHandle,\\n    coefficient: RationalId,\\n};\\n"},{"id":"20260606T210001.000000","title":"Tensor Backend","tags":null,"content":"Backends are internal family-specific providers.  The public workflow should\\nnot depend on an ADE, gamma-matrix, Young-symmetrizer, or exceptional-algebra\\nimplementation detail.  A backend validates support, supplies representation\\nmetadata, decomposes binary products, constructs local projector ids, and\\nstreams concrete symbolic terms when rendering is requested.\\nconst decomposition = @import(\\"decomposition.zig\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\n/// BackendId names one registered Lie-family backend.\\npub const BackendId = u16;\\n\\n/// PrimitiveInvariantSpan names backend-owned primitive invariant descriptors.\\npub const PrimitiveInvariantSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// RenderConvention records the concrete rendering convention passed to a backend.\\npub const RenderConvention = struct {\\n    options: rendering.RenderOptions,\\n};\\n\\n/// Capabilities is the internal contract every Lie-family backend must satisfy.\\npub const Capabilities = struct {\\n    validate_algebra: *const fn (*anyopaque, symmetry.SimpleLieAlgebra) anyerror!void,\\n    irrep_metadata: *const fn (*anyopaque, store.AlgebraHandle, store.IrrepHandle) anyerror!store.IrrepMetadata,\\n    decompose_product: *const fn (*anyopaque, decomposition.ProductKey, *anyopaque) anyerror!void,\\n    local_projector: *const fn (*anyopaque, projector.ProjectorKey) anyerror!projector.ProjectorId,\\n    primitive_invariants: *const fn (*anyopaque, []const store.IrrepHandle) anyerror!PrimitiveInvariantSpan,\\n    render_projector: *const fn (*anyopaque, projector.ProjectorId, RenderConvention, *anyopaque) anyerror!void,\\n};\\n\\n/// Record stores one backend state pointer and capability table.\\npub const Record = struct {\\n    id: BackendId,\\n    state: *anyopaque,\\n    family: symmetry.LieFamily,\\n    root_conventions: root_data.AlgebraConventions,\\n    capabilities: Capabilities,\\n};\\n"},{"id":"20260606T210002.000000","title":"Tensor Decomposition","tags":null,"content":"A tensor-product decomposition is the compact statement\\n\\\\[\\n  R\\\\otimes S=\\\\bigoplus_T m_T T.\\n\\\\]\\nThis module stores that statement as flat product terms.  It does not compute\\nthe decomposition; the family backend will do that and intern the result here.\\nconst std = @import(\\"std\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ProductDecompositionHandle names a cached binary decomposition.\\npub const ProductDecompositionHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a product-decomposition handle.\\n    pub fn init(value: u32) ProductDecompositionHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ProductKey identifies a cached tensor-product decomposition.\\npub const ProductKey = struct {\\n    left: store.IrrepHandle,\\n    right: store.IrrepHandle,\\n};\\n\\n/// ProductTerm stores one channel and its multiplicity in a product.\\npub const ProductTerm = struct {\\n    irrep: store.IrrepHandle,\\n    multiplicity: u16,\\n};\\n\\n/// ProductTermSpan stores a flat range of product terms.\\npub const ProductTermSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// Store owns cached binary tensor-product decomposition records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    decompositions: std.ArrayList(ProductDecomposition) = .empty,\\n    terms: std.ArrayList(ProductTerm) = .empty,\\n    product_index: std.AutoHashMap(u64, ProductDecompositionHandle),\\n\\n    /// init constructs an empty decomposition store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{\\n            .allocator = allocator,\\n            .product_index = std.AutoHashMap(u64, ProductDecompositionHandle).init(allocator),\\n        };\\n    }\\n\\n    /// deinit releases decomposition-store memory.\\n    pub fn deinit(self: *Store) void {\\n        self.product_index.deinit();\\n        self.terms.deinit(self.allocator);\\n        self.decompositions.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// findProduct returns a cached product handle if present.\\n    pub fn findProduct(self: Store, key: ProductKey) ?ProductDecompositionHandle {\\n        return self.product_index.get(productKeyBits(key));\\n    }\\n\\n    /// internProduct stores a binary product decomposition.\\n    pub fn internProduct(self: *Store, key: ProductKey, terms: []const ProductTerm) !ProductDecompositionHandle {\\n        if (self.findProduct(key)) |handle| return handle;\\n\\n        const offset: u32 = @intCast(self.terms.items.len);\\n        try self.terms.appendSlice(self.allocator, terms);\\n        errdefer self.terms.shrinkRetainingCapacity(offset);\\n\\n        const handle = ProductDecompositionHandle.init(@intCast(self.decompositions.items.len));\\n        try self.decompositions.append(self.allocator, .{\\n            .key = key,\\n            .terms = .{ .offset = offset, .len = @intCast(terms.len) },\\n        });\\n        errdefer _ = self.decompositions.pop();\\n\\n        try self.product_index.put(productKeyBits(key), handle);\\n        return handle;\\n    }\\n\\n    /// productTerms returns the cached term span for a product handle.\\n    pub fn productTerms(self: Store, handle: ProductDecompositionHandle) ?[]const ProductTerm {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.decompositions.items.len) return null;\\n        const span = self.decompositions.items[index].terms;\\n        const start: usize = @intCast(span.offset);\\n        const end = start + span.len;\\n        return self.terms.items[start..end];\\n    }\\n};\\n\\n/// ProductDecomposition stores a flat range of product terms.\\nconst ProductDecomposition = struct {\\n    key: ProductKey,\\n    terms: ProductTermSpan,\\n};\\n\\n/// SingletCountRequest records a generic invariant-counting problem.\\nconst SingletCountRequest = struct {\\n    algebra: store.AlgebraHandle,\\n    factors: []const store.IrrepHandle,\\n    target: coupling.TargetIrrep = .singlet,\\n};\\n\\nfn productKeyBits(key: ProductKey) u64 {\\n    return (@as(u64, key.left.value) << 32) | @as(u64, key.right.value);\\n}\\n"},{"id":"20260606T210003.000000","title":"Tensor Projector","tags":null,"content":"A projector is an idempotent intertwiner.  It can select a channel in\\n\\\\(R\\\\otimes S\\\\), impose a boundary realization such as a gamma-traceless\\nsubspace, or change between invariant-basis conventions.  This module stores\\nprojector identities and derivation labels; it does not construct formulas.\\nconst std = @import(\\"std\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ProjectorId names one local projector or Clebsch-Gordan kernel.\\npub const ProjectorId = u32;\\n\\n/// ProjectorConvention records configurable projector-rendering choices.\\npub const ProjectorConvention = struct {\\n    render_mode: rendering.ProjectorRenderMode = .named,\\n    signature: rendering.SignatureConvention = .complex,\\n    normalization_id: u32 = 0,\\n};\\n\\n/// ProjectorRole records what mathematical object a projector selects.\\npub const ProjectorRole = union(enum) {\\n    product_channel: ProductChannelKey,\\n    boundary_realization: realization.RealizationHandle,\\n    basis_change: BasisChangeKey,\\n};\\n\\n/// ProductChannelKey identifies one target channel inside a binary product.\\npub const ProductChannelKey = struct {\\n    left: store.IrrepHandle,\\n    right: store.IrrepHandle,\\n    target: store.ChannelHandle,\\n};\\n\\n/// BasisChangeKey identifies a change of invariant-basis convention.\\npub const BasisChangeKey = struct {\\n    source_basis: u32,\\n    target_basis: u32,\\n};\\n\\n/// Store owns projector identities and compact emitted-term spans.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    records: std.ArrayList(ProjectorRecord) = .empty,\\n\\n    /// init constructs an empty projector store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases projector-store memory.\\n    pub fn deinit(self: *Store) void {\\n        self.records.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internProjector returns a stable id for a projector identity.\\n    pub fn internProjector(self: *Store, key: ProjectorKey, kind: ProjectorKind) !ProjectorId {\\n        for (self.records.items, 0..) |record, index| {\\n            if (projectorKeyEql(record.key, key)) return @intCast(index);\\n        }\\n        try self.records.append(self.allocator, .{\\n            .key = key,\\n            .kind = kind,\\n            .term_offset = 0,\\n            .term_len = 0,\\n        });\\n        return @intCast(self.records.items.len - 1);\\n    }\\n};\\n\\n/// ProjectorKey identifies one projector in one convention.\\npub const ProjectorKey = struct {\\n    role: ProjectorRole,\\n    convention: ProjectorConvention,\\n};\\n\\n/// ProjectorKind records how a local projector was derived.\\npub const ProjectorKind = enum {\\n    identity,\\n    quadratic_casimir,\\n    higher_casimir_signature,\\n    local_idempotent_split,\\n    highest_weight_solver,\\n    backend_specific_verified,\\n};\\n\\n/// ProjectorRecord stores one local kernel and its derivation kind.\\nconst ProjectorRecord = struct {\\n    key: ProjectorKey,\\n    kind: ProjectorKind,\\n    term_offset: u32,\\n    term_len: u32,\\n};\\n\\n/// ProjectorAudit stores local splitting diagnostics for feasibility studies.\\nconst ProjectorAudit = struct {\\n    product: ProductChannelKey,\\n    channel_count: u32,\\n    quadratic_collision_count: u32,\\n    true_multiplicity_count: u32,\\n    residual_block_max: u32,\\n};\\n\\nfn projectorKeyEql(left: ProjectorKey, right: ProjectorKey) bool {\\n    return projectorRoleEql(left.role, right.role) and\\n        left.convention.render_mode == right.convention.render_mode and\\n        left.convention.signature == right.convention.signature and\\n        left.convention.normalization_id == right.convention.normalization_id;\\n}\\n\\nfn projectorRoleEql(left: ProjectorRole, right: ProjectorRole) bool {\\n    return switch (left) {\\n        .product_channel => |left_key| switch (right) {\\n            .product_channel => |right_key| left_key.left.value == right_key.left.value and\\n                left_key.right.value == right_key.right.value and\\n                left_key.target.value == right_key.target.value,\\n            else => false,\\n        },\\n        .boundary_realization => |left_handle| switch (right) {\\n            .boundary_realization => |right_handle| left_handle.value == right_handle.value,\\n            else => false,\\n        },\\n        .basis_change => |left_key| switch (right) {\\n            .basis_change => |right_key| left_key.source_basis == right_key.source_basis and left_key.target_basis == right_key.target_basis,\\n            else => false,\\n        },\\n    };\\n}\\n"},{"id":"20260606T211001.000000","title":"Tensor Context","tags":null,"content":"The context module is the procedural boundary of tensor-code.  Public callers\\nget an opaque state pointer and methods; the stores remain in a private\\nimplementation record so callers cannot bypass validation.\\nconst std = @import(\\"std\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst decomposition = @import(\\"decomposition.zig\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ContextId names an initialized tensor-code context or preset.\\npub const ContextId = struct {\\n    value: u32,\\n\\n    /// init constructs a context id from a store index.\\n    pub fn init(value: u32) ContextId {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ContextOptions configures algebra and rendering defaults.\\npub const ContextOptions = struct {\\n    algebra_conventions: root_data.AlgebraConventions = .{},\\n    default_signature: rendering.SignatureConvention = .complex,\\n};\\nRegistration validates cross-store handles before interning.  Metadata and\\nduals are queried through methods, not by reaching into stores.\\n/// Context is an opaque handle to tensor-code stores and caches.\\npub const Context = struct {\\n    state: *anyopaque,\\n    allocator: std.mem.Allocator,\\n\\n    /// init constructs an empty tensor-code context.\\n    pub fn init(allocator: std.mem.Allocator) !Context {\\n        return Context.initWithOptions(allocator, .{});\\n    }\\n\\n    /// initWithOptions constructs a context with explicit conventions.\\n    pub fn initWithOptions(allocator: std.mem.Allocator, options: ContextOptions) !Context {\\n        const state = try allocator.create(Impl);\\n        errdefer allocator.destroy(state);\\n        state.* = Impl.init(allocator, options);\\n        return .{\\n            .state = state,\\n            .allocator = allocator,\\n        };\\n    }\\n\\n    /// deinit releases memory owned by this context.\\n    pub fn deinit(self: *Context) void {\\n        const state = self.impl();\\n        state.deinit();\\n        self.allocator.destroy(state);\\n        self.* = undefined;\\n    }\\n\\n    /// registerAlgebra interns a Lie algebra and returns its handle.\\n    pub fn registerAlgebra(self: *Context, spec: store.AlgebraSpec) !store.AlgebraHandle {\\n        return self.impl().representations.internAlgebra(spec);\\n    }\\n\\n    /// registerIrrep interns an abstract irreducible representation.\\n    pub fn registerIrrep(self: *Context, algebra: store.AlgebraHandle, spec: store.IrrepSpec) !store.IrrepHandle {\\n        return self.impl().representations.internIrrep(algebra, spec);\\n    }\\n\\n    /// dualIrrep returns the registered dual representation.\\n    pub fn dualIrrep(self: *Context, irrep: store.IrrepHandle) !store.IrrepHandle {\\n        return self.impl().representations.dualIrrep(irrep);\\n    }\\n\\n    /// irrepMetadata returns derived metadata for a registered irrep.\\n    pub fn irrepMetadata(self: Context, irrep: store.IrrepHandle) ?store.IrrepMetadata {\\n        return self.impl().representations.irrepMetadata(irrep);\\n    }\\n\\n    /// registerRealization interns a concrete index realization of an irrep.\\n    pub fn registerRealization(self: *Context, spec: realization.RealizationSpec) !realization.RealizationHandle {\\n        try self.validateRealizationSpec(spec);\\n        return self.impl().realizations.internRealization(spec);\\n    }\\n\\n    /// invariantBasis generates or retrieves a compact invariant basis.\\n    pub fn invariantBasis(self: *Context, request: coupling.InvariantBasisRequest) !coupling.BasisHandle {\\n        try self.validateInvariantBasisRequest(request);\\n        return self.impl().couplings.internBasisRequest(request);\\n    }\\n\\n    /// renderInvariant streams one invariant in the requested convention.\\n    pub fn renderInvariant(self: *Context, basis: coupling.BasisHandle, invariant: coupling.InvariantHandle, options: rendering.RenderOptions, sink: anytype) !void {\\n        _ = self;\\n        _ = basis;\\n        _ = invariant;\\n        _ = options;\\n        _ = sink;\\n        return error.NotImplemented;\\n    }\\n\\n    /// evaluateInvariant streams component-evaluation data for one invariant.\\n    pub fn evaluateInvariant(self: *Context, invariant: coupling.InvariantHandle, options: rendering.EvalOptions, sink: anytype) !void {\\n        _ = self;\\n        _ = invariant;\\n        _ = options;\\n        _ = sink;\\n        return error.NotImplemented;\\n    }\\n\\n    fn validateRealizationSpec(self: Context, spec: realization.RealizationSpec) !void {\\n        const target_algebra = self.impl().representations.irrepAlgebra(spec.channel.irrep) orelse return error.UnknownIrrep;\\n        for (spec.ambient) |ambient| {\\n            const ambient_algebra = self.impl().representations.irrepAlgebra(ambient) orelse return error.UnknownIrrep;\\n            if (ambient_algebra.value != target_algebra.value) return error.MixedRealizationAlgebras;\\n        }\\n    }\\n\\n    fn validateInvariantBasisRequest(self: Context, request: coupling.InvariantBasisRequest) !void {\\n        if (!self.impl().representations.containsAlgebra(request.algebra)) return error.UnknownAlgebra;\\n        for (request.external_legs) |leg| {\\n            const leg_algebra = try self.externalLegAlgebra(leg);\\n            if (leg_algebra) |algebra| {\\n                if (algebra.value != request.algebra.value) return error.MixedInvariantAlgebras;\\n            }\\n        }\\n    }\\n\\n    fn externalLegAlgebra(self: Context, leg: realization.ExternalLeg) !?store.AlgebraHandle {\\n        return switch (leg.realization) {\\n            .primitive_irrep => |irrep| self.impl().representations.irrepAlgebra(irrep) orelse return error.UnknownIrrep,\\n            .handle => |handle| {\\n                const irrep = self.impl().realizations.targetIrrep(handle) orelse return error.UnknownRealization;\\n                return self.impl().representations.irrepAlgebra(irrep) orelse return error.UnknownIrrep;\\n            },\\n            .registered_name => null,\\n        };\\n    }\\n\\n    fn impl(self: Context) *Impl {\\n        return @ptrCast(@alignCast(self.state));\\n    }\\n};\\nThe private implementation record owns the flat stores.  It is not imported by\\nthe root API.\\nconst Impl = struct {\\n    id: ContextId,\\n    allocator: std.mem.Allocator,\\n    options: ContextOptions,\\n    representations: store.Store,\\n    realizations: realization.Store,\\n    decompositions: decomposition.Store,\\n    projectors: projector.Store,\\n    couplings: coupling.Store,\\n\\n    fn init(allocator: std.mem.Allocator, options: ContextOptions) Impl {\\n        return .{\\n            .id = ContextId.init(0),\\n            .allocator = allocator,\\n            .options = options,\\n            .representations = store.Store.init(allocator, options.algebra_conventions),\\n            .realizations = realization.Store.init(allocator),\\n            .decompositions = decomposition.Store.init(allocator),\\n            .projectors = projector.Store.init(allocator),\\n            .couplings = coupling.Store.init(allocator),\\n        };\\n    }\\n\\n    fn deinit(self: *Impl) void {\\n        self.couplings.deinit();\\n        self.projectors.deinit();\\n        self.decompositions.deinit();\\n        self.realizations.deinit();\\n        self.representations.deinit();\\n        self.* = Impl.init(self.allocator, self.options);\\n    }\\n};\\n"},{"id":"20260606T230001.000000","title":"Tensor Root Data","tags":null,"content":"Root data is the finite combinatorial description of a semisimple Lie algebra:\\nsimple roots, the Cartan matrix, Dynkin labels, and the positive roots.  For a\\nhighest weight \\\\(\\\\lambda\\\\), the Dynkin labels are the integers\\n\\\\(\\\\lambda_i=\\\\langle \\\\lambda,\\\\alpha_i^\\\\vee\\\\rangle\\\\).  Weyl\'s dimension formula\\nuses the positive roots:\\n\\\\[\\n  \\\\dim V_\\\\lambda =\\n  \\\\prod_{\\\\alpha>0}\\n  \\\\frac{\\\\langle \\\\lambda+\\\\rho,\\\\alpha^\\\\vee\\\\rangle}\\n       {\\\\langle \\\\rho,\\\\alpha^\\\\vee\\\\rangle}.\\n\\\\]\\nFor ADE all roots have the same length.  For \\\\(B_n\\\\), used by odd orthogonal\\nalgebras, the short-root length is handled by exact rational root scales.  The\\nquadratic Casimir is computed in the configurable normalization\\n\\\\[\\n  C_2(\\\\lambda)=\\\\frac{(\\\\lambda,\\\\lambda+2\\\\rho)}{2}\\n\\\\]\\ntimes quadratic_casimir_scale.\\nconst std = @import(\\"std\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\nconst max_rank = 256;\\n\\n/// Rational stores a normalized exact rational number.\\npub const Rational = struct {\\n    numerator: i128 = 0,\\n    denominator: i128 = 1,\\n\\n    /// init constructs and normalizes a rational number.\\n    pub fn init(numerator: i128, denominator: i128) !Rational {\\n        if (denominator == 0) return error.ZeroDenominator;\\n        var out = Rational{ .numerator = numerator, .denominator = denominator };\\n        out.normalize();\\n        return out;\\n    }\\n\\n    /// one returns the rational number 1.\\n    pub fn one() Rational {\\n        return .{ .numerator = 1, .denominator = 1 };\\n    }\\n\\n    fn normalize(self: *Rational) void {\\n        if (self.numerator == 0) {\\n            self.denominator = 1;\\n            return;\\n        }\\n        if (self.denominator < 0) {\\n            self.numerator = -self.numerator;\\n            self.denominator = -self.denominator;\\n        }\\n        const divisor = gcdI128(absI128(self.numerator), absI128(self.denominator));\\n        self.numerator = @divExact(self.numerator, divisor);\\n        self.denominator = @divExact(self.denominator, divisor);\\n    }\\n};\\n\\n/// AlgebraConventions selects configurable exact-algebra normalizations.\\npub const AlgebraConventions = struct {\\n    quadratic_casimir_scale: Rational = .{ .numerator = 1, .denominator = 1 },\\n};\\n\\n/// RootDatum stores a simple algebra with its normalization conventions.\\npub const RootDatum = struct {\\n    algebra: symmetry.SimpleLieAlgebra,\\n    conventions: AlgebraConventions,\\n\\n    /// init validates a supported algebra and returns its root datum.\\n    pub fn init(algebra: symmetry.SimpleLieAlgebra, conventions: AlgebraConventions) !RootDatum {\\n        try validateSimple(algebra);\\n        return .{ .algebra = algebra, .conventions = conventions };\\n    }\\n\\n    /// rank returns the number of simple roots.\\n    pub fn rank(self: RootDatum) u8 {\\n        return self.algebra.rank;\\n    }\\n\\n    /// cartan returns one Cartan matrix entry.\\n    pub fn cartan(self: RootDatum, row: usize, col: usize) !i16 {\\n        return cartanEntry(self.algebra, row, col);\\n    }\\n};\\n\\n/// validateSimple rejects unsupported or rank-inconsistent simple algebras.\\npub fn validateSimple(algebra: symmetry.SimpleLieAlgebra) !void {\\n    if (algebra.rank > max_rank) return error.RankTooLarge;\\n    switch (algebra.family) {\\n        .a => if (algebra.rank < 1) return error.InvalidRank,\\n        .b => if (algebra.rank < 2) return error.InvalidRank,\\n        .d => if (algebra.rank < 4) return error.InvalidRank,\\n        .e6 => if (algebra.rank != 6) return error.InvalidRank,\\n        .e7 => if (algebra.rank != 7) return error.InvalidRank,\\n        .e8 => if (algebra.rank != 8) return error.InvalidRank,\\n        else => return error.UnsupportedAlgebraFamily,\\n    }\\n}\\n\\n/// dualDynkin returns the highest weight of the dual irrep.\\npub fn dualDynkin(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16) ![]i16 {\\n    try validateDynkinLabel(algebra, label);\\n    const out = try allocator.dupe(i16, label);\\n    switch (algebra.family) {\\n        .a => {\\n            for (0..label.len) |i| out[i] = label[label.len - 1 - i];\\n        },\\n        .b => {},\\n        .d => {\\n            if ((algebra.rank % 2) == 1) {\\n                const left = algebra.rank - 2;\\n                const right = algebra.rank - 1;\\n                out[left] = label[right];\\n                out[right] = label[left];\\n            }\\n        },\\n        .e6 => {\\n            out[0] = label[4];\\n            out[1] = label[3];\\n            out[3] = label[1];\\n            out[4] = label[0];\\n        },\\n        .e7, .e8 => {},\\n        else => unreachable,\\n    }\\n    return out;\\n}\\n\\n/// dimension computes the Weyl dimension for a supported highest weight.\\npub fn dimension(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16) !u128 {\\n    try validateDynkinLabel(algebra, label);\\n    var roots = try PositiveRoots.init(allocator, algebra);\\n    defer roots.deinit(allocator);\\n\\n    var product = Rational.one();\\n    for (0..roots.len) |root_index| {\\n        const root = roots.root(root_index);\\n        const top = try rootCorootPairing(algebra, root, label);\\n        const bottom = try rootCorootPairing(algebra, root, null);\\n        product = try mul(product, try div(top, bottom));\\n    }\\n    return rationalToU128(product);\\n}\\n\\n/// quadraticCasimir computes \\\\(C_2(\\\\lambda)=(\\\\lambda,\\\\lambda+2\\\\rho)/2\\\\).\\npub fn quadraticCasimir(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16, conventions: AlgebraConventions) !Rational {\\n    try validateDynkinLabel(algebra, label);\\n    const rank_value: usize = algebra.rank;\\n    const inverse = try inverseCartan(allocator, algebra);\\n    defer allocator.free(inverse);\\n\\n    var total = Rational{};\\n    for (0..rank_value) |i| {\\n        for (0..rank_value) |j| {\\n            const weight = @as(i128, label[i]) * @as(i128, label[j] + 2);\\n            if (weight == 0) continue;\\n            const gram = try mul(simpleRootScale(algebra, i), inverse[i * rank_value + j]);\\n            total = try add(total, try mulInt(gram, weight));\\n        }\\n    }\\n    total = try divInt(total, 2);\\n    return try mul(total, conventions.quadratic_casimir_scale);\\n}\\n\\nfn validateDynkinLabel(algebra: symmetry.SimpleLieAlgebra, label: []const i16) !void {\\n    try validateSimple(algebra);\\n    if (label.len != algebra.rank) return error.InvalidDynkinRank;\\n    for (label) |entry| {\\n        if (entry < 0) return error.InvalidDynkinLabel;\\n    }\\n}\\n\\nfn cartanEntry(algebra: symmetry.SimpleLieAlgebra, row: usize, col: usize) !i16 {\\n    try validateSimple(algebra);\\n    const rank_value: usize = algebra.rank;\\n    if (row >= rank_value or col >= rank_value) return error.CartanIndexOutOfRange;\\n    if (row == col) return 2;\\n    return switch (algebra.family) {\\n        .a => if (adjacent(row, col)) -1 else 0,\\n        .b => bCartan(rank_value, row, col),\\n        .d => if (dConnected(rank_value, row, col)) -1 else 0,\\n        .e6, .e7, .e8 => if (eConnected(algebra.rank, row, col)) -1 else 0,\\n        else => unreachable,\\n    };\\n}\\n\\nfn bCartan(rank_value: usize, row: usize, col: usize) i16 {\\n    if (!adjacent(row, col)) return 0;\\n    if (row == rank_value - 2 and col == rank_value - 1) return -1;\\n    if (row == rank_value - 1 and col == rank_value - 2) return -2;\\n    return -1;\\n}\\n\\nfn adjacent(row: usize, col: usize) bool {\\n    return row + 1 == col or col + 1 == row;\\n}\\n\\nfn dConnected(rank_value: usize, row: usize, col: usize) bool {\\n    if (rank_value < 4) return false;\\n    const branch = rank_value - 3;\\n    if (row <= branch and col <= branch) return adjacent(row, col);\\n    return (row == branch and (col == rank_value - 2 or col == rank_value - 1)) or\\n        (col == branch and (row == rank_value - 2 or row == rank_value - 1));\\n}\\n\\nfn eConnected(rank_value: usize, row: usize, col: usize) bool {\\n    if (edge(row, col, 0, 1) or edge(row, col, 1, 2) or edge(row, col, 2, 3) or edge(row, col, 3, 4)) return true;\\n    if (rank_value >= 7 and edge(row, col, 4, 5)) return true;\\n    if (rank_value >= 8 and edge(row, col, 5, 6)) return true;\\n    return edge(row, col, 2, rank_value - 1);\\n}\\n\\nfn edge(row: usize, col: usize, left: usize, right: usize) bool {\\n    return (row == left and col == right) or (row == right and col == left);\\n}\\n\\nconst PositiveRoots = struct {\\n    rank: usize,\\n    roots: []u8,\\n    len: usize = 0,\\n\\n    fn init(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra) !PositiveRoots {\\n        const rank_value: usize = algebra.rank;\\n        const capacity = positiveRootCapacity(algebra);\\n        var out = PositiveRoots{\\n            .rank = rank_value,\\n            .roots = try allocator.alloc(u8, capacity * rank_value),\\n        };\\n        @memset(out.roots, 0);\\n        errdefer allocator.free(out.roots);\\n\\n        if (algebra.family == .b) {\\n            try out.addBPositiveRoots();\\n            return out;\\n        }\\n\\n        for (0..rank_value) |i| {\\n            var simple = [_]u8{0} ** max_rank;\\n            simple[i] = 1;\\n            try out.add(simple[0..rank_value]);\\n        }\\n\\n        var cursor: usize = 0;\\n        while (cursor < out.len) : (cursor += 1) {\\n            const current = out.root(cursor);\\n            for (0..rank_value) |i| {\\n                if (pairing(algebra, current, i) >= 0) continue;\\n                var candidate = [_]u8{0} ** max_rank;\\n                @memcpy(candidate[0..rank_value], current);\\n                candidate[i] += 1;\\n                try out.add(candidate[0..rank_value]);\\n            }\\n        }\\n        return out;\\n    }\\n\\n    fn addBPositiveRoots(self: *PositiveRoots) !void {\\n        for (0..self.rank) |i| {\\n            var short = [_]u8{0} ** max_rank;\\n            for (i..self.rank) |k| short[k] = 1;\\n            try self.add(short[0..self.rank]);\\n\\n            for (i + 1..self.rank) |j| {\\n                var difference = [_]u8{0} ** max_rank;\\n                for (i..j) |k| difference[k] = 1;\\n                try self.add(difference[0..self.rank]);\\n\\n                var sum = [_]u8{0} ** max_rank;\\n                for (i..j) |k| sum[k] = 1;\\n                for (j..self.rank) |k| sum[k] = 2;\\n                try self.add(sum[0..self.rank]);\\n            }\\n        }\\n    }\\n\\n    fn deinit(self: *PositiveRoots, allocator: std.mem.Allocator) void {\\n        allocator.free(self.roots);\\n        self.* = undefined;\\n    }\\n\\n    fn root(self: PositiveRoots, index: usize) []const u8 {\\n        const start = index * self.rank;\\n        return self.roots[start .. start + self.rank];\\n    }\\n\\n    fn add(self: *PositiveRoots, root_value: []const u8) !void {\\n        if (self.contains(root_value)) return;\\n        if (self.len * self.rank >= self.roots.len) return error.PositiveRootOverflow;\\n        const start = self.len * self.rank;\\n        @memcpy(self.roots[start .. start + self.rank], root_value);\\n        self.len += 1;\\n    }\\n\\n    fn contains(self: PositiveRoots, root_value: []const u8) bool {\\n        for (0..self.len) |i| {\\n            if (std.mem.eql(u8, self.root(i), root_value)) return true;\\n        }\\n        return false;\\n    }\\n};\\n\\nfn positiveRootCapacity(algebra: symmetry.SimpleLieAlgebra) usize {\\n    const rank_value: usize = algebra.rank;\\n    return switch (algebra.family) {\\n        .a => rank_value * (rank_value + 1) / 2,\\n        .b => rank_value * rank_value,\\n        .d => rank_value * (rank_value - 1),\\n        .e6 => 36,\\n        .e7 => 63,\\n        .e8 => 120,\\n        else => unreachable,\\n    };\\n}\\n\\nfn pairing(algebra: symmetry.SimpleLieAlgebra, root: []const u8, coroot_index: usize) i16 {\\n    var out: i16 = 0;\\n    for (root, 0..) |coefficient, j| {\\n        const entry = cartanEntry(algebra, coroot_index, j) catch unreachable;\\n        out += @as(i16, @intCast(coefficient)) * entry;\\n    }\\n    return out;\\n}\\n\\nfn inverseCartan(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra) ![]Rational {\\n    const rank_value: usize = algebra.rank;\\n    const width = rank_value * 2;\\n    var matrix = try allocator.alloc(Rational, rank_value * width);\\n    errdefer allocator.free(matrix);\\n\\n    for (0..rank_value) |row| {\\n        for (0..width) |col| {\\n            matrix[row * width + col] = Rational{};\\n        }\\n        for (0..rank_value) |col| {\\n            matrix[row * width + col] = try Rational.init(try cartanEntry(algebra, row, col), 1);\\n        }\\n        matrix[row * width + rank_value + row] = Rational.one();\\n    }\\n\\n    for (0..rank_value) |col| {\\n        var pivot = col;\\n        while (pivot < rank_value and matrix[pivot * width + col].numerator == 0) : (pivot += 1) {}\\n        if (pivot == rank_value) return error.SingularCartanMatrix;\\n        if (pivot != col) {\\n            for (0..width) |j| {\\n                std.mem.swap(Rational, &matrix[col * width + j], &matrix[pivot * width + j]);\\n            }\\n        }\\n\\n        const pivot_value = matrix[col * width + col];\\n        for (0..width) |j| matrix[col * width + j] = try div(matrix[col * width + j], pivot_value);\\n\\n        for (0..rank_value) |row| {\\n            if (row == col) continue;\\n            const factor = matrix[row * width + col];\\n            if (factor.numerator == 0) continue;\\n            for (0..width) |j| {\\n                matrix[row * width + j] = try sub(matrix[row * width + j], try mul(factor, matrix[col * width + j]));\\n            }\\n        }\\n    }\\n\\n    var out = try allocator.alloc(Rational, rank_value * rank_value);\\n    errdefer allocator.free(out);\\n    for (0..rank_value) |row| {\\n        for (0..rank_value) |col| {\\n            out[row * rank_value + col] = matrix[row * width + rank_value + col];\\n        }\\n    }\\n    allocator.free(matrix);\\n    return out;\\n}\\n\\nfn rootCorootPairing(algebra: symmetry.SimpleLieAlgebra, root: []const u8, label: ?[]const i16) !Rational {\\n    var inner = Rational{};\\n    for (root, 0..) |coefficient, i| {\\n        const dynkin = if (label) |values| values[i] + 1 else 1;\\n        inner = try add(inner, try mulInt(simpleRootScale(algebra, i), @as(i128, coefficient) * @as(i128, dynkin)));\\n    }\\n    return try div(try mulInt(inner, 2), try rootLengthSquared(algebra, root));\\n}\\n\\nfn rootLengthSquared(algebra: symmetry.SimpleLieAlgebra, root: []const u8) !Rational {\\n    var out = Rational{};\\n    for (root, 0..) |left_coefficient, i| {\\n        if (left_coefficient == 0) continue;\\n        for (root, 0..) |right_coefficient, j| {\\n            if (right_coefficient == 0) continue;\\n            const entry = try cartanEntry(algebra, i, j);\\n            const factor = @as(i128, left_coefficient) * @as(i128, right_coefficient) * @as(i128, entry);\\n            out = try add(out, try mulInt(simpleRootScale(algebra, i), factor));\\n        }\\n    }\\n    return out;\\n}\\n\\nfn simpleRootScale(algebra: symmetry.SimpleLieAlgebra, index: usize) Rational {\\n    return switch (algebra.family) {\\n        .b => if (index == algebra.rank - 1)\\n            .{ .numerator = 1, .denominator = 2 }\\n        else\\n            Rational.one(),\\n        else => Rational.one(),\\n    };\\n}\\n\\nfn rationalToU128(value: Rational) !u128 {\\n    if (value.denominator != 1) return error.NonIntegralDimension;\\n    if (value.numerator < 0) return error.NegativeDimension;\\n    return std.math.cast(u128, value.numerator) orelse error.DimensionOverflow;\\n}\\n\\nfn add(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.add(i128, try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, right.numerator, left.denominator)), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn sub(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.sub(i128, try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, right.numerator, left.denominator)), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn mul(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.mul(i128, left.numerator, right.numerator), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn div(left: Rational, right: Rational) !Rational {\\n    if (right.numerator == 0) return error.DivisionByZero;\\n    return Rational.init(try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, left.denominator, right.numerator));\\n}\\n\\nfn mulInt(value: Rational, factor: i128) !Rational {\\n    return Rational.init(try std.math.mul(i128, value.numerator, factor), value.denominator);\\n}\\n\\nfn divInt(value: Rational, divisor: i128) !Rational {\\n    return Rational.init(value.numerator, try std.math.mul(i128, value.denominator, divisor));\\n}\\n\\nfn absI128(value: i128) i128 {\\n    return if (value < 0) -value else value;\\n}\\n\\nfn gcdI128(left: i128, right: i128) i128 {\\n    var a = left;\\n    var b = right;\\n    while (b != 0) {\\n        const next = @mod(a, b);\\n        a = b;\\n        b = next;\\n    }\\n    return a;\\n}\\n\\n"},{"id":"20260607T130000.000000","title":"Correlator Performance Benchmarks","tags":null,"content":"This benchmark times the streamed Wick correlator on representative free-boson,\\nfree-fermion, ghost, and exponential workloads.  It reports medians, not best runs.  The\\nevent sink counts the fully expanded stream; the branch sink counts the compact\\nbranch path and its stack state size.\\nThe event lines describe the expanded primitive event stream: a Wick term\\nboundary, scalar/coordinate/tensor/action factors, residual operator references,\\nand the zero-mode base-case factors.  The branch lines describe one compact\\naccumulated branch object per Wick/base-case branch, while zero modes remain\\nseparate events.  fallback_terms is nonzero only when a case cannot use the\\ncompact branch path.\\nThe runner enforces a 5% median regression gate for every event and branch\\ncase against the recorded baselines below.  Cases with very small baselines run\\nmultiple correlators per sample and report the per-call median, keeping the\\nspeed gate strict while reducing timer noise.  Exponential and mixed cases also\\ncheck primitive-term, tensor-factor, residual, zero-mode, and base-case counts\\nso semantic drift fails the benchmark instead of only changing printed numbers.\\nconst std = @import(\\"std\\");\\nconst cft = @import(\\"cft-code\\");\\n\\nconst max_sample_count = 101;\\n\\nconst MedianGate = struct {\\n    baseline_ns: u128,\\n    limit_num: u128,\\n    limit_den: u128,\\n};\\n\\nconst ExpectedCounts = struct {\\n    primitive_terms: usize,\\n    tensors: usize,\\n    residuals: usize,\\n    zero_modes: usize,\\n    base_cases: usize,\\n};\\n\\nfn medianGate(comptime baseline_ns: u128) MedianGate {\\n    return .{ .baseline_ns = baseline_ns, .limit_num = 21, .limit_den = 20 };\\n}\\n\\nfn measurementBatchCount(baseline_ns: u128) usize {\\n    if (baseline_ns < 20_000) return 32;\\n    if (baseline_ns < 50_000) return 8;\\n    return 1;\\n}\\n\\nfn nowNs() u128 {\\n    var ts: std.os.linux.timespec = undefined;\\n    _ = std.os.linux.clock_gettime(.MONOTONIC, &ts);\\n    return @as(u128, @intCast(ts.sec)) * std.time.ns_per_s + @as(u128, @intCast(ts.nsec));\\n}\\n\\nconst EventSink = struct {\\n    wick_terms: usize = 0,\\n    scalars: usize = 0,\\n    coordinates: usize = 0,\\n    tensors: usize = 0,\\n    actions: usize = 0,\\n    residuals: usize = 0,\\n    zero_modes: usize = 0,\\n    base_cases: usize = 0,\\n\\n    pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n        self.wick_terms += 1;\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), _: anytype) !void {\\n        self.scalars += 1;\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), _: anytype) !void {\\n        self.coordinates += 1;\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), _: anytype) !void {\\n        self.tensors += 1;\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        self.actions += 1;\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), _: anytype) !void {\\n        self.residuals += 1;\\n    }\\n\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n    pub fn emitZeroModeFactor(self: *@This(), _: anytype) !void {\\n        self.zero_modes += 1;\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.base_cases += 1;\\n    }\\n};\\n\\nconst BranchSink = struct {\\n    branch_terms: usize = 0,\\n    fallback_terms: usize = 0,\\n    primitive_terms: usize = 0,\\n    scalars: usize = 0,\\n    coordinates: usize = 0,\\n    tensors: usize = 0,\\n    actions: usize = 0,\\n    residuals: usize = 0,\\n    zero_modes: usize = 0,\\n    base_cases: usize = 0,\\n    branch_state_bytes: usize = 0,\\n\\n    pub fn emitWickBranchTerm(self: *@This(), branch: anytype) !void {\\n        self.branch_terms += 1;\\n        self.primitive_terms += branch.primitiveTermCount();\\n        self.scalars += branch.scalarFactorCount();\\n        self.coordinates += branch.coordinateFactorCount();\\n        self.tensors += branch.tensorFactorCount();\\n        self.actions += branch.actionFactorCount();\\n        self.residuals += branch.residualFactorCount();\\n        const bytes = branchStateBytes(branch);\\n        if (bytes > self.branch_state_bytes) self.branch_state_bytes = bytes;\\n    }\\n\\n    pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n        self.fallback_terms += 1;\\n        self.primitive_terms += 1;\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), _: anytype) !void {\\n        self.scalars += 1;\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), _: anytype) !void {\\n        self.coordinates += 1;\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), _: anytype) !void {\\n        self.tensors += 1;\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        self.actions += 1;\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), _: anytype) !void {\\n        self.residuals += 1;\\n    }\\n\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n    pub fn emitZeroModeFactor(self: *@This(), _: anytype) !void {\\n        self.zero_modes += 1;\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.base_cases += 1;\\n    }\\n};\\n\\nfn branchStateBytes(branch: anytype) usize {\\n    return switch (@typeInfo(@TypeOf(branch))) {\\n        .pointer => |ptr| @sizeOf(ptr.child),\\n        else => @sizeOf(@TypeOf(branch)),\\n    };\\n}\\n\\nfn median(samples: []u128) u128 {\\n    std.mem.sort(u128, samples, {}, struct {\\n        fn lessThan(_: void, left: u128, right: u128) bool {\\n            return left < right;\\n        }\\n    }.lessThan);\\n    return samples[samples.len / 2];\\n}\\n\\nfn requireSampleCount(comptime sample_count: usize) void {\\n    if (sample_count == 0 or sample_count > max_sample_count or (sample_count & 1) == 0) {\\n        @compileError(\\"benchmark sample counts must be odd and <= max_sample_count\\");\\n    }\\n}\\n\\nfn checkEventCounts(comptime name: []const u8, result: EventSink, counts: ExpectedCounts, batch_count: usize) !void {\\n    if (result.wick_terms != counts.primitive_terms * batch_count or\\n        result.tensors != counts.tensors * batch_count or\\n        result.residuals != counts.residuals * batch_count or\\n        result.zero_modes != counts.zero_modes * batch_count or\\n        result.base_cases != counts.base_cases * batch_count)\\n    {\\n        std.debug.print(\\n            \\"event count regression case={s} primitive={} tensors={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n            .{ name, result.wick_terms, result.tensors, result.residuals, result.zero_modes, result.base_cases },\\n        );\\n        return error.BenchmarkCountRegression;\\n    }\\n}\\n\\nfn checkBranchCounts(comptime name: []const u8, result: BranchSink, counts: ExpectedCounts, batch_count: usize) !void {\\n    if (result.primitive_terms != counts.primitive_terms * batch_count or\\n        result.tensors != counts.tensors * batch_count or\\n        result.residuals != counts.residuals * batch_count or\\n        result.zero_modes != counts.zero_modes * batch_count or\\n        result.base_cases != counts.base_cases * batch_count)\\n    {\\n        std.debug.print(\\n            \\"branch count regression case={s} primitive={} tensors={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n            .{ name, result.primitive_terms, result.tensors, result.residuals, result.zero_modes, result.base_cases },\\n        );\\n        return error.BenchmarkCountRegression;\\n    }\\n}\\n\\nfn checkMedianGate(comptime form: []const u8, comptime name: []const u8, median_ns: u128, gate: MedianGate) !void {\\n    if (median_ns * gate.limit_den > gate.baseline_ns * gate.limit_num) {\\n        std.debug.print(\\n            \\"{s} speed regression case={s} median_ns={} baseline_ns={} limit={}/{}\\\\n\\",\\n            .{ form, name, median_ns, gate.baseline_ns, gate.limit_num, gate.limit_den },\\n        );\\n        return error.BenchmarkSpeedRegression;\\n    }\\n}\\n\\nfn benchEvent(comptime name: []const u8, comptime sample_count: usize, theory: anytype, config: anytype, ops: anytype, gate: MedianGate, expected: ExpectedCounts) !void {\\n    comptime requireSampleCount(sample_count);\\n\\n    const batch_count = measurementBatchCount(gate.baseline_ns);\\n    var samples: [max_sample_count]u128 = undefined;\\n    var result = EventSink{};\\n    for (samples[0..sample_count]) |*sample| {\\n        var sink = EventSink{};\\n        const start = nowNs();\\n        for (0..batch_count) |_| {\\n            try theory.correlator(config, ops, &sink);\\n        }\\n        sample.* = (nowNs() - start) / batch_count;\\n        result = sink;\\n    }\\n\\n    const median_ns = median(samples[0..sample_count]);\\n    try checkMedianGate(\\"event\\", name, median_ns, gate);\\n    try checkEventCounts(name, result, expected, batch_count);\\n    std.debug.print(\\n        \\"event case={s} samples={} batch={} median_ns={} wick_terms={} scalars={} coordinates={} tensors={} actions={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n        .{ name, sample_count, batch_count, median_ns, result.wick_terms, result.scalars, result.coordinates, result.tensors, result.actions, result.residuals, result.zero_modes, result.base_cases },\\n    );\\n}\\n\\nfn benchBranch(comptime name: []const u8, comptime sample_count: usize, theory: anytype, config: anytype, ops: anytype, gate: MedianGate, expected: ExpectedCounts) !void {\\n    comptime requireSampleCount(sample_count);\\n\\n    const batch_count = measurementBatchCount(gate.baseline_ns);\\n    var samples: [max_sample_count]u128 = undefined;\\n    var result = BranchSink{};\\n    for (samples[0..sample_count]) |*sample| {\\n        var sink = BranchSink{};\\n        const start = nowNs();\\n        for (0..batch_count) |_| {\\n            try theory.correlator(config, ops, &sink);\\n        }\\n        sample.* = (nowNs() - start) / batch_count;\\n        result = sink;\\n    }\\n\\n    const median_ns = median(samples[0..sample_count]);\\n    try checkMedianGate(\\"branch\\", name, median_ns, gate);\\n    try checkBranchCounts(name, result, expected, batch_count);\\n    std.debug.print(\\n        \\"branch case={s} samples={} batch={} median_ns={} branch_terms={} fallback_terms={} primitive_terms={} scalars={} coordinates={} tensors={} actions={} residuals={} zero_modes={} base_cases={} branch_state_bytes={}\\\\n\\",\\n        .{ name, sample_count, batch_count, median_ns, result.branch_terms, result.fallback_terms, result.primitive_terms, result.scalars, result.coordinates, result.tensors, result.actions, result.residuals, result.zero_modes, result.base_cases, result.branch_state_bytes },\\n    );\\n}\\n\\nfn benchFreeBosonDx(comptime n: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [n]@TypeOf(try X.op.dX(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    inline for (0..n) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"z{}\\", .{i}));\\n        ops_buf[i] = try X.op.dX(&local, mu, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"dx{}\\", .{n});\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchBcSphere(comptime b_count: usize, comptime c_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const Ghost = cft.Bc.sphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var type_local = try Ghost.local(allocator);\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [b_count + c_count]@TypeOf(try Ghost.op.b(&type_local, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try Ghost.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..b_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"b{}\\", .{i}));\\n        ops_buf[i] = try Ghost.op.b(&local, 0, z);\\n    }\\n    inline for (0..c_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"c{}\\", .{i}));\\n        ops_buf[b_count + i] = try Ghost.op.c(&local, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"bc_{}_{}\\", .{ b_count, c_count });\\n\\n    try benchEvent(name, sample_count, Ghost, &Ghost.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, Ghost, &Ghost.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeFermionPsi(comptime n: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const Psi = cft.FreeFermion.sphere(.{ .dimension = 10, .include_antiholomorphic_copy = false });\\n\\n    var type_local = try Psi.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [n]@TypeOf(try Psi.op.psi(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try Psi.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..n) |i| {\\n        const mu = try local.index(std.fmt.comptimePrint(\\"mu{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"psi{}\\", .{i}));\\n        ops_buf[i] = try Psi.op.psi(&local, mu, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"psi{}\\", .{n});\\n\\n    if (n >= 2) {\\n        const TensorGuard = struct {\\n            metric_count: usize = 0,\\n\\n            pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n\\n            pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n                switch (factor) {\\n                    .metric => |metric| {\\n                        switch (metric.left) {\\n                            .symbol => {},\\n                            else => return error.UnexpectedPsiTensorLabel,\\n                        }\\n                        switch (metric.right) {\\n                            .symbol => {},\\n                            else => return error.UnexpectedPsiTensorLabel,\\n                        }\\n                        self.metric_count += 1;\\n                    },\\n                    else => return error.UnexpectedPsiTensorFactor,\\n                }\\n            }\\n\\n            pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickTermEnd(_: *@This()) !void {}\\n            pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n            pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n        };\\n\\n        var guard = TensorGuard{};\\n        try Psi.correlator(&Psi.config.sphere, ops, &guard);\\n        if (guard.metric_count != expected.tensors) return error.BenchmarkCountRegression;\\n    }\\n\\n    try benchEvent(name, sample_count, Psi, &Psi.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, Psi, &Psi.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeBosonExp(comptime exp_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_k = try type_local.momentum(\\"probe_k\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    const probe_zbar = try type_local.coord(\\"probe_zbar\\");\\n    var ops_buf: [exp_count]@TypeOf(try X.op.expX(&type_local, probe_k, probe_z, probe_zbar)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..exp_count) |i| {\\n        const k = try local.momentum(std.fmt.comptimePrint(\\"k{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"e{}\\", .{i}));\\n        const zbar = try local.coord(std.fmt.comptimePrint(\\"eb{}\\", .{i}));\\n        ops_buf[i] = try X.op.expX(&local, k, z, zbar);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"exp{}\\", .{exp_count});\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeBosonDxExp(comptime dx_count: usize, comptime exp_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [dx_count + exp_count]@TypeOf(try X.op.dX(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    inline for (0..dx_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"x{}\\", .{i}));\\n        ops_buf[i] = try X.op.dX(&local, mu, 0, z);\\n    }\\n    inline for (0..exp_count) |i| {\\n        const k = try local.momentum(std.fmt.comptimePrint(\\"k{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"e{}\\", .{i}));\\n        const zbar = try local.coord(std.fmt.comptimePrint(\\"eb{}\\", .{i}));\\n        ops_buf[dx_count + i] = try X.op.expX(&local, k, z, zbar);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"dx{}_exp{}\\", .{ dx_count, exp_count });\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchEtaXiTorus(comptime eta_count: usize, comptime xi_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const EtaXi = cft.EtaXi.sphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var type_local = try EtaXi.local(allocator);\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [eta_count + xi_count]@TypeOf(try EtaXi.op.eta(&type_local, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try EtaXi.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..eta_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"eta{}\\", .{i}));\\n        ops_buf[i] = try EtaXi.op.eta(&local, 0, z);\\n    }\\n    inline for (0..xi_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"xi{}\\", .{i}));\\n        ops_buf[eta_count + i] = try EtaXi.op.xi(&local, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"eta_xi_torus_{}_{}\\", .{ eta_count, xi_count });\\n\\n    try benchEvent(name, sample_count, EtaXi, &EtaXi.config.torus, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, EtaXi, &EtaXi.config.torus, ops, branch_gate, expected);\\n}\\n\\npub fn main() !void {\\n    const allocator = std.heap.page_allocator;\\n    try benchEtaXiTorus(3, 4, 101, allocator, medianGate(16000), medianGate(16000), .{ .primitive_terms = 72, .tensors = 72, .residuals = 0, .zero_modes = 24, .base_cases = 24 });\\n    try benchEtaXiTorus(4, 5, 101, allocator, medianGate(50000), medianGate(50000), .{ .primitive_terms = 480, .tensors = 480, .residuals = 0, .zero_modes = 120, .base_cases = 120 });\\n\\n    try benchFreeBosonDx(8, 101, allocator, medianGate(42779), medianGate(37253), .{ .primitive_terms = 420, .tensors = 420, .residuals = 0, .zero_modes = 105, .base_cases = 105 });\\n    try benchFreeBosonDx(10, 101, allocator, medianGate(231673), medianGate(185435), .{ .primitive_terms = 4725, .tensors = 4725, .residuals = 0, .zero_modes = 945, .base_cases = 945 });\\n    try benchFreeBosonDx(12, 31, allocator, medianGate(2271202), medianGate(2131701), .{ .primitive_terms = 62370, .tensors = 62370, .residuals = 0, .zero_modes = 10395, .base_cases = 10395 });\\n    try benchFreeBosonDx(14, 11, allocator, medianGate(35454428), medianGate(31226026), .{ .primitive_terms = 945945, .tensors = 945945, .residuals = 0, .zero_modes = 135135, .base_cases = 135135 });\\n\\n    try benchBcSphere(3, 6, 101, allocator, medianGate(39095), medianGate(38634), .{ .primitive_terms = 360, .tensors = 360, .residuals = 0, .zero_modes = 120, .base_cases = 120 });\\n    try benchBcSphere(4, 7, 101, allocator, medianGate(234677), medianGate(226326), .{ .primitive_terms = 3360, .tensors = 3360, .residuals = 0, .zero_modes = 840, .base_cases = 840 });\\n    try benchBcSphere(5, 8, 31, allocator, medianGate(2225167), medianGate(2106137), .{ .primitive_terms = 33600, .tensors = 33600, .residuals = 0, .zero_modes = 6720, .base_cases = 6720 });\\n    try benchBcSphere(6, 9, 11, allocator, medianGate(22262770), medianGate(19629812), .{ .primitive_terms = 362880, .tensors = 362880, .residuals = 0, .zero_modes = 60480, .base_cases = 60480 });\\n\\n    try benchFreeBosonExp(2, 101, allocator, medianGate(9277), medianGate(8626), .{ .primitive_terms = 1, .tensors = 1, .residuals = 2, .zero_modes = 1, .base_cases = 1 });\\n    try benchFreeBosonExp(4, 101, allocator, medianGate(10801), medianGate(10893), .{ .primitive_terms = 6, .tensors = 6, .residuals = 12, .zero_modes = 1, .base_cases = 1 });\\n    try benchFreeBosonExp(6, 31, allocator, medianGate(17741), medianGate(18497), .{ .primitive_terms = 15, .tensors = 15, .residuals = 30, .zero_modes = 1, .base_cases = 1 });\\n\\n    try benchFreeBosonDxExp(2, 2, 101, allocator, medianGate(11317), medianGate(11790), .{ .primitive_terms = 14, .tensors = 14, .residuals = 18, .zero_modes = 5, .base_cases = 5 });\\n    try benchFreeBosonDxExp(4, 2, 31, allocator, medianGate(30767), medianGate(32523), .{ .primitive_terms = 185, .tensors = 185, .residuals = 198, .zero_modes = 43, .base_cases = 43 });\\n    try benchFreeBosonDxExp(4, 4, 11, allocator, medianGate(226336), medianGate(218270), .{ .primitive_terms = 3448, .tensors = 3448, .residuals = 5476, .zero_modes = 355, .base_cases = 355 });\\n\\n    try benchFreeFermionPsi(8, 101, allocator, medianGate(30000), medianGate(30000), .{ .primitive_terms = 420, .tensors = 420, .residuals = 0, .zero_modes = 0, .base_cases = 105 });\\n    try benchFreeFermionPsi(10, 101, allocator, medianGate(150000), medianGate(150000), .{ .primitive_terms = 4725, .tensors = 4725, .residuals = 0, .zero_modes = 0, .base_cases = 945 });\\n    try benchFreeFermionPsi(12, 31, allocator, medianGate(1600000), medianGate(1400000), .{ .primitive_terms = 62370, .tensors = 62370, .residuals = 0, .zero_modes = 0, .base_cases = 10395 });\\n    try benchFreeFermionPsi(14, 11, allocator, medianGate(21000000), medianGate(20000000), .{ .primitive_terms = 945945, .tensors = 945945, .residuals = 0, .zero_modes = 0, .base_cases = 135135 });\\n}\\n"},{"id":"20260608T090000.000000","title":"Free Fermion Preset","tags":null,"content":"This node defines the NS-sector free worldsheet fermion on the sphere.  It uses\\nthe shared preset lowering path from Preset Shared Runtime and deliberately\\ndoes not define a torus surface.  Loop-level free-fermion data must later be\\ncombined with the \\\\(\\\\phi\\\\) system rather than frozen here in isolation.\\nThe stringbook convention is\\n\\\\[\\n  \\\\psi^\\\\mu(z)\\\\psi^\\\\nu(w)\\\\sim {\\\\eta^{\\\\mu\\\\nu}\\\\over z-w}.\\n\\\\]\\nDerivative labels mean \\\\(\\\\partial^n\\\\psi^\\\\mu\\\\).  The correlator engine receives\\nonly the lowered fermionic kind ids and pole rule; all theory names remain in\\nthis preset.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nThe preset owns the two chiral operator families.  A compile-time flag can keep\\nonly the holomorphic NS sector when a chiral computation does not need the\\nantiholomorphic copy.\\nconst namespace = \\"free_fermion\\";\\n\\nconst Family = enum {\\n    psi,\\n    psit,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\nSphere PresetThe public surface mirrors the other presets: users get an operator namespace,\\nan opaque sphere config, the bounded text adapter, a local builder, and one\\nstreaming correlator entry point.\\nconst FreeFermionSphereConfig = struct {\\n    dimension: u16,\\n    include_antiholomorphic_copy: bool = true,\\n};\\n\\n/// freeFermionSphere builds the generated preset type for sphere NS free fermions.\\npub fn freeFermionSphere(comptime cfg: FreeFermionSphereConfig) type {\\n    if (cfg.dimension == 0) @compileError(\\"free-fermion target dimension must be nonzero\\");\\n    return struct {\\n        /// op exposes NS free-fermion local operator builders.\\n        pub const op = FreeFermionSphereOp(cfg.include_antiholomorphic_copy);\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = FreeFermionCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for NS free-fermion insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nThe builders pack one target-space vector index and one coordinate.  They do\\nnot expose the compact operator kind ids used by the engine.\\nfn FreeFermionSphereOp(comptime include_antiholomorphic_copy: bool) type {\\n    const Holomorphic = struct {\\n        /// psi builds a holomorphic NS free-fermion insertion.\\n        pub fn psi(local: anytype, mu: Handle.Index, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(fermionOperator(.psi)).single(local, z, n, .{mu});\\n        }\\n    };\\n\\n    if (!include_antiholomorphic_copy) return Holomorphic;\\n\\n    return struct {\\n        /// psi builds a holomorphic NS free-fermion insertion.\\n        pub const psi = Holomorphic.psi;\\n\\n        /// psit builds an antiholomorphic NS free-fermion insertion.\\n        pub fn psit(local: anytype, mu: Handle.Index, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(fermionOperator(.psit)).single(local, zbar, n, .{mu});\\n        }\\n    };\\n}\\nRulesThe sphere rule is a metric numerator times a simple rational pole.  Fermionic\\nstatistics are lowered as data so the generic branch walker supplies the Wick\\npermutation sign for higher-point correlators.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn psiPsi() wick.Expr {\\n    return wick.differentiatedPole(.one, .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn psitPsit() wick.Expr {\\n    return wick.differentiatedPole(.one, .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, antiholomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nconst sphere_holomorphic_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"psi\\", .kind = kind(.psi), .support = .holomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .fermionic },\\n};\\n\\nconst sphere_full_operator_spec = sphere_holomorphic_operator_spec ++ [_]Spec.Operator{\\n    .{ .name = \\"psit\\", .kind = kind(.psit), .support = .antiholomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .fermionic },\\n};\\n\\nconst sphere_holomorphic_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.psi), .right = kind(.psi), .expr = psiPsi() },\\n};\\n\\nconst sphere_full_wick_spec = sphere_holomorphic_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.psit), .right = kind(.psit), .expr = psitPsit() },\\n};\\n\\nconst sphere_holomorphic_wick_storage = Spec.wickRules(&sphere_holomorphic_wick_spec, &sphere_holomorphic_operator_spec);\\nconst sphere_full_wick_storage = Spec.wickRules(&sphere_full_wick_spec, &sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_fermion_storage = Spec.fermionKinds(&sphere_holomorphic_operator_spec);\\nconst sphere_full_fermion_storage = Spec.fermionKinds(&sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_zero_spec = [_]Spec.ZeroModeRule{};\\nconst sphere_full_zero_spec = [_]Spec.ZeroModeRule{};\\n\\nfn sphereSpec(comptime include_antiholomorphic_copy: bool) Spec.Theory {\\n    return .{\\n        .operators = if (include_antiholomorphic_copy) &sphere_full_operator_spec else &sphere_holomorphic_operator_spec,\\n        .wick_rules = if (include_antiholomorphic_copy) &sphere_full_wick_spec else &sphere_holomorphic_wick_spec,\\n        .zero_modes = if (include_antiholomorphic_copy) &sphere_full_zero_spec else &sphere_holomorphic_zero_spec,\\n    };\\n}\\n\\nfn fermionOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_full_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-fermion operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime include_antiholomorphic_copy: bool, comptime wick_count: usize, comptime zero_count: usize, comptime fermion_count: usize) void {\\n    const spec = sphereSpec(include_antiholomorphic_copy);\\n    if (spec.wick_rules.len != wick_count) @compileError(\\"free-fermion sphere spec Wick count does not match lowered rules\\");\\n    if (spec.zero_modes.len != zero_count) @compileError(\\"free-fermion sphere spec zero-mode count does not match lowered rules\\");\\n    if (spec.operators.len != fermion_count) @compileError(\\"free-fermion sphere spec fermion coverage does not match lowered rules\\");\\n}\\n\\nfn FreeFermionRules(comptime include_antiholomorphic_copy: bool) type {\\n    const selected_wick = if (include_antiholomorphic_copy) sphere_full_wick_storage else sphere_holomorphic_wick_storage;\\n    const selected_fermions = if (include_antiholomorphic_copy) sphere_full_fermion_storage else sphere_holomorphic_fermion_storage;\\n    return struct {\\n        const sphere_wick: []const wick.Rule = &selected_wick;\\n        const sphere_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\nfn FreeFermionCorrelatorConfig(comptime cfg: FreeFermionSphereConfig) type {\\n    const rules = FreeFermionRules(cfg.include_antiholomorphic_copy);\\n    return struct {\\n        comptime {\\n            assertSphereSpec(cfg.include_antiholomorphic_copy, rules.sphere_wick.len, 0, rules.sphere_fermion_kinds.len);\\n        }\\n\\n        /// sphere selects NS free-fermion sphere Wick rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &.{},\\n            .fermion_kinds = rules.sphere_fermion_kinds,\\n        }){};\\n    };\\n}\\n"},{"id":"20260608T154500.000000","title":"Lisp Generated Expression Benchmark","tags":null,"content":"This benchmark times the Lisp expression folder over compact generated ABI\\nterm/factor records.  The comparison baseline is the generated C ABI event\\nstream, not the count-only path, because expanded expression output must still\\nread every emitted factor.  Build the generated shared library with\\n-Doptimize=ReleaseFast before timing.\\n(ql:quickload :cffi :silent t)\\n(require :asdf)\\n(push #p\\"lisp/\\" asdf:*central-registry*)\\n(asdf:load-system \\"string-code-cft\\")\\n\\n(defparameter *generated-library* \\"zig-out/lib/libstring_code_cft_generated.so\\")\\n(defparameter *sample-count* 11)\\n\\n(defstruct bench-case name maker fill abi-run-ns)\\n\\n(defun now-ns ()\\n  (multiple-value-bind (sec usec) (sb-ext:get-time-of-day)\\n    (+ (* sec 1000000000) (* usec 1000))))\\n\\n(defun median (values)\\n  (let ((copy (sort (copy-seq values) #\'<)))\\n    (aref copy (floor (length copy) 2))))\\n\\n(defun sample-ns (thunk)\\n  (let ((samples (make-array *sample-count*)))\\n    (dotimes (index *sample-count*)\\n      (sb-ext:gc :full nil)\\n      (let ((start (now-ns)))\\n        (funcall thunk)\\n        (setf (aref samples index) (- (now-ns) start))))\\n    (median samples)))\\n\\n(defun expression-term-count (expr)\\n  (if (and (consp expr) (eq (first expr) \'+))\\n      (length (rest expr))\\n      1))\\n\\n(defun fill-psi (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:psi context\\n                                    (format nil \\"mu~D\\" index)\\n                                    (format nil \\"z~D\\" index)))))\\n\\n(defun fill-dx (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:dX context\\n                                   (format nil \\"mu~D\\" index)\\n                                   (format nil \\"z~D\\" index)))))\\n\\n(defun fill-exp (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:expX context\\n                                     (format nil \\"k~D\\" index)\\n                                     (format nil \\"z~D\\" index)\\n                                     (format nil \\"zb~D\\" index)))))\\n\\n(defun run-case (case)\\n  (let ((context (funcall (bench-case-maker case) :library *generated-library*)))\\n    (unwind-protect\\n         (progn\\n           (funcall (bench-case-fill case) context)\\n           (let* ((frozen (string-code.cft.descriptor:freeze-runtime-operators context))\\n                  (count (string-code.cft.descriptor:count-correlator context frozen))\\n                  (expr-ns (sample-ns\\n                            (lambda ()\\n                              (expression-term-count\\n                               (string-code.cft.descriptor:correlator-expression context frozen)))))\\n                  (ratio (/ (float expr-ns) (bench-case-abi-run-ns case))))\\n             (format t \\"~A count=~D expr_ns=~D abi_run_ns=~D ratio=~,2F~%\\"\\n                     (bench-case-name case) count expr-ns\\n                     (bench-case-abi-run-ns case) ratio)))\\n      (string-code.cft.descriptor:destroy-runtime-context context))))\\n\\n(defparameter *cases*\\n  (list\\n   (make-bench-case :name \\"psi8\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-fermion-runtime\\n                    :fill (fill-psi 8)\\n                    :abi-run-ns 136715)\\n   (make-bench-case :name \\"psi10\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-fermion-runtime\\n                    :fill (fill-psi 10)\\n                    :abi-run-ns 1056899)\\n   (make-bench-case :name \\"dx8\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-boson-runtime\\n                    :fill (fill-dx 8)\\n                    :abi-run-ns 125244)\\n   (make-bench-case :name \\"exp4\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-boson-runtime\\n                    :fill (fill-exp 4)\\n                    :abi-run-ns 8268)))\\n\\n(dolist (case *cases*)\\n  (run-case case))\\n"},{"id":"20260608T231500.000000","title":"Generated Descriptor Boundary","tags":null,"content":"The descriptor boundary is the narrow data interface between generated theory\\nfiles and the current Wick traversal Wick.  Lisp emits only compact ids\\nand primitive factors.  This module validates those tables, lowers them at\\ncomptime, and streams small events back out without building a symbolic tree.\\nconst std = @import(\\"std\\");\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst shared = @import(\\"../presets/shared.zig\\");\\n\\nconst declare = shared.declare;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nconst scalars = declare.scalars;\\n\\n/// descriptor_abi_version is the generated descriptor table ABI understood here.\\npub const descriptor_abi_version: u32 = 1;\\n\\n/// Id is one dense descriptor table index.\\npub const Id = u16;\\n\\n/// LabelRole classifies a generated field label.\\npub const LabelRole = enum {\\n    vector_index,\\n    momentum,\\n    profile,\\n    scalar_parameter,\\n    modular_parameter,\\n};\\n\\n/// SurfaceKind names the supported topology class.\\npub const SurfaceKind = enum {\\n    sphere,\\n    torus,\\n};\\n\\n/// CoordinateModel selects rational or elliptic coordinate kernels.\\npub const CoordinateModel = enum {\\n    rational,\\n    elliptic,\\n};\\n\\n/// Statistics records the exchange parity of one field family.\\npub const Statistics = enum {\\n    bosonic,\\n    fermionic,\\n};\\n\\n/// InsertionShape gives the coordinate slots carried by a field insertion.\\npub const InsertionShape = enum {\\n    single,\\n    pair,\\n};\\nFields and surfaces are lean table rows.  Names are symbol ids from the\\ngenerated symbol table, not strings used by the lowerer.\\n/// Parameter declares one generated scalar or modular parameter.\\npub const Parameter = struct {\\n    id: Id,\\n    symbol: Id,\\n    role: LabelRole,\\n};\\n\\n/// QuantumNumberKind classifies a conserved or representation-valued label.\\npub const QuantumNumberKind = enum {\\n    u1_charge,\\n    ade_irrep,\\n};\\n\\n/// QuantumNumber declares one basis-selection quantum number.\\npub const QuantumNumber = struct {\\n    id: Id,\\n    symbol: Id,\\n    kind: QuantumNumberKind,\\n    group_symbol: ?Id = null,\\n};\\n\\n/// QuantumNumberValue stores one compact field quantum-number value.\\npub const QuantumNumberValue = union(enum) {\\n    integer: i64,\\n    rational: Rational,\\n    symbol: Id,\\n};\\n\\n/// FieldQuantumNumber assigns one quantum-number value to one field family.\\npub const FieldQuantumNumber = struct {\\n    field: Id,\\n    quantum_number: Id,\\n    value: QuantumNumberValue,\\n};\\n\\n/// Surface declares one coordinate model for a generated rule set.\\npub const Surface = struct {\\n    id: Id,\\n    kind: SurfaceKind,\\n    coordinate_model: CoordinateModel,\\n    modular_parameter: ?Id = null,\\n};\\n\\n/// LabelSchema declares one field label slot.\\npub const LabelSchema = struct {\\n    id: Id,\\n    role: LabelRole,\\n    symbol: Id,\\n};\\n\\n/// Field declares one generated local field family.\\npub const Field = struct {\\n    id: Id,\\n    symbol: Id,\\n    insertion: InsertionShape,\\n    labels: []const LabelSchema = &.{},\\n    statistics: Statistics,\\n    zero_mode_consumable: bool = false,\\n    weight: ?Id = null,\\n    anti_weight: ?Id = null,\\n};\\nMetadata expressions are ids over a separate table.  They are preserved for\\nLisp-facing metadata and checked for dangling references.\\n/// Rational is one exact small rational value.\\npub const Rational = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\n/// MetadataExpr is a compact expression tree over ids, labels, and parameters.\\npub const MetadataExpr = union(enum) {\\n    rational: Rational,\\n    parameter: Id,\\n    field_label: struct { field: Id, label: Id },\\n    add: struct { left: Id, right: Id },\\n    mul: struct { left: Id, right: Id },\\n    bilinear: struct { left: Id, right: Id, form_symbol: Id },\\n};\\nWick factors name primitive concepts.  The lowerer maps these to the current\\nprivate runtime tables without making runtime table types public.\\n/// Side selects the left or right endpoint of a Wick rule.\\npub const Side = enum {\\n    left,\\n    right,\\n};\\n\\n/// CoordinateSlot selects one coordinate component from an insertion.\\npub const CoordinateSlot = enum {\\n    position,\\n    holomorphic,\\n    antiholomorphic,\\n};\\n\\n/// CoordinateRef names one coordinate slot on one endpoint.\\npub const CoordinateRef = struct {\\n    side: Side,\\n    slot: CoordinateSlot,\\n};\\n\\n/// LabelRef names one label slot on one endpoint.\\npub const LabelRef = struct {\\n    side: Side,\\n    slot: Id,\\n};\\n\\n/// ScalarFactor describes one scalar multiplier in a Wick term.\\npub const ScalarFactor = union(enum) {\\n    one,\\n    rational: Rational,\\n    parameter: Id,\\n    neg_parameter_half: Id,\\n    neg_i_parameter_half: Id,\\n    parameter_half: Id,\\n};\\n\\n/// CoordinateFactor describes one coordinate multiplier in a Wick term.\\npub const CoordinateFactor = union(enum) {\\n    difference_power: struct { left: CoordinateRef, right: CoordinateRef, exponent: i16, derive_left: bool = false, derive_right: bool = false },\\n    named_kernel: struct { symbol: Id, left: CoordinateRef, right: CoordinateRef, derive_left: bool = false, derive_right: bool = false },\\n    green_exponential: struct { left: CoordinateRef, right: CoordinateRef },\\n};\\n\\n/// TensorFactor describes one tensor multiplier in a Wick term.\\npub const TensorFactor = union(enum) {\\n    none,\\n    metric: struct { left: LabelRef, right: LabelRef },\\n    momentum_index: struct { momentum: LabelRef, index: LabelRef },\\n    momentum_pair: struct { left: LabelRef, right: LabelRef },\\n};\\n\\n/// ActionFactor describes an operation applied to a residual factor.\\npub const ActionFactor = union(enum) {\\n    profile_derivative: struct { profile: LabelRef, index: LabelRef },\\n};\\n\\n/// WickTerm is one product in a primitive Wick-rule sum.\\npub const WickTerm = struct {\\n    scalars: []const ScalarFactor = &.{},\\n    coordinates: []const CoordinateFactor = &.{},\\n    tensors: []const TensorFactor = &.{},\\n    actions: []const ActionFactor = &.{},\\n    residuals: []const Side = &.{},\\n};\\n\\n/// WickRule declares primitive pair terms for two field families.\\npub const WickRule = struct {\\n    surface: Id,\\n    left: Id,\\n    right: Id,\\n    terms: []const WickTerm,\\n};\\nZero modes use generic generated data.  Reserved service slots are present so\\nlater endpoints can fail deliberately rather than reshaping the ABI.\\n/// ZeroModeKind selects one implemented residual base-case primitive.\\npub const ZeroModeKind = enum {\\n    constant_fermion,\\n    top_form_fermion,\\n    boson_momentum_conservation,\\n};\\n\\n/// ZeroModeRule declares one residual base-case primitive.\\npub const ZeroModeRule = struct {\\n    surface: Id,\\n    kind: ZeroModeKind,\\n    consumes: []const Id,\\n    normalization: ScalarFactor = .one,\\n    two_pi_power: u16 = 0,\\n};\\n\\n/// ReservedServices records descriptor slots not implemented in this slice.\\npub const ReservedServices = struct {\\n    ope_rules: usize = 0,\\n    operator_templates: usize = 0,\\n    relation_rules: usize = 0,\\n    basis_rules: usize = 0,\\n};\\n\\n/// Descriptor is the generated theory data consumed by the lowerer.\\npub const Descriptor = struct {\\n    abi_version: u32 = descriptor_abi_version,\\n    theory_symbol: Id,\\n    theory_hash: u32,\\n    symbols: []const []const u8,\\n    parameters: []const Parameter = &.{},\\n    quantum_numbers: []const QuantumNumber = &.{},\\n    field_quantum_numbers: []const FieldQuantumNumber = &.{},\\n    surfaces: []const Surface,\\n    fields: []const Field,\\n    metadata: []const MetadataExpr = &.{},\\n    wick_rules: []const WickRule,\\n    zero_modes: []const ZeroModeRule = &.{},\\n    result_symbols: []const Id = &.{},\\n    reserved: ReservedServices = .{},\\n};\\nValidation is explicit loops over small descriptor tables.  Runtime tests call\\nthe same predicates that the comptime lowerer uses.\\nfn denseUniqueIds(comptime T: type, items: []const T) !void {\\n    var seen: [256]bool = [_]bool{false} ** 256;\\n    for (items) |item| {\\n        if (item.id >= items.len) return error.NonDenseId;\\n        if (seen[item.id]) return error.DuplicateId;\\n        seen[item.id] = true;\\n    }\\n}\\n\\nfn hasSymbol(d: Descriptor, symbol: Id) bool {\\n    return symbol < d.symbols.len;\\n}\\n\\nfn hasParameter(d: Descriptor, id: Id) bool {\\n    return id < d.parameters.len and d.parameters[id].id == id;\\n}\\n\\nfn hasQuantumNumber(d: Descriptor, id: Id) bool {\\n    return id < d.quantum_numbers.len and d.quantum_numbers[id].id == id;\\n}\\n\\nfn hasSurface(d: Descriptor, id: Id) bool {\\n    return id < d.surfaces.len and d.surfaces[id].id == id;\\n}\\n\\nfn hasField(d: Descriptor, id: Id) bool {\\n    return id < d.fields.len and d.fields[id].id == id;\\n}\\n\\nfn fieldLabel(field: Field, id: Id) ?LabelSchema {\\n    for (field.labels) |label| {\\n        if (label.id == id) return label;\\n    }\\n    return null;\\n}\\n\\nfn validateLabelRef(d: Descriptor, field_id: Id, label_id: Id) !void {\\n    if (!hasField(d, field_id)) return error.DanglingField;\\n    if (fieldLabel(d.fields[field_id], label_id) == null) return error.DanglingLabel;\\n}\\n\\nfn validateMetadata(d: Descriptor, index: Id, expr: MetadataExpr) !void {\\n    switch (expr) {\\n        .rational => |value| if (value.denominator == 0) return error.InvalidRational,\\n        .parameter => |id| if (!hasParameter(d, id)) return error.DanglingParameter,\\n        .field_label => |ref| try validateLabelRef(d, ref.field, ref.label),\\n        .add => |pair| {\\n            if (pair.left >= index or pair.right >= index) return error.DanglingMetadata;\\n        },\\n        .mul => |pair| {\\n            if (pair.left >= index or pair.right >= index) return error.DanglingMetadata;\\n        },\\n        .bilinear => |item| {\\n            if (item.left >= index or item.right >= index) return error.DanglingMetadata;\\n            if (!hasSymbol(d, item.form_symbol)) return error.DanglingSymbol;\\n        },\\n    }\\n}\\nfn labelRole(d: Descriptor, ref: LabelRef, left_field: Id, right_field: Id) !LabelRole {\\n    const field_id = if (ref.side == .left) left_field else right_field;\\n    if (!hasField(d, field_id)) return error.DanglingField;\\n    const label = fieldLabel(d.fields[field_id], ref.slot) orelse return error.DanglingLabel;\\n    return label.role;\\n}\\n\\nfn validateTensor(d: Descriptor, tensor: TensorFactor, left_field: Id, right_field: Id) !void {\\n    switch (tensor) {\\n        .none => {},\\n        .metric => |item| {\\n            if (try labelRole(d, item.left, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.right, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n        .momentum_index => |item| {\\n            if (try labelRole(d, item.momentum, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.index, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n        .momentum_pair => |item| {\\n            if (try labelRole(d, item.left, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.right, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n        },\\n    }\\n}\\n\\nfn validateAction(d: Descriptor, action: ActionFactor, left_field: Id, right_field: Id) !void {\\n    switch (action) {\\n        .profile_derivative => |item| {\\n            if (try labelRole(d, item.profile, left_field, right_field) != .profile) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.index, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n    }\\n}\\n/// validateDescriptor rejects malformed generated descriptor data.\\npub fn validateDescriptor(d: Descriptor) !void {\\n    if (d.abi_version != descriptor_abi_version) return error.UnsupportedDescriptorAbi;\\n    if (!hasSymbol(d, d.theory_symbol)) return error.DanglingSymbol;\\n    if (d.reserved.ope_rules != 0 or d.reserved.operator_templates != 0 or d.reserved.relation_rules != 0 or d.reserved.basis_rules != 0) {\\n        return error.UnsupportedReservedService;\\n    }\\n\\n    try denseUniqueIds(Parameter, d.parameters);\\n    try denseUniqueIds(QuantumNumber, d.quantum_numbers);\\n    try denseUniqueIds(Surface, d.surfaces);\\n    try denseUniqueIds(Field, d.fields);\\n\\n    for (d.parameters) |parameter| {\\n        if (!hasSymbol(d, parameter.symbol)) return error.DanglingSymbol;\\n    }\\n    for (d.quantum_numbers) |number| {\\n        if (!hasSymbol(d, number.symbol)) return error.DanglingSymbol;\\n        if (number.group_symbol) |symbol| if (!hasSymbol(d, symbol)) return error.DanglingSymbol;\\n    }\\n    for (d.field_quantum_numbers) |item| {\\n        if (!hasField(d, item.field)) return error.DanglingField;\\n        if (!hasQuantumNumber(d, item.quantum_number)) return error.DanglingQuantumNumber;\\n        const number = d.quantum_numbers[item.quantum_number];\\n        switch (item.value) {\\n            .integer => if (number.kind != .u1_charge) return error.IncompatibleQuantumNumberValue,\\n            .rational => |value| {\\n                if (number.kind != .u1_charge) return error.IncompatibleQuantumNumberValue;\\n                if (value.denominator == 0) return error.InvalidRational;\\n            },\\n            .symbol => |symbol| {\\n                if (number.kind != .ade_irrep) return error.IncompatibleQuantumNumberValue;\\n                if (!hasSymbol(d, symbol)) return error.DanglingSymbol;\\n            },\\n        }\\n    }\\n    for (d.surfaces) |surface| {\\n        if (surface.modular_parameter) |id| if (!hasParameter(d, id)) return error.DanglingParameter;\\n    }\\n    for (d.fields) |field| {\\n        if (!hasSymbol(d, field.symbol)) return error.DanglingSymbol;\\n        try denseUniqueIds(LabelSchema, field.labels);\\n        for (field.labels) |label| if (!hasSymbol(d, label.symbol)) return error.DanglingSymbol;\\n        if (field.weight) |id| if (id >= d.metadata.len) return error.DanglingMetadata;\\n        if (field.anti_weight) |id| if (id >= d.metadata.len) return error.DanglingMetadata;\\n    }\\n    for (d.metadata, 0..) |expr, index| try validateMetadata(d, @intCast(index), expr);\\n\\n    for (d.wick_rules) |rule| {\\n        if (!hasSurface(d, rule.surface)) return error.DanglingSurface;\\n        if (!hasField(d, rule.left) or !hasField(d, rule.right)) return error.DanglingField;\\n        if (rule.terms.len == 0) return error.EmptyWickRule;\\n        const left_arity = d.fields[rule.left].labels.len;\\n        const right_arity = d.fields[rule.right].labels.len;\\n        for (rule.terms) |term| {\\n            for (term.scalars) |scalar| switch (scalar) {\\n                .parameter, .neg_parameter_half, .neg_i_parameter_half, .parameter_half => |id| if (!hasParameter(d, id)) return error.DanglingParameter,\\n                else => {},\\n            };\\n            for (term.tensors) |tensor| try validateTensor(d, tensor, rule.left, rule.right);\\n            for (term.actions) |action| try validateAction(d, action, rule.left, rule.right);\\n            for (term.residuals) |_| {}\\n        }\\n        if (left_arity != d.fields[rule.left].labels.len or right_arity != d.fields[rule.right].labels.len) return error.InvalidFieldArity;\\n    }\\n\\n    for (d.zero_modes) |rule| {\\n        if (!hasSurface(d, rule.surface)) return error.DanglingSurface;\\n        if (rule.consumes.len == 0) return error.EmptyZeroMode;\\n        for (rule.consumes) |field_id| {\\n            if (!hasField(d, field_id)) return error.DanglingField;\\n            if (!d.fields[field_id].zero_mode_consumable) return error.IncompatibleZeroModeField;\\n        }\\n    }\\n}\\nLowering derives stable private kind and sector ids from descriptor ids.  No\\nfield or theory names affect generic control flow.\\nfn stableId(seed: u32, local: u32) u32 {\\n    var hash = seed ^ 2166136261;\\n    hash = (hash ^ local) *% 16777619;\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn kindId(comptime d: Descriptor, field: Id) operators.OperatorKindId {\\n    return stableId(d.theory_hash, field + 1);\\n}\\n\\nfn sectorId(comptime d: Descriptor, surface: Id, zero_rule: usize) zero_mode.Sector {\\n    return stableId(d.theory_hash ^ 0x51f1_0000, (@as(u32, surface) << 16) | @as(u32, @intCast(zero_rule + 1)));\\n}\\n\\nfn lowerSupport(shape: InsertionShape) wick.Support {\\n    return switch (shape) {\\n        .single => .holomorphic,\\n        .pair => .bulk_pair,\\n    };\\n}\\n\\nfn lowerInsertion(shape: InsertionShape) declare.Spec.InsertionShape {\\n    return switch (shape) {\\n        .single => .single,\\n        .pair => .pair,\\n    };\\n}\\n\\nfn lowerLabelKind(role: LabelRole) declare.Spec.LabelKind {\\n    return switch (role) {\\n        .vector_index => .index,\\n        .momentum => .momentum,\\n        .profile => .profile,\\n        .scalar_parameter, .modular_parameter => @compileError(\\"parameter roles are not field-label runtime slots\\"),\\n    };\\n}\\n\\nfn lowerField(comptime d: Descriptor, comptime field: Field) declare.Spec.Operator {\\n    comptime var labels: [field.labels.len]declare.Spec.LabelKind = undefined;\\n    inline for (field.labels, 0..) |label, index| labels[index] = lowerLabelKind(label.role);\\n    return .{\\n        .name = d.symbols[field.symbol],\\n        .kind = kindId(d, field.id),\\n        .support = lowerSupport(field.insertion),\\n        .insertion = lowerInsertion(field.insertion),\\n        .labels = &labels,\\n        .statistics = if (field.statistics == .fermionic) .fermionic else .bosonic,\\n        .zero_mode_consumable = field.zero_mode_consumable,\\n    };\\n}\\nfn lowerSide(side: Side) wick.Side {\\n    return if (side == .left) .left else .right;\\n}\\n\\nfn lowerCoordSlot(slot: CoordinateSlot) wick.CoordinateSlot {\\n    return switch (slot) {\\n        .position => .position,\\n        .holomorphic => .holomorphic,\\n        .antiholomorphic => .antiholomorphic,\\n    };\\n}\\n\\nfn lowerCoordRef(ref: CoordinateRef) wick.CoordinateRef {\\n    return wick.coord(lowerSide(ref.side), lowerCoordSlot(ref.slot));\\n}\\n\\nfn lowerLabelRef(ref: LabelRef) wick.LabelRef {\\n    return wick.label(lowerSide(ref.side), @intCast(ref.slot));\\n}\\n\\nfn lowerDerivative(left: bool, right: bool) wick.DerivativeAction {\\n    return .{ .include_left = left, .include_right = right };\\n}\\n\\nfn parameterScalar(comptime d: Descriptor, id: Id) @TypeOf(scalars.one()) {\\n    return scalars.atomScalar(parameterAtom(d, id));\\n}\\n\\nfn parameterAtom(comptime d: Descriptor, id: Id) u32 {\\n    return stableId(d.theory_hash ^ 0xa170_0000, id + 1);\\n}\\n\\nfn lowerScalar(comptime d: Descriptor, factor: ScalarFactor) wick.ScalarFactor {\\n    return switch (factor) {\\n        .one => wick.scalar(scalars.one()),\\n        .rational => |value| wick.scalar(scalars.rational(value.numerator, value.denominator)),\\n        .parameter => |id| wick.scalar(parameterScalar(d, id)),\\n        .neg_parameter_half => |id| wick.scalar(scalars.div(scalars.neg(parameterScalar(d, id)), 2)),\\n        .neg_i_parameter_half => |id| wick.scalar(scalars.div(scalars.neg(scalars.i(parameterScalar(d, id))), 2)),\\n        .parameter_half => |id| wick.scalar(scalars.div(parameterScalar(d, id), 2)),\\n    };\\n}\\n\\nfn lowerScalarValue(comptime d: Descriptor, factor: ScalarFactor) @TypeOf(scalars.one()) {\\n    return switch (lowerScalar(d, factor)) {\\n        .value => |value| value,\\n        else => @compileError(\\"descriptor zero-mode normalization must be a value scalar\\"),\\n    };\\n}\\n\\nfn lowerCoordinate(comptime d: Descriptor, factor: CoordinateFactor) wick.CoordinateFactor {\\n    return switch (factor) {\\n        .difference_power => |item| wick.differentiatedPower(\\n            wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)),\\n            item.exponent,\\n            lowerDerivative(item.derive_left, item.derive_right),\\n        ),\\n        .named_kernel => |item| wick.differentiatedGreenKernel(\\n            d.symbols[item.symbol],\\n            wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)),\\n            lowerDerivative(item.derive_left, item.derive_right),\\n        ),\\n        .green_exponential => |item| .{ .green_exponential = wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)) },\\n    };\\n}\\nfn lowerTensor(factor: TensorFactor) wick.TensorFactor {\\n    return switch (factor) {\\n        .none => .none,\\n        .metric => |item| .{ .metric = .{ .left = lowerLabelRef(item.left), .right = lowerLabelRef(item.right) } },\\n        .momentum_index => |item| .{ .momentum_index = .{ .momentum = lowerLabelRef(item.momentum), .index = lowerLabelRef(item.index) } },\\n        .momentum_pair => |item| .{ .momentum_pair = .{ .left = lowerLabelRef(item.left), .right = lowerLabelRef(item.right) } },\\n    };\\n}\\n\\nfn lowerAction(factor: ActionFactor) wick.ActionFactor {\\n    return switch (factor) {\\n        .profile_derivative => |item| wick.profileDerivative(lowerLabelRef(item.profile), lowerLabelRef(item.index)),\\n    };\\n}\\n\\nfn LoweredTerm(comptime d: Descriptor, comptime term: WickTerm) type {\\n    return struct {\\n        const scalar_storage = blk: {\\n            var storage: [term.scalars.len]wick.ScalarFactor = undefined;\\n            for (term.scalars, 0..) |factor, index| storage[index] = lowerScalar(d, factor);\\n            break :blk storage;\\n        };\\n        const coordinate_storage = blk: {\\n            var storage: [term.coordinates.len]wick.CoordinateFactor = undefined;\\n            for (term.coordinates, 0..) |factor, index| storage[index] = lowerCoordinate(d, factor);\\n            break :blk storage;\\n        };\\n        const tensor_storage = blk: {\\n            var storage: [term.tensors.len]wick.TensorFactor = undefined;\\n            for (term.tensors, 0..) |factor, index| storage[index] = lowerTensor(factor);\\n            break :blk storage;\\n        };\\n        const action_storage = blk: {\\n            var storage: [term.actions.len]wick.ActionFactor = undefined;\\n            for (term.actions, 0..) |factor, index| storage[index] = lowerAction(factor);\\n            break :blk storage;\\n        };\\n        const residual_storage = blk: {\\n            var storage: [term.residuals.len]wick.Side = undefined;\\n            for (term.residuals, 0..) |side, index| storage[index] = lowerSide(side);\\n            break :blk storage;\\n        };\\n        const value = wick.termWithResiduals(&scalar_storage, &coordinate_storage, &tensor_storage, &action_storage, &residual_storage);\\n    };\\n}\\n\\nfn LoweredWickRule(comptime d: Descriptor, comptime rule: WickRule) type {\\n    return struct {\\n        const terms = blk: {\\n            var storage: [rule.terms.len]wick.Term = undefined;\\n            for (rule.terms, 0..) |term, index| storage[index] = LoweredTerm(d, term).value;\\n            break :blk storage;\\n        };\\n        const value = wick.rule(\\n            wick.pattern(kindId(d, rule.left), lowerSupport(d.fields[rule.left].insertion)),\\n            wick.pattern(kindId(d, rule.right), lowerSupport(d.fields[rule.right].insertion)),\\n            wick.expr(&terms),\\n        );\\n    };\\n}\\n\\nfn lowerWickRule(comptime d: Descriptor, comptime rule: WickRule) wick.Rule {\\n    return LoweredWickRule(d, rule).value;\\n}\\nThe zero-mode lowering keeps the generated rule generic, then adapts to the\\ncurrent runtime procedures behind the descriptor boundary.\\nfn lowerConsumeKinds(comptime d: Descriptor, comptime consumes: []const Id) [consumes.len]operators.OperatorKindId {\\n    var storage: [consumes.len]operators.OperatorKindId = undefined;\\n    inline for (consumes, 0..) |field_id, index| storage[index] = kindId(d, field_id);\\n    return storage;\\n}\\n\\nfn LoweredZeroMode(comptime d: Descriptor, comptime rule: ZeroModeRule, comptime index: usize) type {\\n    return struct {\\n        const consume_storage = lowerConsumeKinds(d, rule.consumes);\\n        const sector = sectorId(d, rule.surface, index);\\n        const value = switch (rule.kind) {\\n            .constant_fermion => zero_mode.rule(sector, .{ .constant_fermion = .{\\n                .support = .sphere_holomorphic,\\n                .fermion_kind_ids = &consume_storage,\\n                .normalization = lowerScalarValue(d, rule.normalization),\\n            } }),\\n            .top_form_fermion => zero_mode.rule(sector, .{ .top_form_fermion = .{\\n                .support = .sphere_holomorphic,\\n                .field_kind_ids = &consume_storage,\\n                .normalization = lowerScalarValue(d, rule.normalization),\\n            } }),\\n            .boson_momentum_conservation => zero_mode.rule(sector, .{ .boson_momentum_conservation = .{\\n                .exp_kind_ids = &consume_storage,\\n                .profile_kind_ids = &.{},\\n                .normalization = .{\\n                    .scalar = lowerScalarValue(d, rule.normalization),\\n                    .two_pi_power = if (rule.two_pi_power == 0) .none else .{ .literal = rule.two_pi_power },\\n                },\\n            } }),\\n        };\\n    };\\n}\\n\\nfn lowerZeroMode(comptime d: Descriptor, comptime rule: ZeroModeRule, comptime index: usize) zero_mode.Rule {\\n    return LoweredZeroMode(d, rule, index).value;\\n}\\n\\nfn fieldSpecStorage(comptime d: Descriptor) [d.fields.len]declare.Spec.Operator {\\n    var storage: [d.fields.len]declare.Spec.Operator = undefined;\\n    inline for (d.fields, 0..) |field, index| storage[index] = lowerField(d, field);\\n    return storage;\\n}\\n\\nfn wickStorage(comptime d: Descriptor) [d.wick_rules.len]wick.Rule {\\n    var storage: [d.wick_rules.len]wick.Rule = undefined;\\n    inline for (d.wick_rules, 0..) |rule, index| storage[index] = lowerWickRule(d, rule);\\n    return storage;\\n}\\n\\nfn zeroStorage(comptime d: Descriptor) [d.zero_modes.len]zero_mode.Rule {\\n    var storage: [d.zero_modes.len]zero_mode.Rule = undefined;\\n    inline for (d.zero_modes, 0..) |rule, index| storage[index] = lowerZeroMode(d, rule, index);\\n    return storage;\\n}\\n\\nfn fermionStorage(comptime fields: []const Field, comptime d: Descriptor) [fermionCount(fields)]operators.OperatorKindId {\\n    var storage: [fermionCount(fields)]operators.OperatorKindId = undefined;\\n    comptime var index: usize = 0;\\n    inline for (fields) |field| {\\n        if (field.statistics == .fermionic) {\\n            storage[index] = kindId(d, field.id);\\n            index += 1;\\n        }\\n    }\\n    return storage;\\n}\\n\\nfn fermionCount(comptime fields: []const Field) usize {\\n    comptime var count: usize = 0;\\n    inline for (fields) |field| {\\n        if (field.statistics == .fermionic) count += 1;\\n    }\\n    return count;\\n}\\nRuntime contexts own only symbol names, label values, and packed local\\noperators.  Count mode uses a sink that never constructs expression objects.\\n/// ResultEventKind names one streamed Lisp-facing result event.\\npub const ResultEventKind = enum(u8) {\\n    sum_term_begin,\\n    sum_term_end,\\n    wick_term_begin,\\n    wick_term_end,\\n    scalar,\\n    coordinate,\\n    tensor,\\n    zero_mode,\\n    residual_operator,\\n};\\n\\n/// ResultEvent is one compact streamed result record.\\npub const ResultEvent = struct {\\n    kind: ResultEventKind,\\n    a: u32 = 0,\\n    b: u32 = 0,\\n    c: u32 = 0,\\n    d: i32 = 0,\\n    name: ?[]const u8 = null,\\n};\\n\\n/// StreamFn consumes one result event and user payload.\\npub const StreamFn = *const fn (*anyopaque, ResultEvent) anyerror!void;\\n\\nconst CountSink = struct {\\n    count: usize = 0,\\n\\n    pub fn emitWickBranchSign(_: *@This(), _: i8) !void {}\\n    pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickResidualOperator(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n    pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.count += 1;\\n    }\\n};\\nfn labelSymbol(value: kernel.Call.LabelValue) u32 {\\n    return switch (value) {\\n        .symbol => |symbol| symbol,\\n        .integer => |item| @intCast(item),\\n        .rational => 0,\\n        .tensor => |item| item,\\n    };\\n}\\n\\nfn scalarFlags(imaginary_power: u2, atom_power: i8) u32 {\\n    return @as(u32, imaginary_power) | (@as(u32, @as(u8, @bitCast(atom_power))) << 8);\\n}\\n\\nfn scalarValueEvent(rational: anytype, imaginary_power: u2, atom: ?u32, atom_power: i8) !ResultEvent {\\n    const numerator = std.math.cast(i32, rational.numerator) orelse return error.ScalarNumeratorOutOfRange;\\n    const denominator = std.math.cast(u32, rational.denominator) orelse return error.ScalarDenominatorOutOfRange;\\n    return .{\\n        .kind = .scalar,\\n        .a = atom orelse 0,\\n        .b = denominator,\\n        .c = scalarFlags(imaginary_power, atom_power),\\n        .d = numerator,\\n        .name = \\"scalar-monomial\\",\\n    };\\n}\\n\\nfn scalarEvent(value: anytype) !ResultEvent {\\n    return switch (value) {\\n        .one => .{ .kind = .scalar },\\n        .rational => |rational| scalarValueEvent(rational, 0, null, 0),\\n        .monomial => |monomial| scalarValueEvent(monomial.rational, monomial.imaginary_power, monomial.atom, monomial.atom_power),\\n    };\\n}\\n\\nconst EventSink = struct {\\n    payload: *anyopaque,\\n    stream: StreamFn,\\n\\n    fn emit(self: *@This(), event: ResultEvent) !void {\\n        try self.stream(self.payload, event);\\n    }\\n\\n    pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n        if (sign < 0) try self.emit(.{ .kind = .scalar, .d = -1 });\\n    }\\n\\n    pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n        try self.emit(.{ .kind = .wick_term_begin, .a = @intCast(event.left_index), .b = @intCast(event.right_index), .c = @intCast(event.term_index) });\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .value => |value| try self.emit(try scalarEvent(value)),\\n            .label_bilinear_phase => |item| try self.emit(.{ .kind = .scalar, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = item.form }),\\n            .cocycle => |item| try self.emit(.{ .kind = .scalar, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = item.table }),\\n            .spin_structure => |name| try self.emit(.{ .kind = .scalar, .name = name }),\\n        }\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n        switch (factor.kernel) {\\n            .difference_power => |item| try self.emit(.{ .kind = .coordinate, .a = item.coordinate.left, .b = item.coordinate.right, .d = item.exponent }),\\n            .logarithm => |item| try self.emit(.{ .kind = .coordinate, .a = item.left, .b = item.right, .name = \\"log\\" }),\\n            .green_kernel => |item| try self.emit(.{ .kind = .coordinate, .a = item.coordinate.left, .b = item.coordinate.right, .name = item.name }),\\n            .green_exponential => |item| try self.emit(.{ .kind = .coordinate, .a = item.left, .b = item.right, .name = \\"exp-green\\" }),\\n        }\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .none => try self.emit(.{ .kind = .tensor }),\\n            .metric => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = \\"metric\\" }),\\n            .momentum_index => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.momentum), .b = labelSymbol(item.index), .name = \\"momentum-index\\" }),\\n            .momentum_pair => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = \\"momentum-pair\\" }),\\n            else => try self.emit(.{ .kind = .tensor }),\\n        }\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        try self.emit(.{ .kind = .residual_operator, .name = \\"action\\" });\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), residual: anytype) !void {\\n        try self.emit(.{ .kind = .residual_operator, .a = @intCast(residual.input_index) });\\n    }\\n\\n    pub fn emitWickTermEnd(self: *@This()) !void {\\n        try self.emit(.{ .kind = .wick_term_end });\\n    }\\n\\n    pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .bc_top_form => try self.emit(.{ .kind = .zero_mode, .name = \\"bc-top-form\\" }),\\n            .eta_xi_zero_mode => |item| try self.emit(.{ .kind = .zero_mode, .a = item.xi.coordinate, .name = \\"eta-xi-zero-mode\\" }),\\n            .momentum_delta => |item| {\\n                try self.emit(try scalarEvent(item.scalar));\\n                try self.emit(.{ .kind = .zero_mode, .a = item.two_pi_power, .b = @intCast(item.momenta.len), .name = \\"momentum-delta\\" });\\n                for (item.momenta) |momentum| {\\n                    try self.emit(.{ .kind = .zero_mode, .a = labelSymbol(momentum), .name = \\"momentum-delta-momentum\\" });\\n                }\\n            },\\n            else => try self.emit(.{ .kind = .zero_mode }),\\n        }\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        try self.emit(.{ .kind = .sum_term_end });\\n    }\\n};\\nfn insertionFrom(shape: InsertionShape, coords: []const u32) !operators.OperatorInsertion {\\n    return switch (shape) {\\n        .single => if (coords.len == 1) .{ .single = .{ .position = coords[0], .derivatives = 0 } } else error.InvalidCoordinateArity,\\n        .pair => if (coords.len == 2) .{ .pair = .{\\n            .holomorphic_position = coords[0],\\n            .antiholomorphic_position = coords[1],\\n            .holomorphic_derivatives = 0,\\n            .antiholomorphic_derivatives = 0,\\n        } } else error.InvalidCoordinateArity,\\n    };\\n}\\n\\n/// GeneratedTheory lowers one descriptor to an executable theory boundary.\\npub fn GeneratedTheory(comptime d: Descriptor) type {\\n    comptime {\\n        validateDescriptor(d) catch |err| @compileError(@errorName(err));\\n    }\\n    const rules = wickStorage(d);\\n    const zeros = zeroStorage(d);\\n    const fermions = fermionStorage(d.fields, d);\\n    const Config = declare.correlatorConfig(.{\\n        .wick_rules = &rules,\\n        .zero_modes = &zeros,\\n        .fermion_kinds = &fermions,\\n    });\\n\\n    return struct {\\n        /// abi_version is the descriptor ABI accepted by this generated theory.\\n        pub const abi_version = descriptor_abi_version;\\n        /// theory_hash distinguishes generated theories at runtime.\\n        pub const theory_hash = d.theory_hash;\\n        /// descriptor exposes the compact source descriptor for audits.\\n        pub const descriptor = d;\\n        const config = Config{};\\n\\n        /// Context owns symbols and a single mutable operator list.\\n        pub const Context = struct {\\n            allocator: std.mem.Allocator,\\n            symbols: std.ArrayList([]u8) = .empty,\\n            labels: std.ArrayList(kernel.Call.LabelValue) = .empty,\\n            operators_list: std.ArrayList(kernel.Call.LocalOp) = .empty,\\n            frozen_labels: []kernel.Call.LabelValue = &.{},\\n            frozen_ops: []kernel.Call.LocalOp = &.{},\\n            label_store: kernel.Call.LabelStore = .{ .values = &.{} },\\n            next_normal_order_group: normal_ordering.Group = normal_ordering.first,\\n\\n            /// init creates an empty generated-theory context.\\n            pub fn init(allocator: std.mem.Allocator) Context {\\n                return .{ .allocator = allocator };\\n            }\\n\\n            /// deinit releases context-owned buffers.\\n            pub fn deinit(self: *Context) void {\\n                for (self.symbols.items) |name| self.allocator.free(name);\\n                self.symbols.deinit(self.allocator);\\n                self.labels.deinit(self.allocator);\\n                self.operators_list.deinit(self.allocator);\\n                self.allocator.free(self.frozen_labels);\\n                self.allocator.free(self.frozen_ops);\\n            }\\n\\n            /// intern returns a stable context-local symbol id for a name.\\n            pub fn intern(self: *Context, name: []const u8) !u32 {\\n                for (self.symbols.items, 1..) |stored, index| {\\n                    if (std.mem.eql(u8, stored, name)) return @intCast(index);\\n                }\\n                const copy = try self.allocator.dupe(u8, name);\\n                try self.symbols.append(self.allocator, copy);\\n                return @intCast(self.symbols.items.len);\\n            }\\n\\n            /// symbolName resolves a context-local symbol id.\\n            pub fn symbolName(self: *const Context, symbol: u32) ?[]const u8 {\\n                if (symbol == 0 or symbol > self.symbols.items.len) return null;\\n                return self.symbols.items[symbol - 1];\\n            }\\n\\n            /// insertField appends one generated field occurrence.\\n            pub fn insertField(self: *Context, field_id: Id, coords: []const u32, label_symbols: []const u32) !void {\\n                if (field_id >= d.fields.len) return error.DanglingField;\\n                const field = d.fields[field_id];\\n                if (label_symbols.len != field.labels.len) return error.InvalidFieldArity;\\n                const label_start = self.labels.items.len;\\n                for (label_symbols) |symbol| {\\n                    try self.labels.append(self.allocator, .{ .symbol = symbol });\\n                }\\n                try self.operators_list.append(self.allocator, .{\\n                    .insertion = try insertionFrom(field.insertion, coords),\\n                    .kind = kindId(d, field_id),\\n                    .labels = @intCast(label_start),\\n                });\\n            }\\n\\n            /// normalOrderLast tags the last count insertions as one normal product.\\n            pub fn normalOrderLast(self: *Context, count: usize) !void {\\n                try normal_ordering.tagLast(self.operators_list.items, count, &self.next_normal_order_group);\\n            }\\n\\n            /// freeze returns the current immutable operator sequence.\\n            pub fn freeze(self: *Context) !kernel.Call.MultiOp {\\n                self.allocator.free(self.frozen_labels);\\n                self.allocator.free(self.frozen_ops);\\n                self.frozen_labels = try self.labels.toOwnedSlice(self.allocator);\\n                self.frozen_ops = try self.operators_list.toOwnedSlice(self.allocator);\\n                self.label_store = .{ .values = self.frozen_labels };\\n                return .{ .operators = self.frozen_ops, .labels = &self.label_store };\\n            }\\n        };\\n\\n        /// descriptorAbiVersion returns the generated descriptor ABI version.\\n        pub fn descriptorAbiVersion() u32 {\\n            return abi_version;\\n        }\\n\\n        /// theoryHash returns the generated theory hash.\\n        pub fn theoryHash() u32 {\\n            return theory_hash;\\n        }\\n\\n        /// scalarAtomParameterName resolves a descriptor parameter scalar atom.\\n        pub fn scalarAtomParameterName(atom: u32) ?[]const u8 {\\n            inline for (d.parameters) |parameter| {\\n                if (parameterAtom(d, parameter.id) == atom) return d.symbols[parameter.symbol];\\n            }\\n            return null;\\n        }\\n\\n        /// contextCreate allocates a generated-theory context.\\n        pub fn contextCreate(allocator: std.mem.Allocator) !*Context {\\n            const ctx = try allocator.create(Context);\\n            ctx.* = Context.init(allocator);\\n            return ctx;\\n        }\\n\\n        /// contextDestroy releases a generated-theory context.\\n        pub fn contextDestroy(ctx: *Context) void {\\n            const allocator = ctx.allocator;\\n            ctx.deinit();\\n            allocator.destroy(ctx);\\n        }\\n\\n        /// symbolIntern interns one runtime symbol name.\\n        pub fn symbolIntern(ctx: *Context, name: []const u8) !u32 {\\n            return ctx.intern(name);\\n        }\\n\\n        /// fieldInsert appends one field occurrence.\\n        pub fn fieldInsert(ctx: *Context, field_id: Id, coords: []const u32, label_symbols: []const u32) !void {\\n            return ctx.insertField(field_id, coords, label_symbols);\\n        }\\n\\n        /// normalOrdering tags the last count insertions as one normal product.\\n        pub fn normalOrdering(ctx: *Context, count: usize) !void {\\n            return ctx.normalOrderLast(count);\\n        }\\n\\n        /// operatorListFreeze freezes the current operator list.\\n        pub fn operatorListFreeze(ctx: *Context) !kernel.Call.MultiOp {\\n            return ctx.freeze();\\n        }\\n\\n        /// correlatorCount returns the number of accepted streamed branches.\\n        pub fn correlatorCount(ops: kernel.Call.MultiOp) !usize {\\n            var sink = CountSink{};\\n            try shared.streamCorrelator(&config, ops, &sink);\\n            return sink.count;\\n        }\\n\\n        /// correlatorRun streams result events through a caller-provided function.\\n        pub fn correlatorRun(ops: kernel.Call.MultiOp, payload: *anyopaque, stream: StreamFn) !void {\\n            var sink = EventSink{ .payload = payload, .stream = stream };\\n            try sink.emit(.{ .kind = .sum_term_begin });\\n            try shared.streamCorrelator(&config, ops, &sink);\\n        }\\n    };\\n}\\n"},{"id":"20260608T234500.000000","title":"Lisp Descriptor Emitter","tags":null,"content":"This node is the Lisp side of the descriptor boundary\\nGenerated Descriptor Boundary.  It has three jobs:\\nstore normalized row data, print deterministic Zig descriptor tables, and wrap\\nthe generated C ABI for the REPL.  The descriptor rows are compile-time data;\\nruntime expression construction only touches the C ABI records returned by Zig.\\n(defpackage #:string-code.cft.descriptor\\n  (:use #:cl)\\n  (:export\\n   #:make-theory\\n   #:add-symbol\\n   #:add-parameter\\n   #:add-quantum-number\\n   #:add-field-quantum-number\\n   #:add-surface\\n   #:add-field\\n   #:add-metadata\\n   #:add-wick-rule\\n   #:add-zero-mode\\n   #:make-wick-term\\n   #:emit-zig-descriptor\\n   #:write-build-manifest\\n   #:load-generated-library\\n   #:make-runtime-context\\n   #:destroy-runtime-context\\n   #:set-runtime-constant\\n   #:intern-runtime-symbol\\n   #:insert-runtime-field\\n   #:normal-order-runtime-field\\n   #:freeze-runtime-operators\\n   #:run-correlator\\n   #:count-correlator\\n   #:correlator-expression\\n   #:collect-correlator))\\n\\n(in-package #:string-code.cft.descriptor)\\nThe IR is plain row data.  Constructors append rows in declaration order and\\nthe emitter sorts id-bearing tables before printing.  Quantum-number schema\\nrows are separate from field assignments so the next basis-generator pass can\\nscan charges and irreps without reading Wick rules.\\nSymbols are stored once per theory.  Every DSL symbol is normalized to a small\\ndescriptor string at this boundary, so generated Zig never sees Lisp packages.\\n(defstruct (theory (:constructor %make-theory))\\n  name\\n  hash\\n  (symbols (make-array 0 :adjustable t :fill-pointer 0))\\n  (parameters nil)\\n  (quantum-numbers nil)\\n  (field-quantum-numbers nil)\\n  (surfaces nil)\\n  (fields nil)\\n  (metadata nil)\\n  (wick-rules nil)\\n  (zero-modes nil)\\n  (result-symbols nil))\\n\\n(defstruct parameter id symbol role)\\n(defstruct quantum-number id symbol kind group-symbol)\\n(defstruct field-quantum-number field quantum-number value)\\n(defstruct surface id kind coordinate-model modular-parameter)\\n(defstruct label-schema id role symbol)\\n(defstruct field id symbol insertion labels statistics zero-mode weight anti-weight)\\n(defstruct wick-term scalars coordinates tensors actions residuals)\\n(defstruct wick-rule surface left right terms)\\n(defstruct zero-mode surface kind consumes normalization two-pi-power)\\n(defstruct (runtime-context (:constructor %make-runtime-context))\\n  handle\\n  theory-id\\n  constants\\n  (scalar-atom-names (make-hash-table))\\n  scalar-monomials\\n  (symbols (make-array 0 :adjustable t :fill-pointer 0)))\\n(defun descriptor-symbol-name (value)\\n  (etypecase value\\n    (string value)\\n    (symbol (string-downcase (string value)))))\\n\\n(defun add-symbol (theory name)\\n  (let ((symbols (theory-symbols theory))\\n        (text (descriptor-symbol-name name)))\\n    (or (loop for index below (length symbols)\\n              when (string= (aref symbols index) text)\\n                return index)\\n        (vector-push-extend text symbols))))\\n\\n(defun make-theory (name hash)\\n  (let ((theory (%make-theory :name name :hash hash)))\\n    (add-symbol theory name)\\n    theory))\\n\\n(defun add-parameter (theory symbol role)\\n  (let ((id (length (theory-parameters theory))))\\n    (push (make-parameter :id id :symbol (add-symbol theory symbol) :role role)\\n          (theory-parameters theory))\\n    id))\\n\\n(defun add-quantum-number (theory symbol kind &key group)\\n  (let ((id (length (theory-quantum-numbers theory))))\\n    (push (make-quantum-number\\n           :id id\\n           :symbol (add-symbol theory (descriptor-symbol-name symbol))\\n           :kind kind\\n           :group-symbol (when group (add-symbol theory (descriptor-symbol-name group))))\\n          (theory-quantum-numbers theory))\\n    id))\\n\\n(defun add-field-quantum-number (theory field quantum-number value)\\n  (push (make-field-quantum-number\\n         :field field\\n         :quantum-number quantum-number\\n         :value value)\\n        (theory-field-quantum-numbers theory)))\\n\\n(defun add-surface (theory kind coordinate-model &key modular-parameter)\\n  (let ((id (length (theory-surfaces theory))))\\n    (push (make-surface :id id :kind kind :coordinate-model coordinate-model\\n                        :modular-parameter modular-parameter)\\n          (theory-surfaces theory))\\n    id))\\nField labels infer roles from declaration tags unless the caller passes a full\\nlabel row.  This is intentionally small and deterministic.\\n(defun role-of-label (label)\\n  (etypecase label\\n    (label-schema (label-schema-role label))\\n    (cons (second label))\\n    (symbol :vector-index)))\\n\\n(defun label-name (label)\\n  (etypecase label\\n    (label-schema (label-schema-symbol label))\\n    (cons (first label))\\n    (symbol label)))\\n\\n(defun make-label-rows (theory labels)\\n  (loop for label in labels\\n        for id from 0\\n        collect (make-label-schema\\n                 :id id\\n                 :role (role-of-label label)\\n                 :symbol (add-symbol theory (string-downcase (string (label-name label)))))))\\n\\n(defun add-field (theory symbol insertion labels statistics &key zero-mode weight anti-weight)\\n  (let ((id (length (theory-fields theory))))\\n    (push (make-field :id id\\n                      :symbol (add-symbol theory symbol)\\n                      :insertion insertion\\n                      :labels (make-label-rows theory labels)\\n                      :statistics statistics\\n                      :zero-mode zero-mode\\n                      :weight weight\\n                      :anti-weight anti-weight)\\n          (theory-fields theory))\\n    id))\\n(defun metadata-symbol-id (theory value)\\n  (etypecase value\\n    (integer value)\\n    (string (add-symbol theory value))\\n    (symbol (add-symbol theory (string-downcase (string value))))))\\n\\n(declaim (ftype function add-metadata))\\n\\n(defun normalized-metadata-form (theory form)\\n  (etypecase form\\n    (integer form)\\n    (cons\\n     (case (first form)\\n       (:rational form)\\n       (:parameter form)\\n       (:field-label form)\\n       (:add (list :add\\n                   (add-metadata theory (second form))\\n                   (add-metadata theory (third form))))\\n       (:mul (list :mul\\n                   (add-metadata theory (second form))\\n                   (add-metadata theory (third form))))\\n       (:bilinear (list :bilinear\\n                        (add-metadata theory (second form))\\n                        (add-metadata theory (third form))\\n                        (metadata-symbol-id theory (fourth form))))\\n       (otherwise (error \\"Unknown metadata form ~S.\\" form))))))\\n\\n(defun metadata-row-id (theory row)\\n  (let ((count (length (theory-metadata theory))))\\n    (loop for item in (theory-metadata theory)\\n          for index from 0\\n          when (equal item row)\\n            return (- count index 1))))\\n\\n(defun add-metadata (theory form)\\n  (let ((row (normalized-metadata-form theory form)))\\n    (if (integerp row)\\n        row\\n        (or (metadata-row-id theory row)\\n            (let ((id (length (theory-metadata theory))))\\n              (push row (theory-metadata theory))\\n              id)))))\\n\\n(defun add-wick-rule (theory surface left right terms)\\n  (push (make-wick-rule :surface surface :left left :right right :terms terms)\\n        (theory-wick-rules theory)))\\n\\n(defun add-zero-mode (theory surface kind consumes &key (normalization :one) (two-pi-power 0))\\n  (push (make-zero-mode :surface surface :kind kind :consumes consumes\\n                        :normalization normalization :two-pi-power two-pi-power)\\n        (theory-zero-modes theory)))\\n\\n(defun by-id (items reader)\\n  (sort (copy-list items) #\'< :key reader))\\nThe emitter prints only primitive descriptor data.  It does not branch on\\ntheory or field names; all semantic choices have already been lowered into\\ndense ids and compact tagged rows.\\n(defun zig-keyword (value)\\n  (substitute #\\\\_ #\\\\- (string-downcase (string value))))\\n\\n(defun emit-id-list (stream ids)\\n  (format stream \\"&.{\\")\\n  (loop for id in ids for first = t then nil\\n        do (format stream \\"~:[, ~;~]~D\\" first id))\\n  (format stream \\"}\\"))\\n\\n(defun emit-symbols (stream theory var)\\n  (format stream \\"const ~A = [_][]const u8{ \\" var)\\n  (loop for index below (length (theory-symbols theory))\\n        for first = t then nil\\n        do (format stream \\"~:[, ~;~]~S\\" first (aref (theory-symbols theory) index)))\\n  (format stream \\" };~%\\"))\\n\\n(defun emit-label (stream label)\\n  (format stream \\".{ .id = ~D, .role = .~A, .symbol = ~D }\\"\\n          (label-schema-id label)\\n          (zig-keyword (label-schema-role label))\\n          (label-schema-symbol label)))\\n(defun emit-field (stream field)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .insertion = .~A, .labels = &.{\\"\\n          (field-id field) (field-symbol field) (zig-keyword (field-insertion field)))\\n  (loop for label in (field-labels field) for first = t then nil\\n        do (progn\\n             (unless first (format stream \\", \\"))\\n             (emit-label stream label)))\\n  (format stream \\"}, .statistics = .~A\\"\\n          (zig-keyword (field-statistics field)))\\n  (when (field-zero-mode field)\\n    (format stream \\", .zero_mode_consumable = true\\"))\\n  (when (field-weight field)\\n    (format stream \\", .weight = ~D\\" (field-weight field)))\\n  (when (field-anti-weight field)\\n    (format stream \\", .anti_weight = ~D\\" (field-anti-weight field)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-surface (stream surface)\\n  (format stream \\".{ .id = ~D, .kind = .~A, .coordinate_model = .~A\\"\\n          (surface-id surface)\\n          (zig-keyword (surface-kind surface))\\n          (zig-keyword (surface-coordinate-model surface)))\\n  (when (surface-modular-parameter surface)\\n    (format stream \\", .modular_parameter = ~D\\" (surface-modular-parameter surface)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-parameter (stream parameter)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .role = .~A }\\"\\n          (parameter-id parameter)\\n          (parameter-symbol parameter)\\n          (zig-keyword (parameter-role parameter))))\\n\\n(defun emit-quantum-number (stream number)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .kind = .~A\\"\\n          (quantum-number-id number)\\n          (quantum-number-symbol number)\\n          (zig-keyword (quantum-number-kind number)))\\n  (when (quantum-number-group-symbol number)\\n    (format stream \\", .group_symbol = ~D\\" (quantum-number-group-symbol number)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-rational-row (stream numerator denominator)\\n  (format stream \\".{ .numerator = ~D, .denominator = ~D }\\" numerator denominator))\\n\\n(defun emit-quantum-value (stream value)\\n  (etypecase value\\n    (integer\\n     (format stream \\".{ .integer = ~D }\\" value))\\n    (rational\\n     (format stream \\".{ .rational = \\")\\n     (emit-rational-row stream (numerator value) (denominator value))\\n     (format stream \\" }\\"))\\n    (cons\\n     (ecase (first value)\\n       (:symbol\\n        (format stream \\".{ .symbol = ~D }\\" (second value)))\\n       (:rational\\n        (format stream \\".{ .rational = \\")\\n        (emit-rational-row stream (second value) (third value))\\n        (format stream \\" }\\"))))))\\n\\n(defun emit-field-quantum-number (stream item)\\n  (format stream \\".{ .field = ~D, .quantum_number = ~D, .value = \\"\\n          (field-quantum-number-field item)\\n          (field-quantum-number-quantum-number item))\\n  (emit-quantum-value stream (field-quantum-number-value item))\\n  (format stream \\" }\\"))\\n\\n(defun emit-metadata (stream form)\\n  (case (first form)\\n    (:rational\\n     (format stream \\".{ .rational = \\")\\n     (emit-rational-row stream (second form) (third form))\\n     (format stream \\" }\\"))\\n    (:parameter\\n     (format stream \\".{ .parameter = ~D }\\" (second form)))\\n    (:field-label\\n     (format stream \\".{ .field_label = .{ .field = ~D, .label = ~D } }\\"\\n             (second form) (third form)))\\n    (:add\\n     (format stream \\".{ .add = .{ .left = ~D, .right = ~D } }\\"\\n             (second form) (third form)))\\n    (:mul\\n     (format stream \\".{ .mul = .{ .left = ~D, .right = ~D } }\\"\\n             (second form) (third form)))\\n    (:bilinear\\n     (format stream \\".{ .bilinear = .{ .left = ~D, .right = ~D, .form_symbol = ~D } }\\"\\n             (second form) (third form) (fourth form)))\\n    (otherwise (error \\"Unknown metadata form ~S.\\" form))))\\n\\n(defun side-name (value)\\n  (ecase value\\n    (:left \\"left\\")\\n    (:right \\"right\\")))\\n\\n(defun coordinate-slot-name (value)\\n  (ecase value\\n    (:position \\"position\\")\\n    (:holomorphic \\"holomorphic\\")\\n    (:antiholomorphic \\"antiholomorphic\\")))\\n\\n(defun emit-coordinate-ref (stream value)\\n  (etypecase value\\n    (symbol\\n     (format stream \\".{ .side = .~A, .slot = .position }\\" (side-name value)))\\n    (cons\\n     (format stream \\".{ .side = .~A, .slot = .~A }\\"\\n             (side-name (first value))\\n             (coordinate-slot-name (second value))))))\\n\\n(defun emit-label-ref (stream value)\\n  (format stream \\".{ .side = .~A, .slot = ~D }\\"\\n          (side-name (first value)) (second value)))\\n\\n(defun emit-zig-list (stream values printer)\\n  (format stream \\"&.{\\")\\n  (loop for value in values for first = t then nil\\n        do (progn\\n             (unless first (format stream \\", \\"))\\n             (funcall printer stream value)))\\n  (format stream \\"}\\"))\\n\\n(defun emit-scalar-factor (stream form)\\n  (etypecase form\\n    (symbol\\n     (ecase form\\n       (:one (format stream \\".one\\"))))\\n    (cons\\n     (ecase (first form)\\n       (:rational\\n        (format stream \\".{ .rational = \\")\\n        (emit-rational-row stream (second form) (third form))\\n        (format stream \\" }\\"))\\n       (:parameter\\n        (format stream \\".{ .parameter = ~D }\\" (second form)))\\n       (:neg-parameter-half\\n        (format stream \\".{ .neg_parameter_half = ~D }\\" (second form)))\\n       (:neg-i-parameter-half\\n        (format stream \\".{ .neg_i_parameter_half = ~D }\\" (second form)))\\n       (:parameter-half\\n        (format stream \\".{ .parameter_half = ~D }\\" (second form)))))))\\n\\n(defun derive-left-p (value)\\n  (and (consp value) (member :derive-left value)))\\n\\n(defun derive-right-p (value)\\n  (and (consp value) (member :derive-right value)))\\n\\n(defun emit-coordinate-factor (theory stream form)\\n  (ecase (first form)\\n    (:difference-power\\n     (format stream \\".{ .difference_power = .{ .left = \\")\\n     (emit-coordinate-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\", .exponent = ~D\\" (fourth form))\\n     (when (derive-left-p form)\\n       (format stream \\", .derive_left = true\\"))\\n     (when (derive-right-p form)\\n       (format stream \\", .derive_right = true\\"))\\n     (format stream \\" } }\\"))\\n    (:named-kernel\\n     (format stream \\".{ .named_kernel = .{ .symbol = ~D, .left = \\"\\n             (metadata-symbol-id theory (second form)))\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (fourth form))\\n     (when (derive-left-p form)\\n       (format stream \\", .derive_left = true\\"))\\n     (when (derive-right-p form)\\n       (format stream \\", .derive_right = true\\"))\\n     (format stream \\" } }\\"))\\n    (:green-exponential\\n     (format stream \\".{ .green_exponential = .{ .left = \\")\\n     (emit-coordinate-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\" } }\\"))))\\n\\n(defun emit-tensor-factor (stream form)\\n  (ecase (first form)\\n    (:metric\\n     (format stream \\".{ .metric = .{ .left = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))\\n    (:momentum-index\\n     (format stream \\".{ .momentum_index = .{ .momentum = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .index = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))\\n    (:momentum-pair\\n     (format stream \\".{ .momentum_pair = .{ .left = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))))\\n\\n(defun emit-side (stream side)\\n  (format stream \\".~A\\" (side-name side)))\\n\\n(defun emit-wick-term (theory stream term)\\n  (format stream \\".{ .scalars = \\")\\n  (emit-zig-list stream (or (wick-term-scalars term) \'(:one)) #\'emit-scalar-factor)\\n  (format stream \\", .coordinates = \\")\\n  (emit-zig-list stream (wick-term-coordinates term)\\n                 (lambda (out form) (emit-coordinate-factor theory out form)))\\n  (format stream \\", .tensors = \\")\\n  (emit-zig-list stream (wick-term-tensors term) #\'emit-tensor-factor)\\n  (format stream \\", .residuals = \\")\\n  (emit-zig-list stream (wick-term-residuals term) #\'emit-side)\\n  (format stream \\" }\\"))\\n\\n(defun emit-wick-rule (stream rule term-var)\\n  (format stream \\".{ .surface = ~D, .left = ~D, .right = ~D, .terms = &~A }\\"\\n          (wick-rule-surface rule)\\n          (wick-rule-left rule)\\n          (wick-rule-right rule)\\n          term-var))\\n\\n(defun emit-zero-mode (stream rule)\\n  (format stream \\".{ .surface = ~D, .kind = .~A, .consumes = \\"\\n          (zero-mode-surface rule)\\n          (zig-keyword (zero-mode-kind rule)))\\n  (emit-id-list stream (zero-mode-consumes rule))\\n  (unless (eq (zero-mode-normalization rule) :one)\\n    (format stream \\", .normalization = \\")\\n    (emit-scalar-factor stream (zero-mode-normalization rule)))\\n  (unless (zerop (zero-mode-two-pi-power rule))\\n    (format stream \\", .two_pi_power = ~D\\" (zero-mode-two-pi-power rule)))\\n  (format stream \\" }\\"))\\n(defun emit-row-array (stream type var rows printer)\\n  (format stream \\"const ~A = [_]d.~A{~%\\" var type)\\n  (dolist (row rows)\\n    (format stream \\"    \\")\\n    (funcall printer stream row)\\n    (format stream \\",~%\\"))\\n  (format stream \\"};~%\\"))\\n\\n(defun prepare-coordinate-symbols (theory form)\\n  (when (eq (first form) :named-kernel)\\n    (metadata-symbol-id theory (second form))))\\n\\n(defun prepare-emitter-symbols (theory)\\n  (dolist (rule (theory-wick-rules theory))\\n    (dolist (term (wick-rule-terms rule))\\n      (dolist (factor (wick-term-coordinates term))\\n        (prepare-coordinate-symbols theory factor)))))\\n\\n(defun emit-wick-term-arrays (stream theory rules prefix)\\n  (loop for rule in rules\\n        for index from 0\\n        for var = (format nil \\"~A_terms_~D\\" prefix index)\\n        collect var\\n        do (emit-row-array stream \\"WickTerm\\" var (wick-rule-terms rule)\\n                           (lambda (out term) (emit-wick-term theory out term)))))\\n\\n(defun emit-zig-descriptor (theory stream &key (descriptor-name \\"generated_descriptor\\") (prefix \\"generated\\"))\\n  (prepare-emitter-symbols theory)\\n  (let ((symbols-var (format nil \\"~A_symbols\\" prefix))\\n        (parameters-var (format nil \\"~A_parameters\\" prefix))\\n        (quantum-numbers-var (format nil \\"~A_quantum_numbers\\" prefix))\\n        (field-quantum-numbers-var (format nil \\"~A_field_quantum_numbers\\" prefix))\\n        (surfaces-var (format nil \\"~A_surfaces\\" prefix))\\n        (fields-var (format nil \\"~A_fields\\" prefix))\\n        (metadata-var (format nil \\"~A_metadata\\" prefix))\\n        (wick-var (format nil \\"~A_wick\\" prefix))\\n        (zero-modes-var (format nil \\"~A_zero_modes\\" prefix)))\\n  (emit-symbols stream theory symbols-var)\\n  (emit-row-array stream \\"Parameter\\" parameters-var\\n                  (by-id (theory-parameters theory) #\'parameter-id)\\n                  #\'emit-parameter)\\n  (emit-row-array stream \\"QuantumNumber\\" quantum-numbers-var\\n                  (by-id (theory-quantum-numbers theory) #\'quantum-number-id)\\n                  #\'emit-quantum-number)\\n  (emit-row-array stream \\"FieldQuantumNumber\\" field-quantum-numbers-var\\n                  (reverse (theory-field-quantum-numbers theory))\\n                  #\'emit-field-quantum-number)\\n  (emit-row-array stream \\"Surface\\" surfaces-var\\n                  (by-id (theory-surfaces theory) #\'surface-id)\\n                  #\'emit-surface)\\n  (emit-row-array stream \\"Field\\" fields-var\\n                  (by-id (theory-fields theory) #\'field-id)\\n                  #\'emit-field)\\n  (emit-row-array stream \\"MetadataExpr\\" metadata-var\\n                  (reverse (theory-metadata theory))\\n                  #\'emit-metadata)\\n  (let* ((rules (reverse (theory-wick-rules theory)))\\n         (term-vars (emit-wick-term-arrays stream theory rules prefix)))\\n    (format stream \\"const ~A = [_]d.WickRule{~%\\" wick-var)\\n    (loop for rule in rules\\n          for term-var in term-vars\\n          do (progn\\n               (format stream \\"    \\")\\n               (emit-wick-rule stream rule term-var)\\n               (format stream \\",~%\\")))\\n    (format stream \\"};~%\\"))\\n  (emit-row-array stream \\"ZeroModeRule\\" zero-modes-var\\n                  (reverse (theory-zero-modes theory))\\n                  #\'emit-zero-mode)\\n  (format stream \\"pub const ~A = d.Descriptor{~%\\" descriptor-name)\\n  (format stream \\"    .theory_symbol = 0,~%\\")\\n  (format stream \\"    .theory_hash = ~D,~%\\" (theory-hash theory))\\n  (format stream \\"    .symbols = &~A,~%\\" symbols-var)\\n  (format stream \\"    .parameters = &~A,~%\\" parameters-var)\\n  (format stream \\"    .quantum_numbers = &~A,~%\\" quantum-numbers-var)\\n  (format stream \\"    .field_quantum_numbers = &~A,~%\\" field-quantum-numbers-var)\\n  (format stream \\"    .surfaces = &~A,~%\\" surfaces-var)\\n  (format stream \\"    .fields = &~A,~%\\" fields-var)\\n  (format stream \\"    .metadata = &~A,~%\\" metadata-var)\\n  (format stream \\"    .wick_rules = &~A,~%\\" wick-var)\\n  (format stream \\"    .zero_modes = &~A,~%\\" zero-modes-var)\\n  (format stream \\"};~%\\")))\\nThe manifest is deliberately small: source hash, generated path, and library\\npath.  Runtime calls require a loaded library and otherwise fail before any\\nexpression object is built.  CFFI definitions are installed lazily because a\\nuser can inspect presets without loading CFFI or a generated shared object.\\n(defun write-build-manifest (path &key source-hash zig-path library-path)\\n  (with-open-file (stream path :direction :output)\\n    (format stream \\"(:source-hash ~S :zig-path ~S :library-path ~S)~%\\"\\n            source-hash zig-path library-path)))\\n\\n(defvar *abi-bound* nil)\\n(defvar *abi-callback-pointer* nil)\\n(defvar *abi-chunk-callback-pointer* nil)\\n(defvar *abi-sinks* (make-hash-table))\\n(defvar *next-abi-sink-id* 0)\\n(defvar *event-size* 0)\\n(defvar *factor-size* 0)\\n(defvar *name-size* 0)\\n(defvar *name-ptr-offset* 0)\\n(defvar *name-len-offset* 0)\\n(defvar *term-size* 0)\\n(defparameter *event-kinds*\\n  #(:sum-term-begin :sum-term-end :wick-term-begin :wick-term-end\\n    :scalar :coordinate :tensor :zero-mode :residual-operator))\\n\\n(declaim\\n (ftype function\\n        set-mem-aref* mem-ref* foreign-slot-value* null-pointer-p*\\n        pointer-address* inc-pointer* foreign-type-size* event-pointer-at\\n        name-pointer-at term-pointer-at term-expression\\n        %abi-last-error %abi-context-create\\n        %abi-context-destroy %abi-scalar-atom-name\\n        %abi-symbol-intern %abi-field-insert\\n        %abi-normal-ordering %abi-operator-list-freeze\\n        %abi-correlator-count %abi-correlator-run\\n        %abi-correlator-run-buffered %abi-correlator-expression-records\\n        %abi-expression-buffer-free))\\n\\n(declaim\\n (ftype function\\n        factor-name-token-by-id factor-coordinate-expression-values\\n        factor-tensor-expression-values factor-scalar-expression-values\\n        factor-zero-mode-expression-values factor-expression-values\\n        term-product-expression))\\n\\n(defun cffi-package ()\\n  (or (find-package \'#:cffi)\\n      (error \\"CFFI is required to use generated libraries.\\")))\\n\\n(defun cffi-symbol (name)\\n  (intern name (cffi-package)))\\n\\n(defun foreign-string-to-lisp* (&rest args)\\n  (apply (symbol-function (cffi-symbol \\"FOREIGN-STRING-TO-LISP\\")) args))\\n\\n(defun foreign-alloc* (&rest args)\\n  (apply (symbol-function (cffi-symbol \\"FOREIGN-ALLOC\\")) args))\\n\\n(defun foreign-free* (pointer)\\n  (funcall (symbol-function (cffi-symbol \\"FOREIGN-FREE\\")) pointer))\\n\\n(defun event-from-pointer (pointer)\\n  (let* ((type \'(:struct generated-event))\\n         (kind (foreign-slot-value* pointer type \'kind))\\n         (name-pointer (foreign-slot-value* pointer type \'name-ptr))\\n         (name-length (foreign-slot-value* pointer type \'name-len))\\n         (name (and (> name-length 0)\\n                    (not (null-pointer-p* name-pointer))\\n                    (foreign-string-to-lisp* name-pointer :count name-length))))\\n    (list :kind (aref *event-kinds* kind)\\n          :a (foreign-slot-value* pointer type \'a)\\n          :b (foreign-slot-value* pointer type \'b)\\n          :c (foreign-slot-value* pointer type \'c)\\n          :d (foreign-slot-value* pointer type \'d)\\n          :name name)))\\n\\n(defun ensure-abi-bindings ()\\n  (unless *abi-bound*\\n    (let ((defcstruct (cffi-symbol \\"DEFCSTRUCT\\"))\\n          (defcfun (cffi-symbol \\"DEFCFUN\\"))\\n          (defcallback (cffi-symbol \\"DEFCALLBACK\\"))\\n          (callback (cffi-symbol \\"CALLBACK\\"))\\n          (inc-pointer (cffi-symbol \\"INC-POINTER\\"))\\n          (foreign-type-size (cffi-symbol \\"FOREIGN-TYPE-SIZE\\"))\\n\\t          (foreign-slot-offset (cffi-symbol \\"FOREIGN-SLOT-OFFSET\\"))\\n\\t          (with-foreign-slots (cffi-symbol \\"WITH-FOREIGN-SLOTS\\"))\\n\\t          (mem-aref (cffi-symbol \\"MEM-AREF\\"))\\n          (mem-ref (cffi-symbol \\"MEM-REF\\"))\\n          (foreign-slot-value (cffi-symbol \\"FOREIGN-SLOT-VALUE\\"))\\n          (null-pointer-p (cffi-symbol \\"NULL-POINTER-P\\"))\\n          (pointer-address (cffi-symbol \\"POINTER-ADDRESS\\")))\\n      (eval `(,defcstruct generated-event\\n               (kind :uint8)\\n               (a :uint32)\\n               (b :uint32)\\n               (c :uint32)\\n               (d :int32)\\n               (name-ptr :pointer)\\n               (name-len :size)))\\n      (eval `(,defcstruct generated-factor\\n               (kind :uint8)\\n               (a :uint32)\\n               (b :uint32)\\n               (c :uint32)\\n               (d :int32)\\n               (name-id :uint32)))\\n      (eval `(,defcstruct generated-name\\n               (ptr :pointer)\\n               (len :size)))\\n      (eval `(,defcstruct generated-term\\n               (first-factor :size)\\n               (factor-count :size)))\\n      (eval `(defun set-mem-aref* (pointer type index value)\\n               (setf (,mem-aref pointer type index) value)))\\n      (eval `(defun mem-ref* (pointer type)\\n               (,mem-ref pointer type)))\\n      (eval `(defun foreign-slot-value* (pointer type slot)\\n               (,foreign-slot-value pointer type slot)))\\n      (eval `(defun null-pointer-p* (pointer)\\n               (,null-pointer-p pointer)))\\n      (eval `(defun pointer-address* (pointer)\\n               (,pointer-address pointer)))\\n      (eval `(defun inc-pointer* (pointer offset)\\n               (,inc-pointer pointer offset)))\\n      (eval `(defun foreign-type-size* (type)\\n               (,foreign-type-size type)))\\n      (eval `(defun event-pointer-at (events index)\\n               (,inc-pointer events (* index *event-size*))))\\n      (eval `(defun name-pointer-at (names index)\\n               (,inc-pointer names (* index *name-size*))))\\n      (eval `(defun term-pointer-at (terms index)\\n               (,inc-pointer terms (* index *term-size*))))\\n      (setf *event-size* (eval `(,foreign-type-size \'(:struct generated-event)))\\n            *factor-size* (eval `(,foreign-type-size \'(:struct generated-factor)))\\n            *name-size* (eval `(,foreign-type-size \'(:struct generated-name)))\\n            *name-ptr-offset* (eval `(,foreign-slot-offset \'(:struct generated-name) \'ptr))\\n            *name-len-offset* (eval `(,foreign-slot-offset \'(:struct generated-name) \'len))\\n            *term-size* (eval `(,foreign-type-size \'(:struct generated-term))))\\n      (eval `(defun term-expression (builder factors names term)\\n               (declare (optimize (speed 3) (safety 1) (debug 0)))\\n               (,with-foreign-slots ((first-factor factor-count) term (:struct generated-term))\\n                 (let ((items nil)\\n                       (scalars nil)\\n                       (greens nil)\\n                       (momentum-pairs nil)\\n                       (others nil))\\n                   (loop for index from first-factor below (+ first-factor factor-count)\\n                         for factor = (,inc-pointer factors (* index *factor-size*))\\n                         do (,with-foreign-slots ((kind a b c d name-id) factor (:struct generated-factor))\\n                              (let ((expr (factor-expression-values builder names kind a b c d name-id)))\\n                                (when (and expr (not (eql expr 1)))\\n                                  (push expr items)\\n                                  (cond\\n                                    ((= kind 4) (push expr scalars))\\n                                    ((and (= kind 5) (exp-green-expression-p expr))\\n                                     (push expr greens))\\n                                    ((and (= kind 6) (momentum-pair-expression-p expr))\\n                                     (push expr momentum-pairs))\\n                                    (t (push expr others)))))))\\n                   (term-product-expression items scalars greens momentum-pairs others)))))\\n      (eval `(,defcfun (\\"sc_generated_last_error\\" %abi-last-error) :pointer))\\n      (eval `(,defcfun (\\"sc_generated_context_create\\" %abi-context-create) :pointer\\n               (theory-id :uint32)))\\n      (eval `(,defcfun (\\"sc_generated_context_destroy\\" %abi-context-destroy) :void\\n               (context :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_scalar_atom_name\\" %abi-scalar-atom-name) :int\\n               (theory-id :uint32)\\n               (atom :uint32)\\n               (out-name :pointer)\\n               (out-name-len :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_symbol_intern\\" %abi-symbol-intern) :int\\n               (context :pointer)\\n               (name :string)\\n               (name-len :size)\\n               (out-symbol :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_field_insert\\" %abi-field-insert) :int\\n               (context :pointer)\\n               (field-id :uint16)\\n               (coordinates :pointer)\\n               (coordinate-count :size)\\n               (labels :pointer)\\n               (label-count :size)))\\n      (eval `(,defcfun (\\"sc_generated_normal_ordering\\" %abi-normal-ordering) :int\\n               (context :pointer)\\n               (field-count :size)))\\n      (eval `(,defcfun (\\"sc_generated_operator_list_freeze\\" %abi-operator-list-freeze) :int\\n               (context :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_count\\" %abi-correlator-count) :int\\n               (context :pointer)\\n               (out-count :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_run\\" %abi-correlator-run) :int\\n               (context :pointer)\\n               (payload :pointer)\\n               (callback :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_run_buffered\\" %abi-correlator-run-buffered) :int\\n               (context :pointer)\\n               (payload :pointer)\\n               (callback :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_expression_records\\" %abi-correlator-expression-records) :int\\n               (context :pointer)\\n               (out-terms :pointer)\\n               (out-term-count :pointer)\\n               (out-factors :pointer)\\n               (out-factor-count :pointer)\\n               (out-names :pointer)\\n               (out-name-count :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_expression_buffer_free\\" %abi-expression-buffer-free) :void\\n               (terms :pointer)\\n               (term-count :size)\\n               (factors :pointer)\\n               (factor-count :size)\\n               (names :pointer)\\n               (name-count :size)))\\n      (eval `(,defcallback %abi-event-callback :int\\n               ((payload :pointer) (event :pointer))\\n               (let ((sink (gethash (mem-ref* payload :uint64) *abi-sinks*)))\\n                 (when sink\\n                   (funcall sink event))\\n                 0)))\\n      (eval `(,defcallback %abi-event-chunk-callback :int\\n               ((payload :pointer) (events :pointer) (event-count :size))\\n               (let ((sink (gethash (mem-ref* payload :uint64) *abi-sinks*)))\\n                 (when sink\\n                   (loop for index below event-count\\n                         do (funcall sink (event-pointer-at events index))))\\n                 0)))\\n      (setf *abi-callback-pointer* (eval `(,callback %abi-event-callback)))\\n      (setf *abi-chunk-callback-pointer* (eval `(,callback %abi-event-chunk-callback)))\\n      (setf *abi-bound* t))))\\n\\n(defun load-generated-library (path)\\n  (funcall (symbol-function (cffi-symbol \\"LOAD-FOREIGN-LIBRARY\\")) path)\\n  (ensure-abi-bindings)\\n  path)\\n\\n(defun check-abi (status)\\n  (unless (zerop status)\\n    (let ((message (%abi-last-error)))\\n      (error \\"Generated ABI call failed: ~A\\"\\n             (foreign-string-to-lisp* message)))))\\n\\n(defun keyword-name (name)\\n  (intern (string-upcase name) \'#:keyword))\\n\\n(defun constant-name (name)\\n  (etypecase name\\n    (string (keyword-name name))\\n    (symbol (keyword-name (string name)))))\\n\\n(defun normalize-runtime-constants (constants)\\n  (let ((table (make-hash-table)))\\n    (dolist (item constants table)\\n      (etypecase item\\n        (cons\\n         (setf (gethash (constant-name (car item)) table) (cdr item)))\\n        (symbol\\n         (setf (gethash (constant-name item) table) (constant-name item)))))))\\n\\n(defun set-runtime-constant (context name value)\\n  \\"Set one symbolic scalar constant for later expression construction.\\"\\n  (setf (gethash (constant-name name) (runtime-context-constants context)) value\\n        (runtime-context-scalar-monomials context) nil)\\n  value)\\n\\n(defun runtime-constant-value (context name)\\n  (multiple-value-bind (value present)\\n      (gethash name (runtime-context-constants context))\\n    (if present value name)))\\n\\n(defun make-runtime-context (&key library theory-id constants)\\n  (unless theory-id\\n    (error \\"A generated theory id is required.\\"))\\n  (when library\\n    (load-generated-library library))\\n  (ensure-abi-bindings)\\n  (let ((handle (%abi-context-create theory-id)))\\n    (when (null-pointer-p* handle)\\n      (check-abi -1))\\n    (%make-runtime-context :handle handle :theory-id theory-id\\n                           :constants (normalize-runtime-constants constants))))\\n\\n(defun destroy-runtime-context (context)\\n  (when (runtime-context-handle context)\\n    (%abi-context-destroy (runtime-context-handle context))\\n    (setf (runtime-context-handle context) nil)))\\n\\n(defun runtime-handle (context)\\n  (or (runtime-context-handle context)\\n      (error \\"No generated runtime context is active.\\")))\\n\\n(defun remember-runtime-symbol (context id name)\\n  (let ((symbols (runtime-context-symbols context)))\\n    (loop while (< (length symbols) id)\\n          do (vector-push-extend nil symbols))\\n    (setf (aref symbols (1- id)) (keyword-name name))\\n    id))\\n\\n(defun runtime-symbol-name (context id)\\n  (let ((symbols (runtime-context-symbols context)))\\n    (if (and (> id 0) (<= id (length symbols)))\\n        (or (aref symbols (1- id)) `(:symbol ,id))\\n        `(:symbol ,id))))\\n\\n(defun intern-runtime-symbol (context name)\\n  (let ((out (foreign-alloc* :uint32)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-symbol-intern (runtime-handle context) name (length name) out))\\n           (remember-runtime-symbol context (mem-ref* out :uint32) name))\\n      (foreign-free* out))))\\nRuntime insertion is intentionally shallow.  Lisp interns coordinate and label\\nnames once per context, passes only dense ids through C, and lets Zig own the\\noperator-list storage.\\n(defun copy-uint32-values (values)\\n  (let* ((count (length values))\\n         (pointer (foreign-alloc* :uint32 :count (max 1 count))))\\n    (loop for value in values\\n          for index from 0\\n          do (set-mem-aref* pointer :uint32 index value))\\n    (values pointer count)))\\n\\n(defun insert-runtime-field (context field-id coordinates labels)\\n  (multiple-value-bind (coord-pointer coord-count) (copy-uint32-values coordinates)\\n    (multiple-value-bind (label-pointer label-count) (copy-uint32-values labels)\\n      (unwind-protect\\n           (check-abi\\n            (%abi-field-insert (runtime-handle context) field-id coord-pointer coord-count label-pointer label-count))\\n        (foreign-free* label-pointer)\\n        (foreign-free* coord-pointer))))\\n  field-id)\\n\\n(defun normal-order-runtime-field (context count)\\n  (check-abi\\n   (%abi-normal-ordering (runtime-handle context) count))\\n  count)\\n\\n(defun freeze-runtime-operators (context)\\n  (check-abi\\n   (%abi-operator-list-freeze (runtime-handle context)))\\n  :generated)\\n\\n(defun count-correlator (context frozen)\\n  (declare (ignore frozen))\\n  (let ((out (foreign-alloc* :size)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-correlator-count (runtime-handle context) out))\\n           (mem-ref* out :size))\\n      (foreign-free* out))))\\n\\n(defun run-correlator-raw (context sink)\\n  (let* ((sink-id (prog1 *next-abi-sink-id*\\n                    (incf *next-abi-sink-id*)))\\n         (payload (foreign-alloc* :uint64)))\\n    (setf (gethash sink-id *abi-sinks*) sink)\\n    (set-mem-aref* payload :uint64 0 sink-id)\\n    (unwind-protect\\n         (check-abi\\n          (%abi-correlator-run (runtime-handle context) payload *abi-callback-pointer*))\\n      (remhash sink-id *abi-sinks*)\\n      (foreign-free* payload))))\\n\\n(defun run-correlator-buffered (context sink)\\n  (let* ((sink-id (prog1 *next-abi-sink-id*\\n                    (incf *next-abi-sink-id*)))\\n         (payload (foreign-alloc* :uint64)))\\n    (setf (gethash sink-id *abi-sinks*) sink)\\n    (set-mem-aref* payload :uint64 0 sink-id)\\n    (unwind-protect\\n         (check-abi\\n          (%abi-correlator-run-buffered (runtime-handle context) payload *abi-chunk-callback-pointer*))\\n      (remhash sink-id *abi-sinks*)\\n      (foreign-free* payload))))\\n\\n(defun run-correlator (context frozen sink)\\n  (declare (ignore frozen))\\n  (run-correlator-raw context\\n                      (lambda (event)\\n                        (funcall sink (event-from-pointer event)))))\\n\\n(defstruct (expression-builder (:constructor make-expression-builder (context)))\\n  context\\n  terms)\\n\\n(declaim\\n (inline name-ptr name-len name-token product-expression sum-expression))\\n\\n(defun name-ptr (name)\\n  (mem-ref* (inc-pointer* name *name-ptr-offset*) :pointer))\\n\\n(defun name-len (name)\\n  (mem-ref* (inc-pointer* name *name-len-offset*) :size))\\n\\n(defun name-token (name)\\n  (let ((pointer (name-ptr name))\\n        (length (name-len name)))\\n    (when (and (> length 0) (not (null-pointer-p* pointer)))\\n      (keyword-name (foreign-string-to-lisp* pointer :count length)))))\\n\\n(defun factor-name-token-by-id (names id)\\n  (and (> id 0) (aref names (1- id))))\\n\\n(defun product-expression (factors)\\n  (cond\\n    ((null factors) 1)\\n    ((null (cdr factors)) (car factors))\\n    (t (cons \'* factors))))\\n\\n(defun sum-expression (terms)\\n  (cond\\n    ((null terms) 0)\\n    ((null (cdr terms)) (car terms))\\n    (t (cons \'+ terms))))\\n\\n(defun exp-green-expression-p (expr)\\n  (and (consp expr) (eq (first expr) :exp-green)))\\n\\n(defun momentum-pair-expression-p (expr)\\n  (and (consp expr) (eq (first expr) :momentum-pair)))\\n\\n(defun explicit-green-expression (green exponent)\\n  `(expt (- ,(second green) ,(third green)) ,exponent))\\n\\n(defun product-factor-list (expr)\\n  (if (and (consp expr) (eq (first expr) \'*))\\n      (rest expr)\\n      (list expr)))\\n\\n(defun momentum-sum-expression (momenta)\\n  (cond\\n    ((null momenta) 0)\\n    ((null (cdr momenta)) (car momenta))\\n    (t (cons \'+ momenta))))\\n\\n(defun momentum-delta-expression (header momenta)\\n  (let ((power (second header))\\n        (sum (momentum-sum-expression momenta)))\\n    (product-expression\\n     (remove 1\\n             (list (if (zerop power) 1 `(expt (* 2 :pi) ,power))\\n                   `(:momentum-delta ,power ,sum))))))\\n\\n(defun rewrite-zero-mode-factors (items)\\n  (let ((rest nil)\\n        (momenta nil)\\n        (header nil)\\n        (has-delta nil))\\n    (dolist (item items)\\n      (when (and (consp item) (eq (first item) :momentum-delta-header))\\n        (setf has-delta t)))\\n    (dolist (item items)\\n      (cond\\n        ((and (consp item) (eq (first item) :momentum-delta-header))\\n         (setf header item))\\n        ((and (consp item) (eq (first item) :momentum-delta-momentum))\\n         (push (second item) momenta))\\n        ((and has-delta (consp item) (eq (first item) :residual))\\n         nil)\\n        (t (push item rest))))\\n    (if header\\n        (append (nreverse rest)\\n                (list (momentum-delta-expression header (nreverse momenta))))\\n        items)))\\n\\n(defun term-product-expression (items scalars greens momentum-pairs others)\\n  (if (and greens momentum-pairs)\\n      (let ((exponent (product-expression\\n                       (append\\n                        (loop for scalar in (nreverse scalars)\\n                              append (product-factor-list scalar))\\n                        (nreverse momentum-pairs)))))\\n        (product-expression\\n         (append\\n          (mapcar (lambda (green)\\n                    (explicit-green-expression green exponent))\\n                  (nreverse greens))\\n          (rewrite-zero-mode-factors (nreverse others)))))\\n      (product-expression (rewrite-zero-mode-factors (nreverse items)))))\\n\\n(defun factor-coordinate-expression-values (builder names a b d name-id)\\n  (let* ((context (expression-builder-context builder))\\n         (left (runtime-symbol-name context a))\\n         (right (runtime-symbol-name context b))\\n         (name (factor-name-token-by-id names name-id)))\\n    (cond\\n      ((null name) `(expt (- ,left ,right) ,d))\\n      ((eq name :exp-green) `(:exp-green ,left ,right))\\n      (t `(,name ,left ,right)))))\\n\\n(defun factor-tensor-expression-values (builder names a b name-id)\\n  (let* ((context (expression-builder-context builder))\\n         (left (runtime-symbol-name context a))\\n         (right (runtime-symbol-name context b))\\n         (name (or (factor-name-token-by-id names name-id) :tensor)))\\n    `(,name ,left ,right)))\\n\\n(defun scalar-atom-expression (context atom)\\n  (when (> atom 0)\\n    (let ((name\\n            (multiple-value-bind (cached present)\\n                (gethash atom (runtime-context-scalar-atom-names context))\\n              (if present\\n                  cached\\n                  (let ((out-name (foreign-alloc* :pointer))\\n                        (out-name-len (foreign-alloc* :size)))\\n                    (unwind-protect\\n                         (let ((status (%abi-scalar-atom-name\\n                                        (runtime-context-theory-id context)\\n                                        atom out-name out-name-len)))\\n                           (if (zerop status)\\n                               (let* ((pointer (mem-ref* out-name :pointer))\\n                                      (length (mem-ref* out-name-len :size))\\n                                      (token (keyword-name\\n                                              (foreign-string-to-lisp*\\n                                               pointer :count length))))\\n                                 (setf (gethash atom (runtime-context-scalar-atom-names context))\\n                                       token))\\n                               (setf (gethash atom (runtime-context-scalar-atom-names context))\\n                                     `(:scalar-atom ,atom))))\\n                      (foreign-free* out-name-len)\\n                      (foreign-free* out-name)))))))\\n      (if (keywordp name)\\n          (runtime-constant-value context name)\\n          name))))\\n\\n(defun rational-expression (numerator denominator)\\n  (cond\\n    ((= numerator denominator) 1)\\n    ((= denominator 1) numerator)\\n    (t `(/ ,numerator ,denominator))))\\n\\n(defun scalar-atom-power-expression (atom power)\\n  (cond\\n    ((or (null atom) (= power 0)) 1)\\n    ((= power 1) atom)\\n    (t `(expt ,atom ,power))))\\n\\n(defun signed-byte8 (value)\\n  (if (> value 127) (- value 256) value))\\n\\n(defun scalar-imaginary-factors (power)\\n  (ecase power\\n    (0 nil)\\n    (1 \'(:i))\\n    (2 \'(-1))\\n    (3 \'(-1 :i))))\\nThe expression folder consumes compact term/factor/name arrays from Zig.  It\\nbuilds the user-facing S-expression only after the expansion stream has already\\nbeen accepted, and it keeps scalar monomial decoding cached by atom/flags.\\n(defun make-scalar-monomial-expression (context atom denominator flags numerator)\\n  (let* ((imaginary-power (logand flags 3))\\n         (atom-power (signed-byte8 (ldb (byte 8 8) flags)))\\n         (atom-expr (scalar-atom-expression context atom))\\n         (coefficient (if (and (numberp atom-expr) (= atom-power 1))\\n                          (/ (* numerator atom-expr) denominator)\\n                          (rational-expression numerator denominator)))\\n         (atom-factor (unless (and (numberp atom-expr) (= atom-power 1))\\n                        (scalar-atom-power-expression atom-expr atom-power))))\\n    (product-expression\\n     (remove-if (lambda (factor) (or (null factor) (eql factor 1)))\\n                (append\\n                 (list coefficient)\\n                 (scalar-imaginary-factors imaginary-power)\\n                 (list atom-factor))))))\\n\\n(defun cached-scalar-monomial-expression (context atom denominator flags numerator)\\n  (loop for row in (runtime-context-scalar-monomials context)\\n        when (and (= atom (aref row 0))\\n                  (= denominator (aref row 1))\\n                  (= flags (aref row 2))\\n                  (= numerator (aref row 3)))\\n          return (aref row 4)\\n        finally\\n           (let ((expr (make-scalar-monomial-expression\\n                        context atom denominator flags numerator)))\\n             (push (vector atom denominator flags numerator expr)\\n                   (runtime-context-scalar-monomials context))\\n             (return expr))))\\n\\n(defun scalar-monomial-expression (builder atom denominator flags numerator)\\n  (let ((context (expression-builder-context builder)))\\n    (cached-scalar-monomial-expression context atom denominator flags numerator)))\\n\\n(defun factor-scalar-expression-values (builder names a b c d name-id)\\n  (let ((sign d))\\n    (cond\\n      ((and (= sign -1) (= name-id 0)) -1)\\n      (t\\n       (let ((name (factor-name-token-by-id names name-id)))\\n         (cond\\n           ((eq name :scalar-monomial)\\n            (scalar-monomial-expression builder a b c d))\\n           (name\\n             `(,name\\n               ,(runtime-symbol-name (expression-builder-context builder) a)\\n               ,(runtime-symbol-name (expression-builder-context builder) b)))\\n           (t 1)))))))\\n\\n(defun factor-zero-mode-expression-values (builder names a b name-id)\\n  (let ((name (factor-name-token-by-id names name-id)))\\n    (cond\\n      ((eq name :momentum-delta)\\n       `(:momentum-delta-header ,a ,b))\\n      ((eq name :momentum-delta-momentum)\\n       `(:momentum-delta-momentum\\n         ,(runtime-symbol-name (expression-builder-context builder) a)))\\n      ((eq name :eta-xi-zero-mode)\\n       `(:eta-xi-zero-mode ,(runtime-symbol-name (expression-builder-context builder) a)))\\n      ((eq name :bc-top-form)\\n       \'(:bc-top-form))\\n      (name `(,name))\\n      (t \'(:zero-mode)))))\\n\\n(defun factor-expression-values (builder names kind a b c d name-id)\\n  (case kind\\n    (4 (factor-scalar-expression-values builder names a b c d name-id))\\n    (5 (factor-coordinate-expression-values builder names a b d name-id))\\n    (6 (factor-tensor-expression-values builder names a b name-id))\\n    (7 (factor-zero-mode-expression-values builder names a b name-id))\\n    (8 `(:residual ,a))\\n    (otherwise 1)))\\n\\n(defun expression-name-vector (names count)\\n  (let ((tokens (make-array count)))\\n    (loop for index below count\\n          do (setf (aref tokens index)\\n                   (name-token (name-pointer-at names index))))\\n    tokens))\\n\\n(defun fold-expression-records (context)\\n  (let ((out-terms (foreign-alloc* :pointer))\\n        (out-term-count (foreign-alloc* :size))\\n        (out-factors (foreign-alloc* :pointer))\\n        (out-factor-count (foreign-alloc* :size))\\n        (out-names (foreign-alloc* :pointer))\\n        (out-name-count (foreign-alloc* :size))\\n        (builder (make-expression-builder context)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-correlator-expression-records\\n             (runtime-handle context)\\n             out-terms out-term-count out-factors out-factor-count out-names out-name-count))\\n           (let ((terms (mem-ref* out-terms :pointer))\\n                 (term-count (mem-ref* out-term-count :size))\\n                 (factors (mem-ref* out-factors :pointer))\\n                 (factor-count (mem-ref* out-factor-count :size))\\n                 (names (mem-ref* out-names :pointer))\\n                 (name-count (mem-ref* out-name-count :size)))\\n             (unwind-protect\\n                  (let ((name-tokens (expression-name-vector names name-count)))\\n                    (loop for index below term-count\\n                          do (push (term-expression builder factors name-tokens\\n                                                    (term-pointer-at terms index))\\n                                   (expression-builder-terms builder))))\\n               (%abi-expression-buffer-free terms term-count factors factor-count names name-count)))\\n           (sum-expression (nreverse (expression-builder-terms builder))))\\n      (foreign-free* out-name-count)\\n      (foreign-free* out-names)\\n      (foreign-free* out-factor-count)\\n      (foreign-free* out-factors)\\n      (foreign-free* out-term-count)\\n      (foreign-free* out-terms))))\\n\\n(defun correlator-expression (context frozen)\\n  (declare (ignore frozen))\\n  (fold-expression-records context))\\n\\n(defun collect-correlator (context frozen &key (limit 1024))\\n  (let ((events nil)\\n        (count 0))\\n    (run-correlator context frozen\\n                    (lambda (event)\\n                      (when (>= count limit)\\n                        (error \\"Result collection limit reached.\\"))\\n                      (incf count)\\n                      (push event events)))\\n    (nreverse events)))\\n"},{"id":"20260608T235500.000000","title":"Lisp Generated Fixture Declarations","tags":null,"content":"This node records the minimal Lisp-owned declarations for the generated\\nfixtures.  They use the generic emitter from\\nLisp Descriptor Emitter and add one REPL macro\\nthat owns runtime lifetime and returns the symbolic expression directly.\\n(defpackage #:string-code.cft.fixtures\\n  (:use #:cl #:string-code.cft.descriptor)\\n  (:export\\n   #:free-fermion-10\\n   #:eta-xi-sphere\\n   #:eta-xi-torus\\n   #:bc-sphere\\n   #:free-boson-10\\n   #:make-free-fermion-runtime\\n   #:psi\\n   #:make-eta-xi-sphere-runtime\\n   #:make-eta-xi-torus-runtime\\n   #:eta\\n   #:xi\\n   #:make-bc-runtime\\n   #:b-field\\n   #:c-field\\n   #:make-free-boson-runtime\\n   #:dX\\n   #:expX\\n   #:with-correlator-context\\n   #:correlator\\n   #:corr\\n   #:ops\\n   #:R))\\n\\n(in-package #:string-code.cft.fixtures)\\n\\n(defun runtime-symbol-id (context value)\\n  (etypecase value\\n    (integer value)\\n    (string (intern-runtime-symbol context value))\\n    (symbol (intern-runtime-symbol context (string-downcase (string value))))))\\nThe REPL macro layer rewrites user field forms into explicit runtime insertions.\\nops just groups forms; R groups forms and then asks Zig to normal-order the\\nnumber of fields in that group.  No symbolic expression is built before the\\noperator list is frozen.\\n(eval-when (:compile-toplevel :load-toplevel :execute)\\n  (defun fixture-call-symbol (name)\\n    (if (symbolp name)\\n        (multiple-value-bind (symbol status)\\n            (find-symbol (symbol-name name) \\"STRING-CODE.CFT.FIXTURES\\")\\n          (if status symbol name))\\n        name))\\n\\n  (defun literal-symbol-form (value)\\n    (if (symbolp value) `\',value value))\\n\\n  (defun fixture-form-name (form)\\n    (and (consp form) (symbolp (first form)) (first form)))\\n\\n  (defun field-form-p (form)\\n    (and (consp form)\\n         (not (member (fixture-form-name form) \'(ops R)\\n                      :test #\'string-equal))))\\n\\n  (defun field-call-form (context form)\\n    (unless (consp form)\\n      (error \\"Correlator field form must be a list, got ~S.\\" form))\\n    `(,(fixture-call-symbol (first form)) ,context\\n      ,@(mapcar #\'literal-symbol-form (rest form))))\\n\\n  (declaim (ftype function correlator-body-forms correlator-form-forms))\\n\\n  (defun insertion-count (form)\\n    (cond\\n      ((field-form-p form) 1)\\n      ((and (consp form) (string-equal (fixture-form-name form) \'ops))\\n       (loop for item in (rest form) sum (insertion-count item)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'R))\\n       (loop for item in (rest form) sum (insertion-count item)))\\n      (t (error \\"Unknown correlator form ~S.\\" form))))\\n\\n  (defun correlator-form-forms (context form)\\n    (cond\\n      ((field-form-p form) (list (field-call-form context form)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'ops))\\n       (correlator-body-forms context (rest form)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'R))\\n       (append (correlator-body-forms context (rest form))\\n               `((normal-order-runtime-field ,context ,(insertion-count form)))))\\n      (t (error \\"Unknown correlator form ~S.\\" form))))\\n\\n  (defun correlator-body-forms (context forms)\\n    (loop for form in forms append (correlator-form-forms context form))))\\n\\n(defmacro with-correlator-context ((context (maker &rest runtime-args)) &body body)\\n  \\"Bind CONTEXT to a generated runtime and release it after BODY.\\"\\n  `(let ((,context (,(fixture-call-symbol maker) ,@runtime-args)))\\n     (unwind-protect\\n          (progn ,@body)\\n       (destroy-runtime-context ,context))))\\n\\n(defmacro correlator ((maker &rest runtime-args) &body fields)\\n  \\"Evaluate fixture field forms and return the symbolic correlator expression.\\"\\n  (let ((context (gensym \\"CONTEXT-\\"))\\n        (frozen (gensym \\"FROZEN-\\")))\\n    `(with-correlator-context (,context (,(fixture-call-symbol maker) ,@runtime-args))\\n       ,@(correlator-body-forms context fields)\\n       (let ((,frozen (freeze-runtime-operators ,context)))\\n         (correlator-expression ,context ,frozen)))))\\n\\n(defmacro corr ((maker &rest runtime-args) &body fields)\\n  \\"Alias for CORRELATOR.\\"\\n  `(correlator (,maker ,@runtime-args) ,@fields))\\nPreset accessors return fresh descriptors for inspection.  Runtime constructors\\ncreate C ABI contexts for evaluation.\\n(defun free-fermion-10 ()\\n  (string-code.cft.presets:free-fermion-10))\\n\\n(defun eta-xi-sphere ()\\n  (string-code.cft.presets:eta-xi-sphere))\\n(defun eta-xi-torus ()\\n  (string-code.cft.presets:eta-xi-torus))\\n\\n(defun bc-sphere ()\\n  (string-code.cft.presets:bc-sphere))\\n(defun free-boson-10 ()\\n  (string-code.cft.presets:free-boson-10))\\nThe user-facing runtime constructors resolve generated theory ids through the\\npreset registry.  The numeric ids stay compiler-owned.\\n(defun make-preset-runtime (name library &key constants)\\n  (make-runtime-context\\n   :library library\\n   :theory-id (string-code.cft.presets:preset-theory-id-for name)\\n   :constants constants))\\n\\n(defun make-free-fermion-runtime (&key library)\\n  (make-preset-runtime \'free-fermion-10 library))\\n\\n(defun psi (context mu z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        (list (runtime-symbol-id context mu))))\\n\\n(defun make-eta-xi-sphere-runtime (&key library)\\n  (make-preset-runtime \'eta-xi-sphere library))\\n\\n(defun make-eta-xi-torus-runtime (&key library)\\n  (make-preset-runtime \'eta-xi-torus library))\\n\\n(defun eta (context z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun xi (context z)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n(defun make-bc-runtime (&key library)\\n  (make-preset-runtime \'bc-sphere library))\\n\\n(defun b-field (context z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun c-field (context z)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun free-boson-constants (alpha-prime constants)\\n  (acons :alpha-prime alpha-prime constants))\\n\\n(defun make-free-boson-runtime (&key library (alpha-prime :alpha-prime) constants)\\n  (make-preset-runtime \'free-boson-10 library\\n                       :constants (free-boson-constants alpha-prime constants)))\\n\\n(defun dX (context mu z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        (list (runtime-symbol-id context mu))))\\n\\n(defun expX (context k z z-bar)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z)\\n                              (runtime-symbol-id context z-bar))\\n                        (list (runtime-symbol-id context k))))\\n"},{"id":"20260609T001000.000000","title":"Generated Fixture ABI","tags":null,"content":"This node exposes the smallest C ABI for the descriptor fixtures emitted by\\nLisp Preset Compiler.  The exported entrypoints are\\ntheory-generic: a theory id selects the generated descriptor instance, and field\\nids distinguish runtime insertions.\\nconst std = @import(\\"std\\");\\nconst descriptor = @import(\\"correlators/descriptor.zig\\");\\nconst dispatch = @import(\\"correlators/generated_dispatch.zig\\");\\nconst kernel = @import(\\"kernel.zig\\");\\n\\nconst allocator = std.heap.c_allocator;\\nconst TheoryId = dispatch.TheoryId;\\nconst ContextTag = dispatch.ContextTag;\\n\\n/// Context is an opaque generated-theory runtime handle for the C ABI.\\nconst Context = struct {\\n    inner: ContextTag,\\n    frozen: ?kernel.Call.MultiOp = null,\\n};\\nThe ABI event is a stable POD record.  String names are borrowed for the\\nduration of the callback and copied by Lisp only when it collects events.\\n/// Event is one stable C ABI result event.\\npub const Event = extern struct {\\n    kind: u8,\\n    a: u32,\\n    b: u32,\\n    c: u32,\\n    d: i32,\\n    name_ptr: ?[*]const u8,\\n    name_len: usize,\\n};\\n\\n/// ExpressionTerm is one product in the expanded symbolic sum.\\npub const ExpressionTerm = extern struct {\\n    first_factor: usize,\\n    factor_count: usize,\\n};\\n\\n/// ExpressionFactor is one nontrivial symbolic product factor.\\npub const ExpressionFactor = extern struct {\\n    kind: u8,\\n    a: u32,\\n    b: u32,\\n    c: u32,\\n    d: i32,\\n    name_id: u32,\\n};\\n\\n/// ExpressionName is one borrowed symbolic name used by expression factors.\\npub const ExpressionName = extern struct {\\n    ptr: ?[*]const u8,\\n    len: usize,\\n};\\n\\n/// EventCallback receives one streamed result event.\\npub const EventCallback = *const fn (?*anyopaque, *const Event) callconv(.c) c_int;\\n\\n/// EventChunkCallback receives a bounded chunk of streamed result events.\\npub const EventChunkCallback = *const fn (?*anyopaque, [*]const Event, usize) callconv(.c) c_int;\\n\\nconst StreamState = struct {\\n    payload: ?*anyopaque,\\n    callback: EventCallback,\\n};\\n\\nconst event_chunk_capacity = 256;\\n\\nconst BufferedStreamState = struct {\\n    payload: ?*anyopaque,\\n    callback: EventChunkCallback,\\n    events: [event_chunk_capacity]Event = undefined,\\n    len: usize = 0,\\n\\n    fn flush(self: *@This()) !void {\\n        if (self.len == 0) return;\\n        if (self.callback(self.payload, &self.events, self.len) != 0) return error.CallbackFailed;\\n        self.len = 0;\\n    }\\n\\n    fn push(self: *@This(), event: Event) !void {\\n        self.events[self.len] = event;\\n        self.len += 1;\\n        if (self.len == self.events.len) try self.flush();\\n    }\\n};\\n\\nfn abiEvent(event: descriptor.ResultEvent) Event {\\n    const name_ptr: ?[*]const u8 = if (event.name) |name| name.ptr else null;\\n    const name_len: usize = if (event.name) |name| name.len else 0;\\n    return .{\\n        .kind = @intFromEnum(event.kind),\\n        .a = event.a,\\n        .b = event.b,\\n        .c = event.c,\\n        .d = event.d,\\n        .name_ptr = name_ptr,\\n        .name_len = name_len,\\n    };\\n}\\n\\nfn streamThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *StreamState = @ptrCast(@alignCast(payload));\\n    const stable_event = abiEvent(event);\\n    if (state.callback(state.payload, &stable_event) != 0) return error.CallbackFailed;\\n}\\n\\nfn bufferedStreamThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *BufferedStreamState = @ptrCast(@alignCast(payload));\\n    try state.push(abiEvent(event));\\n}\\nExpression records are the fast REPL path.  Zig still streams the correlator\\nonce, but this state stores only term ranges, nontrivial factors, and interned\\nborrowed names.  Lisp builds cons expressions from these compact rows after the\\nkernel returns.\\nconst NameKey = struct {\\n    ptr: usize,\\n    len: usize,\\n};\\n\\nconst ExpressionRecordState = struct {\\n    terms: std.ArrayList(ExpressionTerm) = .empty,\\n    factors: std.ArrayList(ExpressionFactor) = .empty,\\n    names: std.ArrayList(ExpressionName) = .empty,\\n    name_ids: std.AutoHashMap(NameKey, u32) = std.AutoHashMap(NameKey, u32).init(allocator),\\n    term_start: usize = 0,\\n\\n    fn deinit(self: *@This()) void {\\n        self.terms.deinit(allocator);\\n        self.factors.deinit(allocator);\\n        self.names.deinit(allocator);\\n        self.name_ids.deinit();\\n    }\\n\\n    fn nameId(self: *@This(), maybe_name: ?[]const u8) !u32 {\\n        const name = maybe_name orelse return 0;\\n        const key = NameKey{ .ptr = @intFromPtr(name.ptr), .len = name.len };\\n        if (self.name_ids.get(key)) |id| return id;\\n        const id: u32 = @intCast(self.names.items.len + 1);\\n        try self.names.append(allocator, .{ .ptr = name.ptr, .len = name.len });\\n        try self.name_ids.put(key, id);\\n        return id;\\n    }\\n\\n    fn finishTerm(self: *@This()) !void {\\n        try self.terms.append(allocator, .{\\n            .first_factor = self.term_start,\\n            .factor_count = self.factors.items.len - self.term_start,\\n        });\\n        self.term_start = self.factors.items.len;\\n    }\\n\\n    fn pushFactor(self: *@This(), event: descriptor.ResultEvent) !void {\\n        if (event.kind == .scalar and event.d == 0 and event.name == null) return;\\n        try self.factors.append(allocator, .{\\n            .kind = @intFromEnum(event.kind),\\n            .a = event.a,\\n            .b = event.b,\\n            .c = event.c,\\n            .d = event.d,\\n            .name_id = try self.nameId(event.name),\\n        });\\n    }\\n};\\n\\nfn expressionRecordThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *ExpressionRecordState = @ptrCast(@alignCast(payload));\\n    switch (event.kind) {\\n        .sum_term_begin => state.term_start = state.factors.items.len,\\n        .sum_term_end => try state.finishTerm(),\\n        .wick_term_begin, .wick_term_end => {},\\n        .scalar, .coordinate, .tensor, .zero_mode, .residual_operator => try state.pushFactor(event),\\n    }\\n}\\nErrors are reported by a process-local short buffer.  The ABI does not allocate\\nerrors or throw across the C boundary.\\nvar last_error_storage: [160]u8 = [_]u8{0} ** 160;\\n\\nfn clearError() void {\\n    last_error_storage[0] = 0;\\n}\\n\\nfn setErrorName(name: []const u8) c_int {\\n    const len = @min(name.len, last_error_storage.len - 1);\\n    @memcpy(last_error_storage[0..len], name[0..len]);\\n    last_error_storage[len] = 0;\\n    return -1;\\n}\\n\\nfn setError(err: anyerror) c_int {\\n    return setErrorName(@errorName(err));\\n}\\n\\n/// sc_generated_last_error returns the last ABI error string.\\nexport fn sc_generated_last_error() [*:0]const u8 {\\n    return @ptrCast(&last_error_storage);\\n}\\nContext creation and destruction dispatch on generated theory ids.  The\\nentrypoints are generic; no theory-specific exported functions are needed.\\n/// sc_generated_descriptor_abi_version returns the descriptor ABI version.\\nexport fn sc_generated_descriptor_abi_version() u32 {\\n    return descriptor.descriptor_abi_version;\\n}\\n\\n/// sc_generated_theory_hash returns the generated theory hash for a theory id.\\nexport fn sc_generated_theory_hash(raw_theory: u32) u32 {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| {\\n        _ = setError(err);\\n        return 0;\\n    };\\n    return dispatch.theoryHash(id);\\n}\\n\\n/// sc_generated_scalar_atom_name returns a descriptor parameter name for an atom id.\\nexport fn sc_generated_scalar_atom_name(raw_theory: u32, atom: u32, out_ptr: ?*?[*]const u8, out_len: ?*usize) c_int {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| return setError(err);\\n    const ptr = out_ptr orelse return setErrorName(\\"NullOutput\\");\\n    const len = out_len orelse return setErrorName(\\"NullOutput\\");\\n    const name = dispatch.scalarAtomParameterName(id, atom) orelse return setErrorName(\\"UnknownScalarAtom\\");\\n    ptr.* = name.ptr;\\n    len.* = name.len;\\n    return 0;\\n}\\n\\n/// sc_generated_context_create allocates a context for a generated theory id.\\nexport fn sc_generated_context_create(raw_theory: u32) ?*Context {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| {\\n        _ = setError(err);\\n        return null;\\n    };\\n    const ctx = allocator.create(Context) catch |err| {\\n        _ = setError(err);\\n        return null;\\n    };\\n    ctx.* = .{ .inner = ContextTag.create(id) catch |err| {\\n        allocator.destroy(ctx);\\n        _ = setError(err);\\n        return null;\\n    } };\\n    return ctx;\\n}\\n\\n/// sc_generated_context_destroy releases a generated context.\\nexport fn sc_generated_context_destroy(ctx: ?*Context) void {\\n    const handle = ctx orelse return;\\n    handle.inner.destroy();\\n    allocator.destroy(handle);\\n}\\nThe mutation entrypoints operate on compact ids and slices supplied by Lisp.\\nfn byteSlice(ptr: ?[*]const u8, len: usize) ![]const u8 {\\n    const data = ptr orelse return error.NullPointer;\\n    return data[0..len];\\n}\\n\\nfn u32Slice(ptr: ?[*]const u32, len: usize) ![]const u32 {\\n    if (len == 0) return &.{};\\n    const data = ptr orelse return error.NullPointer;\\n    return data[0..len];\\n}\\n\\n/// sc_generated_symbol_intern interns a runtime symbol in one context.\\nexport fn sc_generated_symbol_intern(ctx: ?*Context, name_ptr: ?[*]const u8, name_len: usize, out_symbol: ?*u32) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const out = out_symbol orelse return setErrorName(\\"NullOutput\\");\\n    const name = byteSlice(name_ptr, name_len) catch |err| return setError(err);\\n    out.* = handle.inner.symbolIntern(name) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_field_insert appends one generated field occurrence.\\nexport fn sc_generated_field_insert(ctx: ?*Context, field_id: u16, coords_ptr: ?[*]const u32, coord_len: usize, labels_ptr: ?[*]const u32, label_len: usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const coords = u32Slice(coords_ptr, coord_len) catch |err| return setError(err);\\n    const labels = u32Slice(labels_ptr, label_len) catch |err| return setError(err);\\n    handle.inner.fieldInsert(field_id, coords, labels) catch |err| return setError(err);\\n    handle.frozen = null;\\n    return 0;\\n}\\n\\n/// sc_generated_normal_ordering tags the last count fields as one normal product.\\nexport fn sc_generated_normal_ordering(ctx: ?*Context, count: usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    handle.inner.normalOrdering(count) catch |err| return setError(err);\\n    handle.frozen = null;\\n    return 0;\\n}\\n\\n/// sc_generated_operator_list_freeze freezes the current operator list.\\nexport fn sc_generated_operator_list_freeze(ctx: ?*Context) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    handle.frozen = handle.inner.freeze() catch |err| return setError(err);\\n    return 0;\\n}\\nCount and stream run against the last frozen operator list.  Count uses the\\ndescriptor count sink and does not allocate Lisp expression data.\\nfn frozen(handle: *Context) !kernel.Call.MultiOp {\\n    return handle.frozen orelse error.OperatorListNotFrozen;\\n}\\n\\n/// sc_generated_correlator_count returns the number of accepted branches.\\nexport fn sc_generated_correlator_count(ctx: ?*Context, out_count: ?*usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const out = out_count orelse return setErrorName(\\"NullOutput\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    out.* = handle.inner.count(ops) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_correlator_run streams result events to a callback.\\nexport fn sc_generated_correlator_run(ctx: ?*Context, payload: ?*anyopaque, callback: ?EventCallback) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const cb = callback orelse return setErrorName(\\"NullCallback\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = StreamState{ .payload = payload, .callback = cb };\\n    handle.inner.run(ops, &state, streamThunk) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_correlator_run_buffered streams bounded event chunks.\\nexport fn sc_generated_correlator_run_buffered(ctx: ?*Context, payload: ?*anyopaque, callback: ?EventChunkCallback) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const cb = callback orelse return setErrorName(\\"NullCallback\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = BufferedStreamState{ .payload = payload, .callback = cb };\\n    handle.inner.run(ops, &state, bufferedStreamThunk) catch |err| return setError(err);\\n    state.flush() catch |err| return setError(err);\\n    return 0;\\n}\\nThe expression-record export owns the temporary arrays until Lisp has copied or\\nfolded them.  On every allocation failure it frees earlier arrays before\\nreturning an ABI error.\\n/// sc_generated_correlator_expression_records returns compact expression terms and factors.\\nexport fn sc_generated_correlator_expression_records(\\n    ctx: ?*Context,\\n    out_terms: ?*[*]ExpressionTerm,\\n    out_term_count: ?*usize,\\n    out_factors: ?*[*]ExpressionFactor,\\n    out_factor_count: ?*usize,\\n    out_names: ?*[*]ExpressionName,\\n    out_name_count: ?*usize,\\n) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const terms_out = out_terms orelse return setErrorName(\\"NullOutput\\");\\n    const term_count_out = out_term_count orelse return setErrorName(\\"NullOutput\\");\\n    const factors_out = out_factors orelse return setErrorName(\\"NullOutput\\");\\n    const factor_count_out = out_factor_count orelse return setErrorName(\\"NullOutput\\");\\n    const names_out = out_names orelse return setErrorName(\\"NullOutput\\");\\n    const name_count_out = out_name_count orelse return setErrorName(\\"NullOutput\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = ExpressionRecordState{};\\n    defer state.deinit();\\n    handle.inner.run(ops, &state, expressionRecordThunk) catch |err| return setError(err);\\n    const factors = state.factors.toOwnedSlice(allocator) catch |err| return setError(err);\\n    const terms = state.terms.toOwnedSlice(allocator) catch |err| {\\n        allocator.free(factors);\\n        return setError(err);\\n    };\\n    const names = state.names.toOwnedSlice(allocator) catch |err| {\\n        allocator.free(terms);\\n        allocator.free(factors);\\n        return setError(err);\\n    };\\n    terms_out.* = terms.ptr;\\n    term_count_out.* = terms.len;\\n    factors_out.* = factors.ptr;\\n    factor_count_out.* = factors.len;\\n    names_out.* = names.ptr;\\n    name_count_out.* = names.len;\\n    return 0;\\n}\\n\\n/// sc_generated_expression_buffer_free releases compact expression buffers.\\nexport fn sc_generated_expression_buffer_free(\\n    terms: ?[*]ExpressionTerm,\\n    term_count: usize,\\n    factors: ?[*]ExpressionFactor,\\n    factor_count: usize,\\n    names: ?[*]ExpressionName,\\n    name_count: usize,\\n) void {\\n    if (terms) |ptr| allocator.free(ptr[0..term_count]);\\n    if (factors) |ptr| allocator.free(ptr[0..factor_count]);\\n    if (names) |ptr| allocator.free(ptr[0..name_count]);\\n}\\nThe tests exercise the ABI on one tensor/coordinate path and one count path.\\nconst AbiRecorder = struct {\\n    count: usize = 0,\\n    saw_tensor: bool = false,\\n    saw_coordinate: bool = false,\\n\\n    fn push(payload: ?*anyopaque, event_ptr: *const Event) callconv(.c) c_int {\\n        const self: *AbiRecorder = @ptrCast(@alignCast(payload.?));\\n        const event = event_ptr.*;\\n        self.count += 1;\\n        if (event.kind == @intFromEnum(descriptor.ResultEventKind.tensor) and event.a == 1 and event.b == 2) {\\n            self.saw_tensor = true;\\n        }\\n        if (event.kind == @intFromEnum(descriptor.ResultEventKind.coordinate) and event.a == 3 and event.b == 4) {\\n            self.saw_coordinate = true;\\n        }\\n        return 0;\\n    }\\n};\\n\\ntest \\"generated C ABI counts and streams free-fermion events\\" {\\n    try selfTest();\\n}\\n\\n/// selfTest runs the generated C ABI fixture invariants.\\npub fn selfTest() !void {\\n    const ctx = sc_generated_context_create(@intFromEnum(dispatch.first_theory_id)) orelse return error.ContextCreateFailed;\\n    defer sc_generated_context_destroy(ctx);\\n\\n    var mu: u32 = 0;\\n    var nu: u32 = 0;\\n    var z: u32 = 0;\\n    var w: u32 = 0;\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"mu\\".ptr, 2, &mu));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"nu\\".ptr, 2, &nu));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"z\\".ptr, 1, &z));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"w\\".ptr, 1, &w));\\n\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_field_insert(ctx, 0, &.{z}, 1, &.{mu}, 1));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_field_insert(ctx, 0, &.{w}, 1, &.{nu}, 1));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_operator_list_freeze(ctx));\\n\\n    var count: usize = 0;\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_correlator_count(ctx, &count));\\n    try std.testing.expectEqual(@as(usize, 1), count);\\n\\n    var recorder = AbiRecorder{};\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_correlator_run(ctx, &recorder, AbiRecorder.push));\\n    try std.testing.expect(recorder.saw_tensor);\\n    try std.testing.expect(recorder.saw_coordinate);\\n}\\n"},{"id":"20260609T003900.000000","title":"Lisp CFT System","tags":null,"content":"The Lisp system loads descriptor emission first, then preset declarations, then\\nthe REPL fixture helpers that insert fields through the generated C ABI.\\n(defsystem \\"string-code-cft\\"\\n  :description \\"Lisp CFT presets, descriptor emission, and generated C ABI runtime helpers.\\"\\n  :serial t\\n  :depends-on (\\"uiop\\")\\n  :components ((:file \\"string-code-cft-descriptor\\")\\n               (:file \\"string-code-cft-presets\\")\\n               (:module \\"presets\\"\\n                :serial t\\n                :components\\n                ((:file \\"free-fermion-10\\")\\n                 (:file \\"eta-xi\\")\\n                 (:file \\"bc-sphere\\")\\n                 (:file \\"free-boson-10\\")))\\n               (:file \\"string-code-cft-fixtures\\")))\\n"},{"id":"20260609T004000.000000","title":"Lisp Preset Compiler","tags":null,"content":"This node lowers compact Lisp CFT presets to descriptor rows and emits the Zig\\nfixture and dispatch modules imported by the generated C ABI.  It does not evaluate\\ncorrelators and it does not own REPL insertion helpers.\\nPackageThe public surface is deliberately small: define presets, inspect one lowered\\ntheory, emit generated Zig sources, or compile the fixed generated ABI library.\\n(eval-when (:compile-toplevel :load-toplevel :execute)\\n  (require :asdf))\\n\\n(defpackage #:string-code.cft.presets\\n  (:use #:cl #:string-code.cft.descriptor)\\n  (:export\\n   #:define-cft-preset\\n   #:preset-theory\\n   #:preset-theory-id-for\\n   #:write-generated-fixtures\\n   #:write-generated-sources\\n   #:compile-preset-library\\n   #:free-fermion-10\\n   #:eta-xi-sphere\\n   #:eta-xi-torus\\n   #:bc-sphere\\n   #:free-boson-10))\\n\\n(in-package #:string-code.cft.presets)\\nDescriptor LayersThe preset path has four layers, and only the first one is user-facing:\\nThe preset file declares CFT data: parameters, surfaces, fields, Wick\\n  contractions, and zero modes.\\nThe Lisp descriptor rows are compact normalized data for Zig: symbols,\\n  surfaces, fields, metadata, Wick terms, and zero-mode rows.\\nThe generated Zig fixture needs internal names: table prefixes, descriptor\\n  constants, generated theory type names, and stable theory ids.\\nThe C ABI receives a theory id at runtime so the REPL can select a compiled\\n  generated theory.\\nThe generated names and ids are compiler data.  They are kept here, not in\\npreset files.\\nRegistryThe registry stores compile-time identity for the generated ABI.  The builder is\\na function so users can inspect a fresh descriptor before compiling.\\n(defstruct preset name builder theory-name hash theory-id)\\n(defstruct field-shape id insertion labels)\\n\\n(defparameter *presets* (make-hash-table))\\n(defparameter *next-theory-id* 1)\\nPreset names generate theory names, type names, descriptor prefixes, and theory\\nids.  Numeric suffixes such as -10 are treated as dimension labels and are\\nnot part of generated Zig type names.  Surface suffixes are kept only when a\\nfamily has more than one generated surface.\\n(defun preset-options-form-p (form)\\n  (and (consp form) (keywordp (first form))))\\n\\n(defun split-name-parts (name)\\n  (let ((text (string-downcase (string name)))\\n        (start 0)\\n        (parts nil))\\n    (loop for index from 0 below (length text)\\n          when (char= (aref text index) #\\\\-)\\n            do (progn\\n                 (when (< start index)\\n                   (push (subseq text start index) parts))\\n                 (setf start (1+ index))))\\n    (when (< start (length text))\\n      (push (subseq text start) parts))\\n    (nreverse parts)))\\n(defun numeric-name-part-p (part)\\n  (and (> (length part) 0)\\n       (loop for char across part always (digit-char-p char))))\\n\\n(defun surface-name-part-p (part)\\n  (member part \'(\\"sphere\\" \\"torus\\" \\"disk\\") :test #\'string=))\\n\\n(defun drop-trailing-numeric-parts (parts)\\n  (loop while (and parts (numeric-name-part-p (car (last parts))))\\n        do (setf parts (butlast parts)))\\n  parts)\\n\\n(defun drop-trailing-surface-part (parts)\\n  (if (and parts (surface-name-part-p (car (last parts))))\\n      (butlast parts)\\n      parts))\\n(defun join-name-parts (parts separator)\\n  (with-output-to-string (stream)\\n    (loop for part in parts\\n          for first = t then nil\\n          do (progn\\n               (unless first\\n                 (write-string separator stream))\\n               (write-string part stream)))))\\n\\n(defun generated-theory-name (name)\\n  (join-name-parts (drop-trailing-surface-part (split-name-parts name)) \\"-\\"))\\nTheory ids are assigned in preset load order.  Hashes are generated from the\\ncanonical theory name and declaration forms.\\n(defun next-theory-id-form ()\\n  (prog1 *next-theory-id*\\n    (incf *next-theory-id*)))\\n\\n(defun hash-byte (hash byte)\\n  (logand #xffffffff (* #x01000193 (logxor hash byte))))\\n\\n(defun generated-theory-hash (name forms)\\n  (let ((hash #x811c9dc5)\\n        (text (with-output-to-string (stream)\\n                (prin1 name stream)\\n                (prin1 forms stream))))\\n    (loop for char across text\\n          do (setf hash (hash-byte hash (char-code char))))\\n    hash))\\nThe macro accepts optional metadata for experiments, but built-in preset files\\ndo not need ABI headers.\\n(defmacro define-cft-preset (name &body body)\\n  \\"Define NAME as a descriptor preset from CFT declaration forms.\\"\\n  (let* ((user-options (if (preset-options-form-p (first body)) (first body) nil))\\n         (forms (if user-options (rest body) body))\\n         (theory-name (or (getf user-options :name)\\n                          (generated-theory-name name)))\\n         (hash (or (getf user-options :hash)\\n                   (generated-theory-hash theory-name forms)))\\n         (theory-id (or (getf user-options :theory-id)\\n                        (next-theory-id-form))))\\n    `(progn\\n       (defun ,name ()\\n         (build-cft-preset ,theory-name ,hash \',forms))\\n       (setf (gethash \',name *presets*)\\n             (make-preset :name \',name\\n                          :builder #\',name\\n                          :theory-name ,theory-name\\n                          :hash ,hash\\n                          :theory-id ,theory-id))\\n       \',name)))\\nTablesPreset declarations resolve names to compact descriptor ids.  Fields and labels\\nuse normalized string keys so case differences in Lisp symbols do not leak into\\nthe descriptor.\\n(defun table-get (table key label)\\n  (multiple-value-bind (value found) (gethash key table)\\n    (unless found\\n      (error \\"Unknown ~A ~S.\\" label key))\\n    value))\\n\\n(defun option-value (items key &optional default)\\n  (let ((tail (member key items)))\\n    (if tail (second tail) default)))\\n(defun field-key (name)\\n  (string-downcase (string name)))\\n\\n(defun label-key (field label)\\n  (list (field-key field) (string-downcase (string label))))\\nField labels must be visible before weight metadata is parsed, because weights\\nmay refer to the field being declared.\\n(defun register-field-shape (fields labels field labels-spec)\\n  (let ((field-name (field-key field))\\n        (field-id (hash-table-count fields)))\\n    (setf (gethash field-name fields)\\n          (make-field-shape :id field-id :insertion nil :labels labels-spec))\\n    (loop for label in labels-spec\\n          for slot from 0\\n          do (setf (gethash (label-key field (first label)) labels) slot))\\n    field-id))\\n\\n(defun set-field-shape-insertion (fields field insertion)\\n  (setf (field-shape-insertion (table-get fields (field-key field) \\"field\\"))\\n        insertion))\\nReferencesReference parsers convert preset names into descriptor ids.  Integer ids are\\naccepted only as an escape hatch for generated code.\\n(defun parse-parameter-ref (parameters value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get parameters value \\"parameter\\"))))\\n\\n(defun parse-quantum-number-ref (quantum-numbers value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get quantum-numbers value \\"quantum number\\"))))\\n\\n(defun parse-quantum-number-kind (kind)\\n  (ecase kind\\n    ((:u1 :u1-charge u1 u1-charge) :u1-charge)\\n    ((:ade-irrep ade-irrep) :ade-irrep)))\\nQuantum-number values are deliberately scalar.  U(1) charges lower to exact\\nintegers or rationals; representation-valued numbers lower to symbol ids.  The\\nZig descriptor validator checks that the value kind matches the declared\\nquantum-number kind.\\n(defun parse-field-ref (fields value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (field-shape-id (table-get fields (field-key value) \\"field\\")))))\\n\\n(defun parse-quantum-value (theory value)\\n  (etypecase value\\n    (integer value)\\n    (rational value)\\n    (string `(:symbol ,(add-symbol theory value)))\\n    (symbol `(:symbol ,(add-symbol theory (string-downcase (string value)))))\\n    (cons\\n     (ecase (first value)\\n       ((:rational rational)\\n        `(:rational ,(second value) ,(third value)))))))\\n\\n(defun parse-field-quantum-numbers (theory quantum-numbers field-id specs)\\n  (dolist (spec specs)\\n    (destructuring-bind (name value) spec\\n      (add-field-quantum-number theory\\n                                field-id\\n                                (parse-quantum-number-ref quantum-numbers name)\\n                                (parse-quantum-value theory value)))))\\n\\n(defun parse-label-ref (labels field value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get labels (label-key field value) \\"field label\\"))))\\nMetadataMetadata forms describe field weights and similar declaration-time expressions.\\nThey lower to the compact descriptor metadata language.\\n(defun parse-metadata-form (theory parameters fields labels form)\\n  (etypecase form\\n    (integer form)\\n    (cons\\n     (ecase (first form)\\n       ((:rational rational)\\n        `(:rational ,(second form) ,(third form)))\\n       ((:parameter parameter)\\n        `(:parameter ,(parse-parameter-ref parameters (second form))))\\n       ((:field-label field-label)\\n        `(:field-label ,(parse-field-ref fields (second form))\\n                       ,(parse-label-ref labels (second form) (third form))))\\n       ((:add add +)\\n        `(:add ,(parse-metadata-id theory parameters fields labels (second form))\\n               ,(parse-metadata-id theory parameters fields labels (third form))))\\n       ((:mul mul *)\\n        `(:mul ,(parse-metadata-id theory parameters fields labels (second form))\\n               ,(parse-metadata-id theory parameters fields labels (third form))))\\n       ((:bilinear bilinear dot)\\n        `(:bilinear ,(parse-metadata-id theory parameters fields labels (second form))\\n                    ,(parse-metadata-id theory parameters fields labels (third form))\\n                    ,(fourth form)))))))\\nThe descriptor builder interns duplicate metadata rows, so repeated symbols\\nsuch as \\\\(k\\\\) in \\\\(k \\\\cdot k\\\\) do not allocate duplicate descriptor nodes.\\n(defun parse-metadata-id (theory parameters fields labels form)\\n  (add-metadata theory (parse-metadata-form theory parameters fields labels form)))\\nWick TermsScalar factors are user-facing products of exact rationals, i, and named\\nparameters.  The parser lowers common products to the compact descriptor tags\\nused by Zig.\\n(defun i-symbol-p (value)\\n  (and (symbolp value) (string-equal value \'i)))\\n\\n(defun rational-factor (value)\\n  (cond\\n    ((integerp value) value)\\n    ((rationalp value) value)\\n    ((and (consp value) (member (first value) \'(:rational rational)))\\n     (/ (second value) (third value)))\\n    (t nil)))\\nThe product parser is intentionally narrow.  Unsupported scalar forms fail at\\npreset compile time instead of leaking into the generated descriptor.\\n(defun scalar-product-factors (form)\\n  (if (and (consp form) (member (first form) \'(* :mul mul)))\\n      (rest form)\\n      (list form)))\\n\\n(defun scalar-factor-product (parameters form)\\n  (let ((coefficient 1)\\n        (imaginary-power 0)\\n        (parameter nil))\\n    (dolist (factor (scalar-product-factors form))\\n      (let ((rational (rational-factor factor)))\\n        (cond\\n          (rational\\n           (setf coefficient (* coefficient rational)))\\n          ((i-symbol-p factor)\\n           (setf imaginary-power (mod (1+ imaginary-power) 4)))\\n          ((and (consp factor) (member (first factor) \'(:parameter parameter)))\\n           (when parameter\\n             (error \\"Scalar factor has more than one parameter: ~S.\\" form))\\n           (setf parameter (parse-parameter-ref parameters (second factor))))\\n          ((symbolp factor)\\n           (when parameter\\n             (error \\"Scalar factor has more than one parameter: ~S.\\" form))\\n           (setf parameter (parse-parameter-ref parameters factor)))\\n          (t\\n           (error \\"Unsupported scalar factor ~S in ~S.\\" factor form)))))\\n    (values coefficient imaginary-power parameter)))\\nThe final lowering preserves the compact descriptor format without exposing it\\nto preset authors.\\n(defun parse-scalar-factor (parameters form)\\n  (if (eq form :one)\\n      :one\\n      (multiple-value-bind (coefficient imaginary-power parameter)\\n          (scalar-factor-product parameters form)\\n        (cond\\n          ((and (null parameter) (zerop imaginary-power) (= coefficient 1))\\n           :one)\\n          ((and (null parameter) (zerop imaginary-power))\\n           `(:rational ,(numerator coefficient) ,(denominator coefficient)))\\n          ((and parameter (zerop imaginary-power) (= coefficient 1))\\n           `(:parameter ,parameter))\\n          ((and parameter (zerop imaginary-power) (= coefficient 1/2))\\n           `(:parameter-half ,parameter))\\n          ((and parameter (zerop imaginary-power) (= coefficient -1/2))\\n           `(:neg-parameter-half ,parameter))\\n          ((and parameter (= imaginary-power 1) (= coefficient -1/2))\\n           `(:neg-i-parameter-half ,parameter))\\n          (t\\n           (error \\"Scalar factor cannot be lowered to the current compact descriptor: ~S.\\"\\n                  form))))))\\nCoordinate factors are already descriptor-shaped.  The parser only normalizes\\noperator names and preserves coordinate slots.\\n(defun parse-coordinate-factor (form)\\n  (ecase (first form)\\n    ((:difference-power difference-power)\\n     `(:difference-power ,(second form)\\n                         ,(third form)\\n                         ,(fourth form)\\n                         ,@(cddddr form)))\\n    ((:named-kernel named-kernel)\\n     `(:named-kernel ,(second form)\\n                     ,(third form)\\n                     ,(fourth form)\\n                     ,@(cddddr form)))\\n    ((:green-exponential green-exponential)\\n     `(:green-exponential ,(second form)\\n                          ,(third form)))))\\nTensor factors need the current Wick pair because (:left mu) and (:right k)\\nrefer to labels on different fields.\\n(defun parse-side-label-ref (labels left-field right-field ref)\\n  (destructuring-bind (side value) ref\\n    (ecase side\\n      (:left `(:left ,(parse-label-ref labels left-field value)))\\n      (:right `(:right ,(parse-label-ref labels right-field value))))))\\n(defun parse-tensor-factor (labels left-field right-field form)\\n  (ecase (first form)\\n    ((:metric metric)\\n     `(:metric ,(parse-side-label-ref labels left-field right-field (second form))\\n               ,(parse-side-label-ref labels left-field right-field (third form))))\\n    ((:momentum-index momentum-index)\\n     `(:momentum-index ,(parse-side-label-ref labels left-field right-field (second form))\\n                       ,(parse-side-label-ref labels left-field right-field (third form))))\\n    ((:momentum-pair momentum-pair)\\n     `(:momentum-pair ,(parse-side-label-ref labels left-field right-field (second form))\\n                      ,(parse-side-label-ref labels left-field right-field (third form))))))\\nThe high-level Wick syntax binds field-call arguments to endpoint-local label\\nand coordinate refs.  Single fields have one position coordinate; pair fields\\nuse holomorphic and antiholomorphic slots.\\n(defun insertion-coordinate-slots (insertion)\\n  (ecase insertion\\n    (:single \'(:position))\\n    (:pair \'(:holomorphic :antiholomorphic))))\\n\\n(defun coordinate-ref-form (side slot)\\n  (if (eq slot :position)\\n      side\\n      (list side slot)))\\n(defun bind-field-call (fields side call label-env coordinate-env)\\n  (destructuring-bind (field &rest args) call\\n    (let* ((shape (table-get fields (field-key field) \\"field\\"))\\n           (label-count (length (field-shape-labels shape)))\\n           (slots (insertion-coordinate-slots (field-shape-insertion shape)))\\n           (labels (subseq args 0 label-count))\\n           (coordinates (subseq args label-count)))\\n      (unless (= (length coordinates) (length slots))\\n        (error \\"Field call ~S has ~D coordinate arguments; expected ~D.\\"\\n               call (length coordinates) (length slots)))\\n      (loop for label in labels\\n            for slot from 0\\n            do (setf (gethash label label-env) (list side slot)))\\n      (loop for coordinate in coordinates\\n            for slot in slots\\n            do (setf (gethash coordinate coordinate-env)\\n                     (coordinate-ref-form side slot)))\\n      field)))\\nExpression lowering is intentionally small: products, coordinate differences,\\nnamed coordinate kernels, Green exponentials, and the supported tensor\\nstructures.\\n(defun env-ref (env name label)\\n  (multiple-value-bind (value found) (gethash name env)\\n    (unless found\\n      (error \\"Unknown ~A variable ~S.\\" label name))\\n    value))\\n\\n(defun coordinate-ref-position-p (ref)\\n  (symbolp ref))\\n\\n(defun difference-form-p (form)\\n  (and (consp form) (member (first form) \'(- :difference difference))))\\n(defun lower-difference-power (coordinate-env difference exponent)\\n  (unless (and (difference-form-p difference)\\n               (= (length difference) 3))\\n    (error \\"Expected coordinate difference, got ~S.\\" difference))\\n  (let ((left (env-ref coordinate-env (second difference) \\"coordinate\\"))\\n        (right (env-ref coordinate-env (third difference) \\"coordinate\\")))\\n    `(:difference-power ,left ,right ,exponent\\n                        ,@(when (coordinate-ref-position-p left) \'(:derive-left))\\n                        ,@(when (coordinate-ref-position-p right) \'(:derive-right)))))\\n(defun named-coordinate-kernel-p (form coordinate-env)\\n  (and (consp form)\\n       (= (length form) 3)\\n       (symbolp (first form))\\n       (gethash (second form) coordinate-env)\\n       (gethash (third form) coordinate-env)))\\n\\n(defun parse-expression-coordinate (coordinate-env form)\\n  (cond\\n    ((and (consp form) (member (first form) \'(/ :div div))\\n          (= (length form) 3)\\n          (eql (second form) 1))\\n     (lower-difference-power coordinate-env (third form) -1))\\n    ((and (consp form) (member (first form) \'(pow expt))\\n          (= (length form) 3))\\n     (lower-difference-power coordinate-env (second form) (third form)))\\n    ((and (consp form) (member (first form) \'(green-exp green-exponential))\\n          (= (length form) 3))\\n     `(:green-exponential ,(env-ref coordinate-env (second form) \\"coordinate\\")\\n                          ,(env-ref coordinate-env (third form) \\"coordinate\\")))\\n    ((named-coordinate-kernel-p form coordinate-env)\\n     `(:named-kernel ,(first form)\\n                     ,(env-ref coordinate-env (second form) \\"coordinate\\")\\n                     ,(env-ref coordinate-env (third form) \\"coordinate\\")\\n                     :derive-left))\\n    (t nil)))\\n(defun parse-expression-tensor (label-env form)\\n  (when (consp form)\\n    (case (first form)\\n      ((metric)\\n       `(:metric ,(env-ref label-env (second form) \\"label\\")\\n                 ,(env-ref label-env (third form) \\"label\\")))\\n      ((momentum-index)\\n       `(:momentum-index ,(env-ref label-env (second form) \\"label\\")\\n                         ,(env-ref label-env (third form) \\"label\\")))\\n      ((momentum-pair)\\n       `(:momentum-pair ,(env-ref label-env (second form) \\"label\\")\\n                        ,(env-ref label-env (third form) \\"label\\"))))))\\nThe expression term collector classifies product factors and leaves all scalar\\nfactors together so (* -1/2 i alpha-prime) lowers as one scalar.\\n(defun expression-product-factors (form)\\n  (if (and (consp form) (member (first form) \'(* :mul mul)))\\n      (loop for factor in (rest form) append (expression-product-factors factor))\\n      (list form)))\\n\\n(defun parse-expression-term (parameters label-env coordinate-env expression residuals)\\n  (let ((scalar-factors nil)\\n        (coordinates nil)\\n        (tensors nil))\\n    (dolist (factor (expression-product-factors expression))\\n      (let ((coordinate (parse-expression-coordinate coordinate-env factor))\\n            (tensor (parse-expression-tensor label-env factor)))\\n        (cond\\n          (coordinate (push coordinate coordinates))\\n          (tensor (push tensor tensors))\\n          (t (push factor scalar-factors)))))\\n    (make-wick-term\\n     :scalars (list (parse-scalar-factor\\n                     parameters\\n                     (if scalar-factors\\n                         `(* ,@(reverse scalar-factors))\\n                         :one)))\\n     :coordinates (reverse coordinates)\\n     :tensors (reverse tensors)\\n     :residuals residuals)))\\nThe descriptor-shaped term form remains as an explicit escape hatch for\\ngenerated or debugging code.\\n(defun parse-term (parameters labels left-field right-field form)\\n  (unless (eq (first form) \'term)\\n    (error \\"Expected TERM form, got ~S.\\" form))\\n  (let ((body (rest form)))\\n    (make-wick-term\\n     :scalars (mapcar (lambda (item) (parse-scalar-factor parameters item))\\n                      (option-value body :scalars \'(:one)))\\n     :coordinates (mapcar #\'parse-coordinate-factor\\n                          (option-value body :coordinates nil))\\n     :tensors (mapcar (lambda (item)\\n                        (parse-tensor-factor labels left-field right-field item))\\n                      (option-value body :tensors nil))\\n     :residuals (option-value body :residuals nil))))\\nDeclarationsDeclaration forms append descriptor rows in source order.  Quantum-number\\nschema rows are separate from field quantum-number assignments so the later\\nbasis generator can scan them without touching Wick data.\\n(defun apply-preset-form\\n    (theory parameters quantum-numbers surfaces fields labels form)\\n  (ecase (first form)\\n    (parameter\\n     (setf (gethash (second form) parameters)\\n           (add-parameter theory (third form) (or (fourth form) :scalar-parameter))))\\n    (quantum-number\\n     (setf (gethash (second form) quantum-numbers)\\n           (add-quantum-number theory\\n                               (or (option-value (cdddr form) :symbol)\\n                                   (string-downcase (string (second form))))\\n                               (parse-quantum-number-kind (third form))\\n                               :group (option-value (cdddr form) :group))))\\n    (surface\\n     (setf (gethash (second form) surfaces)\\n           (add-surface theory (third form) (fourth form)\\n                        :modular-parameter\\n                        (when (option-value (cddddr form) :modular-parameter)\\n                          (parse-parameter-ref\\n                           parameters\\n                           (option-value (cddddr form) :modular-parameter))))))\\n    (field\\n     (destructuring-bind (id symbol insertion label-spec statistics &rest options) (rest form)\\n       (let* ((field-id (register-field-shape fields labels id label-spec))\\n              (weight-form (option-value options :weight))\\n              (anti-weight-form (option-value options :anti-weight))\\n              (weight-id (when weight-form\\n                           (parse-metadata-id theory parameters fields labels weight-form)))\\n              (anti-weight-id (cond\\n                                ((null anti-weight-form) nil)\\n                                ((equal anti-weight-form weight-form) weight-id)\\n                                (t (parse-metadata-id theory parameters fields labels\\n                                                      anti-weight-form)))))\\n         (set-field-shape-insertion fields id insertion)\\n         (unless (= field-id\\n                    (add-field theory symbol insertion label-spec statistics\\n                               :zero-mode (option-value options :zero-mode)\\n                               :weight weight-id\\n                               :anti-weight anti-weight-id))\\n           (error \\"Field table drift while declaring ~S.\\" id))\\n         (parse-field-quantum-numbers\\n          theory quantum-numbers field-id\\n          (option-value options :quantum-numbers nil)))))\\n    (wick\\n     (destructuring-bind (surface-id left &rest body) (rest form)\\n       (if (and body (consp (first body)) (not (eq (caar body) \'term)))\\n           (let* ((right (first body))\\n                  (expression (second body))\\n                  (options (cddr body))\\n                  (label-env (make-hash-table))\\n                  (coordinate-env (make-hash-table))\\n                  (left-field (bind-field-call fields :left left label-env coordinate-env))\\n                  (right-field (bind-field-call fields :right right label-env coordinate-env)))\\n             (add-wick-rule\\n              theory\\n              (table-get surfaces surface-id \\"surface\\")\\n              (parse-field-ref fields left-field)\\n              (parse-field-ref fields right-field)\\n              (list (parse-expression-term parameters label-env coordinate-env\\n                                           expression\\n                                           (option-value options :residuals nil)))))\\n           (destructuring-bind (left-field right-field) left\\n             (add-wick-rule\\n              theory\\n              (table-get surfaces surface-id \\"surface\\")\\n              (parse-field-ref fields left-field)\\n              (parse-field-ref fields right-field)\\n              (mapcar (lambda (term)\\n                        (parse-term parameters labels left-field right-field term))\\n                      body))))))\\n    (zero-mode\\n     (destructuring-bind (surface-id kind consumes &rest options) (rest form)\\n       (add-zero-mode theory\\n                      (table-get surfaces surface-id \\"surface\\")\\n                      kind\\n                      (mapcar (lambda (field) (parse-field-ref fields field)) consumes)\\n                      :normalization (option-value options :normalization :one)\\n                      :two-pi-power (option-value options :two-pi-power 0))))))\\nBuilding a preset uses only small name tables and returns the descriptor theory.\\n(defun build-cft-preset (name hash forms)\\n  (let ((theory (make-theory name hash))\\n        (parameters (make-hash-table))\\n        (quantum-numbers (make-hash-table))\\n        (surfaces (make-hash-table))\\n        (fields (make-hash-table :test #\'equal))\\n        (labels (make-hash-table :test #\'equal)))\\n    (dolist (form forms theory)\\n      (apply-preset-form\\n       theory parameters quantum-numbers surfaces fields labels form))))\\nFixture EmissionPresets are emitted in generated theory-id order.  The ids are assigned when\\nthe preset files load through ASDF.\\n(defun sorted-presets ()\\n  (sort (loop for preset being the hash-values of *presets*\\n              collect preset)\\n        #\'< :key #\'preset-theory-id))\\n(defun preset-name-parts (preset)\\n  (drop-trailing-numeric-parts (split-name-parts (preset-name preset))))\\n\\n(defun preset-family-parts (preset)\\n  (drop-trailing-surface-part (preset-name-parts preset)))\\n\\n(defun same-family-p (left right)\\n  (equal (preset-family-parts left) (preset-family-parts right)))\\n\\n(defun preset-family-count (preset presets)\\n  (loop for item in presets count (same-family-p preset item)))\\nGenerated Zig names are derived from the preset set.  A family with one surface\\ngets a short type such as Bc; a family with multiple surfaces keeps the\\nsurface suffix, such as EtaXiSphere.\\n(defun generated-name-parts (preset presets)\\n  (if (> (preset-family-count preset presets) 1)\\n      (preset-name-parts preset)\\n      (preset-family-parts preset)))\\n\\n(defun generated-prefix (preset presets)\\n  (join-name-parts (generated-name-parts preset presets) \\"_\\"))\\n\\n(defun generated-descriptor-name (preset presets)\\n  (format nil \\"~A_descriptor\\" (generated-prefix preset presets)))\\n\\n(defun capitalized-name-part (part)\\n  (string-capitalize part))\\n\\n(defun generated-type-name (preset presets)\\n  (with-output-to-string (stream)\\n    (dolist (part (generated-name-parts preset presets))\\n      (write-string (capitalized-name-part part) stream))))\\n(defun preset-key (name)\\n  (etypecase name\\n    (symbol\\n     (or (find-symbol (symbol-name name) (find-package \'#:string-code.cft.presets))\\n         name))\\n    (string\\n     (find-symbol (string-upcase name) (find-package \'#:string-code.cft.presets)))))\\n\\n(defun registered-preset (name)\\n  (table-get *presets* (preset-key name) \\"preset\\"))\\n\\n(defun preset-theory (name)\\n  \\"Build the descriptor theory declared by preset NAME.\\"\\n  (funcall (preset-builder (registered-preset name))))\\n\\n(defun preset-theory-id-for (name)\\n  \\"Return the generated ABI theory id for preset NAME.\\"\\n  (preset-theory-id (registered-preset name)))\\nThe prologue is the only Zig syntax that is not descriptor data.\\n(defun emit-fixture-prologue (stream)\\n  (format stream \\"const descriptor = @import(\\\\\\"descriptor.zig\\\\\\");~%~%\\")\\n  (format stream \\"const d = descriptor;~%~%\\")\\n  (format stream \\"fn cref(side: d.Side) d.CoordinateRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = .position };~%\\")\\n  (format stream \\"}~%~%\\")\\n  (format stream \\"fn bulk(side: d.Side, slot: d.CoordinateSlot) d.CoordinateRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = slot };~%\\")\\n  (format stream \\"}~%~%\\")\\n  (format stream \\"fn lref(side: d.Side, slot: d.Id) d.LabelRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = slot };~%\\")\\n  (format stream \\"}~%~%\\"))\\nEach preset becomes one descriptor plus one generated theory type.\\n(defun emit-fixture-type (stream preset presets)\\n  (emit-zig-descriptor (funcall (preset-builder preset)) stream\\n                       :prefix (generated-prefix preset presets)\\n                       :descriptor-name (generated-descriptor-name preset presets))\\n  (format stream \\"~%/// ~A is the lowered generated preset.~%\\"\\n          (generated-type-name preset presets))\\n  (format stream \\"pub const ~A = d.GeneratedTheory(~A);~%~%\\"\\n          (generated-type-name preset presets)\\n          (generated-descriptor-name preset presets)))\\nThe generated fixture self-test checks descriptor validity without allocating\\ncorrelator expressions.\\n(defun emit-fixture-self-test (stream presets)\\n  (format stream \\"/// selfTest validates that generated preset descriptors are loadable.~%\\")\\n  (format stream \\"pub fn selfTest() !void {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    try d.validateDescriptor(~A);~%\\"\\n            (generated-descriptor-name preset presets)))\\n  (format stream \\"}~%\\"))\\n(defun write-generated-fixtures\\n    (&key (path \\"src/cft-code/correlators/generated_fixtures.zig\\"))\\n  \\"Write the Zig fixture module from registered Lisp presets.\\"\\n  (with-open-file (stream path :direction :output :if-exists :supersede\\n                               :if-does-not-exist :create)\\n    (let ((presets (sorted-presets)))\\n      (emit-fixture-prologue stream)\\n      (dolist (preset presets)\\n        (emit-fixture-type stream preset presets))\\n      (emit-fixture-self-test stream presets)))\\n  path)\\nDispatch EmissionThe dispatch module is generated with the fixtures.  It owns TheoryId, the\\ncontext union, and every switch over generated theory types.\\n(defun emit-dispatch-prologue (stream)\\n  (format stream \\"const std = @import(\\\\\\"std\\\\\\");~%\\")\\n  (format stream \\"const fixtures = @import(\\\\\\"generated_fixtures.zig\\\\\\");~%\\")\\n  (format stream \\"const kernel = @import(\\\\\\"../kernel.zig\\\\\\");~%~%\\")\\n  (format stream \\"const allocator = std.heap.c_allocator;~%~%\\"))\\n\\n(defun emit-theory-id (stream presets)\\n  (format stream \\"/// TheoryId selects one generated fixture through the generic ABI.~%\\")\\n  (format stream \\"pub const TheoryId = enum(u32) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    ~A = ~D,~%\\"\\n            (generated-prefix preset presets)\\n            (preset-theory-id preset)))\\n  (format stream \\"};~%~%\\")\\n  (format stream \\"pub const first_theory_id = TheoryId.~A;~%~%\\"\\n          (generated-prefix (first presets) presets)))\\n(defun emit-context-fields (stream presets)\\n  (format stream \\"pub const ContextTag = union(TheoryId) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    ~A: *fixtures.~A.Context,~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"~%\\"))\\nThe context methods are direct switches.  They do not allocate and expose only\\nthe operations the C ABI needs.\\n(defun emit-create-method (stream presets)\\n  (format stream \\"    pub fn create(id: TheoryId) !ContextTag {~%\\")\\n  (format stream \\"        return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => .{ .~A = try fixtures.~A.contextCreate(allocator) },~%\\"\\n            (generated-prefix preset presets)\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-destroy-method (stream presets)\\n  (format stream \\"    pub fn destroy(self: ContextTag) void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.contextDestroy(inner),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-symbol-intern-method (stream presets)\\n  (format stream \\"    pub fn symbolIntern(self: ContextTag, name: []const u8) !u32 {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.symbolIntern(inner, name),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-field-insert-method (stream presets)\\n  (format stream \\"    pub fn fieldInsert(self: ContextTag, field_id: u16, coords: []const u32, labels: []const u32) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| try fixtures.~A.fieldInsert(inner, field_id, coords, labels),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-normal-ordering-method (stream presets)\\n  (format stream \\"    pub fn normalOrdering(self: ContextTag, field_count: usize) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| try fixtures.~A.normalOrdering(inner, field_count),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-freeze-method (stream presets)\\n  (format stream \\"    pub fn freeze(self: ContextTag) !kernel.Call.MultiOp {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.operatorListFreeze(inner),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-count-method (stream presets)\\n  (format stream \\"    pub fn count(self: ContextTag, ops: kernel.Call.MultiOp) !usize {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => fixtures.~A.correlatorCount(ops),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-run-method (stream presets)\\n  (format stream \\"    pub fn run(self: ContextTag, ops: kernel.Call.MultiOp, state: anytype, thunk: anytype) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => try fixtures.~A.correlatorRun(ops, state, thunk),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-context-end (stream)\\n  (format stream \\"};~%~%\\"))\\nThe remaining dispatch functions decode raw ids and expose generated theory\\nhashes to Lisp.\\n(defun emit-scalar-atom-name-function (stream presets)\\n  (format stream \\"pub fn scalarAtomParameterName(id: TheoryId, atom: u32) ?[]const u8 {~%\\")\\n  (format stream \\"    return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        .~A => fixtures.~A.scalarAtomParameterName(atom),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%~%\\"))\\n\\n(defun emit-theory-id-function (stream presets)\\n  (format stream \\"pub fn theoryId(raw: u32) !TheoryId {~%\\")\\n  (format stream \\"    return switch (raw) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        ~D => .~A,~%\\"\\n            (preset-theory-id preset)\\n            (generated-prefix preset presets)))\\n  (format stream \\"        else => error.UnknownTheory,~%\\")\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%~%\\"))\\n(defun emit-theory-hash-function (stream presets)\\n  (format stream \\"pub fn theoryHash(id: TheoryId) u32 {~%\\")\\n  (format stream \\"    return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        .~A => fixtures.~A.theoryHash(),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%\\"))\\n\\n(defun write-generated-dispatch\\n    (&key (path \\"src/cft-code/correlators/generated_dispatch.zig\\"))\\n  \\"Write the generated Zig dispatcher for registered presets.\\"\\n  (let ((presets (sorted-presets)))\\n    (with-open-file (stream path :direction :output :if-exists :supersede\\n                                 :if-does-not-exist :create)\\n      (emit-dispatch-prologue stream)\\n      (emit-theory-id stream presets)\\n      (emit-context-fields stream presets)\\n      (emit-create-method stream presets)\\n      (emit-destroy-method stream presets)\\n      (emit-symbol-intern-method stream presets)\\n      (emit-field-insert-method stream presets)\\n      (emit-normal-ordering-method stream presets)\\n      (emit-freeze-method stream presets)\\n      (emit-count-method stream presets)\\n      (emit-run-method stream presets)\\n      (emit-context-end stream)\\n      (emit-scalar-atom-name-function stream presets)\\n      (emit-theory-id-function stream presets)\\n      (emit-theory-hash-function stream presets)))\\n  path)\\nCompilationThe compile helper writes the generated fixture and dispatch sources imported\\nby src/cft-code/generated_abi.zig.\\n(defun write-generated-sources ()\\n  \\"Write all generated Zig modules derived from registered Lisp presets.\\"\\n  (write-generated-fixtures)\\n  (write-generated-dispatch))\\n\\n(defun compile-preset-library\\n    (&key\\n       (library-path \\"zig-out/lib/libstring_code_cft_generated.so\\")\\n       (global-cache-dir \\"/tmp/zig-global-cache\\")\\n       (zig \\"zig\\"))\\n  \\"Emit registered presets and compile the generated C ABI shared library.\\"\\n  (write-generated-sources)\\n  (uiop:run-program\\n   (list zig \\"build-lib\\" \\"-OReleaseFast\\"\\n         \\"--dep\\" \\"tensor-code\\"\\n         \\"-Mroot=src/cft-code/generated_abi.zig\\"\\n         \\"-Mtensor-code=src/tensor-code/tensor-code.zig\\"\\n         \\"-lc\\" \\"--name\\" \\"string_code_cft_generated\\"\\n         \\"-dynamic\\" \\"--global-cache-dir\\" global-cache-dir\\n         \\"-femit-bin\\" library-path)\\n   :output *standard-output*\\n   :error-output *error-output*)\\n  library-path)\\n"},{"id":"20260609T004100.000000","title":"Lisp Free Fermion Preset","tags":null,"content":"This preset declares the indexed sphere free fermion.  The only Wick datum is\\nthe \\\\(\\\\eta_{\\\\mu\\\\nu}/(z-w)\\\\) contraction.\\n(in-package #:string-code.cft.presets)\\n(define-cft-preset free-fermion-10\\n  (surface sphere :sphere :rational)\\n  (quantum-number spin10 :ade-irrep :group d5)\\n  (field psi \\"psi\\" :single ((mu :vector-index)) :fermionic\\n         :quantum-numbers ((spin10 vector)))\\n  (wick sphere (psi mu z) (psi nu w)\\n    (* (metric mu nu) (/ 1 (- z w)))))\\n"},{"id":"20260609T004300.000000","title":"Lisp bc Sphere Preset","tags":null,"content":"The sphere \\\\(bc\\\\) preset declares a single \\\\(b(z)c(w)\\\\) rational-pole\\ncontraction and the \\\\(c\\\\) top-form zero mode.\\n(in-package #:string-code.cft.presets)\\n(define-cft-preset bc-sphere\\n  (surface sphere :sphere :rational)\\n  (quantum-number ghost-number :u1)\\n  (field b \\"b\\" :single nil :fermionic\\n         :quantum-numbers ((ghost-number -1)))\\n  (field c \\"c\\" :single nil :fermionic\\n         :zero-mode t\\n         :quantum-numbers ((ghost-number 1)))\\n  (wick sphere (b z) (c w)\\n    (/ 1 (- z w)))\\n  (zero-mode sphere :top-form-fermion (c)))\\n"},{"id":"20260609T004400.000000","title":"Lisp Free Boson Preset","tags":null,"content":"This preset declares the ten-dimensional free boson on the sphere.  The scalar\\nparameter alpha-prime is symbolic in the descriptor and can be assigned a\\nruntime convention value by the Lisp REPL layer.\\n(in-package #:string-code.cft.presets)\\nThe field declarations keep label-dependent conformal weights in descriptor\\nmetadata.  The \\\\(k \\\\cdot k\\\\) expression references the expX momentum label\\nby name, not by a numeric slot.\\n(define-cft-preset free-boson-10\\n  (parameter alpha-prime \\"alpha-prime\\" :scalar-parameter)\\n  (surface sphere :sphere :rational)\\n  (quantum-number spin10 :ade-irrep :group d5)\\n  (field dX \\"dX\\" :single ((mu :vector-index)) :bosonic\\n         :quantum-numbers ((spin10 vector)))\\n  (field expX \\"expX\\" :pair ((k :momentum)) :bosonic\\n         :zero-mode t\\n         :weight (* (rational 1 4)\\n                    (* (parameter alpha-prime)\\n                       (dot (field-label expX k)\\n                            (field-label expX k)\\n                            eta)))\\n         :anti-weight (* (rational 1 4)\\n                         (* (parameter alpha-prime)\\n                            (dot (field-label expX k)\\n                                 (field-label expX k)\\n                                 eta))))\\nThe \\\\(dX dX\\\\) contraction carries the metric tensor and the second-order\\nrational pole.\\n  (wick sphere (dX mu z) (dX nu w)\\n    (* -1/2 alpha-prime\\n       (metric mu nu)\\n       (pow (- z w) -2)))\\nThe \\\\(dX\\\\,e^{ikX}\\\\) contraction leaves the plane wave as a residual operator,\\nwhich keeps the expansion tree fixed for the streaming kernel.\\n  (wick sphere (dX mu z) (expX k w wb)\\n    (* -1/2 i alpha-prime\\n       (momentum-index k mu)\\n       (/ 1 (- z w)))\\n    :residuals (:right))\\nThe exponential-exponential rule emits holomorphic and antiholomorphic Green\\nfactors with the shared momentum-pair tensor.\\n  (wick sphere (expX p z zb) (expX k w wb)\\n    (* 1/2 alpha-prime\\n       (momentum-pair p k)\\n       (green-exp z w)\\n       (green-exp zb wb))\\n    :residuals (:left :right))\\nThe sphere zero-mode factor is the stringbook momentum-conservation\\nnormalization for ten noncompact bosons.\\n  (zero-mode sphere :boson-momentum-conservation (expX) :two-pi-power 10))\\n"}]'),ts=t(47710);let tl=function(e){let n=arguments.length>1&&void 0!==arguments[1]?arguments[1]:[],t=[],r=0;return n.forEach((n,i)=>{let a=n[1]+1;t.push(e.substring(r,n[0]).replace(" ","\xa0"),(0,o.jsx)(H.x,{as:"u",children:e.substring(n[0],a).replace(" ","\xa0")},n+" "+i)),r=a}),t.push(e.substring(r).replace(" ","\xa0")),(0,o.jsx)(o.Fragment,{children:t})},tc=e=>{let{result:n,onClick:t}=e,{highlightColor:r,emacsTheme:i}=(0,_.useContext)(eX.N),a=i[1],{id:s,tags:l,title:c}=n.item,d=n.matches,u=(0,_.useMemo)(()=>null==d?void 0:d.find(e=>"title"===e.key),[d]);return(0,o.jsxs)(ti.HC,{p:2,borderRadius:4,cursor:"pointer",bgColor:a["bg-alt"],_hover:{borderColor:r,color:r,opacity:.8},onClick:()=>t(s),children:[(0,o.jsx)(A.X,{size:"sm",children:u?tl(c,u.indices):c}),l&&(0,o.jsx)(ts.E,{marginTop:2,spacingY:1,children:l.map(e=>(0,o.jsx)(ts.U,{children:(0,o.jsx)(el.Vp,{size:"sm",colorScheme:"blue",children:e})},e))})]})};var td=JSON.parse('{"keys":[{"path":["tags"],"id":"tags","weight":1,"src":"tags"},{"path":["title"],"id":"title","weight":1,"src":"title"},{"path":["content"],"id":"content","weight":1,"src":"content"}],"records":[{"i":0,"$":{"1":{"v":"Overview","n":1},"2":{"v":"The aim of this project is to provide a set of convenient and performant tools for performing worldsheet string theory calculations. The project is split into the following parts:\\nCFT-Code is the kernel for performing worldsheet CFT calculations. It defines local operators, their OPE and correlation functions (on various Riemann surfaces).\\nTensor-Code defines tensor structures and provides utilities for their manipulation and generation.\\nOn-Shell-Code defines string amplitudes in the on-shell formalism.\\nSFT-Code defines string field theory vertices and brackets given an abstract set of local coordinate data. It provides utilities for the local coordinate data construction of open-closed \\\\(SL(2)\\\\) vertices.\\nNLSM-Code defines utilities for quantizing nonlinear sigma models on weakly curved target spacetimes. As such it defines the Polyakov path integral in a suitable regularization scheme. Composite operators are defined by inserting fields into the regularized path integral.\\nPure-Spinor-Code defines utilities for quantizing the \\\\(AdS_5 \\\\times S^5\\\\) pure spinor worldsheet theory by defining its path integral in a suitable regularization scheme. Composite operators are defined by inserting fields into the regularized path integral.\\nFor each part, we have a few physical goals in mind, guiding the implementation requirements:\\nCFT-CodeCovariantly solve R-sector OPE and correlation functions for GSO-projected free fermion + ghosts theories. By solving, we mean to all mass levels and at practical speeds. This provides the basic ingredients for the treatment of RR backgrounds in SFT and for computing elusive high-point on-shell amplitudes involving the R-sector.\\nTensor-CodeFor many applications (beyond the worldsheet), one needs to quickly generate independent invariant tensors contained in some tensor product of representations. Current Lie algebra packages are severely lacking in this regarding, and we aim to fill this gap.\\nOn-Shell-CodeCompute higher-point string amplitudes. A good testing ground is to reproduce known results regarding the SUSY completion of \\\\(R^4\\\\) term of Type IIB superstring, and to extend it further. This will require very efficient handling of tensor structures on both the string theory and the EFT side. For example, we expect there to be a 16-dilatino term and a 16-dilatino amplitude should have about 80 million independent invariant tensors in it.\\nAs a check, we also aim to compute the RR and NSNS amplitudes at four-point tree-level and one-loop and check they are related by SUSY . This is nontrivial, the R-sector amplitudes involve excited spin fields upon picture-raising.\\nSFT-CodeCompute the anomalous scaling dimension of the Konishi string in the large radius \\\\(AdS_5 \\\\times S^5\\\\) regime. To begin with, at tree-level, but more ambitiously also incorporate string loops, which go beyond integrability.\\nCarry out loop calculations on Calabi-Yau orientifolds, expressing the answers in terms of the 2d \\\\((2,2)\\\\) SCFT conformal data.\\nNLSM-CodeGiven numeric Calabi-Yau metrics, obtain the 2d \\\\((2,2)\\\\) SCFT conformal data in a large volume expansion. A good test-case is the IIA quintic as it only has a single volume modulus.\\nPure-Spinor-CodeSystematically quantize the \\\\(AdS_5 \\\\times S^5\\\\) pure spinor \\\\(PSU(2,2|4)\\\\) sigma model and use it to compute the pure spinor BRST cohomology at mass level 0 and 1.\\nIn a previously written Mathematica prototype, we have encountered that there are a few major bottlenecks towards achieving these goals.\\nFree field calculations fundamentally lead to a huge combinatorial blow-up of the number of terms involved. When scaling up, it is infeasible to store the intermediate expressions in RAM. Rather, we construct a stream of data that emits the various expressions and introduce filters along the way that extract the desired result. We are then only limited by the size of that result.\\nR-sector OPE was severely limited by the speed at which we could construct invariant tensors. Our algorithm was to construct a largely overcomplete set of invariants and then statistically (by plugging in random components) determine an independent subset. We must now leverage representation theory better to do less computation.\\nWe have not succeeded in writing the code abstractly enough so that adding new CFTs, combining CFTs to form a valid string worldsheet and adding boundaries, orientifolds could be done without large interference with the code. We must now design the CFT-Code kernel abstractly enough to avoid these issues.\\nDue to the large expressions involved, each part has a core written in a performant programming language that handles memory explicitly. We expose a well-defined API that allows one to easily write wrappers for say Python, Mathematica, ... backing a DSL. Also note that having such a concise API to carry out worldsheet calculations is useful for agentic coding as it saves a large amount of context.\\n","n":0.037}}},{"i":1,"$":{"1":{"v":"CFT-Code","n":1},"2":{"v":"The following module is the worldsheet CFT core of the project. It defines symbolic Expressions used in worldsheet CFT calculations i.e. local Operators multiplied by nontrivial Coefficients and computes their OPE and Correlators. It can also generate a basis of local operator with a given set of quantum numbers via Basis-Generation. The data of a CFT or BCFT is specified by generated preset constructors and executed by CFT Kernel.\\nAs far as dependencies go, this module only depends on Tensor-Code, which forms an integral part of the definition of Coefficients and determines the properties of various quantum numbers of Operators.  The public API follows Tensor-Code: implementation nodes such as Presets, Theory, and CFT Kernel organize the code, but clients import this root module and use the selected declarations below.\\nconst preset_impl = @import(\\"presets.zig\\");\\n\\n/// FreeBoson groups the public free-boson preset constructors.\\npub const FreeBoson = preset_impl.FreeBoson;\\n/// Bc groups the public bc-ghost preset constructors.\\npub const Bc = preset_impl.Bc;\\n/// FreeFermion groups the public NS free-fermion preset constructors.\\npub const FreeFermion = preset_impl.FreeFermion;\\n/// EtaXi groups the public eta-xi preset constructors.\\npub const EtaXi = preset_impl.EtaXi;\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub const product = preset_impl.product;\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub const boundary = preset_impl.boundary;\\n\\ntest \\"root CFT API exposes selected constructors without implementation namespaces\\" {\\n    const testing = @import(\\"std\\").testing;\\n    try testing.expect(@hasDecl(@This(), \\"FreeBoson\\"));\\n    try testing.expect(@hasDecl(@This(), \\"Bc\\"));\\n    try testing.expect(@hasDecl(@This(), \\"FreeFermion\\"));\\n    try testing.expect(@hasDecl(@This(), \\"EtaXi\\"));\\n    try testing.expect(@hasDecl(@This(), \\"product\\"));\\n    try testing.expect(@hasDecl(@This(), \\"boundary\\"));\\n    try testing.expect(@hasDecl(FreeBoson, \\"make\\"));\\n    try testing.expect(@hasDecl(FreeBoson, \\"boundary\\"));\\n    try testing.expect(@hasDecl(Bc, \\"sphere\\"));\\n    try testing.expect(@hasDecl(Bc, \\"diskBoundary\\"));\\n    try testing.expect(@hasDecl(FreeFermion, \\"sphere\\"));\\n    try testing.expect(@hasDecl(EtaXi, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(FreeBoson, \\"Config\\"));\\n    try testing.expect(!@hasDecl(FreeBoson, \\"BoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(Bc, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(Bc, \\"DiskConfig\\"));\\n    try testing.expect(!@hasDecl(FreeFermion, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(EtaXi, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"Handle\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"scalars\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeBoson\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"bcSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeFermionSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"etaXiSphere\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"freeBosonBoundary\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"bcDiskBoundary\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"presets\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"kernel\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"theory\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"expressions\\"));\\n    try testing.expect(!@hasDecl(@This(), \\"Generated\\"));\\n}\\nThis root block is the public CFT API for now.  It intentionally groups each\\nCFT family behind one compact object and does not export raw Wick-rule data,\\ncorrelator configs, generated-kernel internals, or the preset implementation\\nnamespace.  Application modules such as on-shell code, SFT code, NLSM code,\\nand pure-spinor code should build theories through the constructors above; a\\nlater public user-defined-CFT DSL should be one deliberate export rather than\\na leak of the preset runtime.\\n","n":0.051}}},{"i":2,"$":{"1":{"v":"SFT-Code","n":1},"2":{"v":"This module defines string field theory (SFT) brackets and vertices for open, closed and open-closed string fields theories (for all five superstrings and the bosonic string). As such, it applies conformal transforms defined by consistent local coordinate data on Operators and follows up by computing their OPE (in case of brackets) or Correlators in the case of vertices, integrated over for now abstract chains in the moduli space, with appropriate PCO insertions in the superstring case.\\n","n":0.115}}},{"i":3,"$":{"1":{"v":"NLSM-Code","n":1},"2":{"v":"This is an overview of NLSM-Code\\n","n":0.408}}},{"i":4,"$":{"1":{"v":"On-Shell-Code","n":1},"2":{"v":"This code consumes CFT-Code and handles on-shell string theory computations. That is, given a set of vertex Operators, it computes their string amplitudes from their Correlators, via prescriptions appropriate for the given string theory. We shall support all five superstrings: heterotic \\\\(SO(32)\\\\), \\\\(E_8 \\\\times E_8\\\\), Type IIA, Type IIB and Type I, and bosonic string theory. In both open, closed and open-closed sectors. For free fields, we can derive the open, open-closed sector data from that of the closed sector given a boundary condition on the fundamental fields (doubling trick). Note that each requires us to define the appropriate ghost system Theory sectors. The superstring theories each come with the a distinct notion of a GSO projection.\\n","n":0.092}}},{"i":5,"$":{"1":{"v":"Pure-Spinor-Code","n":1},"2":{"v":"This module computes OPE and Correlators in the CFT defined by the appropriately regularized pure spinor NLSM on \\\\(PSU(2,2|4)\\\\).\\n","n":0.229}}},{"i":6,"$":{"1":{"v":"Normal-Ordering","n":1},"2":{"v":"This module owns the generic normal-ordering group policy.  The group id stays\\non kernel.Call.LocalOp because Wick traversal needs it in the hot pair loop;\\nthis module only allocates, tags, and compares those compact ids.\\nconst kernel = @import(\\"../kernel.zig\\");\\n\\n/// Group is the compact id stored on local operators in one normal product.\\npub const Group = u16;\\n\\n/// none marks an operator that is not inside a normal-ordered product.\\npub const none: Group = 0;\\n\\n/// first is the first valid context-local normal-ordering group id.\\npub const first: Group = 1;\\nGroup ids are context-local.  Overflow wraps to none, and the next request\\nfails instead of silently producing an untagged normal product.\\n/// next returns a fresh normal-ordering group and advances the counter.\\npub fn next(counter: *Group) !Group {\\n    if (counter.* == none) return error.TooManyNormalOrderedProducts;\\n    const group = counter.*;\\n    counter.* +%= 1;\\n    return group;\\n}\\n\\n/// tag returns OP marked as part of GROUP.\\npub fn tag(op: kernel.Call.LocalOp, group: Group) kernel.Call.LocalOp {\\n    var tagged = op;\\n    tagged.normal_order_group = group;\\n    return tagged;\\n}\\nThe Wick kernel treats equal nonzero group ids as a forbidden contraction pair.\\nBulk tagging is used by both generated descriptor contexts and hand-written\\nZig preset builders.\\n/// same reports whether two operators are in the same nonempty normal product.\\npub fn same(left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) bool {\\n    const group = left.normal_order_group;\\n    return group != none and group == right.normal_order_group;\\n}\\n\\n/// tagLast marks the last COUNT operators as one normal-ordered product.\\npub fn tagLast(ops: []kernel.Call.LocalOp, count: usize, counter: *Group) !void {\\n    if (count == 0 or count > ops.len) return error.InvalidNormalOrderGroup;\\n    const group = try next(counter);\\n    const start = ops.len - count;\\n    for (ops[start..]) |*op| {\\n        op.* = tag(op.*, group);\\n    }\\n}\\n","n":0.062}}},{"i":7,"$":{"1":{"v":"OPE","n":1},"2":{"v":"This module defines the notion of an OPE, which takes symbolic Expressions and computes their OPE (again Expressions) via several possible tactics inferred from the definition of the Theory involved.\\nSince the OPE is a local expression, we evaluate it via the definition of the theory on the Riemann sphere. When the user defines a non-degenerate pairing (for a unitary CFT, can be the usual BPZ pairing) on the basis of Operators, we can evaluate the OPE by writing out all Operators that can appear in the OPE (we know the quantum numbers of the inputs so we know the quantum numbers of the output, being in their tensor product) via Basis-Generation and compute their coefficients by combining the pairing with Correlators. This way, we can immediately transfer optimizations of Correlators and Basis-Generation to an optimized OPE.\\n","n":0.086}}},{"i":8,"$":{"1":{"v":"Operators","n":1},"2":{"v":"const coefficient = @import(\\"coefficient.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nA local operator can be inserted at a point and carry extra information captured in the code by its body.\\n/// A local operator inserted at a point\\npub const Operator = struct {\\n    insertion: OperatorInsertion,\\n    body: OperatorBodyId,\\n};\\n\\n/// An identifier for operator body\\npub const OperatorBodyId = u32;\\nAn operator body can either be that of the identity, a generic atomic operator or a normal-ordered product of operator bodies (in free field theory).\\n/// An operator body is either atomic or normal-ordered\\npub const OperatorBody = union(enum) {\\n    identity,\\n    atomic: AtomicBody,\\n    normal_ordered: []const OperatorBodyId,\\n};\\nAn atomic operator body can come in different kinds specified when defining the Theory and can carry extra runtime information.\\n/// An atomic operator body comes in different kinds and can carry extra information\\npub const AtomicBody = struct {\\n    kind: OperatorKindId,\\n    extra: OperatorExtraId,\\n};\\n\\n/// An identifier for operator kind\\npub const OperatorKindId = u32;\\n/// An identifier for operator extra information\\npub const OperatorExtraId = u32;\\nAn operator kind contains what is required to define a local operator at the CFT level.\\n/// Defines the various kinds of operators\\npub const OperatorKind = struct {\\n    holomorphicity: Holomorphicity,\\n    conformal_weights: ConformalWeights,\\n    quantum_numbers: tensor.QuantumNumbers,\\n    supported_correlator_tactics: []const CorrelatorTactic,\\n};\\nIn the case of interest, that means definining whether the operator is holomorphic/antiholomorphic or both,\\n/// Determines whether an operator is holomorphic/antiholomorphic or both\\npub const Holomorphicity = enum {\\n    holomorphic,\\n    antiholomorphic,\\n    both,\\n};\\nits conformal weight(s) that can either be fixed at compile time or computed at runtime\\n/// ConformalWeights stores fixed weights or a rule for computing them.\\npub const ConformalWeights = union(enum) {\\n    fixed: FixedConformalWeights,\\n    computed: WeightRuleId,\\n};\\n\\n/// FixedConformalWeights stores optional left and right conformal weights.\\npub const FixedConformalWeights = struct {\\n    h: ?f16,\\n    hbar: ?f16,\\n};\\n\\n/// WeightRuleId names a rule that computes conformal weights from labels.\\npub const WeightRuleId = u16;\\nand lastly what quantum numbers supported by Tensor-Code it carries and what tactics of correlator computation it supports.\\nCurrently, the supported correlator tactics are to either keep the correlator abstract (only return zero if selection rule is no met, otherwise keep in abstract form), evaluate it via Wick contractions of input operators or bosonize the input operators first and then evaluate via Wick contractions.\\n/// A tactic for the the evaluation of correlators\\npub const CorrelatorTactic = enum {\\n    abstract,\\n    wick,\\n    bosonize,\\n};\\nThe operator insertion point be denoted by a single point such as \\\\(z\\\\) or \\\\(\\\\bar{z}\\\\), or both \\\\(z, \\\\bar{z}\\\\), which are typed as variables from Coefficient. We bundle the derivatives of a local operator with the insertion point.\\n/// Operator insertion data are specified by a position and a number of derivatives\\npub const OperatorInsertion = union(enum) {\\n    single: struct {\\n        position: coefficient.Variable,\\n        derivatives: u8,\\n        },\\n    pair: struct {\\n        holomorphic_position: coefficient.Variable,\\n        antiholomorphic_position: coefficient.Variable,\\n        holomorphic_derivatives: u8,\\n        antiholomorphic_derivatives: u8,\\n    },\\n};\\n","n":0.048}}},{"i":9,"$":{"1":{"v":"Correlators","n":1},"2":{"v":"This module defines the notion of a correlator, which takes symbolic Expressions and computes their correlator (a Coefficient) via several possible tactics inferred from the definition of the Theory involved.\\nThe base tactics are\\nAbstract: the correlator remains unevaluated, only returns zero if selection rules as inferred through Tensor-Code from the quantum numbers of the inputs (they must intertwine to a singlet modulo possible anomalies, which can vary from one Riemann surface to another i.e. ghost number adding up to 3 on the sphere for bc system) are not met.\\nWick: the correlator is evaluated by Wick contractions until a base case is reached. Both the value of the Wick propagator and what the base case is can depend on the Riemann surface, but the underlying abstract algorithm remains the same. What we mean by base case is that the correlator dependence on zero-modes cannot quite be inferred by a Wick contraction logic (e.g. why the base case <ccc> is not zero for c-ghosts of bc system on a sphere, note that e.g. the base case <c  c c> follows). The zero mode data is specified by the user, just as the Wick contractions are surface-by-surface, yet the underlying algorithm remains fixed.\\nBosonization: the correlator is evaluated by writing out all tensor structures of Tensor-Code that can appear in the result and matching their coefficients by plugging in particular values of group-theoretic indices to both sides of the correlator, evaluating the tensor structures numerically and evaluating the correlator via bosonization rules and the Wick tactic on the bosonized form of the operators.\\n","n":0.062}}},{"i":10,"$":{"1":{"v":"Coefficient","n":1},"2":{"v":"const tensor = @import(\\"tensor-code\\");\\nA coefficient is a rational function multiplied by tensor structures from\\nTensor-Code.  The full expression store will grow\\nlater, but Wick evaluation already needs a small exact vocabulary for scalar\\nmultipliers and coordinate kernels.  These records are deliberately plain\\nvalues so primitive contractions can pass them around without allocation.\\n/// Rational stores one normalized exact rational number.\\npub const Rational = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\nfn absInt(value: i64) u64 {\\n    if (value == @import(\\"std\\").math.minInt(i64)) return @as(u64, 1) << 63;\\n    return if (value < 0) @intCast(-value) else @intCast(value);\\n}\\n\\nfn gcd(first: u64, second: u64) u64 {\\n    var a = first;\\n    var b = second;\\n    while (b != 0) {\\n        const rem = a % b;\\n        a = b;\\n        b = rem;\\n    }\\n    return if (a == 0) 1 else a;\\n}\\n\\n/// rational constructs a normalized exact rational number.\\npub fn rational(numerator: i64, denominator: i64) ?Rational {\\n    if (denominator == 0) return null;\\n    var num = numerator;\\n    var den = denominator;\\n    if (den < 0) {\\n        num = -num;\\n        den = -den;\\n    }\\n    const factor: i64 = @intCast(gcd(absInt(num), absInt(den)));\\n    return .{\\n        .numerator = @divExact(num, factor),\\n        .denominator = @divExact(den, factor),\\n    };\\n}\\n\\n/// integer constructs an exact rational integer.\\npub fn integer(value: i64) Rational {\\n    return .{ .numerator = value, .denominator = 1 };\\n}\\nCoordinate kernels are the coordinate-dependent pieces produced by Wick\\ncontractions.  They are not a simplifier; they are the compact output of a\\nsingle primitive contraction after insertion derivatives have acted where the\\nWick rule requested them.\\n/// CoordinateDifference stores one ordered coordinate difference.\\npub const CoordinateDifference = struct {\\n    left: Variable,\\n    right: Variable,\\n};\\n\\n/// CoordinateKernel stores one coordinate-dependent kernel factor.\\npub const CoordinateKernel = union(enum) {\\n    difference_power: struct { coordinate: CoordinateDifference, exponent: i16 },\\n    logarithm: CoordinateDifference,\\n    green_kernel: struct { name: []const u8, coordinate: CoordinateDifference, left_derivatives: u8 = 0, right_derivatives: u8 = 0 },\\n    green_exponential: CoordinateDifference,\\n};\\n\\n/// CoordinateFactor stores a coordinate kernel with its rational derivative multiplier.\\npub const CoordinateFactor = struct {\\n    scalar: Rational = .{ .numerator = 1, .denominator = 1 },\\n    kernel: CoordinateKernel,\\n};\\nThe older coefficient shell is kept because higher layers already refer to a\\ncoefficient as a sum of tensor-weighted rational functions.  Its internals\\nremain private until the real simplifier exists.\\n/// A coefficient is a sum of rational functions multiplied by a tensor structure\\npub const Coeff = struct {\\n    summands: []const CoeffSummand,\\n};\\n\\nconst CoeffSummand = struct {\\n    rational_function: RationalFunction,\\n    tensor_structure: tensor.TensorStructure,\\n};\\n\\nA rational function can be written as a product of polynomial factors raised to a power.\\nconst RationalFunction = struct {\\n    factors: []const Factor,\\n};\\n\\nconst Factor = struct {\\n    base: Polynomial,\\n    exponent: i16,\\n};\\nA polynomial is a sum of monomial terms.\\nconst Polynomial = struct {\\n    terms: []const PolynomialTerm,\\n};\\n\\nconst PolynomialTerm = struct {\\n    monomial_coefficient: Scalar,\\n    monomial: Monomial,\\n};\\n\\nconst Scalar = f32;\\n\\nconst Monomial = struct {\\n    terms: []const MonomialTerm,\\n};\\n\\nconst MonomialTerm = struct {\\n   variable: Variable,\\n   power: u16,\\n};\\nVariables are internally assigned a numeric identifier.\\n/// Variables are internally assigned numeric identifiers\\npub const Variable = u32;\\n","n":0.047}}},{"i":11,"$":{"1":{"v":"Tensor-Code","n":1},"2":{"v":"This module handles finite-dimensional representation data for the symmetry\\nalgebras used by the other modules.  For a Lie algebra \\\\(\\\\mathfrak g\\\\), the\\ncentral problem is to construct bases of\\n\\\\[\\n  \\\\operatorname{Hom}_{\\\\mathfrak g}\\n  \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_k\\\\right),\\n\\\\]\\nusually with \\\\(T=\\\\mathbf 1\\\\).  These bases are Clebsch-Gordan data and\\ninvariant tensors.  Concrete index notation, such as gamma matrices, Young\\nsymmetrizers, metrics, epsilons, or structure constants, is a rendering layer\\nover this representation-theoretic basis.\\nThe public module exposes named declarations directly.  Source files such as\\nSymmetry and Tensor Kernel\\norganize implementation and prose, but clients should import this node and use\\nthe individual public declarations below.  The caller-facing object is Context:\\nit registers algebras, irreps, and realizations, then returns compact handles\\nfor invariant bases and rendered/evaluated output streams.\\nconst context = @import(\\"context.zig\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst kernel = @import(\\"kernel.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\n/// SimpleLieAlgebra stores the simple family and rank.\\npub const SimpleLieAlgebra = symmetry.SimpleLieAlgebra;\\n/// LieFamily names the supported simple Lie families.\\npub const LieFamily = symmetry.LieFamily;\\n/// QuantumNumbers is the tensor-product list of representation ids.\\npub const QuantumNumbers = symmetry.QuantumNumbers;\\n/// RepresentationId names a representation in a representation store.\\npub const RepresentationId = symmetry.RepresentationId;\\n/// TensorStructure stores simple built-in tensor structures.\\npub const TensorStructure = symmetry.TensorStructure;\\n\\n/// AlgebraHandle names a registered Lie algebra in a tensor context.\\npub const AlgebraHandle = store.AlgebraHandle;\\n/// IrrepHandle names an abstract irreducible representation.\\npub const IrrepHandle = store.IrrepHandle;\\n/// RealizationHandle names a concrete index realization of an irrep.\\npub const RealizationHandle = realization.RealizationHandle;\\n/// RealizationChannel selects one irrep copy inside a realization product.\\npub const RealizationChannel = realization.RealizationChannel;\\n/// BasisHandle names a generated invariant basis.\\npub const BasisHandle = coupling.BasisHandle;\\n/// InvariantHandle names one invariant inside a basis.\\npub const InvariantHandle = coupling.InvariantHandle;\\n/// Context is an opaque handle to tensor-code stores and caches.\\npub const Context = context.Context;\\n/// ContextOptions configures algebra and rendering defaults.\\npub const ContextOptions = context.ContextOptions;\\n/// AlgebraConventions selects configurable exact-algebra normalizations.\\npub const AlgebraConventions = root_data.AlgebraConventions;\\n/// AlgebraSpec describes a requested algebra before it is interned.\\npub const AlgebraSpec = store.AlgebraSpec;\\n/// IrrepSpec describes a requested representation before it is interned.\\npub const IrrepSpec = store.IrrepSpec;\\n/// ExternalLeg describes a public invariant leg with named free indices.\\npub const ExternalLeg = realization.ExternalLeg;\\n/// RealizationSpec describes a primitive or projected external index model.\\npub const RealizationSpec = realization.RealizationSpec;\\n/// InvariantBasisRequest is the CFT-facing request for invariant tensors.\\npub const InvariantBasisRequest = coupling.InvariantBasisRequest;\\n/// TensorExpansionTerm stores a CFT coefficient attached to one invariant id.\\npub const TensorExpansionTerm = coupling.TensorExpansionTerm;\\n/// RenderOptions controls streamed invariant rendering.\\npub const RenderOptions = rendering.RenderOptions;\\n/// EvalOptions controls component evaluation of one invariant.\\npub const EvalOptions = rendering.EvalOptions;\\n/// SymbolicTerm stores one streamed coefficient times symbolic atoms.\\npub const SymbolicTerm = rendering.SymbolicTerm;\\n/// SymbolicAtom stores one symbolic invariant contraction atom.\\npub const SymbolicAtom = rendering.SymbolicAtom;\\n\\n/// TensorExprId names a tensor expression stored by tensor-code.\\npub const TensorExprId = kernel.TensorExprId;\\nThis root block is the public tensor-code API for now.  The compatibility\\nexports QuantumNumbers, RepresentationId, and TensorStructure come from\\nSymmetry, while TensorExprId comes from\\nTensor Kernel.  They exist because CFT data\\nstructures currently store compact tensor and representation ids.\\nThe central workflow is:\\nregister an algebra with Context.registerAlgebra;\\nregister abstract irreps by Dynkin labels with Context.registerIrrep;\\noptionally register concrete index models with Context.registerRealization;\\nrequest a basis with Context.invariantBasis;\\nstream a concrete convention with Context.renderInvariant or\\n   Context.evaluateInvariant.\\nThe constructors are deliberately on the public data records, so callers do not\\nneed to import implementation helpers:\\nvar ctx = try tensor.Context.init(allocator);\\ndefer ctx.deinit();\\nconst algebra = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\nconst spinor = try ctx.registerIrrep(algebra, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\nconst leg = tensor.ExternalLeg.primitive(spinor, &.{\\n    .init(.spinor, \\"a\\"),\\n});\\nconst basis = try ctx.invariantBasis(.{\\n    .algebra = algebra,\\n    .external_legs = &.{leg},\\n});\\nConventions are configurable at context construction:\\nvar ctx = try tensor.Context.initWithOptions(allocator, .{\\n    .algebra_conventions = .{\\n        .quadratic_casimir_scale = .{ .numerator = 1, .denominator = 1 },\\n    },\\n    .default_signature = .complex,\\n});\\ndefer ctx.deinit();\\nHelper enums inside requests and render options are intentionally not root\\nexports; callers can use inferred literals such as .singlet, .auto,\\n.vector, and .complex.  Cache handles, decomposition handles, channel\\nhandles, stream sources, schema records, and backend capability tables stay\\nbehind this boundary until a caller needs them.\\nPublic API GlossaryLie algebra \\\\(\\\\mathfrak g\\\\): the symmetry algebra, such as \\\\(A_n\\\\), \\\\(D_n\\\\),\\n  or \\\\(E_8\\\\).  Tensor-code uses \\\\(\\\\mathfrak g\\\\) to decide which tensor\\n  structures are invariant.\\nRoot datum: the Cartan matrix, simple roots, coroots, and weight lattice\\n  conventions used internally for \\\\(\\\\mathfrak g\\\\).  CFT code should not handle\\n  this directly.\\nIrrep: an irreducible representation of \\\\(\\\\mathfrak g\\\\).  For simple Lie\\n  algebras this is identified by Dynkin labels, not by dimension.\\nDynkin label: the integer highest-weight label of an irrep.  It is the\\n  stable representation id used by the mathematics layer.\\nTensor product decomposition: the expansion\\n  \\\\[\\n    R\\\\otimes S = \\\\bigoplus_T m_T T\\n  \\\\]\\n  into irreps \\\\(T\\\\) with multiplicities \\\\(m_T\\\\).  Tensor-code caches this\\n  compactly; it does not expand component tensors during decomposition.\\nMultiplicity: the number of independent copies of the same irrep inside a\\n  product.  A dimension is never enough to distinguish these copies.\\nChannel: one copy of an irrep inside a tensor product.  If\\n  \\\\(R\\\\otimes S\\\\) contains two copies of \\\\(T\\\\), those two copies have the same\\n  irrep but different channel copy labels.\\nRealization: a concrete index model for an irrep.  A realization may be\\n  primitive, such as one spinor index, or constrained, such as a traceless or\\n  projected tensor product.\\nBackend: the internal family-specific capability table described in\\n  Tensor Backend.  It supplies decomposition,\\n  local projector, primitive invariant, and rendering routines for a Lie\\n  family without changing the public request flow.\\nPrimitive realization: a representation whose public indices are already\\n  the natural storage indices, such as a vector index, spinor index,\\n  fundamental index, or adjoint index.\\nConstrained realization: a representation embedded in a larger ambient\\n  tensor product, such as the \\\\(144\\\\subset 10\\\\otimes 16\\\\) gamma-traceless\\n  channel.  Tensor-code records the boundary projector once and then treats the\\n  leg as the irrep, not as the full ambient product at every internal step.\\nIndex: a caller-named free slot in a realization.  An index name is not a\\n  representation by itself.\\nExternal leg: one field/operator tensor slot supplied by a caller.  It has\\n  an irrep, a realization, and named free indices.\\nIntertwiner: a \\\\(\\\\mathfrak g\\\\)-equivariant map between representations.\\n  Clebsch-Gordan coefficients are component formulas for such maps.\\nInvariant: a basis vector in\\n  \\\\[\\n    \\\\operatorname{Hom}_{\\\\mathfrak g}\\n    \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_k\\\\right).\\n  \\\\]\\n  Usually \\\\(T=\\\\mathbf 1\\\\), so the invariant is a singlet tensor.\\nCoupling path: the compact internal description of one invariant as a tree\\n  of local Clebsch-Gordan maps.\\nProjector: an idempotent intertwiner selecting one channel or constrained\\n  realization from an ambient product.  Projectors may be rendered as named\\n  operators, block formulas, or fully expanded symbolic terms.\\nRenderer: the procedure that turns an invariant id into concrete notation,\\n  such as \\\\(\\\\delta\\\\)\'s, \\\\(\\\\epsilon\\\\)\'s, Young symmetrizers, gamma matrices, or\\n  structure constants.\\nSignature convention: the metric and Clifford-algebra convention used when\\n  rendering or evaluating components.  Complex, Euclidean, and Lorentzian\\n  signatures are conventions on the same abstract representation data.\\nTensor expansion: the object CFT code carries: rational-function ids times\\n  tensor invariant ids.  It does not require expanded tensor formulas.\\n","n":0.03}}},{"i":12,"$":{"1":{"v":"Expressions","n":1},"2":{"v":"const coefficient = @import(\\"coefficient.zig\\");\\nconst operators = @import(\\"operators.zig\\");\\nThe symbolic expressions we work with are simply Coefficients multiplied by local Operators.\\n/// A symbolic expression encountered in worldsheet calculations\\npub const Expr = struct {\\n    summands: []const ExprSummand\\n};\\n/// Each summand is a local operator with a coefficient\\npub const ExprSummand = struct {\\n    coeff: coefficient.Coeff,\\n    operators: operators.Operator\\n};\\n","n":0.139}}},{"i":13,"$":{"1":{"v":"Theory","n":1},"2":{"v":"A theory declaration says what it means to specify a CFT or BCFT before any\\nruntime correlator is evaluated.  This module is not the executor: it is the\\ncompact vocabulary of ids, sectors, operator families, conformal structures,\\npresentations, Wick and zero-mode rule names, boundary data, and local\\ncorrelator configs used by Correlators and OPE.  CFT Kernel\\nconsumes this data as comptime preset data.\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nIdentifier NamespaceMost theory objects are referenced by compact integer ids.  Keeping them in\\none namespace avoids a flat module full of unrelated public aliases while\\nstill making the table layout explicit.\\n/// Id groups compact handles used by CFT and BCFT presets.\\npub const Id = struct {\\n    const Sector = u16;\\n    const Strategy = u16;\\n    const StatisticsRule = u16;\\n    const SectorPresentation = u16;\\n    const ConformalStructure = u16;\\n    const QuantumRule = u16;\\n    const StrategySupportRule = u16;\\n    const WickRuleSet = u16;\\n    const Pairing = u16;\\n    const InversePairing = u16;\\n    const PairingData = u16;\\n    const ZeroModeRuleSet = u16;\\n    const CorrelatorConfig = u16;\\n    const LocalOpeConfig = u16;\\n    const CocycleTable = u16;\\n    /// FiniteProjection names finite projections used by public basis queries.\\n    pub const FiniteProjection = u16;\\n    const Lattice = u16;\\n    const LatticeVector = u32;\\n    /// ModeAlgebra names a mode algebra used by public descendant filters.\\n    pub const ModeAlgebra = u16;\\n    const ModeActionRule = u16;\\n    const LabelSchema = u16;\\n    /// LabelSpan names a compact slice of runtime body labels accepted by body builders.\\n    pub const LabelSpan = u32;\\n    const Extra = u32;\\n    const ConventionSet = u16;\\n    const WeightRule = u16;\\n    const CFT = u16;\\n    const LabelMap = u16;\\n    const BoundaryCondition = u16;\\n    const BoundaryStack = u16;\\n    const BoundarySpinData = u16;\\n    const ChanPatonMatrixUnit = u32;\\n    const Grading = u16;\\n};\\nBulk And Boundary SpecificationsThe bulk CFT spec owns only bulk data: Operators, sectors, quantum labels,\\npresentations, and rule tables.  A BCFT is a separate extension built from\\nbulk data plus boundary conditions, boundary stacks, boundary operator\\nfamilies, and Wick/zero-mode configs compiled for the boundary problem.\\n/// Spec groups bulk CFT and BCFT preset declarations.\\npub const Spec = struct {\\n    const Sector = struct {\\n        name: []const u8,\\n        tactic_preference: []const Id.Strategy,\\n        statistics_rule: Id.StatisticsRule,\\n        presentations: []const Id.SectorPresentation,\\n    };\\n\\n    /// CFT is the compile-time declaration of a bulk CFT preset.\\n    pub const CFT = struct {\\n        sectors: []const Sector,\\n        bulk_operator_kinds: []const operators.OperatorKind,\\n        conformal_structure: Id.ConformalStructure,\\n        conformal_structures: []const ConformalStructure,\\n        quantum_names: []const Label.QuantumName,\\n        label_schemas: []const Label.Schema,\\n        conventions: []const Rule.ConventionSet,\\n        weight_rules: []const Rule.Weight,\\n        quantum_rules: []const Rule.Quantum,\\n        statistics_rules: []const Rule.Statistics,\\n        strategy_support_rules: []const Rule.StrategySupport,\\n        wick_rule_sets: []const Rule.WickSet,\\n        pairing_data: []const Rule.PairingData,\\n        zero_mode_rule_sets: []const Rule.ZeroModeSet,\\n        correlator_configs: []const Runtime.CorrelatorConfig,\\n        local_ope_configs: []const Runtime.LocalOpeConfig,\\n        cocycle_tables: []const Rule.CocycleTable,\\n        presentations: []const Presentation.Sector,\\n        finite_projections: []const Rule.FiniteProjection,\\n        lattices: []const Presentation.Lattice,\\n        mode_algebras: []const Mode.Algebra,\\n        mode_action_rules: []const Mode.ActionRule,\\n    };\\n\\n    const ConformalStructure = struct {\\n        stress_tensor: Runtime.OperatorRef,\\n        susy: WorldsheetSusy,\\n        supercurrents: []const Runtime.OperatorRef,\\n        r_currents: []const Runtime.OperatorRef,\\n    };\\n\\n    const WorldsheetSusy = enum {\\n        none,\\n        n1_1,\\n        n2_2,\\n        n1_chiral,\\n        n2_chiral,\\n        custom,\\n    };\\n\\n    /// BCFT extends a bulk preset with boundary operator and Wick data.\\n    pub const BCFT = struct {\\n        name: []const u8,\\n        bulk: Id.CFT,\\n        boundary_conditions: []const Boundary.Condition,\\n        boundary_stacks: []const Boundary.Stack,\\n        boundary_stress_tensor: Runtime.OperatorRef,\\n        boundary_spin: []const Boundary.SpinData,\\n        boundary_operator_kinds: []const operators.OperatorKindId,\\n        boundary_changing_kinds: []const Boundary.ChangingKind,\\n        wick_rule_sets: []const Rule.WickSet,\\n        pairing_data: []const Rule.PairingData,\\n        zero_mode_rule_sets: []const Rule.ZeroModeSet,\\n        correlator_configs: []const Runtime.CorrelatorConfig,\\n        local_ope_configs: []const Runtime.LocalOpeConfig,\\n    };\\n};\\nBoundary NamespaceBoundary conditions are not part of the bulk CFT.  They enter only when a\\nBCFT extension is formed.  Stacks encode direct sums of boundary conditions;\\nChan-Paton multiplicity is label data on boundary bodies.\\n/// Boundary groups boundary-condition, stack, and Chan-Paton data.\\npub const Boundary = struct {\\n    const Condition = struct {\\n        name: []const u8,\\n        simple_data: Id.Extra,\\n    };\\n\\n    const Stack = struct {\\n        name: []const u8,\\n        entries: []const StackEntry,\\n    };\\n\\n    const StackEntry = struct {\\n        boundary: Id.BoundaryCondition,\\n        multiplicity: u32,\\n    };\\n\\n    /// ChangingKind identifies a boundary-local kind from one condition to another.\\n    pub const ChangingKind = struct {\\n        from: Id.BoundaryCondition,\\n        to: Id.BoundaryCondition,\\n        kind: operators.OperatorKindId,\\n    };\\n\\n    const SpinData = struct {\\n        id: Id.BoundarySpinData,\\n    };\\n\\n    const ChanPatonLabel = struct {\\n        endpoints: ChanPatonEndpoints,\\n        factor: ChanPatonFactor,\\n    };\\n\\n    const ChanPatonEndpoints = struct {\\n        from_stack: Id.BoundaryStack,\\n        to_stack: Id.BoundaryStack,\\n        from_entry: u32,\\n        to_entry: u32,\\n    };\\n\\n    const ChanPatonFactor = union(enum) {\\n        matrix_unit: ChanPatonMatrixUnit,\\n        tensor_factor: tensor.TensorExprId,\\n    };\\n\\n    const ChanPatonMatrixUnit = struct {\\n        row: u32,\\n        col: u32,\\n    };\\n};\\nLabels And RulesLabels carry runtime operator body data such as momenta, picture charge,\\nlattice vectors, derivative orders, and Chan-Paton endpoints.  Rule\\ndeclarations name client or preset procedures without storing their large\\npayloads here.\\n/// Label groups quantum-number and operator-label schema declarations.\\npub const Label = struct {\\n    const QuantumName = struct {\\n        name: []const u8,\\n    };\\n\\n    /// QuantumFilter constrains one named quantum-number value.\\n    pub const QuantumFilter = struct {\\n        name: u16,\\n        value: i64,\\n    };\\n\\n    const Schema = struct {\\n        name: []const u8,\\n        fields: []const Field,\\n    };\\n\\n    const Field = struct {\\n        name: []const u8,\\n        kind: FieldKind,\\n    };\\n\\n    const FieldKind = enum {\\n        integer,\\n        rational,\\n        lattice_vector,\\n        tensor_expr,\\n        opaque_data,\\n    };\\n};\\nconst Rule = struct {\\n    const ConventionSet = struct {\\n        name: []const u8,\\n    };\\n\\n    const Weight = struct {\\n        name: []const u8,\\n    };\\n\\n    const Quantum = struct {\\n        name: []const u8,\\n    };\\n\\n    const Statistics = struct {\\n        name: []const u8,\\n    };\\n\\n    const StrategySupport = struct {\\n        name: []const u8,\\n    };\\n\\n    const WickSet = struct {\\n        name: []const u8,\\n        conventions: Id.ConventionSet,\\n    };\\n\\n    const ZeroModeSet = struct {\\n        name: []const u8,\\n    };\\n\\n    const CocycleTable = struct {\\n        name: []const u8,\\n    };\\n\\n    const FiniteProjection = struct {\\n        name: []const u8,\\n    };\\n\\n    const PairingData = struct {\\n        sector: Id.Sector,\\n        pairing: Id.Pairing,\\n        inverse_pairing: Id.InversePairing,\\n    };\\n};\\nPresentations, Lattices, And ModesPresentations say how sectors are computed: direct fields, first-order\\nsystems, bosonized lattices, current algebras, or abstract conformal data.\\nLattices cover free bosons, RNS bosonized charges, SO(32) bosonization, and\\nE8xE8/Narain current-lattice presentations.\\nconst Presentation = struct {\\n    const Sector = struct {\\n        sectors: []const Id.Sector,\\n        name: []const u8,\\n        operator_map: []const Map,\\n        lattice: ?Id.Lattice,\\n        weight_rule: ?Id.WeightRule,\\n        wick_rules: ?Id.WickRuleSet,\\n    };\\n\\n    const Map = struct {\\n        from: operators.OperatorKindId,\\n        to: operators.OperatorKindId,\\n        label_map: Id.LabelMap,\\n    };\\n\\n    const Lattice = struct {\\n        name: []const u8,\\n        rank: u16,\\n        bilinear_form: Id.Extra,\\n        cocycles: ?Id.CocycleTable,\\n    };\\n};\\nModes are stored as operator-body data before evaluation.  This covers\\nfree-field modes, Virasoro descendants, beta-gamma modes, and Kac-Moody\\ncurrent modes for affine-module spectra generated by Basis-Generation.\\nconst Mode = struct {\\n    const Algebra = struct {\\n        name: []const u8,\\n        generators: []const Generator,\\n        action_rule: Id.ModeActionRule,\\n    };\\n\\n    const Generator = struct {\\n        name: []const u8,\\n        indices: []const IndexSlot,\\n        statistics: StatisticsData,\\n    };\\n\\n    const ActionRule = struct {\\n        name: []const u8,\\n    };\\n\\n    const IndexSlot = struct {\\n        representation: tensor.RepresentationId,\\n    };\\n\\n    const StatisticsData = struct {\\n        parity: u1,\\n    };\\n\\n    const Descendant = struct {\\n        algebra: Id.ModeAlgebra,\\n        generator: u16,\\n        mode_number: Runtime.Rational,\\n        target: operators.OperatorBodyId,\\n        labels: Id.LabelSpan,\\n    };\\n};\\nRuntime Config DataSurface and spin-structure declarations compile to small correlator configs.\\nThe runtime correlator receives only a pointer to such a config; OPE selects\\none of these local configs for sphere or disk computations.\\n/// Runtime groups small data passed to generic runtime procedures.\\npub const Runtime = struct {\\n    /// CorrelatorConfig selects the Wick and zero-mode rules for a correlator call.\\n    pub const CorrelatorConfig = struct {\\n        wick_rules: Id.WickRuleSet,\\n        zero_modes: Id.ZeroModeRuleSet,\\n    };\\n\\n    /// LocalOpeConfig selects the local correlator model used internally by OPE.\\n    pub const LocalOpeConfig = struct {\\n        domain: OperatorDomain,\\n        support: ChiralSupport,\\n        correlator_config: Id.CorrelatorConfig,\\n    };\\n\\n    /// OperatorDomain identifies whether an operator body is bulk or boundary data.\\n    pub const OperatorDomain = enum {\\n        bulk,\\n        boundary,\\n        boundary_changing,\\n    };\\n\\n    /// ChiralSupport records the chiral support of candidates and OPE targets.\\n    pub const ChiralSupport = enum {\\n        holomorphic,\\n        antiholomorphic,\\n        mixed,\\n        boundary,\\n    };\\n\\n    const OperatorRef = struct {\\n        kind: operators.OperatorKindId,\\n        labels: Id.LabelSpan,\\n    };\\n\\n    /// ConformalWeights stores holomorphic and antiholomorphic weights.\\n    pub const ConformalWeights = struct {\\n        h: ?Rational,\\n        hbar: ?Rational,\\n    };\\n\\n    /// LevelBound limits descendant level in basis or OPE generation.\\n    pub const LevelBound = struct {\\n        max: Rational,\\n    };\\n\\n    const Rational = struct {\\n        numerator: i64,\\n        denominator: i64,\\n    };\\n};\\n","n":0.028}}},{"i":14,"$":{"1":{"v":"Basis-Generation","n":1}}},{"i":15,"$":{"1":{"v":"Abstract","n":1}}},{"i":16,"$":{"1":{"v":"Wick","n":1},"2":{"v":"The Wick correlator is a generic driver.  It chooses primitive pair\\noperations and manages stack-bounded branch state; the tactic owns all\\nphysics.  The first implementation works on original input occurrences.  It\\ndoes not allocate an expression tree: a valid full branch is replayed directly\\nto the sink as primitive Wick terms followed by the zero-mode base case.\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\n\\nconst PairChoice = struct {\\n    left: u8,\\n    right: u8,\\n    term_index: u16,\\n    payload: u64 = 0,\\n};\\n\\nconst default_operator_count = 128;\\nconst no_candidate_index: u16 = 0xffff;\\n\\nconst PairCandidate = struct {\\n    left: u8,\\n    right: u8,\\n    term_index: u16,\\n    payload: u64,\\n    residuals: [2]u8,\\n    residual_count: u8,\\n    mandatory_before_base: bool,\\n    next_for_left: u16 = no_candidate_index,\\n    next_for_right: u16 = no_candidate_index,\\n};\\n\\nfn pairDepth(comptime operator_count: usize) usize {\\n    return operator_count / 2;\\n}\\n\\nfn pairCandidateCount(comptime operator_count: usize) usize {\\n    return (operator_count * (operator_count - 1)) / 2;\\n}\\n\\nfn tacticOperatorCount(comptime Tactic: type) comptime_int {\\n    return if (@hasDecl(Tactic, \\"traversal_operator_count\\")) Tactic.traversal_operator_count else default_operator_count;\\n}\\n\\nfn Scratch(comptime operator_count: usize) type {\\n    return struct {\\n        residual_indices: [operator_count]usize = undefined,\\n        pair_stack: [pairDepth(operator_count)]PairChoice = undefined,\\n        candidates: [pairCandidateCount(operator_count)]PairCandidate = undefined,\\n        first_candidate_by_operator: [operator_count]u16 = undefined,\\n        last_candidate_by_operator: [operator_count]u16 = undefined,\\n        first_candidate_by_left: [operator_count]u16 = undefined,\\n        end_candidate_by_left: [operator_count]u16 = undefined,\\n        zero_mode_survivable: [operator_count]bool = undefined,\\n        zero_mode_forced_mask: u128 = undefined,\\n        parities: [operator_count]bool = undefined,\\n        parity_mask: u128 = undefined,\\n\\n        pub fn init() @This() {\\n            return .{};\\n        }\\n    };\\n}\\nThe driver first builds a compact pair-candidate list.  Tactics may provide a\\ncheap count hook; otherwise a dry sink falls back to probing the pair emitter.\\nZero-mode viability follows the same pattern.  Valid branches are replayed\\nonce to the real sink, so invalid branches never leak prefix factors into the\\nstream.\\nconst NullSink = struct {\\n    /// emitWickTermStart discards a dry-run pair term boundary.\\n    pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n    /// emitWickScalar discards a dry-run scalar factor.\\n    pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n    /// emitWickCoordinate discards a dry-run coordinate factor.\\n    pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n    /// emitWickTensor discards a dry-run tensor factor.\\n    pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n    /// emitWickAction discards a dry-run action factor.\\n    pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n    /// emitWickTermEnd discards a dry-run pair term end.\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n    /// emitZeroModeFactor discards a dry-run zero-mode factor.\\n    pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n    /// emitZeroModeBaseEnd discards a dry-run zero-mode success marker.\\n    pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n};\\n\\nfn bit(index: usize) u128 {\\n    return @as(u128, 1) << @intCast(index);\\n}\\n\\nfn initialLiveMask(comptime operator_count: usize, len: usize) u128 {\\n    if (len == operator_count) return ~@as(u128, 0);\\n    return bit(len) - 1;\\n}\\n\\nfn live(mask: u128, index: usize) bool {\\n    return (mask & bit(index)) != 0;\\n}\\n\\nfn firstLiveIndex(mask: u128) usize {\\n    const low: u64 = @truncate(mask);\\n    if (low != 0) return @ctz(low);\\n    const high: u64 = @truncate(mask >> 64);\\n    return 64 + @ctz(high);\\n}\\n\\nfn residualSlice(scratch: anytype, len: usize, live_mask: u128) []const usize {\\n    var count: usize = 0;\\n    var index: usize = 0;\\n    while (index < len) : (index += 1) {\\n        if (live(live_mask, index)) {\\n            scratch.residual_indices[count] = index;\\n            count += 1;\\n        }\\n    }\\n    return scratch.residual_indices[0..count];\\n}\\n\\nfn pairTermCount(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize) !usize {\\n    if (@hasDecl(Tactic, \\"pairTermCount\\")) {\\n        return Tactic.pairTermCount(config_ptr, ops, left, right);\\n    }\\n    var null_sink = NullSink{};\\n    return Tactic.emitPairTerms(config_ptr, ops, left, right, &null_sink);\\n}\\n\\nfn pairTermResiduals(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize, residuals: *[2]usize) !usize {\\n    if (@hasDecl(Tactic, \\"pairTermResiduals\\")) {\\n        return Tactic.pairTermResiduals(config_ptr, ops, left, right, term_index, residuals);\\n    } else {\\n        return 0;\\n    }\\n}\\n\\nfn pairTermPayload(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize) !u64 {\\n    if (@hasDecl(Tactic, \\"pairTermPayload\\")) {\\n        return Tactic.pairTermPayload(config_ptr, ops, left, right, term_index);\\n    }\\n    return @intCast(term_index);\\n}\\n\\nfn pairTermInfo(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, left: usize, right: usize, term_index: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n    if (@hasDecl(Tactic, \\"pairTermInfo\\")) {\\n        return Tactic.pairTermInfo(config_ptr, ops, left, right, term_index, residuals, payload, residual_count);\\n    }\\n    payload.* = try pairTermPayload(Tactic, config_ptr, ops, left, right, term_index);\\n    residual_count.* = try pairTermResiduals(Tactic, config_ptr, ops, left, right, term_index, residuals);\\n}\\n\\nfn residualsKeepPair(left: usize, right: usize, residuals: []const usize) bool {\\n    var keeps_left = false;\\n    var keeps_right = false;\\n    for (residuals) |residual| {\\n        keeps_left = keeps_left or residual == left;\\n        keeps_right = keeps_right or residual == right;\\n    }\\n    return keeps_left and keeps_right;\\n}\\n\\nfn appendCandidateForOperator(scratch: anytype, operator_index: usize, candidate_index: u16) void {\\n    const last = scratch.last_candidate_by_operator[operator_index];\\n    if (last == no_candidate_index) {\\n        scratch.first_candidate_by_operator[operator_index] = candidate_index;\\n    } else if (scratch.candidates[last].left == operator_index) {\\n        scratch.candidates[last].next_for_left = candidate_index;\\n    } else {\\n        scratch.candidates[last].next_for_right = candidate_index;\\n    }\\n    scratch.last_candidate_by_operator[operator_index] = candidate_index;\\n}\\n\\nfn nextCandidateForOperator(candidate: PairCandidate, operator_index: usize) u16 {\\n    return if (candidate.left == operator_index) candidate.next_for_left else candidate.next_for_right;\\n}\\n\\nfn buildCandidates(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) !usize {\\n    var operator_index: usize = 0;\\n    while (operator_index < ops.operators.len) : (operator_index += 1) {\\n        scratch.first_candidate_by_operator[operator_index] = no_candidate_index;\\n        scratch.last_candidate_by_operator[operator_index] = no_candidate_index;\\n        scratch.first_candidate_by_left[operator_index] = no_candidate_index;\\n        scratch.end_candidate_by_left[operator_index] = no_candidate_index;\\n    }\\n\\n    var candidate_count: usize = 0;\\n    var left: usize = 0;\\n    while (left < ops.operators.len) : (left += 1) {\\n        const left_start = candidate_count;\\n        var right = left + 1;\\n        while (right < ops.operators.len) : (right += 1) {\\n            if (normal_ordering.same(ops.operators[left], ops.operators[right])) continue;\\n            const term_count = try pairTermCount(Tactic, config_ptr, ops, left, right);\\n            var term_index: usize = 0;\\n            while (term_index < term_count) : (term_index += 1) {\\n                if (candidate_count == scratch.candidates.len) return error.TooManyWickCandidates;\\n                var residuals: [2]usize = undefined;\\n                var payload: u64 = undefined;\\n                var residual_count: usize = undefined;\\n                try pairTermInfo(Tactic, config_ptr, ops, left, right, term_index, &residuals, &payload, &residual_count);\\n                if (residual_count > residuals.len) return error.TooManyWickTermResiduals;\\n                var compact_residuals: [2]u8 = undefined;\\n                var residual_index: usize = 0;\\n                while (residual_index < residual_count) : (residual_index += 1) {\\n                    compact_residuals[residual_index] = @intCast(residuals[residual_index]);\\n                }\\n                const compact_candidate_index: u16 = @intCast(candidate_count);\\n                scratch.candidates[candidate_count] = .{\\n                    .left = @intCast(left),\\n                    .right = @intCast(right),\\n                    .term_index = @intCast(term_index),\\n                    .payload = payload,\\n                    .residuals = compact_residuals,\\n                    .residual_count = @intCast(residual_count),\\n                    .mandatory_before_base = residualsKeepPair(left, right, residuals[0..residual_count]),\\n                };\\n                appendCandidateForOperator(scratch, left, compact_candidate_index);\\n                appendCandidateForOperator(scratch, right, compact_candidate_index);\\n                candidate_count += 1;\\n            }\\n        }\\n        if (candidate_count != left_start) {\\n            scratch.first_candidate_by_left[left] = @intCast(left_start);\\n            scratch.end_candidate_by_left[left] = @intCast(candidate_count);\\n        }\\n    }\\n    return candidate_count;\\n}\\n\\nfn buildParities(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) void {\\n    scratch.parity_mask = 0;\\n    var index: usize = 0;\\n    while (index < ops.operators.len) : (index += 1) {\\n        const parity = if (@hasDecl(Tactic, \\"operatorParity\\")) Tactic.operatorParity(config_ptr, ops, index) else false;\\n        scratch.parities[index] = parity;\\n        if (parity) scratch.parity_mask |= bit(index);\\n    }\\n}\\n\\nfn buildZeroModeSurvivability(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype) void {\\n    scratch.zero_mode_forced_mask = 0;\\n    var index: usize = 0;\\n    while (index < ops.operators.len) : (index += 1) {\\n        const survivable = operatorCanRemainInZeroMode(Tactic, config_ptr, ops, index);\\n        scratch.zero_mode_survivable[index] = survivable;\\n        if (!survivable) scratch.zero_mode_forced_mask |= bit(index);\\n    }\\n}\\n\\nfn pairSign(scratch: anytype, live_mask: u128, left: usize, right: usize) i8 {\\n    const endpoints = bit(left) | bit(right);\\n    if ((scratch.parity_mask & endpoints) != endpoints) return 1;\\n    const below_right = bit(right) - 1;\\n    const below_or_at_left = bit(left + 1) - 1;\\n    const between = below_right & ~below_or_at_left;\\n    const crossings = @popCount(live_mask & scratch.parity_mask & between);\\n    return if ((crossings & 1) == 0) 1 else -1;\\n}\\n\\nfn operatorCanRemainInZeroMode(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n    if (@hasDecl(Tactic, \\"operatorCanRemainInZeroMode\\")) {\\n        return Tactic.operatorCanRemainInZeroMode(config_ptr, ops, index);\\n    }\\n    return true;\\n}\\n\\nfn firstForcedResidual(scratch: anytype, live_mask: u128) ?usize {\\n    const forced = live_mask & scratch.zero_mode_forced_mask;\\n    return if (forced == 0) null else firstLiveIndex(forced);\\n}\\n\\nfn zeroModeSucceeds(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, residual: []const usize) !bool {\\n    if (@hasDecl(Tactic, \\"zeroModeBaseCaseSucceeds\\")) {\\n        return Tactic.zeroModeBaseCaseSucceeds(config_ptr, ops, residual);\\n    }\\n    var null_sink = NullSink{};\\n    return Tactic.emitZeroModeBaseCase(config_ptr, ops, residual, &null_sink);\\n}\\nAt a zero-mode base case, the branch stack is replayed before the base case so\\nthe sink receives one fully expanded streamed term.  Pair factors are not kept\\nin memory.\\nfn emitPairTerm(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, choice: PairChoice, sink: anytype) !void {\\n    if (@hasDecl(Tactic, \\"emitPairTermPayload\\")) {\\n        try Tactic.emitPairTermPayload(config_ptr, ops, choice.left, choice.right, choice.payload, sink);\\n        return;\\n    }\\n    if (@hasDecl(Tactic, \\"emitPairTerm\\")) {\\n        try Tactic.emitPairTerm(config_ptr, ops, choice.left, choice.right, choice.term_index, sink);\\n        return;\\n    }\\n    if (choice.term_index != 0) return error.InvalidWickTerm;\\n    if (try Tactic.emitPairTerms(config_ptr, ops, choice.left, choice.right, sink) == 0) {\\n        return error.InvalidWickBranch;\\n    }\\n}\\n\\nfn emitBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, scratch: anytype, depth: usize, sign: i8, residual: []const usize, sink: anytype) !void {\\n    if (sign < 0 and @hasDecl(Tactic, \\"emitBranchSign\\")) {\\n        try Tactic.emitBranchSign(config_ptr, sign, sink);\\n    }\\n\\n    var index: usize = 0;\\n    while (index < depth) : (index += 1) {\\n        try emitPairTerm(Tactic, config_ptr, ops, scratch.pair_stack[index], sink);\\n    }\\n\\n    if (!try Tactic.emitZeroModeBaseCase(config_ptr, ops, residual, sink)) {\\n        return error.InvalidWickBranch;\\n    }\\n}\\n\\nfn pairResultLiveMask(live_mask: u128, candidate: PairCandidate) u128 {\\n    var next = live_mask & ~bit(candidate.left) & ~bit(candidate.right);\\n    var residual_index: usize = 0;\\n    while (residual_index < candidate.residual_count) : (residual_index += 1) {\\n        next |= bit(candidate.residuals[residual_index]);\\n    }\\n    return next;\\n}\\n\\nfn pairClearedLiveMask(live_mask: u128, candidate: PairCandidate) u128 {\\n    return live_mask & ~bit(candidate.left) & ~bit(candidate.right);\\n}\\n\\nfn hasMandatoryCandidate(scratch: anytype, live_mask: u128, next_candidate_index: usize, candidate_count: usize) bool {\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        if (!candidate.mandatory_before_base) continue;\\n        if (live(live_mask, candidate.left) and live(live_mask, candidate.right)) return true;\\n    }\\n    return false;\\n}\\n\\nfn pureFullContraction(scratch: anytype, ops: kernel.Call.MultiOp, candidate_count: usize) bool {\\n    if ((ops.operators.len & 1) != 0) return false;\\n\\n    var operator_index: usize = 0;\\n    while (operator_index < ops.operators.len) : (operator_index += 1) {\\n        if (scratch.zero_mode_survivable[operator_index]) return false;\\n    }\\n\\n    var candidate_index: usize = 0;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        if (scratch.candidates[candidate_index].residual_count != 0) return false;\\n    }\\n\\n    return true;\\n}\\n\\nfn pushBranchPairTerm(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, candidate: PairCandidate) !void {\\n    try Tactic.pushBranchPairTerm(config_ptr, ops, candidate.left, candidate.right, candidate.payload, branch);\\n}\\n\\nfn emitAccumulatedBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, sign: i8, residual: []const usize, sink: anytype) !void {\\n    try Tactic.emitAccumulatedBranch(config_ptr, ops, branch, sign, residual, sink);\\n}\\n\\nfn emitFullAccumulatedBranch(comptime Tactic: type, config_ptr: *const Tactic.Config, ops: kernel.Call.MultiOp, branch: anytype, sign: i8, sink: anytype) !void {\\n    if (@hasDecl(Tactic, \\"emitFullContractionBranch\\")) {\\n        try Tactic.emitFullContractionBranch(config_ptr, ops, branch, sign, sink);\\n        return;\\n    }\\n    try Tactic.emitAccumulatedBranch(config_ptr, ops, branch, sign, &.{}, sink);\\n}\\n\\nfn walkFullAccumulated(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    branch: *Tactic.BranchState,\\n    live_mask: u128,\\n    depth: usize,\\n    sign: i8,\\n    sink: anytype,\\n) !usize {\\n    if (live_mask == 0) {\\n        try emitFullAccumulatedBranch(Tactic, config_ptr, ops, branch, sign, sink);\\n        return 1;\\n    }\\n    if (depth == scratch.pair_stack.len) return 0;\\n\\n    const first = firstLiveIndex(live_mask);\\n\\n    var branch_count: usize = 0;\\n    var candidate_index: usize = scratch.first_candidate_by_left[first];\\n    const candidate_end: usize = scratch.end_candidate_by_left[first];\\n    while (candidate_index < candidate_end) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, right)) continue;\\n\\n        const saved = branch.snapshot();\\n        const next_mask = pairClearedLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n        branch_count += try walkFullAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, depth + 1, next_sign, sink);\\n        branch.restore(saved);\\n    }\\n\\n    return branch_count;\\n}\\n\\nfn walkAccumulated(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    branch: *Tactic.BranchState,\\n    live_mask: u128,\\n    next_candidate_index: usize,\\n    candidate_count: usize,\\n    depth: usize,\\n    sign: i8,\\n    sink: anytype,\\n) !usize {\\n    var branch_count: usize = 0;\\n    const forced_residual = firstForcedResidual(scratch, live_mask);\\n\\n    if (forced_residual == null and !hasMandatoryCandidate(scratch, live_mask, next_candidate_index, candidate_count)) {\\n        const residual = residualSlice(scratch, ops.operators.len, live_mask);\\n        if (try zeroModeSucceeds(Tactic, config_ptr, ops, residual)) {\\n            try emitAccumulatedBranch(Tactic, config_ptr, ops, branch, sign, residual, sink);\\n            branch_count += 1;\\n        }\\n    }\\n\\n    if (depth == scratch.pair_stack.len) return branch_count;\\n\\n    if (forced_residual) |forced| {\\n        var candidate_link = scratch.first_candidate_by_operator[forced];\\n        while (candidate_link != no_candidate_index) {\\n            const candidate_index: usize = candidate_link;\\n            const candidate = scratch.candidates[candidate_index];\\n            candidate_link = nextCandidateForOperator(candidate, forced);\\n            if (candidate_index < next_candidate_index) continue;\\n            const left: usize = candidate.left;\\n            const right: usize = candidate.right;\\n            if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n            const saved = branch.snapshot();\\n            const next_mask = pairResultLiveMask(live_mask, candidate);\\n            const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n            try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n            branch_count += try walkAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, sink);\\n            branch.restore(saved);\\n            if (candidate.mandatory_before_base) return branch_count;\\n        }\\n        return branch_count;\\n    }\\n\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n        const saved = branch.snapshot();\\n        const next_mask = pairResultLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        try pushBranchPairTerm(Tactic, config_ptr, ops, branch, candidate);\\n        branch_count += try walkAccumulated(Tactic, config_ptr, ops, scratch, branch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, sink);\\n        branch.restore(saved);\\n        if (candidate.mandatory_before_base) return branch_count;\\n    }\\n\\n    return branch_count;\\n}\\n\\nThe recursion enumerates pair subsets in lexicographic order, which prevents\\nthe same disjoint pairing set from being reached in multiple orders.  At each\\nnode it first tries the zero-mode base case, then extends the branch by one\\nallowed pair.\\nfn walk(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    scratch: anytype,\\n    live_mask: u128,\\n    next_candidate_index: usize,\\n    candidate_count: usize,\\n    depth: usize,\\n    sign: i8,\\n    emit: bool,\\n    sink: anytype,\\n) !usize {\\n    var branch_count: usize = 0;\\n    const forced_residual = firstForcedResidual(scratch, live_mask);\\n\\n    if (forced_residual == null and !hasMandatoryCandidate(scratch, live_mask, next_candidate_index, candidate_count)) {\\n        const residual = residualSlice(scratch, ops.operators.len, live_mask);\\n        if (try zeroModeSucceeds(Tactic, config_ptr, ops, residual)) {\\n            if (emit) try emitBranch(Tactic, config_ptr, ops, scratch, depth, sign, residual, sink);\\n            branch_count += 1;\\n        }\\n    }\\n\\n    if (depth == scratch.pair_stack.len) return branch_count;\\n\\n    if (forced_residual) |forced| {\\n        var candidate_link = scratch.first_candidate_by_operator[forced];\\n        while (candidate_link != no_candidate_index) {\\n            const candidate_index: usize = candidate_link;\\n            const candidate = scratch.candidates[candidate_index];\\n            candidate_link = nextCandidateForOperator(candidate, forced);\\n            if (candidate_index < next_candidate_index) continue;\\n            const left: usize = candidate.left;\\n            const right: usize = candidate.right;\\n            if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n            const next_mask = pairResultLiveMask(live_mask, candidate);\\n            const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n            if (!emit) {\\n                branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, false, sink);\\n                if (candidate.mandatory_before_base) return branch_count;\\n                continue;\\n            }\\n\\n            scratch.pair_stack[depth] = .{ .left = candidate.left, .right = candidate.right, .term_index = candidate.term_index, .payload = candidate.payload };\\n            branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, true, sink);\\n            if (candidate.mandatory_before_base) return branch_count;\\n        }\\n        return branch_count;\\n    }\\n\\n    var candidate_index = next_candidate_index;\\n    while (candidate_index < candidate_count) : (candidate_index += 1) {\\n        const candidate = scratch.candidates[candidate_index];\\n        const left: usize = candidate.left;\\n        const right: usize = candidate.right;\\n        if (!live(live_mask, left) or !live(live_mask, right)) continue;\\n\\n        const next_mask = pairResultLiveMask(live_mask, candidate);\\n        const next_sign = sign * pairSign(scratch, live_mask, left, right);\\n        if (!emit) {\\n            branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, false, sink);\\n            if (candidate.mandatory_before_base) return branch_count;\\n            continue;\\n        }\\n\\n        scratch.pair_stack[depth] = .{ .left = candidate.left, .right = candidate.right, .term_index = candidate.term_index, .payload = candidate.payload };\\n        branch_count += try walk(Tactic, config_ptr, ops, scratch, next_mask, candidate_index + 1, candidate_count, depth + 1, next_sign, true, sink);\\n        if (candidate.mandatory_before_base) return branch_count;\\n    }\\n\\n    return branch_count;\\n}\\nThe public entry point is specialized by a comptime tactic.  A tactic provides\\nthe config type, primitive pair emitter, and zero-mode base-case emitter.\\n/// wickCorrelator streams all Wick branches accepted by the tactic base case.\\npub fn wickCorrelator(\\n    comptime Tactic: type,\\n    config_ptr: *const Tactic.Config,\\n    ops: kernel.Call.MultiOp,\\n    sink: anytype,\\n) !void {\\n    const max_operator_count = tacticOperatorCount(Tactic);\\n    var scratch_storage = Scratch(max_operator_count).init();\\n    const scratch = &scratch_storage;\\n    if (ops.operators.len > max_operator_count) return error.TooManyWickOperators;\\n    const candidate_count = try buildCandidates(Tactic, config_ptr, ops, scratch);\\n    buildParities(Tactic, config_ptr, ops, scratch);\\n    buildZeroModeSurvivability(Tactic, config_ptr, ops, scratch);\\n    if (@hasDecl(Tactic, \\"BranchState\\") and @hasDecl(Tactic, \\"pushBranchPairTerm\\") and @hasDecl(Tactic, \\"emitAccumulatedBranch\\")) {\\n        var branch = Tactic.BranchState.init();\\n        if (pureFullContraction(scratch, ops, candidate_count)) {\\n            _ = try walkFullAccumulated(Tactic, config_ptr, ops, scratch, &branch, initialLiveMask(max_operator_count, ops.operators.len), 0, 1, sink);\\n            return;\\n        }\\n        _ = try walkAccumulated(Tactic, config_ptr, ops, scratch, &branch, initialLiveMask(max_operator_count, ops.operators.len), 0, candidate_count, 0, 1, sink);\\n        return;\\n    }\\n    _ = try walk(Tactic, config_ptr, ops, scratch, initialLiveMask(max_operator_count, ops.operators.len), 0, candidate_count, 0, 1, true, sink);\\n}\\n","n":0.02}}},{"i":17,"$":{"1":{"v":"Bosonization","n":1}}},{"i":18,"$":{"1":{"v":"Symmetry","n":1},"2":{"v":"A symmetry is labeled by its name and a Lie Algebra, which can be u(1) or a simple Lie algebra.\\n/// Symmetry labels a named symmetry algebra.\\nconst Symmetry = struct {\\n    name: []const u8,\\n    algebra: LieAlgebra,\\n};\\n\\n/// LieAlgebra distinguishes U(1) from simple Lie algebras.\\nconst LieAlgebra = union(enum) {\\n    u1,\\n    simple: SimpleLieAlgebra,\\n};\\n\\n/// SimpleLieAlgebra stores the simple family and rank.\\npub const SimpleLieAlgebra = struct {\\n    family: LieFamily,\\n    rank: u8,\\n};\\n\\n/// LieFamily names the supported simple Lie families.\\npub const LieFamily = enum {\\n    a,\\n    b,\\n    c,\\n    d,\\n    e6,\\n    e7,\\n    e8,\\n    f4,\\n    g2,\\n};\\n\\nA quantum number is labeled by the symmetry algebra and its representation.\\n/// QuantumNumbers is the tensor-product list of representation ids.\\npub const QuantumNumbers = []const RepresentationId;\\n\\n/// SymmetryId names a symmetry in a symmetry store.\\npub const SymmetryId = u8;\\n/// RepresentationId names a representation in a representation store.\\npub const RepresentationId = u8;\\n\\n/// Representation stores a symmetry id and its label.\\nconst Representation = struct {\\n    symmetry: SymmetryId,\\n    label: RepresentationLabel,\\n};\\n\\n/// RepresentationLabel stores either a rational charge or Dynkin labels.\\nconst RepresentationLabel = union(enum) {\\n    u1_charge: U1ChargeRational,\\n    dynkin: []const i8,\\n};\\n\\n/// U1ChargeRational stores a rational U(1) charge.\\npub const U1ChargeRational = struct {\\n    numerator: i8,\\n    denominator: i8,\\n};\\nAn abstract tensor index carries a label and a particular representation.\\n/// TensorIndex stores a representation id and concrete index label.\\nconst TensorIndex = struct {\\n    representation: RepresentationId,\\n    label: u32,\\n};\\n\\n/// TensorIndexId names a tensor index in a tensor expression.\\npub const TensorIndexId = u32;\\nAn example of a concrete tensor structures - should only appear for SO-type groups, todo.\\n/// TensorStructure stores simple built-in tensor structures.\\npub const TensorStructure = union(enum) {\\n    none,\\n    kronecker_delta: struct {\\n        left: TensorIndexId,\\n        right: TensorIndexId,\\n    },\\n};\\n","n":0.062}}},{"i":19,"$":{"1":{"v":"Clebsch-Gordan","n":1},"2":{"v":"This node is the handoff for implementing the tensor-code backend that will\\neventually generate Clebsch-Gordan data and invariant tensor bases.  The first\\nbackend milestone is not explicit CG coefficients.  It is a reliable,\\ngeneric, cached decomposition engine:\\n\\\\[\\n  R\\\\otimes S = \\\\bigoplus_T m_T T.\\n\\\\]\\nThe CG generator will consume these decompositions to enumerate coupling paths\\nto a target representation, usually the singlet \\\\(\\\\mathbf 1\\\\).\\nGoalsThe implementation must follow AGENTS.md:\\nwrite simple explicit procedural Zig;\\ndesign lean data structures first;\\navoid large intermediate symbolic expressions;\\nkeep data flows parallelizable;\\nexpose the bare minimum public API;\\nkeep the CFT-facing API in Tensor-Code;\\nkeep internal backend/decomposition/projector details out of the public root\\n  unless a caller actually needs them;\\ndocument the implementation in org-roam nodes and keep prose concise.\\nThe mathematical and product requirements are:\\nsupport \\\\(A_n\\\\) for \\\\(SU(N)\\\\);\\nsupport \\\\(B_n,D_n\\\\) for \\\\(SO(N)\\\\) and \\\\(Spin(N)\\\\);\\nsupport \\\\(E_6,E_7,E_8\\\\), with \\\\(E_8\\\\) a key target;\\ndo not overfocus the architecture on \\\\(SO(10)\\\\), even though \\\\(Spin(10)\\\\)\\n  is the main stress test;\\nidentify irreps by Dynkin labels, not dimensions;\\ntreat multiplicities as first-class channel copies;\\ndistinguish abstract irreps from concrete index realizations;\\nsupport constrained realizations such as \\\\(144\\\\subset 10\\\\otimes16\\\\) without\\n  carrying the full ambient tensor product through every internal step;\\nrepresent large invariant bases as compact coupling paths, not dense tensor\\n  components;\\nrender explicit gamma/projector formulas only on demand and by streaming\\n  terms to a sink;\\nmake every projector appearing in a returned result fully expandable.\\nThe goals are concrete:\\nAbstract representation theory must be correct first.  Irreps are Dynkin\\n   labels; decompositions have explicit multiplicities; channels are not\\n   identified by dimension.\\nLarge invariant bases must be stored as compact coupling paths.  A basis\\n   element is a recipe: external legs, intermediate channels, and local\\n   projector/CG kernel ids.\\nProjectors in returned results must not be opaque tokens.  A projector id\\n   may be the compact storage form, but it must carry enough derivation data to\\n   expand into concrete symbolic terms later.\\nExpansion must be streamed.  Fully expandable does not mean eagerly\\n   expanded.  It means the renderer can enumerate the terms without inventing\\n   missing identities or asking the user for hidden Fierz relations.\\nThe runnable target is not merely obtaining \\\\(73744\\\\) abstract basis\\n   expressions.  The runnable target is also being able to expand each selected\\n   basis element through a caller-supplied filter, and for the resulting stream\\n   to be consumed at high throughput by CFT/correlator code.\\nFilters are part of the execution model.  They may discard terms by index\\n   support, operator kind, realization leg, component assignment, symmetry,\\n   coefficient class, or target downstream contraction.  The renderer must\\n   apply filters while streaming, not after materializing the full expansion.\\n   Partial filtering must be sound: a partial product may be rejected only\\n   when no completion can survive.  Otherwise the filter must return\\n   undecided and the expansion continues.\\nCoupling-tree policy is part of the basis convention.  Build one canonical\\n   tree first, and certify independence for that tree.  Multiple trees are\\n   different bases unless a basis-change or reduction map is explicitly\\n   constructed.\\nFierz and projector identities must be derived from the backend\\n   construction: channel decomposition, idempotent splitting, highest-weight\\n   solvers, Casimir separation where valid, and explicit residual linear\\n   algebra only when necessary.  They are not user-supplied tables.\\nCompute estimates must include projector expansion cost.  Counting coupling\\n   paths alone is insufficient if each path contains a projector whose\\n   expansion is large.\\nThe motivating stress target remains:\\n\\\\[\\n  \\\\dim\\\\operatorname{Hom}_{Spin(10)}(\\\\mathbf 1,16^{12}) = 73744.\\n\\\\]\\nThose \\\\(73744\\\\) objects should be records describing coupling paths and local\\nprojector ids.  They should not be expanded into component tensors during basis\\nenumeration.  However, every projector id in those paths must be traceable to\\na stored expansion procedure.  When the caller asks for concrete notation,\\nrendering should stream terms built from metrics, epsilons, named operators,\\ngamma-like backend operators, or projector blocks.  The stream must be\\nfilterable as it is produced.  A successful implementation is one where a\\ndownstream consumer can iterate through selected expanded terms without waiting\\nfor the full expansion of all \\\\(73744\\\\) invariants to be allocated.\\nNon-SolutionsThe following are explicitly not acceptable:\\nReturning only abstract representation-theory counts.  Counts are useful for\\n  feasibility, but CFT code needs actual invariant handles and eventually\\n  streamable tensor expressions.\\nReturning opaque named projectors with no derivation recipe.  A compact\\n  projector id is acceptable only if the backend can expand it.\\nReturning compact coupling paths with no efficient expansion path.  A list\\n  of \\\\(73744\\\\) abstract expressions is not enough.\\nExpanding all \\\\(73744\\\\) basis elements into memory before filtering.  Filters\\n  must be pushed into the streaming expansion loop.\\nTreating rendering as a slow debug printer.  Rendering is part of the\\n  runnable computational path and must feed downstream correlator code at a\\n  fast pace.\\nTreating \\\\(144\\\\subset10\\\\otimes16\\\\) as \\\\(10\\\\otimes16\\\\) throughout all\\n  internal coupling steps and projecting only at the very end.  The realization\\n  projector belongs at the boundary.\\nRelying on hand-entered Fierz identities.  Identities must follow from the\\n  algebraic construction and verified projector/idempotent relations.\\nBuilding dense tensors or dense component spaces such as \\\\(16^{12}\\\\).\\nDesigning an \\\\(SO(10)\\\\)-only system.  \\\\(Spin(10)\\\\) is a stress test, not the\\n  architecture.\\nMaking gamma matrices part of the generic public ADE API.  Gamma-like\\n  objects are backend-rendered named operators or internal SO/Spin atoms.\\nOptimizing by hiding mathematical ambiguity.  Multiplicity copies,\\n  realization channels, basis conventions, and signature/rendering conventions\\n  must be explicit.\\nRejecting a partial expanded term because the current prefix does not yet\\n  match a filter.  Prefix filters must distinguish reject from undecided.\\nConcatenating paths from several coupling trees and calling the union a\\n  larger independent basis.  This is allowed only after constructing and\\n  applying a basis-change or reduction map.\\nWeasel Modes And Required EvidenceAn implementation is not accepted because it can print plausible data.  It\\nmust produce the required records and expose enough evidence that the records\\nare real.  The following failure modes are common and must be rejected during\\nreview:\\nClaiming a \\"fast structure generator\\" that only returns the count of\\n  invariants.  Required evidence: stored basis handles with inspectable path\\n  records, one record per basis element.\\nReturning a list of abstract labels without local channel data.  Required\\n  evidence: each path stores the coupling tree, intermediate channel handles,\\n  multiplicity-copy ids, and local projector/CG kernel ids.\\nReturning projectors as names such as P_144 with no expansion route.\\n  Required evidence: every projector id reports its derivation kind and an\\n  expansion source, or returns a typed NotExpandable error before it appears\\n  in a returned basis.\\nUsing an overcomplete gamma-monomial list and calling it a basis.  Required\\n  evidence: basis construction happens in representation/multiplicity space\\n  before rendering; gamma terms are a rendering of already-independent basis\\n  elements.\\nRunning a dense component-space linear solve and hiding the cost behind a\\n  small output.  Required evidence: logs/counters for maximum allocated weight\\n  multiset size, path count, projector count, and no allocations proportional\\n  to \\\\(16^{12}\\\\) or another full tensor component space.\\nFiltering after materializing all expanded terms.  Required evidence:\\n  filters are called during expansion, and a reject-all filter allocates no\\n  large output buffer.\\nFailing to distinguish multiplicity copies.  Required evidence:\\n  decomposition terms include multiplicity, and local kernels/projectors\\n  carry the selected copy.\\nTreating projected realizations as ambient products until the final output.\\n  Required evidence: external legs using constrained realizations carry a\\n  boundary projector once, and internal coupling uses the target irrep.\\nClaiming independence from successful numerical spot checks only.  Required\\n  evidence: independence follows from the decomposition path construction, and\\n  numerical/rendered checks are secondary audits.\\nHardcoding known decompositions for the acceptance tests.  Required\\n  evidence: tests include at least one generated decomposition not present as a\\n  literal special case, and the backend exposes counters showing which generic\\n  procedure produced it.\\nEvery major stage must produce a compact audit record:\\nalgebra and convention ids;\\ninput irreps;\\nnumber of weights generated;\\ntotal weight multiplicity;\\nproduct multiset size;\\ndecomposition channel count;\\nsum of channel dimensions times multiplicities;\\nnumber of basis paths;\\nnumber of distinct projector ids;\\nmaximum known projector expansion size, if available;\\nfilter accept/reject/undecided counts during streamed expansion.\\nThese audits are mandatory internal milestone evidence, not optional debug\\noutput.  They are not user-facing API by default, but each backend milestone\\nmust produce enough audit data to prove that it is doing the promised\\ncomputation.\\nIndependence By ConstructionThe basis must be independent before rendering.  The construction should be:\\nFor each binary product \\\\(R\\\\otimes S\\\\), decompose into irreps:\\n   \\\\[\\n     R\\\\otimes S=\\\\bigoplus_T M_{RS}^T\\\\otimes T.\\n   \\\\]\\n   Here \\\\(M_{RS}^T\\\\) is the finite multiplicity space.  If \\\\(m_T=\\\\dim\\n   M_{RS}^T\\\\), choose a basis vector \\\\(e_i\\\\) for each copy \\\\(i=0,\\\\dots,m_T-1\\\\).\\nA local CG kernel is an intertwiner selecting one basis vector in\\n   \\\\(M_{RS}^T\\\\).  Distinct multiplicity-copy ids are distinct basis vectors in\\n   that multiplicity space.\\nFor a fixed canonical coupling tree, a global path is a product of local\\n   choices: at every internal node, choose the output channel and\\n   multiplicity-copy basis vector.\\nThe set of all paths ending in the requested target \\\\(T\\\\), usually\\n   \\\\(\\\\mathbf 1\\\\), is a basis of the multiplicity space\\n   \\\\[\\n     \\\\operatorname{Hom}_{\\\\mathfrak g}\\n     \\\\left(T,\\\\;R_1\\\\otimes\\\\cdots\\\\otimes R_n\\\\right)\\n   \\\\]\\n   for that tree and basis convention.\\nIndependence follows for that fixed tree because the decomposition is a direct\\nsum of irreducible channels and explicit multiplicity spaces.  We are not\\nselecting linearly independent gamma expressions from an overcomplete list.  We\\nare selecting basis vectors in multiplicity spaces and composing intertwiners\\nalong one tree convention.\\nDifferent tree policies give different coordinate systems on the same\\nmultiplicity space.  They must not be concatenated.  If the implementation\\nlater supports several trees, each tree must separately produce the target\\nmultiplicity.  The combined count across trees is not an independence\\ncertificate; it is redundant data until a basis-change or reduction map is\\navailable.\\nRendered formulas may look dependent because gamma matrices, epsilons,\\nmetrics, and projector blocks satisfy identities.  Those identities are part\\nof the rendering backend.  They must not be used to define the abstract basis.\\nRendering must be a faithful expansion of the already-selected path basis in a\\nchosen convention.  If a renderer cannot prove or verify faithfulness for a\\nconvention, it must report a typed error or mark the expansion as an audit\\ntarget; it must not silently collapse or add basis elements.\\nRequired independence checks:\\nThe number of emitted basis paths equals the target multiplicity computed by\\n  dynamic programming over decompositions.\\nFor every binary decomposition, the sum\\n  \\\\[\\n    \\\\sum_T m_T\\\\dim T\\n  \\\\]\\n  equals \\\\(\\\\dim R\\\\,\\\\dim S\\\\).\\nLocal projector records for a product channel must satisfy the abstract\\n  projector laws in their construction record: idempotency and channel\\n  separation.  When explicit formulas exist, audits should verify\\n  \\\\(P_iP_j=\\\\delta_{ij}P_i\\\\) in the chosen representation of the local product.\\nBoundary realization projectors must satisfy the same rule: e.g.\\n  \\\\(144\\\\subset10\\\\otimes16\\\\) is represented by an idempotent whose image has\\n  dimension \\\\(144\\\\), and the complementary channel is accounted for.\\nRendered term streams are audits of the basis, not the source of the basis.\\n  Component evaluation or filtered numerical checks can catch renderer bugs\\n  but cannot replace the multiplicity-space construction.\\nImplementation InstructionsProceed in this order.  Do not skip ahead to rendered gamma formulas or\\nhigh-level invariant counts.\\nImplement the backend registry privately in Context.  It must select a\\n   backend by algebra family and return typed unsupported-capability errors.\\nImplement flat weight storage and a weight-system cache.  Store signed\\n   weight coordinates in one buffer and weight records as offsets.  Add audit\\n   counters for weight count and total multiplicity.\\nImplement the generic highest-weight weight-system generator.  Validate it\\n   on known dimensions before any tensor-product code is accepted.\\nImplement tensor-product character multiplication and subtraction.  The\\n   result must be flat product terms with multiplicities.  Store the result in\\n   Tensor Decomposition.\\nAdd decomposition acceptance tests:\\n   \\\\(SU(2):2\\\\otimes2=1\\\\oplus3\\\\),\\n   \\\\(Spin(10):10\\\\otimes16=16\\\\oplus144\\\\),\\n   \\\\(Spin(10):16\\\\otimes16=10\\\\oplus120\\\\oplus126\\\\), and\\n   \\\\(E_8:248\\\\otimes248\\\\) with the known channels.\\nImplement dynamic-programming path counting to a target using cached binary\\n   decompositions.  This is still counting, but it must use the same channel\\n   records that path enumeration will use.\\nImplement path enumeration for one canonical coupling tree.  Each emitted\\n   path must store its local channel choices and multiplicity-copy ids.  The\\n   path count must equal the dynamic count.  Do not enumerate several trees\\n   until basis-change or reduction maps exist.\\nImplement projector identity creation.  Each projector id must record its\\n   role, convention, derivation kind, and expansion obligation.\\nImplement a minimal expansion interface that can walk one path and call a\\n   sink with streamed symbolic terms.  At first it may return typed\\n   NotImplemented for missing local formulas, but it must not return opaque\\n   successful placeholders.\\nImplement filters inside the expansion loop.  The filter decision is\\n    tri-state: accept, reject, or undecided.  A partial term can be\\n    rejected only when rejection is final under all completions.  Prove with a\\n    reject-all filter test that no large output buffer is allocated.\\nOnly after the generic path and projector infrastructure is correct, add\\n    specialized SO/Spin gamma/projector renderers, SU tableau/Gelfand-Tsetlin\\n    optimizations, or E-type special renderers.\\nAt every step, prefer the smallest working data structure.  Add a new\\nabstraction only when it removes real duplication or separates a hot data path.\\nDo not add public exports for backend internals unless CFT code has a concrete\\ncall site that needs them.\\nCurrent StateThe public API is rooted at Tensor-Code.  Public\\ncallers create a context, register an algebra, register irreps, optionally\\nregister concrete realizations, and request an invariant basis:\\nvar ctx = try tensor.Context.init(allocator);\\ndefer ctx.deinit();\\n\\nconst so10 = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\nconst spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\nconst leg = tensor.ExternalLeg.primitive(spinor, &.{\\n    .init(.spinor, \\"a\\"),\\n});\\nconst basis = try ctx.invariantBasis(.{\\n    .algebra = so10,\\n    .external_legs = &.{leg},\\n});\\nConventions are configurable at context construction:\\nvar ctx = try tensor.Context.initWithOptions(allocator, .{\\n    .algebra_conventions = .{\\n        .quadratic_casimir_scale = .{ .numerator = 1, .denominator = 1 },\\n    },\\n    .default_signature = .complex,\\n});\\ndefer ctx.deinit();\\nThe current substrate is:\\nTensor Root Data validates supported simple algebras,\\n  stores Cartan data, computes positive roots, dual Dynkin labels, Weyl\\n  dimensions, and quadratic Casimirs.\\nTensor Representation Store interns algebras and\\n  irreps.  It computes metadata from Dynkin labels and provides explicit\\n  dualIrrep lookup without recursively closing registration under duals.\\nTensor Realization records concrete external\\n  index shapes.  Projected realizations store a selected channel, so a\\n  constrained leg such as \\\\(144\\\\subset 10\\\\otimes16\\\\) is a first-class boundary\\n  realization.\\nTensor Decomposition owns the binary product\\n  decomposition cache.  It stores flat product terms and has a hash index keyed\\n  by the pair of irrep handles.\\nTensor Projector owns projector identities:\\n  product-channel projectors, boundary-realization projectors, and basis-change\\n  projectors.\\nTensor Backend defines the internal backend\\n  capability contract.\\nTensor Rendering defines streamed symbolic\\n  terms.  SymbolicTerm carries borrowed slices valid only until the sink\\n  returns.  The public root does not expose a gamma-specific type; SO/Spin\\n  renderers can emit backend operators through generic symbolic atoms.\\nBuild-checked metadata cases currently include:\\n\\\\(B_4\\\\): vector \\\\(9\\\\), spinor \\\\(16\\\\);\\n\\\\(D_5\\\\): vector \\\\(10\\\\), spinor \\\\(16\\\\), vector-spinor \\\\(144\\\\);\\n\\\\(E_6\\\\): fundamental \\\\(27\\\\);\\n\\\\(E_7\\\\): fundamental \\\\(56\\\\);\\n\\\\(E_8\\\\): adjoint \\\\(248\\\\).\\nThe following are still intentionally not implemented:\\nbinary decomposition algorithms;\\nweight-system generation;\\nchannel-path enumeration;\\nlocal CG/projector construction;\\nexplicit gamma or projector expansion execution;\\nexpansion filters and downstream stream consumers;\\nContext.renderInvariant;\\nContext.evaluateInvariant.\\nBackend GoalThe first backend should be a generic highest-weight backend.  It should be a\\ncorrectness backend and cache builder, not yet a family-specific optimized\\nrenderer.  Its first job is to answer:\\nis this algebra supported?\\nwhat is the weight system of this irrep?\\nwhat is the decomposition of \\\\(R\\\\otimes S\\\\)?\\nwhat stable projector identity corresponds to one product channel?\\nThe backend must not allocate dense tensor-product component spaces.  It may\\nconstruct compact weight multisets and flat cache records.\\nBackend Build Order1. Backend registryAdd an internal backend registry owned by the private Context implementation.\\nIt maps LieFamily to a backend record and selects a backend from an algebra\\nfamily.  It should not be exported from Tensor-Code.\\nStart with one backend implementation:\\nfile: generic-highest-weight-backend.zig;\\nsupports \\\\(A_n,B_n,D_n,E_6,E_7,E_8\\\\);\\nreturns typed missing-capability errors for unimplemented operations.\\nThe registry should make no CFT-facing API changes.  CFT code still asks the\\ncontext for invariant bases.\\n2. Root-data helpersUse Tensor Root Data as the source of algebra\\nconventions.  Do not duplicate Cartan matrices or exceptional diagrams in the\\nbackend.\\nThe backend will need internal helpers for:\\nrank;\\nCartan entries;\\nsimple-root reflection on weights;\\ndominant-weight test;\\nhighest-weight validation;\\nexact rational inner products where Freudenthal or Casimir formulas need\\n  them.\\nKeep these helper APIs internal unless a later caller clearly needs them.\\n3. Weight storageWeights are not public API.  Store them in flat buffers:\\nconst WeightRef = struct {\\n    dynkin_offset: u32,\\n};\\n\\nconst WeightMultiplicity = struct {\\n    weight: WeightRef,\\n    multiplicity: u32,\\n};\\nThe actual Dynkin-coordinate arrays should live in one flat signed buffer,\\nprobably i32 once tensor products become large enough.  Signed coordinates\\nare necessary because intermediate weights and reflected weights need not be\\ndominant.\\nStore rank once on the weight-system span or get it from the algebra.  Do not\\nstore rank in every weight record.  Do not allocate one heap object per weight.\\n4. Weight-system generatorImplement a cached weight-system generator for one highest-weight irrep.\\nFreudenthal multiplicity recursion is the generic first choice because it\\nworks uniformly for the supported simple families.\\nCache key:\\nconst WeightSystemKey = struct {\\n    algebra: store.AlgebraHandle,\\n    irrep: store.IrrepHandle,\\n};\\nCache value:\\nconst WeightSystem = struct {\\n    weights_offset: u32,\\n    weights_len: u32,\\n    rank: u8,\\n    dynkin_coords_offset: u32,\\n    dynkin_coords_len: u32,\\n};\\nAcceptance checks:\\n\\\\(D_5\\\\) spinor has total weight multiplicity \\\\(16\\\\);\\n\\\\(D_5\\\\) vector has total weight multiplicity \\\\(10\\\\);\\n\\\\(B_4\\\\) spinor has total weight multiplicity \\\\(16\\\\);\\n\\\\(E_8\\\\) adjoint has total weight multiplicity \\\\(248\\\\).\\n5. Tensor-product decompositionUse character subtraction first.  It is generic and easier to audit than a\\nfamily-specific closed formula.\\nAlgorithm:\\nget cached weight systems for \\\\(R\\\\) and \\\\(S\\\\);\\nadd all pairs of weights to build the weight multiset for \\\\(R\\\\otimes S\\\\);\\nfind a maximal dominant weight still present, using one deterministic\\n   dominance-order rule;\\nits remaining multiplicity is the multiplicity of the next channel;\\nsubtract that many copies of the cached character of that highest weight;\\nrepeat until the multiset is empty.\\nThe dominance selection rule must be explicit and stable.  A practical first\\nrule is:\\nscan only weights with positive residual multiplicity;\\ndiscard non-dominant weights;\\nchoose a dominant weight not strictly below another remaining dominant\\n   weight in the root partial order;\\nbreak ties by lexicographic Dynkin labels.\\nAfter every subtraction, no residual multiplicity may become negative.  A\\nnegative residual is a backend bug, not a recoverable ambiguity.  The selected\\nchannel multiplicity is the residual multiplicity of the selected highest\\nweight before subtraction.\\nThe audit invariant after each subtraction is:\\n\\\\[\\n  \\\\chi_{\\\\mathrm{remaining}}+\\\\sum_i m_i\\\\chi_{T_i}\\n  = \\\\chi_R\\\\chi_S .\\n\\\\]\\nThe final residual character must be zero.  This catches lost multiplicities,\\nwrong dominant-weight selection, and incorrect subtraction signs.\\nThis is not the final fastest algorithm.  It is the first trusted backend.  It\\nwill become the oracle against which specialized \\\\(SO/Spin\\\\), \\\\(SU\\\\), and\\nexceptional backends are checked.\\nAcceptance checks:\\n\\\\(SU(2): 2\\\\otimes2 = 1\\\\oplus3\\\\);\\n\\\\(Spin(10): 10\\\\otimes16 = 16\\\\oplus144\\\\);\\n\\\\(Spin(10): 16\\\\otimes16 = 10\\\\oplus120\\\\oplus126\\\\);\\n\\\\(E_8: 248\\\\otimes248\\\\) matches the known decomposition, including\\n  multiplicities and dimensions.\\n6. Decomposition cache integrationThe backend writes decompositions into Tensor Decomposition.\\nThe cache stores:\\nproduct key \\\\(R,S\\\\);\\nflat product terms \\\\((T,m_T)\\\\);\\nstable handle for repeated requests.\\nThe future internal context method should look like:\\nfn decomposeProduct(ctx: *Context, left: store.IrrepHandle, right: store.IrrepHandle) !decomposition.ProductDecompositionHandle\\nDo not expose this method publicly unless CFT code needs it.\\n7. Projector identities and expansion obligationsFor each product term, the backend may create a product-channel projector\\nidentity in Tensor Projector.  The first\\ndecomposition backend does not need to expand projector formulas immediately,\\nbut a successful basis result must not contain projector ids that have no\\nexpansion route.  Internal blocked records are allowed only if they fail before\\nbeing returned as successful invariant data.  Each projector record needs\\nenough information to recover an expansion path.\\nThe projector identity records:\\nleft irrep;\\nright irrep;\\ntarget channel;\\nconvention id;\\nderivation kind;\\nexpansion source, such as Casimir polynomial, idempotent split,\\n  highest-weight solver output, or backend-specific verified construction.\\nDo not store expandability as a boolean.  The backend needs enough state to\\ndistinguish several useful stages:\\nconst ExpansionStatus = enum {\\n    not_requested,\\n    expandable_named,\\n    expandable_blocks,\\n    expandable_terms,\\n    blocked_missing_formula,\\n    blocked_unverified_identity,\\n};\\nexpandable_named is only a rendering mode.  It is not proof that the\\nprojector satisfies the fully-expandable requirement.  A projector appearing in\\na successful invariant basis must have a derivation route that can reach\\nexpandable_terms in the requested convention, even when the caller chooses to\\nrender it as a compact name.\\nAcceptance checks:\\nrepeated requests return the same projector id;\\nmultiplicity copies are explicit;\\nno symbolic projector expansion is allocated during decomposition;\\nevery returned projector reports an ExpansionStatus in the requested\\n  convention;\\nblocked projector statuses are typed errors before successful basis return,\\n  not silently named placeholders;\\na basis result rendered in named mode still audits that each local projector\\n  has a term-expansion route.\\n8. Backend capability implementationUpdate Tensor Backend only as needed to support the\\ngeneric backend.  The expected internal procedures are:\\nvalidateAlgebra;\\nirrepMetadata, if a backend-specific metadata path is needed;\\nweightSystem;\\ndecomposeProduct;\\nlocalProjectorId.\\nRendering remains NotImplemented.\\n9. Expansion stream and filtersBefore explicit projector formulas are implemented, define the execution\\nboundary they must satisfy.  The expansion API should be a streaming pipeline:\\nfn renderInvariantExpanded(\\n    ctx: *Context,\\n    basis: coupling.BasisHandle,\\n    invariant: coupling.InvariantHandle,\\n    options: rendering.RenderOptions,\\n    filter: ExpansionFilter,\\n    sink: anytype,\\n) !void\\nThe exact public shape may differ, but the semantics should not:\\nconst FilterDecision = enum {\\n    accept,\\n    reject,\\n    undecided,\\n};\\nthe renderer walks the compact coupling path;\\nlocal projector expansions are pulled lazily;\\neach local expansion term is combined into the current partial term;\\nthe filter is applied as early as possible to borrowed scratch terms;\\naccepted terms are emitted immediately to the sink;\\nrejected terms are not stored;\\nundecided partial terms may continue, but the implementation must track their\\n  frontier size;\\nrepeated local projector expansions are cached by projector id and\\n  convention when doing so reduces work.\\nPartial terms can be rejected only when the predicate is final and sound under\\nall later factors.  Otherwise the filter must return undecided.  This is\\npart of correctness, not just an optimization detail.\\nFilter callbacks and sinks must not retain borrowed slices from streamed\\nterms.  If a caller needs to keep a term, it must copy it into caller-owned\\nstorage.  This keeps the renderer free to reuse scratch buffers and prevents a\\nhidden materialized expansion from growing behind the streaming interface.\\nThe filter is not a post-processing pass over a giant vector.  It is an\\nexecution hook that keeps the streamed expansion small enough to be useful.\\nPossible filter predicates:\\nterm contains or avoids a given external index block;\\nterm uses only selected symbolic operator kinds;\\nterm is compatible with a component assignment;\\nterm can contribute to a downstream contraction;\\nterm has a coefficient/operator class accepted by the target CFT routine.\\nAcceptance checks:\\nexpanding one invariant can stream terms without allocating the full\\n  expansion;\\nexpanding many invariants can reuse local projector expansions;\\na filter that rejects all terms performs no large output allocation;\\na filter that accepts a sparse subset scales with the accepted frontier plus\\n  unavoidable local projector traversal, not with a materialized full basis.\\nCompute ModelThe first backend has four different compute scales:\\nDecomposition compute: weight-system generation, tensor-product weight\\n   multisets, and character subtraction.\\nPath enumeration compute: dynamic programming over cached binary\\n   decompositions to enumerate coupling paths to a target.\\nExpansion compute: streaming the local projector/CG expansions appearing in\\n   selected paths.\\nFilter compute: deciding early which partial or complete expansion terms can\\n   be discarded before they reach downstream CFT code.\\nThe first milestone implements the first scale and prepares records for the\\nthird and fourth.  It must not pretend expansion is free.  A \\\\(73744\\\\)-path\\nresult can be small as stored paths while still being expensive to render if\\neach path contains large expandable projectors.  Therefore every feasibility\\nestimate must track:\\nnumber of basis paths;\\nnumber of distinct local projector ids used by those paths;\\nmaximum and total expected expansion terms per projector when known;\\nwhether expansion can be shared across repeated projector ids;\\nwhether expansion is requested as named, block-expanded, or term-expanded;\\nexpected filter rejection rate;\\nwhether filters can be applied to partial products or only complete terms;\\nnumber of undecided partial terms retained at each expansion layer;\\nsink throughput and backpressure behavior.\\nData-Oriented ConstraintsThe expensive objects are weight systems, tensor-product weight multisets,\\npath records, and projector expansions.  Therefore:\\ncache weight systems by irrep handle;\\nstore weights in flat buffers;\\nsort or hash compact weight keys, not heap-allocated slices;\\nstore product decompositions as flat terms;\\nkeep projector ids as small records;\\ncache projector expansions by projector id and convention when expansion is\\n  requested repeatedly;\\npush filters into expansion loops;\\nemit accepted terms immediately;\\nstream rendered terms into sinks;\\nnever build dense component tensors.\\nThe \\\\(16^{12}\\\\) target should produce coupling paths, not components.  A\\nsingle dense tensor in \\\\(16^{12}\\\\) has \\\\(16^{12}\\\\) components, while the desired\\nanswer has \\\\(73744\\\\) basis paths.  The algorithm must scale with the number of\\nchannels and paths, not the full component space.\\nExplicit Non-Goals For First BackendDo not implement the following in the first backend:\\ngamma-matrix rendering;\\nbulk explicit projector expansion execution;\\nexplicit CG coefficient tables;\\nhigher-Casimir projector splitting;\\nsparse nullspace solvers;\\nparallel execution;\\n\\\\(SU(N)\\\\) Gelfand-Tsetlin or tableau optimized CGs;\\n\\\\(SO/Spin\\\\) Clifford-specialized renderers;\\n\\\\(E_8\\\\) adjoint-specialized renderers.\\nThose belong after binary decomposition is correct, cached, and tested.  The\\ndata model must still be designed so these operations can be added without\\nchanging the meaning of existing projector ids.\\nDefinition Of DoneThe backend handoff is complete when:\\nzig build --global-cache-dir /tmp/zig-global-cache passes;\\nall acceptance decompositions above pass;\\nweight systems are cached and flat;\\nproduct decompositions are cached by key;\\nno dense tensor component arrays are allocated;\\nContext.invariantBasis can internally request product decompositions;\\nprojector records created by the backend have explicit expansion obligations;\\nexpanded invariants can be streamed through a filter without materializing\\n  their full term list;\\nrepeated projector expansions can be cached and reused;\\na downstream sink can consume accepted terms incrementally;\\nrender/evaluate remain explicitly unimplemented until projector formulas and\\n  term streaming are ready.\\n","n":0.016}}},{"i":20,"$":{"1":{"v":"Tensor Kernel","n":0.707},"2":{"v":"Tensor-code supplies compact handles for tensor structures that appear in CFT\\ncoefficients.  The expensive work is generation and selection of invariant\\ntensors, so those procedures should stream candidates rather than building a\\nlarge expression tree inside the kernel.\\nAn invariant source represents basis elements in\\n\\\\[\\n  \\\\operatorname{Hom}_{\\\\mathfrak g}(T,\\\\otimes_i R_i)\\n\\\\]\\nby handles.  Rendering into a concrete notation is backend-dependent: \\\\(A_n\\\\)\\nmay render through Young symmetrizers and \\\\(\\\\epsilon/\\\\delta\\\\) tensors, \\\\(D_n\\\\)\\nthrough metric, epsilon, and spinorial gamma data, and exceptional algebras\\nthrough their available primitive invariant tensors.\\nconst coupling = @import(\\"coupling.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\nThe basic ids are deliberately small.  They name entries in stores owned by a\\npreset, a tensor generator, or a client.\\n/// TensorExprId names a tensor expression stored by tensor-code.\\npub const TensorExprId = u32;\\n/// TensorTermId names one term inside a tensor expression store.\\nconst TensorTermId = u32;\\n/// BasisPathId names one compact coupling path in an invariant basis.\\nconst BasisPathId = u32;\\n/// RenderedTermId names one streamed symbolic tensor term.\\nconst RenderedTermId = u32;\\n/// ReductionTermId names one coefficient in a target invariant basis.\\nconst ReductionTermId = u32;\\n/// InvariantFamilyId names a family of invariant tensor candidates.\\nconst InvariantFamilyId = u16;\\n/// AlgebraSchemaId names a Lie or superalgebra schema.\\nconst AlgebraSchemaId = u16;\\n/// GradingId names a grading attached to a tensor algebra schema.\\nconst GradingId = u16;\\nInvariant generation should be pull-based.  A caller can feed the source to an\\nindependent-basis selector without materializing every candidate first.\\n/// TensorExprSource streams tensor expression ids from a generator.\\nconst TensorExprSource = struct {\\n    next_fn: *const fn (*anyopaque) ?TensorExprId,\\n    state: *anyopaque,\\n\\n    /// next returns the next generated tensor expression id.\\n    pub fn next(self: *TensorExprSource) ?TensorExprId {\\n        return self.next_fn(self.state);\\n    }\\n};\\nBasis paths and rendered terms are streamed separately.  This lets CFT code\\nattach invariant ids to rational functions without asking tensor-code to\\nexpand concrete tensor expressions during ordinary simplification.\\n/// BasisPathSource streams invariant ids from a generated basis.\\nconst BasisPathSource = struct {\\n    next_fn: *const fn (*anyopaque) ?coupling.InvariantHandle,\\n    state: *anyopaque,\\n\\n    /// next returns the next invariant in the basis.\\n    pub fn next(self: *BasisPathSource) ?coupling.InvariantHandle {\\n        return self.next_fn(self.state);\\n    }\\n};\\n\\n/// RenderedTermSource streams symbolic terms for one invariant rendering.\\nconst RenderedTermSource = struct {\\n    next_fn: *const fn (*anyopaque) ?RenderedTermId,\\n    state: *anyopaque,\\n\\n    /// next returns the next rendered symbolic term.\\n    pub fn next(self: *RenderedTermSource) ?RenderedTermId {\\n        return self.next_fn(self.state);\\n    }\\n};\\n\\n/// ReductionTermSource streams coefficients in a chosen invariant basis.\\nconst ReductionTermSource = struct {\\n    next_fn: *const fn (*anyopaque) ?ReductionTermId,\\n    state: *anyopaque,\\n\\n    /// next returns the next reduction coefficient term.\\n    pub fn next(self: *ReductionTermSource) ?ReductionTermId {\\n        return self.next_fn(self.state);\\n    }\\n};\\nAn invariant family says which algebra and external representations define the\\ncandidate space.  The actual candidates are produced by a generator.\\n/// InvariantFamily declares the tensor-invariant problem to generate.\\nconst InvariantFamily = struct {\\n    name: []const u8,\\n    algebra: AlgebraSchemaId,\\n    external_representations: []const symmetry.RepresentationId,\\n};\\nThe new invariant request keeps external leg names and realizations.  The old\\nfamily type remains for compatibility with early kernel sketches.\\n/// InvariantRequest records a named public invariant-basis request.\\nconst InvariantRequest = struct {\\n    name: []const u8,\\n    algebra: store.AlgebraHandle,\\n    external_legs: []const realization.ExternalLeg,\\n    target: coupling.TargetIrrep = .singlet,\\n    tree_policy: coupling.TreePolicy = .auto,\\n    basis_policy: coupling.BasisPolicy = .default,\\n};\\nThe algebra schema stores only compact references to core tensors such as a\\nmetric or structure constants.  This is enough for CFT coefficient code to\\nrefer to a symmetry algebra without owning its full tensor simplifier.\\n/// AlgebraSchema records the compact tensor data of a symmetry algebra.\\nconst AlgebraSchema = struct {\\n    name: []const u8,\\n    symmetry: symmetry.SymmetryId,\\n    grading: ?GradingId = null,\\n    metric: ?TensorExprId = null,\\n    structure_constants: ?TensorExprId = null,\\n};\\n","n":0.043}}},{"i":21,"$":{"1":{"v":"CFT Kernel","n":0.707},"2":{"v":"The kernel is the runtime executor for a CFT preset.  It does not define what a\\nCFT is; that belongs to Theory.  The kernel receives a theory declaration at\\ncomptime, builds a generated type from it, and exposes only the small\\nruntime operations that Correlators, OPE, and Basis-Generation need.\\nconst operators = @import(\\"expressions/operators.zig\\");\\nconst theory = @import(\\"theory/theory.zig\\");\\nconst tensor = @import(\\"tensor-code\\");\\nStreamed OutputA correlator or OPE should emit terms as soon as possible instead of\\nmaterializing a large Expression.  The stream namespace holds the small output\\nrecords used by sinks.\\n/// Stream groups coefficient and projected-local-term records emitted by kernels.\\npub const Stream = struct {\\n    /// Scalar is the numeric coefficient type used by the kernel skeleton.\\n    pub const Scalar = f64;\\n    /// CoordinateFactor names a coordinate factor in a coefficient store.\\n    pub const CoordinateFactor = u32;\\n\\n    /// CoeffTerm is one streamed scalar-coordinate-tensor coefficient term.\\n    pub const CoeffTerm = struct {\\n        scalar: Scalar,\\n        coordinate_factor: CoordinateFactor,\\n        tensor_structure: ?tensor.TensorExprId,\\n    };\\n\\n    /// LocalTerm is one streamed projected local OPE term.\\n    pub const LocalTerm = struct {\\n        coeff: CoeffTerm,\\n        insertion: operators.OperatorInsertion,\\n        body: operators.OperatorBodyId,\\n    };\\n};\\nRuntime CallsThe public OPE and correlator accept any number of local Operators through a\\nMultiOp.  At runtime a local operator is only its insertion, operator kind,\\nand a span into an immutable label store.  The typed preset builder will create\\nthese spans; the kernel only needs compact data for fast Wick and zero-mode\\npasses.\\nProjection and basis queries are call data; Theory owns the\\nquantum-number, weight, domain, and mode ids they refer to.\\n/// Call groups compact input records passed to generated kernels.\\npub const Call = struct {\\n    /// RationalLabel stores one exact rational operator label.\\n    pub const RationalLabel = struct {\\n        numerator: i64,\\n        denominator: i64,\\n    };\\n\\n    /// LabelValue stores one compact runtime operator label.\\n    pub const LabelValue = union(enum) {\\n        integer: i64,\\n        rational: RationalLabel,\\n        tensor: tensor.TensorExprId,\\n        symbol: u32,\\n    };\\n\\n    /// LabelStore is the immutable label arena carried by a MultiOp.\\n    pub const LabelStore = struct {\\n        values: []const LabelValue,\\n    };\\n\\n    /// LocalOp is one runtime local operator with labels stored out-of-line.\\n    pub const LocalOp = struct {\\n        insertion: operators.OperatorInsertion,\\n        kind: operators.OperatorKindId,\\n        labels: theory.Id.LabelSpan,\\n        /// Equal nonzero ids mark factors in the same normal-ordered product.\\n        normal_order_group: u16 = 0,\\n    };\\n\\n    /// MultiOp is an ordered collection of local operators and their label store.\\n    pub const MultiOp = struct {\\n        operators: []const LocalOp,\\n        labels: *const LabelStore,\\n    };\\n\\n    /// OpeProjection stores the target and filters for projected OPE output.\\n    pub const OpeProjection = struct {\\n        target: OpeTarget,\\n        target_weights: ?theory.Runtime.ConformalWeights,\\n        quantum_filters: []const theory.Label.QuantumFilter,\\n        max_level: ?theory.Runtime.LevelBound,\\n    };\\n\\n    /// OpeTarget tells the OPE where the projected local term is inserted.\\n    pub const OpeTarget = union(enum) {\\n        first_insertion,\\n        explicit: operators.OperatorInsertion,\\n    };\\n\\n    /// BasisQuery stores filters for streamed operator or state generation.\\n    pub const BasisQuery = struct {\\n        weights: ?theory.Runtime.ConformalWeights,\\n        quantum_filters: []const theory.Label.QuantumFilter,\\n        domain: ?theory.Runtime.OperatorDomain,\\n        support: ?theory.Runtime.ChiralSupport,\\n        max_level: ?theory.Runtime.LevelBound,\\n        finite_projections: []const theory.Id.FiniteProjection,\\n        descendants: DescendantFilter,\\n    };\\n\\n    /// DescendantFilter limits which mode descendants basis generation may use.\\n    pub const DescendantFilter = struct {\\n        mode_algebras: []const theory.Id.ModeAlgebra,\\n        max_level: ?theory.Runtime.LevelBound,\\n    };\\n\\n    /// BasisState is one streamed candidate body with computed weights.\\n    pub const BasisState = struct {\\n        body: operators.OperatorBodyId,\\n        weights: theory.Runtime.ConformalWeights,\\n    };\\n};\\nBody ConstructionThe body store is generated from a theory operator schema.  Later\\nimplementations will validate labels and canonicalize normal products here;\\nthe first version only build-checks the boundary.\\nconst Body = struct {\\n    const Schema = struct {\\n        bulk_kinds: []const operators.OperatorKind,\\n        boundary_kind_ids: []const operators.OperatorKindId = &.{},\\n        boundary_changing_kinds: []const theory.Boundary.ChangingKind = &.{},\\n    };\\n\\n    const CanonicalResult = struct {\\n        coeff: Stream.Scalar,\\n        body: ?operators.OperatorBodyId,\\n    };\\n\\n    fn Store(comptime schema: Schema) type {\\n        return struct {\\n            const operator_schema = schema;\\n\\n            /// atomic constructs an atomic operator body handle.\\n            pub fn atomic(kind: operators.OperatorKindId, labels: theory.Id.LabelSpan) !operators.OperatorBodyId {\\n                _ = labels;\\n                return @as(operators.OperatorBodyId, kind);\\n            }\\n\\n            /// normalOrdered constructs a canonical normal-product body handle.\\n            pub fn normalOrdered(factors: []const operators.OperatorBodyId) !CanonicalResult {\\n                if (factors.len == 0) {\\n                    return .{ .coeff = 1, .body = null };\\n                }\\n                return .{ .coeff = 1, .body = factors[0] };\\n            }\\n        };\\n    }\\n};\\nThe store is a generated type because its schema is preset data.  This is the\\nright use of comptime: it specializes a hot construction boundary without\\nmoving runtime operator labels or generated basis states to compile time.\\nGenerated KernelsThe generated bulk kernel owns no boundary data.  It receives a bulk\\nSpec.CFT, exposes public runtime operations, and keeps preset tables private\\nto the generated type.\\n/// CFTKernel returns the generated kernel type for a bulk CFT preset.\\npub fn CFTKernel(comptime spec: theory.Spec.CFT) type {\\n    return struct {\\n        const cft_spec = spec;\\n        const bulk_operator_kinds = spec.bulk_operator_kinds;\\n\\n        /// Bodies constructs operator bodies using this preset\'s schema.\\n        pub const Bodies = Body.Store(.{\\n            .bulk_kinds = spec.bulk_operator_kinds,\\n        });\\n\\n        /// ope computes the projected OPE of any number of local operators.\\n        pub fn ope(input: Call.MultiOp, projection: Call.OpeProjection, sink: anytype) !void {\\n            _ = input;\\n            _ = projection;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// correlator evaluates any number of local operators with a config.\\n        pub fn correlator(config: *const theory.Runtime.CorrelatorConfig, input: Call.MultiOp, sink: anytype) !void {\\n            _ = config;\\n            _ = input;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// basis streams operator or state candidates satisfying a query.\\n        pub fn basis(query: Call.BasisQuery, sink: anytype) !void {\\n            _ = query;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n    };\\n}\\nThe BCFT generated kernel extends an already generated bulk kernel.  Boundary\\ndata enters only through the Spec.BCFT supplied here.\\n/// BCFTKernel returns the generated kernel type for a BCFT extension.\\npub fn BCFTKernel(comptime Bulk: type, comptime bcft: theory.Spec.BCFT) type {\\n    return struct {\\n        const bulk = Bulk;\\n        const bcft_spec = bcft;\\n\\n        /// Bodies constructs bulk, boundary, and boundary-changing bodies.\\n        pub const Bodies = Body.Store(.{\\n            .bulk_kinds = Bulk.bulk_operator_kinds,\\n            .boundary_kind_ids = bcft.boundary_operator_kinds,\\n            .boundary_changing_kinds = bcft.boundary_changing_kinds,\\n        });\\n\\n        /// ope computes the projected OPE for this BCFT extension.\\n        pub fn ope(input: Call.MultiOp, projection: Call.OpeProjection, sink: anytype) !void {\\n            _ = input;\\n            _ = projection;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// correlator evaluates bulk and boundary local operators with a config.\\n        pub fn correlator(config: *const theory.Runtime.CorrelatorConfig, input: Call.MultiOp, sink: anytype) !void {\\n            _ = config;\\n            _ = input;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n\\n        /// basis streams bulk, boundary, or boundary-changing candidates.\\n        pub fn basis(query: Call.BasisQuery, sink: anytype) !void {\\n            _ = query;\\n            _ = sink;\\n            return error.NotImplemented;\\n        }\\n    };\\n}\\nBoundary TestThe first test protects the runtime call shape: a MultiOp carries the local\\noperators and the label store needed to interpret them, without receiving a\\nseparate context.\\ntest \\"MultiOp carries local operators and labels\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const labels = Call.LabelStore{\\n        .values = &.{ .{ .integer = 7 } },\\n    };\\n    const ops = [_]Call.LocalOp{\\n        .{\\n            .insertion = .{ .single = .{ .position = 1, .derivatives = 0 } },\\n            .kind = 2,\\n            .labels = 0,\\n        },\\n    };\\n    const input = Call.MultiOp{\\n        .operators = &ops,\\n        .labels = &labels,\\n    };\\n\\n    try testing.expectEqual(@as(usize, 1), input.operators.len);\\n    try testing.expectEqual(@as(operators.OperatorKindId, 2), input.operators[0].kind);\\n    switch (input.labels.values[0]) {\\n        .integer => |value| try testing.expectEqual(@as(i64, 7), value),\\n        else => return error.UnexpectedLabelKind,\\n    }\\n}\\nThe second test checks the kernel/theory boundary that matters now: a BCFT\\ngenerated kernel extends the body schema without mutating the bulk generated\\nkernel.\\ntest \\"BCFT kernel extends bulk schema without mutating bulk schema\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const bulk_spec = theory.Spec.CFT{\\n        .sectors = &.{},\\n        .bulk_operator_kinds = &.{},\\n        .conformal_structure = 0,\\n        .conformal_structures = &.{},\\n        .quantum_names = &.{},\\n        .label_schemas = &.{},\\n        .conventions = &.{},\\n        .weight_rules = &.{},\\n        .quantum_rules = &.{},\\n        .statistics_rules = &.{},\\n        .strategy_support_rules = &.{},\\n        .wick_rule_sets = &.{},\\n        .pairing_data = &.{},\\n        .zero_mode_rule_sets = &.{},\\n        .correlator_configs = &.{},\\n        .local_ope_configs = &.{},\\n        .cocycle_tables = &.{},\\n        .presentations = &.{},\\n        .finite_projections = &.{},\\n        .lattices = &.{},\\n        .mode_algebras = &.{},\\n        .mode_action_rules = &.{},\\n    };\\n\\n    const bcft_spec = theory.Spec.BCFT{\\n        .name = \\"empty boundary extension\\",\\n        .bulk = 0,\\n        .boundary_conditions = &.{},\\n        .boundary_stacks = &.{},\\n        .boundary_stress_tensor = .{ .kind = 0, .labels = 0 },\\n        .boundary_spin = &.{},\\n        .boundary_operator_kinds = &.{ 1, 2 },\\n        .boundary_changing_kinds = &.{},\\n        .wick_rule_sets = &.{},\\n        .pairing_data = &.{},\\n        .zero_mode_rule_sets = &.{},\\n        .correlator_configs = &.{},\\n        .local_ope_configs = &.{},\\n    };\\n\\n    const Bulk = CFTKernel(bulk_spec);\\n    const BoundaryKernel = BCFTKernel(Bulk, bcft_spec);\\n\\n    try testing.expectEqual(@as(usize, 0), Bulk.Bodies.operator_schema.boundary_kind_ids.len);\\n    try testing.expectEqual(@as(usize, 2), BoundaryKernel.Bodies.operator_schema.boundary_kind_ids.len);\\n    try testing.expectEqual(@as(operators.OperatorKindId, 1), BoundaryKernel.Bodies.operator_schema.boundary_kind_ids[0]);\\n}\\n","n":0.028}}},{"i":22,"$":{"1":{"v":"String-Code","n":1},"2":{"v":"The root module exposes the two build-checked kernels.  Higher string modules\\ndepend on these through their public APIs.\\n/// tensor exposes tensor-code data structures and tensor handles.\\npub const tensor = @import(\\"tensor-code\\");\\n/// cft exposes the selected worldsheet CFT public API.\\npub const cft = @import(\\"cft-code\\");\\n\\ntest \\"top-level API exposes tensor and cft boundaries\\" {\\n    const testing = @import(\\"std\\").testing;\\n    try testing.expect(@hasDecl(@This(), \\"tensor\\"));\\n    try testing.expect(@hasDecl(@This(), \\"cft\\"));\\n    try testing.expect(@hasDecl(cft, \\"FreeBoson\\"));\\n    try testing.expect(@hasDecl(cft, \\"Bc\\"));\\n    try testing.expect(@hasDecl(cft, \\"FreeFermion\\"));\\n    try testing.expect(@hasDecl(cft, \\"EtaXi\\"));\\n    try testing.expect(@hasDecl(cft, \\"product\\"));\\n    try testing.expect(@hasDecl(cft, \\"boundary\\"));\\n    try testing.expect(@hasDecl(cft.FreeBoson, \\"make\\"));\\n    try testing.expect(@hasDecl(cft.FreeBoson, \\"boundary\\"));\\n    try testing.expect(@hasDecl(cft.Bc, \\"sphere\\"));\\n    try testing.expect(@hasDecl(cft.Bc, \\"diskBoundary\\"));\\n    try testing.expect(@hasDecl(cft.FreeFermion, \\"sphere\\"));\\n    try testing.expect(@hasDecl(cft.EtaXi, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(cft.FreeBoson, \\"Config\\"));\\n    try testing.expect(!@hasDecl(cft.FreeBoson, \\"BoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(cft.Bc, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft.Bc, \\"DiskConfig\\"));\\n    try testing.expect(!@hasDecl(cft.FreeFermion, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft.EtaXi, \\"SphereConfig\\"));\\n    try testing.expect(!@hasDecl(cft, \\"Handle\\"));\\n    try testing.expect(!@hasDecl(cft, \\"scalars\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeBoson\\"));\\n    try testing.expect(!@hasDecl(cft, \\"bcSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeFermionSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"etaXiSphere\\"));\\n    try testing.expect(!@hasDecl(cft, \\"freeBosonBoundary\\"));\\n    try testing.expect(!@hasDecl(cft, \\"bcDiskBoundary\\"));\\n    try testing.expect(!@hasDecl(cft, \\"presets\\"));\\n    try testing.expect(!@hasDecl(cft, \\"kernel\\"));\\n    try testing.expect(!@hasDecl(cft, \\"theory\\"));\\n    try testing.expect(!@hasDecl(cft, \\"expressions\\"));\\n}\\n\\ntest \\"tensor context interns projected realization requests without expansion\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    var ctx = try tensor.Context.init(testing.allocator);\\n    defer ctx.deinit();\\n\\n    const so10 = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\n    const so10_again = try ctx.registerAlgebra(.{ .simple = .{ .family = .d, .rank = 5 } });\\n    try testing.expectEqual(so10.value, so10_again.value);\\n\\n    const vector = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 0 } });\\n    const spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 0, 0, 0, 0, 1 } });\\n    const vector_spinor = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 1 } });\\n    const vector_spinor_again = try ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0, 0, 0, 1 } });\\n    try testing.expectEqual(vector_spinor.value, vector_spinor_again.value);\\n    try testing.expectEqual(@as(u128, 10), ctx.irrepMetadata(vector).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 16), ctx.irrepMetadata(spinor).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 144), ctx.irrepMetadata(vector_spinor).?.dimension.?);\\n    const conjugate_spinor = try ctx.dualIrrep(spinor);\\n    try testing.expect(conjugate_spinor.value != spinor.value);\\n    try testing.expectEqual(spinor.value, (try ctx.dualIrrep(conjugate_spinor)).value);\\n    try testing.expectError(error.InvalidDynkinRank, ctx.registerIrrep(so10, .{ .dynkin = &.{ 1, 0 } }));\\n\\n    const su2 = try ctx.registerAlgebra(.{ .simple = .{ .family = .a, .rank = 1 } });\\n    const fundamental = try ctx.registerIrrep(su2, .{ .dynkin = &.{1} });\\n    try testing.expectError(error.MixedRealizationAlgebras, ctx.registerRealization(tensor.RealizationSpec.projected(vector_spinor, &.{fundamental}, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }, 0)));\\n\\n    const channel = tensor.RealizationChannel.first(vector_spinor);\\n    const realization = try ctx.registerRealization(tensor.RealizationSpec.projectedChannel(channel, &.{ vector, spinor }, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }).named(\\"SO10.vector_spinor\\"));\\n    const realization_again = try ctx.registerRealization(tensor.RealizationSpec.projectedChannel(channel, &.{ vector, spinor }, &.{\\n        .init(.vector, \\"m\\"),\\n        .init(.spinor, \\"a\\"),\\n    }).named(\\"SO10.vector_spinor\\"));\\n    try testing.expectEqual(realization.value, realization_again.value);\\n\\n    const leg = tensor.ExternalLeg.realized(realization, &.{\\n        .init(.vector, \\"m1\\"),\\n        .init(.spinor, \\"a1\\"),\\n    });\\n    const basis = try ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{ leg, leg, leg, leg, leg, leg },\\n    });\\n    const basis_again = try ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{ leg, leg, leg, leg, leg, leg },\\n    });\\n    try testing.expectEqual(basis.value, basis_again.value);\\n\\n    try testing.expectError(error.MixedInvariantAlgebras, ctx.invariantBasis(.{\\n        .algebra = so10,\\n        .external_legs = &.{tensor.ExternalLeg.primitive(fundamental, &.{\\n            .init(.fundamental, \\"i\\"),\\n        })},\\n    }));\\n\\n    const so9 = try ctx.registerAlgebra(.{ .simple = .{ .family = .b, .rank = 4 } });\\n    const so9_vector = try ctx.registerIrrep(so9, .{ .dynkin = &.{ 1, 0, 0, 0 } });\\n    const so9_spinor = try ctx.registerIrrep(so9, .{ .dynkin = &.{ 0, 0, 0, 1 } });\\n    try testing.expectEqual(@as(u128, 9), ctx.irrepMetadata(so9_vector).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 16), ctx.irrepMetadata(so9_spinor).?.dimension.?);\\n\\n    const e6 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e6, .rank = 6 } });\\n    const e7 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e7, .rank = 7 } });\\n    const e8 = try ctx.registerAlgebra(.{ .simple = .{ .family = .e8, .rank = 8 } });\\n    const e6_27 = try ctx.registerIrrep(e6, .{ .dynkin = &.{ 1, 0, 0, 0, 0, 0 } });\\n    const e7_56 = try ctx.registerIrrep(e7, .{ .dynkin = &.{ 0, 0, 0, 0, 0, 1, 0 } });\\n    const e8_248 = try ctx.registerIrrep(e8, .{ .dynkin = &.{ 0, 0, 0, 0, 0, 0, 1, 0 } });\\n    try testing.expectEqual(@as(u128, 27), ctx.irrepMetadata(e6_27).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 56), ctx.irrepMetadata(e7_56).?.dimension.?);\\n    try testing.expectEqual(@as(u128, 248), ctx.irrepMetadata(e8_248).?.dimension.?);\\n}\\n","n":0.04}}},{"i":23,"$":{"1":{"v":"Presets","n":1},"2":{"v":"Presets are the physics-facing constructors for common worldsheet CFT data.\\nThey sit between Theory and\\nCFT Kernel.  A user asks for a free boson, a \\\\(bc\\\\)\\nsystem, a product theory, or a boundary extension, and receives a generated\\ntype with a small operator namespace and named correlator configs.\\nThe implementation is split by audit target:\\nPreset Shared Runtime defines compact handles,\\n  local-operator input, Wick templates, zero-mode templates, and the streaming\\n  correlator boundary.\\nFree Boson Preset defines the free-boson bulk and\\n  Neumann/Dirichlet disk data.\\nbc Preset defines the \\\\(bc\\\\) sphere and disk data.\\nFree Fermion Preset defines the NS free fermion\\n  on the sphere.\\neta-xi Preset defines the \\\\(\\\\eta,\\\\xi\\\\) system\\n  on the sphere and records its torus draft metadata.\\nPreset Composition joins bulk sectors and boundary\\n  extensions without introducing a new runtime operator representation.\\nLisp Preset Compiler and the Lisp preset nodes\\n  declare descriptor fixtures that are translated to the generated Zig ABI\\n  module for REPL use.\\nThis node is an internal preset bridge used by CFT-Code.\\nIt is not the user API.  It re-exports only the declarations the root CFT API\\nneeds to build compact family objects such as FreeBoson and Bc.  Raw\\nWick-rule data, correlator config records, and runtime resolver types stay in\\nPreset Shared Runtime for implementation modules.\\nconst shared = @import(\\"presets/shared.zig\\");\\nconst declare = shared.declare;\\nconst free_boson = @import(\\"presets/free_boson.zig\\");\\nconst bc = @import(\\"presets/bc.zig\\");\\nconst free_fermion = @import(\\"presets/free_fermion.zig\\");\\nconst eta_xi = @import(\\"presets/eta_xi.zig\\");\\nconst composition = @import(\\"presets/composition.zig\\");\\n\\nconst freeBoson = free_boson.freeBoson;\\nconst bcSphere = bc.bcSphere;\\nconst freeFermionSphere = free_fermion.freeFermionSphere;\\nconst etaXiSphere = eta_xi.etaXiSphere;\\nconst freeBosonBoundary = free_boson.boundaryExtension;\\nconst bcDiskBoundary = bc.boundaryExtension;\\nShared BridgeScalar convention helpers are available only to preset implementation modules.\\nThe root CFT API intentionally does not re-export them.\\nconst scalars = declare.scalars;\\nSmall private sink adapters keep tests focused on the event they inspect.\\nfn noopWickTermStart(_: anytype, _: anytype) !void {}\\nfn noopWickScalar(_: anytype, _: anytype) !void {}\\nfn noopWickCoordinate(_: anytype, _: anytype) !void {}\\nfn noopWickTensor(_: anytype, _: anytype) !void {}\\nfn noopWickAction(_: anytype, _: anytype) !void {}\\nfn noopWickTermEnd(_: anytype) !void {}\\nfn noopZeroModeFactor(_: anytype, _: anytype) !void {}\\nfn noopZeroModeBaseEnd(_: anytype) !void {}\\nPreset FamiliesThe constructor bridge is grouped by CFT family.  The root public API can\\nre-export these family objects without proliferating flat constructor names.\\n/// FreeBoson groups the free-boson preset constructors.\\npub const FreeBoson = struct {\\n    /// make builds the generated preset type for a noncompact free-boson CFT.\\n    pub const make = free_boson.freeBoson;\\n    /// boundary declares the Neumann/Dirichlet free-boson boundary extension.\\n    pub const boundary = free_boson.boundaryExtension;\\n};\\n\\n/// Bc groups the bc-ghost preset constructors.\\npub const Bc = struct {\\n    /// sphere builds the generated preset type for the sphere bc ghost CFT.\\n    pub const sphere = bc.bcSphere;\\n    /// diskBoundary declares boundary and mixed bulk-boundary bc rules on the disk.\\n    pub const diskBoundary = bc.boundaryExtension;\\n};\\n\\n/// FreeFermion groups the NS free-fermion preset constructors.\\npub const FreeFermion = struct {\\n    /// sphere builds the generated preset type for sphere NS free fermions.\\n    pub const sphere = free_fermion.freeFermionSphere;\\n};\\n\\n/// EtaXi groups the eta-xi preset constructors.\\npub const EtaXi = struct {\\n    /// sphere builds the generated preset type for the sphere eta-xi system.\\n    pub const sphere = eta_xi.etaXiSphere;\\n};\\n\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub const product = composition.product;\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub const boundary = composition.boundary;\\nInterface ChecksThe tests here are intentionally behavioral.  They check concepts that should\\nsurvive later implementation work: composition exposes only selected\\nnamespaces, rule slices compose from the supplied factors, labels remain\\nout-of-line, and the current correlator boundary streams primitive Wick terms\\nrather than allocating a final expression.\\ntest \\"presets compose operator builders without exposing theory tables\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 26 });\\n    const BosonicSphere = product(.{ X, bcSphere(.{}) });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n        .chan_paton = X.target.boundaryStack(\\"stack\\"),\\n    });\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const BosonicDisk = boundary(BosonicSphere, .{ XBoundary, GhostBoundary });\\n\\n    var local = try BosonicDisk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const x = try @TypeOf(XBoundary).op.expXBoundary(&local, k, y);\\n    const c = try @TypeOf(GhostBoundary).op.cBoundary(&local, 0, y);\\n    const ops = try local.ops(.{ x, c });\\n\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"pair_lookup\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"zero_modes\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BosonicDisk.config.disk), \\"config_entries\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(X.config.sphere), \\"wick_rules\\"));\\n    const LocalType = @typeInfo(@TypeOf(local)).pointer.child;\\n    try testing.expect(@typeInfo(LocalType) == .@\\"opaque\\");\\n    try testing.expect(!@hasDecl(LocalType, \\"symbol\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"op\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"add\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"normalOrdered\\"));\\n    try testing.expect(!@hasDecl(LocalType, \\"finish\\"));\\n    const OpsType = @typeInfo(@TypeOf(ops)).pointer.child;\\n    try testing.expect(@typeInfo(OpsType) == .@\\"opaque\\");\\n    try testing.expect(!@hasDecl(OpsType, \\"operators\\"));\\n    try testing.expect(!@hasDecl(OpsType, \\"labels\\"));\\n}\\n\\ntest \\"preset composition plumbing stays inside shared lowering\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    try testing.expect(!@hasDecl(shared, \\"ConfigEntry\\"));\\n    try testing.expect(!@hasDecl(shared, \\"correlatorSliceCount\\"));\\n    try testing.expect(!@hasDecl(shared, \\"appendCorrelatorSlice\\"));\\n    try testing.expect(!@hasDecl(shared, \\"boundaryExtensionSliceCount\\"));\\n    try testing.expect(!@hasDecl(shared, \\"appendBoundaryExtensionSlice\\"));\\n    try testing.expect(!@hasDecl(shared, \\"mergeSphereConfigs\\"));\\n    try testing.expect(!@hasDecl(shared, \\"mergeBoundaryConfig\\"));\\n    try testing.expect(@hasDecl(shared, \\"declare\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"mergeSphereConfigs\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"mergeBoundaryConfig\\"));\\n    try testing.expect(!@hasDecl(shared, \\"Spec\\"));\\n    try testing.expect(!@hasDecl(shared, \\"wick\\"));\\n    try testing.expect(!@hasDecl(shared, \\"zero_mode\\"));\\n    try testing.expect(!@hasDecl(shared, \\"scalars\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"Spec\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"wick\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"zero_mode\\"));\\n    try testing.expect(@hasDecl(shared.declare, \\"scalars\\"));\\n}\\n\\ntest \\"preset rules and namespaces compose from supplied factors\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const Ghost = bcSphere(.{});\\n    const MatterOnly = product(.{X});\\n    const GhostOnly = product(.{Ghost});\\n    const MatterGhost = product(.{ X, Ghost });\\n\\n    try testing.expect(@hasDecl(MatterOnly.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(GhostOnly.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(MatterGhost.config, \\"sphere\\"));\\n    try testing.expect(!@hasDecl(MatterGhost.op, \\"free_boson\\"));\\n    try testing.expect(!@hasDecl(MatterGhost.op, \\"bc\\"));\\n\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const DiskMatter = boundary(MatterGhost, .{XBoundary});\\n    const DiskMatterGhost = boundary(MatterGhost, .{ XBoundary, GhostBoundary });\\n\\n    try testing.expect(@hasDecl(DiskMatter.op, \\"bulk\\"));\\n    try testing.expect(@hasDecl(DiskMatterGhost.op, \\"bulk\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(DiskMatter.config.disk), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(DiskMatterGhost.config.disk), \\"wick_rules\\"));\\n}\\n\\ntest \\"local operator tokens are owned by their builder\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var first = try X.local(testing.allocator);\\n    defer first.deinit();\\n    var second = try X.local(testing.allocator);\\n    defer second.deinit();\\n\\n    const mu = try first.index(\\"mu\\");\\n    const z = try first.coord(\\"z\\");\\n    const dx = try X.op.dX(&first, mu, 0, z);\\n\\n    try testing.expectError(error.InvalidLocalOperator, second.ops(.{dx}));\\n}\\n\\ntest \\"preset compile-time flags change generated public behavior\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const HolomorphicBc = bcSphere(.{ .include_antiholomorphic_copy = false });\\n    const MatterGhost = product(.{ X, HolomorphicBc });\\n    const BoundaryOnlyGhost = boundary(HolomorphicBc, .{\\n        bcDiskBoundary(.{ .include_mixed_bulk_boundary = false }),\\n    });\\n\\n    try testing.expect(@hasDecl(HolomorphicBc.op, \\"b\\"));\\n    try testing.expect(!@hasDecl(HolomorphicBc.op, \\"bt\\"));\\n    try testing.expect(@hasDecl(MatterGhost.config, \\"sphere\\"));\\n    try testing.expect(@hasDecl(BoundaryOnlyGhost.op, \\"bulk\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(BoundaryOnlyGhost.config.disk), \\"wick_rules\\"));\\n\\n    const FullGhostDisk = boundary(HolomorphicBc, .{bcDiskBoundary(.{})});\\n\\n    var local = try FullGhostDisk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try HolomorphicBc.op.b(&local, 0, z),\\n        try @TypeOf(bcDiskBoundary(.{})).op.cBoundary(&local, 0, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n        pub const emitZeroModeFactor = noopZeroModeFactor;\\n        pub const emitZeroModeBaseEnd = noopZeroModeBaseEnd;\\n\\n        wick_terms: usize = 0,\\n\\n        /// emitWickTermStart counts one streamed primitive contraction.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n    };\\n\\n    var full_sink = Sink{};\\n    try FullGhostDisk.correlator(&FullGhostDisk.config.disk, ops, &full_sink);\\n    try testing.expectEqual(@as(usize, 1), full_sink.wick_terms);\\n\\n    var boundary_only_sink = Sink{};\\n    try BoundaryOnlyGhost.correlator(&BoundaryOnlyGhost.config.disk, ops, &boundary_only_sink);\\n    try testing.expectEqual(@as(usize, 0), boundary_only_sink.wick_terms);\\n}\\n\\ntest \\"free fermion sphere uses generic fermionic Wick signs\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Psi = freeFermionSphere(.{ .dimension = 10 });\\n\\n    var local = try Psi.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const rho = try local.index(\\"rho\\");\\n    const sigma = try local.index(\\"sigma\\");\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const ops = try local.ops(.{\\n        try Psi.op.psi(&local, mu, 0, z1),\\n        try Psi.op.psi(&local, nu, 0, z2),\\n        try Psi.op.psi(&local, rho, 0, z3),\\n        try Psi.op.psi(&local, sigma, 0, z4),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n        pub const emitZeroModeFactor = noopZeroModeFactor;\\n        pub const emitZeroModeBaseEnd = noopZeroModeBaseEnd;\\n\\n        branch_count: usize = 0,\\n        primitive_count: usize = 0,\\n        negative_branch_count: usize = 0,\\n        pending_negative: bool = false,\\n\\n        /// emitWickBranchSign records signs supplied by the generic branch walker.\\n        pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n            if (sign < 0) self.pending_negative = true;\\n        }\\n\\n        /// emitWickTermStart records the first primitive term of each Wick branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.primitive_count += 1;\\n            if ((self.primitive_count % 2) == 1) {\\n                self.branch_count += 1;\\n                if (self.pending_negative) {\\n                    self.negative_branch_count += 1;\\n                    self.pending_negative = false;\\n                }\\n            }\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Psi.correlator(&Psi.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 3), sink.branch_count);\\n    try testing.expectEqual(@as(usize, 6), sink.primitive_count);\\n    try testing.expectEqual(@as(usize, 1), sink.negative_branch_count);\\n    try testing.expect(!@hasDecl(@TypeOf(Psi.config.sphere), \\"fermion_kinds\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(Psi.config.sphere), \\"pair_lookup\\"));\\n}\\n\\ntest \\"eta-xi sphere saturates one xi zero mode\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const EtaXiPreset = etaXiSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try EtaXiPreset.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const xi = try EtaXiPreset.op.xi(&local, 0, z);\\n\\n    const SaturatedSink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        zero_mode_count: usize = 0,\\n        base_count: usize = 0,\\n        coordinate: ?shared.Handle.Coord = null,\\n\\n        /// emitZeroModeFactor records the single xi constant-mode factor.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .eta_xi_zero_mode => |zero| {\\n                    try testing.expectEqual(.sphere_holomorphic, zero.support);\\n                    try testing.expectEqual(@as(u8, 0), zero.xi.derivative_order);\\n                    self.zero_mode_count += 1;\\n                    self.coordinate = @as(shared.Handle.Coord, @enumFromInt(zero.xi.coordinate));\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records a completed zero-mode base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_count += 1;\\n        }\\n    };\\n\\n    var saturated_sink = SaturatedSink{};\\n    const saturated_ops = try local.ops(.{xi});\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.sphere, saturated_ops, &saturated_sink);\\n    try testing.expectEqual(@as(usize, 1), saturated_sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 1), saturated_sink.base_count);\\n    try testing.expectEqual(z, saturated_sink.coordinate.?);\\n\\n    var derivative_local = try EtaXiPreset.local(testing.allocator);\\n    defer derivative_local.deinit();\\n    const zd = try derivative_local.coord(\\"z\\");\\n    const xi_derivative = try EtaXiPreset.op.xi(&derivative_local, 1, zd);\\n    var rejected_sink = SaturatedSink{};\\n    const rejected_ops = try derivative_local.ops(.{xi_derivative});\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.sphere, rejected_ops, &rejected_sink);\\n    try testing.expectEqual(@as(usize, 0), rejected_sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 0), rejected_sink.base_count);\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.sphere), \\"zero_modes\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.sphere), \\"fermion_kinds\\"));\\n}\\n\\ntest \\"eta-xi torus lowers prime-form log-derivative Wick and xi zero mode\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const EtaXiPreset = etaXiSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try EtaXiPreset.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z_eta = try local.coord(\\"z_eta\\");\\n    const z_xi_a = try local.coord(\\"z_xi_a\\");\\n    const z_xi_b = try local.coord(\\"z_xi_b\\");\\n    const ops = try local.ops(.{\\n        try EtaXiPreset.op.eta(&local, 0, z_eta),\\n        try EtaXiPreset.op.xi(&local, 0, z_xi_a),\\n        try EtaXiPreset.op.xi(&local, 0, z_xi_b),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        term_count: usize = 0,\\n        kernel_count: usize = 0,\\n        zero_mode_count: usize = 0,\\n        base_count: usize = 0,\\n\\n        /// emitWickTermStart records one eta-xi torus primitive contraction.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickCoordinate requires the elliptic prime-form log derivative.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            switch (factor.kernel) {\\n                .green_kernel => |kernel| {\\n                    try testing.expectEqualStrings(\\"elliptic_prime_form_log_derivative\\", kernel.name);\\n                    try testing.expectEqual(@as(u8, 0), kernel.left_derivatives);\\n                    try testing.expectEqual(@as(u8, 0), kernel.right_derivatives);\\n                    self.kernel_count += 1;\\n                },\\n                else => return error.UnexpectedTorusKernel,\\n            }\\n        }\\n\\n        /// emitZeroModeFactor requires the torus holomorphic xi zero mode.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .eta_xi_zero_mode => |zero| {\\n                    try testing.expectEqual(.torus_holomorphic, zero.support);\\n                    try testing.expectEqual(@as(u8, 0), zero.xi.derivative_order);\\n                    self.zero_mode_count += 1;\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records one successful torus base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try EtaXiPreset.correlator(&EtaXiPreset.config.torus, ops, &sink);\\n    try testing.expectEqual(@as(usize, 2), sink.term_count);\\n    try testing.expectEqual(@as(usize, 2), sink.kernel_count);\\n    try testing.expectEqual(@as(usize, 2), sink.zero_mode_count);\\n    try testing.expectEqual(@as(usize, 2), sink.base_count);\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.torus), \\"wick_rules\\"));\\n    try testing.expect(!@hasDecl(@TypeOf(EtaXiPreset.config.torus), \\"zero_modes\\"));\\n}\\n\\ntest \\"compact text sink inspects a small correlator without custom callbacks\\" {\\n    const testing = @import(\\"std\\").testing;\\n    const Buffer = struct {\\n        bytes: [512]u8 = undefined,\\n        len: usize = 0,\\n\\n        pub fn writeAll(self: *@This(), data: []const u8) !void {\\n            if (self.len + data.len > self.bytes.len) return error.NoSpaceLeft;\\n            @memcpy(self.bytes[self.len..][0..data.len], data);\\n            self.len += data.len;\\n        }\\n\\n        fn slice(self: *const @This()) []const u8 {\\n            return self.bytes[0..self.len];\\n        }\\n    };\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n        try X.op.dX(&local, nu, 0, w),\\n    });\\n\\n    var buffer = Buffer{};\\n    var text = X.text.compact(&buffer, .small);\\n    try X.correlator(&X.config.sphere, ops, &text);\\n    try testing.expect(@import(\\"std\\").mem.indexOf(u8, buffer.slice(), \\"eta(\\") != null);\\n}\\n\\ntest \\"bc zero modes consume c jets by top-form saturation\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.c(&local, 0, z1),\\n        try Ghost.op.c(&local, 1, z2),\\n        try Ghost.op.c(&local, 2, z3),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        factor_count: usize = 0,\\n        base_end_count: usize = 0,\\n        derivative_sum: u16 = 0,\\n\\n        /// emitZeroModeFactor records the top-form factor emitted by the base case.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            self.factor_count += 1;\\n            switch (factor) {\\n                .bc_top_form => |top| {\\n                    if (top.support != .sphere_holomorphic) return error.UnexpectedBcSupport;\\n                    self.derivative_sum = @as(u16, top.jets[0].derivative_order) + top.jets[1].derivative_order + top.jets[2].derivative_order;\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that every residual insertion was consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.factor_count);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n    try testing.expectEqual(@as(u16, 3), sink.derivative_sum);\\n}\\n\\ntest \\"disk bc zero modes use one doubled chiral top form\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{});\\n    const GhostBoundary = bcDiskBoundary(.{});\\n    const Disk = boundary(Ghost, .{GhostBoundary});\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try Disk.op.bulk.c(&local, 0, z),\\n        try Disk.op.bulk.ct(&local, 0, zbar),\\n        try @TypeOf(GhostBoundary).op.cBoundary(&local, 0, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        saw_disk_doubled: bool = false,\\n\\n        /// emitZeroModeFactor records whether the doubled disk top form was emitted.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => |top| self.saw_disk_doubled = top.support == .disk_doubled,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd accepts the successful zero-mode base case.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expect(sink.saw_disk_doubled);\\n}\\n\\ntest \\"free boson zero modes emit momentum delta and profile presentations\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const f0 = try local.targetFunction(\\"f\\");\\n    const fhat = try local.fourierTransform(\\"fhat\\");\\n    const coeff = try local.profileCoefficient(\\"a\\");\\n    const point = try local.targetPoint(\\"x0\\");\\n    const position_profile = X.profile.positionSpace(f0);\\n    const fourier_profile = X.profile.fourier(fhat);\\n    const polynomial_profile = X.profile.polynomialRnc(point, .{.{ coeff, 2 }});\\n    const ops = try local.ops(.{\\n        try X.op.expX(&local, k, z, zbar),\\n        try X.op.profile(&local, position_profile, z, zbar),\\n        try X.op.profile(&local, fourier_profile, z, zbar),\\n        try X.op.profile(&local, polynomial_profile, z, zbar),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        delta_count: usize = 0,\\n        gaussian_count: usize = 0,\\n        fourier_count: usize = 0,\\n        polynomial_count: usize = 0,\\n        base_end_count: usize = 0,\\n        two_pi_power: u16 = 0,\\n\\n        /// emitZeroModeFactor records each free-boson zero-mode factor by presentation.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => |delta| {\\n                    self.delta_count += 1;\\n                    self.two_pi_power = delta.two_pi_power;\\n                    if (delta.projector != null or delta.momenta.len != 1) return error.UnexpectedMomentumDelta;\\n                },\\n                .profile_gaussian_differential => self.gaussian_count += 1,\\n                .profile_fourier_integral => self.fourier_count += 1,\\n                .profile_polynomial_integral => self.polynomial_count += 1,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that all profile and plane-wave residuals were consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.delta_count);\\n    try testing.expectEqual(@as(u16, 10), sink.two_pi_power);\\n    try testing.expectEqual(@as(usize, 1), sink.gaussian_count);\\n    try testing.expectEqual(@as(usize, 1), sink.fourier_count);\\n    try testing.expectEqual(@as(usize, 1), sink.polynomial_count);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n}\\n\\ntest \\"disk free boson zero modes emit Neumann delta and Dirichlet phase\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const Disk = boundary(product(.{X}), .{XBoundary});\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const ops = try local.ops(.{\\n        try @TypeOf(XBoundary).op.expXBoundary(&local, k, y),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        delta_count: usize = 0,\\n        phase_count: usize = 0,\\n        base_end_count: usize = 0,\\n        two_pi_power: u16 = 0,\\n        saw_disk_scalar: bool = false,\\n        saw_projector: bool = false,\\n        saw_position: bool = false,\\n\\n        /// emitZeroModeFactor records Neumann momentum conservation and Dirichlet phase data.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => |delta| {\\n                    self.delta_count += 1;\\n                    self.two_pi_power = delta.two_pi_power;\\n                    self.saw_projector = delta.projector != null;\\n                    switch (delta.scalar) {\\n                        .monomial => |monomial| self.saw_disk_scalar = monomial.atom != null and monomial.atom_power == 1,\\n                        else => return error.UnexpectedDiskNormalization,\\n                    }\\n                    if (delta.momenta.len != 1) return error.UnexpectedMomentumDelta;\\n                },\\n                .dirichlet_phase => |phase| {\\n                    self.phase_count += 1;\\n                    self.saw_position = phase.momenta.len == 1;\\n                    switch (phase.position) {\\n                        .target_point => {},\\n                        else => return error.UnexpectedDirichletPosition,\\n                    }\\n                },\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd records that the disk constant mode consumed the insertion.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.delta_count);\\n    try testing.expectEqual(@as(usize, 1), sink.phase_count);\\n    try testing.expectEqual(@as(u16, 4), sink.two_pi_power);\\n    try testing.expect(sink.saw_disk_scalar);\\n    try testing.expect(sink.saw_projector);\\n    try testing.expect(sink.saw_position);\\n    try testing.expectEqual(@as(usize, 1), sink.base_end_count);\\n}\\n\\ntest \\"unconsumed free boson derivative kills the zero-mode base case\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const z = try local.coord(\\"z\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickTermStart = noopWickTermStart;\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickAction = noopWickAction;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        base_end_count: usize = 0,\\n\\n        /// emitZeroModeFactor fails if an unconsumed derivative field emits a zero-mode factor.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedZeroModeFactor;\\n        }\\n\\n        /// emitZeroModeBaseEnd fails because the derivative residual should not be consumed.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_end_count += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 0), sink.base_end_count);\\n}\\n\\ntest \\"pair Wick streams preset scalar atoms through correlator\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const preset_alpha = scalars.atom(\\"free_boson\\", \\"alpha_prime\\");\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z),\\n        try X.op.dX(&local, nu, 0, w),\\n    });\\n\\n    switch (scalars.rational(2, 4)) {\\n        .rational => |value| {\\n            try testing.expectEqual(@as(i64, 1), value.numerator);\\n            try testing.expectEqual(@as(i64, 2), value.denominator);\\n        },\\n        else => return error.UnexpectedScalarShape,\\n    }\\n\\n    const Sink = struct {\\n        expected_alpha: @TypeOf(preset_alpha),\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n\\n        /// emitWickTermStart accepts the primitive free-boson contraction.\\n        pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickScalar checks the configured alpha-prime atom.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 2) return error.UnexpectedScalarFactor;\\n                        if (monomial.imaginary_power != 0 or monomial.atom_power != 1) return error.UnexpectedScalarFactor;\\n                        try testing.expectEqual(self.expected_alpha, monomial.atom.?);\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarShape,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the differentiated two-point pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.exponent != -2) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor accepts the contracted target-space tensor.\\n        pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitWickTermEnd accepts the completed primitive contraction.\\n        pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n        /// emitZeroModeFactor accepts the empty free-boson constant mode.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the full contraction base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{ .expected_alpha = preset_alpha };\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n}\\n\\ntest \\"pair Wick resolves derivative action on a vector profile\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const z = try local.coord(\\"z\\");\\n    const w = try local.coord(\\"w\\");\\n    const wbar = try local.coord(\\"wbar\\");\\n    const f0 = try local.targetFunction(\\"f\\");\\n    const f = X.profile.positionSpace(f0);\\n\\n    const dx = try X.op.dX(&local, mu, 0, z);\\n    const profile = try X.op.profileVector(&local, f, nu, w, wbar);\\n    const ops = try local.ops(.{ dx, profile });\\n\\n    const Sink = struct {\\n        term_count: usize = 0,\\n        scalar_count: usize = 0,\\n        coordinate_count: usize = 0,\\n        tensor_count: usize = 0,\\n        action_count: usize = 0,\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n        saw_tensor_none: bool = false,\\n        saw_action: bool = false,\\n        expected_z: u32,\\n        expected_w: u32,\\n        expected_profile: u32,\\n        expected_index: u32,\\n\\n        /// emitWickTermStart records the single primitive profile Wick term.\\n        pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n            if (event.left_index != 0 or event.right_index != 1 or event.term_index != 0) return error.UnexpectedWickTermEvent;\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar checks the expected free-boson profile scalar.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            self.scalar_count += 1;\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 2 or monomial.atom == null) return error.UnexpectedScalarFactor;\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarFactor,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the resolved profile pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            self.coordinate_count += 1;\\n            if (factor.scalar.numerator != 1 or factor.scalar.denominator != 1) return error.UnexpectedCoordinateScalar;\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.coordinate.left != self.expected_z or power.coordinate.right != self.expected_w or power.exponent != -1) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor checks that no tensor numerator is emitted.\\n        pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n            self.tensor_count += 1;\\n            switch (factor) {\\n                .none => self.saw_tensor_none = true,\\n                else => return error.UnexpectedTensorFactor,\\n            }\\n        }\\n\\n        /// emitWickAction checks the profile-derivative action.\\n        pub fn emitWickAction(self: *@This(), factor: anytype) !void {\\n            self.action_count += 1;\\n            switch (factor) {\\n                .profile_derivative => |action| {\\n                    switch (action.profile) {\\n                        .symbol => |value| if (value != self.expected_profile) return error.UnexpectedProfileLabel,\\n                        else => return error.UnexpectedProfileLabel,\\n                    }\\n                    switch (action.index) {\\n                        .symbol => |value| if (value != self.expected_index) return error.UnexpectedIndexLabel,\\n                        else => return error.UnexpectedIndexLabel,\\n                    }\\n                    self.saw_action = true;\\n                },\\n                else => return error.UnexpectedProfileAction,\\n            }\\n        }\\n\\n        /// emitWickTermEnd accepts the completed primitive term.\\n        pub fn emitWickTermEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving profile zero-mode factor.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the successful residual base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{\\n        .expected_z = @intFromEnum(z),\\n        .expected_w = @intFromEnum(w),\\n        .expected_profile = @intFromEnum(f),\\n        .expected_index = @intFromEnum(mu),\\n    };\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.term_count);\\n    try testing.expectEqual(@as(usize, 1), sink.scalar_count);\\n    try testing.expectEqual(@as(usize, 1), sink.coordinate_count);\\n    try testing.expectEqual(@as(usize, 1), sink.tensor_count);\\n    try testing.expectEqual(@as(usize, 1), sink.action_count);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n    try testing.expect(sink.saw_tensor_none);\\n    try testing.expect(sink.saw_action);\\n\\n    const coeff = try local.profileCoefficient(\\"a\\");\\n    const x0 = try local.targetPoint(\\"x0\\");\\n    const polynomial_linear = X.profile.polynomialRnc(x0, .{.{ coeff, 1 }});\\n    const polynomial_quadratic = X.profile.polynomialRnc(x0, .{.{ coeff, 2 }});\\n    try testing.expect(@intFromEnum(polynomial_linear) != @intFromEnum(polynomial_quadratic));\\n}\\n\\ntest \\"pair Wick resolves config-backed boundary projectors\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n    const XBoundary = freeBosonBoundary(.{\\n        .neumann = X.target.subspace(&.{ 0, 1, 2, 3 }),\\n        .dirichlet = X.target.complement(&.{ 0, 1, 2, 3 }),\\n        .dirichlet_position = X.target.point(\\"x0\\"),\\n    });\\n    const Disk = boundary(product(.{X}), .{XBoundary});\\n    const neumann = X.target.subspace(&.{ 0, 1, 2, 3 });\\n\\n    var local = try Disk.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const nu = try local.index(\\"nu\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const y2 = try local.boundaryCoord(\\"y2\\");\\n    const left = try @TypeOf(XBoundary).op.dXBoundary(&local, mu, 0, y);\\n    const right = try @TypeOf(XBoundary).op.dXBoundary(&local, nu, 1, y2);\\n    const ops = try local.ops(.{ left, right });\\n\\n    const Sink = struct {\\n        term_count: usize = 0,\\n        scalar_count: usize = 0,\\n        coordinate_count: usize = 0,\\n        tensor_count: usize = 0,\\n        action_count: usize = 0,\\n        saw_scalar: bool = false,\\n        saw_coordinate: bool = false,\\n        saw_projector: bool = false,\\n        expected_y: u32,\\n        expected_y2: u32,\\n        expected_neumann: u32,\\n\\n        /// emitWickTermStart records the single boundary Wick term.\\n        pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n            if (event.left_index != 0 or event.right_index != 1 or event.term_index != 0) return error.UnexpectedWickTermEvent;\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar checks the boundary free-boson scalar factor.\\n        pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n            self.scalar_count += 1;\\n            switch (factor) {\\n                .value => |scalar| switch (scalar) {\\n                    .monomial => |monomial| {\\n                        if (monomial.rational.numerator != -1 or monomial.rational.denominator != 1 or monomial.atom == null) return error.UnexpectedScalarFactor;\\n                        self.saw_scalar = true;\\n                    },\\n                    else => return error.UnexpectedScalarFactor,\\n                },\\n                else => return error.UnexpectedScalarFactor,\\n            }\\n        }\\n\\n        /// emitWickCoordinate checks the differentiated boundary pole.\\n        pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n            self.coordinate_count += 1;\\n            if (factor.scalar.numerator != 2 or factor.scalar.denominator != 1) return error.UnexpectedCoordinateScalar;\\n            switch (factor.kernel) {\\n                .difference_power => |power| {\\n                    if (power.coordinate.left != self.expected_y or power.coordinate.right != self.expected_y2 or power.exponent != -3) return error.UnexpectedCoordinateFactor;\\n                    self.saw_coordinate = true;\\n                },\\n                else => return error.UnexpectedCoordinateFactor,\\n            }\\n        }\\n\\n        /// emitWickTensor checks that the Neumann projector resolves from config.\\n        pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n            self.tensor_count += 1;\\n            switch (factor) {\\n                .projector_metric => |projector_metric| switch (projector_metric.projector) {\\n                    .config => |value| switch (value) {\\n                        .tensor_projector => |projector| {\\n                            if (@intFromEnum(projector) != self.expected_neumann) return error.UnexpectedProjectorConfig;\\n                            self.saw_projector = true;\\n                        },\\n                        else => return error.UnexpectedProjectorConfig,\\n                    },\\n                    else => return error.UnexpectedProjectorSource,\\n                },\\n                else => return error.UnexpectedTensorFactor,\\n            }\\n        }\\n\\n        /// emitWickAction counts unexpected profile actions.\\n        pub fn emitWickAction(self: *@This(), factor: anytype) !void {\\n            _ = factor;\\n            self.action_count += 1;\\n        }\\n\\n        /// emitWickTermEnd accepts the completed boundary term.\\n        pub fn emitWickTermEnd(self: *@This()) !void {\\n            _ = self;\\n        }\\n\\n        /// emitZeroModeFactor accepts disk empty-base normalization factors.\\n        pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n\\n        /// emitZeroModeBaseEnd accepts the successful full contraction base case.\\n        pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n    };\\n\\n    var sink = Sink{\\n        .expected_y = @intFromEnum(y),\\n        .expected_y2 = @intFromEnum(y2),\\n        .expected_neumann = @intFromEnum(neumann),\\n    };\\n    try Disk.correlator(&Disk.config.disk, ops, &sink);\\n    try testing.expectEqual(@as(usize, 1), sink.term_count);\\n    try testing.expectEqual(@as(usize, 1), sink.scalar_count);\\n    try testing.expectEqual(@as(usize, 1), sink.coordinate_count);\\n    try testing.expectEqual(@as(usize, 1), sink.tensor_count);\\n    try testing.expectEqual(@as(usize, 0), sink.action_count);\\n    try testing.expect(sink.saw_scalar);\\n    try testing.expect(sink.saw_coordinate);\\n    try testing.expect(sink.saw_projector);\\n}\\ntest \\"recursive correlator enumerates free-boson pairings without dangling branches\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const ops = try local.ops(.{\\n        try X.op.dX(&local, mu, 0, z1),\\n        try X.op.dX(&local, mu, 0, z2),\\n        try X.op.dX(&local, mu, 0, z3),\\n        try X.op.dX(&local, mu, 0, z4),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        wick_terms: usize = 0,\\n        zero_modes: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickTermStart counts one primitive contraction in an accepted branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor counts the free-boson constant-mode factor.\\n        pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .momentum_delta => self.zero_modes += 1,\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try X.correlator(&X.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 6), sink.wick_terms);\\n    try testing.expectEqual(@as(usize, 3), sink.zero_modes);\\n    try testing.expectEqual(@as(usize, 3), sink.base_cases);\\n}\\n\\ntest \\"recursive correlator leaves bc top-form residuals after all b contractions\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const z5 = try local.coord(\\"z5\\");\\n    const z6 = try local.coord(\\"z6\\");\\n    const z7 = try local.coord(\\"z7\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.b(&local, 0, z1),\\n        try Ghost.op.b(&local, 0, z2),\\n        try Ghost.op.c(&local, 0, z3),\\n        try Ghost.op.c(&local, 0, z4),\\n        try Ghost.op.c(&local, 0, z5),\\n        try Ghost.op.c(&local, 0, z6),\\n        try Ghost.op.c(&local, 0, z7),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        wick_terms: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickTermStart counts one b-c contraction in an accepted branch.\\n        pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n            self.wick_terms += 1;\\n        }\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving c-zero-mode top form.\\n        pub fn emitZeroModeFactor(_: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => {},\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 40), sink.wick_terms);\\n    try testing.expectEqual(@as(usize, 20), sink.base_cases);\\n}\\n\\ntest \\"recursive correlator emits fermion signs for bc pair crossings\\" {\\n    const testing = @import(\\"std\\").testing;\\n\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const z3 = try local.coord(\\"z3\\");\\n    const z4 = try local.coord(\\"z4\\");\\n    const z5 = try local.coord(\\"z5\\");\\n    const z6 = try local.coord(\\"z6\\");\\n    const z7 = try local.coord(\\"z7\\");\\n    const ops = try local.ops(.{\\n        try Ghost.op.b(&local, 0, z1),\\n        try Ghost.op.b(&local, 0, z2),\\n        try Ghost.op.c(&local, 0, z3),\\n        try Ghost.op.c(&local, 0, z4),\\n        try Ghost.op.c(&local, 0, z5),\\n        try Ghost.op.c(&local, 0, z6),\\n        try Ghost.op.c(&local, 0, z7),\\n    });\\n\\n    const Sink = struct {\\n        pub const emitWickScalar = noopWickScalar;\\n        pub const emitWickCoordinate = noopWickCoordinate;\\n        pub const emitWickTensor = noopWickTensor;\\n        pub const emitWickTermEnd = noopWickTermEnd;\\n\\n        negative_signs: usize = 0,\\n        base_cases: usize = 0,\\n\\n        /// emitWickBranchSign records negative fermion branch signs.\\n        pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n            if (sign != -1) return error.UnexpectedBranchSign;\\n            self.negative_signs += 1;\\n        }\\n\\n        /// emitWickTermStart accepts a primitive contraction.\\n        pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n\\n        /// emitWickAction rejects unexpected action factors.\\n        pub fn emitWickAction(_: *@This(), _: anytype) !void {\\n            return error.UnexpectedAction;\\n        }\\n\\n        /// emitZeroModeFactor accepts the surviving c-zero-mode top form.\\n        pub fn emitZeroModeFactor(_: *@This(), factor: anytype) !void {\\n            switch (factor) {\\n                .bc_top_form => {},\\n                else => return error.UnexpectedZeroModeFactor,\\n            }\\n        }\\n\\n        /// emitZeroModeBaseEnd counts one accepted full Wick branch.\\n        pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n            self.base_cases += 1;\\n        }\\n    };\\n\\n    var sink = Sink{};\\n    try Ghost.correlator(&Ghost.config.sphere, ops, &sink);\\n    try testing.expectEqual(@as(usize, 10), sink.negative_signs);\\n    try testing.expectEqual(@as(usize, 20), sink.base_cases);\\n}\\n","n":0.014}}},{"i":24,"$":{"1":{"v":"Preset Shared Runtime","n":0.577},"2":{"v":"This node contains the small runtime vocabulary shared by the preset\\nconstructors in Free Boson Preset,\\nbc Preset, and\\nPreset Composition.  It deliberately does not define a\\nspecific CFT.  It gives preset authors compact handles, local-operator\\nconstruction, Wick rule templates, zero-mode rule templates, and the first\\nstreaming correlator driver.\\nThe data path is:\\nA user creates typed handles with Local.\\nPreset-owned operator builders choose their own operator kind ids and store\\n   only compact labels plus insertion data in a CFT Kernel MultiOp.\\nA preset supplies a CorrelatorConfig containing Wick and zero-mode rules.\\nThe streaming driver emits matched rules to a sink without building the full\\n   correlator expression in memory.\\nThe compact text sink is only an inspection adapter for small cases.  It keeps\\nexplicit byte and branch limits and reuses the streaming events instead of\\nmaterializing a symbolic expression tree.\\nconst std = @import(\\"std\\");\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst coefficient = @import(\\"../expressions/coefficient.zig\\");\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\nconst theory = @import(\\"../theory/theory.zig\\");\\nconst wick_correlator = @import(\\"../correlators/wick.zig\\");\\nconst KernelOperatorList = kernel.Call.MultiOp;\\n\\n/// LocalOperator is an opaque token for one operator insertion.\\npub const LocalOperator = opaque {};\\n\\n/// OperatorList is an opaque frozen operator sequence for correlator calls.\\npub const OperatorList = opaque {};\\n\\n/// Handle groups compact ids used by preset operator builders.\\npub const Handle = struct {\\n    /// Coord names a holomorphic or antiholomorphic coordinate variable.\\n    pub const Coord = enum(u32) { _ };\\n    /// BoundaryCoord names a real boundary coordinate variable.\\n    pub const BoundaryCoord = enum(u32) { _ };\\n    /// Index names a target-space vector index.\\n    pub const Index = enum(u32) { _ };\\n    /// Momentum names a target-space momentum expression.\\n    pub const Momentum = enum(u32) { _ };\\n    /// Profile names a selected presentation of a target-space profile.\\n    pub const Profile = enum(u32) { _ };\\n    /// TargetFunction names a position-space target profile.\\n    pub const TargetFunction = enum(u32) { _ };\\n    /// FourierTransform names a Fourier-space target profile.\\n    pub const FourierTransform = enum(u32) { _ };\\n    /// TargetPoint names a point in target space.\\n    pub const TargetPoint = enum(u32) { _ };\\n    /// ProfileCoefficient names one coefficient in a polynomial profile.\\n    pub const ProfileCoefficient = enum(u32) { _ };\\n    /// TensorProjector names a tensor-code projector expression.\\n    pub const TensorProjector = enum(u32) { _ };\\n    /// BoundaryStack names stacked boundary-condition Chan-Paton data.\\n    pub const BoundaryStack = enum(u32) { _ };\\n    /// ProfilePresentation records how a profile handle should be lowered.\\n    pub const ProfilePresentation = enum(u2) {\\n        position_space,\\n        fourier,\\n        polynomial_rnc,\\n    };\\n};\\n\\nfn rawToken(value: anytype) u32 {\\n    return @intFromEnum(value);\\n}\\n\\nfn token(comptime T: type, id: u32) T {\\n    return @enumFromInt(if (id == 0) 1 else id);\\n}\\n\\nconst profile_tag_shift = 30;\\nconst profile_payload_mask: u32 = 0x3fff_ffff;\\n\\n/// profileHandle packs a profile presentation tag with a compact payload id.\\npub fn profileHandle(comptime presentation: Handle.ProfilePresentation, payload: u32) Handle.Profile {\\n    const tag = @as(u32, @intFromEnum(presentation)) << profile_tag_shift;\\n    return token(Handle.Profile, tag | (payload & profile_payload_mask));\\n}\\n\\nfn profilePresentation(profile: Handle.Profile) Handle.ProfilePresentation {\\n    return @enumFromInt(@intFromEnum(profile) >> profile_tag_shift);\\n}\\n\\nfn profilePayload(profile: Handle.Profile) u32 {\\n    return @intFromEnum(profile) & profile_payload_mask;\\n}\\n\\nfn stableId(comptime namespace: []const u8, comptime name: []const u8) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (namespace) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    hash = (hash ^ @as(u32, \':\')) *% 16777619;\\n    inline for (name) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\n/// configRef derives a stable id for correlator configuration data.\\nfn configRef(comptime namespace: []const u8, comptime name: []const u8) ConfigRef {\\n    return stableId(namespace, name);\\n}\\n\\nconst ScalarAtom = u32;\\n\\nconst ConfigRef = u32;\\n\\nconst RationalScalar = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\nconst ScalarMonomial = struct {\\n    rational: RationalScalar = .{ .numerator = 1, .denominator = 1 },\\n    imaginary_power: u2 = 0,\\n    atom: ?ScalarAtom = null,\\n    atom_power: i8 = 0,\\n};\\n\\nconst RuleScalar = union(enum) {\\n    one,\\n    rational: RationalScalar,\\n    monomial: ScalarMonomial,\\n};\\n\\n/// scalars exposes basic algebra for compact structured rule scalars.\\nconst scalars = ScalarDsl;\\n\\nconst ScalarDsl = struct {\\n    fn absInt(value: i64) u64 {\\n        if (value == std.math.minInt(i64)) return @as(u64, 1) << 63;\\n        return if (value < 0) @intCast(-value) else @intCast(value);\\n    }\\n\\n    fn gcd(first: u64, second: u64) u64 {\\n        var a = first;\\n        var b = second;\\n        while (b != 0) {\\n            const rem = a % b;\\n            a = b;\\n            b = rem;\\n        }\\n        return if (a == 0) 1 else a;\\n    }\\n\\n    fn rationalValue(comptime numerator: i64, comptime denominator: i64) RationalScalar {\\n        if (denominator == 0) @compileError(\\"rational denominator cannot be zero\\");\\n        var num = numerator;\\n        var den = denominator;\\n        if (den < 0) {\\n            num = -num;\\n            den = -den;\\n        }\\n        const factor: i64 = @intCast(gcd(absInt(num), absInt(den)));\\n        return .{\\n            .numerator = @divExact(num, factor),\\n            .denominator = @divExact(den, factor),\\n        };\\n    }\\n\\n    /// atom derives a stable scalar atom id from a preset namespace and atom name.\\n    pub fn atom(comptime namespace: []const u8, comptime name: []const u8) ScalarAtom {\\n        return stableId(namespace, name);\\n    }\\n\\n    /// one constructs the multiplicative unit scalar.\\n    pub fn one() RuleScalar {\\n        return .one;\\n    }\\n\\n    /// rational constructs an exact rational scalar.\\n    pub fn rational(comptime numerator: i64, comptime denominator: i64) RuleScalar {\\n        return .{ .rational = rationalValue(numerator, denominator) };\\n    }\\n\\n    /// atomScalar constructs a scalar consisting of one named atom.\\n    pub fn atomScalar(comptime atom_id: ScalarAtom) RuleScalar {\\n        return monomial(1, 1, 0, atom_id, 1);\\n    }\\n\\n    /// monomial constructs a rational times i-power times atom-power scalar.\\n    pub fn monomial(comptime numerator: i64, comptime denominator: i64, comptime imaginary_power: u2, comptime atom_id: ?ScalarAtom, comptime atom_power: i8) RuleScalar {\\n        return .{ .monomial = .{\\n            .rational = rationalValue(numerator, denominator),\\n            .imaginary_power = imaginary_power,\\n            .atom = atom_id,\\n            .atom_power = atom_power,\\n        } };\\n    }\\n\\n    fn monomialFrom(comptime value: RuleScalar) ScalarMonomial {\\n        return switch (value) {\\n            .one => .{},\\n            .rational => |r| .{ .rational = r },\\n            .monomial => |m| m,\\n        };\\n    }\\n\\n    /// scale multiplies a scalar by an exact rational number.\\n    pub fn scale(comptime value: RuleScalar, comptime numerator: i64, comptime denominator: i64) RuleScalar {\\n        var factor = monomialFrom(value);\\n        factor.rational = rationalValue(factor.rational.numerator * numerator, factor.rational.denominator * denominator);\\n        return .{ .monomial = factor };\\n    }\\n\\n    /// neg negates a scalar.\\n    pub fn neg(comptime value: RuleScalar) RuleScalar {\\n        return scale(value, -1, 1);\\n    }\\n\\n    /// div divides a scalar by an integer denominator.\\n    pub fn div(comptime value: RuleScalar, comptime denominator: i64) RuleScalar {\\n        return scale(value, 1, denominator);\\n    }\\n\\n    /// i multiplies a scalar by the imaginary unit.\\n    pub fn i(comptime value: RuleScalar) RuleScalar {\\n        var factor = monomialFrom(value);\\n        factor.imaginary_power +%= 1;\\n        return .{ .monomial = factor };\\n    }\\n};\\n\\n/// familyKind derives a compact operator kind id from a preset namespace and local family enum.\\nfn familyKind(comptime namespace: []const u8, comptime family: anytype) operators.OperatorKindId {\\n    return stableId(namespace, @tagName(family));\\n}\\n\\n/// sectorId derives a compact zero-mode sector id from a preset namespace and local sector enum.\\nfn sectorId(comptime namespace: []const u8, comptime sector: anytype) u32 {\\n    return stableId(namespace, @tagName(sector));\\n}\\n\\nconst LocalStateImpl = struct {\\n    allocator: std.mem.Allocator,\\n    labels: std.ArrayList(kernel.Call.LabelValue) = .empty,\\n    operators_list: std.ArrayList(kernel.Call.LocalOp) = .empty,\\n    pending_operators: std.ArrayList(*LocalOperatorImpl) = .empty,\\n    owned_labels: []kernel.Call.LabelValue = &.{},\\n    owned_operators: []kernel.Call.LocalOp = &.{},\\n    label_store: kernel.Call.LabelStore = .{ .values = &.{} },\\n    next_symbol: u32 = 1,\\n    next_normal_order_group: normal_ordering.Group = normal_ordering.first,\\n    finished: bool = false,\\n};\\n\\nconst LocalOperatorImpl = struct {\\n    owner: *LocalStateImpl,\\n    item: kernel.Call.LocalOp,\\n};\\n\\n/// Local builds typed handles and freezes operator lists for correlator calls.\\npub const Local = opaque {\\n    /// init constructs an empty local-operator builder.\\n    pub fn init(allocator: std.mem.Allocator) !*Local {\\n        const state = try allocator.create(LocalStateImpl);\\n        state.* = .{ .allocator = allocator };\\n        return @ptrCast(state);\\n    }\\n\\n    fn data(self: *Local) *LocalStateImpl {\\n        return @ptrCast(@alignCast(self));\\n    }\\n\\n    /// deinit releases memory owned by this local builder and its frozen operator list.\\n    pub fn deinit(self: *Local) void {\\n        const state = self.data();\\n        const allocator = state.allocator;\\n        if (state.finished) {\\n            allocator.free(state.owned_labels);\\n            allocator.free(state.owned_operators);\\n        } else {\\n            state.labels.deinit(allocator);\\n            state.operators_list.deinit(allocator);\\n        }\\n        for (state.pending_operators.items) |pending| {\\n            allocator.destroy(pending);\\n        }\\n        state.pending_operators.deinit(allocator);\\n        allocator.destroy(state);\\n    }\\n\\n    fn next(self: *Local) u32 {\\n        const state = self.data();\\n        const value = state.next_symbol;\\n        state.next_symbol += 1;\\n        return value;\\n    }\\n\\n    /// coord creates a named bulk coordinate token.\\n    pub fn coord(self: *Local, name: []const u8) !Handle.Coord {\\n        _ = name;\\n        return token(Handle.Coord, self.next());\\n    }\\n\\n    /// boundaryCoord creates a named boundary coordinate token.\\n    pub fn boundaryCoord(self: *Local, name: []const u8) !Handle.BoundaryCoord {\\n        _ = name;\\n        return token(Handle.BoundaryCoord, self.next());\\n    }\\n\\n    /// index creates a target-space index token.\\n    pub fn index(self: *Local, name: []const u8) !Handle.Index {\\n        _ = name;\\n        return token(Handle.Index, self.next());\\n    }\\n\\n    /// momentum creates a target-space momentum token.\\n    pub fn momentum(self: *Local, name: []const u8) !Handle.Momentum {\\n        _ = name;\\n        return token(Handle.Momentum, self.next());\\n    }\\n\\n    /// targetFunction creates a position-space target-profile token.\\n    pub fn targetFunction(self: *Local, name: []const u8) !Handle.TargetFunction {\\n        _ = name;\\n        return token(Handle.TargetFunction, self.next());\\n    }\\n\\n    /// fourierTransform creates a Fourier-space profile token.\\n    pub fn fourierTransform(self: *Local, name: []const u8) !Handle.FourierTransform {\\n        _ = name;\\n        return token(Handle.FourierTransform, self.next());\\n    }\\n\\n    /// targetPoint creates a target-space point token.\\n    pub fn targetPoint(self: *Local, name: []const u8) !Handle.TargetPoint {\\n        _ = name;\\n        return token(Handle.TargetPoint, self.next());\\n    }\\n\\n    /// profileCoefficient creates a polynomial-profile coefficient token.\\n    pub fn profileCoefficient(self: *Local, name: []const u8) !Handle.ProfileCoefficient {\\n        _ = name;\\n        return token(Handle.ProfileCoefficient, self.next());\\n    }\\n\\n    fn labelSpan(self: *Local, values: []const kernel.Call.LabelValue) !theory.Id.LabelSpan {\\n        const state = self.data();\\n        const start = state.labels.items.len;\\n        try state.labels.appendSlice(state.allocator, values);\\n        return @intCast(start);\\n    }\\n\\n    fn symbol(value: anytype) kernel.Call.LabelValue {\\n        return .{ .symbol = rawToken(value) };\\n    }\\n\\n    fn op(self: *Local, kind: operators.OperatorKindId, insertion: operators.OperatorInsertion, values: []const kernel.Call.LabelValue) !*LocalOperator {\\n        const state = self.data();\\n        const pending = try state.allocator.create(LocalOperatorImpl);\\n        pending.* = .{\\n            .owner = state,\\n            .item = .{\\n            .insertion = insertion,\\n            .kind = kind,\\n            .labels = try self.labelSpan(values),\\n            },\\n        };\\n        try state.pending_operators.append(state.allocator, pending);\\n        return @ptrCast(pending);\\n    }\\n\\n    fn rawOperator(self: *Local, item: *LocalOperator) !kernel.Call.LocalOp {\\n        const state = self.data();\\n        const pending: *LocalOperatorImpl = @ptrCast(@alignCast(item));\\n        if (pending.owner != state) return error.InvalidLocalOperator;\\n        return pending.item;\\n    }\\n\\n    fn addRaw(self: *Local, item: kernel.Call.LocalOp) !void {\\n        const state = self.data();\\n        if (state.finished) return error.LocalAlreadyFinished;\\n        try state.operators_list.append(state.allocator, item);\\n    }\\n\\n    fn add(self: *Local, item: *LocalOperator) !void {\\n        try self.addRaw(try self.rawOperator(item));\\n    }\\n\\n    fn isLocalOperatorToken(comptime Item: type) bool {\\n        return switch (@typeInfo(Item)) {\\n            .pointer => |ptr| ptr.child == LocalOperator,\\n            else => false,\\n        };\\n    }\\n\\n    fn addItem(self: *Local, item: anytype) !void {\\n        if (comptime isLocalOperatorToken(@TypeOf(item))) {\\n            try self.add(item);\\n        } else {\\n            try self.addNormalOrdered(item);\\n        }\\n    }\\n\\n    fn addNormalOrdered(self: *Local, items: anytype) !void {\\n        const state = self.data();\\n        const group = try normal_ordering.next(&state.next_normal_order_group);\\n        inline for (items) |item| {\\n            try self.addRaw(normal_ordering.tag(try self.rawOperator(item), group));\\n        }\\n    }\\n\\n    /// ops freezes local operators; nested tuples are normal-ordered products.\\n    pub fn ops(self: *Local, items: anytype) !*OperatorList {\\n        inline for (items) |item| {\\n            try self.addItem(item);\\n        }\\n        return self.freeze();\\n    }\\n\\n    fn freeze(self: *Local) !*OperatorList {\\n        const state = self.data();\\n        if (state.finished) return error.LocalAlreadyFinished;\\n        state.owned_operators = try state.operators_list.toOwnedSlice(state.allocator);\\n        state.owned_labels = try state.labels.toOwnedSlice(state.allocator);\\n        state.label_store = .{ .values = state.owned_labels };\\n        state.finished = true;\\n        return @ptrCast(state);\\n    }\\n};\\n\\n/// wick exposes compact preset-author helpers for primitive Wick rule templates.\\nconst wick = WickDsl;\\n\\nconst WickDsl = struct {\\n    /// Support tells the evaluator which insertion coordinate slots it reads.\\n    pub const Support = enum {\\n        holomorphic,\\n        antiholomorphic,\\n        bulk_pair,\\n        boundary,\\n    };\\n\\n    /// Side selects the left or right operator in a Wick rule expression.\\n    pub const Side = enum { left, right };\\n\\n    /// CoordinateSlot selects the coordinate component of a matched insertion.\\n    pub const CoordinateSlot = enum { position, holomorphic, antiholomorphic };\\n\\n    /// CoordinateRef refers to one coordinate of one side of a Wick rule.\\n    pub const CoordinateRef = struct {\\n        side: Side,\\n        slot: CoordinateSlot,\\n    };\\n\\n    /// LabelRef refers to one runtime label of one side of a Wick rule.\\n    pub const LabelRef = struct {\\n        side: Side,\\n        slot: u8,\\n    };\\n\\n    /// ScalarFactor describes one non-coordinate scalar multiplier.\\n    pub const ScalarFactor = union(enum) {\\n        value: RuleScalar,\\n        label_bilinear_phase: struct { left: LabelRef, right: LabelRef, form: []const u8 },\\n        cocycle: struct { table: []const u8, left: LabelRef, right: LabelRef },\\n        spin_structure: []const u8,\\n    };\\n\\n    /// TensorRef identifies tensor data coming from labels or selected config data.\\n    pub const TensorRef = union(enum) {\\n        label: LabelRef,\\n        config: ConfigRef,\\n    };\\n\\n    /// TensorFactor describes one tensor multiplier in a Wick coefficient.\\n    pub const TensorFactor = union(enum) {\\n        none,\\n        metric: struct { left: LabelRef, right: LabelRef },\\n        momentum_index: struct { momentum: LabelRef, index: LabelRef },\\n        momentum_pair: struct { left: LabelRef, right: LabelRef },\\n        projector_metric: struct { projector: TensorRef, left: LabelRef, right: LabelRef },\\n        projector_momentum_index: struct { projector: TensorRef, momentum: LabelRef, index: LabelRef },\\n        projector_momentum_pair: struct { projector: TensorRef, left: LabelRef, right: LabelRef },\\n    };\\n\\n    /// ActionFactor describes a non-multiplicative action produced by a Wick contraction.\\n    pub const ActionFactor = union(enum) {\\n        profile_derivative: struct { profile: LabelRef, index: LabelRef },\\n        projected_profile_derivative: struct { projector: TensorRef, profile: LabelRef, index: LabelRef },\\n    };\\n\\n    /// CoordinateDifference is the coordinate difference read by a Wick kernel.\\n    pub const CoordinateDifference = struct {\\n        left: CoordinateRef,\\n        right: CoordinateRef,\\n    };\\n\\n    /// DerivativeAction tells resolution how insertion derivative labels act on a coordinate factor.\\n    pub const DerivativeAction = struct {\\n        include_left: bool = false,\\n        include_right: bool = false,\\n    };\\n\\n    /// CoordinateFactor describes one coordinate-dependent multiplier.\\n    pub const CoordinateFactor = union(enum) {\\n        difference_power: struct { coordinate: CoordinateDifference, exponent: i16, derivatives: DerivativeAction = .{} },\\n        logarithm: struct { coordinate: CoordinateDifference, derivatives: DerivativeAction = .{} },\\n        green_kernel: struct { name: []const u8, coordinate: CoordinateDifference, derivatives: DerivativeAction = .{} },\\n        green_exponential: CoordinateDifference,\\n    };\\n\\n    /// Term is one product of scalar, coordinate, tensor, and action factors.\\n    pub const Term = struct {\\n        scalars: []const ScalarFactor,\\n        coordinates: []const CoordinateFactor,\\n        tensors: []const TensorFactor,\\n        actions: []const ActionFactor = &.{},\\n        residuals: []const Side = &.{},\\n    };\\n\\n    /// Expr is a sum of Wick coefficient terms.\\n    pub const Expr = struct {\\n        terms: []const Term,\\n    };\\n\\n    const PoleExpr = struct {\\n        scalar: ScalarFactor,\\n        numerator: TensorFactor,\\n        coordinate: CoordinateDifference,\\n        exponent: i16,\\n    };\\n\\n    const GreenExponentialExpr = struct {\\n        scalar: ScalarFactor,\\n        momentum_pair: TensorFactor,\\n        coordinate: CoordinateDifference,\\n    };\\n\\n    /// Pattern is one side of a primitive Wick rule.\\n    pub const Pattern = struct {\\n        kind: operators.OperatorKindId,\\n        support: Support,\\n    };\\n\\n    /// Rule is one preset-authored primitive Wick rule.\\n    pub const Rule = struct {\\n        left: Pattern,\\n        right: Pattern,\\n        expr: Expr,\\n    };\\n\\n    /// rule constructs one primitive Wick rule template.\\n    pub fn rule(left: Pattern, right: Pattern, expression: Expr) Rule {\\n        return .{ .left = left, .right = right, .expr = expression };\\n    }\\n\\n    /// pattern constructs one Wick-rule side from a preset-owned kind id.\\n    pub fn pattern(comptime kind: operators.OperatorKindId, comptime support: Support) Pattern {\\n        return .{ .kind = kind, .support = support };\\n    }\\n\\n    /// coord refers to one coordinate slot of one Wick-rule side.\\n    pub fn coord(side: Side, slot: CoordinateSlot) CoordinateRef {\\n        return .{ .side = side, .slot = slot };\\n    }\\n\\n    /// label refers to one label slot of one Wick-rule side.\\n    pub fn label(side: Side, slot: u8) LabelRef {\\n        return .{ .side = side, .slot = slot };\\n    }\\n\\n    /// difference constructs a coordinate difference.\\n    pub fn difference(left: CoordinateRef, right: CoordinateRef) CoordinateDifference {\\n        return .{ .left = left, .right = right };\\n    }\\n\\n    /// scalar constructs a scalar factor from a rule scalar.\\n    pub fn scalar(comptime value: RuleScalar) ScalarFactor {\\n        return .{ .value = value };\\n    }\\n\\n    /// term constructs one product term in a Wick expression.\\n    pub fn term(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors };\\n    }\\n\\n    /// termWithActions constructs one Wick term that also carries operator actions.\\n    pub fn termWithActions(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor, comptime actions: []const ActionFactor) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors, .actions = actions };\\n    }\\n\\n    /// termWithResiduals constructs one Wick term with surviving input sides.\\n    pub fn termWithResiduals(comptime scalar_factors: []const ScalarFactor, comptime coordinates: []const CoordinateFactor, comptime tensors: []const TensorFactor, comptime actions: []const ActionFactor, comptime residuals: []const Side) Term {\\n        return .{ .scalars = scalar_factors, .coordinates = coordinates, .tensors = tensors, .actions = actions, .residuals = residuals };\\n    }\\n\\n    /// expr constructs a Wick expression from one or more terms.\\n    pub fn expr(comptime terms: []const Term) Expr {\\n        return .{ .terms = terms };\\n    }\\n\\n    /// profileDerivative constructs the action of a target derivative on a profile label.\\n    pub fn profileDerivative(comptime profile: LabelRef, comptime index: LabelRef) ActionFactor {\\n        return .{ .profile_derivative = .{ .profile = profile, .index = index } };\\n    }\\n\\n    /// projectedProfileDerivative constructs a projected target derivative on a profile label.\\n    pub fn projectedProfileDerivative(comptime projector: TensorRef, comptime profile: LabelRef, comptime index: LabelRef) ActionFactor {\\n        return .{ .projected_profile_derivative = .{ .projector = projector, .profile = profile, .index = index } };\\n    }\\n\\n    /// differencePower constructs a power of a coordinate difference.\\n    pub fn differencePower(comptime coordinate: CoordinateDifference, comptime exponent: i16) CoordinateFactor {\\n        return .{ .difference_power = .{ .coordinate = coordinate, .exponent = exponent } };\\n    }\\n\\n    /// differentiatedPower applies matched insertion derivative labels to a coordinate power.\\n    pub fn differentiatedPower(comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .difference_power = .{ .coordinate = coordinate, .exponent = exponent, .derivatives = derivatives } };\\n    }\\n\\n    /// logarithm constructs a logarithm of a coordinate difference.\\n    pub fn logarithm(comptime coordinate: CoordinateDifference) CoordinateFactor {\\n        return .{ .logarithm = .{ .coordinate = coordinate } };\\n    }\\n\\n    /// differentiatedLogarithm applies matched insertion derivative labels to a logarithm.\\n    pub fn differentiatedLogarithm(comptime coordinate: CoordinateDifference, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .logarithm = .{ .coordinate = coordinate, .derivatives = derivatives } };\\n    }\\n\\n    /// greenKernel constructs a named Green-kernel coordinate factor.\\n    pub fn greenKernel(comptime name: []const u8, comptime coordinate: CoordinateDifference) CoordinateFactor {\\n        return .{ .green_kernel = .{ .name = name, .coordinate = coordinate } };\\n    }\\n\\n    /// differentiatedGreenKernel records derivative labels acting on a named Green kernel.\\n    pub fn differentiatedGreenKernel(comptime name: []const u8, comptime coordinate: CoordinateDifference, comptime derivatives: DerivativeAction) CoordinateFactor {\\n        return .{ .green_kernel = .{ .name = name, .coordinate = coordinate, .derivatives = derivatives } };\\n    }\\n\\n    /// pole constructs a primitive pole coefficient.\\n    pub fn pole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16) Expr {\\n        const shape = PoleExpr{\\n            .scalar = scalar(scalar_value),\\n            .numerator = numerator,\\n            .coordinate = coordinate,\\n            .exponent = exponent,\\n        };\\n        return expr(&.{term(&.{shape.scalar}, &.{differencePower(shape.coordinate, shape.exponent)}, &.{shape.numerator})});\\n    }\\n\\n    /// differentiatedPole constructs a pole acted on by matched insertion derivatives.\\n    pub fn differentiatedPole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{term(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator})});\\n    }\\n\\n    /// differentiatedActionPole constructs a differentiated pole carrying profile or custom actions.\\n    pub fn differentiatedActionPole(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime actions: []const ActionFactor) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithActions(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, actions)});\\n    }\\n\\n    /// differentiatedPoleWithResiduals constructs a differentiated pole with surviving sides.\\n    pub fn differentiatedPoleWithResiduals(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime residuals: []const Side) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithResiduals(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, &.{}, residuals)});\\n    }\\n\\n    /// differentiatedActionPoleWithResiduals constructs an action pole with surviving sides.\\n    pub fn differentiatedActionPoleWithResiduals(comptime scalar_value: RuleScalar, comptime numerator: TensorFactor, comptime coordinate: CoordinateDifference, comptime exponent: i16, comptime derivatives: DerivativeAction, comptime actions: []const ActionFactor, comptime residuals: []const Side) Expr {\\n        const scalar_factor = scalar(scalar_value);\\n        return expr(&.{termWithResiduals(&.{scalar_factor}, &.{differentiatedPower(coordinate, exponent, derivatives)}, &.{numerator}, actions, residuals)});\\n    }\\n\\n    /// greenExponential constructs a plane-wave Green-kernel factor.\\n    pub fn greenExponential(comptime scalar_value: RuleScalar, comptime momentum_pair: TensorFactor, comptime coordinate: CoordinateDifference) Expr {\\n        const shape = GreenExponentialExpr{\\n            .scalar = scalar(scalar_value),\\n            .momentum_pair = momentum_pair,\\n            .coordinate = coordinate,\\n        };\\n        return expr(&.{term(&.{shape.scalar}, &.{.{ .green_exponential = shape.coordinate }}, &.{shape.momentum_pair})});\\n    }\\n\\n    /// greenExponentialWithResiduals constructs a Green factor with surviving sides.\\n    pub fn greenExponentialWithResiduals(comptime scalar_value: RuleScalar, comptime momentum_pair: TensorFactor, comptime coordinate: CoordinateDifference, comptime residuals: []const Side) Expr {\\n        const shape = GreenExponentialExpr{\\n            .scalar = scalar(scalar_value),\\n            .momentum_pair = momentum_pair,\\n            .coordinate = coordinate,\\n        };\\n        return expr(&.{termWithResiduals(&.{shape.scalar}, &.{.{ .green_exponential = shape.coordinate }}, &.{shape.momentum_pair}, &.{}, residuals)});\\n    }\\n};\\n\\n/// Spec stores internal declarative metadata before lowering to runtime tables.\\nconst Spec = struct {\\n    /// LabelKind classifies one operator label slot.\\n    pub const LabelKind = enum {\\n        index,\\n        momentum,\\n        profile,\\n    };\\n\\n    /// InsertionShape records the runtime insertion packer required by an operator.\\n    pub const InsertionShape = enum {\\n        single,\\n        pair,\\n    };\\n\\n    /// Statistics records the exchange parity of an operator family.\\n    pub const Statistics = enum {\\n        bosonic,\\n        fermionic,\\n    };\\n\\n    /// Operator declares one local-field family before lowering.\\n    pub const Operator = struct {\\n        name: []const u8,\\n        kind: operators.OperatorKindId,\\n        support: wick.Support,\\n        insertion: InsertionShape,\\n        labels: []const LabelKind = &.{},\\n        statistics: Statistics,\\n        zero_mode_consumable: bool = false,\\n    };\\n\\n    /// CoordinateKernel names the coordinate-factor family required by a Wick rule.\\n    pub const CoordinateKernel = enum {\\n        rational_pole,\\n        rational_green_exponential,\\n        elliptic_green,\\n        elliptic_green_exponential,\\n        elliptic_prime_form,\\n        elliptic_prime_form_log_derivative,\\n    };\\n\\n    /// WickRule records the abstract endpoints, primitive terms, and coordinate kernels.\\n    pub const WickRule = struct {\\n        left: operators.OperatorKindId,\\n        right: operators.OperatorKindId,\\n        term_count: usize = 1,\\n        expr: ?wick.Expr = null,\\n        coordinate_kernels: []const CoordinateKernel = &.{},\\n    };\\n\\n    /// ZeroModeKind identifies implemented and draft zero-mode spec families.\\n    pub const ZeroModeKind = enum {\\n        runtime,\\n        torus_free_boson_constant,\\n        torus_bc_moduli,\\n        torus_eta_xi_zero_mode,\\n    };\\n\\n    /// ZeroModeRule records one residual base-case consumer family.\\n    pub const ZeroModeRule = struct {\\n        sector: zero_mode.Sector,\\n        kind: ZeroModeKind = .runtime,\\n        expr: ?zero_mode.Expr = null,\\n        consumes: []const operators.OperatorKindId = &.{},\\n    };\\n\\n    /// SurfaceKind names the topology class described by a theory spec.\\n    pub const SurfaceKind = enum {\\n        sphere,\\n        disk,\\n        torus,\\n    };\\n\\n    /// CoordinateModel selects the coordinate kernel family for a surface.\\n    pub const CoordinateModel = enum {\\n        rational,\\n        elliptic,\\n    };\\n\\n    /// SurfaceSource records where a surface convention is fixed.\\n    pub const SurfaceSource = enum {\\n        preset,\\n        stringbook,\\n    };\\n\\n    /// Surface records topology and modular data before runtime lowering.\\n    pub const Surface = struct {\\n        kind: SurfaceKind = .sphere,\\n        coordinate_model: CoordinateModel = .rational,\\n        modular_parameters: []const []const u8 = &.{},\\n        source: SurfaceSource = .preset,\\n    };\\n\\n    /// Theory groups the declarative metadata for one preset surface.\\n    pub const Theory = struct {\\n        surface: Surface = .{},\\n        operators: []const Operator,\\n        wick_rules: []const WickRule,\\n        zero_modes: []const ZeroModeRule,\\n    };\\n\\n    /// zeroModeRule lowers one declarative zero-mode spec into the runtime rule.\\n    pub fn zeroModeRule(comptime rule: ZeroModeRule) zero_mode.Rule {\\n        const expr = rule.expr orelse @compileError(\\"draft zero-mode spec cannot be lowered to runtime rules\\");\\n        return zero_mode.rule(rule.sector, expr);\\n    }\\n\\n    /// zeroModeRules lowers declarative zero-mode specs into runtime rules.\\n    pub fn zeroModeRules(comptime rules: []const ZeroModeRule) [rules.len]zero_mode.Rule {\\n        var storage: [rules.len]zero_mode.Rule = undefined;\\n        inline for (rules, 0..) |rule, index| {\\n            storage[index] = zeroModeRule(rule);\\n        }\\n        return storage;\\n    }\\n\\n    /// zeroModeConsumeKindCount returns the number of operator-kind families accepted by a zero-mode spec.\\n    pub fn zeroModeConsumeKindCount(comptime rule: ZeroModeRule) usize {\\n        const expr = rule.expr orelse return rule.consumes.len;\\n        return switch (expr) {\\n            .bc_top_form => |payload| payload.c_kind_ids.len,\\n            .free_boson_constant_mode => |payload| payload.exp_kind_ids.len + payload.profile_kind_ids.len,\\n            .eta_xi_zero_mode => |payload| payload.xi_kind_ids.len,\\n            .constant_fermion => |payload| payload.fermion_kind_ids.len,\\n            .top_form_fermion => |payload| payload.field_kind_ids.len,\\n            .boson_momentum_conservation => |payload| payload.exp_kind_ids.len + payload.profile_kind_ids.len,\\n        };\\n    }\\n\\n    /// wickCoordinateKernelCount counts Wick rules that require one coordinate kernel family.\\n    pub fn wickCoordinateKernelCount(comptime rules: []const WickRule, comptime expected_kernel: CoordinateKernel) usize {\\n        comptime var count: usize = 0;\\n        inline for (rules) |rule| {\\n            inline for (rule.coordinate_kernels) |candidate| {\\n                if (candidate == expected_kernel) count += 1;\\n            }\\n        }\\n        return count;\\n    }\\n\\n    fn operatorSupport(comptime operator_specs: []const Operator, comptime kind_id: operators.OperatorKindId) wick.Support {\\n        inline for (operator_specs) |operator| {\\n            if (operator.kind == kind_id) return operator.support;\\n        }\\n        @compileError(\\"missing operator spec for Wick rule endpoint\\");\\n    }\\n\\n    /// wickRule lowers one declarative Wick spec into the runtime rule table.\\n    pub fn wickRule(comptime rule: WickRule, comptime operator_specs: []const Operator) wick.Rule {\\n        const expression = rule.expr orelse @compileError(\\"draft Wick spec cannot be lowered to runtime rules\\");\\n        return wick.rule(\\n            wick.pattern(rule.left, operatorSupport(operator_specs, rule.left)),\\n            wick.pattern(rule.right, operatorSupport(operator_specs, rule.right)),\\n            expression,\\n        );\\n    }\\n\\n    /// wickRules lowers declarative Wick specs into the runtime rule table.\\n    pub fn wickRules(comptime rules: []const WickRule, comptime operator_specs: []const Operator) [rules.len]wick.Rule {\\n        var storage: [rules.len]wick.Rule = undefined;\\n        inline for (rules, 0..) |rule, index| {\\n            storage[index] = wickRule(rule, operator_specs);\\n        }\\n        return storage;\\n    }\\n\\n    /// fermionKindCount counts operators that contribute exchange signs.\\n    pub fn fermionKindCount(comptime operator_specs: []const Operator) usize {\\n        comptime var count: usize = 0;\\n        inline for (operator_specs) |operator| {\\n            if (operator.statistics == .fermionic) count += 1;\\n        }\\n        return count;\\n    }\\n\\n    /// fermionKinds lowers operator statistics into runtime fermion kind ids.\\n    pub fn fermionKinds(comptime operator_specs: []const Operator) [fermionKindCount(operator_specs)]operators.OperatorKindId {\\n        var storage: [fermionKindCount(operator_specs)]operators.OperatorKindId = undefined;\\n        comptime var index: usize = 0;\\n        inline for (operator_specs) |operator| {\\n            if (operator.statistics == .fermionic) {\\n                storage[index] = operator.kind;\\n                index += 1;\\n            }\\n        }\\n        return storage;\\n    }\\n};\\n\\nfn coordVariable(coord: Handle.Coord) u32 {\\n    return rawToken(coord);\\n}\\n\\nfn singleInsertion(z: Handle.Coord, derivatives: u8) operators.OperatorInsertion {\\n    return .{ .single = .{\\n        .position = coordVariable(z),\\n        .derivatives = derivatives,\\n    } };\\n}\\n\\nfn pairInsertion(z: Handle.Coord, zbar: Handle.Coord) operators.OperatorInsertion {\\n    return .{ .pair = .{\\n        .holomorphic_position = coordVariable(z),\\n        .antiholomorphic_position = coordVariable(zbar),\\n        .holomorphic_derivatives = 0,\\n        .antiholomorphic_derivatives = 0,\\n    } };\\n}\\n\\nfn labelTupleCount(comptime Labels: type) comptime_int {\\n    const info = @typeInfo(Labels);\\n    if (info != .@\\"struct\\" or !info.@\\"struct\\".is_tuple) @compileError(\\"operator labels must be passed as a tuple\\");\\n    return info.@\\"struct\\".fields.len;\\n}\\n\\nfn fillSymbolLabels(comptime count: usize, out: *[count]kernel.Call.LabelValue, labels: anytype) void {\\n    inline for (0..count) |index| {\\n        out[index] = Local.symbol(labels[index]);\\n    }\\n}\\n\\nfn buildKindOperator(local: *Local, kind_id: operators.OperatorKindId, insertion: operators.OperatorInsertion, labels: anytype) !*LocalOperator {\\n    const count = comptime labelTupleCount(@TypeOf(labels));\\n    var values: [count]kernel.Call.LabelValue = undefined;\\n    fillSymbolLabels(count, &values, labels);\\n    return local.op(kind_id, insertion, values[0..]);\\n}\\n\\nfn assertOperatorShape(comptime operator: Spec.Operator, comptime insertion: Spec.InsertionShape, comptime label_count: usize) void {\\n    if (operator.insertion != insertion) @compileError(\\"operator builder insertion shape does not match spec\\");\\n    if (operator.labels.len != label_count) @compileError(\\"operator builder label count does not match spec\\");\\n}\\n\\nfn boundaryCoord(y: Handle.BoundaryCoord) Handle.Coord {\\n    return token(Handle.Coord, rawToken(y));\\n}\\n\\nfn buildSingleOperator(local: *Local, comptime operator: Spec.Operator, z: Handle.Coord, derivatives: u8, labels: anytype) !*LocalOperator {\\n    comptime assertOperatorShape(operator, .single, labelTupleCount(@TypeOf(labels)));\\n    return buildKindOperator(local, operator.kind, singleInsertion(z, derivatives), labels);\\n}\\n\\nfn buildPairOperator(local: *Local, comptime operator: Spec.Operator, z: Handle.Coord, zbar: Handle.Coord, labels: anytype) !*LocalOperator {\\n    comptime assertOperatorShape(operator, .pair, labelTupleCount(@TypeOf(labels)));\\n    return buildKindOperator(local, operator.kind, pairInsertion(z, zbar), labels);\\n}\\n\\nfn localBuilder(local: anytype) *Local {\\n    const Input = @TypeOf(local);\\n    return switch (@typeInfo(Input)) {\\n        .pointer => |ptr| if (ptr.child == Local)\\n            local\\n        else if (ptr.child == *Local)\\n            local.*\\n        else\\n            @compileError(\\"operator builders require a local builder\\"),\\n        else => @compileError(\\"operator builders require a local builder\\"),\\n    };\\n}\\n\\n/// operatorBuilder generates insertion and label packing from one operator schema.\\nfn operatorBuilder(comptime operator: Spec.Operator) type {\\n    return struct {\\n        /// single builds a single-coordinate local operator from this schema.\\n        pub fn single(local: anytype, z: Handle.Coord, derivatives: u8, labels: anytype) !*LocalOperator {\\n            return buildSingleOperator(localBuilder(local), operator, z, derivatives, labels);\\n        }\\n\\n        /// boundarySingle builds a boundary single-coordinate local operator from this schema.\\n        pub fn boundarySingle(local: anytype, y: Handle.BoundaryCoord, derivatives: u8, labels: anytype) !*LocalOperator {\\n            return buildSingleOperator(localBuilder(local), operator, boundaryCoord(y), derivatives, labels);\\n        }\\n\\n        /// pair builds a bulk-pair local operator from this schema.\\n        pub fn pair(local: anytype, z: Handle.Coord, zbar: Handle.Coord, labels: anytype) !*LocalOperator {\\n            return buildPairOperator(localBuilder(local), operator, z, zbar, labels);\\n        }\\n    };\\n}\\n\\n/// zero_mode exposes compact preset-author helpers for zero-mode rules.\\nconst zero_mode = ZeroModeDsl;\\n\\nconst ZeroModeDsl = struct {\\n    /// Sector names a preset-owned zero-mode sector.\\n    pub const Sector = u32;\\n\\n    /// BcSupport selects the finite c-zero-mode basis used by the bc evaluator.\\n    pub const BcSupport = enum {\\n        sphere_holomorphic,\\n        sphere_antiholomorphic,\\n        disk_doubled,\\n    };\\n\\n    /// BcTopForm declares one top-form ghost zero-mode saturation rule.\\n    pub const BcTopForm = struct {\\n        support: BcSupport,\\n        c_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// EtaXiSupport selects the chiral xi constant mode saturated by a surface.\\n    pub const EtaXiSupport = enum {\\n        sphere_holomorphic,\\n        sphere_antiholomorphic,\\n        torus_holomorphic,\\n        torus_antiholomorphic,\\n    };\\n\\n    /// EtaXiZeroMode declares one xi constant-mode saturation rule.\\n    pub const EtaXiZeroMode = struct {\\n        support: EtaXiSupport,\\n        xi_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// ConstantFermion declares one generic constant fermion saturation rule.\\n    pub const ConstantFermion = struct {\\n        support: EtaXiSupport,\\n        fermion_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// TopFormFermion declares one generic top-form fermion saturation rule.\\n    pub const TopFormFermion = struct {\\n        support: BcSupport,\\n        field_kind_ids: []const operators.OperatorKindId,\\n        normalization: RuleScalar = .one,\\n    };\\n\\n    /// RankSource records where a power of 2pi gets its exponent.\\n    pub const RankSource = union(enum) {\\n        none,\\n        literal: u16,\\n        target_dimension: ConfigRef,\\n        projector_rank: ConfigRef,\\n    };\\n\\n    /// Normalization records the CFT normalization attached to a zero-mode rule.\\n    pub const Normalization = struct {\\n        scalar: RuleScalar = .one,\\n        two_pi_power: RankSource = .none,\\n    };\\n\\n    /// FreeBosonConstantMode declares the free-boson constant-mode base case.\\n    pub const FreeBosonConstantMode = struct {\\n        integration_projector: ?ConfigRef = null,\\n        fixed_projector: ?ConfigRef = null,\\n        fixed_position: ?ConfigRef = null,\\n        exp_kind_ids: []const operators.OperatorKindId,\\n        profile_kind_ids: []const operators.OperatorKindId,\\n        normalization: Normalization = .{},\\n    };\\n\\n    /// Expr selects the zero-mode evaluator and payload.\\n    pub const Expr = union(enum) {\\n        bc_top_form: BcTopForm,\\n        free_boson_constant_mode: FreeBosonConstantMode,\\n        eta_xi_zero_mode: EtaXiZeroMode,\\n        constant_fermion: ConstantFermion,\\n        top_form_fermion: TopFormFermion,\\n        boson_momentum_conservation: FreeBosonConstantMode,\\n    };\\n\\n    /// Rule is one preset-authored zero-mode rule.\\n    pub const Rule = struct {\\n        sector: Sector,\\n        expr: Expr,\\n    };\\n\\n    /// rule constructs a zero-mode rule.\\n    pub fn rule(sector: Sector, expr: Expr) Rule {\\n        return .{ .sector = sector, .expr = expr };\\n    }\\n};\\n\\nconst PairLookupEntry = struct {\\n    key: u64,\\n    rule_index: u32,\\n    reversed: bool,\\n};\\n\\nconst BranchBudget = enum {\\n    standard,\\n};\\n\\nconst BranchLimits = struct {\\n    max_operators: usize,\\n    max_zero_mode_residuals: usize,\\n    max_zero_mode_items: usize,\\n    max_terms: usize,\\n    max_scalars: usize,\\n    max_coordinates: usize,\\n    max_tensors: usize,\\n    max_actions: usize,\\n    max_residuals: usize,\\n    max_cached_terms: usize,\\n    max_cached_scalars: usize,\\n    max_cached_coordinates: usize,\\n    max_cached_tensors: usize,\\n    max_cached_actions: usize,\\n    max_cached_residuals: usize,\\n};\\n\\nconst CorrelatorConfigInput = struct {\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry = &.{},\\n    fermion_kinds: []const operators.OperatorKindId = &.{},\\n    branch_budget: BranchBudget = .standard,\\n};\\n\\nconst CorrelatorConfigData = struct {\\n    wick_rules: []const wick.Rule,\\n    pair_lookup: []const PairLookupEntry,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry,\\n    fermion_kinds: []const operators.OperatorKindId,\\n};\\n\\n/// correlatorConfig creates an opaque public handle for lowered correlator data.\\nfn correlatorConfig(comptime input: CorrelatorConfigInput) type {\\n    return struct {\\n        const branch_limits = branchLimits(input);\\n        const pair_lookup_storage = pairLookupStorage(input.wick_rules);\\n        const impl = CorrelatorConfigData{\\n            .wick_rules = input.wick_rules,\\n            .pair_lookup = &pair_lookup_storage,\\n            .zero_modes = input.zero_modes,\\n            .config_entries = input.config_entries,\\n            .fermion_kinds = input.fermion_kinds,\\n        };\\n    };\\n}\\n\\nfn configData(config_ptr: anytype) *const CorrelatorConfigData {\\n    const Ptr = @TypeOf(config_ptr);\\n    const ptr_info = @typeInfo(Ptr);\\n    if (ptr_info != .pointer) @compileError(\\"correlator config must be passed by pointer\\");\\n    const Config = ptr_info.pointer.child;\\n    if (!@hasDecl(Config, \\"impl\\")) @compileError(\\"invalid correlator config handle\\");\\n    return &Config.impl;\\n}\\n\\nconst ConfigValue = union(enum) {\\n    tensor_projector: Handle.TensorProjector,\\n    target_point: Handle.TargetPoint,\\n    target_dimension: u16,\\n    boundary_stack: Handle.BoundaryStack,\\n};\\n\\nconst ConfigEntry = struct {\\n    id: ConfigRef,\\n    value: ConfigValue,\\n};\\n\\n/// config exposes typed constructors for private config payload entries.\\nconst ConfigDsl = struct {\\n    /// tensorProjector binds a tensor-projector handle to a config reference.\\n    pub fn tensorProjector(id: ConfigRef, value: Handle.TensorProjector) ConfigEntry {\\n        return .{ .id = id, .value = .{ .tensor_projector = value } };\\n    }\\n\\n    /// targetPoint binds a target-space point handle to a config reference.\\n    pub fn targetPoint(id: ConfigRef, value: Handle.TargetPoint) ConfigEntry {\\n        return .{ .id = id, .value = .{ .target_point = value } };\\n    }\\n\\n    /// targetDimension binds a target-space dimension to a config reference.\\n    pub fn targetDimension(id: ConfigRef, value: u16) ConfigEntry {\\n        return .{ .id = id, .value = .{ .target_dimension = value } };\\n    }\\n\\n    /// boundaryStack binds Chan-Paton boundary-stack data to a config reference.\\n    pub fn boundaryStack(id: ConfigRef, value: Handle.BoundaryStack) ConfigEntry {\\n        return .{ .id = id, .value = .{ .boundary_stack = value } };\\n    }\\n};\\n\\n/// configEntries packs typed config entries without exposing their carrier type.\\nfn configEntries(comptime entries: anytype) [@typeInfo(@TypeOf(entries)).@\\"struct\\".fields.len]ConfigEntry {\\n    var storage: [@typeInfo(@TypeOf(entries)).@\\"struct\\".fields.len]ConfigEntry = undefined;\\n    inline for (entries, 0..) |entry, index| {\\n        storage[index] = entry;\\n    }\\n    return storage;\\n}\\n\\nconst BoundaryExtensionInput = struct {\\n    op: type,\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry = &.{},\\n    fermion_kinds: []const operators.OperatorKindId = &.{},\\n};\\n\\nconst BoundaryExtensionData = struct {\\n    wick_rules: []const wick.Rule,\\n    zero_modes: []const zero_mode.Rule,\\n    config_entries: []const ConfigEntry,\\n    fermion_kinds: []const operators.OperatorKindId,\\n};\\n\\nconst LoweredSlice = enum {\\n    wick_rules,\\n    zero_modes,\\n    config_entries,\\n    fermion_kinds,\\n};\\n\\nfn correlatorSliceCount(config_ptr: anytype, comptime slice: LoweredSlice) usize {\\n    const data = configData(config_ptr);\\n    return switch (slice) {\\n        .wick_rules => data.wick_rules.len,\\n        .zero_modes => data.zero_modes.len,\\n        .config_entries => data.config_entries.len,\\n        .fermion_kinds => data.fermion_kinds.len,\\n    };\\n}\\n\\nfn appendCorrelatorSlice(config_ptr: anytype, comptime slice: LoweredSlice, storage: anytype, index: *usize) void {\\n    const data = configData(config_ptr);\\n    switch (slice) {\\n        .wick_rules => for (data.wick_rules) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .zero_modes => for (data.zero_modes) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .config_entries => for (data.config_entries) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .fermion_kinds => for (data.fermion_kinds) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n    }\\n}\\n\\n/// BoundaryExtension creates an opaque public handle for boundary preset data.\\nfn BoundaryExtension(comptime input: BoundaryExtensionInput) type {\\n    return struct {\\n        /// op exposes boundary-local operator builders for this extension.\\n        pub const op = input.op;\\n        const impl = BoundaryExtensionData{\\n            .wick_rules = input.wick_rules,\\n            .zero_modes = input.zero_modes,\\n            .config_entries = input.config_entries,\\n            .fermion_kinds = input.fermion_kinds,\\n        };\\n    };\\n}\\n\\nfn boundaryExtensionData(comptime extension: anytype) BoundaryExtensionData {\\n    const Extension = @TypeOf(extension);\\n    if (!@hasDecl(Extension, \\"impl\\")) @compileError(\\"invalid boundary extension handle\\");\\n    return Extension.impl;\\n}\\n\\nfn boundaryExtensionSliceCount(comptime extension: anytype, comptime slice: LoweredSlice) usize {\\n    const data = boundaryExtensionData(extension);\\n    return switch (slice) {\\n        .wick_rules => data.wick_rules.len,\\n        .zero_modes => data.zero_modes.len,\\n        .config_entries => data.config_entries.len,\\n        .fermion_kinds => data.fermion_kinds.len,\\n    };\\n}\\n\\nfn appendBoundaryExtensionSlice(comptime extension: anytype, comptime slice: LoweredSlice, storage: anytype, index: *usize) void {\\n    const data = boundaryExtensionData(extension);\\n    switch (slice) {\\n        .wick_rules => for (data.wick_rules) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .zero_modes => for (data.zero_modes) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .config_entries => for (data.config_entries) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n        .fermion_kinds => for (data.fermion_kinds) |item| {\\n            storage[index.*] = item;\\n            index.* += 1;\\n        },\\n    }\\n}\\n\\nfn factorHasSphereConfig(comptime Factor: type) bool {\\n    return @hasDecl(Factor, \\"config\\") and @hasDecl(Factor.config, \\"sphere\\");\\n}\\n\\nfn sphereConfigSliceCount(comptime factors: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    inline for (factors) |Factor| {\\n        if (factorHasSphereConfig(Factor)) count += correlatorSliceCount(&Factor.config.sphere, slice);\\n    }\\n    return count;\\n}\\n\\nfn appendSphereConfigSlice(comptime factors: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    inline for (factors) |Factor| {\\n        if (factorHasSphereConfig(Factor)) {\\n            appendCorrelatorSlice(&Factor.config.sphere, slice, storage, &index);\\n        }\\n    }\\n}\\n\\nfn mergedSphereWickStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .wick_rules)]wick.Rule {\\n    var storage: [sphereConfigSliceCount(factors, .wick_rules)]wick.Rule = undefined;\\n    appendSphereConfigSlice(factors, .wick_rules, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereZeroStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .zero_modes)]zero_mode.Rule {\\n    var storage: [sphereConfigSliceCount(factors, .zero_modes)]zero_mode.Rule = undefined;\\n    appendSphereConfigSlice(factors, .zero_modes, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereConfigStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .config_entries)]ConfigEntry {\\n    var storage: [sphereConfigSliceCount(factors, .config_entries)]ConfigEntry = undefined;\\n    appendSphereConfigSlice(factors, .config_entries, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedSphereFermionStorage(comptime factors: anytype) [sphereConfigSliceCount(factors, .fermion_kinds)]operators.OperatorKindId {\\n    var storage: [sphereConfigSliceCount(factors, .fermion_kinds)]operators.OperatorKindId = undefined;\\n    appendSphereConfigSlice(factors, .fermion_kinds, storage[0..]);\\n    return storage;\\n}\\n\\n/// mergeSphereConfigs builds one opaque sphere config from independent factors.\\nfn mergeSphereConfigs(comptime factors: anytype) type {\\n    return struct {\\n        const wick_storage = mergedSphereWickStorage(factors);\\n        const zero_storage = mergedSphereZeroStorage(factors);\\n        const config_storage = mergedSphereConfigStorage(factors);\\n        const fermion_storage = mergedSphereFermionStorage(factors);\\n\\n        /// config is the merged sphere correlator config handle.\\n        pub const config = correlatorConfig(.{\\n            .wick_rules = &wick_storage,\\n            .zero_modes = &zero_storage,\\n            .config_entries = &config_storage,\\n            .fermion_kinds = &fermion_storage,\\n        }){};\\n    };\\n}\\n\\nfn boundaryExtensionSliceTotal(comptime extensions: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    inline for (extensions) |extension| {\\n        count += boundaryExtensionSliceCount(extension, slice);\\n    }\\n    return count;\\n}\\n\\nfn boundaryConfigSliceCount(comptime Bulk: type, comptime extensions: anytype, comptime slice: LoweredSlice) usize {\\n    var count: usize = 0;\\n    if (factorHasSphereConfig(Bulk)) count += correlatorSliceCount(&Bulk.config.sphere, slice);\\n    inline for (extensions) |extension| {\\n        count += boundaryExtensionSliceCount(extension, slice);\\n    }\\n    return count;\\n}\\n\\nfn appendBoundaryExtensionSlices(comptime extensions: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    inline for (extensions) |extension| {\\n        appendBoundaryExtensionSlice(extension, slice, storage, &index);\\n    }\\n}\\n\\nfn appendBoundaryConfigSlices(comptime Bulk: type, comptime extensions: anytype, comptime slice: LoweredSlice, storage: anytype) void {\\n    var index: usize = 0;\\n    if (factorHasSphereConfig(Bulk)) {\\n        appendCorrelatorSlice(&Bulk.config.sphere, slice, storage, &index);\\n    }\\n    inline for (extensions) |extension| {\\n        appendBoundaryExtensionSlice(extension, slice, storage, &index);\\n    }\\n}\\n\\nfn mergedBoundaryWickStorage(comptime extensions: anytype) [boundaryExtensionSliceTotal(extensions, .wick_rules)]wick.Rule {\\n    var storage: [boundaryExtensionSliceTotal(extensions, .wick_rules)]wick.Rule = undefined;\\n    appendBoundaryExtensionSlices(extensions, .wick_rules, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryZeroStorage(comptime extensions: anytype) [boundaryExtensionSliceTotal(extensions, .zero_modes)]zero_mode.Rule {\\n    var storage: [boundaryExtensionSliceTotal(extensions, .zero_modes)]zero_mode.Rule = undefined;\\n    appendBoundaryExtensionSlices(extensions, .zero_modes, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryConfigStorage(comptime Bulk: type, comptime extensions: anytype) [boundaryConfigSliceCount(Bulk, extensions, .config_entries)]ConfigEntry {\\n    var storage: [boundaryConfigSliceCount(Bulk, extensions, .config_entries)]ConfigEntry = undefined;\\n    appendBoundaryConfigSlices(Bulk, extensions, .config_entries, storage[0..]);\\n    return storage;\\n}\\n\\nfn mergedBoundaryFermionStorage(comptime Bulk: type, comptime extensions: anytype) [boundaryConfigSliceCount(Bulk, extensions, .fermion_kinds)]operators.OperatorKindId {\\n    var storage: [boundaryConfigSliceCount(Bulk, extensions, .fermion_kinds)]operators.OperatorKindId = undefined;\\n    appendBoundaryConfigSlices(Bulk, extensions, .fermion_kinds, storage[0..]);\\n    return storage;\\n}\\n\\n/// mergeBoundaryConfig builds one opaque disk config from bulk data and extensions.\\nfn mergeBoundaryConfig(comptime Bulk: type, comptime extensions: anytype) type {\\n    return struct {\\n        const wick_storage = mergedBoundaryWickStorage(extensions);\\n        const zero_storage = mergedBoundaryZeroStorage(extensions);\\n        const config_storage = mergedBoundaryConfigStorage(Bulk, extensions);\\n        const fermion_storage = mergedBoundaryFermionStorage(Bulk, extensions);\\n\\n        /// config is the merged disk correlator config handle.\\n        pub const config = correlatorConfig(.{\\n            .wick_rules = &wick_storage,\\n            .zero_modes = &zero_storage,\\n            .config_entries = &config_storage,\\n            .fermion_kinds = &fermion_storage,\\n        }){};\\n    };\\n}\\n\\nconst DeclareSpec = Spec;\\nconst DeclareWick = wick;\\nconst DeclareZeroMode = zero_mode;\\nconst DeclareScalars = scalars;\\nconst DeclareConfig = ConfigDsl;\\nconst declareConfigRef = configRef;\\nconst declareFamilyKind = familyKind;\\nconst declareSectorId = sectorId;\\nconst declareOperatorBuilder = operatorBuilder;\\nconst declareCorrelatorConfig = correlatorConfig;\\nconst declareBoundaryExtension = BoundaryExtension;\\nconst declareConfigEntries = configEntries;\\nconst declareMergeSphereConfigs = mergeSphereConfigs;\\nconst declareMergeBoundaryConfig = mergeBoundaryConfig;\\n\\n/// declare exposes preset declaration lowering without leaking individual internals.\\npub const declare = struct {\\n    /// Spec stores declarative metadata before lowering to runtime tables.\\n    pub const Spec = DeclareSpec;\\n    /// wick exposes compact preset-author helpers for Wick rules.\\n    pub const wick = DeclareWick;\\n    /// zero_mode exposes compact preset-author helpers for zero-mode rules.\\n    pub const zero_mode = DeclareZeroMode;\\n    /// scalars exposes exact scalar helpers for preset rule coefficients.\\n    pub const scalars = DeclareScalars;\\n    /// config exposes typed constructors for private config payload entries.\\n    pub const config = DeclareConfig;\\n\\n    /// configRef derives a compact config id from a namespace-local name.\\n    pub const configRef = declareConfigRef;\\n    /// familyKind derives a compact operator kind id from a preset family tag.\\n    pub const familyKind = declareFamilyKind;\\n    /// sectorId derives a compact zero-mode sector id from a preset sector tag.\\n    pub const sectorId = declareSectorId;\\n    /// operatorBuilder generates insertion and label packing from one operator schema.\\n    pub const operatorBuilder = declareOperatorBuilder;\\n    /// correlatorConfig creates an opaque public handle for lowered correlator data.\\n    pub const correlatorConfig = declareCorrelatorConfig;\\n    /// BoundaryExtension creates an opaque public handle for boundary preset data.\\n    pub const BoundaryExtension = declareBoundaryExtension;\\n    /// configEntries packs typed config entries without exposing their carrier type.\\n    pub const configEntries = declareConfigEntries;\\n    /// mergeSphereConfigs builds one opaque sphere config from independent factors.\\n    pub const mergeSphereConfigs = declareMergeSphereConfigs;\\n    /// mergeBoundaryConfig builds one opaque disk config from bulk data and extensions.\\n    pub const mergeBoundaryConfig = declareMergeBoundaryConfig;\\n};\\n\\nfn wickRuleKey(left_kind: operators.OperatorKindId, right_kind: operators.OperatorKindId) u64 {\\n    return (@as(u64, left_kind) << 32) | @as(u64, right_kind);\\n}\\n\\nfn pairLookupEntryCount(comptime rules: []const wick.Rule) usize {\\n    var count: usize = 0;\\n    for (rules) |rule| {\\n        count += 1;\\n        if (rule.left.kind != rule.right.kind) count += 1;\\n    }\\n    return count;\\n}\\n\\nfn sortPairLookup(entries: []PairLookupEntry) void {\\n    var index: usize = 1;\\n    while (index < entries.len) : (index += 1) {\\n        const value = entries[index];\\n        var hole = index;\\n        while (hole > 0 and entries[hole - 1].key > value.key) : (hole -= 1) {\\n            entries[hole] = entries[hole - 1];\\n        }\\n        entries[hole] = value;\\n    }\\n}\\n\\nfn pairLookupStorage(comptime rules: []const wick.Rule) [pairLookupEntryCount(rules)]PairLookupEntry {\\n    var entries: [pairLookupEntryCount(rules)]PairLookupEntry = undefined;\\n    var entry_index: usize = 0;\\n\\n    for (rules, 0..) |rule, rule_index| {\\n        if (rule_index > std.math.maxInt(u32)) @compileError(\\"too many Wick rules for compact rule index\\");\\n        entries[entry_index] = .{\\n            .key = wickRuleKey(rule.left.kind, rule.right.kind),\\n            .rule_index = @intCast(rule_index),\\n            .reversed = false,\\n        };\\n        entry_index += 1;\\n\\n        if (rule.left.kind != rule.right.kind) {\\n            entries[entry_index] = .{\\n                .key = wickRuleKey(rule.right.kind, rule.left.kind),\\n                .rule_index = @intCast(rule_index),\\n                .reversed = true,\\n            };\\n            entry_index += 1;\\n        }\\n    }\\n\\n    sortPairLookup(entries[0..]);\\n    return entries;\\n}\\n\\nconst Correlator = struct {\\n    /// WickPair is one oriented primitive Wick contraction between two concrete local operators.\\n    const WickPair = struct {\\n        rule: *const wick.Rule,\\n        config: *const CorrelatorConfigData,\\n        left: kernel.Call.LocalOp,\\n        right: kernel.Call.LocalOp,\\n        labels: *const kernel.Call.LabelStore,\\n        reversed: bool,\\n\\n        fn sideOp(self: WickPair, side: wick.Side) kernel.Call.LocalOp {\\n            return switch (side) {\\n                .left => if (self.reversed) self.right else self.left,\\n                .right => if (self.reversed) self.left else self.right,\\n            };\\n        }\\n\\n        fn coordinate(self: WickPair, coordinate_ref: wick.CoordinateRef) ?coefficient.Variable {\\n            const op = self.sideOp(coordinate_ref.side);\\n            return switch (op.insertion) {\\n                .single => |single| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => single.position,\\n                    .antiholomorphic => null,\\n                },\\n                .pair => |pair| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => pair.holomorphic_position,\\n                    .antiholomorphic => pair.antiholomorphic_position,\\n                },\\n            };\\n        }\\n\\n        fn derivativeCount(self: WickPair, coordinate_ref: wick.CoordinateRef) ?u8 {\\n            const op = self.sideOp(coordinate_ref.side);\\n            return switch (op.insertion) {\\n                .single => |single| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => single.derivatives,\\n                    .antiholomorphic => null,\\n                },\\n                .pair => |pair| switch (coordinate_ref.slot) {\\n                    .position, .holomorphic => pair.holomorphic_derivatives,\\n                    .antiholomorphic => pair.antiholomorphic_derivatives,\\n                },\\n            };\\n        }\\n\\n        fn label(self: WickPair, label_ref: wick.LabelRef) ?kernel.Call.LabelValue {\\n            const op = self.sideOp(label_ref.side);\\n            const index: usize = @as(usize, @intCast(op.labels)) + label_ref.slot;\\n            if (index >= self.labels.values.len) return null;\\n            return self.labels.values[index];\\n        }\\n\\n        fn configValue(self: WickPair, config_ref: ConfigRef) ?ConfigValue {\\n            for (self.config.config_entries) |entry| {\\n                if (entry.id == config_ref) return entry.value;\\n            }\\n            return null;\\n        }\\n    };\\n\\n    /// WickResidualRef points to one surviving original input occurrence.\\n    const WickResidualRef = struct {\\n        input_index: usize,\\n        operator: kernel.Call.LocalOp,\\n    };\\n\\n    /// ResolvedCoordinateRef is one concrete coordinate variable with its derivative count.\\n    const ResolvedCoordinateRef = struct {\\n        variable: coefficient.Variable,\\n        derivatives: u8,\\n    };\\n\\n    /// ResolvedCoordinateDifference is a concrete coordinate difference read by a Wick factor.\\n    const ResolvedCoordinateDifference = struct {\\n        left: ResolvedCoordinateRef,\\n        right: ResolvedCoordinateRef,\\n    };\\n\\n    /// ResolvedDerivativeAction records the derivative counts requested by one coordinate factor.\\n    const ResolvedDerivativeAction = struct {\\n        left: ?u8 = null,\\n        right: ?u8 = null,\\n    };\\n\\n    /// ResolvedTensorRef is tensor data resolved either from an operator label or config entry.\\n    const ResolvedTensorRef = union(enum) {\\n        label: kernel.Call.LabelValue,\\n        config: ConfigValue,\\n    };\\n\\n    /// ResolvedScalarFactor is a scalar factor with all label references plugged in.\\n    const ResolvedScalarFactor = union(enum) {\\n        value: RuleScalar,\\n        label_bilinear_phase: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue, form: []const u8 },\\n        cocycle: struct { table: []const u8, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        spin_structure: []const u8,\\n    };\\n\\n    /// ResolvedTensorFactor is a tensor multiplier with concrete labels and config values.\\n    const ResolvedTensorFactor = union(enum) {\\n        none,\\n        metric: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        momentum_index: struct { momentum: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        momentum_pair: struct { left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        projector_metric: struct { projector: ResolvedTensorRef, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n        projector_momentum_index: struct { projector: ResolvedTensorRef, momentum: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        projector_momentum_pair: struct { projector: ResolvedTensorRef, left: kernel.Call.LabelValue, right: kernel.Call.LabelValue },\\n    };\\n\\n    /// ResolvedActionFactor is a Wick action with concrete profile, index, and projector data.\\n    const ResolvedActionFactor = union(enum) {\\n        profile_derivative: struct { profile: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n        projected_profile_derivative: struct { projector: ResolvedTensorRef, profile: kernel.Call.LabelValue, index: kernel.Call.LabelValue },\\n    };\\n\\n    /// ResolvedCoordinateFactor is a coordinate kernel with concrete coordinates and derivative data.\\n    const ResolvedCoordinateFactor = union(enum) {\\n        difference_power: struct { coordinate: ResolvedCoordinateDifference, exponent: i16, derivatives: ResolvedDerivativeAction },\\n        logarithm: struct { coordinate: ResolvedCoordinateDifference, derivatives: ResolvedDerivativeAction },\\n        green_kernel: struct { name: []const u8, coordinate: ResolvedCoordinateDifference, derivatives: ResolvedDerivativeAction },\\n        green_exponential: ResolvedCoordinateDifference,\\n    };\\n\\n    /// ResolvedTerm is one Wick term whose factors can be resolved without allocation.\\n    const ResolvedTerm = struct {\\n        pair: WickPair,\\n        source: *const wick.Term,\\n\\n        fn scalarCount(self: ResolvedTerm) usize {\\n            return self.source.scalars.len;\\n        }\\n\\n        fn coordinateCount(self: ResolvedTerm) usize {\\n            return self.source.coordinates.len;\\n        }\\n\\n        fn tensorCount(self: ResolvedTerm) usize {\\n            return self.source.tensors.len;\\n        }\\n\\n        fn actionCount(self: ResolvedTerm) usize {\\n            return self.source.actions.len;\\n        }\\n\\n        fn residualCount(self: ResolvedTerm) usize {\\n            return self.source.residuals.len;\\n        }\\n\\n        fn scalar(self: ResolvedTerm, index: usize) ?ResolvedScalarFactor {\\n            if (index >= self.source.scalars.len) return null;\\n            return resolveScalarFactor(self.pair, self.source.scalars[index]);\\n        }\\n\\n        fn coordinate(self: ResolvedTerm, index: usize) ?ResolvedCoordinateFactor {\\n            if (index >= self.source.coordinates.len) return null;\\n            return resolveCoordinateFactor(self.pair, self.source.coordinates[index]);\\n        }\\n\\n        fn tensor(self: ResolvedTerm, index: usize) ?ResolvedTensorFactor {\\n            if (index >= self.source.tensors.len) return null;\\n            return resolveTensorFactor(self.pair, self.source.tensors[index]);\\n        }\\n\\n        fn action(self: ResolvedTerm, index: usize) ?ResolvedActionFactor {\\n            if (index >= self.source.actions.len) return null;\\n            return resolveActionFactor(self.pair, self.source.actions[index]);\\n        }\\n\\n        fn residual(self: ResolvedTerm, index: usize) ?wick.Side {\\n            if (index >= self.source.residuals.len) return null;\\n            return self.source.residuals[index];\\n        }\\n    };\\n\\n    /// ResolvedWickPair gives allocation-free access to concrete Wick terms for one pair.\\n    const ResolvedWickPair = struct {\\n        pair: WickPair,\\n\\n        fn termCount(self: ResolvedWickPair) usize {\\n            return self.pair.rule.expr.terms.len;\\n        }\\n\\n        fn term(self: ResolvedWickPair, index: usize) ?ResolvedTerm {\\n            if (index >= self.pair.rule.expr.terms.len) return null;\\n            return .{ .pair = self.pair, .source = &self.pair.rule.expr.terms[index] };\\n        }\\n    };\\n\\n    /// PrimitiveWickTerm is one complete primitive Wick contribution without later simplification.\\n    const PrimitiveWickTerm = struct {\\n        resolved: ResolvedTerm,\\n\\n        fn scalarCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.scalarCount();\\n        }\\n\\n        fn coordinateCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.coordinateCount();\\n        }\\n\\n        fn tensorCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.tensorCount();\\n        }\\n\\n        fn actionCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.actionCount();\\n        }\\n\\n        fn residualCount(self: PrimitiveWickTerm) usize {\\n            return self.resolved.residualCount();\\n        }\\n\\n        fn scalar(self: PrimitiveWickTerm, index: usize) ?ResolvedScalarFactor {\\n            return self.resolved.scalar(index);\\n        }\\n\\n        fn coordinate(self: PrimitiveWickTerm, index: usize) ?coefficient.CoordinateFactor {\\n            const factor = self.resolved.coordinate(index) orelse return null;\\n            return evaluateCoordinateFactor(factor);\\n        }\\n\\n        fn tensor(self: PrimitiveWickTerm, index: usize) ?ResolvedTensorFactor {\\n            return self.resolved.tensor(index);\\n        }\\n\\n        fn action(self: PrimitiveWickTerm, index: usize) ?ResolvedActionFactor {\\n            return self.resolved.action(index);\\n        }\\n\\n        fn residual(self: PrimitiveWickTerm, index: usize) ?wick.Side {\\n            return self.resolved.residual(index);\\n        }\\n    };\\n\\n    /// PrimitiveWickPair gives allocation-free complete terms for one primitive Wick pair.\\n    const PrimitiveWickPair = struct {\\n        pair: WickPair,\\n\\n        fn termCount(self: PrimitiveWickPair) usize {\\n            return self.pair.rule.expr.terms.len;\\n        }\\n\\n        fn term(self: PrimitiveWickPair, index: usize) ?PrimitiveWickTerm {\\n            const resolved = (ResolvedWickPair{ .pair = self.pair }).term(index) orelse return null;\\n            return .{ .resolved = resolved };\\n        }\\n    };\\n\\n    fn resolveCoordinateRef(pair: WickPair, coordinate_ref: wick.CoordinateRef) ?ResolvedCoordinateRef {\\n        return .{\\n            .variable = pair.coordinate(coordinate_ref) orelse return null,\\n            .derivatives = pair.derivativeCount(coordinate_ref) orelse return null,\\n        };\\n    }\\n\\n    fn resolveCoordinateDifference(pair: WickPair, difference: wick.CoordinateDifference) ?ResolvedCoordinateDifference {\\n        return .{\\n            .left = resolveCoordinateRef(pair, difference.left) orelse return null,\\n            .right = resolveCoordinateRef(pair, difference.right) orelse return null,\\n        };\\n    }\\n\\n    fn resolveDerivativeAction(pair: WickPair, coordinate: wick.CoordinateDifference, action: wick.DerivativeAction) ?ResolvedDerivativeAction {\\n        return .{\\n            .left = if (action.include_left) pair.derivativeCount(coordinate.left) orelse return null else null,\\n            .right = if (action.include_right) pair.derivativeCount(coordinate.right) orelse return null else null,\\n        };\\n    }\\n\\n    fn resolveTensorRef(pair: WickPair, tensor_ref: wick.TensorRef) ?ResolvedTensorRef {\\n        return switch (tensor_ref) {\\n            .label => |label_ref| .{ .label = pair.label(label_ref) orelse return null },\\n            .config => |config_ref| .{ .config = pair.configValue(config_ref) orelse return null },\\n        };\\n    }\\n\\n    fn resolveScalarFactor(pair: WickPair, factor: wick.ScalarFactor) ?ResolvedScalarFactor {\\n        return switch (factor) {\\n            .value => |value| .{ .value = value },\\n            .label_bilinear_phase => |phase| .{ .label_bilinear_phase = .{\\n                .left = pair.label(phase.left) orelse return null,\\n                .right = pair.label(phase.right) orelse return null,\\n                .form = phase.form,\\n            } },\\n            .cocycle => |cocycle| .{ .cocycle = .{\\n                .table = cocycle.table,\\n                .left = pair.label(cocycle.left) orelse return null,\\n                .right = pair.label(cocycle.right) orelse return null,\\n            } },\\n            .spin_structure => |spin_structure| .{ .spin_structure = spin_structure },\\n        };\\n    }\\n\\n    fn resolveTensorFactor(pair: WickPair, factor: wick.TensorFactor) ?ResolvedTensorFactor {\\n        return switch (factor) {\\n            .none => .none,\\n            .metric => |metric| .{ .metric = .{\\n                .left = pair.label(metric.left) orelse return null,\\n                .right = pair.label(metric.right) orelse return null,\\n            } },\\n            .momentum_index => |item| .{ .momentum_index = .{\\n                .momentum = pair.label(item.momentum) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .momentum_pair => |item| .{ .momentum_pair = .{\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n            .projector_metric => |item| .{ .projector_metric = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n            .projector_momentum_index => |item| .{ .projector_momentum_index = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .momentum = pair.label(item.momentum) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .projector_momentum_pair => |item| .{ .projector_momentum_pair = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .left = pair.label(item.left) orelse return null,\\n                .right = pair.label(item.right) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn resolveActionFactor(pair: WickPair, factor: wick.ActionFactor) ?ResolvedActionFactor {\\n        return switch (factor) {\\n            .profile_derivative => |item| .{ .profile_derivative = .{\\n                .profile = pair.label(item.profile) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n            .projected_profile_derivative => |item| .{ .projected_profile_derivative = .{\\n                .projector = resolveTensorRef(pair, item.projector) orelse return null,\\n                .profile = pair.label(item.profile) orelse return null,\\n                .index = pair.label(item.index) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn resolveCoordinateFactor(pair: WickPair, factor: wick.CoordinateFactor) ?ResolvedCoordinateFactor {\\n        return switch (factor) {\\n            .difference_power => |item| .{ .difference_power = .{\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .exponent = item.exponent,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .logarithm => |item| .{ .logarithm = .{\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .green_kernel => |item| .{ .green_kernel = .{\\n                .name = item.name,\\n                .coordinate = resolveCoordinateDifference(pair, item.coordinate) orelse return null,\\n                .derivatives = resolveDerivativeAction(pair, item.coordinate, item.derivatives) orelse return null,\\n            } },\\n            .green_exponential => |coordinate| .{ .green_exponential = resolveCoordinateDifference(pair, coordinate) orelse return null },\\n        };\\n    }\\n\\n    fn coordinateDifference(coordinate: ResolvedCoordinateDifference) coefficient.CoordinateDifference {\\n        return .{\\n            .left = coordinate.left.variable,\\n            .right = coordinate.right.variable,\\n        };\\n    }\\n\\n    fn selectedDerivativeCount(action: ResolvedDerivativeAction) u16 {\\n        const left: u16 = if (action.left) |count| count else 0;\\n        const right: u16 = if (action.right) |count| count else 0;\\n        return left + right;\\n    }\\n\\n    fn selectedRightDerivativeCount(action: ResolvedDerivativeAction) u16 {\\n        return if (action.right) |count| count else 0;\\n    }\\n\\n    fn checkedMulInt(left: i64, right: i64) ?i64 {\\n        const result = @mulWithOverflow(left, right);\\n        if (result[1] != 0) return null;\\n        return result[0];\\n    }\\n\\n    fn checkedAddExponent(exponent: i16, derivative_count: u16) ?i16 {\\n        const next = @as(i32, exponent) - @as(i32, derivative_count);\\n        if (next < std.math.minInt(i16) or next > std.math.maxInt(i16)) return null;\\n        return @intCast(next);\\n    }\\n\\n    fn fallingPower(exponent: i16, derivative_count: u16) ?i64 {\\n        var value: i64 = 1;\\n        var current: i64 = exponent;\\n        var index: u16 = 0;\\n        while (index < derivative_count) : (index += 1) {\\n            value = checkedMulInt(value, current) orelse return null;\\n            current -= 1;\\n        }\\n        return value;\\n    }\\n\\n    fn factorial(value: u16) ?i64 {\\n        var result: i64 = 1;\\n        var current: u16 = 2;\\n        while (current <= value) : (current += 1) {\\n            result = checkedMulInt(result, current) orelse return null;\\n        }\\n        return result;\\n    }\\n\\n    fn signFromParity(power: u16) i64 {\\n        return if ((power & 1) == 0) 1 else -1;\\n    }\\n\\n    fn evaluateDifferencePower(item: anytype) ?coefficient.CoordinateFactor {\\n        const derivative_count = selectedDerivativeCount(item.derivatives);\\n        const right_count = selectedRightDerivativeCount(item.derivatives);\\n        const falling = fallingPower(item.exponent, derivative_count) orelse return null;\\n        const signed = checkedMulInt(signFromParity(right_count), falling) orelse return null;\\n        return .{\\n            .scalar = coefficient.rational(signed, 1) orelse return null,\\n            .kernel = .{ .difference_power = .{\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .exponent = checkedAddExponent(item.exponent, derivative_count) orelse return null,\\n            } },\\n        };\\n    }\\n\\n    fn evaluateLogarithm(item: anytype) ?coefficient.CoordinateFactor {\\n        const derivative_count = selectedDerivativeCount(item.derivatives);\\n        if (derivative_count == 0) {\\n            return .{\\n                .scalar = coefficient.integer(1),\\n                .kernel = .{ .logarithm = coordinateDifference(item.coordinate) },\\n            };\\n        }\\n\\n        const right_count = selectedRightDerivativeCount(item.derivatives);\\n        const base = factorial(derivative_count - 1) orelse return null;\\n        const signed = checkedMulInt(signFromParity(right_count + derivative_count - 1), base) orelse return null;\\n        return .{\\n            .scalar = coefficient.rational(signed, 1) orelse return null,\\n            .kernel = .{ .difference_power = .{\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .exponent = -@as(i16, @intCast(derivative_count)),\\n            } },\\n        };\\n    }\\n\\n    fn evaluateGreenKernel(item: anytype) coefficient.CoordinateFactor {\\n        return .{\\n            .scalar = coefficient.integer(1),\\n            .kernel = .{ .green_kernel = .{\\n                .name = item.name,\\n                .coordinate = coordinateDifference(item.coordinate),\\n                .left_derivatives = if (item.derivatives.left) |count| count else 0,\\n                .right_derivatives = if (item.derivatives.right) |count| count else 0,\\n            } },\\n        };\\n    }\\n\\n    fn evaluateCoordinateFactor(factor: ResolvedCoordinateFactor) ?coefficient.CoordinateFactor {\\n        return switch (factor) {\\n            .difference_power => |item| evaluateDifferencePower(item),\\n            .logarithm => |item| evaluateLogarithm(item),\\n            .green_kernel => |item| evaluateGreenKernel(item),\\n            .green_exponential => |coordinate| .{\\n                .scalar = coefficient.integer(1),\\n                .kernel = .{ .green_exponential = coordinateDifference(coordinate) },\\n            },\\n        };\\n    }\\n\\n    const WickTermEvent = struct {\\n        left_index: usize,\\n        right_index: usize,\\n        term_index: usize,\\n    };\\n\\n    const MatchedWickTerm = struct {\\n        pair: WickPair,\\n        rule_index: usize,\\n        term_index: usize,\\n    };\\n};\\n\\nconst ZeroModeRuntime = struct {\\n    const ResidualCursor = struct {\\n        ops: kernel.Call.MultiOp,\\n        indices: ?[]const usize,\\n\\n        fn len(self: ResidualCursor) usize {\\n            return if (self.indices) |indices| indices.len else self.ops.operators.len;\\n        }\\n\\n        fn opIndex(self: ResidualCursor, residual_index: usize) usize {\\n            return if (self.indices) |indices| indices[residual_index] else residual_index;\\n        }\\n\\n        fn op(self: ResidualCursor, residual_index: usize) kernel.Call.LocalOp {\\n            return self.ops.operators[self.opIndex(residual_index)];\\n        }\\n    };\\n\\n    const CJet = struct {\\n        coordinate: coefficient.Variable,\\n        derivative_order: u8,\\n    };\\n\\n    const MomentumDelta = struct {\\n        projector: ?ConfigValue,\\n        momenta: []const kernel.Call.LabelValue,\\n        two_pi_power: u16,\\n        scalar: RuleScalar,\\n    };\\n\\n    const DirichletPhase = struct {\\n        projector: ConfigValue,\\n        position: ConfigValue,\\n        momenta: []const kernel.Call.LabelValue,\\n    };\\n\\n    const ProfileFactor = struct {\\n        profile: kernel.Call.LabelValue,\\n        coordinate: coefficient.Variable,\\n    };\\n\\n    const ZeroModeFactor = union(enum) {\\n        bc_top_form: struct {\\n            support: zero_mode.BcSupport,\\n            normalization: RuleScalar,\\n            jets: [3]CJet,\\n        },\\n        eta_xi_zero_mode: struct {\\n            support: zero_mode.EtaXiSupport,\\n            normalization: RuleScalar,\\n            xi: CJet,\\n        },\\n        momentum_delta: MomentumDelta,\\n        dirichlet_phase: DirichletPhase,\\n        profile_fourier_integral: ProfileFactor,\\n        profile_polynomial_integral: ProfileFactor,\\n        profile_gaussian_differential: ProfileFactor,\\n    };\\n};\\n\\nconst TextBudget = enum {\\n    small,\\n    medium,\\n};\\n\\n/// text exposes bounded result-inspection sinks.\\npub const text = struct {\\n    /// compact returns a writer-backed sink for small symbolic correlators.\\n    pub fn compact(writer: anytype, comptime budget: TextBudget) CompactTextSink(@TypeOf(writer), budget) {\\n        return .{ .writer = writer };\\n    }\\n};\\n\\nfn textByteLimit(comptime budget: TextBudget) usize {\\n    return switch (budget) {\\n        .small => 4096,\\n        .medium => 65536,\\n    };\\n}\\n\\nfn textBranchLimit(comptime budget: TextBudget) usize {\\n    return switch (budget) {\\n        .small => 32,\\n        .medium => 1024,\\n    };\\n}\\n\\nfn CompactTextSink(comptime Writer: type, comptime budget: TextBudget) type {\\n    return struct {\\n        const Self = @This();\\n\\n        writer: Writer,\\n        bytes_written: usize = 0,\\n        branch_count: usize = 0,\\n        branch_open: bool = false,\\n        branch_has_factor: bool = false,\\n        term_open: bool = false,\\n        term_has_factor: bool = false,\\n        pending_sign: i8 = 1,\\n\\n        fn writeAll(self: *Self, bytes: []const u8) !void {\\n            if (self.bytes_written + bytes.len > textByteLimit(budget)) return error.RenderLimitExceeded;\\n            try self.writer.writeAll(bytes);\\n            self.bytes_written += bytes.len;\\n        }\\n\\n        fn writeFmt(self: *Self, comptime fmt: []const u8, args: anytype) !void {\\n            var buffer: [256]u8 = undefined;\\n            const rendered = std.fmt.bufPrint(&buffer, fmt, args) catch return error.RenderLimitExceeded;\\n            try self.writeAll(rendered);\\n        }\\n\\n        fn beginBranch(self: *Self) !void {\\n            if (self.branch_open) return;\\n            if (self.branch_count == textBranchLimit(budget)) return error.RenderLimitExceeded;\\n            if (self.branch_count != 0) {\\n                try self.writeAll(if (self.pending_sign < 0) \\" - \\" else \\" + \\");\\n            } else if (self.pending_sign < 0) {\\n                try self.writeAll(\\"-\\");\\n            }\\n            self.branch_open = true;\\n            self.branch_has_factor = false;\\n            self.pending_sign = 1;\\n        }\\n\\n        fn factorSeparator(self: *Self) !void {\\n            try self.beginBranch();\\n            if (self.term_open) {\\n                if (self.term_has_factor) try self.writeAll(\\"*\\");\\n                self.term_has_factor = true;\\n                return;\\n            }\\n            if (self.branch_has_factor) try self.writeAll(\\"*\\");\\n            self.branch_has_factor = true;\\n        }\\n\\n        fn writeRational(self: *Self, value: RationalScalar) !void {\\n            if (value.denominator == 1) return self.writeFmt(\\"{}\\", .{value.numerator});\\n            return self.writeFmt(\\"{}/{}\\", .{ value.numerator, value.denominator });\\n        }\\n\\n        fn writeRuleScalar(self: *Self, value: RuleScalar) !void {\\n            switch (value) {\\n                .one => try self.writeAll(\\"1\\"),\\n                .rational => |rational| try self.writeRational(rational),\\n                .monomial => |monomial| {\\n                    try self.writeRational(monomial.rational);\\n                    if (monomial.imaginary_power != 0) try self.writeFmt(\\"*i^{}\\", .{monomial.imaginary_power});\\n                    if (monomial.atom) |atom| try self.writeFmt(\\"*s{}^{}\\", .{ atom, monomial.atom_power });\\n                },\\n            }\\n        }\\n\\n        fn writeLabel(self: *Self, value: kernel.Call.LabelValue) !void {\\n            switch (value) {\\n                .integer => |item| try self.writeFmt(\\"{}\\", .{item}),\\n                .rational => |item| try self.writeFmt(\\"{}/{}\\", .{ item.numerator, item.denominator }),\\n                .tensor => |item| try self.writeFmt(\\"T{}\\", .{item}),\\n                .symbol => |item| try self.writeFmt(\\"q{}\\", .{item}),\\n            }\\n        }\\n\\n        fn writeConfig(self: *Self, value: ConfigValue) !void {\\n            switch (value) {\\n                .tensor_projector => |item| try self.writeFmt(\\"P{}\\", .{@intFromEnum(item)}),\\n                .target_point => |item| try self.writeFmt(\\"x{}\\", .{@intFromEnum(item)}),\\n                .target_dimension => |item| try self.writeFmt(\\"D{}\\", .{item}),\\n                .boundary_stack => |item| try self.writeFmt(\\"CP{}\\", .{@intFromEnum(item)}),\\n            }\\n        }\\n\\n        fn writeTensorRef(self: *Self, value: Correlator.ResolvedTensorRef) !void {\\n            switch (value) {\\n                .label => |item| try self.writeLabel(item),\\n                .config => |item| try self.writeConfig(item),\\n            }\\n        }\\n\\n        fn writeCoordinateDifference(self: *Self, value: coefficient.CoordinateDifference) !void {\\n            try self.writeFmt(\\"(z{}-z{})\\", .{ value.left, value.right });\\n        }\\n\\n        pub fn emitWickBranchSign(self: *Self, sign: i8) !void {\\n            if (sign < 0) self.pending_sign = -1;\\n        }\\n\\n        pub fn emitWickBranchTerm(self: *Self, branch: anytype) !void {\\n            const Proxy = struct {\\n                renderer: *Self,\\n\\n                pub fn emitWickTermStart(proxy: *@This(), event: Correlator.WickTermEvent) !void {\\n                    try proxy.renderer.emitWickTermStart(event);\\n                }\\n\\n                pub fn emitWickScalar(proxy: *@This(), factor: Correlator.ResolvedScalarFactor) !void {\\n                    try proxy.renderer.emitWickScalar(factor);\\n                }\\n\\n                pub fn emitWickCoordinate(proxy: *@This(), factor: coefficient.CoordinateFactor) !void {\\n                    try proxy.renderer.emitWickCoordinate(factor);\\n                }\\n\\n                pub fn emitWickTensor(proxy: *@This(), factor: Correlator.ResolvedTensorFactor) !void {\\n                    try proxy.renderer.emitWickTensor(factor);\\n                }\\n\\n                pub fn emitWickAction(proxy: *@This(), factor: Correlator.ResolvedActionFactor) !void {\\n                    try proxy.renderer.emitWickAction(factor);\\n                }\\n\\n                pub fn emitWickResidualOperator(proxy: *@This(), residual: Correlator.WickResidualRef) !void {\\n                    try proxy.renderer.emitWickResidualOperator(residual);\\n                }\\n\\n                pub fn emitWickTermEnd(proxy: *@This()) !void {\\n                    try proxy.renderer.emitWickTermEnd();\\n                }\\n            };\\n\\n            var proxy = Proxy{ .renderer = self };\\n            try branch.emit(&proxy);\\n        }\\n\\n        pub fn emitWickTermStart(self: *Self, _: Correlator.WickTermEvent) !void {\\n            try self.factorSeparator();\\n            try self.writeAll(\\"(\\");\\n            self.term_open = true;\\n            self.term_has_factor = false;\\n        }\\n\\n        pub fn emitWickScalar(self: *Self, factor: Correlator.ResolvedScalarFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .value => |value| try self.writeRuleScalar(value),\\n                .label_bilinear_phase => |item| {\\n                    try self.writeFmt(\\"phase[{s}](\\", .{item.form});\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .cocycle => |item| {\\n                    try self.writeFmt(\\"cocycle[{s}](\\", .{item.table});\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .spin_structure => |name| try self.writeFmt(\\"spin[{s}]\\", .{name}),\\n            }\\n        }\\n\\n        pub fn emitWickCoordinate(self: *Self, factor: coefficient.CoordinateFactor) !void {\\n            try self.factorSeparator();\\n            if (factor.scalar.numerator != 1 or factor.scalar.denominator != 1) {\\n                try self.writeFmt(\\"{}/{}*\\", .{ factor.scalar.numerator, factor.scalar.denominator });\\n            }\\n            switch (factor.kernel) {\\n                .difference_power => |item| {\\n                    try self.writeCoordinateDifference(item.coordinate);\\n                    try self.writeFmt(\\"^{}\\", .{item.exponent});\\n                },\\n                .logarithm => |coordinate| {\\n                    try self.writeAll(\\"log\\");\\n                    try self.writeCoordinateDifference(coordinate);\\n                },\\n                .green_kernel => |item| {\\n                    try self.writeFmt(\\"G[{s};{},{}]\\", .{ item.name, item.left_derivatives, item.right_derivatives });\\n                    try self.writeCoordinateDifference(item.coordinate);\\n                },\\n                .green_exponential => |coordinate| {\\n                    try self.writeAll(\\"expG\\");\\n                    try self.writeCoordinateDifference(coordinate);\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickTensor(self: *Self, factor: Correlator.ResolvedTensorFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .none => try self.writeAll(\\"1\\"),\\n                .metric => |item| {\\n                    try self.writeAll(\\"eta(\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .momentum_index => |item| {\\n                    try self.writeAll(\\"p(\\");\\n                    try self.writeLabel(item.momentum);\\n                    try self.writeAll(\\"; \\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .momentum_pair => |item| {\\n                    try self.writeAll(\\"p(\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\").p(\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_metric => |item| {\\n                    try self.writeAll(\\"eta[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_momentum_index => |item| {\\n                    try self.writeAll(\\"p[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.momentum);\\n                    try self.writeAll(\\"; \\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projector_momentum_pair => |item| {\\n                    try self.writeAll(\\"p[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.left);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.right);\\n                    try self.writeAll(\\")\\");\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickAction(self: *Self, factor: Correlator.ResolvedActionFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .profile_derivative => |item| {\\n                    try self.writeAll(\\"dProfile(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n                .projected_profile_derivative => |item| {\\n                    try self.writeAll(\\"dProfile[\\");\\n                    try self.writeTensorRef(item.projector);\\n                    try self.writeAll(\\"](\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeLabel(item.index);\\n                    try self.writeAll(\\")\\");\\n                },\\n            }\\n        }\\n\\n        pub fn emitWickResidualOperator(self: *Self, residual: Correlator.WickResidualRef) !void {\\n            try self.factorSeparator();\\n            try self.writeFmt(\\"op{}\\", .{residual.input_index});\\n        }\\n\\n        pub fn emitWickTermEnd(self: *Self) !void {\\n            if (!self.term_has_factor) try self.writeAll(\\"1\\");\\n            try self.writeAll(\\")\\");\\n            self.term_open = false;\\n            self.branch_has_factor = true;\\n        }\\n\\n        pub fn emitZeroModeFactor(self: *Self, factor: ZeroModeRuntime.ZeroModeFactor) !void {\\n            try self.factorSeparator();\\n            switch (factor) {\\n                .bc_top_form => |item| try self.writeFmt(\\"bcTop[{s}]\\", .{@tagName(item.support)}),\\n                .eta_xi_zero_mode => |item| try self.writeFmt(\\"etaXiZero[{s};z{}]\\", .{ @tagName(item.support), item.xi.coordinate }),\\n                .momentum_delta => |item| try self.writeFmt(\\"deltaP[{}]\\", .{item.momenta.len}),\\n                .dirichlet_phase => |item| {\\n                    try self.writeAll(\\"dirichletPhase[\\");\\n                    try self.writeConfig(item.projector);\\n                    try self.writeAll(\\",\\");\\n                    try self.writeConfig(item.position);\\n                    try self.writeAll(\\"]\\");\\n                },\\n                .profile_fourier_integral => |item| {\\n                    try self.writeAll(\\"profileFourier(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n                .profile_polynomial_integral => |item| {\\n                    try self.writeAll(\\"profilePolynomial(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n                .profile_gaussian_differential => |item| {\\n                    try self.writeAll(\\"profileGaussian(\\");\\n                    try self.writeLabel(item.profile);\\n                    try self.writeFmt(\\";z{})\\", .{item.coordinate});\\n                },\\n            }\\n        }\\n\\n        pub fn emitZeroModeBaseEnd(self: *Self) !void {\\n            try self.beginBranch();\\n            if (!self.branch_has_factor) try self.writeAll(\\"1\\");\\n            self.branch_open = false;\\n            self.branch_has_factor = false;\\n            self.branch_count += 1;\\n        }\\n    };\\n}\\n\\nfn residualConsumed(mask: u128, index: usize) bool {\\n    return (mask & (@as(u128, 1) << @intCast(index))) != 0;\\n}\\n\\nfn markResidualConsumed(mask: *u128, index: usize) void {\\n    mask.* |= @as(u128, 1) << @intCast(index);\\n}\\n\\nfn kindIn(kind: operators.OperatorKindId, candidates: []const operators.OperatorKindId) bool {\\n    for (candidates) |candidate| {\\n        if (candidate == kind) return true;\\n    }\\n    return false;\\n}\\n\\nfn localPosition(op: kernel.Call.LocalOp) ?coefficient.Variable {\\n    return switch (op.insertion) {\\n        .single => |single| single.position,\\n        .pair => |pair| pair.holomorphic_position,\\n    };\\n}\\n\\nfn localDerivativeOrder(op: kernel.Call.LocalOp) u8 {\\n    return switch (op.insertion) {\\n        .single => |single| single.derivatives,\\n        .pair => |pair| pair.holomorphic_derivatives,\\n    };\\n}\\n\\nfn firstLabel(ops: kernel.Call.MultiOp, op: kernel.Call.LocalOp) ?kernel.Call.LabelValue {\\n    const index: usize = @intCast(op.labels);\\n    if (index >= ops.labels.values.len) return null;\\n    return ops.labels.values[index];\\n}\\n\\nfn configValue(config_ptr: *const CorrelatorConfigData, config_ref: ConfigRef) ?ConfigValue {\\n    for (config_ptr.config_entries) |entry| {\\n        if (entry.id == config_ref) return entry.value;\\n    }\\n    return null;\\n}\\n\\nfn projectorRank(value: ConfigValue) ?u16 {\\n    return switch (value) {\\n        .tensor_projector => |projector| @intCast(@intFromEnum(projector) >> 24),\\n        else => null,\\n    };\\n}\\n\\nfn rankValue(config_ptr: *const CorrelatorConfigData, source: zero_mode.RankSource) ?u16 {\\n    return switch (source) {\\n        .none => 0,\\n        .literal => |value| value,\\n        .target_dimension => |config_ref| switch (configValue(config_ptr, config_ref) orelse return null) {\\n            .target_dimension => |dimension| dimension,\\n            else => null,\\n        },\\n        .projector_rank => |config_ref| projectorRank(configValue(config_ptr, config_ref) orelse return null),\\n    };\\n}\\n\\nfn hasRankSource(source: zero_mode.RankSource) bool {\\n    return switch (source) {\\n        .none => false,\\n        else => true,\\n    };\\n}\\n\\nfn emitBcTopForm(rule: zero_mode.BcTopForm, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var jets: [3]ZeroModeRuntime.CJet = undefined;\\n    var residual_ids: [3]usize = undefined;\\n    var jet_count: usize = 0;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.c_kind_ids)) continue;\\n        if (jet_count >= jets.len) {\\n            overflow = true;\\n            continue;\\n        }\\n        jets[jet_count] = .{\\n            .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            .derivative_order = localDerivativeOrder(op),\\n        };\\n        residual_ids[jet_count] = residual_index;\\n        jet_count += 1;\\n    }\\n\\n    if (overflow or jet_count != 3) return;\\n    for (residual_ids) |residual_id| {\\n        markResidualConsumed(consumed, residual_id);\\n    }\\n    try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .bc_top_form = .{\\n        .support = rule.support,\\n        .normalization = rule.normalization,\\n        .jets = jets,\\n    } });\\n}\\n\\nfn acceptBcTopForm(rule: zero_mode.BcTopForm, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var residual_ids: [3]usize = undefined;\\n    var jet_count: usize = 0;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.c_kind_ids)) continue;\\n        if (localPosition(op) == null) return error.InvalidZeroModeOperator;\\n        if (jet_count >= residual_ids.len) {\\n            overflow = true;\\n            continue;\\n        }\\n        residual_ids[jet_count] = residual_index;\\n        jet_count += 1;\\n    }\\n\\n    if (overflow or jet_count != 3) return;\\n    for (residual_ids) |residual_id| {\\n        markResidualConsumed(consumed, residual_id);\\n    }\\n}\\n\\nfn emitEtaXiZeroMode(rule: zero_mode.EtaXiZeroMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var xi: ZeroModeRuntime.CJet = undefined;\\n    var residual_id: usize = 0;\\n    var found = false;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.xi_kind_ids)) continue;\\n        if (localDerivativeOrder(op) != 0 or found) {\\n            overflow = true;\\n            continue;\\n        }\\n        xi = .{\\n            .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            .derivative_order = 0,\\n        };\\n        residual_id = residual_index;\\n        found = true;\\n    }\\n\\n    if (overflow or !found) return;\\n    markResidualConsumed(consumed, residual_id);\\n    try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .eta_xi_zero_mode = .{\\n        .support = rule.support,\\n        .normalization = rule.normalization,\\n        .xi = xi,\\n    } });\\n}\\n\\nfn acceptEtaXiZeroMode(rule: zero_mode.EtaXiZeroMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var residual_id: usize = 0;\\n    var found = false;\\n    var overflow = false;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (!kindIn(op.kind, rule.xi_kind_ids)) continue;\\n        if (localPosition(op) == null) return error.InvalidZeroModeOperator;\\n        if (localDerivativeOrder(op) != 0 or found) {\\n            overflow = true;\\n            continue;\\n        }\\n        residual_id = residual_index;\\n        found = true;\\n    }\\n\\n    if (overflow or !found) return;\\n    markResidualConsumed(consumed, residual_id);\\n}\\n\\nfn emitFreeBosonConstantMode(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n    var momenta: [limits.max_zero_mode_items]kernel.Call.LabelValue = undefined;\\n    var momentum_count: usize = 0;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (kindIn(op.kind, rule.exp_kind_ids)) {\\n            if (momentum_count >= momenta.len) return error.TooManyZeroModeItems;\\n            momenta[momentum_count] = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            momentum_count += 1;\\n            markResidualConsumed(consumed, residual_index);\\n            continue;\\n        }\\n\\n        if (kindIn(op.kind, rule.profile_kind_ids)) {\\n            const label = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            const profile = switch (label) {\\n                .symbol => |value| @as(Handle.Profile, @enumFromInt(value)),\\n                else => return error.InvalidZeroModeOperator,\\n            };\\n            const factor = ZeroModeRuntime.ProfileFactor{\\n                .profile = label,\\n                .coordinate = localPosition(op) orelse return error.InvalidZeroModeOperator,\\n            };\\n            switch (profilePresentation(profile)) {\\n                .position_space => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_gaussian_differential = factor }),\\n                .fourier => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_fourier_integral = factor }),\\n                .polynomial_rnc => try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .profile_polynomial_integral = factor }),\\n            }\\n            markResidualConsumed(consumed, residual_index);\\n        }\\n    }\\n\\n    if (momentum_count != 0 or hasRankSource(rule.normalization.two_pi_power)) {\\n        const projector = if (rule.integration_projector) |config_ref| configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig else null;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .momentum_delta = .{\\n            .projector = projector,\\n            .momenta = momenta[0..momentum_count],\\n            .two_pi_power = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig,\\n            .scalar = rule.normalization.scalar,\\n        } });\\n    }\\n\\n    if (rule.fixed_projector) |projector_ref| {\\n        if (momentum_count == 0) return;\\n        const position_ref = rule.fixed_position orelse return error.InvalidZeroModeConfig;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .dirichlet_phase = .{\\n            .projector = configValue(config_ptr, projector_ref) orelse return error.InvalidZeroModeConfig,\\n            .position = configValue(config_ptr, position_ref) orelse return error.InvalidZeroModeConfig,\\n            .momenta = momenta[0..momentum_count],\\n        } });\\n    }\\n}\\n\\nfn acceptFreeBosonConstantMode(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n    var momentum_count: usize = 0;\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (residualConsumed(consumed.*, residual_index)) continue;\\n        const op = residual.op(residual_index);\\n        if (kindIn(op.kind, rule.exp_kind_ids)) {\\n            if (momentum_count >= limits.max_zero_mode_items) return error.TooManyZeroModeItems;\\n            _ = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            momentum_count += 1;\\n            markResidualConsumed(consumed, residual_index);\\n            continue;\\n        }\\n\\n        if (kindIn(op.kind, rule.profile_kind_ids)) {\\n            const label = firstLabel(residual.ops, op) orelse return error.InvalidZeroModeOperator;\\n            const profile = switch (label) {\\n                .symbol => |value| @as(Handle.Profile, @enumFromInt(value)),\\n                else => return error.InvalidZeroModeOperator,\\n            };\\n            _ = localPosition(op) orelse return error.InvalidZeroModeOperator;\\n            _ = profilePresentation(profile);\\n            markResidualConsumed(consumed, residual_index);\\n        }\\n    }\\n\\n    if (momentum_count != 0 or hasRankSource(rule.normalization.two_pi_power)) {\\n        if (rule.integration_projector) |config_ref| _ = configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig;\\n        _ = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig;\\n    }\\n\\n    if (rule.fixed_projector) |projector_ref| {\\n        if (momentum_count == 0) return;\\n        const position_ref = rule.fixed_position orelse return error.InvalidZeroModeConfig;\\n        _ = configValue(config_ptr, projector_ref) orelse return error.InvalidZeroModeConfig;\\n        _ = configValue(config_ptr, position_ref) orelse return error.InvalidZeroModeConfig;\\n    }\\n}\\n\\nfn allResidualsConsumed(residual: ZeroModeRuntime.ResidualCursor, consumed: u128) bool {\\n    var residual_index: usize = 0;\\n    while (residual_index < residual.len()) : (residual_index += 1) {\\n        if (!residualConsumed(consumed, residual_index)) return false;\\n    }\\n    return true;\\n}\\n\\nfn emitEmptyFreeBosonConstantMode(config_ptr: *const CorrelatorConfigData, rule: zero_mode.FreeBosonConstantMode, sink: anytype) !void {\\n    if (hasRankSource(rule.normalization.two_pi_power)) {\\n        const empty_momenta: [0]kernel.Call.LabelValue = .{};\\n        const projector = if (rule.integration_projector) |config_ref| configValue(config_ptr, config_ref) orelse return error.InvalidZeroModeConfig else null;\\n        try sink.emitZeroModeFactor(ZeroModeRuntime.ZeroModeFactor{ .momentum_delta = .{\\n            .projector = projector,\\n            .momenta = empty_momenta[0..],\\n            .two_pi_power = rankValue(config_ptr, rule.normalization.two_pi_power) orelse return error.InvalidZeroModeConfig,\\n            .scalar = rule.normalization.scalar,\\n        } });\\n    }\\n}\\n\\nfn BcTopFormProcedure(comptime rule: zero_mode.BcTopForm) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.c_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return acceptBcTopForm(rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return emitBcTopForm(rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            _ = config_ptr;\\n            _ = sink;\\n        }\\n    };\\n}\\n\\nfn EtaXiZeroModeProcedure(comptime rule: zero_mode.EtaXiZeroMode) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.xi_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return acceptEtaXiZeroMode(rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            _ = limits;\\n            _ = config_ptr;\\n            return emitEtaXiZeroMode(rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            _ = config_ptr;\\n            _ = sink;\\n        }\\n    };\\n}\\n\\nfn ConstantFermionProcedure(comptime rule: zero_mode.ConstantFermion) type {\\n    const adapted = zero_mode.EtaXiZeroMode{\\n        .support = rule.support,\\n        .xi_kind_ids = rule.fermion_kind_ids,\\n        .normalization = rule.normalization,\\n    };\\n    return EtaXiZeroModeProcedure(adapted);\\n}\\n\\nfn TopFormFermionProcedure(comptime rule: zero_mode.TopFormFermion) type {\\n    const adapted = zero_mode.BcTopForm{\\n        .support = rule.support,\\n        .c_kind_ids = rule.field_kind_ids,\\n        .normalization = rule.normalization,\\n    };\\n    return BcTopFormProcedure(adapted);\\n}\\n\\nfn FreeBosonConstantModeProcedure(comptime rule: zero_mode.FreeBosonConstantMode) type {\\n    return struct {\\n        fn canConsumeKind(kind: operators.OperatorKindId) bool {\\n            return kindIn(kind, rule.exp_kind_ids) or kindIn(kind, rule.profile_kind_ids);\\n        }\\n\\n        fn accept(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128) !void {\\n            return acceptFreeBosonConstantMode(limits, config_ptr, rule, residual, consumed);\\n        }\\n\\n        fn emit(comptime limits: BranchLimits, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor, consumed: *u128, sink: anytype) !void {\\n            return emitFreeBosonConstantMode(limits, config_ptr, rule, residual, consumed, sink);\\n        }\\n\\n        fn emitEmpty(config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n            return emitEmptyFreeBosonConstantMode(config_ptr, rule, sink);\\n        }\\n    };\\n}\\n\\nfn ZeroModeProcedure(comptime expr: zero_mode.Expr) type {\\n    return switch (expr) {\\n        .bc_top_form => |rule| BcTopFormProcedure(rule),\\n        .free_boson_constant_mode => |rule| FreeBosonConstantModeProcedure(rule),\\n        .eta_xi_zero_mode => |rule| EtaXiZeroModeProcedure(rule),\\n        .constant_fermion => |rule| ConstantFermionProcedure(rule),\\n        .top_form_fermion => |rule| TopFormFermionProcedure(rule),\\n        .boson_momentum_conservation => |rule| FreeBosonConstantModeProcedure(rule),\\n    };\\n}\\n\\nfn generatedZeroModeCanConsumeKind(comptime ConfigHandle: type, kind: operators.OperatorKindId) bool {\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        if (ZeroModeProcedure(rule.expr).canConsumeKind(kind)) return true;\\n    }\\n    return false;\\n}\\n\\nfn generatedZeroModeCursorSucceeds(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, residual: ZeroModeRuntime.ResidualCursor) !bool {\\n    const limits = ConfigHandle.branch_limits;\\n    if (residual.len() > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n\\n    var consumed: u128 = 0;\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).accept(limits, config_ptr, residual, &consumed);\\n    }\\n\\n    return allResidualsConsumed(residual, consumed);\\n}\\n\\nfn generatedZeroModeBaseCaseSucceeds(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: []const usize) !bool {\\n    if (residual_indices.len == 0) return true;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    return generatedZeroModeCursorSucceeds(ConfigHandle, config_ptr, residual);\\n}\\n\\nfn generatedEmptyZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, sink: anytype) !void {\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emitEmpty(config_ptr, sink);\\n    }\\n    try sink.emitZeroModeBaseEnd();\\n}\\n\\nfn generatedTrustedZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: []const usize, sink: anytype) !void {\\n    const limits = ConfigHandle.branch_limits;\\n    if (residual_indices.len > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n    if (residual_indices.len == 0) {\\n        try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        return;\\n    }\\n\\n    var consumed: u128 = 0;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    // The branch walker has already dry-run validated these residuals.\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emit(limits, config_ptr, residual, &consumed, sink);\\n    }\\n    if (!allResidualsConsumed(residual, consumed)) return error.InvalidWickBranch;\\n    try sink.emitZeroModeBaseEnd();\\n}\\n\\n/// generatedZeroModeBaseCase consumes residual operators with configured zero-mode procedures.\\nfn generatedZeroModeBaseCase(comptime ConfigHandle: type, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, residual_indices: ?[]const usize, sink: anytype) !bool {\\n    const limits = ConfigHandle.branch_limits;\\n    const residual = ZeroModeRuntime.ResidualCursor{ .ops = ops, .indices = residual_indices };\\n    if (residual.len() > limits.max_zero_mode_residuals) return error.TooManyZeroModeResiduals;\\n    if (residual.len() == 0) {\\n        try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        return true;\\n    }\\n    if (!try generatedZeroModeCursorSucceeds(ConfigHandle, config_ptr, residual)) return false;\\n\\n    var consumed: u128 = 0;\\n    inline for (ConfigHandle.impl.zero_modes) |rule| {\\n        try ZeroModeProcedure(rule.expr).emit(limits, config_ptr, residual, &consumed, sink);\\n    }\\n\\n    if (!allResidualsConsumed(residual, consumed)) return false;\\n    try sink.emitZeroModeBaseEnd();\\n    return true;\\n}\\n\\nfn ruleMatches(rule: wick.Rule, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) ?bool {\\n    if (rule.left.kind == left.kind and rule.right.kind == right.kind) {\\n        return false;\\n    }\\n    if (rule.left.kind == right.kind and rule.right.kind == left.kind) {\\n        return true;\\n    }\\n    return null;\\n}\\n\\nfn matchRule(config_ptr: *const CorrelatorConfigData, rule: *const wick.Rule, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, labels: *const kernel.Call.LabelStore) ?Correlator.WickPair {\\n    const reversed = ruleMatches(rule.*, left, right) orelse return null;\\n    return .{\\n        .rule = rule,\\n        .config = config_ptr,\\n        .left = left,\\n        .right = right,\\n        .labels = labels,\\n        .reversed = reversed,\\n    };\\n}\\n\\nfn lowerBoundPairLookup(entries: []const PairLookupEntry, key: u64) usize {\\n    var lo: usize = 0;\\n    var hi: usize = entries.len;\\n    while (lo < hi) {\\n        const mid = lo + ((hi - lo) / 2);\\n        if (entries[mid].key < key) {\\n            lo = mid + 1;\\n        } else {\\n            hi = mid;\\n        }\\n    }\\n    return lo;\\n}\\n\\nfn emitPrimitiveWickTerm(pair: Correlator.WickPair, left_index: usize, right_index: usize, term_index: usize, sink: anytype) !void {\\n    const primitive = (Correlator.PrimitiveWickPair{ .pair = pair }).term(term_index) orelse return error.InvalidWickTerm;\\n\\n    const event = Correlator.WickTermEvent{\\n        .left_index = left_index,\\n        .right_index = right_index,\\n        .term_index = term_index,\\n    };\\n    try sink.emitWickTermStart(event);\\n\\n    var scalar_index: usize = 0;\\n    while (scalar_index < primitive.scalarCount()) : (scalar_index += 1) {\\n        try sink.emitWickScalar(primitive.scalar(scalar_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var coordinate_index: usize = 0;\\n    while (coordinate_index < primitive.coordinateCount()) : (coordinate_index += 1) {\\n        try sink.emitWickCoordinate(primitive.coordinate(coordinate_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var tensor_index: usize = 0;\\n    while (tensor_index < primitive.tensorCount()) : (tensor_index += 1) {\\n        try sink.emitWickTensor(primitive.tensor(tensor_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var action_index: usize = 0;\\n    while (action_index < primitive.actionCount()) : (action_index += 1) {\\n        try sink.emitWickAction(primitive.action(action_index) orelse return error.InvalidWickFactor);\\n    }\\n\\n    var residual_index: usize = 0;\\n    while (residual_index < primitive.residualCount()) : (residual_index += 1) {\\n        try emitWickResidualOperator(pair, left_index, right_index, primitive.residual(residual_index) orelse return error.InvalidWickFactor, sink);\\n    }\\n\\n    try sink.emitWickTermEnd();\\n}\\n\\nfn wickPairTermPayloadFrom(rule_index: usize, term_index: usize, reversed: bool) !u64 {\\n    if (rule_index > std.math.maxInt(u32)) return error.TooManyWickRules;\\n    if (term_index > std.math.maxInt(u16)) return error.TooManyWickTerms;\\n    return @as(u64, @intCast(rule_index)) |\\n        (@as(u64, @intCast(term_index)) << 32) |\\n        (@as(u64, @intFromBool(reversed)) << 48);\\n}\\n\\nfn wickPayloadRuleIndex(payload: u64) usize {\\n    return @intCast(payload & 0xffff_ffff);\\n}\\n\\nfn wickPayloadTermIndex(payload: u64) usize {\\n    return @intCast((payload >> 32) & 0xffff);\\n}\\n\\nfn wickPayloadReversed(payload: u64) bool {\\n    return ((payload >> 48) & 1) != 0;\\n}\\n\\nfn actualSideIndex(pair: Correlator.WickPair, left_index: usize, right_index: usize, side: wick.Side) usize {\\n    return switch (side) {\\n        .left => if (pair.reversed) right_index else left_index,\\n        .right => if (pair.reversed) left_index else right_index,\\n    };\\n}\\n\\nfn emitWickResidualOperator(pair: Correlator.WickPair, left_index: usize, right_index: usize, side: wick.Side, sink: anytype) !void {\\n    const SinkPtr = @TypeOf(sink);\\n    const sink_info = @typeInfo(SinkPtr);\\n    if (sink_info != .pointer) return;\\n    const Sink = sink_info.pointer.child;\\n    if (!@hasDecl(Sink, \\"emitWickResidualOperator\\")) return;\\n\\n    const input_index = actualSideIndex(pair, left_index, right_index, side);\\n    const operator = switch (side) {\\n        .left => pair.sideOp(.left),\\n        .right => pair.sideOp(.right),\\n    };\\n    try sink.emitWickResidualOperator(Correlator.WickResidualRef{\\n        .input_index = input_index,\\n        .operator = operator,\\n    });\\n}\\n\\nconst cache_pair_direct_width = 16;\\nconst cache_pair_hash_slots = cache_pair_direct_width * cache_pair_direct_width;\\nconst no_cached_pair = std.math.maxInt(u16);\\n\\nfn branchBudgetOperatorCount(comptime budget: BranchBudget) usize {\\n    return switch (budget) {\\n        .standard => 128,\\n    };\\n}\\n\\nfn branchBudgetCacheTerms(comptime budget: BranchBudget) usize {\\n    return switch (budget) {\\n        .standard => 66,\\n    };\\n}\\n\\nfn maxTermFactors(comptime rules: []const wick.Rule, comptime selector: enum { scalars, coordinates, tensors, actions, residuals }) usize {\\n    var result: usize = 0;\\n    for (rules) |rule| {\\n        for (rule.expr.terms) |term| {\\n            const count = switch (selector) {\\n                .scalars => term.scalars.len,\\n                .coordinates => term.coordinates.len,\\n                .tensors => term.tensors.len,\\n                .actions => term.actions.len,\\n                .residuals => term.residuals.len,\\n            };\\n            if (count > result) result = count;\\n        }\\n    }\\n    return result;\\n}\\n\\nfn atLeastOne(value: usize) usize {\\n    return if (value == 0) 1 else value;\\n}\\n\\nfn branchLimits(comptime input: CorrelatorConfigInput) BranchLimits {\\n    const max_operators = branchBudgetOperatorCount(input.branch_budget);\\n    if (max_operators > 128) @compileError(\\"zero-mode consumption mask supports at most 128 residual operators\\");\\n    const max_terms = max_operators / 2;\\n    const max_cached_terms = branchBudgetCacheTerms(input.branch_budget);\\n    const scalar_terms = atLeastOne(maxTermFactors(input.wick_rules, .scalars));\\n    const coordinate_terms = atLeastOne(maxTermFactors(input.wick_rules, .coordinates));\\n    const tensor_terms = atLeastOne(maxTermFactors(input.wick_rules, .tensors));\\n    const action_terms = atLeastOne(maxTermFactors(input.wick_rules, .actions));\\n    const residual_terms = atLeastOne(maxTermFactors(input.wick_rules, .residuals));\\n\\n    return .{\\n        .max_operators = max_operators,\\n        .max_zero_mode_residuals = max_operators,\\n        .max_zero_mode_items = max_operators,\\n        .max_terms = max_terms,\\n        .max_scalars = scalar_terms * max_terms,\\n        .max_coordinates = coordinate_terms * max_terms,\\n        .max_tensors = tensor_terms * max_terms,\\n        .max_actions = action_terms * max_terms,\\n        .max_residuals = residual_terms * max_terms,\\n        .max_cached_terms = max_cached_terms,\\n        .max_cached_scalars = scalar_terms * max_cached_terms,\\n        .max_cached_coordinates = coordinate_terms * max_cached_terms,\\n        .max_cached_tensors = tensor_terms * max_cached_terms,\\n        .max_cached_actions = action_terms * max_cached_terms,\\n        .max_cached_residuals = residual_terms * max_cached_terms,\\n    };\\n}\\n\\nconst AccumulatedWickTerm = struct {\\n    event: Correlator.WickTermEvent,\\n    cache_index: u16 = no_cached_pair,\\n    scalar_start: u16,\\n    scalar_count: u16 = 0,\\n    coordinate_start: u16,\\n    coordinate_count: u16 = 0,\\n    tensor_start: u16,\\n    tensor_count: u16 = 0,\\n    action_start: u16,\\n    action_count: u16 = 0,\\n    residual_start: u16,\\n    residual_count: u16 = 0,\\n};\\n\\nconst CachedPairKey = struct {\\n    left: u8,\\n    right: u8,\\n    payload: u64,\\n};\\n\\nfn AccumulatedBranch(comptime limits: BranchLimits) type {\\n    return struct {\\n        const Self = @This();\\n\\n        const Snapshot = struct {\\n            term_count: u16,\\n            scalar_count: u16,\\n            coordinate_count: u16,\\n            tensor_count: u16,\\n            action_count: u16,\\n            residual_count: u16,\\n            logical_scalar_count: u16,\\n            logical_coordinate_count: u16,\\n            logical_tensor_count: u16,\\n            logical_action_count: u16,\\n            logical_residual_count: u16,\\n        };\\n\\n        terms: [limits.max_terms]AccumulatedWickTerm = undefined,\\n        term_count: u16 = 0,\\n        scalars: [limits.max_scalars]Correlator.ResolvedScalarFactor = undefined,\\n        scalar_count: u16 = 0,\\n        coordinates: [limits.max_coordinates]coefficient.CoordinateFactor = undefined,\\n        coordinate_count: u16 = 0,\\n        tensors: [limits.max_tensors]Correlator.ResolvedTensorFactor = undefined,\\n        tensor_count: u16 = 0,\\n        actions: [limits.max_actions]Correlator.ResolvedActionFactor = undefined,\\n        action_count: u16 = 0,\\n        residuals: [limits.max_residuals]Correlator.WickResidualRef = undefined,\\n        residual_count: u16 = 0,\\n        cache_keys: [limits.max_cached_terms]CachedPairKey = undefined,\\n        cache_pair_index: [cache_pair_hash_slots]u16 = [_]u16{no_cached_pair} ** cache_pair_hash_slots,\\n        cache_terms: [limits.max_cached_terms]AccumulatedWickTerm = undefined,\\n        cache_count: u16 = 0,\\n        cache_scalars: [limits.max_cached_scalars]Correlator.ResolvedScalarFactor = undefined,\\n        cache_scalar_count: u16 = 0,\\n        cache_coordinates: [limits.max_cached_coordinates]coefficient.CoordinateFactor = undefined,\\n        cache_coordinate_count: u16 = 0,\\n        cache_tensors: [limits.max_cached_tensors]Correlator.ResolvedTensorFactor = undefined,\\n        cache_tensor_count: u16 = 0,\\n        cache_actions: [limits.max_cached_actions]Correlator.ResolvedActionFactor = undefined,\\n        cache_action_count: u16 = 0,\\n        cache_residuals: [limits.max_cached_residuals]Correlator.WickResidualRef = undefined,\\n        cache_residual_count: u16 = 0,\\n        logical_scalar_count: u16 = 0,\\n        logical_coordinate_count: u16 = 0,\\n        logical_tensor_count: u16 = 0,\\n        logical_action_count: u16 = 0,\\n        logical_residual_count: u16 = 0,\\n\\n        /// init constructs an empty branch factor accumulator.\\n        pub fn init() Self {\\n            return .{};\\n        }\\n\\n        /// snapshot records current array lengths for backtracking.\\n        pub fn snapshot(self: *const Self) Snapshot {\\n            return .{\\n                .term_count = self.term_count,\\n                .scalar_count = self.scalar_count,\\n                .coordinate_count = self.coordinate_count,\\n                .tensor_count = self.tensor_count,\\n                .action_count = self.action_count,\\n                .residual_count = self.residual_count,\\n                .logical_scalar_count = self.logical_scalar_count,\\n                .logical_coordinate_count = self.logical_coordinate_count,\\n                .logical_tensor_count = self.logical_tensor_count,\\n                .logical_action_count = self.logical_action_count,\\n                .logical_residual_count = self.logical_residual_count,\\n            };\\n        }\\n\\n        /// restore truncates accumulated factors to a previous snapshot.\\n        pub fn restore(self: *Self, saved: Snapshot) void {\\n            self.term_count = saved.term_count;\\n            self.scalar_count = saved.scalar_count;\\n            self.coordinate_count = saved.coordinate_count;\\n            self.tensor_count = saved.tensor_count;\\n            self.action_count = saved.action_count;\\n            self.residual_count = saved.residual_count;\\n            self.logical_scalar_count = saved.logical_scalar_count;\\n            self.logical_coordinate_count = saved.logical_coordinate_count;\\n            self.logical_tensor_count = saved.logical_tensor_count;\\n            self.logical_action_count = saved.logical_action_count;\\n            self.logical_residual_count = saved.logical_residual_count;\\n        }\\n\\n        fn currentTerm(self: *Self) *AccumulatedWickTerm {\\n            return &self.terms[self.term_count - 1];\\n        }\\n\\n        fn cachePairSlot(left: usize, right: usize) usize {\\n            if (left < cache_pair_direct_width and right < cache_pair_direct_width) {\\n                return left * cache_pair_direct_width + right;\\n            }\\n            return ((left * 131) ^ right) & (cache_pair_hash_slots - 1);\\n        }\\n\\n        fn findCache(self: *const Self, left: usize, right: usize, payload: u64) ?u16 {\\n            const direct = self.cache_pair_index[cachePairSlot(left, right)];\\n            if (direct != no_cached_pair) {\\n                const key = self.cache_keys[direct];\\n                if (key.left == left and key.right == right and key.payload == payload) return direct;\\n            }\\n\\n            var index: u16 = 0;\\n            while (index < self.cache_count) : (index += 1) {\\n                const key = self.cache_keys[index];\\n                if (key.left == left and key.right == right and key.payload == payload) return index;\\n            }\\n            return null;\\n        }\\n\\n        fn appendCachedTerm(self: *Self, cache_index: u16) !void {\\n            if (self.term_count == self.terms.len) return error.TooManyAccumulatedWickTerms;\\n            const cached = self.cache_terms[cache_index];\\n            self.terms[self.term_count] = cached;\\n            self.terms[self.term_count].cache_index = cache_index;\\n            self.term_count += 1;\\n            self.logical_scalar_count += cached.scalar_count;\\n            self.logical_coordinate_count += cached.coordinate_count;\\n            self.logical_tensor_count += cached.tensor_count;\\n            self.logical_action_count += cached.action_count;\\n            self.logical_residual_count += cached.residual_count;\\n        }\\n\\n        /// primitiveTermCount returns the number of primitive pair terms in this branch.\\n        pub fn primitiveTermCount(self: *const Self) usize {\\n            return self.term_count;\\n        }\\n\\n        /// scalarFactorCount returns the number of scalar factors in this branch.\\n        pub fn scalarFactorCount(self: *const Self) usize {\\n            return self.logical_scalar_count;\\n        }\\n\\n        /// coordinateFactorCount returns the number of coordinate factors in this branch.\\n        pub fn coordinateFactorCount(self: *const Self) usize {\\n            return self.logical_coordinate_count;\\n        }\\n\\n        /// tensorFactorCount returns the number of tensor factors in this branch.\\n        pub fn tensorFactorCount(self: *const Self) usize {\\n            return self.logical_tensor_count;\\n        }\\n\\n        /// actionFactorCount returns the number of action factors in this branch.\\n        pub fn actionFactorCount(self: *const Self) usize {\\n            return self.logical_action_count;\\n        }\\n\\n        /// residualFactorCount returns the number of residual operator refs in this branch.\\n        pub fn residualFactorCount(self: *const Self) usize {\\n            return self.logical_residual_count;\\n        }\\n\\n        /// pushPairTerm appends one cached or freshly resolved primitive pair term.\\n        pub fn pushPairTerm(branch: *Self, config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64) !void {\\n            if (branch.findCache(left_index, right_index, payload)) |cache_index| {\\n                try branch.appendCachedTerm(cache_index);\\n                return;\\n            }\\n\\n            if (branch.cache_count == branch.cache_terms.len) {\\n                try emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, branch);\\n                return;\\n            }\\n\\n            const cache_index = branch.cache_count;\\n            const CacheSink = struct {\\n                branch: *Self,\\n                cache_index: u16,\\n\\n                fn current(self: *@This()) *AccumulatedWickTerm {\\n                    return &self.branch.cache_terms[self.cache_index];\\n                }\\n\\n                pub fn emitWickTermStart(self: *@This(), event: Correlator.WickTermEvent) !void {\\n                    self.branch.cache_terms[self.cache_index] = .{\\n                        .event = event,\\n                        .scalar_start = self.branch.cache_scalar_count,\\n                        .coordinate_start = self.branch.cache_coordinate_count,\\n                        .tensor_start = self.branch.cache_tensor_count,\\n                        .action_start = self.branch.cache_action_count,\\n                        .residual_start = self.branch.cache_residual_count,\\n                    };\\n                }\\n\\n                pub fn emitWickScalar(self: *@This(), factor: Correlator.ResolvedScalarFactor) !void {\\n                    if (self.branch.cache_scalar_count == self.branch.cache_scalars.len) return error.TooManyCachedScalars;\\n                    self.branch.cache_scalars[self.branch.cache_scalar_count] = factor;\\n                    self.branch.cache_scalar_count += 1;\\n                    self.current().scalar_count += 1;\\n                }\\n\\n                pub fn emitWickCoordinate(self: *@This(), factor: coefficient.CoordinateFactor) !void {\\n                    if (self.branch.cache_coordinate_count == self.branch.cache_coordinates.len) return error.TooManyCachedCoordinates;\\n                    self.branch.cache_coordinates[self.branch.cache_coordinate_count] = factor;\\n                    self.branch.cache_coordinate_count += 1;\\n                    self.current().coordinate_count += 1;\\n                }\\n\\n                pub fn emitWickTensor(self: *@This(), factor: Correlator.ResolvedTensorFactor) !void {\\n                    if (self.branch.cache_tensor_count == self.branch.cache_tensors.len) return error.TooManyCachedTensors;\\n                    self.branch.cache_tensors[self.branch.cache_tensor_count] = factor;\\n                    self.branch.cache_tensor_count += 1;\\n                    self.current().tensor_count += 1;\\n                }\\n\\n                pub fn emitWickAction(self: *@This(), factor: Correlator.ResolvedActionFactor) !void {\\n                    if (self.branch.cache_action_count == self.branch.cache_actions.len) return error.TooManyCachedActions;\\n                    self.branch.cache_actions[self.branch.cache_action_count] = factor;\\n                    self.branch.cache_action_count += 1;\\n                    self.current().action_count += 1;\\n                }\\n\\n                pub fn emitWickResidualOperator(self: *@This(), residual: Correlator.WickResidualRef) !void {\\n                    if (self.branch.cache_residual_count == self.branch.cache_residuals.len) return error.TooManyCachedResiduals;\\n                    self.branch.cache_residuals[self.branch.cache_residual_count] = residual;\\n                    self.branch.cache_residual_count += 1;\\n                    self.current().residual_count += 1;\\n                }\\n\\n                pub fn emitWickTermEnd(_: *@This()) !void {}\\n            };\\n\\n            var cache_sink = CacheSink{ .branch = branch, .cache_index = cache_index };\\n            try emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, &cache_sink);\\n            branch.cache_keys[cache_index] = .{ .left = @intCast(left_index), .right = @intCast(right_index), .payload = payload };\\n            const direct_slot = cachePairSlot(left_index, right_index);\\n            if (branch.cache_pair_index[direct_slot] == no_cached_pair) branch.cache_pair_index[direct_slot] = cache_index;\\n            branch.cache_count += 1;\\n            try branch.appendCachedTerm(cache_index);\\n        }\\n\\n        /// emitWickTermStart appends one primitive term boundary.\\n        pub fn emitWickTermStart(self: *Self, event: Correlator.WickTermEvent) !void {\\n            if (self.term_count == self.terms.len) return error.TooManyAccumulatedWickTerms;\\n            self.terms[self.term_count] = .{\\n                .event = event,\\n                .scalar_start = self.scalar_count,\\n                .coordinate_start = self.coordinate_count,\\n                .tensor_start = self.tensor_count,\\n                .action_start = self.action_count,\\n                .residual_start = self.residual_count,\\n            };\\n            self.term_count += 1;\\n        }\\n\\n        /// emitWickScalar appends one scalar factor.\\n        pub fn emitWickScalar(self: *Self, factor: Correlator.ResolvedScalarFactor) !void {\\n            if (self.scalar_count == self.scalars.len) return error.TooManyAccumulatedScalars;\\n            self.scalars[self.scalar_count] = factor;\\n            self.scalar_count += 1;\\n            self.currentTerm().scalar_count += 1;\\n            self.logical_scalar_count += 1;\\n        }\\n\\n        /// emitWickCoordinate appends one coordinate factor.\\n        pub fn emitWickCoordinate(self: *Self, factor: coefficient.CoordinateFactor) !void {\\n            if (self.coordinate_count == self.coordinates.len) return error.TooManyAccumulatedCoordinates;\\n            self.coordinates[self.coordinate_count] = factor;\\n            self.coordinate_count += 1;\\n            self.currentTerm().coordinate_count += 1;\\n            self.logical_coordinate_count += 1;\\n        }\\n\\n        /// emitWickTensor appends one tensor factor.\\n        pub fn emitWickTensor(self: *Self, factor: Correlator.ResolvedTensorFactor) !void {\\n            if (self.tensor_count == self.tensors.len) return error.TooManyAccumulatedTensors;\\n            self.tensors[self.tensor_count] = factor;\\n            self.tensor_count += 1;\\n            self.currentTerm().tensor_count += 1;\\n            self.logical_tensor_count += 1;\\n        }\\n\\n        /// emitWickAction appends one action factor.\\n        pub fn emitWickAction(self: *Self, factor: Correlator.ResolvedActionFactor) !void {\\n            if (self.action_count == self.actions.len) return error.TooManyAccumulatedActions;\\n            self.actions[self.action_count] = factor;\\n            self.action_count += 1;\\n            self.currentTerm().action_count += 1;\\n            self.logical_action_count += 1;\\n        }\\n\\n        /// emitWickResidualOperator appends one residual operator reference.\\n        pub fn emitWickResidualOperator(self: *Self, residual: Correlator.WickResidualRef) !void {\\n            if (self.residual_count == self.residuals.len) return error.TooManyAccumulatedResiduals;\\n            self.residuals[self.residual_count] = residual;\\n            self.residual_count += 1;\\n            self.currentTerm().residual_count += 1;\\n            self.logical_residual_count += 1;\\n        }\\n\\n        /// emitWickTermEnd accepts the primitive term boundary.\\n        pub fn emitWickTermEnd(_: *Self) !void {}\\n\\n        /// emit streams all accumulated primitive terms to the final sink.\\n        pub fn emit(self: *const Self, sink: anytype) !void {\\n            const SinkPtr = @TypeOf(sink);\\n            const sink_info = @typeInfo(SinkPtr);\\n            if (sink_info == .pointer and @hasDecl(sink_info.pointer.child, \\"emitWickBranchTerm\\")) {\\n                try sink.emitWickBranchTerm(self);\\n                return;\\n            }\\n\\n            const emit_residuals = sink_info == .pointer and @hasDecl(sink_info.pointer.child, \\"emitWickResidualOperator\\");\\n            const replay_actions = self.logical_action_count != 0;\\n            const replay_residuals = emit_residuals and self.logical_residual_count != 0;\\n\\n            var term_index: usize = 0;\\n            while (term_index < self.term_count) : (term_index += 1) {\\n                const term = self.terms[term_index];\\n                const source_term = if (term.cache_index == no_cached_pair) term else self.cache_terms[term.cache_index];\\n                try sink.emitWickTermStart(term.event);\\n\\n                var scalar_index: usize = 0;\\n                while (scalar_index < term.scalar_count) : (scalar_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickScalar(self.scalars[source_term.scalar_start + scalar_index]);\\n                    } else {\\n                        try sink.emitWickScalar(self.cache_scalars[source_term.scalar_start + scalar_index]);\\n                    }\\n                }\\n\\n                var coordinate_index: usize = 0;\\n                while (coordinate_index < term.coordinate_count) : (coordinate_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickCoordinate(self.coordinates[source_term.coordinate_start + coordinate_index]);\\n                    } else {\\n                        try sink.emitWickCoordinate(self.cache_coordinates[source_term.coordinate_start + coordinate_index]);\\n                    }\\n                }\\n\\n                var tensor_index: usize = 0;\\n                while (tensor_index < term.tensor_count) : (tensor_index += 1) {\\n                    if (term.cache_index == no_cached_pair) {\\n                        try sink.emitWickTensor(self.tensors[source_term.tensor_start + tensor_index]);\\n                    } else {\\n                        try sink.emitWickTensor(self.cache_tensors[source_term.tensor_start + tensor_index]);\\n                    }\\n                }\\n\\n                if (replay_actions) {\\n                    var action_index: usize = 0;\\n                    while (action_index < term.action_count) : (action_index += 1) {\\n                        if (term.cache_index == no_cached_pair) {\\n                            try sink.emitWickAction(self.actions[source_term.action_start + action_index]);\\n                        } else {\\n                            try sink.emitWickAction(self.cache_actions[source_term.action_start + action_index]);\\n                        }\\n                    }\\n                }\\n\\n                if (replay_residuals) {\\n                    var residual_index: usize = 0;\\n                    while (residual_index < term.residual_count) : (residual_index += 1) {\\n                        if (term.cache_index == no_cached_pair) {\\n                            try sink.emitWickResidualOperator(self.residuals[source_term.residual_start + residual_index]);\\n                        } else {\\n                            try sink.emitWickResidualOperator(self.cache_residuals[source_term.residual_start + residual_index]);\\n                        }\\n                    }\\n                }\\n\\n                try sink.emitWickTermEnd();\\n            }\\n        }\\n    };\\n}\\n\\nfn matchIndexedWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, term_ordinal: usize) !?Correlator.MatchedWickTerm {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var remaining = term_ordinal;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const entry = config_ptr.pair_lookup[entry_index];\\n        const rule_index: usize = @intCast(entry.rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        const rule = &config_ptr.wick_rules[rule_index];\\n        if (remaining < rule.expr.terms.len) {\\n            return .{ .pair = .{\\n                .rule = rule,\\n                .config = config_ptr,\\n                .left = left,\\n                .right = right,\\n                .labels = ops.labels,\\n                .reversed = entry.reversed,\\n            }, .rule_index = rule_index, .term_index = remaining };\\n        }\\n        remaining -= rule.expr.terms.len;\\n    }\\n\\n    _ = left_index;\\n    _ = right_index;\\n    return null;\\n}\\n\\nfn matchScannedWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, term_ordinal: usize) ?Correlator.MatchedWickTerm {\\n    var remaining = term_ordinal;\\n\\n    for (config_ptr.wick_rules, 0..) |*rule, rule_index| {\\n        if (matchRule(config_ptr, rule, left, right, ops.labels)) |pair| {\\n            if (remaining < pair.rule.expr.terms.len) {\\n                return .{ .pair = pair, .rule_index = rule_index, .term_index = remaining };\\n            }\\n            remaining -= pair.rule.expr.terms.len;\\n        }\\n    }\\n\\n    _ = left_index;\\n    _ = right_index;\\n    return null;\\n}\\n\\nfn matchWickPairTerm(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !?Correlator.MatchedWickTerm {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return matchIndexedWickPairTerm(config_ptr, ops, left_index, right_index, left, right, term_ordinal);\\n    }\\n\\n    return matchScannedWickPairTerm(config_ptr, ops, left_index, right_index, left, right, term_ordinal);\\n}\\n\\nfn emitIndexedWickPairTerms(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, sink: anytype) !usize {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var emitted: usize = 0;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const entry = config_ptr.pair_lookup[entry_index];\\n        const rule_index: usize = @intCast(entry.rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        const rule = &config_ptr.wick_rules[rule_index];\\n        const pair = Correlator.WickPair{\\n            .rule = rule,\\n            .config = config_ptr,\\n            .left = left,\\n            .right = right,\\n            .labels = ops.labels,\\n            .reversed = entry.reversed,\\n        };\\n\\n        var term_index: usize = 0;\\n        while (term_index < pair.rule.expr.terms.len) : (term_index += 1) {\\n            try emitPrimitiveWickTerm(pair, left_index, right_index, term_index, sink);\\n            emitted += 1;\\n        }\\n    }\\n\\n    return emitted;\\n}\\n\\nfn emitScannedWickPairTerms(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp, sink: anytype) !usize {\\n    var emitted: usize = 0;\\n\\n    for (config_ptr.wick_rules) |*rule| {\\n        if (matchRule(config_ptr, rule, left, right, ops.labels)) |pair| {\\n            var term_index: usize = 0;\\n            while (term_index < pair.rule.expr.terms.len) : (term_index += 1) {\\n                try emitPrimitiveWickTerm(pair, left_index, right_index, term_index, sink);\\n                emitted += 1;\\n            }\\n        }\\n    }\\n\\n    return emitted;\\n}\\n\\nfn countIndexedWickPairTerms(config_ptr: *const CorrelatorConfigData, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) !usize {\\n    const key = wickRuleKey(left.kind, right.kind);\\n    var entry_index = lowerBoundPairLookup(config_ptr.pair_lookup, key);\\n    var count: usize = 0;\\n\\n    while (entry_index < config_ptr.pair_lookup.len and config_ptr.pair_lookup[entry_index].key == key) : (entry_index += 1) {\\n        const rule_index: usize = @intCast(config_ptr.pair_lookup[entry_index].rule_index);\\n        if (rule_index >= config_ptr.wick_rules.len) return error.InvalidPairLookup;\\n        count += config_ptr.wick_rules[rule_index].expr.terms.len;\\n    }\\n\\n    return count;\\n}\\n\\nfn countScannedWickPairTerms(config_ptr: *const CorrelatorConfigData, left: kernel.Call.LocalOp, right: kernel.Call.LocalOp) usize {\\n    var count: usize = 0;\\n    for (config_ptr.wick_rules) |rule| {\\n        if (ruleMatches(rule, left, right) != null) count += rule.expr.terms.len;\\n    }\\n    return count;\\n}\\n\\n/// countWickPairTerms counts primitive terms for two input positions without resolving factors.\\nfn countWickPairTermsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize) !usize {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return countIndexedWickPairTerms(config_ptr, left, right);\\n    }\\n\\n    return countScannedWickPairTerms(config_ptr, left, right);\\n}\\n\\nfn emitWickPairTermsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, sink: anytype) !usize {\\n    if (left_index >= ops.operators.len or right_index >= ops.operators.len or left_index == right_index) return error.InvalidOperatorIndex;\\n\\n    const left = ops.operators[left_index];\\n    const right = ops.operators[right_index];\\n    if (config_ptr.pair_lookup.len != 0) {\\n        return emitIndexedWickPairTerms(config_ptr, ops, left_index, right_index, left, right, sink);\\n    }\\n\\n    return emitScannedWickPairTerms(config_ptr, ops, left_index, right_index, left, right, sink);\\n}\\n\\nfn emitWickPairTermData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, sink: anytype) !void {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    try emitPrimitiveWickTerm(matched.pair, left_index, right_index, matched.term_index, sink);\\n}\\n\\nfn wickPairTermPayloadData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !u64 {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    return wickPairTermPayloadFrom(matched.rule_index, matched.term_index, matched.pair.reversed);\\n}\\n\\nfn wickPairTermInfoData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    const source = matched.pair.rule.expr.terms[matched.term_index].residuals;\\n    if (source.len > residuals.len) return error.TooManyWickTermResiduals;\\n    for (source, 0..) |side, index| {\\n        residuals[index] = actualSideIndex(matched.pair, left_index, right_index, side);\\n    }\\n    payload.* = try wickPairTermPayloadFrom(matched.rule_index, matched.term_index, matched.pair.reversed);\\n    residual_count.* = source.len;\\n}\\n\\nfn emitWickPairTermPayloadData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, sink: anytype) !void {\\n    const rule_index = wickPayloadRuleIndex(payload);\\n    if (rule_index >= config_ptr.wick_rules.len) return error.InvalidWickTerm;\\n    const pair = Correlator.WickPair{\\n        .rule = &config_ptr.wick_rules[rule_index],\\n        .config = config_ptr,\\n        .left = ops.operators[left_index],\\n        .right = ops.operators[right_index],\\n        .labels = ops.labels,\\n        .reversed = wickPayloadReversed(payload),\\n    };\\n    try emitPrimitiveWickTerm(pair, left_index, right_index, wickPayloadTermIndex(payload), sink);\\n}\\n\\nfn wickPairTermResidualsData(config_ptr: *const CorrelatorConfigData, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize) !usize {\\n    const matched = try matchWickPairTerm(config_ptr, ops, left_index, right_index, term_ordinal) orelse return error.InvalidWickTerm;\\n    const source = matched.pair.rule.expr.terms[matched.term_index].residuals;\\n    if (source.len > residuals.len) return error.TooManyWickTermResiduals;\\n    for (source, 0..) |side, index| {\\n        residuals[index] = actualSideIndex(matched.pair, left_index, right_index, side);\\n    }\\n    return source.len;\\n}\\n\\nfn RuntimeWickTactic(comptime ConfigHandle: type) type {\\n    return struct {\\n        /// Config is the shared preset runtime correlator config.\\n        pub const Config = CorrelatorConfigData;\\n        const traversal_operator_count = ConfigHandle.branch_limits.max_operators;\\n        /// BranchState accumulates resolved Wick factors along one DFS branch.\\n        pub const BranchState = AccumulatedBranch(ConfigHandle.branch_limits);\\n\\n        /// emitPairTerms adapts original occurrence pairs to the primitive Wick emitter.\\n        pub fn emitPairTerms(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, sink: anytype) !usize {\\n            return emitWickPairTermsData(config_ptr, ops, left_index, right_index, sink);\\n        }\\n\\n        /// emitPairTerm emits one selected primitive Wick term.\\n        pub fn emitPairTerm(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, sink: anytype) !void {\\n            return emitWickPairTermData(config_ptr, ops, left_index, right_index, term_ordinal, sink);\\n        }\\n\\n        /// pairTermCount counts primitive pair terms without emitting factors.\\n        pub fn pairTermCount(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize) !usize {\\n            return countWickPairTermsData(config_ptr, ops, left_index, right_index);\\n        }\\n\\n        /// pairTermResiduals returns original residual occurrences for one primitive term.\\n        pub fn pairTermResiduals(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize) !usize {\\n            return wickPairTermResidualsData(config_ptr, ops, left_index, right_index, term_ordinal, residuals);\\n        }\\n\\n        /// pairTermPayload returns cached rule payload for a primitive pair term.\\n        pub fn pairTermPayload(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize) !u64 {\\n            return wickPairTermPayloadData(config_ptr, ops, left_index, right_index, term_ordinal);\\n        }\\n\\n        /// pairTermInfo returns residual occurrences and cached replay payload from one pair match.\\n        pub fn pairTermInfo(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, term_ordinal: usize, residuals: *[2]usize, payload: *u64, residual_count: *usize) !void {\\n            return wickPairTermInfoData(config_ptr, ops, left_index, right_index, term_ordinal, residuals, payload, residual_count);\\n        }\\n\\n        /// emitPairTermPayload replays one primitive term without matching rules again.\\n        pub fn emitPairTermPayload(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, sink: anytype) !void {\\n            return emitWickPairTermPayloadData(config_ptr, ops, left_index, right_index, payload, sink);\\n        }\\n\\n        /// pushBranchPairTerm appends one primitive pair term to a branch accumulator.\\n        pub fn pushBranchPairTerm(config_ptr: *const Config, ops: kernel.Call.MultiOp, left_index: usize, right_index: usize, payload: u64, branch: *BranchState) !void {\\n            return branch.pushPairTerm(config_ptr, ops, left_index, right_index, payload);\\n        }\\n\\n        /// emitAccumulatedBranch streams cached pair factors followed by the zero-mode base case.\\n        pub fn emitAccumulatedBranch(config_ptr: *const Config, ops: kernel.Call.MultiOp, branch: *const BranchState, sign: i8, residual_indices: []const usize, sink: anytype) !void {\\n            if (sign < 0) {\\n                try emitBranchSign(config_ptr, sign, sink);\\n            }\\n            try branch.emit(sink);\\n            try generatedTrustedZeroModeBaseCase(ConfigHandle, config_ptr, ops, residual_indices, sink);\\n        }\\n\\n        /// emitFullContractionBranch streams a full Wick branch with the empty base case.\\n        pub fn emitFullContractionBranch(config_ptr: *const Config, ops: kernel.Call.MultiOp, branch: *const BranchState, sign: i8, sink: anytype) !void {\\n            if (sign < 0) {\\n                try emitBranchSign(config_ptr, sign, sink);\\n            }\\n            try branch.emit(sink);\\n            _ = ops;\\n            try generatedEmptyZeroModeBaseCase(ConfigHandle, config_ptr, sink);\\n        }\\n\\n        /// operatorParity returns true for configured fermionic operator kinds.\\n        pub fn operatorParity(config_ptr: *const Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n            const kind = ops.operators[index].kind;\\n            return kindIn(kind, config_ptr.fermion_kinds);\\n        }\\n\\n        /// operatorCanRemainInZeroMode returns true when a residual can be consumed by zero modes.\\n        pub fn operatorCanRemainInZeroMode(config_ptr: *const Config, ops: kernel.Call.MultiOp, index: usize) bool {\\n            _ = config_ptr;\\n            return generatedZeroModeCanConsumeKind(ConfigHandle, ops.operators[index].kind);\\n        }\\n\\n        /// emitBranchSign forwards an accumulated fermion sign when the sink accepts it.\\n        pub fn emitBranchSign(_: *const Config, sign: i8, sink: anytype) !void {\\n            const SinkPtr = @TypeOf(sink);\\n            const sink_info = @typeInfo(SinkPtr);\\n            if (sink_info != .pointer) return;\\n            const Sink = sink_info.pointer.child;\\n            if (@hasDecl(Sink, \\"emitWickBranchSign\\")) {\\n                try sink.emitWickBranchSign(sign);\\n            }\\n        }\\n\\n        /// emitZeroModeBaseCase adapts residual original occurrences to the zero-mode evaluator.\\n        pub fn emitZeroModeBaseCase(config_ptr: *const Config, ops: kernel.Call.MultiOp, residual_indices: []const usize, sink: anytype) !bool {\\n            return generatedZeroModeBaseCase(ConfigHandle, config_ptr, ops, residual_indices, sink);\\n        }\\n\\n        /// zeroModeBaseCaseSucceeds tests base-case viability without emitting factors.\\n        pub fn zeroModeBaseCaseSucceeds(config_ptr: *const Config, ops: kernel.Call.MultiOp, residual_indices: []const usize) !bool {\\n            return generatedZeroModeBaseCaseSucceeds(ConfigHandle, config_ptr, ops, residual_indices);\\n        }\\n    };\\n}\\n\\nfn operatorListData(ops: anytype) KernelOperatorList {\\n    const Ops = @TypeOf(ops);\\n    if (Ops == KernelOperatorList) return ops;\\n    return switch (@typeInfo(Ops)) {\\n        .pointer => |ptr| if (ptr.child == OperatorList) blk: {\\n            const state: *const LocalStateImpl = @ptrCast(@alignCast(ops));\\n            break :blk .{ .operators = state.owned_operators, .labels = &state.label_store };\\n        } else {\\n            @compileError(\\"correlator operators must come from local.ops\\");\\n        },\\n        else => @compileError(\\"correlator operators must come from local.ops\\"),\\n    };\\n}\\n\\n/// streamCorrelator evaluates Wick branches and streams accepted base cases.\\npub fn streamCorrelator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n    const Ptr = @TypeOf(config_ptr);\\n    const ptr_info = @typeInfo(Ptr);\\n    if (ptr_info != .pointer) @compileError(\\"correlator config must be passed by pointer\\");\\n    const ConfigHandle = ptr_info.pointer.child;\\n    const data = configData(config_ptr);\\n    return wick_correlator.wickCorrelator(RuntimeWickTactic(ConfigHandle), data, operatorListData(ops), sink);\\n}\\n","n":0.008}}},{"i":25,"$":{"1":{"v":"Free Boson Preset","n":0.577},"2":{"v":"This node defines the free-boson preset on the sphere and its\\nNeumann/Dirichlet boundary extension.  It uses the compact vocabulary from\\nPreset Shared Runtime and is exported through\\nPresets.\\nThe preset does not perform tensor algebra.  A \\\\(D\\\\)-dimensional target space\\nonly fixes which \\\\(SO(D)\\\\)-type labels are expected; later coefficient evaluation\\nuses Tensor-Code to evaluate metrics, momenta, and\\nprojectors.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Local = shared.Local;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nThe preset owns the free-boson operator vocabulary.  The enum below is\\nphysics data; Preset Shared Runtime only receives the compact kind ids derived\\nfrom it.  The base separates this preset\'s ids from other implemented presets,\\nwhile individual ids still come from enum order rather than handwritten\\nnumbers.\\nconst namespace = \\"free_boson\\";\\nconst scalars = declare.scalars;\\nconst RuleScalar = @TypeOf(scalars.one());\\nconst alpha_prime_atom = scalars.atom(namespace, \\"alpha_prime\\");\\nconst k_disk_atom = scalars.atom(namespace, \\"K_D2\\");\\nconst neumann_projector_ref = declare.configRef(namespace, \\"neumann_projector\\");\\nconst dirichlet_projector_ref = declare.configRef(namespace, \\"dirichlet_projector\\");\\nconst bulk_boundary_projector_ref = declare.configRef(namespace, \\"bulk_boundary_projector\\");\\nconst dirichlet_position_ref = declare.configRef(namespace, \\"dirichlet_position\\");\\nconst chan_paton_ref = declare.configRef(namespace, \\"chan_paton\\");\\nconst target_dimension_ref = declare.configRef(namespace, \\"target_dimension\\");\\n\\nconst Family = enum {\\n    x,\\n    d_x,\\n    d_xt,\\n    exp_x,\\n    profile_x,\\n    d_x_boundary,\\n    exp_x_boundary,\\n    profile_x_boundary,\\n};\\n\\nconst ZeroSector = enum {\\n    bulk,\\n    boundary,\\n    torus,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\n\\nfn zeroSector(comptime sector: ZeroSector) zero_mode.Sector {\\n    return declare.sectorId(namespace, sector);\\n}\\n\\nfn rawToken(value: anytype) u32 {\\n    return @intFromEnum(value);\\n}\\n\\nfn token(comptime T: type, id: u32) T {\\n    return @enumFromInt(if (id == 0) 1 else id);\\n}\\n\\nfn stableNameId(comptime name: []const u8) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (name) |byte| {\\n        hash = (hash ^ @as(u32, byte)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn stableSubspaceId(comptime dimensions: []const u16) u32 {\\n    var hash: u32 = 2166136261;\\n    inline for (dimensions) |dimension| {\\n        hash = (hash ^ @as(u32, dimension & 0xff)) *% 16777619;\\n        hash = (hash ^ @as(u32, dimension >> 8)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn projectorId(comptime rank: u8, hash: u32) u32 {\\n    return (@as(u32, rank) << 24) | (hash & 0x00ff_ffff);\\n}\\n\\nfn polynomialProfileTerm(term: anytype) struct { coefficient: Handle.ProfileCoefficient, power: u16 } {\\n    const Term = @TypeOf(term);\\n    const info = @typeInfo(Term);\\n    if (info != .@\\"struct\\" or !info.@\\"struct\\".is_tuple or info.@\\"struct\\".fields.len != 2) {\\n        @compileError(\\"polynomial profile terms must be .{ coefficient, power } tuples\\");\\n    }\\n    return .{ .coefficient = term[0], .power = term[1] };\\n}\\n\\nfn polynomialProfileId(point: Handle.TargetPoint, terms: anytype) u32 {\\n    var hash = rawToken(point) ^ 0x9e37_79b9;\\n    inline for (terms) |raw_term| {\\n        const term = polynomialProfileTerm(raw_term);\\n        hash = (hash ^ rawToken(term.coefficient)) *% 16777619;\\n        hash = (hash ^ @as(u32, term.power)) *% 16777619;\\n    }\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nconst TargetSpace = struct {\\n    /// subspace names a target-space projector onto a coordinate subspace.\\n    pub fn subspace(comptime dimensions: []const u16) Handle.TensorProjector {\\n        return token(Handle.TensorProjector, projectorId(@intCast(dimensions.len), stableSubspaceId(dimensions)));\\n    }\\n\\n    /// complement names the complementary target-space projector.\\n    pub fn complement(comptime dimensions: []const u16) Handle.TensorProjector {\\n        return token(Handle.TensorProjector, projectorId(0, stableSubspaceId(dimensions) ^ 0xa5a5_a5a5));\\n    }\\n\\n    /// point names a target-space point used by boundary conditions.\\n    pub fn point(comptime name: []const u8) Handle.TargetPoint {\\n        return token(Handle.TargetPoint, stableNameId(name));\\n    }\\n\\n    /// boundaryStack names Chan-Paton data for a stacked boundary condition.\\n    pub fn boundaryStack(comptime name: []const u8) Handle.BoundaryStack {\\n        return token(Handle.BoundaryStack, stableNameId(name));\\n    }\\n};\\nBulk PresetThe default normalization is the stringbook free-field normalization\\n\\\\[\\n  T^X=-{1\\\\over\\\\alpha\'}\\\\,\\\\partial X^\\\\mu \\\\partial X_\\\\mu\\n\\\\]\\n.  The rule scalar remains symbolic here because\\ndifferent applications set \\\\(\\\\alpha\'\\\\) and \\\\(\\\\partial X\\\\) normalizations\\ndifferently.\\nconst FreeBosonConfig = struct {\\n    dimension: u16,\\n};\\n\\n/// freeBoson builds the generated preset type for a noncompact free-boson CFT.\\npub fn freeBoson(comptime cfg: FreeBosonConfig) type {\\n    return struct {\\n        /// op exposes free-boson local operator builders.\\n        pub const op = FreeBosonOp;\\n        /// profile exposes profile-presentation builders.\\n        pub const profile = ProfileOp;\\n        /// target exposes deterministic target-space tokens for preset data.\\n        pub const target = TargetSpace;\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = FreeBosonCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for free-boson insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nThe derivative meaning is \\\\(dX(\\\\mu,n,z)=\\\\partial^{n+1}X^\\\\mu(z)\\\\).  The\\nexplicit \\\\(X\\\\) body is kept because NLSM expansions and position-space profiles\\nproduce logarithmic Green kernels rather than only plane-wave contractions.\\nconst FreeBosonOp = struct {\\n    /// X builds an explicit bulk free-boson field insertion.\\n    pub fn X(local: anytype, mu: Handle.Index, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.x)).pair(local, z, zbar, .{mu});\\n    }\\n\\n    /// dX builds a holomorphic derivative field insertion.\\n    pub fn dX(local: anytype, mu: Handle.Index, n: u8, z: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.d_x)).single(local, z, n, .{mu});\\n    }\\n\\n    /// dXt builds an antiholomorphic derivative field insertion.\\n    pub fn dXt(local: anytype, mu: Handle.Index, n: u8, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.d_xt)).single(local, zbar, n, .{mu});\\n    }\\n\\n    /// expX builds a normal-ordered plane-wave insertion.\\n    pub fn expX(local: anytype, k: Handle.Momentum, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.exp_x)).pair(local, z, zbar, .{k});\\n    }\\n\\n    /// profile builds a target-space profile insertion.\\n    pub fn profile(local: anytype, f: Handle.Profile, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(sphereOperator(.profile_x)).pair(local, z, zbar, .{f});\\n    }\\n\\n    /// profileVector builds a vector-valued target-space profile insertion.\\n    pub fn profileVector(local: anytype, f: Handle.Profile, nu: Handle.Index, z: Handle.Coord, zbar: Handle.Coord) !Operator {\\n        return declare.operatorBuilder(profile_vector_operator).pair(local, z, zbar, .{ f, nu });\\n    }\\n};\\nProfiles are handles to a chosen presentation.  Fourier profiles resolve to\\nplane waves and momentum conservation can partially evaluate the inverse\\nFourier transform.  Position-space profiles keep the zero-mode integral\\nexplicit.  Polynomial profiles support Riemann-normal-coordinate expansions.\\nconst ProfileOp = struct {\\n    /// positionSpace selects the exact residual position-space profile presentation.\\n    pub fn positionSpace(f: Handle.TargetFunction) Handle.Profile {\\n        return shared.profileHandle(.position_space, rawToken(f));\\n    }\\n\\n    /// fourier selects the Fourier-space profile presentation.\\n    pub fn fourier(f_hat: Handle.FourierTransform) Handle.Profile {\\n        return shared.profileHandle(.fourier, rawToken(f_hat));\\n    }\\n\\n    /// polynomialRnc selects a finite polynomial profile presentation.\\n    pub fn polynomialRnc(point: Handle.TargetPoint, terms: anytype) Handle.Profile {\\n        return shared.profileHandle(.polynomial_rnc, polynomialProfileId(point, terms));\\n    }\\n};\\nSphere RulesOn the sphere the primitive free-boson rules include same-chirality\\n\\\\(\\\\partial X\\\\,\\\\partial X\\\\) contractions, \\\\(\\\\partial X\\\\) contractions with\\nplane waves and target-space profiles, and the plane-wave Green-kernel factor.\\nLabels supply indices, momenta, and profile handles at runtime.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn bulkHolomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .holomorphic), wick.coord(.right, .holomorphic));\\n}\\n\\nfn bulkAntiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .antiholomorphic), wick.coord(.right, .antiholomorphic));\\n}\\n\\nfn bulkToBoundaryHolomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .holomorphic), wick.coord(.right, .position));\\n}\\n\\nfn bulkToBoundaryAntiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .antiholomorphic), wick.coord(.right, .position));\\n}\\n\\nfn freeBosonDxDx(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn freeBosonDxtDxt(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, antiholomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn freeBosonDxExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .momentum_index = .{\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn freeBosonDxProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, holomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.profileDerivative(wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn freeBosonDxtExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .momentum_index = .{\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, antiholomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn freeBosonDxtProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, antiholomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.profileDerivative(wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn freeBosonExpXExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.expr(&.{wick.termWithResiduals(&.{wick.scalar(scalars.div(alpha, 2))}, &.{\\n        .{ .green_exponential = bulkHolomorphicDifference() },\\n        .{ .green_exponential = bulkAntiholomorphicDifference() },\\n    }, &.{.{ .momentum_pair = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }}, &.{}, &.{ .left, .right })});\\n}\\n\\nfn sphereZeroSpec(comptime normalization: RuleScalar) [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.bulk), .expr = .{ .free_boson_constant_mode = .{\\n            .exp_kind_ids = &.{kind(.exp_x)},\\n            .profile_kind_ids = &.{kind(.profile_x)},\\n            .normalization = .{\\n                .scalar = normalization,\\n                .two_pi_power = .{ .target_dimension = target_dimension_ref },\\n            },\\n        } } },\\n    };\\n}\\n\\nfn sphereZeroStorage() [1]zero_mode.Rule {\\n    const spec = sphereZeroSpec(.one);\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nconst sphere_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"X\\", .kind = kind(.x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"dX\\", .kind = kind(.d_x), .support = .holomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"dXt\\", .kind = kind(.d_xt), .support = .antiholomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"expX\\", .kind = kind(.exp_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.momentum}, .statistics = .bosonic, .zero_mode_consumable = true },\\n    .{ .name = \\"profile\\", .kind = kind(.profile_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{.profile}, .statistics = .bosonic, .zero_mode_consumable = true },\\n};\\n\\nfn sphereWickSpec(comptime alpha: RuleScalar) [7]Spec.WickRule {\\n    return [_]Spec.WickRule{\\n        .{ .left = kind(.d_x), .right = kind(.d_x), .expr = freeBosonDxDx(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.d_xt), .expr = freeBosonDxtDxt(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.exp_x), .expr = freeBosonDxExpX(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.exp_x), .expr = freeBosonDxtExpX(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.profile_x), .expr = freeBosonDxProfile(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.profile_x), .expr = freeBosonDxtProfile(alpha) },\\n        .{ .left = kind(.exp_x), .right = kind(.exp_x), .expr = freeBosonExpXExpX(alpha) },\\n    };\\n}\\n\\nconst sphere_wick_spec = sphereWickSpec(.one);\\n\\nconst torus_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.d_x), .right = kind(.d_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.d_xt), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_x), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_x), .right = kind(.profile_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.d_xt), .right = kind(.profile_x), .coordinate_kernels = &.{.elliptic_green} },\\n    .{ .left = kind(.exp_x), .right = kind(.exp_x), .coordinate_kernels = &.{.elliptic_green_exponential} },\\n};\\n\\nconst sphere_zero_spec = sphereZeroSpec(.one);\\nThe torus draft follows the genus-one convention in the stringbook: the\\ncoordinate obeys \\\\(z\\\\sim z+2\\\\pi\\\\sim z+2\\\\pi\\\\tau\\\\), the nonzero modes use the\\ntorus Green function built from \\\\(\\\\theta_1(z|\\\\tau)\\\\), and the constant\\ntarget-space mode gives the momentum-conservation factor in the exponential\\ncorrelator .  The metadata below records only that\\nzero-mode sector and its consumed operator families; it is not lowered into a\\nruntime config until the torus evaluator is implemented.\\nconst torus_zero_spec = [_]Spec.ZeroModeRule{\\n    .{\\n        .sector = zeroSector(.torus),\\n        .kind = .torus_free_boson_constant,\\n        .consumes = &.{ kind(.exp_x), kind(.profile_x) },\\n    },\\n};\\n\\nconst sphere_spec = Spec.Theory{\\n    .operators = &sphere_operator_spec,\\n    .wick_rules = &sphere_wick_spec,\\n    .zero_modes = &sphere_zero_spec,\\n};\\n\\nconst torus_draft_spec = Spec.Theory{\\n    .surface = .{\\n        .kind = .torus,\\n        .coordinate_model = .elliptic,\\n        .modular_parameters = &.{\\"tau\\"},\\n        .source = .stringbook,\\n    },\\n    .operators = &sphere_operator_spec,\\n    .wick_rules = &torus_wick_spec,\\n    .zero_modes = &torus_zero_spec,\\n};\\n\\nconst boundary_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"dXBoundary\\", .kind = kind(.d_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.index}, .statistics = .bosonic },\\n    .{ .name = \\"expXBoundary\\", .kind = kind(.exp_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.momentum}, .statistics = .bosonic, .zero_mode_consumable = true },\\n    .{ .name = \\"profileBoundary\\", .kind = kind(.profile_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{.profile}, .statistics = .bosonic, .zero_mode_consumable = true },\\n};\\n\\nconst disk_operator_spec = sphere_operator_spec ++ boundary_operator_spec;\\n\\nconst profile_vector_operator = Spec.Operator{ .name = \\"profileVector\\", .kind = kind(.profile_x), .support = .bulk_pair, .insertion = .pair, .labels = &.{ .profile, .index }, .statistics = .bosonic, .zero_mode_consumable = true };\\nconst boundary_profile_vector_operator = Spec.Operator{ .name = \\"profileBoundaryVector\\", .kind = kind(.profile_x_boundary), .support = .boundary, .insertion = .single, .labels = &.{ .profile, .index }, .statistics = .bosonic, .zero_mode_consumable = true };\\n\\nfn sphereOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-boson sphere operator spec\\");\\n}\\n\\nfn boundaryOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (boundary_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-boson boundary operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime wick_count: usize, comptime zero_count: usize) void {\\n    if (sphere_spec.wick_rules.len != wick_count) @compileError(\\"free-boson sphere spec Wick count does not match lowered rules\\");\\n    if (sphere_spec.zero_modes.len != zero_count) @compileError(\\"free-boson sphere spec zero-mode count does not match lowered rules\\");\\n    if (Spec.zeroModeConsumeKindCount(sphere_spec.zero_modes[0]) != 2) @compileError(\\"free-boson sphere spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn assertTorusDraftSpec() void {\\n    if (torus_draft_spec.surface.kind != .torus) @compileError(\\"free-boson torus draft spec surface kind is incomplete\\");\\n    if (torus_draft_spec.surface.coordinate_model != .elliptic) @compileError(\\"free-boson torus draft spec coordinate model is incomplete\\");\\n    if (torus_draft_spec.surface.modular_parameters.len != 1) @compileError(\\"free-boson torus draft spec modular metadata is incomplete\\");\\n    if (torus_draft_spec.surface.source != .stringbook) @compileError(\\"free-boson torus draft spec source convention is incomplete\\");\\n    if (torus_draft_spec.operators.len != sphere_spec.operators.len) @compileError(\\"free-boson torus draft spec operator metadata is incomplete\\");\\n    if (torus_draft_spec.wick_rules.len != sphere_spec.wick_rules.len) @compileError(\\"free-boson torus draft spec Wick metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_green) != 6) @compileError(\\"free-boson torus draft spec Green-kernel metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_green_exponential) != 1) @compileError(\\"free-boson torus draft spec exponential Green-kernel metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .rational_pole) != 0) @compileError(\\"free-boson torus draft spec still uses rational pole metadata\\");\\n    if (torus_draft_spec.zero_modes.len != 1) @compileError(\\"free-boson torus draft spec zero-mode metadata is incomplete\\");\\n    if (Spec.zeroModeConsumeKindCount(torus_draft_spec.zero_modes[0]) != 2) @compileError(\\"free-boson torus draft spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn FreeBosonRules(comptime cfg: FreeBosonConfig) type {\\n    _ = cfg;\\n    const alpha = scalars.atomScalar(alpha_prime_atom);\\n    return struct {\\n        const sphere_wick_runtime_spec = sphereWickSpec(alpha);\\n        const sphere_wick_storage = Spec.wickRules(&sphere_wick_runtime_spec, &sphere_operator_spec);\\n\\n        const sphere_wick: []const wick.Rule = &sphere_wick_storage;\\n    };\\n}\\n\\nfn FreeBosonCorrelatorConfig(comptime cfg: FreeBosonConfig) type {\\n    const rules = FreeBosonRules(cfg);\\n    return struct {\\n        const sphere_zero_storage = sphereZeroStorage();\\n        comptime {\\n            assertSphereSpec(rules.sphere_wick.len, sphere_zero_storage.len);\\n            assertTorusDraftSpec();\\n        }\\n\\n        /// sphere selects free-boson sphere Wick and zero-mode rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &sphere_zero_storage,\\n            .config_entries = &declare.configEntries(.{\\n                declare.config.targetDimension(target_dimension_ref, cfg.dimension),\\n            }),\\n        }){};\\n    };\\n}\\nBoundary ExtensionThe boundary extension is separate from the bulk free boson.  It adds\\nNeumann/Dirichlet data, optional Chan-Paton stack data, boundary-local\\noperators, mixed bulk-boundary rules, and disk zero-mode rules.  The doubling\\nchoice is already reflected in these rule templates before runtime.\\nconst FreeBosonBoundaryConfig = struct {\\n    neumann: Handle.TensorProjector,\\n    dirichlet: Handle.TensorProjector,\\n    dirichlet_position: Handle.TargetPoint,\\n    chan_paton: ?Handle.BoundaryStack = null,\\n};\\n\\nconst FreeBosonBoundaryOp = struct {\\n    /// dXBoundary builds stringbook\'s holomorphic boundary limit of dX.\\n    pub fn dXBoundary(local: anytype, mu: Handle.Index, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.d_x_boundary)).boundarySingle(local, y, n, .{mu});\\n    }\\n\\n    /// expXBoundary builds a Neumann boundary plane-wave insertion.\\n    pub fn expXBoundary(local: anytype, k: Handle.Momentum, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.exp_x_boundary)).boundarySingle(local, y, 0, .{k});\\n    }\\n\\n    /// profileBoundary builds a boundary profile insertion.\\n    pub fn profileBoundary(local: anytype, f: Handle.Profile, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundaryOperator(.profile_x_boundary)).boundarySingle(local, y, 0, .{f});\\n    }\\n\\n    /// profileBoundaryVector builds a vector-valued boundary profile insertion.\\n    pub fn profileBoundaryVector(local: anytype, f: Handle.Profile, nu: Handle.Index, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(boundary_profile_vector_operator).boundarySingle(local, y, 0, .{ f, nu });\\n    }\\n};\\n\\nfn FreeBosonBoundaryExtension(comptime cfg: FreeBosonBoundaryConfig) type {\\n    const data = FreeBosonBoundaryData(cfg);\\n    return declare.BoundaryExtension(.{\\n        .op = FreeBosonBoundaryOp,\\n        .wick_rules = data.disk_wick,\\n        .zero_modes = data.disk_zero_modes,\\n        .config_entries = data.config_entries,\\n    });\\n}\\n\\n/// boundaryExtension declares the Neumann/Dirichlet free-boson boundary extension.\\npub fn boundaryExtension(comptime cfg: FreeBosonBoundaryConfig) FreeBosonBoundaryExtension(cfg) {\\n    return .{};\\n}\\nThe boundary rules use projectors named by the correlator config.  These are\\nnot fake runtime labels: they point to configuration data such as the Neumann\\nprojector and the bulk-boundary doubling projector.\\nfn boundaryFreeBosonDxDx(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.neg(alpha), .{ .projector_metric = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn boundaryFreeBosonDxExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.neg(scalars.i(alpha)), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn boundaryFreeBosonDxProfile(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.neg(alpha), .none, holomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = neumann_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn boundaryFreeBosonExpXExpX(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.greenExponentialWithResiduals(alpha, .{ .projector_momentum_pair = .{\\n        .projector = .{ .config = neumann_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), &.{ .left, .right });\\n}\\n\\nfn mixedFreeBosonDxDxBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .projector_metric = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn mixedFreeBosonDxtDxBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPole(scalars.div(scalars.neg(alpha), 2), .{ .projector_metric = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryAntiholomorphicDifference(), -2, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn mixedFreeBosonDxExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxProfileBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, bulkToBoundaryHolomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = bulk_boundary_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxtExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedPoleWithResiduals(scalars.div(scalars.neg(scalars.i(alpha)), 2), .{ .projector_momentum_index = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .momentum = wick.label(.right, 0),\\n        .index = wick.label(.left, 0),\\n    } }, bulkToBoundaryAntiholomorphicDifference(), -1, .{ .include_left = true }, &.{.right});\\n}\\n\\nfn mixedFreeBosonDxtProfileBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.differentiatedActionPoleWithResiduals(scalars.div(scalars.neg(alpha), 2), .none, bulkToBoundaryAntiholomorphicDifference(), -1, .{ .include_left = true }, &.{\\n        wick.projectedProfileDerivative(.{ .config = bulk_boundary_projector_ref }, wick.label(.right, 0), wick.label(.left, 0)),\\n    }, &.{.right});\\n}\\n\\nfn mixedFreeBosonExpXExpXBoundary(comptime alpha: RuleScalar) wick.Expr {\\n    return wick.greenExponentialWithResiduals(scalars.div(alpha, 2), .{ .projector_momentum_pair = .{\\n        .projector = .{ .config = bulk_boundary_projector_ref },\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, bulkToBoundaryHolomorphicDifference(), &.{ .left, .right });\\n}\\n\\nfn diskWickSpec(comptime alpha: RuleScalar) [11]Spec.WickRule {\\n    return [_]Spec.WickRule{\\n        .{ .left = kind(.d_x_boundary), .right = kind(.d_x_boundary), .expr = boundaryFreeBosonDxDx(alpha) },\\n        .{ .left = kind(.d_x_boundary), .right = kind(.exp_x_boundary), .expr = boundaryFreeBosonDxExpX(alpha) },\\n        .{ .left = kind(.d_x_boundary), .right = kind(.profile_x_boundary), .expr = boundaryFreeBosonDxProfile(alpha) },\\n        .{ .left = kind(.exp_x_boundary), .right = kind(.exp_x_boundary), .expr = boundaryFreeBosonExpXExpX(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.d_x_boundary), .expr = mixedFreeBosonDxDxBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.d_x_boundary), .expr = mixedFreeBosonDxtDxBoundary(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonDxExpXBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonDxtExpXBoundary(alpha) },\\n        .{ .left = kind(.d_x), .right = kind(.profile_x_boundary), .expr = mixedFreeBosonDxProfileBoundary(alpha) },\\n        .{ .left = kind(.d_xt), .right = kind(.profile_x_boundary), .expr = mixedFreeBosonDxtProfileBoundary(alpha) },\\n        .{ .left = kind(.exp_x), .right = kind(.exp_x_boundary), .expr = mixedFreeBosonExpXExpXBoundary(alpha) },\\n    };\\n}\\n\\nfn diskZeroSpec(comptime normalization: RuleScalar) [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.boundary), .expr = .{ .free_boson_constant_mode = .{\\n            .integration_projector = neumann_projector_ref,\\n            .fixed_projector = dirichlet_projector_ref,\\n            .fixed_position = dirichlet_position_ref,\\n            .exp_kind_ids = &.{ kind(.exp_x), kind(.exp_x_boundary) },\\n            .profile_kind_ids = &.{ kind(.profile_x), kind(.profile_x_boundary) },\\n            .normalization = .{\\n                .scalar = normalization,\\n                .two_pi_power = .{ .projector_rank = neumann_projector_ref },\\n            },\\n        } } },\\n    };\\n}\\n\\nfn diskZeroStorage() [1]zero_mode.Rule {\\n    const spec = diskZeroSpec(scalars.atomScalar(k_disk_atom));\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nfn FreeBosonBoundaryData(comptime cfg: FreeBosonBoundaryConfig) type {\\n    const alpha = scalars.atomScalar(alpha_prime_atom);\\n    return struct {\\n        const disk_wick_runtime_spec = diskWickSpec(alpha);\\n        const disk_wick_storage = Spec.wickRules(&disk_wick_runtime_spec, &disk_operator_spec);\\n        const disk_zero_storage = diskZeroStorage();\\n        const config_storage = if (cfg.chan_paton) |stack| declare.configEntries(.{\\n            declare.config.tensorProjector(neumann_projector_ref, cfg.neumann),\\n            declare.config.tensorProjector(dirichlet_projector_ref, cfg.dirichlet),\\n            declare.config.tensorProjector(bulk_boundary_projector_ref, cfg.neumann),\\n            declare.config.targetPoint(dirichlet_position_ref, cfg.dirichlet_position),\\n            declare.config.boundaryStack(chan_paton_ref, stack),\\n        }) else declare.configEntries(.{\\n            declare.config.tensorProjector(neumann_projector_ref, cfg.neumann),\\n            declare.config.tensorProjector(dirichlet_projector_ref, cfg.dirichlet),\\n            declare.config.tensorProjector(bulk_boundary_projector_ref, cfg.neumann),\\n            declare.config.targetPoint(dirichlet_position_ref, cfg.dirichlet_position),\\n        });\\n\\n        const disk_wick: []const wick.Rule = &disk_wick_storage;\\n        const disk_zero_modes: []const zero_mode.Rule = &disk_zero_storage;\\n        const config_entries = &config_storage;\\n    };\\n}\\n\\ntest \\"free-boson schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n    const X = freeBoson(.{ .dimension = 10 });\\n\\n    var local = try X.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const k = try local.momentum(\\"k\\");\\n    const z = try local.coord(\\"z\\");\\n    const zbar = try local.coord(\\"zbar\\");\\n    const dx = try X.op.dX(&local, mu, 2, z);\\n    const exp = try X.op.expX(&local, k, z, zbar);\\n    const ops = try local.ops(.{ dx, exp });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(dx)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(exp)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\ntest \\"free-boson boundary schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n\\n    var local = try Local.init(testing.allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    const k = try local.momentum(\\"k\\");\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const dx = try FreeBosonBoundaryOp.dXBoundary(&local, mu, 3, y);\\n    const exp = try FreeBosonBoundaryOp.expXBoundary(&local, k, y);\\n    const ops = try local.ops(.{ dx, exp });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(dx)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(exp)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n","n":0.018}}},{"i":26,"$":{"1":{"v":"bc Preset","n":0.707},"2":{"v":"This node defines the \\\\(bc\\\\) ghost preset on the sphere and its disk boundary\\nextension.  It is independent of the matter CFT and uses the shared\\noperator/Wick vocabulary from Preset Shared Runtime.\\nThe local OPE is\\n\\\\[\\n  b(z)c(0)\\\\sim {1\\\\over z},\\n\\\\]\\nwith weights \\\\(h_b=2\\\\), \\\\(h_c=-1\\\\).  The sphere top form requires three\\n\\\\(c\\\\)-ghost insertions in each chiral copy .\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Local = shared.Local;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nThe ghost preset owns the \\\\(b,c\\\\) operator vocabulary.  It uses a separate id\\nblock from the free boson and derives each concrete kind id from the enum tag,\\nso this file names physics families without maintaining numeric ids per\\noperator.\\nconst namespace = \\"bc\\";\\n\\nconst Family = enum {\\n    b,\\n    c,\\n    bt,\\n    ct,\\n    b_boundary,\\n    c_boundary,\\n};\\n\\nconst ZeroSector = enum {\\n    sphere,\\n    disk,\\n    torus,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\n\\nfn zeroSector(comptime sector: ZeroSector) zero_mode.Sector {\\n    return declare.sectorId(namespace, sector);\\n}\\n\\nSphere PresetThe sphere preset exposes a holomorphic copy and, by default, an\\nantiholomorphic copy.  The config flag changes both the builder namespace and\\nthe emitted rule slice at compile time.\\nconst BcSphereConfig = struct {\\n    include_antiholomorphic_copy: bool = true,\\n};\\n\\n/// bcSphere builds the generated preset type for the sphere bc ghost CFT.\\npub fn bcSphere(comptime cfg: BcSphereConfig) type {\\n    return struct {\\n        /// op exposes bc ghost local operator builders.\\n        pub const op = BcSphereOp(cfg.include_antiholomorphic_copy);\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = BcSphereCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for bc insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nDerivative labels mean \\\\(\\\\partial^n b\\\\), \\\\(\\\\partial^n c\\\\), and their\\nantiholomorphic copies.  No ghost-number bookkeeping is performed here; that is\\npart of theory data and later basis or correlator evaluation.\\nfn BcSphereOp(comptime include_antiholomorphic_copy: bool) type {\\n    const Holomorphic = struct {\\n        /// b builds a holomorphic b-ghost insertion.\\n        pub fn b(local: anytype, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.b)).single(local, z, n, .{});\\n        }\\n\\n        /// c builds a holomorphic c-ghost insertion.\\n        pub fn c(local: anytype, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.c)).single(local, z, n, .{});\\n        }\\n    };\\n\\n    if (!include_antiholomorphic_copy) return Holomorphic;\\n\\n    return struct {\\n        /// b builds a holomorphic b-ghost insertion.\\n        pub const b = Holomorphic.b;\\n        /// c builds a holomorphic c-ghost insertion.\\n        pub const c = Holomorphic.c;\\n\\n        /// bt builds an antiholomorphic b-ghost insertion.\\n        pub fn bt(local: anytype, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.bt)).single(local, zbar, n, .{});\\n        }\\n\\n        /// ct builds an antiholomorphic c-ghost insertion.\\n        pub fn ct(local: anytype, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(bcOperator(.ct)).single(local, zbar, n, .{});\\n        }\\n    };\\n}\\nRulesThe \\\\(bc\\\\) Wick rules have no tensor numerator.  Their zero-mode rule records\\nthe top-form saturation separately from pairwise contractions.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn bcBC() wick.Expr {\\n    return wick.differentiatedPole(.one, .none, holomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn bcBtCt() wick.Expr {\\n    return wick.differentiatedPole(.one, .none, antiholomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn sphereZeroSpec(comptime cfg: BcSphereConfig) if (cfg.include_antiholomorphic_copy) [2]Spec.ZeroModeRule else [1]Spec.ZeroModeRule {\\n    const holomorphic = Spec.ZeroModeRule{ .sector = zeroSector(.sphere), .expr = .{ .bc_top_form = .{\\n        .support = .sphere_holomorphic,\\n        .c_kind_ids = &.{kind(.c)},\\n        .normalization = .one,\\n    } } };\\n    if (!cfg.include_antiholomorphic_copy) return [_]Spec.ZeroModeRule{holomorphic};\\n    return [_]Spec.ZeroModeRule{\\n        holomorphic,\\n        .{ .sector = zeroSector(.sphere), .expr = .{ .bc_top_form = .{\\n            .support = .sphere_antiholomorphic,\\n            .c_kind_ids = &.{kind(.ct)},\\n            .normalization = .one,\\n        } } },\\n    };\\n}\\n\\nfn sphereZeroStorage(comptime cfg: BcSphereConfig) if (cfg.include_antiholomorphic_copy) [2]zero_mode.Rule else [1]zero_mode.Rule {\\n    const spec = sphereZeroSpec(cfg);\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nconst sphere_holomorphic_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"b\\", .kind = kind(.b), .support = .holomorphic, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"c\\", .kind = kind(.c), .support = .holomorphic, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst sphere_full_operator_spec = sphere_holomorphic_operator_spec ++ [_]Spec.Operator{\\n    .{ .name = \\"bt\\", .kind = kind(.bt), .support = .antiholomorphic, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"ct\\", .kind = kind(.ct), .support = .antiholomorphic, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst sphere_holomorphic_fermion_storage = Spec.fermionKinds(&sphere_holomorphic_operator_spec);\\n\\nconst sphere_full_fermion_storage = Spec.fermionKinds(&sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c), .expr = bcBC() },\\n};\\n\\nconst sphere_full_wick_spec = sphere_holomorphic_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.bt), .right = kind(.ct), .expr = bcBtCt() },\\n};\\n\\nconst sphere_holomorphic_wick_storage = Spec.wickRules(&sphere_holomorphic_wick_spec, &sphere_holomorphic_operator_spec);\\n\\nconst sphere_full_wick_storage = Spec.wickRules(&sphere_full_wick_spec, &sphere_full_operator_spec);\\n\\nconst torus_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c), .coordinate_kernels = &.{.elliptic_prime_form} },\\n    .{ .left = kind(.bt), .right = kind(.ct), .coordinate_kernels = &.{.elliptic_prime_form} },\\n};\\n\\nconst sphere_holomorphic_zero_spec = sphereZeroSpec(.{ .include_antiholomorphic_copy = false });\\n\\nconst sphere_full_zero_spec = sphereZeroSpec(.{});\\nFor the genus-one amplitude the stringbook fixes one integrated \\\\(b\\\\) insertion\\nfor the torus modulus and one residual \\\\(c\\\\) zero mode in each chiral copy,\\nafter one puncture is fixed by torus translations .  The\\ndraft metadata records those holomorphic and antiholomorphic \\\\(b,c\\\\) families\\nwithout lowering them into the current sphere/disk top-form runtime evaluator.\\nconst torus_zero_spec = [_]Spec.ZeroModeRule{\\n    .{\\n        .sector = zeroSector(.torus),\\n        .kind = .torus_bc_moduli,\\n        .consumes = &.{ kind(.b), kind(.c), kind(.bt), kind(.ct) },\\n    },\\n};\\n\\nfn bcSphereSpec(comptime include_antiholomorphic_copy: bool) Spec.Theory {\\n    return .{\\n        .operators = if (include_antiholomorphic_copy) &sphere_full_operator_spec else &sphere_holomorphic_operator_spec,\\n        .wick_rules = if (include_antiholomorphic_copy) &sphere_full_wick_spec else &sphere_holomorphic_wick_spec,\\n        .zero_modes = if (include_antiholomorphic_copy) &sphere_full_zero_spec else &sphere_holomorphic_zero_spec,\\n    };\\n}\\n\\nconst torus_draft_spec = Spec.Theory{\\n    .surface = .{\\n        .kind = .torus,\\n        .coordinate_model = .elliptic,\\n        .modular_parameters = &.{\\"tau\\"},\\n        .source = .stringbook,\\n    },\\n    .operators = &sphere_full_operator_spec,\\n    .wick_rules = &torus_wick_spec,\\n    .zero_modes = &torus_zero_spec,\\n};\\n\\nconst disk_boundary_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"bBoundary\\", .kind = kind(.b_boundary), .support = .boundary, .insertion = .single, .statistics = .fermionic },\\n    .{ .name = \\"cBoundary\\", .kind = kind(.c_boundary), .support = .boundary, .insertion = .single, .statistics = .fermionic, .zero_mode_consumable = true },\\n};\\n\\nconst disk_full_operator_spec = sphere_full_operator_spec ++ disk_boundary_operator_spec;\\n\\nfn bcOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_full_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing bc operator spec\\");\\n}\\n\\nfn bcBoundaryOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (disk_boundary_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing bc boundary operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime include_antiholomorphic_copy: bool, comptime wick_count: usize, comptime zero_count: usize, comptime fermion_count: usize) void {\\n    const spec = bcSphereSpec(include_antiholomorphic_copy);\\n    if (spec.wick_rules.len != wick_count) @compileError(\\"bc sphere spec Wick count does not match lowered rules\\");\\n    if (spec.zero_modes.len != zero_count) @compileError(\\"bc sphere spec zero-mode count does not match lowered rules\\");\\n    if (spec.operators.len != fermion_count) @compileError(\\"bc sphere spec fermion coverage does not match lowered rules\\");\\n}\\n\\nfn assertTorusDraftSpec() void {\\n    if (torus_draft_spec.surface.kind != .torus) @compileError(\\"bc torus draft spec surface kind is incomplete\\");\\n    if (torus_draft_spec.surface.coordinate_model != .elliptic) @compileError(\\"bc torus draft spec coordinate model is incomplete\\");\\n    if (torus_draft_spec.surface.modular_parameters.len != 1) @compileError(\\"bc torus draft spec modular metadata is incomplete\\");\\n    if (torus_draft_spec.surface.source != .stringbook) @compileError(\\"bc torus draft spec source convention is incomplete\\");\\n    if (torus_draft_spec.operators.len != sphere_full_operator_spec.len) @compileError(\\"bc torus draft spec operator metadata is incomplete\\");\\n    if (torus_draft_spec.wick_rules.len != sphere_full_wick_spec.len) @compileError(\\"bc torus draft spec Wick metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .elliptic_prime_form) != 2) @compileError(\\"bc torus draft spec prime-form metadata is incomplete\\");\\n    if (Spec.wickCoordinateKernelCount(torus_draft_spec.wick_rules, .rational_pole) != 0) @compileError(\\"bc torus draft spec still uses rational pole metadata\\");\\n    if (torus_draft_spec.zero_modes.len != 1) @compileError(\\"bc torus draft spec zero-mode metadata is incomplete\\");\\n    if (Spec.zeroModeConsumeKindCount(torus_draft_spec.zero_modes[0]) != 4) @compileError(\\"bc torus draft spec zero-mode coverage is incomplete\\");\\n}\\n\\nfn BcSphereRules(comptime include_antiholomorphic_copy: bool) type {\\n    const selected_wick = if (include_antiholomorphic_copy) sphere_full_wick_storage else sphere_holomorphic_wick_storage;\\n    const selected_fermions = if (include_antiholomorphic_copy) sphere_full_fermion_storage else sphere_holomorphic_fermion_storage;\\n    return struct {\\n        const sphere_wick: []const wick.Rule = &selected_wick;\\n        const sphere_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\nfn BcSphereCorrelatorConfig(comptime cfg: BcSphereConfig) type {\\n    const rules = BcSphereRules(cfg.include_antiholomorphic_copy);\\n    return struct {\\n        const sphere_zero_storage = sphereZeroStorage(cfg);\\n        comptime {\\n            assertSphereSpec(cfg.include_antiholomorphic_copy, rules.sphere_wick.len, sphere_zero_storage.len, rules.sphere_fermion_kinds.len);\\n            assertTorusDraftSpec();\\n        }\\n\\n        /// sphere selects bc sphere Wick and zero-mode rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &sphere_zero_storage,\\n            .fermion_kinds = rules.sphere_fermion_kinds,\\n        }){};\\n    };\\n}\\nDisk BoundaryThe disk extension adds boundary-local \\\\(b,c\\\\) operators and mixed\\nbulk-boundary contractions.  It is an extension chosen by a BCFT composition,\\nnot a modification of the bulk \\\\(bc\\\\) preset.\\nconst BcDiskConfig = struct {\\n    include_mixed_bulk_boundary: bool = true,\\n};\\n\\nconst BcDiskBoundaryOp = struct {\\n    /// bBoundary builds a boundary b-ghost insertion.\\n    pub fn bBoundary(local: anytype, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(bcBoundaryOperator(.b_boundary)).boundarySingle(local, y, n, .{});\\n    }\\n\\n    /// cBoundary builds a boundary c-ghost insertion.\\n    pub fn cBoundary(local: anytype, n: u8, y: Handle.BoundaryCoord) !Operator {\\n        return declare.operatorBuilder(bcBoundaryOperator(.c_boundary)).boundarySingle(local, y, n, .{});\\n    }\\n};\\n\\nfn BcBoundaryExtension(comptime cfg: BcDiskConfig) type {\\n    const data = BcDiskData(cfg);\\n    return declare.BoundaryExtension(.{\\n        .op = BcDiskBoundaryOp,\\n        .wick_rules = if (cfg.include_mixed_bulk_boundary) &disk_full_wick_storage else &disk_boundary_wick_storage,\\n        .zero_modes = data.disk_zero_modes,\\n        .fermion_kinds = data.disk_fermion_kinds,\\n    });\\n}\\n\\n/// boundaryExtension declares boundary and mixed bulk-boundary bc rules on the disk.\\npub fn boundaryExtension(comptime cfg: BcDiskConfig) BcBoundaryExtension(cfg) {\\n    return .{};\\n}\\n\\nconst disk_boundary_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.b_boundary), .right = kind(.c_boundary), .expr = bcBC() },\\n};\\n\\nconst disk_full_wick_spec = disk_boundary_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.b), .right = kind(.c_boundary), .expr = bcBC() },\\n    .{ .left = kind(.c), .right = kind(.b_boundary), .expr = bcBC() },\\n    .{ .left = kind(.bt), .right = kind(.c_boundary), .expr = bcBtCt() },\\n    .{ .left = kind(.ct), .right = kind(.b_boundary), .expr = bcBtCt() },\\n};\\n\\nconst disk_boundary_wick_storage = Spec.wickRules(&disk_boundary_wick_spec, &disk_boundary_operator_spec);\\n\\nconst disk_full_wick_storage = Spec.wickRules(&disk_full_wick_spec, &disk_full_operator_spec);\\n\\nconst disk_boundary_fermion_storage = Spec.fermionKinds(&disk_boundary_operator_spec);\\n\\nconst disk_full_fermion_storage = Spec.fermionKinds(&disk_full_operator_spec);\\n\\nfn diskZeroSpec() [1]Spec.ZeroModeRule {\\n    return [_]Spec.ZeroModeRule{\\n        .{ .sector = zeroSector(.disk), .expr = .{ .bc_top_form = .{\\n            .support = .disk_doubled,\\n            .c_kind_ids = &.{ kind(.c), kind(.ct), kind(.c_boundary) },\\n            .normalization = .one,\\n        } } },\\n    };\\n}\\n\\nfn diskZeroStorage(comptime cfg: BcDiskConfig) [1]zero_mode.Rule {\\n    _ = cfg;\\n    const spec = diskZeroSpec();\\n    return Spec.zeroModeRules(&spec);\\n}\\n\\nfn BcDiskData(comptime cfg: BcDiskConfig) type {\\n    const selected_fermions = if (cfg.include_mixed_bulk_boundary) disk_full_fermion_storage else disk_boundary_fermion_storage;\\n    return struct {\\n        const disk_zero_storage = diskZeroStorage(cfg);\\n\\n        const disk_zero_modes: []const zero_mode.Rule = &disk_zero_storage;\\n        const disk_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\ntest \\"bc schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n    const Ghost = bcSphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var local = try Ghost.local(testing.allocator);\\n    defer local.deinit();\\n\\n    const z1 = try local.coord(\\"z1\\");\\n    const z2 = try local.coord(\\"z2\\");\\n    const b = try Ghost.op.b(&local, 1, z1);\\n    const c = try Ghost.op.c(&local, 2, z2);\\n    const ops = try local.ops(.{ b, c });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(b)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(c)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\ntest \\"bc boundary schema builders return opaque local tokens\\" {\\n    const testing = std.testing;\\n\\n    var local = try Local.init(testing.allocator);\\n    defer local.deinit();\\n\\n    const y = try local.boundaryCoord(\\"y\\");\\n    const b = try BcDiskBoundaryOp.bBoundary(&local, 1, y);\\n    const c = try BcDiskBoundaryOp.cBoundary(&local, 2, y);\\n    const ops = try local.ops(.{ b, c });\\n\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(b)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(c)).pointer.child) == .@\\"opaque\\");\\n    try testing.expect(@typeInfo(@typeInfo(@TypeOf(ops)).pointer.child) == .@\\"opaque\\");\\n}\\n\\n","n":0.024}}},{"i":27,"$":{"1":{"v":"Preset Composition","n":0.707},"2":{"v":"Composition turns bulk presets such as Free Boson Preset and\\nbc Preset into products and BCFTs.  It uses the shared\\nruntime vocabulary from Preset Shared Runtime.\\nThe important invariant is that tensor products do not allocate a new runtime\\noperator representation.  Product quantum numbers remain lists at theory level;\\nruntime local operators are still just insertion data, a body kind, and a label\\nspan.  Composition asks the shared lowering path to merge opaque configs, so it\\ndoes not name config-entry records or slice append helpers.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst Builder = *shared.Local;\\nProduct CFTsA product preset forms its sphere config from the generated sphere config of\\neach factor.  It deliberately does not synthesize preset-named operator\\nnamespaces.  Future friendly aliases belong in the user DSL layer, not in the\\nlowering path.\\nfn ProductCorrelatorConfig(comptime factors: anytype) type {\\n    const merged = declare.mergeSphereConfigs(factors);\\n    return struct {\\n        /// sphere selects composed product Wick and zero-mode rules.\\n        pub const sphere = merged.config;\\n    };\\n}\\n\\n/// product builds the generated preset type for a product of independent bulk presets.\\npub fn product(comptime factors: anytype) type {\\n    return struct {\\n        /// op is intentionally empty; use the factor builders passed to product.\\n        pub const op = struct {};\\n        /// config exposes named correlator configs for this product preset.\\n        pub const config = ProductCorrelatorConfig(factors);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for product-theory insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nBoundary ExtensionsA BCFT is built from a bulk preset plus explicit boundary extensions.  The bulk\\npreset remains boundary-free; boundary data contributes new operator families,\\nmixed Wick rules, zero-mode caveats, and optional Chan-Paton stack data.\\nBoundary-local builders remain on the extension handles passed to boundary,\\nso composition does not inspect preset names to create aliases.\\nThe disk config is formed by the shared merge helper.  Composition keeps only\\nthe physics-facing bulk/extension relationship and does not see packed config\\nentries, slice selectors, or append procedures.\\nfn BoundaryCorrelatorConfig(comptime Bulk: type, comptime extensions: anytype) type {\\n    const merged = declare.mergeBoundaryConfig(Bulk, extensions);\\n    return struct {\\n        /// disk selects composed boundary Wick and zero-mode rules.\\n        pub const disk = merged.config;\\n    };\\n}\\n\\nfn BoundaryOp(comptime Bulk: type, comptime extensions: anytype) type {\\n    _ = extensions;\\n    return struct {\\n        /// bulk exposes the bulk operator namespaces.\\n        pub const bulk = Bulk.op;\\n    };\\n}\\n\\n/// boundary builds the generated preset type for a BCFT from a bulk preset and boundary extensions.\\npub fn boundary(comptime Bulk: type, comptime extensions: anytype) type {\\n    return struct {\\n        /// op exposes bulk and boundary operator builders.\\n        pub const op = BoundaryOp(Bulk, extensions);\\n        /// config exposes named correlator configs for this BCFT.\\n        pub const config = BoundaryCorrelatorConfig(Bulk, extensions);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for BCFT insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\n","n":0.045}}},{"i":28,"$":{"1":{"v":"Tensor Representation Store","n":0.577},"2":{"v":"This module owns the abstract representation data.  A Lie algebra and an\\nirrep are interned once, then every downstream system carries small typed\\nhandles.  That keeps tensor generation independent of how a representation is\\neventually rendered as indices, matrices, or symbolic projectors.\\nconst std = @import(\\"std\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\nHandles are public because other modules need stable references.  The records\\nbehind them stay private.\\n/// AlgebraHandle names a registered Lie algebra in a tensor context.\\npub const AlgebraHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an algebra handle from a store index.\\n    pub fn init(value: u32) AlgebraHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// IrrepHandle names an abstract irreducible representation.\\npub const IrrepHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an irrep handle from a store index.\\n    pub fn init(value: u32) IrrepHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ChannelHandle names one irrep copy inside a local product.\\npub const ChannelHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a channel handle from a store index.\\n    pub fn init(value: u32) ChannelHandle {\\n        return .{ .value = value };\\n    }\\n};\\nSpecs are caller-owned construction data.  Interning clones only the slices we\\nmust retain.\\n/// AlgebraSpec describes a requested algebra before it is interned.\\npub const AlgebraSpec = union(enum) {\\n    u1,\\n    simple: symmetry.SimpleLieAlgebra,\\n};\\n\\n/// IrrepSpec describes a requested representation before it is interned.\\npub const IrrepSpec = union(enum) {\\n    u1_charge: symmetry.U1ChargeRational,\\n    dynkin: []const i16,\\n};\\n\\n/// IrrepMetadata stores derived representation data.\\npub const IrrepMetadata = struct {\\n    dimension: ?u128 = null,\\n    dual: ?IrrepHandle = null,\\n    quadratic_casimir: ?root_data.Rational = null,\\n};\\nThe store is intentionally simple: linear lookup first, then append.  The first\\nimplementation has few registered algebras and irreps compared with the later\\ncoupling search, so this avoids prematurely adding hash table machinery.  If\\nprofiles show this store in the hot path, the record layout can stay fixed\\nwhile we add an index.\\n/// Store owns interned algebra and irrep records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    conventions: root_data.AlgebraConventions,\\n    algebras: std.ArrayList(AlgebraRecord) = .empty,\\n    irreps: std.ArrayList(IrrepRecord) = .empty,\\n\\n    /// init constructs an empty representation store.\\n    pub fn init(allocator: std.mem.Allocator, conventions: root_data.AlgebraConventions) Store {\\n        return .{ .allocator = allocator, .conventions = conventions };\\n    }\\n\\n    /// deinit releases all representation-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.irreps.items) |record| {\\n            switch (record.label) {\\n                .dynkin => |label| self.allocator.free(label),\\n                .u1_charge => {},\\n            }\\n        }\\n        self.irreps.deinit(self.allocator);\\n        self.algebras.deinit(self.allocator);\\n        self.* = Store.init(self.allocator, self.conventions);\\n    }\\n\\n    /// internAlgebra returns the stable handle for an algebra spec.\\n    pub fn internAlgebra(self: *Store, spec: AlgebraSpec) !AlgebraHandle {\\n        try validateAlgebraSpec(spec);\\n        for (self.algebras.items, 0..) |record, index| {\\n            if (algebraSpecEql(record.spec, spec)) {\\n                return AlgebraHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        try self.algebras.append(self.allocator, .{\\n            .spec = spec,\\n            .root_data = try makeRootDatum(spec, self.conventions),\\n        });\\n        return AlgebraHandle.init(@intCast(self.algebras.items.len - 1));\\n    }\\n\\n    /// containsAlgebra returns whether an algebra handle belongs to this store.\\n    pub fn containsAlgebra(self: Store, handle: AlgebraHandle) bool {\\n        return self.algebraRecord(handle) != null;\\n    }\\n\\n    /// internIrrep returns the stable handle for an irrep spec in an algebra.\\n    pub fn internIrrep(self: *Store, algebra: AlgebraHandle, spec: IrrepSpec) anyerror!IrrepHandle {\\n        const algebra_record = self.algebraRecord(algebra) orelse return error.UnknownAlgebra;\\n        try validateIrrepSpec(algebra_record.spec, spec);\\n\\n        for (self.irreps.items, 0..) |record, index| {\\n            if (handleEql(record.algebra, algebra) and irrepLabelSpecEql(record.label, spec)) {\\n                return IrrepHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const label = try cloneIrrepSpec(self.allocator, spec);\\n        errdefer freeIrrepLabel(self.allocator, label);\\n\\n        try self.irreps.append(self.allocator, .{\\n            .algebra = algebra,\\n            .label = label,\\n            .metadata = .{},\\n        });\\n        errdefer _ = self.irreps.pop();\\n        const handle = IrrepHandle.init(@intCast(self.irreps.items.len - 1));\\n        try self.fillIrrepMetadata(handle);\\n        return handle;\\n    }\\n\\n    /// dualIrrep returns the handle for the dual irrep, interning it if requested.\\n    pub fn dualIrrep(self: *Store, handle: IrrepHandle) anyerror!IrrepHandle {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.irreps.items.len) return error.UnknownIrrep;\\n        if (self.irreps.items[index].metadata.dual) |dual| return dual;\\n\\n        const record = self.irreps.items[index];\\n        const dual = switch (record.label) {\\n            .u1_charge => |charge| try self.internIrrep(record.algebra, .{ .u1_charge = .{\\n                .numerator = -charge.numerator,\\n                .denominator = charge.denominator,\\n            } }),\\n            .dynkin => |label| dual: {\\n                const algebra = self.algebraRecord(record.algebra) orelse return error.UnknownAlgebra;\\n                const dual_label = try root_data.dualDynkin(self.allocator, algebra.spec.simple, label);\\n                defer self.allocator.free(dual_label);\\n                break :dual try self.internIrrep(record.algebra, .{ .dynkin = dual_label });\\n            },\\n        };\\n\\n        self.irreps.items[index].metadata.dual = dual;\\n        const dual_index: usize = @intCast(dual.value);\\n        if (dual_index < self.irreps.items.len) self.irreps.items[dual_index].metadata.dual = handle;\\n        return dual;\\n    }\\n\\n    /// containsIrrep returns whether an irrep handle belongs to this store.\\n    pub fn containsIrrep(self: Store, handle: IrrepHandle) bool {\\n        return self.irrepRecord(handle) != null;\\n    }\\n\\n    /// irrepMetadata returns derived metadata for an irrep handle.\\n    pub fn irrepMetadata(self: Store, handle: IrrepHandle) ?IrrepMetadata {\\n        const record = self.irrepRecord(handle) orelse return null;\\n        return record.metadata;\\n    }\\n\\n    /// irrepAlgebra returns the algebra that owns an irrep handle.\\n    pub fn irrepAlgebra(self: Store, handle: IrrepHandle) ?AlgebraHandle {\\n        const record = self.irrepRecord(handle) orelse return null;\\n        return record.algebra;\\n    }\\n\\n    fn algebraRecord(self: Store, handle: AlgebraHandle) ?AlgebraRecord {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.algebras.items.len) return null;\\n        return self.algebras.items[index];\\n    }\\n\\n    fn irrepRecord(self: Store, handle: IrrepHandle) ?IrrepRecord {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.irreps.items.len) return null;\\n        return self.irreps.items[index];\\n    }\\n\\n    fn fillIrrepMetadata(self: *Store, handle: IrrepHandle) anyerror!void {\\n        const index: usize = @intCast(handle.value);\\n        const record = self.irreps.items[index];\\n        const algebra = self.algebraRecord(record.algebra) orelse return error.UnknownAlgebra;\\n\\n        switch (record.label) {\\n            .u1_charge => |charge| {\\n                self.irreps.items[index].metadata.dimension = 1;\\n                self.irreps.items[index].metadata.quadratic_casimir = try u1QuadraticCasimir(charge);\\n                if (charge.numerator == 0) self.irreps.items[index].metadata.dual = handle;\\n            },\\n            .dynkin => |label| {\\n                const simple = algebra.spec.simple;\\n                self.irreps.items[index].metadata.dimension = try root_data.dimension(self.allocator, simple, label);\\n                self.irreps.items[index].metadata.quadratic_casimir = try root_data.quadraticCasimir(self.allocator, simple, label, self.conventions);\\n                const dual_label = try root_data.dualDynkin(self.allocator, simple, label);\\n                defer self.allocator.free(dual_label);\\n                if (std.mem.eql(i16, label, dual_label)) {\\n                    self.irreps.items[index].metadata.dual = handle;\\n                } else if (self.findIrrep(record.algebra, .{ .dynkin = dual_label })) |dual| {\\n                    self.irreps.items[index].metadata.dual = dual;\\n                }\\n            },\\n        }\\n    }\\n\\n    fn findIrrep(self: Store, algebra: AlgebraHandle, spec: IrrepSpec) ?IrrepHandle {\\n        for (self.irreps.items, 0..) |record, index| {\\n            if (handleEql(record.algebra, algebra) and irrepLabelSpecEql(record.label, spec)) {\\n                return IrrepHandle.init(@intCast(index));\\n            }\\n        }\\n        return null;\\n    }\\n};\\nPrivate records carry derived metadata, root data, and future product-channel\\noccurrences.  They are not exported because callers should not depend on the\\nstorage layout.\\nconst AlgebraRecord = struct {\\n    spec: AlgebraSpec,\\n    root_data: ?root_data.RootDatum,\\n};\\n\\nconst IrrepRecord = struct {\\n    algebra: AlgebraHandle,\\n    label: IrrepLabel,\\n    metadata: IrrepMetadata,\\n};\\n\\nconst IrrepLabel = union(enum) {\\n    u1_charge: symmetry.U1ChargeRational,\\n    dynkin: []const i16,\\n};\\n\\nconst ChannelRecord = struct {\\n    irrep: IrrepHandle,\\n    multiplicity_copy: u16,\\n};\\n\\nconst OccurrenceKind = enum {\\n    external_leg,\\n    product_node,\\n    realization,\\n};\\n\\nconst OccurrenceRecord = struct {\\n    channel: ChannelHandle,\\n    kind: OccurrenceKind,\\n    source_id: u32,\\n    realization: ?u32 = null,\\n};\\nInterning equality is structural and allocation-free.  Validation happens before\\ninterning: a simple-algebra irrep must have one non-negative Dynkin label per\\nrank, and a U(1) irrep must be a rational charge.  Unknown dimension and dual\\ndata is represented by null, never by a fake handle.\\nfn handleEql(left: AlgebraHandle, right: AlgebraHandle) bool {\\n    return left.value == right.value;\\n}\\n\\nfn validateAlgebraSpec(spec: AlgebraSpec) !void {\\n    switch (spec) {\\n        .u1 => {},\\n        .simple => |simple| try root_data.validateSimple(simple),\\n    }\\n}\\n\\nfn makeRootDatum(spec: AlgebraSpec, conventions: root_data.AlgebraConventions) !?root_data.RootDatum {\\n    return switch (spec) {\\n        .u1 => null,\\n        .simple => |simple| try root_data.RootDatum.init(simple, conventions),\\n    };\\n}\\n\\nfn algebraSpecEql(left: AlgebraSpec, right: AlgebraSpec) bool {\\n    return switch (left) {\\n        .u1 => switch (right) {\\n            .u1 => true,\\n            .simple => false,\\n        },\\n        .simple => |left_simple| switch (right) {\\n            .u1 => false,\\n            .simple => |right_simple| left_simple.family == right_simple.family and left_simple.rank == right_simple.rank,\\n        },\\n    };\\n}\\n\\nfn validateIrrepSpec(algebra: AlgebraSpec, spec: IrrepSpec) !void {\\n    switch (algebra) {\\n        .u1 => switch (spec) {\\n            .u1_charge => |charge| {\\n                if (charge.denominator == 0) return error.InvalidU1Charge;\\n            },\\n            .dynkin => return error.InvalidIrrepForAlgebra,\\n        },\\n        .simple => |simple| switch (spec) {\\n            .u1_charge => return error.InvalidIrrepForAlgebra,\\n            .dynkin => |label| {\\n                if (label.len != simple.rank) return error.InvalidDynkinRank;\\n                for (label) |entry| {\\n                    if (entry < 0) return error.InvalidDynkinLabel;\\n                }\\n            },\\n        },\\n    }\\n}\\n\\nfn u1QuadraticCasimir(charge: symmetry.U1ChargeRational) !root_data.Rational {\\n    const numerator = @as(i128, charge.numerator) * @as(i128, charge.numerator);\\n    const denominator = @as(i128, charge.denominator) * @as(i128, charge.denominator);\\n    return root_data.Rational.init(numerator, denominator);\\n}\\n\\nfn irrepLabelSpecEql(left: IrrepLabel, right: IrrepSpec) bool {\\n    return switch (left) {\\n        .u1_charge => |left_charge| switch (right) {\\n            .u1_charge => |right_charge| left_charge.numerator == right_charge.numerator and left_charge.denominator == right_charge.denominator,\\n            .dynkin => false,\\n        },\\n        .dynkin => |left_dynkin| switch (right) {\\n            .u1_charge => false,\\n            .dynkin => |right_dynkin| std.mem.eql(i16, left_dynkin, right_dynkin),\\n        },\\n    };\\n}\\n\\nfn cloneIrrepSpec(allocator: std.mem.Allocator, spec: IrrepSpec) !IrrepLabel {\\n    return switch (spec) {\\n        .u1_charge => |charge| .{ .u1_charge = charge },\\n        .dynkin => |label| .{ .dynkin = try allocator.dupe(i16, label) },\\n    };\\n}\\n\\nfn freeIrrepLabel(allocator: std.mem.Allocator, label: IrrepLabel) void {\\n    switch (label) {\\n        .dynkin => |dynkin| allocator.free(dynkin),\\n        .u1_charge => {},\\n    }\\n}\\n","n":0.028}}},{"i":29,"$":{"1":{"v":"Tensor Realization","n":0.707},"2":{"v":"Realizations map abstract irreps to concrete index layouts.  This is where a\\nfield acquires caller-visible slots such as vector, spinor, fundamental, or\\nadjoint indices.  The coupling search still reasons about the target irrep, so\\nprojected realizations like an irrep inside \\\\(V \\\\otimes S\\\\) do not force every\\nlater step to carry the full ambient product.\\nconst std = @import(\\"std\\");\\nconst store = @import(\\"representation-store.zig\\");\\nPublic handles identify registered concrete realizations and external legs.\\n/// RealizationHandle names a concrete index realization of an irrep.\\npub const RealizationHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a realization handle from a store index.\\n    pub fn init(value: u32) RealizationHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ExternalLegHandle names one external leg in an invariant request.\\npub const ExternalLegHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an external-leg handle from a store index.\\n    pub fn init(value: u32) ExternalLegHandle {\\n        return .{ .value = value };\\n    }\\n};\\nConstruction records are public because CFT code must be able to describe its\\nfree indices.  The arrays are caller-owned until they are registered in the\\ncontext.\\n/// RealizationRef identifies a primitive or registered external tensor shape.\\npub const RealizationRef = union(enum) {\\n    primitive_irrep: store.IrrepHandle,\\n    handle: RealizationHandle,\\n    registered_name: []const u8,\\n};\\n\\n/// IndexKind classifies the visible slot type of an external index.\\npub const IndexKind = enum {\\n    vector,\\n    spinor,\\n    conjugate_spinor,\\n    fundamental,\\n    anti_fundamental,\\n    adjoint,\\n    form,\\n    custom,\\n};\\n\\n/// NamedIndex records one caller-supplied free index name.\\npub const NamedIndex = struct {\\n    kind: IndexKind,\\n    name: []const u8,\\n\\n    /// init constructs one caller-named index slot.\\n    pub fn init(kind: IndexKind, name: []const u8) NamedIndex {\\n        return .{ .kind = kind, .name = name };\\n    }\\n};\\n\\n/// RealizationChannel selects one irrep copy inside a realization product.\\npub const RealizationChannel = struct {\\n    irrep: store.IrrepHandle,\\n    copy: u16 = 0,\\n\\n    /// init constructs a selected irrep copy.\\n    pub fn init(irrep: store.IrrepHandle, copy: u16) RealizationChannel {\\n        return .{ .irrep = irrep, .copy = copy };\\n    }\\n\\n    /// first selects the first copy of an irrep.\\n    pub fn first(irrep: store.IrrepHandle) RealizationChannel {\\n        return .{ .irrep = irrep };\\n    }\\n};\\n\\n/// ExternalLeg describes a public invariant leg with named free indices.\\npub const ExternalLeg = struct {\\n    realization: RealizationRef,\\n    indices: []const NamedIndex,\\n    label: ?[]const u8 = null,\\n\\n    /// primitive constructs a leg whose realization is the irrep itself.\\n    pub fn primitive(irrep: store.IrrepHandle, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .primitive_irrep = irrep },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// realized constructs a leg from a registered realization handle.\\n    pub fn realized(realization: RealizationHandle, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .handle = realization },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// registered constructs a leg from a preset realization name.\\n    pub fn registered(name: []const u8, indices: []const NamedIndex) ExternalLeg {\\n        return .{\\n            .realization = .{ .registered_name = name },\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// named returns the leg with a caller-facing label attached.\\n    pub fn named(self: ExternalLeg, label: []const u8) ExternalLeg {\\n        var out = self;\\n        out.label = label;\\n        return out;\\n    }\\n};\\n\\n/// RealizationSpec describes a primitive or projected external index model.\\npub const RealizationSpec = struct {\\n    channel: RealizationChannel,\\n    ambient: []const store.IrrepHandle = &.{},\\n    indices: []const NamedIndex,\\n    name: ?[]const u8 = null,\\n\\n    /// primitive constructs a direct index realization of an irrep.\\n    pub fn primitive(target: store.IrrepHandle, indices: []const NamedIndex) RealizationSpec {\\n        return .{\\n            .channel = RealizationChannel.first(target),\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// projected constructs an irrep realization inside an ambient product.\\n    pub fn projected(target: store.IrrepHandle, ambient: []const store.IrrepHandle, indices: []const NamedIndex, copy: u16) RealizationSpec {\\n        return RealizationSpec.projectedChannel(RealizationChannel.init(target, copy), ambient, indices);\\n    }\\n\\n    /// projectedChannel constructs a realization from an explicit channel.\\n    pub fn projectedChannel(channel: RealizationChannel, ambient: []const store.IrrepHandle, indices: []const NamedIndex) RealizationSpec {\\n        return .{\\n            .channel = channel,\\n            .ambient = ambient,\\n            .indices = indices,\\n        };\\n    }\\n\\n    /// named returns the realization spec with a preset-facing name attached.\\n    pub fn named(self: RealizationSpec, name: []const u8) RealizationSpec {\\n        var out = self;\\n        out.name = name;\\n        return out;\\n    }\\n\\n    /// targetIrrep returns the abstract irrep realized by this index model.\\n    pub fn targetIrrep(self: RealizationSpec) store.IrrepHandle {\\n        return self.channel.irrep;\\n    }\\n};\\nThe store owns cloned realization specs and interns repeated requests.\\n/// Store owns interned concrete realization records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    specs: std.ArrayList(RealizationSpec) = .empty,\\n\\n    /// init constructs an empty realization store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases all realization-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.specs.items) |spec| {\\n            freeRealizationSpec(self.allocator, spec);\\n        }\\n        self.specs.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internRealization returns the stable handle for a realization spec.\\n    pub fn internRealization(self: *Store, spec: RealizationSpec) !RealizationHandle {\\n        for (self.specs.items, 0..) |stored, index| {\\n            if (realizationSpecEql(stored, spec)) {\\n                return RealizationHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const owned = try cloneRealizationSpec(self.allocator, spec);\\n        errdefer freeRealizationSpec(self.allocator, owned);\\n        try self.specs.append(self.allocator, owned);\\n        return RealizationHandle.init(@intCast(self.specs.items.len - 1));\\n    }\\n\\n    /// containsRealization returns whether a realization handle belongs to this store.\\n    pub fn containsRealization(self: Store, handle: RealizationHandle) bool {\\n        return self.realizationSpec(handle) != null;\\n    }\\n\\n    /// targetIrrep returns the irrep realized by a stored realization handle.\\n    pub fn targetIrrep(self: Store, handle: RealizationHandle) ?store.IrrepHandle {\\n        const spec = self.realizationSpec(handle) orelse return null;\\n        return spec.targetIrrep();\\n    }\\n\\n    fn realizationSpec(self: Store, handle: RealizationHandle) ?RealizationSpec {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.specs.items.len) return null;\\n        return self.specs.items[index];\\n    }\\n};\\nThese private records describe the future projector derivation path for a\\nrealization.  The selected channel is a single value, so future embedding\\nconventions have one natural place to attach.\\n/// IndexSlotSpec declares one slot in a realization layout.\\nconst IndexSlotSpec = struct {\\n    kind: IndexKind,\\n    default_name: ?[]const u8 = null,\\n};\\n\\n/// RealizationRequest declares an irrep as a projected ambient tensor shape.\\nconst RealizationRequest = struct {\\n    channel: RealizationChannel,\\n    ambient: []const store.IrrepHandle,\\n    index_layout: []const IndexSlotSpec,\\n};\\n\\n/// RealizationKind distinguishes primitive from projected realizations.\\nconst RealizationKind = enum {\\n    primitive,\\n    projected_channel,\\n};\\n\\n/// RealizationRecord stores one concrete index layout for an irrep.\\nconst RealizationRecord = struct {\\n    channel: RealizationChannel,\\n    kind: RealizationKind,\\n    ambient_offset: u32,\\n    ambient_len: u16,\\n    index_layout_offset: u32,\\n    index_layout_len: u16,\\n    projector: ?RealizationProjectorId = null,\\n};\\n\\n/// RealizationProjectorId names a derived boundary realization projector.\\nconst RealizationProjectorId = u32;\\n\\n/// AmbientFactor records one irrep in an ambient realization product.\\nconst AmbientFactor = struct {\\n    irrep: store.IrrepHandle,\\n};\\n\\n/// RealizationRule records the construction request for one realization.\\nconst RealizationRule = struct {\\n    request: RealizationRequest,\\n    projector: ?RealizationProjectorId = null,\\n};\\nStructural equality and clone/free helpers are private implementation details.\\nThey keep public construction records lightweight while making registered\\nrealizations stable after caller buffers go out of scope.\\nfn realizationSpecEql(left: RealizationSpec, right: RealizationSpec) bool {\\n    return realizationChannelEql(left.channel, right.channel) and\\n        optionalStringEql(left.name, right.name) and\\n        irrepSliceEql(left.ambient, right.ambient) and\\n        namedIndexSliceEql(left.indices, right.indices);\\n}\\n\\nfn realizationChannelEql(left: RealizationChannel, right: RealizationChannel) bool {\\n    return left.irrep.value == right.irrep.value and left.copy == right.copy;\\n}\\n\\nfn irrepSliceEql(left: []const store.IrrepHandle, right: []const store.IrrepHandle) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_item, right_item| {\\n        if (left_item.value != right_item.value) return false;\\n    }\\n    return true;\\n}\\n\\nfn namedIndexSliceEql(left: []const NamedIndex, right: []const NamedIndex) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_item, right_item| {\\n        if (left_item.kind != right_item.kind) return false;\\n        if (!std.mem.eql(u8, left_item.name, right_item.name)) return false;\\n    }\\n    return true;\\n}\\n\\nfn optionalStringEql(left: ?[]const u8, right: ?[]const u8) bool {\\n    if (left == null or right == null) return left == null and right == null;\\n    return std.mem.eql(u8, left.?, right.?);\\n}\\n\\nfn cloneRealizationSpec(allocator: std.mem.Allocator, spec: RealizationSpec) !RealizationSpec {\\n    const ambient = try allocator.dupe(store.IrrepHandle, spec.ambient);\\n    errdefer allocator.free(ambient);\\n\\n    const indices = try cloneNamedIndices(allocator, spec.indices);\\n    errdefer freeNamedIndices(allocator, indices);\\n\\n    const name = if (spec.name) |value| try allocator.dupe(u8, value) else null;\\n    errdefer if (name) |value| allocator.free(value);\\n\\n    return .{\\n        .channel = spec.channel,\\n        .ambient = ambient,\\n        .indices = indices,\\n        .name = name,\\n    };\\n}\\n\\nfn freeRealizationSpec(allocator: std.mem.Allocator, spec: RealizationSpec) void {\\n    allocator.free(spec.ambient);\\n    freeNamedIndices(allocator, spec.indices);\\n    if (spec.name) |name| allocator.free(name);\\n}\\n\\nfn cloneNamedIndices(allocator: std.mem.Allocator, indices: []const NamedIndex) ![]NamedIndex {\\n    const owned = try allocator.alloc(NamedIndex, indices.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |previous| allocator.free(previous.name);\\n        allocator.free(owned);\\n    }\\n\\n    for (indices, owned[0..]) |source, *target| {\\n        const name = try allocator.dupe(u8, source.name);\\n        target.* = .{ .kind = source.kind, .name = name };\\n        initialized += 1;\\n    }\\n\\n    return owned;\\n}\\n\\nfn freeNamedIndices(allocator: std.mem.Allocator, indices: []const NamedIndex) void {\\n    for (indices) |index| {\\n        allocator.free(index.name);\\n    }\\n    allocator.free(indices);\\n}\\n","n":0.028}}},{"i":30,"$":{"1":{"v":"Tensor Coupling","n":0.707},"2":{"v":"Coupling owns invariant-basis requests and, later, the compact paths that span\\nthose bases.  It does not own dense tensor values.  A basis element should be a\\nsmall recipe: which external legs are coupled, which intermediate channels are\\nused, and which local projector kernels render the final invariant if a caller\\nasks for explicit components.\\nconst std = @import(\\"std\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nBasis and invariant handles are public ids.  The path records behind them stay\\nprivate because we want freedom to change the solver layout.\\n/// BasisHandle names a generated invariant basis.\\npub const BasisHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a basis handle from a store index.\\n    pub fn init(value: u32) BasisHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// InvariantHandle names one invariant inside a basis.\\npub const InvariantHandle = struct {\\n    value: u32,\\n\\n    /// init constructs an invariant handle from a store index.\\n    pub fn init(value: u32) InvariantHandle {\\n        return .{ .value = value };\\n    }\\n};\\nThe public request is deliberately small: algebra, external legs, target, and\\nbasis policy.  The default target is the singlet because that is the CFT\\nworkflow for invariant tensor structures.\\n/// TargetIrrep selects the target representation, usually the singlet.\\npub const TargetIrrep = union(enum) {\\n    singlet,\\n    irrep: store.IrrepHandle,\\n};\\n\\n/// TreePolicy controls the coupling tree used for a basis.\\npub const TreePolicy = union(enum) {\\n    auto,\\n    fixed: []const u16,\\n};\\n\\n/// BasisPolicy controls the public basis convention.\\npub const BasisPolicy = enum {\\n    default,\\n    coupling_paths,\\n    channel_adapted,\\n};\\n\\n/// InvariantBasisRequest is the CFT-facing request for invariant tensors.\\npub const InvariantBasisRequest = struct {\\n    algebra: store.AlgebraHandle,\\n    external_legs: []const realization.ExternalLeg,\\n    target: TargetIrrep = .singlet,\\n    tree_policy: TreePolicy = .auto,\\n    basis_policy: BasisPolicy = .default,\\n};\\n\\n/// TensorExpansionTerm stores a CFT coefficient attached to one invariant id.\\npub const TensorExpansionTerm = struct {\\n    rational_function_id: u32,\\n    invariant: InvariantHandle,\\n    scalar_id: u32,\\n};\\nThe store interns basis requests.  This is not invariant generation yet; it is\\nthe stable ownership layer the generator will consume.  The important invariant\\nis that caller-owned slices can disappear after registration.\\n/// Store owns interned invariant-basis requests.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    requests: std.ArrayList(InvariantBasisRequest) = .empty,\\n\\n    /// init constructs an empty coupling store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases all coupling-store memory.\\n    pub fn deinit(self: *Store) void {\\n        for (self.requests.items) |request| {\\n            freeRequest(self.allocator, request);\\n        }\\n        self.requests.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internBasisRequest returns a stable basis handle for a request.\\n    pub fn internBasisRequest(self: *Store, request: InvariantBasisRequest) !BasisHandle {\\n        for (self.requests.items, 0..) |stored, index| {\\n            if (requestEql(stored, request)) {\\n                return BasisHandle.init(@intCast(index));\\n            }\\n        }\\n\\n        const owned = try cloneRequest(self.allocator, request);\\n        errdefer freeRequest(self.allocator, owned);\\n        try self.requests.append(self.allocator, owned);\\n        return BasisHandle.init(@intCast(self.requests.items.len - 1));\\n    }\\n};\\nInternal coupling records use offsets and lengths into flat stores.  This is\\nthe shape the future dynamic-programming solver should fill without allocating\\nlarge symbolic expressions.\\n/// CouplingInput names either an external leg or a previous coupling node.\\nconst CouplingInput = union(enum) {\\n    external: realization.ExternalLegHandle,\\n    node: u32,\\n};\\n\\n/// CouplingNodeRecord stores one binary coupling step.\\nconst CouplingNodeRecord = struct {\\n    left: CouplingInput,\\n    right: CouplingInput,\\n    output: store.ChannelHandle,\\n    kernel: projector.ProjectorId,\\n};\\n\\n/// CouplingPathRecord stores a compact invariant basis element.\\nconst CouplingPathRecord = struct {\\n    external_offset: u32,\\n    external_len: u16,\\n    node_offset: u32,\\n    node_len: u16,\\n};\\n\\n/// BasisRecord stores one generated invariant basis.\\nconst BasisRecord = struct {\\n    request: InvariantBasisRequest,\\n    path_offset: u32,\\n    path_len: u32,\\n};\\n\\n/// ReachabilityRecord stores pruning data for one coupling tree node.\\nconst ReachabilityRecord = struct {\\n    node: u32,\\n    irrep_offset: u32,\\n    irrep_len: u32,\\n};\\nRequest equality is structural.  This makes repeated high-level calls cheap and\\ndeterministic before the expensive solver exists.\\nfn requestEql(left: InvariantBasisRequest, right: InvariantBasisRequest) bool {\\n    return left.algebra.value == right.algebra.value and\\n        targetEql(left.target, right.target) and\\n        treePolicyEql(left.tree_policy, right.tree_policy) and\\n        left.basis_policy == right.basis_policy and\\n        externalLegsEql(left.external_legs, right.external_legs);\\n}\\n\\nfn targetEql(left: TargetIrrep, right: TargetIrrep) bool {\\n    return switch (left) {\\n        .singlet => switch (right) {\\n            .singlet => true,\\n            .irrep => false,\\n        },\\n        .irrep => |left_irrep| switch (right) {\\n            .singlet => false,\\n            .irrep => |right_irrep| left_irrep.value == right_irrep.value,\\n        },\\n    };\\n}\\n\\nfn treePolicyEql(left: TreePolicy, right: TreePolicy) bool {\\n    return switch (left) {\\n        .auto => switch (right) {\\n            .auto => true,\\n            .fixed => false,\\n        },\\n        .fixed => |left_fixed| switch (right) {\\n            .auto => false,\\n            .fixed => |right_fixed| std.mem.eql(u16, left_fixed, right_fixed),\\n        },\\n    };\\n}\\n\\nfn externalLegsEql(left: []const realization.ExternalLeg, right: []const realization.ExternalLeg) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_leg, right_leg| {\\n        if (!externalLegEql(left_leg, right_leg)) return false;\\n    }\\n    return true;\\n}\\n\\nfn externalLegEql(left: realization.ExternalLeg, right: realization.ExternalLeg) bool {\\n    return realizationRefEql(left.realization, right.realization) and\\n        namedIndicesEql(left.indices, right.indices) and\\n        optionalStringEql(left.label, right.label);\\n}\\n\\nfn realizationRefEql(left: realization.RealizationRef, right: realization.RealizationRef) bool {\\n    return switch (left) {\\n        .primitive_irrep => |left_irrep| switch (right) {\\n            .primitive_irrep => |right_irrep| left_irrep.value == right_irrep.value,\\n            else => false,\\n        },\\n        .handle => |left_handle| switch (right) {\\n            .handle => |right_handle| left_handle.value == right_handle.value,\\n            else => false,\\n        },\\n        .registered_name => |left_name| switch (right) {\\n            .registered_name => |right_name| std.mem.eql(u8, left_name, right_name),\\n            else => false,\\n        },\\n    };\\n}\\n\\nfn namedIndicesEql(left: []const realization.NamedIndex, right: []const realization.NamedIndex) bool {\\n    if (left.len != right.len) return false;\\n    for (left, right) |left_index, right_index| {\\n        if (left_index.kind != right_index.kind) return false;\\n        if (!std.mem.eql(u8, left_index.name, right_index.name)) return false;\\n    }\\n    return true;\\n}\\n\\nfn optionalStringEql(left: ?[]const u8, right: ?[]const u8) bool {\\n    if (left == null or right == null) return left == null and right == null;\\n    return std.mem.eql(u8, left.?, right.?);\\n}\\nThe clone/free helpers keep ownership local to this module.  Later, generated\\npath data can be added beside requests without changing the public request\\nshape.\\nfn cloneRequest(allocator: std.mem.Allocator, request: InvariantBasisRequest) !InvariantBasisRequest {\\n    const external_legs = try cloneExternalLegs(allocator, request.external_legs);\\n    errdefer freeExternalLegs(allocator, external_legs);\\n\\n    return .{\\n        .algebra = request.algebra,\\n        .external_legs = external_legs,\\n        .target = request.target,\\n        .tree_policy = try cloneTreePolicy(allocator, request.tree_policy),\\n        .basis_policy = request.basis_policy,\\n    };\\n}\\n\\nfn freeRequest(allocator: std.mem.Allocator, request: InvariantBasisRequest) void {\\n    freeExternalLegs(allocator, request.external_legs);\\n    switch (request.tree_policy) {\\n        .fixed => |fixed| allocator.free(fixed),\\n        .auto => {},\\n    }\\n}\\n\\nfn cloneTreePolicy(allocator: std.mem.Allocator, policy: TreePolicy) !TreePolicy {\\n    return switch (policy) {\\n        .auto => .auto,\\n        .fixed => |fixed| .{ .fixed = try allocator.dupe(u16, fixed) },\\n    };\\n}\\n\\nfn cloneExternalLegs(allocator: std.mem.Allocator, legs: []const realization.ExternalLeg) ![]realization.ExternalLeg {\\n    const owned = try allocator.alloc(realization.ExternalLeg, legs.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |leg| freeExternalLeg(allocator, leg);\\n        allocator.free(owned);\\n    }\\n\\n    for (legs, owned[0..]) |source, *target| {\\n        target.* = try cloneExternalLeg(allocator, source);\\n        initialized += 1;\\n    }\\n    return owned;\\n}\\n\\nfn freeExternalLegs(allocator: std.mem.Allocator, legs: []const realization.ExternalLeg) void {\\n    for (legs) |leg| freeExternalLeg(allocator, leg);\\n    allocator.free(legs);\\n}\\n\\nfn cloneExternalLeg(allocator: std.mem.Allocator, leg: realization.ExternalLeg) !realization.ExternalLeg {\\n    const indices = try cloneNamedIndices(allocator, leg.indices);\\n    errdefer freeNamedIndices(allocator, indices);\\n\\n    const label = if (leg.label) |value| try allocator.dupe(u8, value) else null;\\n    errdefer if (label) |value| allocator.free(value);\\n\\n    const ref = try cloneRealizationRef(allocator, leg.realization);\\n    errdefer freeRealizationRef(allocator, ref);\\n\\n    return .{ .realization = ref, .indices = indices, .label = label };\\n}\\n\\nfn freeExternalLeg(allocator: std.mem.Allocator, leg: realization.ExternalLeg) void {\\n    freeRealizationRef(allocator, leg.realization);\\n    freeNamedIndices(allocator, leg.indices);\\n    if (leg.label) |label| allocator.free(label);\\n}\\n\\nfn cloneRealizationRef(allocator: std.mem.Allocator, ref: realization.RealizationRef) !realization.RealizationRef {\\n    return switch (ref) {\\n        .primitive_irrep => |irrep| .{ .primitive_irrep = irrep },\\n        .handle => |handle| .{ .handle = handle },\\n        .registered_name => |name| .{ .registered_name = try allocator.dupe(u8, name) },\\n    };\\n}\\n\\nfn freeRealizationRef(allocator: std.mem.Allocator, ref: realization.RealizationRef) void {\\n    switch (ref) {\\n        .registered_name => |name| allocator.free(name),\\n        .primitive_irrep, .handle => {},\\n    }\\n}\\n\\nfn cloneNamedIndices(allocator: std.mem.Allocator, indices: []const realization.NamedIndex) ![]realization.NamedIndex {\\n    const owned = try allocator.alloc(realization.NamedIndex, indices.len);\\n    var initialized: usize = 0;\\n    errdefer {\\n        for (owned[0..initialized]) |index| allocator.free(index.name);\\n        allocator.free(owned);\\n    }\\n\\n    for (indices, owned[0..]) |source, *target| {\\n        target.* = .{\\n            .kind = source.kind,\\n            .name = try allocator.dupe(u8, source.name),\\n        };\\n        initialized += 1;\\n    }\\n    return owned;\\n}\\n\\nfn freeNamedIndices(allocator: std.mem.Allocator, indices: []const realization.NamedIndex) void {\\n    for (indices) |index| allocator.free(index.name);\\n    allocator.free(indices);\\n}\\n","n":0.029}}},{"i":31,"$":{"1":{"v":"Tensor Rendering","n":0.707},"2":{"v":"Rendering turns compact invariant ids into concrete notation.  Signature is a\\nrendering/evaluation convention, not a different abstract representation.  The\\nsymbolic output is streamed one term at a time so a large expanded projector\\ndoes not become one large allocation.\\nconst coupling = @import(\\"coupling.zig\\");\\n\\n/// SignatureConvention selects the metric convention for rendering/evaluation.\\npub const SignatureConvention = enum {\\n    complex,\\n    euclidean,\\n    lorentzian_mostly_plus,\\n    lorentzian_mostly_minus,\\n};\\n\\n/// ProjectorRenderMode controls how much derived projector data is expanded.\\npub const ProjectorRenderMode = enum {\\n    named,\\n    expanded_blocks,\\n    expanded_terms,\\n};\\n\\n/// RenderFormat selects the output syntax level.\\npub const RenderFormat = enum {\\n    symbolic_terms,\\n    text,\\n};\\n\\n/// RenderOptions controls streamed invariant rendering.\\npub const RenderOptions = struct {\\n    format: RenderFormat = .symbolic_terms,\\n    projectors: ProjectorRenderMode = .named,\\n    signature: SignatureConvention = .complex,\\n    preserve_caller_index_names: bool = true,\\n};\\n\\n/// EvalOptions controls component evaluation of one invariant.\\npub const EvalOptions = struct {\\n    signature: SignatureConvention = .complex,\\n};\\n\\n/// IndexRef names one symbolic index in a rendered term.\\npub const IndexRef = u32;\\n\\n/// IndexBlockId names a compact antisymmetric or ordered index block.\\npub const IndexBlockId = u32;\\n\\n/// RationalId names an exact rational coefficient in the scalar store.\\npub const RationalId = u32;\\n\\n/// GammaBlock stores one internal antisymmetrized gamma factor.\\nconst GammaBlock = struct {\\n    spinor_left: IndexRef,\\n    spinor_right: IndexRef,\\n    vector_block: IndexBlockId,\\n    rank: u8,\\n    chirality: u8,\\n    duality: DualityTag = .none,\\n};\\n\\n/// DualityTag records middle-form self-duality data.\\npub const DualityTag = enum {\\n    none,\\n    self_dual,\\n    anti_self_dual,\\n};\\n\\n/// SymbolicAtom stores one symbolic invariant contraction atom.\\npub const SymbolicAtom = union(enum) {\\n    metric_pair: struct { left: IndexRef, right: IndexRef },\\n    generalized_delta: struct { upper: IndexBlockId, lower: IndexBlockId },\\n    epsilon: struct { block: IndexBlockId },\\n    hodge_star: struct { input: IndexBlockId, output: IndexBlockId },\\n    antisymmetrizer: struct { block: IndexBlockId },\\n    named_operator: struct { operator_id: u32, first_index: u32, index_len: u16 },\\n};\\n\\n/// SymbolicTerm stores one streamed coefficient times symbolic atoms.\\npub const SymbolicTerm = struct {\\n    coefficient: RationalId,\\n    /// atoms is borrowed and valid only until the sink returns.\\n    atoms: []const SymbolicAtom,\\n};\\n\\n/// emitTerm sends one symbolic term to a caller-provided sink.\\npub fn emitTerm(sink: anytype, term: SymbolicTerm) !void {\\n    try sink.emitTerm(term);\\n}\\n\\n/// RenderedTermSpan stores a compact range of already emitted terms.\\nconst RenderedTermSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// ReductionTerm stores one coefficient of an invariant in a target basis.\\nconst ReductionTerm = struct {\\n    invariant: coupling.InvariantHandle,\\n    coefficient: RationalId,\\n};\\n","n":0.054}}},{"i":32,"$":{"1":{"v":"Tensor Backend","n":0.707},"2":{"v":"Backends are internal family-specific providers.  The public workflow should\\nnot depend on an ADE, gamma-matrix, Young-symmetrizer, or exceptional-algebra\\nimplementation detail.  A backend validates support, supplies representation\\nmetadata, decomposes binary products, constructs local projector ids, and\\nstreams concrete symbolic terms when rendering is requested.\\nconst decomposition = @import(\\"decomposition.zig\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\n/// BackendId names one registered Lie-family backend.\\npub const BackendId = u16;\\n\\n/// PrimitiveInvariantSpan names backend-owned primitive invariant descriptors.\\npub const PrimitiveInvariantSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// RenderConvention records the concrete rendering convention passed to a backend.\\npub const RenderConvention = struct {\\n    options: rendering.RenderOptions,\\n};\\n\\n/// Capabilities is the internal contract every Lie-family backend must satisfy.\\npub const Capabilities = struct {\\n    validate_algebra: *const fn (*anyopaque, symmetry.SimpleLieAlgebra) anyerror!void,\\n    irrep_metadata: *const fn (*anyopaque, store.AlgebraHandle, store.IrrepHandle) anyerror!store.IrrepMetadata,\\n    decompose_product: *const fn (*anyopaque, decomposition.ProductKey, *anyopaque) anyerror!void,\\n    local_projector: *const fn (*anyopaque, projector.ProjectorKey) anyerror!projector.ProjectorId,\\n    primitive_invariants: *const fn (*anyopaque, []const store.IrrepHandle) anyerror!PrimitiveInvariantSpan,\\n    render_projector: *const fn (*anyopaque, projector.ProjectorId, RenderConvention, *anyopaque) anyerror!void,\\n};\\n\\n/// Record stores one backend state pointer and capability table.\\npub const Record = struct {\\n    id: BackendId,\\n    state: *anyopaque,\\n    family: symmetry.LieFamily,\\n    root_conventions: root_data.AlgebraConventions,\\n    capabilities: Capabilities,\\n};\\n","n":0.075}}},{"i":33,"$":{"1":{"v":"Tensor Decomposition","n":0.707},"2":{"v":"A tensor-product decomposition is the compact statement\\n\\\\[\\n  R\\\\otimes S=\\\\bigoplus_T m_T T.\\n\\\\]\\nThis module stores that statement as flat product terms.  It does not compute\\nthe decomposition; the family backend will do that and intern the result here.\\nconst std = @import(\\"std\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ProductDecompositionHandle names a cached binary decomposition.\\npub const ProductDecompositionHandle = struct {\\n    value: u32,\\n\\n    /// init constructs a product-decomposition handle.\\n    pub fn init(value: u32) ProductDecompositionHandle {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ProductKey identifies a cached tensor-product decomposition.\\npub const ProductKey = struct {\\n    left: store.IrrepHandle,\\n    right: store.IrrepHandle,\\n};\\n\\n/// ProductTerm stores one channel and its multiplicity in a product.\\npub const ProductTerm = struct {\\n    irrep: store.IrrepHandle,\\n    multiplicity: u16,\\n};\\n\\n/// ProductTermSpan stores a flat range of product terms.\\npub const ProductTermSpan = struct {\\n    offset: u32,\\n    len: u32,\\n};\\n\\n/// Store owns cached binary tensor-product decomposition records.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    decompositions: std.ArrayList(ProductDecomposition) = .empty,\\n    terms: std.ArrayList(ProductTerm) = .empty,\\n    product_index: std.AutoHashMap(u64, ProductDecompositionHandle),\\n\\n    /// init constructs an empty decomposition store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{\\n            .allocator = allocator,\\n            .product_index = std.AutoHashMap(u64, ProductDecompositionHandle).init(allocator),\\n        };\\n    }\\n\\n    /// deinit releases decomposition-store memory.\\n    pub fn deinit(self: *Store) void {\\n        self.product_index.deinit();\\n        self.terms.deinit(self.allocator);\\n        self.decompositions.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// findProduct returns a cached product handle if present.\\n    pub fn findProduct(self: Store, key: ProductKey) ?ProductDecompositionHandle {\\n        return self.product_index.get(productKeyBits(key));\\n    }\\n\\n    /// internProduct stores a binary product decomposition.\\n    pub fn internProduct(self: *Store, key: ProductKey, terms: []const ProductTerm) !ProductDecompositionHandle {\\n        if (self.findProduct(key)) |handle| return handle;\\n\\n        const offset: u32 = @intCast(self.terms.items.len);\\n        try self.terms.appendSlice(self.allocator, terms);\\n        errdefer self.terms.shrinkRetainingCapacity(offset);\\n\\n        const handle = ProductDecompositionHandle.init(@intCast(self.decompositions.items.len));\\n        try self.decompositions.append(self.allocator, .{\\n            .key = key,\\n            .terms = .{ .offset = offset, .len = @intCast(terms.len) },\\n        });\\n        errdefer _ = self.decompositions.pop();\\n\\n        try self.product_index.put(productKeyBits(key), handle);\\n        return handle;\\n    }\\n\\n    /// productTerms returns the cached term span for a product handle.\\n    pub fn productTerms(self: Store, handle: ProductDecompositionHandle) ?[]const ProductTerm {\\n        const index: usize = @intCast(handle.value);\\n        if (index >= self.decompositions.items.len) return null;\\n        const span = self.decompositions.items[index].terms;\\n        const start: usize = @intCast(span.offset);\\n        const end = start + span.len;\\n        return self.terms.items[start..end];\\n    }\\n};\\n\\n/// ProductDecomposition stores a flat range of product terms.\\nconst ProductDecomposition = struct {\\n    key: ProductKey,\\n    terms: ProductTermSpan,\\n};\\n\\n/// SingletCountRequest records a generic invariant-counting problem.\\nconst SingletCountRequest = struct {\\n    algebra: store.AlgebraHandle,\\n    factors: []const store.IrrepHandle,\\n    target: coupling.TargetIrrep = .singlet,\\n};\\n\\nfn productKeyBits(key: ProductKey) u64 {\\n    return (@as(u64, key.left.value) << 32) | @as(u64, key.right.value);\\n}\\n","n":0.052}}},{"i":34,"$":{"1":{"v":"Tensor Projector","n":0.707},"2":{"v":"A projector is an idempotent intertwiner.  It can select a channel in\\n\\\\(R\\\\otimes S\\\\), impose a boundary realization such as a gamma-traceless\\nsubspace, or change between invariant-basis conventions.  This module stores\\nprojector identities and derivation labels; it does not construct formulas.\\nconst std = @import(\\"std\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ProjectorId names one local projector or Clebsch-Gordan kernel.\\npub const ProjectorId = u32;\\n\\n/// ProjectorConvention records configurable projector-rendering choices.\\npub const ProjectorConvention = struct {\\n    render_mode: rendering.ProjectorRenderMode = .named,\\n    signature: rendering.SignatureConvention = .complex,\\n    normalization_id: u32 = 0,\\n};\\n\\n/// ProjectorRole records what mathematical object a projector selects.\\npub const ProjectorRole = union(enum) {\\n    product_channel: ProductChannelKey,\\n    boundary_realization: realization.RealizationHandle,\\n    basis_change: BasisChangeKey,\\n};\\n\\n/// ProductChannelKey identifies one target channel inside a binary product.\\npub const ProductChannelKey = struct {\\n    left: store.IrrepHandle,\\n    right: store.IrrepHandle,\\n    target: store.ChannelHandle,\\n};\\n\\n/// BasisChangeKey identifies a change of invariant-basis convention.\\npub const BasisChangeKey = struct {\\n    source_basis: u32,\\n    target_basis: u32,\\n};\\n\\n/// Store owns projector identities and compact emitted-term spans.\\npub const Store = struct {\\n    allocator: std.mem.Allocator,\\n    records: std.ArrayList(ProjectorRecord) = .empty,\\n\\n    /// init constructs an empty projector store.\\n    pub fn init(allocator: std.mem.Allocator) Store {\\n        return .{ .allocator = allocator };\\n    }\\n\\n    /// deinit releases projector-store memory.\\n    pub fn deinit(self: *Store) void {\\n        self.records.deinit(self.allocator);\\n        self.* = Store.init(self.allocator);\\n    }\\n\\n    /// internProjector returns a stable id for a projector identity.\\n    pub fn internProjector(self: *Store, key: ProjectorKey, kind: ProjectorKind) !ProjectorId {\\n        for (self.records.items, 0..) |record, index| {\\n            if (projectorKeyEql(record.key, key)) return @intCast(index);\\n        }\\n        try self.records.append(self.allocator, .{\\n            .key = key,\\n            .kind = kind,\\n            .term_offset = 0,\\n            .term_len = 0,\\n        });\\n        return @intCast(self.records.items.len - 1);\\n    }\\n};\\n\\n/// ProjectorKey identifies one projector in one convention.\\npub const ProjectorKey = struct {\\n    role: ProjectorRole,\\n    convention: ProjectorConvention,\\n};\\n\\n/// ProjectorKind records how a local projector was derived.\\npub const ProjectorKind = enum {\\n    identity,\\n    quadratic_casimir,\\n    higher_casimir_signature,\\n    local_idempotent_split,\\n    highest_weight_solver,\\n    backend_specific_verified,\\n};\\n\\n/// ProjectorRecord stores one local kernel and its derivation kind.\\nconst ProjectorRecord = struct {\\n    key: ProjectorKey,\\n    kind: ProjectorKind,\\n    term_offset: u32,\\n    term_len: u32,\\n};\\n\\n/// ProjectorAudit stores local splitting diagnostics for feasibility studies.\\nconst ProjectorAudit = struct {\\n    product: ProductChannelKey,\\n    channel_count: u32,\\n    quadratic_collision_count: u32,\\n    true_multiplicity_count: u32,\\n    residual_block_max: u32,\\n};\\n\\nfn projectorKeyEql(left: ProjectorKey, right: ProjectorKey) bool {\\n    return projectorRoleEql(left.role, right.role) and\\n        left.convention.render_mode == right.convention.render_mode and\\n        left.convention.signature == right.convention.signature and\\n        left.convention.normalization_id == right.convention.normalization_id;\\n}\\n\\nfn projectorRoleEql(left: ProjectorRole, right: ProjectorRole) bool {\\n    return switch (left) {\\n        .product_channel => |left_key| switch (right) {\\n            .product_channel => |right_key| left_key.left.value == right_key.left.value and\\n                left_key.right.value == right_key.right.value and\\n                left_key.target.value == right_key.target.value,\\n            else => false,\\n        },\\n        .boundary_realization => |left_handle| switch (right) {\\n            .boundary_realization => |right_handle| left_handle.value == right_handle.value,\\n            else => false,\\n        },\\n        .basis_change => |left_key| switch (right) {\\n            .basis_change => |right_key| left_key.source_basis == right_key.source_basis and left_key.target_basis == right_key.target_basis,\\n            else => false,\\n        },\\n    };\\n}\\n","n":0.049}}},{"i":35,"$":{"1":{"v":"Tensor Context","n":0.707},"2":{"v":"The context module is the procedural boundary of tensor-code.  Public callers\\nget an opaque state pointer and methods; the stores remain in a private\\nimplementation record so callers cannot bypass validation.\\nconst std = @import(\\"std\\");\\nconst coupling = @import(\\"coupling.zig\\");\\nconst decomposition = @import(\\"decomposition.zig\\");\\nconst projector = @import(\\"projector.zig\\");\\nconst realization = @import(\\"realization.zig\\");\\nconst rendering = @import(\\"rendering.zig\\");\\nconst root_data = @import(\\"root-data.zig\\");\\nconst store = @import(\\"representation-store.zig\\");\\n\\n/// ContextId names an initialized tensor-code context or preset.\\npub const ContextId = struct {\\n    value: u32,\\n\\n    /// init constructs a context id from a store index.\\n    pub fn init(value: u32) ContextId {\\n        return .{ .value = value };\\n    }\\n};\\n\\n/// ContextOptions configures algebra and rendering defaults.\\npub const ContextOptions = struct {\\n    algebra_conventions: root_data.AlgebraConventions = .{},\\n    default_signature: rendering.SignatureConvention = .complex,\\n};\\nRegistration validates cross-store handles before interning.  Metadata and\\nduals are queried through methods, not by reaching into stores.\\n/// Context is an opaque handle to tensor-code stores and caches.\\npub const Context = struct {\\n    state: *anyopaque,\\n    allocator: std.mem.Allocator,\\n\\n    /// init constructs an empty tensor-code context.\\n    pub fn init(allocator: std.mem.Allocator) !Context {\\n        return Context.initWithOptions(allocator, .{});\\n    }\\n\\n    /// initWithOptions constructs a context with explicit conventions.\\n    pub fn initWithOptions(allocator: std.mem.Allocator, options: ContextOptions) !Context {\\n        const state = try allocator.create(Impl);\\n        errdefer allocator.destroy(state);\\n        state.* = Impl.init(allocator, options);\\n        return .{\\n            .state = state,\\n            .allocator = allocator,\\n        };\\n    }\\n\\n    /// deinit releases memory owned by this context.\\n    pub fn deinit(self: *Context) void {\\n        const state = self.impl();\\n        state.deinit();\\n        self.allocator.destroy(state);\\n        self.* = undefined;\\n    }\\n\\n    /// registerAlgebra interns a Lie algebra and returns its handle.\\n    pub fn registerAlgebra(self: *Context, spec: store.AlgebraSpec) !store.AlgebraHandle {\\n        return self.impl().representations.internAlgebra(spec);\\n    }\\n\\n    /// registerIrrep interns an abstract irreducible representation.\\n    pub fn registerIrrep(self: *Context, algebra: store.AlgebraHandle, spec: store.IrrepSpec) !store.IrrepHandle {\\n        return self.impl().representations.internIrrep(algebra, spec);\\n    }\\n\\n    /// dualIrrep returns the registered dual representation.\\n    pub fn dualIrrep(self: *Context, irrep: store.IrrepHandle) !store.IrrepHandle {\\n        return self.impl().representations.dualIrrep(irrep);\\n    }\\n\\n    /// irrepMetadata returns derived metadata for a registered irrep.\\n    pub fn irrepMetadata(self: Context, irrep: store.IrrepHandle) ?store.IrrepMetadata {\\n        return self.impl().representations.irrepMetadata(irrep);\\n    }\\n\\n    /// registerRealization interns a concrete index realization of an irrep.\\n    pub fn registerRealization(self: *Context, spec: realization.RealizationSpec) !realization.RealizationHandle {\\n        try self.validateRealizationSpec(spec);\\n        return self.impl().realizations.internRealization(spec);\\n    }\\n\\n    /// invariantBasis generates or retrieves a compact invariant basis.\\n    pub fn invariantBasis(self: *Context, request: coupling.InvariantBasisRequest) !coupling.BasisHandle {\\n        try self.validateInvariantBasisRequest(request);\\n        return self.impl().couplings.internBasisRequest(request);\\n    }\\n\\n    /// renderInvariant streams one invariant in the requested convention.\\n    pub fn renderInvariant(self: *Context, basis: coupling.BasisHandle, invariant: coupling.InvariantHandle, options: rendering.RenderOptions, sink: anytype) !void {\\n        _ = self;\\n        _ = basis;\\n        _ = invariant;\\n        _ = options;\\n        _ = sink;\\n        return error.NotImplemented;\\n    }\\n\\n    /// evaluateInvariant streams component-evaluation data for one invariant.\\n    pub fn evaluateInvariant(self: *Context, invariant: coupling.InvariantHandle, options: rendering.EvalOptions, sink: anytype) !void {\\n        _ = self;\\n        _ = invariant;\\n        _ = options;\\n        _ = sink;\\n        return error.NotImplemented;\\n    }\\n\\n    fn validateRealizationSpec(self: Context, spec: realization.RealizationSpec) !void {\\n        const target_algebra = self.impl().representations.irrepAlgebra(spec.channel.irrep) orelse return error.UnknownIrrep;\\n        for (spec.ambient) |ambient| {\\n            const ambient_algebra = self.impl().representations.irrepAlgebra(ambient) orelse return error.UnknownIrrep;\\n            if (ambient_algebra.value != target_algebra.value) return error.MixedRealizationAlgebras;\\n        }\\n    }\\n\\n    fn validateInvariantBasisRequest(self: Context, request: coupling.InvariantBasisRequest) !void {\\n        if (!self.impl().representations.containsAlgebra(request.algebra)) return error.UnknownAlgebra;\\n        for (request.external_legs) |leg| {\\n            const leg_algebra = try self.externalLegAlgebra(leg);\\n            if (leg_algebra) |algebra| {\\n                if (algebra.value != request.algebra.value) return error.MixedInvariantAlgebras;\\n            }\\n        }\\n    }\\n\\n    fn externalLegAlgebra(self: Context, leg: realization.ExternalLeg) !?store.AlgebraHandle {\\n        return switch (leg.realization) {\\n            .primitive_irrep => |irrep| self.impl().representations.irrepAlgebra(irrep) orelse return error.UnknownIrrep,\\n            .handle => |handle| {\\n                const irrep = self.impl().realizations.targetIrrep(handle) orelse return error.UnknownRealization;\\n                return self.impl().representations.irrepAlgebra(irrep) orelse return error.UnknownIrrep;\\n            },\\n            .registered_name => null,\\n        };\\n    }\\n\\n    fn impl(self: Context) *Impl {\\n        return @ptrCast(@alignCast(self.state));\\n    }\\n};\\nThe private implementation record owns the flat stores.  It is not imported by\\nthe root API.\\nconst Impl = struct {\\n    id: ContextId,\\n    allocator: std.mem.Allocator,\\n    options: ContextOptions,\\n    representations: store.Store,\\n    realizations: realization.Store,\\n    decompositions: decomposition.Store,\\n    projectors: projector.Store,\\n    couplings: coupling.Store,\\n\\n    fn init(allocator: std.mem.Allocator, options: ContextOptions) Impl {\\n        return .{\\n            .id = ContextId.init(0),\\n            .allocator = allocator,\\n            .options = options,\\n            .representations = store.Store.init(allocator, options.algebra_conventions),\\n            .realizations = realization.Store.init(allocator),\\n            .decompositions = decomposition.Store.init(allocator),\\n            .projectors = projector.Store.init(allocator),\\n            .couplings = coupling.Store.init(allocator),\\n        };\\n    }\\n\\n    fn deinit(self: *Impl) void {\\n        self.couplings.deinit();\\n        self.projectors.deinit();\\n        self.decompositions.deinit();\\n        self.realizations.deinit();\\n        self.representations.deinit();\\n        self.* = Impl.init(self.allocator, self.options);\\n    }\\n};\\n","n":0.04}}},{"i":36,"$":{"1":{"v":"Tensor Root Data","n":0.577},"2":{"v":"Root data is the finite combinatorial description of a semisimple Lie algebra:\\nsimple roots, the Cartan matrix, Dynkin labels, and the positive roots.  For a\\nhighest weight \\\\(\\\\lambda\\\\), the Dynkin labels are the integers\\n\\\\(\\\\lambda_i=\\\\langle \\\\lambda,\\\\alpha_i^\\\\vee\\\\rangle\\\\).  Weyl\'s dimension formula\\nuses the positive roots:\\n\\\\[\\n  \\\\dim V_\\\\lambda =\\n  \\\\prod_{\\\\alpha>0}\\n  \\\\frac{\\\\langle \\\\lambda+\\\\rho,\\\\alpha^\\\\vee\\\\rangle}\\n       {\\\\langle \\\\rho,\\\\alpha^\\\\vee\\\\rangle}.\\n\\\\]\\nFor ADE all roots have the same length.  For \\\\(B_n\\\\), used by odd orthogonal\\nalgebras, the short-root length is handled by exact rational root scales.  The\\nquadratic Casimir is computed in the configurable normalization\\n\\\\[\\n  C_2(\\\\lambda)=\\\\frac{(\\\\lambda,\\\\lambda+2\\\\rho)}{2}\\n\\\\]\\ntimes quadratic_casimir_scale.\\nconst std = @import(\\"std\\");\\nconst symmetry = @import(\\"symmetry.zig\\");\\n\\nconst max_rank = 256;\\n\\n/// Rational stores a normalized exact rational number.\\npub const Rational = struct {\\n    numerator: i128 = 0,\\n    denominator: i128 = 1,\\n\\n    /// init constructs and normalizes a rational number.\\n    pub fn init(numerator: i128, denominator: i128) !Rational {\\n        if (denominator == 0) return error.ZeroDenominator;\\n        var out = Rational{ .numerator = numerator, .denominator = denominator };\\n        out.normalize();\\n        return out;\\n    }\\n\\n    /// one returns the rational number 1.\\n    pub fn one() Rational {\\n        return .{ .numerator = 1, .denominator = 1 };\\n    }\\n\\n    fn normalize(self: *Rational) void {\\n        if (self.numerator == 0) {\\n            self.denominator = 1;\\n            return;\\n        }\\n        if (self.denominator < 0) {\\n            self.numerator = -self.numerator;\\n            self.denominator = -self.denominator;\\n        }\\n        const divisor = gcdI128(absI128(self.numerator), absI128(self.denominator));\\n        self.numerator = @divExact(self.numerator, divisor);\\n        self.denominator = @divExact(self.denominator, divisor);\\n    }\\n};\\n\\n/// AlgebraConventions selects configurable exact-algebra normalizations.\\npub const AlgebraConventions = struct {\\n    quadratic_casimir_scale: Rational = .{ .numerator = 1, .denominator = 1 },\\n};\\n\\n/// RootDatum stores a simple algebra with its normalization conventions.\\npub const RootDatum = struct {\\n    algebra: symmetry.SimpleLieAlgebra,\\n    conventions: AlgebraConventions,\\n\\n    /// init validates a supported algebra and returns its root datum.\\n    pub fn init(algebra: symmetry.SimpleLieAlgebra, conventions: AlgebraConventions) !RootDatum {\\n        try validateSimple(algebra);\\n        return .{ .algebra = algebra, .conventions = conventions };\\n    }\\n\\n    /// rank returns the number of simple roots.\\n    pub fn rank(self: RootDatum) u8 {\\n        return self.algebra.rank;\\n    }\\n\\n    /// cartan returns one Cartan matrix entry.\\n    pub fn cartan(self: RootDatum, row: usize, col: usize) !i16 {\\n        return cartanEntry(self.algebra, row, col);\\n    }\\n};\\n\\n/// validateSimple rejects unsupported or rank-inconsistent simple algebras.\\npub fn validateSimple(algebra: symmetry.SimpleLieAlgebra) !void {\\n    if (algebra.rank > max_rank) return error.RankTooLarge;\\n    switch (algebra.family) {\\n        .a => if (algebra.rank < 1) return error.InvalidRank,\\n        .b => if (algebra.rank < 2) return error.InvalidRank,\\n        .d => if (algebra.rank < 4) return error.InvalidRank,\\n        .e6 => if (algebra.rank != 6) return error.InvalidRank,\\n        .e7 => if (algebra.rank != 7) return error.InvalidRank,\\n        .e8 => if (algebra.rank != 8) return error.InvalidRank,\\n        else => return error.UnsupportedAlgebraFamily,\\n    }\\n}\\n\\n/// dualDynkin returns the highest weight of the dual irrep.\\npub fn dualDynkin(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16) ![]i16 {\\n    try validateDynkinLabel(algebra, label);\\n    const out = try allocator.dupe(i16, label);\\n    switch (algebra.family) {\\n        .a => {\\n            for (0..label.len) |i| out[i] = label[label.len - 1 - i];\\n        },\\n        .b => {},\\n        .d => {\\n            if ((algebra.rank % 2) == 1) {\\n                const left = algebra.rank - 2;\\n                const right = algebra.rank - 1;\\n                out[left] = label[right];\\n                out[right] = label[left];\\n            }\\n        },\\n        .e6 => {\\n            out[0] = label[4];\\n            out[1] = label[3];\\n            out[3] = label[1];\\n            out[4] = label[0];\\n        },\\n        .e7, .e8 => {},\\n        else => unreachable,\\n    }\\n    return out;\\n}\\n\\n/// dimension computes the Weyl dimension for a supported highest weight.\\npub fn dimension(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16) !u128 {\\n    try validateDynkinLabel(algebra, label);\\n    var roots = try PositiveRoots.init(allocator, algebra);\\n    defer roots.deinit(allocator);\\n\\n    var product = Rational.one();\\n    for (0..roots.len) |root_index| {\\n        const root = roots.root(root_index);\\n        const top = try rootCorootPairing(algebra, root, label);\\n        const bottom = try rootCorootPairing(algebra, root, null);\\n        product = try mul(product, try div(top, bottom));\\n    }\\n    return rationalToU128(product);\\n}\\n\\n/// quadraticCasimir computes \\\\(C_2(\\\\lambda)=(\\\\lambda,\\\\lambda+2\\\\rho)/2\\\\).\\npub fn quadraticCasimir(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra, label: []const i16, conventions: AlgebraConventions) !Rational {\\n    try validateDynkinLabel(algebra, label);\\n    const rank_value: usize = algebra.rank;\\n    const inverse = try inverseCartan(allocator, algebra);\\n    defer allocator.free(inverse);\\n\\n    var total = Rational{};\\n    for (0..rank_value) |i| {\\n        for (0..rank_value) |j| {\\n            const weight = @as(i128, label[i]) * @as(i128, label[j] + 2);\\n            if (weight == 0) continue;\\n            const gram = try mul(simpleRootScale(algebra, i), inverse[i * rank_value + j]);\\n            total = try add(total, try mulInt(gram, weight));\\n        }\\n    }\\n    total = try divInt(total, 2);\\n    return try mul(total, conventions.quadratic_casimir_scale);\\n}\\n\\nfn validateDynkinLabel(algebra: symmetry.SimpleLieAlgebra, label: []const i16) !void {\\n    try validateSimple(algebra);\\n    if (label.len != algebra.rank) return error.InvalidDynkinRank;\\n    for (label) |entry| {\\n        if (entry < 0) return error.InvalidDynkinLabel;\\n    }\\n}\\n\\nfn cartanEntry(algebra: symmetry.SimpleLieAlgebra, row: usize, col: usize) !i16 {\\n    try validateSimple(algebra);\\n    const rank_value: usize = algebra.rank;\\n    if (row >= rank_value or col >= rank_value) return error.CartanIndexOutOfRange;\\n    if (row == col) return 2;\\n    return switch (algebra.family) {\\n        .a => if (adjacent(row, col)) -1 else 0,\\n        .b => bCartan(rank_value, row, col),\\n        .d => if (dConnected(rank_value, row, col)) -1 else 0,\\n        .e6, .e7, .e8 => if (eConnected(algebra.rank, row, col)) -1 else 0,\\n        else => unreachable,\\n    };\\n}\\n\\nfn bCartan(rank_value: usize, row: usize, col: usize) i16 {\\n    if (!adjacent(row, col)) return 0;\\n    if (row == rank_value - 2 and col == rank_value - 1) return -1;\\n    if (row == rank_value - 1 and col == rank_value - 2) return -2;\\n    return -1;\\n}\\n\\nfn adjacent(row: usize, col: usize) bool {\\n    return row + 1 == col or col + 1 == row;\\n}\\n\\nfn dConnected(rank_value: usize, row: usize, col: usize) bool {\\n    if (rank_value < 4) return false;\\n    const branch = rank_value - 3;\\n    if (row <= branch and col <= branch) return adjacent(row, col);\\n    return (row == branch and (col == rank_value - 2 or col == rank_value - 1)) or\\n        (col == branch and (row == rank_value - 2 or row == rank_value - 1));\\n}\\n\\nfn eConnected(rank_value: usize, row: usize, col: usize) bool {\\n    if (edge(row, col, 0, 1) or edge(row, col, 1, 2) or edge(row, col, 2, 3) or edge(row, col, 3, 4)) return true;\\n    if (rank_value >= 7 and edge(row, col, 4, 5)) return true;\\n    if (rank_value >= 8 and edge(row, col, 5, 6)) return true;\\n    return edge(row, col, 2, rank_value - 1);\\n}\\n\\nfn edge(row: usize, col: usize, left: usize, right: usize) bool {\\n    return (row == left and col == right) or (row == right and col == left);\\n}\\n\\nconst PositiveRoots = struct {\\n    rank: usize,\\n    roots: []u8,\\n    len: usize = 0,\\n\\n    fn init(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra) !PositiveRoots {\\n        const rank_value: usize = algebra.rank;\\n        const capacity = positiveRootCapacity(algebra);\\n        var out = PositiveRoots{\\n            .rank = rank_value,\\n            .roots = try allocator.alloc(u8, capacity * rank_value),\\n        };\\n        @memset(out.roots, 0);\\n        errdefer allocator.free(out.roots);\\n\\n        if (algebra.family == .b) {\\n            try out.addBPositiveRoots();\\n            return out;\\n        }\\n\\n        for (0..rank_value) |i| {\\n            var simple = [_]u8{0} ** max_rank;\\n            simple[i] = 1;\\n            try out.add(simple[0..rank_value]);\\n        }\\n\\n        var cursor: usize = 0;\\n        while (cursor < out.len) : (cursor += 1) {\\n            const current = out.root(cursor);\\n            for (0..rank_value) |i| {\\n                if (pairing(algebra, current, i) >= 0) continue;\\n                var candidate = [_]u8{0} ** max_rank;\\n                @memcpy(candidate[0..rank_value], current);\\n                candidate[i] += 1;\\n                try out.add(candidate[0..rank_value]);\\n            }\\n        }\\n        return out;\\n    }\\n\\n    fn addBPositiveRoots(self: *PositiveRoots) !void {\\n        for (0..self.rank) |i| {\\n            var short = [_]u8{0} ** max_rank;\\n            for (i..self.rank) |k| short[k] = 1;\\n            try self.add(short[0..self.rank]);\\n\\n            for (i + 1..self.rank) |j| {\\n                var difference = [_]u8{0} ** max_rank;\\n                for (i..j) |k| difference[k] = 1;\\n                try self.add(difference[0..self.rank]);\\n\\n                var sum = [_]u8{0} ** max_rank;\\n                for (i..j) |k| sum[k] = 1;\\n                for (j..self.rank) |k| sum[k] = 2;\\n                try self.add(sum[0..self.rank]);\\n            }\\n        }\\n    }\\n\\n    fn deinit(self: *PositiveRoots, allocator: std.mem.Allocator) void {\\n        allocator.free(self.roots);\\n        self.* = undefined;\\n    }\\n\\n    fn root(self: PositiveRoots, index: usize) []const u8 {\\n        const start = index * self.rank;\\n        return self.roots[start .. start + self.rank];\\n    }\\n\\n    fn add(self: *PositiveRoots, root_value: []const u8) !void {\\n        if (self.contains(root_value)) return;\\n        if (self.len * self.rank >= self.roots.len) return error.PositiveRootOverflow;\\n        const start = self.len * self.rank;\\n        @memcpy(self.roots[start .. start + self.rank], root_value);\\n        self.len += 1;\\n    }\\n\\n    fn contains(self: PositiveRoots, root_value: []const u8) bool {\\n        for (0..self.len) |i| {\\n            if (std.mem.eql(u8, self.root(i), root_value)) return true;\\n        }\\n        return false;\\n    }\\n};\\n\\nfn positiveRootCapacity(algebra: symmetry.SimpleLieAlgebra) usize {\\n    const rank_value: usize = algebra.rank;\\n    return switch (algebra.family) {\\n        .a => rank_value * (rank_value + 1) / 2,\\n        .b => rank_value * rank_value,\\n        .d => rank_value * (rank_value - 1),\\n        .e6 => 36,\\n        .e7 => 63,\\n        .e8 => 120,\\n        else => unreachable,\\n    };\\n}\\n\\nfn pairing(algebra: symmetry.SimpleLieAlgebra, root: []const u8, coroot_index: usize) i16 {\\n    var out: i16 = 0;\\n    for (root, 0..) |coefficient, j| {\\n        const entry = cartanEntry(algebra, coroot_index, j) catch unreachable;\\n        out += @as(i16, @intCast(coefficient)) * entry;\\n    }\\n    return out;\\n}\\n\\nfn inverseCartan(allocator: std.mem.Allocator, algebra: symmetry.SimpleLieAlgebra) ![]Rational {\\n    const rank_value: usize = algebra.rank;\\n    const width = rank_value * 2;\\n    var matrix = try allocator.alloc(Rational, rank_value * width);\\n    errdefer allocator.free(matrix);\\n\\n    for (0..rank_value) |row| {\\n        for (0..width) |col| {\\n            matrix[row * width + col] = Rational{};\\n        }\\n        for (0..rank_value) |col| {\\n            matrix[row * width + col] = try Rational.init(try cartanEntry(algebra, row, col), 1);\\n        }\\n        matrix[row * width + rank_value + row] = Rational.one();\\n    }\\n\\n    for (0..rank_value) |col| {\\n        var pivot = col;\\n        while (pivot < rank_value and matrix[pivot * width + col].numerator == 0) : (pivot += 1) {}\\n        if (pivot == rank_value) return error.SingularCartanMatrix;\\n        if (pivot != col) {\\n            for (0..width) |j| {\\n                std.mem.swap(Rational, &matrix[col * width + j], &matrix[pivot * width + j]);\\n            }\\n        }\\n\\n        const pivot_value = matrix[col * width + col];\\n        for (0..width) |j| matrix[col * width + j] = try div(matrix[col * width + j], pivot_value);\\n\\n        for (0..rank_value) |row| {\\n            if (row == col) continue;\\n            const factor = matrix[row * width + col];\\n            if (factor.numerator == 0) continue;\\n            for (0..width) |j| {\\n                matrix[row * width + j] = try sub(matrix[row * width + j], try mul(factor, matrix[col * width + j]));\\n            }\\n        }\\n    }\\n\\n    var out = try allocator.alloc(Rational, rank_value * rank_value);\\n    errdefer allocator.free(out);\\n    for (0..rank_value) |row| {\\n        for (0..rank_value) |col| {\\n            out[row * rank_value + col] = matrix[row * width + rank_value + col];\\n        }\\n    }\\n    allocator.free(matrix);\\n    return out;\\n}\\n\\nfn rootCorootPairing(algebra: symmetry.SimpleLieAlgebra, root: []const u8, label: ?[]const i16) !Rational {\\n    var inner = Rational{};\\n    for (root, 0..) |coefficient, i| {\\n        const dynkin = if (label) |values| values[i] + 1 else 1;\\n        inner = try add(inner, try mulInt(simpleRootScale(algebra, i), @as(i128, coefficient) * @as(i128, dynkin)));\\n    }\\n    return try div(try mulInt(inner, 2), try rootLengthSquared(algebra, root));\\n}\\n\\nfn rootLengthSquared(algebra: symmetry.SimpleLieAlgebra, root: []const u8) !Rational {\\n    var out = Rational{};\\n    for (root, 0..) |left_coefficient, i| {\\n        if (left_coefficient == 0) continue;\\n        for (root, 0..) |right_coefficient, j| {\\n            if (right_coefficient == 0) continue;\\n            const entry = try cartanEntry(algebra, i, j);\\n            const factor = @as(i128, left_coefficient) * @as(i128, right_coefficient) * @as(i128, entry);\\n            out = try add(out, try mulInt(simpleRootScale(algebra, i), factor));\\n        }\\n    }\\n    return out;\\n}\\n\\nfn simpleRootScale(algebra: symmetry.SimpleLieAlgebra, index: usize) Rational {\\n    return switch (algebra.family) {\\n        .b => if (index == algebra.rank - 1)\\n            .{ .numerator = 1, .denominator = 2 }\\n        else\\n            Rational.one(),\\n        else => Rational.one(),\\n    };\\n}\\n\\nfn rationalToU128(value: Rational) !u128 {\\n    if (value.denominator != 1) return error.NonIntegralDimension;\\n    if (value.numerator < 0) return error.NegativeDimension;\\n    return std.math.cast(u128, value.numerator) orelse error.DimensionOverflow;\\n}\\n\\nfn add(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.add(i128, try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, right.numerator, left.denominator)), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn sub(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.sub(i128, try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, right.numerator, left.denominator)), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn mul(left: Rational, right: Rational) !Rational {\\n    return Rational.init(try std.math.mul(i128, left.numerator, right.numerator), try std.math.mul(i128, left.denominator, right.denominator));\\n}\\n\\nfn div(left: Rational, right: Rational) !Rational {\\n    if (right.numerator == 0) return error.DivisionByZero;\\n    return Rational.init(try std.math.mul(i128, left.numerator, right.denominator), try std.math.mul(i128, left.denominator, right.numerator));\\n}\\n\\nfn mulInt(value: Rational, factor: i128) !Rational {\\n    return Rational.init(try std.math.mul(i128, value.numerator, factor), value.denominator);\\n}\\n\\nfn divInt(value: Rational, divisor: i128) !Rational {\\n    return Rational.init(value.numerator, try std.math.mul(i128, value.denominator, divisor));\\n}\\n\\nfn absI128(value: i128) i128 {\\n    return if (value < 0) -value else value;\\n}\\n\\nfn gcdI128(left: i128, right: i128) i128 {\\n    var a = left;\\n    var b = right;\\n    while (b != 0) {\\n        const next = @mod(a, b);\\n        a = b;\\n        b = next;\\n    }\\n    return a;\\n}\\n\\n","n":0.023}}},{"i":37,"$":{"1":{"v":"Correlator Performance Benchmarks","n":0.577},"2":{"v":"This benchmark times the streamed Wick correlator on representative free-boson,\\nfree-fermion, ghost, and exponential workloads.  It reports medians, not best runs.  The\\nevent sink counts the fully expanded stream; the branch sink counts the compact\\nbranch path and its stack state size.\\nThe event lines describe the expanded primitive event stream: a Wick term\\nboundary, scalar/coordinate/tensor/action factors, residual operator references,\\nand the zero-mode base-case factors.  The branch lines describe one compact\\naccumulated branch object per Wick/base-case branch, while zero modes remain\\nseparate events.  fallback_terms is nonzero only when a case cannot use the\\ncompact branch path.\\nThe runner enforces a 5% median regression gate for every event and branch\\ncase against the recorded baselines below.  Cases with very small baselines run\\nmultiple correlators per sample and report the per-call median, keeping the\\nspeed gate strict while reducing timer noise.  Exponential and mixed cases also\\ncheck primitive-term, tensor-factor, residual, zero-mode, and base-case counts\\nso semantic drift fails the benchmark instead of only changing printed numbers.\\nconst std = @import(\\"std\\");\\nconst cft = @import(\\"cft-code\\");\\n\\nconst max_sample_count = 101;\\n\\nconst MedianGate = struct {\\n    baseline_ns: u128,\\n    limit_num: u128,\\n    limit_den: u128,\\n};\\n\\nconst ExpectedCounts = struct {\\n    primitive_terms: usize,\\n    tensors: usize,\\n    residuals: usize,\\n    zero_modes: usize,\\n    base_cases: usize,\\n};\\n\\nfn medianGate(comptime baseline_ns: u128) MedianGate {\\n    return .{ .baseline_ns = baseline_ns, .limit_num = 21, .limit_den = 20 };\\n}\\n\\nfn measurementBatchCount(baseline_ns: u128) usize {\\n    if (baseline_ns < 20_000) return 32;\\n    if (baseline_ns < 50_000) return 8;\\n    return 1;\\n}\\n\\nfn nowNs() u128 {\\n    var ts: std.os.linux.timespec = undefined;\\n    _ = std.os.linux.clock_gettime(.MONOTONIC, &ts);\\n    return @as(u128, @intCast(ts.sec)) * std.time.ns_per_s + @as(u128, @intCast(ts.nsec));\\n}\\n\\nconst EventSink = struct {\\n    wick_terms: usize = 0,\\n    scalars: usize = 0,\\n    coordinates: usize = 0,\\n    tensors: usize = 0,\\n    actions: usize = 0,\\n    residuals: usize = 0,\\n    zero_modes: usize = 0,\\n    base_cases: usize = 0,\\n\\n    pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n        self.wick_terms += 1;\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), _: anytype) !void {\\n        self.scalars += 1;\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), _: anytype) !void {\\n        self.coordinates += 1;\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), _: anytype) !void {\\n        self.tensors += 1;\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        self.actions += 1;\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), _: anytype) !void {\\n        self.residuals += 1;\\n    }\\n\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n    pub fn emitZeroModeFactor(self: *@This(), _: anytype) !void {\\n        self.zero_modes += 1;\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.base_cases += 1;\\n    }\\n};\\n\\nconst BranchSink = struct {\\n    branch_terms: usize = 0,\\n    fallback_terms: usize = 0,\\n    primitive_terms: usize = 0,\\n    scalars: usize = 0,\\n    coordinates: usize = 0,\\n    tensors: usize = 0,\\n    actions: usize = 0,\\n    residuals: usize = 0,\\n    zero_modes: usize = 0,\\n    base_cases: usize = 0,\\n    branch_state_bytes: usize = 0,\\n\\n    pub fn emitWickBranchTerm(self: *@This(), branch: anytype) !void {\\n        self.branch_terms += 1;\\n        self.primitive_terms += branch.primitiveTermCount();\\n        self.scalars += branch.scalarFactorCount();\\n        self.coordinates += branch.coordinateFactorCount();\\n        self.tensors += branch.tensorFactorCount();\\n        self.actions += branch.actionFactorCount();\\n        self.residuals += branch.residualFactorCount();\\n        const bytes = branchStateBytes(branch);\\n        if (bytes > self.branch_state_bytes) self.branch_state_bytes = bytes;\\n    }\\n\\n    pub fn emitWickTermStart(self: *@This(), _: anytype) !void {\\n        self.fallback_terms += 1;\\n        self.primitive_terms += 1;\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), _: anytype) !void {\\n        self.scalars += 1;\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), _: anytype) !void {\\n        self.coordinates += 1;\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), _: anytype) !void {\\n        self.tensors += 1;\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        self.actions += 1;\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), _: anytype) !void {\\n        self.residuals += 1;\\n    }\\n\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n\\n    pub fn emitZeroModeFactor(self: *@This(), _: anytype) !void {\\n        self.zero_modes += 1;\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.base_cases += 1;\\n    }\\n};\\n\\nfn branchStateBytes(branch: anytype) usize {\\n    return switch (@typeInfo(@TypeOf(branch))) {\\n        .pointer => |ptr| @sizeOf(ptr.child),\\n        else => @sizeOf(@TypeOf(branch)),\\n    };\\n}\\n\\nfn median(samples: []u128) u128 {\\n    std.mem.sort(u128, samples, {}, struct {\\n        fn lessThan(_: void, left: u128, right: u128) bool {\\n            return left < right;\\n        }\\n    }.lessThan);\\n    return samples[samples.len / 2];\\n}\\n\\nfn requireSampleCount(comptime sample_count: usize) void {\\n    if (sample_count == 0 or sample_count > max_sample_count or (sample_count & 1) == 0) {\\n        @compileError(\\"benchmark sample counts must be odd and <= max_sample_count\\");\\n    }\\n}\\n\\nfn checkEventCounts(comptime name: []const u8, result: EventSink, counts: ExpectedCounts, batch_count: usize) !void {\\n    if (result.wick_terms != counts.primitive_terms * batch_count or\\n        result.tensors != counts.tensors * batch_count or\\n        result.residuals != counts.residuals * batch_count or\\n        result.zero_modes != counts.zero_modes * batch_count or\\n        result.base_cases != counts.base_cases * batch_count)\\n    {\\n        std.debug.print(\\n            \\"event count regression case={s} primitive={} tensors={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n            .{ name, result.wick_terms, result.tensors, result.residuals, result.zero_modes, result.base_cases },\\n        );\\n        return error.BenchmarkCountRegression;\\n    }\\n}\\n\\nfn checkBranchCounts(comptime name: []const u8, result: BranchSink, counts: ExpectedCounts, batch_count: usize) !void {\\n    if (result.primitive_terms != counts.primitive_terms * batch_count or\\n        result.tensors != counts.tensors * batch_count or\\n        result.residuals != counts.residuals * batch_count or\\n        result.zero_modes != counts.zero_modes * batch_count or\\n        result.base_cases != counts.base_cases * batch_count)\\n    {\\n        std.debug.print(\\n            \\"branch count regression case={s} primitive={} tensors={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n            .{ name, result.primitive_terms, result.tensors, result.residuals, result.zero_modes, result.base_cases },\\n        );\\n        return error.BenchmarkCountRegression;\\n    }\\n}\\n\\nfn checkMedianGate(comptime form: []const u8, comptime name: []const u8, median_ns: u128, gate: MedianGate) !void {\\n    if (median_ns * gate.limit_den > gate.baseline_ns * gate.limit_num) {\\n        std.debug.print(\\n            \\"{s} speed regression case={s} median_ns={} baseline_ns={} limit={}/{}\\\\n\\",\\n            .{ form, name, median_ns, gate.baseline_ns, gate.limit_num, gate.limit_den },\\n        );\\n        return error.BenchmarkSpeedRegression;\\n    }\\n}\\n\\nfn benchEvent(comptime name: []const u8, comptime sample_count: usize, theory: anytype, config: anytype, ops: anytype, gate: MedianGate, expected: ExpectedCounts) !void {\\n    comptime requireSampleCount(sample_count);\\n\\n    const batch_count = measurementBatchCount(gate.baseline_ns);\\n    var samples: [max_sample_count]u128 = undefined;\\n    var result = EventSink{};\\n    for (samples[0..sample_count]) |*sample| {\\n        var sink = EventSink{};\\n        const start = nowNs();\\n        for (0..batch_count) |_| {\\n            try theory.correlator(config, ops, &sink);\\n        }\\n        sample.* = (nowNs() - start) / batch_count;\\n        result = sink;\\n    }\\n\\n    const median_ns = median(samples[0..sample_count]);\\n    try checkMedianGate(\\"event\\", name, median_ns, gate);\\n    try checkEventCounts(name, result, expected, batch_count);\\n    std.debug.print(\\n        \\"event case={s} samples={} batch={} median_ns={} wick_terms={} scalars={} coordinates={} tensors={} actions={} residuals={} zero_modes={} base_cases={}\\\\n\\",\\n        .{ name, sample_count, batch_count, median_ns, result.wick_terms, result.scalars, result.coordinates, result.tensors, result.actions, result.residuals, result.zero_modes, result.base_cases },\\n    );\\n}\\n\\nfn benchBranch(comptime name: []const u8, comptime sample_count: usize, theory: anytype, config: anytype, ops: anytype, gate: MedianGate, expected: ExpectedCounts) !void {\\n    comptime requireSampleCount(sample_count);\\n\\n    const batch_count = measurementBatchCount(gate.baseline_ns);\\n    var samples: [max_sample_count]u128 = undefined;\\n    var result = BranchSink{};\\n    for (samples[0..sample_count]) |*sample| {\\n        var sink = BranchSink{};\\n        const start = nowNs();\\n        for (0..batch_count) |_| {\\n            try theory.correlator(config, ops, &sink);\\n        }\\n        sample.* = (nowNs() - start) / batch_count;\\n        result = sink;\\n    }\\n\\n    const median_ns = median(samples[0..sample_count]);\\n    try checkMedianGate(\\"branch\\", name, median_ns, gate);\\n    try checkBranchCounts(name, result, expected, batch_count);\\n    std.debug.print(\\n        \\"branch case={s} samples={} batch={} median_ns={} branch_terms={} fallback_terms={} primitive_terms={} scalars={} coordinates={} tensors={} actions={} residuals={} zero_modes={} base_cases={} branch_state_bytes={}\\\\n\\",\\n        .{ name, sample_count, batch_count, median_ns, result.branch_terms, result.fallback_terms, result.primitive_terms, result.scalars, result.coordinates, result.tensors, result.actions, result.residuals, result.zero_modes, result.base_cases, result.branch_state_bytes },\\n    );\\n}\\n\\nfn benchFreeBosonDx(comptime n: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [n]@TypeOf(try X.op.dX(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    inline for (0..n) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"z{}\\", .{i}));\\n        ops_buf[i] = try X.op.dX(&local, mu, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"dx{}\\", .{n});\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchBcSphere(comptime b_count: usize, comptime c_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const Ghost = cft.Bc.sphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var type_local = try Ghost.local(allocator);\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [b_count + c_count]@TypeOf(try Ghost.op.b(&type_local, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try Ghost.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..b_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"b{}\\", .{i}));\\n        ops_buf[i] = try Ghost.op.b(&local, 0, z);\\n    }\\n    inline for (0..c_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"c{}\\", .{i}));\\n        ops_buf[b_count + i] = try Ghost.op.c(&local, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"bc_{}_{}\\", .{ b_count, c_count });\\n\\n    try benchEvent(name, sample_count, Ghost, &Ghost.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, Ghost, &Ghost.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeFermionPsi(comptime n: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const Psi = cft.FreeFermion.sphere(.{ .dimension = 10, .include_antiholomorphic_copy = false });\\n\\n    var type_local = try Psi.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [n]@TypeOf(try Psi.op.psi(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try Psi.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..n) |i| {\\n        const mu = try local.index(std.fmt.comptimePrint(\\"mu{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"psi{}\\", .{i}));\\n        ops_buf[i] = try Psi.op.psi(&local, mu, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"psi{}\\", .{n});\\n\\n    if (n >= 2) {\\n        const TensorGuard = struct {\\n            metric_count: usize = 0,\\n\\n            pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n\\n            pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n                switch (factor) {\\n                    .metric => |metric| {\\n                        switch (metric.left) {\\n                            .symbol => {},\\n                            else => return error.UnexpectedPsiTensorLabel,\\n                        }\\n                        switch (metric.right) {\\n                            .symbol => {},\\n                            else => return error.UnexpectedPsiTensorLabel,\\n                        }\\n                        self.metric_count += 1;\\n                    },\\n                    else => return error.UnexpectedPsiTensorFactor,\\n                }\\n            }\\n\\n            pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n            pub fn emitWickTermEnd(_: *@This()) !void {}\\n            pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n            pub fn emitZeroModeBaseEnd(_: *@This()) !void {}\\n        };\\n\\n        var guard = TensorGuard{};\\n        try Psi.correlator(&Psi.config.sphere, ops, &guard);\\n        if (guard.metric_count != expected.tensors) return error.BenchmarkCountRegression;\\n    }\\n\\n    try benchEvent(name, sample_count, Psi, &Psi.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, Psi, &Psi.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeBosonExp(comptime exp_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_k = try type_local.momentum(\\"probe_k\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    const probe_zbar = try type_local.coord(\\"probe_zbar\\");\\n    var ops_buf: [exp_count]@TypeOf(try X.op.expX(&type_local, probe_k, probe_z, probe_zbar)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..exp_count) |i| {\\n        const k = try local.momentum(std.fmt.comptimePrint(\\"k{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"e{}\\", .{i}));\\n        const zbar = try local.coord(std.fmt.comptimePrint(\\"eb{}\\", .{i}));\\n        ops_buf[i] = try X.op.expX(&local, k, z, zbar);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"exp{}\\", .{exp_count});\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchFreeBosonDxExp(comptime dx_count: usize, comptime exp_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const X = cft.FreeBoson.make(.{ .dimension = 10 });\\n\\n    var type_local = try X.local(allocator);\\n    const probe_mu = try type_local.index(\\"probe_mu\\");\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [dx_count + exp_count]@TypeOf(try X.op.dX(&type_local, probe_mu, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try X.local(allocator);\\n    defer local.deinit();\\n\\n    const mu = try local.index(\\"mu\\");\\n    inline for (0..dx_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"x{}\\", .{i}));\\n        ops_buf[i] = try X.op.dX(&local, mu, 0, z);\\n    }\\n    inline for (0..exp_count) |i| {\\n        const k = try local.momentum(std.fmt.comptimePrint(\\"k{}\\", .{i}));\\n        const z = try local.coord(std.fmt.comptimePrint(\\"e{}\\", .{i}));\\n        const zbar = try local.coord(std.fmt.comptimePrint(\\"eb{}\\", .{i}));\\n        ops_buf[dx_count + i] = try X.op.expX(&local, k, z, zbar);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"dx{}_exp{}\\", .{ dx_count, exp_count });\\n\\n    try benchEvent(name, sample_count, X, &X.config.sphere, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, X, &X.config.sphere, ops, branch_gate, expected);\\n}\\n\\nfn benchEtaXiTorus(comptime eta_count: usize, comptime xi_count: usize, comptime sample_count: usize, allocator: std.mem.Allocator, event_gate: MedianGate, branch_gate: MedianGate, expected: ExpectedCounts) !void {\\n    const EtaXi = cft.EtaXi.sphere(.{ .include_antiholomorphic_copy = false });\\n\\n    var type_local = try EtaXi.local(allocator);\\n    const probe_z = try type_local.coord(\\"probe_z\\");\\n    var ops_buf: [eta_count + xi_count]@TypeOf(try EtaXi.op.eta(&type_local, 0, probe_z)) = undefined;\\n    type_local.deinit();\\n\\n    var local = try EtaXi.local(allocator);\\n    defer local.deinit();\\n\\n    inline for (0..eta_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"eta{}\\", .{i}));\\n        ops_buf[i] = try EtaXi.op.eta(&local, 0, z);\\n    }\\n    inline for (0..xi_count) |i| {\\n        const z = try local.coord(std.fmt.comptimePrint(\\"xi{}\\", .{i}));\\n        ops_buf[eta_count + i] = try EtaXi.op.xi(&local, 0, z);\\n    }\\n    const ops = try local.ops(ops_buf);\\n    const name = std.fmt.comptimePrint(\\"eta_xi_torus_{}_{}\\", .{ eta_count, xi_count });\\n\\n    try benchEvent(name, sample_count, EtaXi, &EtaXi.config.torus, ops, event_gate, expected);\\n    try benchBranch(name, sample_count, EtaXi, &EtaXi.config.torus, ops, branch_gate, expected);\\n}\\n\\npub fn main() !void {\\n    const allocator = std.heap.page_allocator;\\n    try benchEtaXiTorus(3, 4, 101, allocator, medianGate(16000), medianGate(16000), .{ .primitive_terms = 72, .tensors = 72, .residuals = 0, .zero_modes = 24, .base_cases = 24 });\\n    try benchEtaXiTorus(4, 5, 101, allocator, medianGate(50000), medianGate(50000), .{ .primitive_terms = 480, .tensors = 480, .residuals = 0, .zero_modes = 120, .base_cases = 120 });\\n\\n    try benchFreeBosonDx(8, 101, allocator, medianGate(42779), medianGate(37253), .{ .primitive_terms = 420, .tensors = 420, .residuals = 0, .zero_modes = 105, .base_cases = 105 });\\n    try benchFreeBosonDx(10, 101, allocator, medianGate(231673), medianGate(185435), .{ .primitive_terms = 4725, .tensors = 4725, .residuals = 0, .zero_modes = 945, .base_cases = 945 });\\n    try benchFreeBosonDx(12, 31, allocator, medianGate(2271202), medianGate(2131701), .{ .primitive_terms = 62370, .tensors = 62370, .residuals = 0, .zero_modes = 10395, .base_cases = 10395 });\\n    try benchFreeBosonDx(14, 11, allocator, medianGate(35454428), medianGate(31226026), .{ .primitive_terms = 945945, .tensors = 945945, .residuals = 0, .zero_modes = 135135, .base_cases = 135135 });\\n\\n    try benchBcSphere(3, 6, 101, allocator, medianGate(39095), medianGate(38634), .{ .primitive_terms = 360, .tensors = 360, .residuals = 0, .zero_modes = 120, .base_cases = 120 });\\n    try benchBcSphere(4, 7, 101, allocator, medianGate(234677), medianGate(226326), .{ .primitive_terms = 3360, .tensors = 3360, .residuals = 0, .zero_modes = 840, .base_cases = 840 });\\n    try benchBcSphere(5, 8, 31, allocator, medianGate(2225167), medianGate(2106137), .{ .primitive_terms = 33600, .tensors = 33600, .residuals = 0, .zero_modes = 6720, .base_cases = 6720 });\\n    try benchBcSphere(6, 9, 11, allocator, medianGate(22262770), medianGate(19629812), .{ .primitive_terms = 362880, .tensors = 362880, .residuals = 0, .zero_modes = 60480, .base_cases = 60480 });\\n\\n    try benchFreeBosonExp(2, 101, allocator, medianGate(9277), medianGate(8626), .{ .primitive_terms = 1, .tensors = 1, .residuals = 2, .zero_modes = 1, .base_cases = 1 });\\n    try benchFreeBosonExp(4, 101, allocator, medianGate(10801), medianGate(10893), .{ .primitive_terms = 6, .tensors = 6, .residuals = 12, .zero_modes = 1, .base_cases = 1 });\\n    try benchFreeBosonExp(6, 31, allocator, medianGate(17741), medianGate(18497), .{ .primitive_terms = 15, .tensors = 15, .residuals = 30, .zero_modes = 1, .base_cases = 1 });\\n\\n    try benchFreeBosonDxExp(2, 2, 101, allocator, medianGate(11317), medianGate(11790), .{ .primitive_terms = 14, .tensors = 14, .residuals = 18, .zero_modes = 5, .base_cases = 5 });\\n    try benchFreeBosonDxExp(4, 2, 31, allocator, medianGate(30767), medianGate(32523), .{ .primitive_terms = 185, .tensors = 185, .residuals = 198, .zero_modes = 43, .base_cases = 43 });\\n    try benchFreeBosonDxExp(4, 4, 11, allocator, medianGate(226336), medianGate(218270), .{ .primitive_terms = 3448, .tensors = 3448, .residuals = 5476, .zero_modes = 355, .base_cases = 355 });\\n\\n    try benchFreeFermionPsi(8, 101, allocator, medianGate(30000), medianGate(30000), .{ .primitive_terms = 420, .tensors = 420, .residuals = 0, .zero_modes = 0, .base_cases = 105 });\\n    try benchFreeFermionPsi(10, 101, allocator, medianGate(150000), medianGate(150000), .{ .primitive_terms = 4725, .tensors = 4725, .residuals = 0, .zero_modes = 0, .base_cases = 945 });\\n    try benchFreeFermionPsi(12, 31, allocator, medianGate(1600000), medianGate(1400000), .{ .primitive_terms = 62370, .tensors = 62370, .residuals = 0, .zero_modes = 0, .base_cases = 10395 });\\n    try benchFreeFermionPsi(14, 11, allocator, medianGate(21000000), medianGate(20000000), .{ .primitive_terms = 945945, .tensors = 945945, .residuals = 0, .zero_modes = 0, .base_cases = 135135 });\\n}\\n","n":0.021}}},{"i":38,"$":{"1":{"v":"Free Fermion Preset","n":0.577},"2":{"v":"This node defines the NS-sector free worldsheet fermion on the sphere.  It uses\\nthe shared preset lowering path from Preset Shared Runtime and deliberately\\ndoes not define a torus surface.  Loop-level free-fermion data must later be\\ncombined with the \\\\(\\\\phi\\\\) system rather than frozen here in isolation.\\nThe stringbook convention is\\n\\\\[\\n  \\\\psi^\\\\mu(z)\\\\psi^\\\\nu(w)\\\\sim {\\\\eta^{\\\\mu\\\\nu}\\\\over z-w}.\\n\\\\]\\nDerivative labels mean \\\\(\\\\partial^n\\\\psi^\\\\mu\\\\).  The correlator engine receives\\nonly the lowered fermionic kind ids and pole rule; all theory names remain in\\nthis preset.\\nconst std = @import(\\"std\\");\\nconst shared = @import(\\"shared.zig\\");\\nconst declare = shared.declare;\\n\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst Handle = shared.Handle;\\nconst Builder = *shared.Local;\\nconst Operator = *shared.LocalOperator;\\nconst Spec = declare.Spec;\\nconst wick = declare.wick;\\nThe preset owns the two chiral operator families.  A compile-time flag can keep\\nonly the holomorphic NS sector when a chiral computation does not need the\\nantiholomorphic copy.\\nconst namespace = \\"free_fermion\\";\\n\\nconst Family = enum {\\n    psi,\\n    psit,\\n};\\n\\nfn kind(comptime family: Family) operators.OperatorKindId {\\n    return declare.familyKind(namespace, family);\\n}\\nSphere PresetThe public surface mirrors the other presets: users get an operator namespace,\\nan opaque sphere config, the bounded text adapter, a local builder, and one\\nstreaming correlator entry point.\\nconst FreeFermionSphereConfig = struct {\\n    dimension: u16,\\n    include_antiholomorphic_copy: bool = true,\\n};\\n\\n/// freeFermionSphere builds the generated preset type for sphere NS free fermions.\\npub fn freeFermionSphere(comptime cfg: FreeFermionSphereConfig) type {\\n    if (cfg.dimension == 0) @compileError(\\"free-fermion target dimension must be nonzero\\");\\n    return struct {\\n        /// op exposes NS free-fermion local operator builders.\\n        pub const op = FreeFermionSphereOp(cfg.include_antiholomorphic_copy);\\n        /// config exposes named correlator configs for this preset.\\n        pub const config = FreeFermionCorrelatorConfig(cfg);\\n        /// text exposes bounded result-inspection sinks.\\n        pub const text = shared.text;\\n        /// local constructs a label-preserving local-operator builder.\\n        pub fn local(allocator: std.mem.Allocator) !Builder {\\n            return shared.Local.init(allocator);\\n        }\\n\\n        /// correlator streams rule matches for NS free-fermion insertions.\\n        pub fn correlator(config_ptr: anytype, ops: anytype, sink: anytype) !void {\\n            return shared.streamCorrelator(config_ptr, ops, sink);\\n        }\\n    };\\n}\\nThe builders pack one target-space vector index and one coordinate.  They do\\nnot expose the compact operator kind ids used by the engine.\\nfn FreeFermionSphereOp(comptime include_antiholomorphic_copy: bool) type {\\n    const Holomorphic = struct {\\n        /// psi builds a holomorphic NS free-fermion insertion.\\n        pub fn psi(local: anytype, mu: Handle.Index, n: u8, z: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(fermionOperator(.psi)).single(local, z, n, .{mu});\\n        }\\n    };\\n\\n    if (!include_antiholomorphic_copy) return Holomorphic;\\n\\n    return struct {\\n        /// psi builds a holomorphic NS free-fermion insertion.\\n        pub const psi = Holomorphic.psi;\\n\\n        /// psit builds an antiholomorphic NS free-fermion insertion.\\n        pub fn psit(local: anytype, mu: Handle.Index, n: u8, zbar: Handle.Coord) !Operator {\\n            return declare.operatorBuilder(fermionOperator(.psit)).single(local, zbar, n, .{mu});\\n        }\\n    };\\n}\\nRulesThe sphere rule is a metric numerator times a simple rational pole.  Fermionic\\nstatistics are lowered as data so the generic branch walker supplies the Wick\\npermutation sign for higher-point correlators.\\nfn holomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn antiholomorphicDifference() wick.CoordinateDifference {\\n    return wick.difference(wick.coord(.left, .position), wick.coord(.right, .position));\\n}\\n\\nfn psiPsi() wick.Expr {\\n    return wick.differentiatedPole(.one, .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, holomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nfn psitPsit() wick.Expr {\\n    return wick.differentiatedPole(.one, .{ .metric = .{\\n        .left = wick.label(.left, 0),\\n        .right = wick.label(.right, 0),\\n    } }, antiholomorphicDifference(), -1, .{ .include_left = true, .include_right = true });\\n}\\n\\nconst sphere_holomorphic_operator_spec = [_]Spec.Operator{\\n    .{ .name = \\"psi\\", .kind = kind(.psi), .support = .holomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .fermionic },\\n};\\n\\nconst sphere_full_operator_spec = sphere_holomorphic_operator_spec ++ [_]Spec.Operator{\\n    .{ .name = \\"psit\\", .kind = kind(.psit), .support = .antiholomorphic, .insertion = .single, .labels = &.{.index}, .statistics = .fermionic },\\n};\\n\\nconst sphere_holomorphic_wick_spec = [_]Spec.WickRule{\\n    .{ .left = kind(.psi), .right = kind(.psi), .expr = psiPsi() },\\n};\\n\\nconst sphere_full_wick_spec = sphere_holomorphic_wick_spec ++ [_]Spec.WickRule{\\n    .{ .left = kind(.psit), .right = kind(.psit), .expr = psitPsit() },\\n};\\n\\nconst sphere_holomorphic_wick_storage = Spec.wickRules(&sphere_holomorphic_wick_spec, &sphere_holomorphic_operator_spec);\\nconst sphere_full_wick_storage = Spec.wickRules(&sphere_full_wick_spec, &sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_fermion_storage = Spec.fermionKinds(&sphere_holomorphic_operator_spec);\\nconst sphere_full_fermion_storage = Spec.fermionKinds(&sphere_full_operator_spec);\\n\\nconst sphere_holomorphic_zero_spec = [_]Spec.ZeroModeRule{};\\nconst sphere_full_zero_spec = [_]Spec.ZeroModeRule{};\\n\\nfn sphereSpec(comptime include_antiholomorphic_copy: bool) Spec.Theory {\\n    return .{\\n        .operators = if (include_antiholomorphic_copy) &sphere_full_operator_spec else &sphere_holomorphic_operator_spec,\\n        .wick_rules = if (include_antiholomorphic_copy) &sphere_full_wick_spec else &sphere_holomorphic_wick_spec,\\n        .zero_modes = if (include_antiholomorphic_copy) &sphere_full_zero_spec else &sphere_holomorphic_zero_spec,\\n    };\\n}\\n\\nfn fermionOperator(comptime family: Family) Spec.Operator {\\n    const kind_id = kind(family);\\n    inline for (sphere_full_operator_spec) |operator| {\\n        if (operator.kind == kind_id) return operator;\\n    }\\n    @compileError(\\"missing free-fermion operator spec\\");\\n}\\n\\nfn assertSphereSpec(comptime include_antiholomorphic_copy: bool, comptime wick_count: usize, comptime zero_count: usize, comptime fermion_count: usize) void {\\n    const spec = sphereSpec(include_antiholomorphic_copy);\\n    if (spec.wick_rules.len != wick_count) @compileError(\\"free-fermion sphere spec Wick count does not match lowered rules\\");\\n    if (spec.zero_modes.len != zero_count) @compileError(\\"free-fermion sphere spec zero-mode count does not match lowered rules\\");\\n    if (spec.operators.len != fermion_count) @compileError(\\"free-fermion sphere spec fermion coverage does not match lowered rules\\");\\n}\\n\\nfn FreeFermionRules(comptime include_antiholomorphic_copy: bool) type {\\n    const selected_wick = if (include_antiholomorphic_copy) sphere_full_wick_storage else sphere_holomorphic_wick_storage;\\n    const selected_fermions = if (include_antiholomorphic_copy) sphere_full_fermion_storage else sphere_holomorphic_fermion_storage;\\n    return struct {\\n        const sphere_wick: []const wick.Rule = &selected_wick;\\n        const sphere_fermion_kinds: []const operators.OperatorKindId = &selected_fermions;\\n    };\\n}\\n\\nfn FreeFermionCorrelatorConfig(comptime cfg: FreeFermionSphereConfig) type {\\n    const rules = FreeFermionRules(cfg.include_antiholomorphic_copy);\\n    return struct {\\n        comptime {\\n            assertSphereSpec(cfg.include_antiholomorphic_copy, rules.sphere_wick.len, 0, rules.sphere_fermion_kinds.len);\\n        }\\n\\n        /// sphere selects NS free-fermion sphere Wick rules.\\n        pub const sphere = declare.correlatorConfig(.{\\n            .wick_rules = rules.sphere_wick,\\n            .zero_modes = &.{},\\n            .fermion_kinds = rules.sphere_fermion_kinds,\\n        }){};\\n    };\\n}\\n","n":0.036}}},{"i":39,"$":{"1":{"v":"Lisp Generated Expression Benchmark","n":0.5},"2":{"v":"This benchmark times the Lisp expression folder over compact generated ABI\\nterm/factor records.  The comparison baseline is the generated C ABI event\\nstream, not the count-only path, because expanded expression output must still\\nread every emitted factor.  Build the generated shared library with\\n-Doptimize=ReleaseFast before timing.\\n(ql:quickload :cffi :silent t)\\n(require :asdf)\\n(push #p\\"lisp/\\" asdf:*central-registry*)\\n(asdf:load-system \\"string-code-cft\\")\\n\\n(defparameter *generated-library* \\"zig-out/lib/libstring_code_cft_generated.so\\")\\n(defparameter *sample-count* 11)\\n\\n(defstruct bench-case name maker fill abi-run-ns)\\n\\n(defun now-ns ()\\n  (multiple-value-bind (sec usec) (sb-ext:get-time-of-day)\\n    (+ (* sec 1000000000) (* usec 1000))))\\n\\n(defun median (values)\\n  (let ((copy (sort (copy-seq values) #\'<)))\\n    (aref copy (floor (length copy) 2))))\\n\\n(defun sample-ns (thunk)\\n  (let ((samples (make-array *sample-count*)))\\n    (dotimes (index *sample-count*)\\n      (sb-ext:gc :full nil)\\n      (let ((start (now-ns)))\\n        (funcall thunk)\\n        (setf (aref samples index) (- (now-ns) start))))\\n    (median samples)))\\n\\n(defun expression-term-count (expr)\\n  (if (and (consp expr) (eq (first expr) \'+))\\n      (length (rest expr))\\n      1))\\n\\n(defun fill-psi (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:psi context\\n                                    (format nil \\"mu~D\\" index)\\n                                    (format nil \\"z~D\\" index)))))\\n\\n(defun fill-dx (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:dX context\\n                                   (format nil \\"mu~D\\" index)\\n                                   (format nil \\"z~D\\" index)))))\\n\\n(defun fill-exp (count)\\n  (lambda (context)\\n    (dotimes (index count)\\n      (string-code.cft.fixtures:expX context\\n                                     (format nil \\"k~D\\" index)\\n                                     (format nil \\"z~D\\" index)\\n                                     (format nil \\"zb~D\\" index)))))\\n\\n(defun run-case (case)\\n  (let ((context (funcall (bench-case-maker case) :library *generated-library*)))\\n    (unwind-protect\\n         (progn\\n           (funcall (bench-case-fill case) context)\\n           (let* ((frozen (string-code.cft.descriptor:freeze-runtime-operators context))\\n                  (count (string-code.cft.descriptor:count-correlator context frozen))\\n                  (expr-ns (sample-ns\\n                            (lambda ()\\n                              (expression-term-count\\n                               (string-code.cft.descriptor:correlator-expression context frozen)))))\\n                  (ratio (/ (float expr-ns) (bench-case-abi-run-ns case))))\\n             (format t \\"~A count=~D expr_ns=~D abi_run_ns=~D ratio=~,2F~%\\"\\n                     (bench-case-name case) count expr-ns\\n                     (bench-case-abi-run-ns case) ratio)))\\n      (string-code.cft.descriptor:destroy-runtime-context context))))\\n\\n(defparameter *cases*\\n  (list\\n   (make-bench-case :name \\"psi8\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-fermion-runtime\\n                    :fill (fill-psi 8)\\n                    :abi-run-ns 136715)\\n   (make-bench-case :name \\"psi10\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-fermion-runtime\\n                    :fill (fill-psi 10)\\n                    :abi-run-ns 1056899)\\n   (make-bench-case :name \\"dx8\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-boson-runtime\\n                    :fill (fill-dx 8)\\n                    :abi-run-ns 125244)\\n   (make-bench-case :name \\"exp4\\"\\n                    :maker #\'string-code.cft.fixtures:make-free-boson-runtime\\n                    :fill (fill-exp 4)\\n                    :abi-run-ns 8268)))\\n\\n(dolist (case *cases*)\\n  (run-case case))\\n","n":0.06}}},{"i":40,"$":{"1":{"v":"Generated Descriptor Boundary","n":0.577},"2":{"v":"The descriptor boundary is the narrow data interface between generated theory\\nfiles and the current Wick traversal Wick.  Lisp emits only compact ids\\nand primitive factors.  This module validates those tables, lowers them at\\ncomptime, and streams small events back out without building a symbolic tree.\\nconst std = @import(\\"std\\");\\nconst kernel = @import(\\"../kernel.zig\\");\\nconst normal_ordering = @import(\\"../normal-ordering/normal-ordering.zig\\");\\nconst operators = @import(\\"../expressions/operators.zig\\");\\nconst shared = @import(\\"../presets/shared.zig\\");\\n\\nconst declare = shared.declare;\\nconst wick = declare.wick;\\nconst zero_mode = declare.zero_mode;\\nconst scalars = declare.scalars;\\n\\n/// descriptor_abi_version is the generated descriptor table ABI understood here.\\npub const descriptor_abi_version: u32 = 1;\\n\\n/// Id is one dense descriptor table index.\\npub const Id = u16;\\n\\n/// LabelRole classifies a generated field label.\\npub const LabelRole = enum {\\n    vector_index,\\n    momentum,\\n    profile,\\n    scalar_parameter,\\n    modular_parameter,\\n};\\n\\n/// SurfaceKind names the supported topology class.\\npub const SurfaceKind = enum {\\n    sphere,\\n    torus,\\n};\\n\\n/// CoordinateModel selects rational or elliptic coordinate kernels.\\npub const CoordinateModel = enum {\\n    rational,\\n    elliptic,\\n};\\n\\n/// Statistics records the exchange parity of one field family.\\npub const Statistics = enum {\\n    bosonic,\\n    fermionic,\\n};\\n\\n/// InsertionShape gives the coordinate slots carried by a field insertion.\\npub const InsertionShape = enum {\\n    single,\\n    pair,\\n};\\nFields and surfaces are lean table rows.  Names are symbol ids from the\\ngenerated symbol table, not strings used by the lowerer.\\n/// Parameter declares one generated scalar or modular parameter.\\npub const Parameter = struct {\\n    id: Id,\\n    symbol: Id,\\n    role: LabelRole,\\n};\\n\\n/// QuantumNumberKind classifies a conserved or representation-valued label.\\npub const QuantumNumberKind = enum {\\n    u1_charge,\\n    ade_irrep,\\n};\\n\\n/// QuantumNumber declares one basis-selection quantum number.\\npub const QuantumNumber = struct {\\n    id: Id,\\n    symbol: Id,\\n    kind: QuantumNumberKind,\\n    group_symbol: ?Id = null,\\n};\\n\\n/// QuantumNumberValue stores one compact field quantum-number value.\\npub const QuantumNumberValue = union(enum) {\\n    integer: i64,\\n    rational: Rational,\\n    symbol: Id,\\n};\\n\\n/// FieldQuantumNumber assigns one quantum-number value to one field family.\\npub const FieldQuantumNumber = struct {\\n    field: Id,\\n    quantum_number: Id,\\n    value: QuantumNumberValue,\\n};\\n\\n/// Surface declares one coordinate model for a generated rule set.\\npub const Surface = struct {\\n    id: Id,\\n    kind: SurfaceKind,\\n    coordinate_model: CoordinateModel,\\n    modular_parameter: ?Id = null,\\n};\\n\\n/// LabelSchema declares one field label slot.\\npub const LabelSchema = struct {\\n    id: Id,\\n    role: LabelRole,\\n    symbol: Id,\\n};\\n\\n/// Field declares one generated local field family.\\npub const Field = struct {\\n    id: Id,\\n    symbol: Id,\\n    insertion: InsertionShape,\\n    labels: []const LabelSchema = &.{},\\n    statistics: Statistics,\\n    zero_mode_consumable: bool = false,\\n    weight: ?Id = null,\\n    anti_weight: ?Id = null,\\n};\\nMetadata expressions are ids over a separate table.  They are preserved for\\nLisp-facing metadata and checked for dangling references.\\n/// Rational is one exact small rational value.\\npub const Rational = struct {\\n    numerator: i64,\\n    denominator: i64,\\n};\\n\\n/// MetadataExpr is a compact expression tree over ids, labels, and parameters.\\npub const MetadataExpr = union(enum) {\\n    rational: Rational,\\n    parameter: Id,\\n    field_label: struct { field: Id, label: Id },\\n    add: struct { left: Id, right: Id },\\n    mul: struct { left: Id, right: Id },\\n    bilinear: struct { left: Id, right: Id, form_symbol: Id },\\n};\\nWick factors name primitive concepts.  The lowerer maps these to the current\\nprivate runtime tables without making runtime table types public.\\n/// Side selects the left or right endpoint of a Wick rule.\\npub const Side = enum {\\n    left,\\n    right,\\n};\\n\\n/// CoordinateSlot selects one coordinate component from an insertion.\\npub const CoordinateSlot = enum {\\n    position,\\n    holomorphic,\\n    antiholomorphic,\\n};\\n\\n/// CoordinateRef names one coordinate slot on one endpoint.\\npub const CoordinateRef = struct {\\n    side: Side,\\n    slot: CoordinateSlot,\\n};\\n\\n/// LabelRef names one label slot on one endpoint.\\npub const LabelRef = struct {\\n    side: Side,\\n    slot: Id,\\n};\\n\\n/// ScalarFactor describes one scalar multiplier in a Wick term.\\npub const ScalarFactor = union(enum) {\\n    one,\\n    rational: Rational,\\n    parameter: Id,\\n    neg_parameter_half: Id,\\n    neg_i_parameter_half: Id,\\n    parameter_half: Id,\\n};\\n\\n/// CoordinateFactor describes one coordinate multiplier in a Wick term.\\npub const CoordinateFactor = union(enum) {\\n    difference_power: struct { left: CoordinateRef, right: CoordinateRef, exponent: i16, derive_left: bool = false, derive_right: bool = false },\\n    named_kernel: struct { symbol: Id, left: CoordinateRef, right: CoordinateRef, derive_left: bool = false, derive_right: bool = false },\\n    green_exponential: struct { left: CoordinateRef, right: CoordinateRef },\\n};\\n\\n/// TensorFactor describes one tensor multiplier in a Wick term.\\npub const TensorFactor = union(enum) {\\n    none,\\n    metric: struct { left: LabelRef, right: LabelRef },\\n    momentum_index: struct { momentum: LabelRef, index: LabelRef },\\n    momentum_pair: struct { left: LabelRef, right: LabelRef },\\n};\\n\\n/// ActionFactor describes an operation applied to a residual factor.\\npub const ActionFactor = union(enum) {\\n    profile_derivative: struct { profile: LabelRef, index: LabelRef },\\n};\\n\\n/// WickTerm is one product in a primitive Wick-rule sum.\\npub const WickTerm = struct {\\n    scalars: []const ScalarFactor = &.{},\\n    coordinates: []const CoordinateFactor = &.{},\\n    tensors: []const TensorFactor = &.{},\\n    actions: []const ActionFactor = &.{},\\n    residuals: []const Side = &.{},\\n};\\n\\n/// WickRule declares primitive pair terms for two field families.\\npub const WickRule = struct {\\n    surface: Id,\\n    left: Id,\\n    right: Id,\\n    terms: []const WickTerm,\\n};\\nZero modes use generic generated data.  Reserved service slots are present so\\nlater endpoints can fail deliberately rather than reshaping the ABI.\\n/// ZeroModeKind selects one implemented residual base-case primitive.\\npub const ZeroModeKind = enum {\\n    constant_fermion,\\n    top_form_fermion,\\n    boson_momentum_conservation,\\n};\\n\\n/// ZeroModeRule declares one residual base-case primitive.\\npub const ZeroModeRule = struct {\\n    surface: Id,\\n    kind: ZeroModeKind,\\n    consumes: []const Id,\\n    normalization: ScalarFactor = .one,\\n    two_pi_power: u16 = 0,\\n};\\n\\n/// ReservedServices records descriptor slots not implemented in this slice.\\npub const ReservedServices = struct {\\n    ope_rules: usize = 0,\\n    operator_templates: usize = 0,\\n    relation_rules: usize = 0,\\n    basis_rules: usize = 0,\\n};\\n\\n/// Descriptor is the generated theory data consumed by the lowerer.\\npub const Descriptor = struct {\\n    abi_version: u32 = descriptor_abi_version,\\n    theory_symbol: Id,\\n    theory_hash: u32,\\n    symbols: []const []const u8,\\n    parameters: []const Parameter = &.{},\\n    quantum_numbers: []const QuantumNumber = &.{},\\n    field_quantum_numbers: []const FieldQuantumNumber = &.{},\\n    surfaces: []const Surface,\\n    fields: []const Field,\\n    metadata: []const MetadataExpr = &.{},\\n    wick_rules: []const WickRule,\\n    zero_modes: []const ZeroModeRule = &.{},\\n    result_symbols: []const Id = &.{},\\n    reserved: ReservedServices = .{},\\n};\\nValidation is explicit loops over small descriptor tables.  Runtime tests call\\nthe same predicates that the comptime lowerer uses.\\nfn denseUniqueIds(comptime T: type, items: []const T) !void {\\n    var seen: [256]bool = [_]bool{false} ** 256;\\n    for (items) |item| {\\n        if (item.id >= items.len) return error.NonDenseId;\\n        if (seen[item.id]) return error.DuplicateId;\\n        seen[item.id] = true;\\n    }\\n}\\n\\nfn hasSymbol(d: Descriptor, symbol: Id) bool {\\n    return symbol < d.symbols.len;\\n}\\n\\nfn hasParameter(d: Descriptor, id: Id) bool {\\n    return id < d.parameters.len and d.parameters[id].id == id;\\n}\\n\\nfn hasQuantumNumber(d: Descriptor, id: Id) bool {\\n    return id < d.quantum_numbers.len and d.quantum_numbers[id].id == id;\\n}\\n\\nfn hasSurface(d: Descriptor, id: Id) bool {\\n    return id < d.surfaces.len and d.surfaces[id].id == id;\\n}\\n\\nfn hasField(d: Descriptor, id: Id) bool {\\n    return id < d.fields.len and d.fields[id].id == id;\\n}\\n\\nfn fieldLabel(field: Field, id: Id) ?LabelSchema {\\n    for (field.labels) |label| {\\n        if (label.id == id) return label;\\n    }\\n    return null;\\n}\\n\\nfn validateLabelRef(d: Descriptor, field_id: Id, label_id: Id) !void {\\n    if (!hasField(d, field_id)) return error.DanglingField;\\n    if (fieldLabel(d.fields[field_id], label_id) == null) return error.DanglingLabel;\\n}\\n\\nfn validateMetadata(d: Descriptor, index: Id, expr: MetadataExpr) !void {\\n    switch (expr) {\\n        .rational => |value| if (value.denominator == 0) return error.InvalidRational,\\n        .parameter => |id| if (!hasParameter(d, id)) return error.DanglingParameter,\\n        .field_label => |ref| try validateLabelRef(d, ref.field, ref.label),\\n        .add => |pair| {\\n            if (pair.left >= index or pair.right >= index) return error.DanglingMetadata;\\n        },\\n        .mul => |pair| {\\n            if (pair.left >= index or pair.right >= index) return error.DanglingMetadata;\\n        },\\n        .bilinear => |item| {\\n            if (item.left >= index or item.right >= index) return error.DanglingMetadata;\\n            if (!hasSymbol(d, item.form_symbol)) return error.DanglingSymbol;\\n        },\\n    }\\n}\\nfn labelRole(d: Descriptor, ref: LabelRef, left_field: Id, right_field: Id) !LabelRole {\\n    const field_id = if (ref.side == .left) left_field else right_field;\\n    if (!hasField(d, field_id)) return error.DanglingField;\\n    const label = fieldLabel(d.fields[field_id], ref.slot) orelse return error.DanglingLabel;\\n    return label.role;\\n}\\n\\nfn validateTensor(d: Descriptor, tensor: TensorFactor, left_field: Id, right_field: Id) !void {\\n    switch (tensor) {\\n        .none => {},\\n        .metric => |item| {\\n            if (try labelRole(d, item.left, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.right, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n        .momentum_index => |item| {\\n            if (try labelRole(d, item.momentum, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.index, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n        .momentum_pair => |item| {\\n            if (try labelRole(d, item.left, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.right, left_field, right_field) != .momentum) return error.IncompatibleLabelRole;\\n        },\\n    }\\n}\\n\\nfn validateAction(d: Descriptor, action: ActionFactor, left_field: Id, right_field: Id) !void {\\n    switch (action) {\\n        .profile_derivative => |item| {\\n            if (try labelRole(d, item.profile, left_field, right_field) != .profile) return error.IncompatibleLabelRole;\\n            if (try labelRole(d, item.index, left_field, right_field) != .vector_index) return error.IncompatibleLabelRole;\\n        },\\n    }\\n}\\n/// validateDescriptor rejects malformed generated descriptor data.\\npub fn validateDescriptor(d: Descriptor) !void {\\n    if (d.abi_version != descriptor_abi_version) return error.UnsupportedDescriptorAbi;\\n    if (!hasSymbol(d, d.theory_symbol)) return error.DanglingSymbol;\\n    if (d.reserved.ope_rules != 0 or d.reserved.operator_templates != 0 or d.reserved.relation_rules != 0 or d.reserved.basis_rules != 0) {\\n        return error.UnsupportedReservedService;\\n    }\\n\\n    try denseUniqueIds(Parameter, d.parameters);\\n    try denseUniqueIds(QuantumNumber, d.quantum_numbers);\\n    try denseUniqueIds(Surface, d.surfaces);\\n    try denseUniqueIds(Field, d.fields);\\n\\n    for (d.parameters) |parameter| {\\n        if (!hasSymbol(d, parameter.symbol)) return error.DanglingSymbol;\\n    }\\n    for (d.quantum_numbers) |number| {\\n        if (!hasSymbol(d, number.symbol)) return error.DanglingSymbol;\\n        if (number.group_symbol) |symbol| if (!hasSymbol(d, symbol)) return error.DanglingSymbol;\\n    }\\n    for (d.field_quantum_numbers) |item| {\\n        if (!hasField(d, item.field)) return error.DanglingField;\\n        if (!hasQuantumNumber(d, item.quantum_number)) return error.DanglingQuantumNumber;\\n        const number = d.quantum_numbers[item.quantum_number];\\n        switch (item.value) {\\n            .integer => if (number.kind != .u1_charge) return error.IncompatibleQuantumNumberValue,\\n            .rational => |value| {\\n                if (number.kind != .u1_charge) return error.IncompatibleQuantumNumberValue;\\n                if (value.denominator == 0) return error.InvalidRational;\\n            },\\n            .symbol => |symbol| {\\n                if (number.kind != .ade_irrep) return error.IncompatibleQuantumNumberValue;\\n                if (!hasSymbol(d, symbol)) return error.DanglingSymbol;\\n            },\\n        }\\n    }\\n    for (d.surfaces) |surface| {\\n        if (surface.modular_parameter) |id| if (!hasParameter(d, id)) return error.DanglingParameter;\\n    }\\n    for (d.fields) |field| {\\n        if (!hasSymbol(d, field.symbol)) return error.DanglingSymbol;\\n        try denseUniqueIds(LabelSchema, field.labels);\\n        for (field.labels) |label| if (!hasSymbol(d, label.symbol)) return error.DanglingSymbol;\\n        if (field.weight) |id| if (id >= d.metadata.len) return error.DanglingMetadata;\\n        if (field.anti_weight) |id| if (id >= d.metadata.len) return error.DanglingMetadata;\\n    }\\n    for (d.metadata, 0..) |expr, index| try validateMetadata(d, @intCast(index), expr);\\n\\n    for (d.wick_rules) |rule| {\\n        if (!hasSurface(d, rule.surface)) return error.DanglingSurface;\\n        if (!hasField(d, rule.left) or !hasField(d, rule.right)) return error.DanglingField;\\n        if (rule.terms.len == 0) return error.EmptyWickRule;\\n        const left_arity = d.fields[rule.left].labels.len;\\n        const right_arity = d.fields[rule.right].labels.len;\\n        for (rule.terms) |term| {\\n            for (term.scalars) |scalar| switch (scalar) {\\n                .parameter, .neg_parameter_half, .neg_i_parameter_half, .parameter_half => |id| if (!hasParameter(d, id)) return error.DanglingParameter,\\n                else => {},\\n            };\\n            for (term.tensors) |tensor| try validateTensor(d, tensor, rule.left, rule.right);\\n            for (term.actions) |action| try validateAction(d, action, rule.left, rule.right);\\n            for (term.residuals) |_| {}\\n        }\\n        if (left_arity != d.fields[rule.left].labels.len or right_arity != d.fields[rule.right].labels.len) return error.InvalidFieldArity;\\n    }\\n\\n    for (d.zero_modes) |rule| {\\n        if (!hasSurface(d, rule.surface)) return error.DanglingSurface;\\n        if (rule.consumes.len == 0) return error.EmptyZeroMode;\\n        for (rule.consumes) |field_id| {\\n            if (!hasField(d, field_id)) return error.DanglingField;\\n            if (!d.fields[field_id].zero_mode_consumable) return error.IncompatibleZeroModeField;\\n        }\\n    }\\n}\\nLowering derives stable private kind and sector ids from descriptor ids.  No\\nfield or theory names affect generic control flow.\\nfn stableId(seed: u32, local: u32) u32 {\\n    var hash = seed ^ 2166136261;\\n    hash = (hash ^ local) *% 16777619;\\n    return if (hash == 0) 1 else hash;\\n}\\n\\nfn kindId(comptime d: Descriptor, field: Id) operators.OperatorKindId {\\n    return stableId(d.theory_hash, field + 1);\\n}\\n\\nfn sectorId(comptime d: Descriptor, surface: Id, zero_rule: usize) zero_mode.Sector {\\n    return stableId(d.theory_hash ^ 0x51f1_0000, (@as(u32, surface) << 16) | @as(u32, @intCast(zero_rule + 1)));\\n}\\n\\nfn lowerSupport(shape: InsertionShape) wick.Support {\\n    return switch (shape) {\\n        .single => .holomorphic,\\n        .pair => .bulk_pair,\\n    };\\n}\\n\\nfn lowerInsertion(shape: InsertionShape) declare.Spec.InsertionShape {\\n    return switch (shape) {\\n        .single => .single,\\n        .pair => .pair,\\n    };\\n}\\n\\nfn lowerLabelKind(role: LabelRole) declare.Spec.LabelKind {\\n    return switch (role) {\\n        .vector_index => .index,\\n        .momentum => .momentum,\\n        .profile => .profile,\\n        .scalar_parameter, .modular_parameter => @compileError(\\"parameter roles are not field-label runtime slots\\"),\\n    };\\n}\\n\\nfn lowerField(comptime d: Descriptor, comptime field: Field) declare.Spec.Operator {\\n    comptime var labels: [field.labels.len]declare.Spec.LabelKind = undefined;\\n    inline for (field.labels, 0..) |label, index| labels[index] = lowerLabelKind(label.role);\\n    return .{\\n        .name = d.symbols[field.symbol],\\n        .kind = kindId(d, field.id),\\n        .support = lowerSupport(field.insertion),\\n        .insertion = lowerInsertion(field.insertion),\\n        .labels = &labels,\\n        .statistics = if (field.statistics == .fermionic) .fermionic else .bosonic,\\n        .zero_mode_consumable = field.zero_mode_consumable,\\n    };\\n}\\nfn lowerSide(side: Side) wick.Side {\\n    return if (side == .left) .left else .right;\\n}\\n\\nfn lowerCoordSlot(slot: CoordinateSlot) wick.CoordinateSlot {\\n    return switch (slot) {\\n        .position => .position,\\n        .holomorphic => .holomorphic,\\n        .antiholomorphic => .antiholomorphic,\\n    };\\n}\\n\\nfn lowerCoordRef(ref: CoordinateRef) wick.CoordinateRef {\\n    return wick.coord(lowerSide(ref.side), lowerCoordSlot(ref.slot));\\n}\\n\\nfn lowerLabelRef(ref: LabelRef) wick.LabelRef {\\n    return wick.label(lowerSide(ref.side), @intCast(ref.slot));\\n}\\n\\nfn lowerDerivative(left: bool, right: bool) wick.DerivativeAction {\\n    return .{ .include_left = left, .include_right = right };\\n}\\n\\nfn parameterScalar(comptime d: Descriptor, id: Id) @TypeOf(scalars.one()) {\\n    return scalars.atomScalar(parameterAtom(d, id));\\n}\\n\\nfn parameterAtom(comptime d: Descriptor, id: Id) u32 {\\n    return stableId(d.theory_hash ^ 0xa170_0000, id + 1);\\n}\\n\\nfn lowerScalar(comptime d: Descriptor, factor: ScalarFactor) wick.ScalarFactor {\\n    return switch (factor) {\\n        .one => wick.scalar(scalars.one()),\\n        .rational => |value| wick.scalar(scalars.rational(value.numerator, value.denominator)),\\n        .parameter => |id| wick.scalar(parameterScalar(d, id)),\\n        .neg_parameter_half => |id| wick.scalar(scalars.div(scalars.neg(parameterScalar(d, id)), 2)),\\n        .neg_i_parameter_half => |id| wick.scalar(scalars.div(scalars.neg(scalars.i(parameterScalar(d, id))), 2)),\\n        .parameter_half => |id| wick.scalar(scalars.div(parameterScalar(d, id), 2)),\\n    };\\n}\\n\\nfn lowerScalarValue(comptime d: Descriptor, factor: ScalarFactor) @TypeOf(scalars.one()) {\\n    return switch (lowerScalar(d, factor)) {\\n        .value => |value| value,\\n        else => @compileError(\\"descriptor zero-mode normalization must be a value scalar\\"),\\n    };\\n}\\n\\nfn lowerCoordinate(comptime d: Descriptor, factor: CoordinateFactor) wick.CoordinateFactor {\\n    return switch (factor) {\\n        .difference_power => |item| wick.differentiatedPower(\\n            wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)),\\n            item.exponent,\\n            lowerDerivative(item.derive_left, item.derive_right),\\n        ),\\n        .named_kernel => |item| wick.differentiatedGreenKernel(\\n            d.symbols[item.symbol],\\n            wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)),\\n            lowerDerivative(item.derive_left, item.derive_right),\\n        ),\\n        .green_exponential => |item| .{ .green_exponential = wick.difference(lowerCoordRef(item.left), lowerCoordRef(item.right)) },\\n    };\\n}\\nfn lowerTensor(factor: TensorFactor) wick.TensorFactor {\\n    return switch (factor) {\\n        .none => .none,\\n        .metric => |item| .{ .metric = .{ .left = lowerLabelRef(item.left), .right = lowerLabelRef(item.right) } },\\n        .momentum_index => |item| .{ .momentum_index = .{ .momentum = lowerLabelRef(item.momentum), .index = lowerLabelRef(item.index) } },\\n        .momentum_pair => |item| .{ .momentum_pair = .{ .left = lowerLabelRef(item.left), .right = lowerLabelRef(item.right) } },\\n    };\\n}\\n\\nfn lowerAction(factor: ActionFactor) wick.ActionFactor {\\n    return switch (factor) {\\n        .profile_derivative => |item| wick.profileDerivative(lowerLabelRef(item.profile), lowerLabelRef(item.index)),\\n    };\\n}\\n\\nfn LoweredTerm(comptime d: Descriptor, comptime term: WickTerm) type {\\n    return struct {\\n        const scalar_storage = blk: {\\n            var storage: [term.scalars.len]wick.ScalarFactor = undefined;\\n            for (term.scalars, 0..) |factor, index| storage[index] = lowerScalar(d, factor);\\n            break :blk storage;\\n        };\\n        const coordinate_storage = blk: {\\n            var storage: [term.coordinates.len]wick.CoordinateFactor = undefined;\\n            for (term.coordinates, 0..) |factor, index| storage[index] = lowerCoordinate(d, factor);\\n            break :blk storage;\\n        };\\n        const tensor_storage = blk: {\\n            var storage: [term.tensors.len]wick.TensorFactor = undefined;\\n            for (term.tensors, 0..) |factor, index| storage[index] = lowerTensor(factor);\\n            break :blk storage;\\n        };\\n        const action_storage = blk: {\\n            var storage: [term.actions.len]wick.ActionFactor = undefined;\\n            for (term.actions, 0..) |factor, index| storage[index] = lowerAction(factor);\\n            break :blk storage;\\n        };\\n        const residual_storage = blk: {\\n            var storage: [term.residuals.len]wick.Side = undefined;\\n            for (term.residuals, 0..) |side, index| storage[index] = lowerSide(side);\\n            break :blk storage;\\n        };\\n        const value = wick.termWithResiduals(&scalar_storage, &coordinate_storage, &tensor_storage, &action_storage, &residual_storage);\\n    };\\n}\\n\\nfn LoweredWickRule(comptime d: Descriptor, comptime rule: WickRule) type {\\n    return struct {\\n        const terms = blk: {\\n            var storage: [rule.terms.len]wick.Term = undefined;\\n            for (rule.terms, 0..) |term, index| storage[index] = LoweredTerm(d, term).value;\\n            break :blk storage;\\n        };\\n        const value = wick.rule(\\n            wick.pattern(kindId(d, rule.left), lowerSupport(d.fields[rule.left].insertion)),\\n            wick.pattern(kindId(d, rule.right), lowerSupport(d.fields[rule.right].insertion)),\\n            wick.expr(&terms),\\n        );\\n    };\\n}\\n\\nfn lowerWickRule(comptime d: Descriptor, comptime rule: WickRule) wick.Rule {\\n    return LoweredWickRule(d, rule).value;\\n}\\nThe zero-mode lowering keeps the generated rule generic, then adapts to the\\ncurrent runtime procedures behind the descriptor boundary.\\nfn lowerConsumeKinds(comptime d: Descriptor, comptime consumes: []const Id) [consumes.len]operators.OperatorKindId {\\n    var storage: [consumes.len]operators.OperatorKindId = undefined;\\n    inline for (consumes, 0..) |field_id, index| storage[index] = kindId(d, field_id);\\n    return storage;\\n}\\n\\nfn LoweredZeroMode(comptime d: Descriptor, comptime rule: ZeroModeRule, comptime index: usize) type {\\n    return struct {\\n        const consume_storage = lowerConsumeKinds(d, rule.consumes);\\n        const sector = sectorId(d, rule.surface, index);\\n        const value = switch (rule.kind) {\\n            .constant_fermion => zero_mode.rule(sector, .{ .constant_fermion = .{\\n                .support = .sphere_holomorphic,\\n                .fermion_kind_ids = &consume_storage,\\n                .normalization = lowerScalarValue(d, rule.normalization),\\n            } }),\\n            .top_form_fermion => zero_mode.rule(sector, .{ .top_form_fermion = .{\\n                .support = .sphere_holomorphic,\\n                .field_kind_ids = &consume_storage,\\n                .normalization = lowerScalarValue(d, rule.normalization),\\n            } }),\\n            .boson_momentum_conservation => zero_mode.rule(sector, .{ .boson_momentum_conservation = .{\\n                .exp_kind_ids = &consume_storage,\\n                .profile_kind_ids = &.{},\\n                .normalization = .{\\n                    .scalar = lowerScalarValue(d, rule.normalization),\\n                    .two_pi_power = if (rule.two_pi_power == 0) .none else .{ .literal = rule.two_pi_power },\\n                },\\n            } }),\\n        };\\n    };\\n}\\n\\nfn lowerZeroMode(comptime d: Descriptor, comptime rule: ZeroModeRule, comptime index: usize) zero_mode.Rule {\\n    return LoweredZeroMode(d, rule, index).value;\\n}\\n\\nfn fieldSpecStorage(comptime d: Descriptor) [d.fields.len]declare.Spec.Operator {\\n    var storage: [d.fields.len]declare.Spec.Operator = undefined;\\n    inline for (d.fields, 0..) |field, index| storage[index] = lowerField(d, field);\\n    return storage;\\n}\\n\\nfn wickStorage(comptime d: Descriptor) [d.wick_rules.len]wick.Rule {\\n    var storage: [d.wick_rules.len]wick.Rule = undefined;\\n    inline for (d.wick_rules, 0..) |rule, index| storage[index] = lowerWickRule(d, rule);\\n    return storage;\\n}\\n\\nfn zeroStorage(comptime d: Descriptor) [d.zero_modes.len]zero_mode.Rule {\\n    var storage: [d.zero_modes.len]zero_mode.Rule = undefined;\\n    inline for (d.zero_modes, 0..) |rule, index| storage[index] = lowerZeroMode(d, rule, index);\\n    return storage;\\n}\\n\\nfn fermionStorage(comptime fields: []const Field, comptime d: Descriptor) [fermionCount(fields)]operators.OperatorKindId {\\n    var storage: [fermionCount(fields)]operators.OperatorKindId = undefined;\\n    comptime var index: usize = 0;\\n    inline for (fields) |field| {\\n        if (field.statistics == .fermionic) {\\n            storage[index] = kindId(d, field.id);\\n            index += 1;\\n        }\\n    }\\n    return storage;\\n}\\n\\nfn fermionCount(comptime fields: []const Field) usize {\\n    comptime var count: usize = 0;\\n    inline for (fields) |field| {\\n        if (field.statistics == .fermionic) count += 1;\\n    }\\n    return count;\\n}\\nRuntime contexts own only symbol names, label values, and packed local\\noperators.  Count mode uses a sink that never constructs expression objects.\\n/// ResultEventKind names one streamed Lisp-facing result event.\\npub const ResultEventKind = enum(u8) {\\n    sum_term_begin,\\n    sum_term_end,\\n    wick_term_begin,\\n    wick_term_end,\\n    scalar,\\n    coordinate,\\n    tensor,\\n    zero_mode,\\n    residual_operator,\\n};\\n\\n/// ResultEvent is one compact streamed result record.\\npub const ResultEvent = struct {\\n    kind: ResultEventKind,\\n    a: u32 = 0,\\n    b: u32 = 0,\\n    c: u32 = 0,\\n    d: i32 = 0,\\n    name: ?[]const u8 = null,\\n};\\n\\n/// StreamFn consumes one result event and user payload.\\npub const StreamFn = *const fn (*anyopaque, ResultEvent) anyerror!void;\\n\\nconst CountSink = struct {\\n    count: usize = 0,\\n\\n    pub fn emitWickBranchSign(_: *@This(), _: i8) !void {}\\n    pub fn emitWickTermStart(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickScalar(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickCoordinate(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickTensor(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickAction(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickResidualOperator(_: *@This(), _: anytype) !void {}\\n    pub fn emitWickTermEnd(_: *@This()) !void {}\\n    pub fn emitZeroModeFactor(_: *@This(), _: anytype) !void {}\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        self.count += 1;\\n    }\\n};\\nfn labelSymbol(value: kernel.Call.LabelValue) u32 {\\n    return switch (value) {\\n        .symbol => |symbol| symbol,\\n        .integer => |item| @intCast(item),\\n        .rational => 0,\\n        .tensor => |item| item,\\n    };\\n}\\n\\nfn scalarFlags(imaginary_power: u2, atom_power: i8) u32 {\\n    return @as(u32, imaginary_power) | (@as(u32, @as(u8, @bitCast(atom_power))) << 8);\\n}\\n\\nfn scalarValueEvent(rational: anytype, imaginary_power: u2, atom: ?u32, atom_power: i8) !ResultEvent {\\n    const numerator = std.math.cast(i32, rational.numerator) orelse return error.ScalarNumeratorOutOfRange;\\n    const denominator = std.math.cast(u32, rational.denominator) orelse return error.ScalarDenominatorOutOfRange;\\n    return .{\\n        .kind = .scalar,\\n        .a = atom orelse 0,\\n        .b = denominator,\\n        .c = scalarFlags(imaginary_power, atom_power),\\n        .d = numerator,\\n        .name = \\"scalar-monomial\\",\\n    };\\n}\\n\\nfn scalarEvent(value: anytype) !ResultEvent {\\n    return switch (value) {\\n        .one => .{ .kind = .scalar },\\n        .rational => |rational| scalarValueEvent(rational, 0, null, 0),\\n        .monomial => |monomial| scalarValueEvent(monomial.rational, monomial.imaginary_power, monomial.atom, monomial.atom_power),\\n    };\\n}\\n\\nconst EventSink = struct {\\n    payload: *anyopaque,\\n    stream: StreamFn,\\n\\n    fn emit(self: *@This(), event: ResultEvent) !void {\\n        try self.stream(self.payload, event);\\n    }\\n\\n    pub fn emitWickBranchSign(self: *@This(), sign: i8) !void {\\n        if (sign < 0) try self.emit(.{ .kind = .scalar, .d = -1 });\\n    }\\n\\n    pub fn emitWickTermStart(self: *@This(), event: anytype) !void {\\n        try self.emit(.{ .kind = .wick_term_begin, .a = @intCast(event.left_index), .b = @intCast(event.right_index), .c = @intCast(event.term_index) });\\n    }\\n\\n    pub fn emitWickScalar(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .value => |value| try self.emit(try scalarEvent(value)),\\n            .label_bilinear_phase => |item| try self.emit(.{ .kind = .scalar, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = item.form }),\\n            .cocycle => |item| try self.emit(.{ .kind = .scalar, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = item.table }),\\n            .spin_structure => |name| try self.emit(.{ .kind = .scalar, .name = name }),\\n        }\\n    }\\n\\n    pub fn emitWickCoordinate(self: *@This(), factor: anytype) !void {\\n        switch (factor.kernel) {\\n            .difference_power => |item| try self.emit(.{ .kind = .coordinate, .a = item.coordinate.left, .b = item.coordinate.right, .d = item.exponent }),\\n            .logarithm => |item| try self.emit(.{ .kind = .coordinate, .a = item.left, .b = item.right, .name = \\"log\\" }),\\n            .green_kernel => |item| try self.emit(.{ .kind = .coordinate, .a = item.coordinate.left, .b = item.coordinate.right, .name = item.name }),\\n            .green_exponential => |item| try self.emit(.{ .kind = .coordinate, .a = item.left, .b = item.right, .name = \\"exp-green\\" }),\\n        }\\n    }\\n\\n    pub fn emitWickTensor(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .none => try self.emit(.{ .kind = .tensor }),\\n            .metric => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = \\"metric\\" }),\\n            .momentum_index => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.momentum), .b = labelSymbol(item.index), .name = \\"momentum-index\\" }),\\n            .momentum_pair => |item| try self.emit(.{ .kind = .tensor, .a = labelSymbol(item.left), .b = labelSymbol(item.right), .name = \\"momentum-pair\\" }),\\n            else => try self.emit(.{ .kind = .tensor }),\\n        }\\n    }\\n\\n    pub fn emitWickAction(self: *@This(), _: anytype) !void {\\n        try self.emit(.{ .kind = .residual_operator, .name = \\"action\\" });\\n    }\\n\\n    pub fn emitWickResidualOperator(self: *@This(), residual: anytype) !void {\\n        try self.emit(.{ .kind = .residual_operator, .a = @intCast(residual.input_index) });\\n    }\\n\\n    pub fn emitWickTermEnd(self: *@This()) !void {\\n        try self.emit(.{ .kind = .wick_term_end });\\n    }\\n\\n    pub fn emitZeroModeFactor(self: *@This(), factor: anytype) !void {\\n        switch (factor) {\\n            .bc_top_form => try self.emit(.{ .kind = .zero_mode, .name = \\"bc-top-form\\" }),\\n            .eta_xi_zero_mode => |item| try self.emit(.{ .kind = .zero_mode, .a = item.xi.coordinate, .name = \\"eta-xi-zero-mode\\" }),\\n            .momentum_delta => |item| {\\n                try self.emit(try scalarEvent(item.scalar));\\n                try self.emit(.{ .kind = .zero_mode, .a = item.two_pi_power, .b = @intCast(item.momenta.len), .name = \\"momentum-delta\\" });\\n                for (item.momenta) |momentum| {\\n                    try self.emit(.{ .kind = .zero_mode, .a = labelSymbol(momentum), .name = \\"momentum-delta-momentum\\" });\\n                }\\n            },\\n            else => try self.emit(.{ .kind = .zero_mode }),\\n        }\\n    }\\n\\n    pub fn emitZeroModeBaseEnd(self: *@This()) !void {\\n        try self.emit(.{ .kind = .sum_term_end });\\n    }\\n};\\nfn insertionFrom(shape: InsertionShape, coords: []const u32) !operators.OperatorInsertion {\\n    return switch (shape) {\\n        .single => if (coords.len == 1) .{ .single = .{ .position = coords[0], .derivatives = 0 } } else error.InvalidCoordinateArity,\\n        .pair => if (coords.len == 2) .{ .pair = .{\\n            .holomorphic_position = coords[0],\\n            .antiholomorphic_position = coords[1],\\n            .holomorphic_derivatives = 0,\\n            .antiholomorphic_derivatives = 0,\\n        } } else error.InvalidCoordinateArity,\\n    };\\n}\\n\\n/// GeneratedTheory lowers one descriptor to an executable theory boundary.\\npub fn GeneratedTheory(comptime d: Descriptor) type {\\n    comptime {\\n        validateDescriptor(d) catch |err| @compileError(@errorName(err));\\n    }\\n    const rules = wickStorage(d);\\n    const zeros = zeroStorage(d);\\n    const fermions = fermionStorage(d.fields, d);\\n    const Config = declare.correlatorConfig(.{\\n        .wick_rules = &rules,\\n        .zero_modes = &zeros,\\n        .fermion_kinds = &fermions,\\n    });\\n\\n    return struct {\\n        /// abi_version is the descriptor ABI accepted by this generated theory.\\n        pub const abi_version = descriptor_abi_version;\\n        /// theory_hash distinguishes generated theories at runtime.\\n        pub const theory_hash = d.theory_hash;\\n        /// descriptor exposes the compact source descriptor for audits.\\n        pub const descriptor = d;\\n        const config = Config{};\\n\\n        /// Context owns symbols and a single mutable operator list.\\n        pub const Context = struct {\\n            allocator: std.mem.Allocator,\\n            symbols: std.ArrayList([]u8) = .empty,\\n            labels: std.ArrayList(kernel.Call.LabelValue) = .empty,\\n            operators_list: std.ArrayList(kernel.Call.LocalOp) = .empty,\\n            frozen_labels: []kernel.Call.LabelValue = &.{},\\n            frozen_ops: []kernel.Call.LocalOp = &.{},\\n            label_store: kernel.Call.LabelStore = .{ .values = &.{} },\\n            next_normal_order_group: normal_ordering.Group = normal_ordering.first,\\n\\n            /// init creates an empty generated-theory context.\\n            pub fn init(allocator: std.mem.Allocator) Context {\\n                return .{ .allocator = allocator };\\n            }\\n\\n            /// deinit releases context-owned buffers.\\n            pub fn deinit(self: *Context) void {\\n                for (self.symbols.items) |name| self.allocator.free(name);\\n                self.symbols.deinit(self.allocator);\\n                self.labels.deinit(self.allocator);\\n                self.operators_list.deinit(self.allocator);\\n                self.allocator.free(self.frozen_labels);\\n                self.allocator.free(self.frozen_ops);\\n            }\\n\\n            /// intern returns a stable context-local symbol id for a name.\\n            pub fn intern(self: *Context, name: []const u8) !u32 {\\n                for (self.symbols.items, 1..) |stored, index| {\\n                    if (std.mem.eql(u8, stored, name)) return @intCast(index);\\n                }\\n                const copy = try self.allocator.dupe(u8, name);\\n                try self.symbols.append(self.allocator, copy);\\n                return @intCast(self.symbols.items.len);\\n            }\\n\\n            /// symbolName resolves a context-local symbol id.\\n            pub fn symbolName(self: *const Context, symbol: u32) ?[]const u8 {\\n                if (symbol == 0 or symbol > self.symbols.items.len) return null;\\n                return self.symbols.items[symbol - 1];\\n            }\\n\\n            /// insertField appends one generated field occurrence.\\n            pub fn insertField(self: *Context, field_id: Id, coords: []const u32, label_symbols: []const u32) !void {\\n                if (field_id >= d.fields.len) return error.DanglingField;\\n                const field = d.fields[field_id];\\n                if (label_symbols.len != field.labels.len) return error.InvalidFieldArity;\\n                const label_start = self.labels.items.len;\\n                for (label_symbols) |symbol| {\\n                    try self.labels.append(self.allocator, .{ .symbol = symbol });\\n                }\\n                try self.operators_list.append(self.allocator, .{\\n                    .insertion = try insertionFrom(field.insertion, coords),\\n                    .kind = kindId(d, field_id),\\n                    .labels = @intCast(label_start),\\n                });\\n            }\\n\\n            /// normalOrderLast tags the last count insertions as one normal product.\\n            pub fn normalOrderLast(self: *Context, count: usize) !void {\\n                try normal_ordering.tagLast(self.operators_list.items, count, &self.next_normal_order_group);\\n            }\\n\\n            /// freeze returns the current immutable operator sequence.\\n            pub fn freeze(self: *Context) !kernel.Call.MultiOp {\\n                self.allocator.free(self.frozen_labels);\\n                self.allocator.free(self.frozen_ops);\\n                self.frozen_labels = try self.labels.toOwnedSlice(self.allocator);\\n                self.frozen_ops = try self.operators_list.toOwnedSlice(self.allocator);\\n                self.label_store = .{ .values = self.frozen_labels };\\n                return .{ .operators = self.frozen_ops, .labels = &self.label_store };\\n            }\\n        };\\n\\n        /// descriptorAbiVersion returns the generated descriptor ABI version.\\n        pub fn descriptorAbiVersion() u32 {\\n            return abi_version;\\n        }\\n\\n        /// theoryHash returns the generated theory hash.\\n        pub fn theoryHash() u32 {\\n            return theory_hash;\\n        }\\n\\n        /// scalarAtomParameterName resolves a descriptor parameter scalar atom.\\n        pub fn scalarAtomParameterName(atom: u32) ?[]const u8 {\\n            inline for (d.parameters) |parameter| {\\n                if (parameterAtom(d, parameter.id) == atom) return d.symbols[parameter.symbol];\\n            }\\n            return null;\\n        }\\n\\n        /// contextCreate allocates a generated-theory context.\\n        pub fn contextCreate(allocator: std.mem.Allocator) !*Context {\\n            const ctx = try allocator.create(Context);\\n            ctx.* = Context.init(allocator);\\n            return ctx;\\n        }\\n\\n        /// contextDestroy releases a generated-theory context.\\n        pub fn contextDestroy(ctx: *Context) void {\\n            const allocator = ctx.allocator;\\n            ctx.deinit();\\n            allocator.destroy(ctx);\\n        }\\n\\n        /// symbolIntern interns one runtime symbol name.\\n        pub fn symbolIntern(ctx: *Context, name: []const u8) !u32 {\\n            return ctx.intern(name);\\n        }\\n\\n        /// fieldInsert appends one field occurrence.\\n        pub fn fieldInsert(ctx: *Context, field_id: Id, coords: []const u32, label_symbols: []const u32) !void {\\n            return ctx.insertField(field_id, coords, label_symbols);\\n        }\\n\\n        /// normalOrdering tags the last count insertions as one normal product.\\n        pub fn normalOrdering(ctx: *Context, count: usize) !void {\\n            return ctx.normalOrderLast(count);\\n        }\\n\\n        /// operatorListFreeze freezes the current operator list.\\n        pub fn operatorListFreeze(ctx: *Context) !kernel.Call.MultiOp {\\n            return ctx.freeze();\\n        }\\n\\n        /// correlatorCount returns the number of accepted streamed branches.\\n        pub fn correlatorCount(ops: kernel.Call.MultiOp) !usize {\\n            var sink = CountSink{};\\n            try shared.streamCorrelator(&config, ops, &sink);\\n            return sink.count;\\n        }\\n\\n        /// correlatorRun streams result events through a caller-provided function.\\n        pub fn correlatorRun(ops: kernel.Call.MultiOp, payload: *anyopaque, stream: StreamFn) !void {\\n            var sink = EventSink{ .payload = payload, .stream = stream };\\n            try sink.emit(.{ .kind = .sum_term_begin });\\n            try shared.streamCorrelator(&config, ops, &sink);\\n        }\\n    };\\n}\\n","n":0.016}}},{"i":41,"$":{"1":{"v":"Lisp Descriptor Emitter","n":0.577},"2":{"v":"This node is the Lisp side of the descriptor boundary\\nGenerated Descriptor Boundary.  It has three jobs:\\nstore normalized row data, print deterministic Zig descriptor tables, and wrap\\nthe generated C ABI for the REPL.  The descriptor rows are compile-time data;\\nruntime expression construction only touches the C ABI records returned by Zig.\\n(defpackage #:string-code.cft.descriptor\\n  (:use #:cl)\\n  (:export\\n   #:make-theory\\n   #:add-symbol\\n   #:add-parameter\\n   #:add-quantum-number\\n   #:add-field-quantum-number\\n   #:add-surface\\n   #:add-field\\n   #:add-metadata\\n   #:add-wick-rule\\n   #:add-zero-mode\\n   #:make-wick-term\\n   #:emit-zig-descriptor\\n   #:write-build-manifest\\n   #:load-generated-library\\n   #:make-runtime-context\\n   #:destroy-runtime-context\\n   #:set-runtime-constant\\n   #:intern-runtime-symbol\\n   #:insert-runtime-field\\n   #:normal-order-runtime-field\\n   #:freeze-runtime-operators\\n   #:run-correlator\\n   #:count-correlator\\n   #:correlator-expression\\n   #:collect-correlator))\\n\\n(in-package #:string-code.cft.descriptor)\\nThe IR is plain row data.  Constructors append rows in declaration order and\\nthe emitter sorts id-bearing tables before printing.  Quantum-number schema\\nrows are separate from field assignments so the next basis-generator pass can\\nscan charges and irreps without reading Wick rules.\\nSymbols are stored once per theory.  Every DSL symbol is normalized to a small\\ndescriptor string at this boundary, so generated Zig never sees Lisp packages.\\n(defstruct (theory (:constructor %make-theory))\\n  name\\n  hash\\n  (symbols (make-array 0 :adjustable t :fill-pointer 0))\\n  (parameters nil)\\n  (quantum-numbers nil)\\n  (field-quantum-numbers nil)\\n  (surfaces nil)\\n  (fields nil)\\n  (metadata nil)\\n  (wick-rules nil)\\n  (zero-modes nil)\\n  (result-symbols nil))\\n\\n(defstruct parameter id symbol role)\\n(defstruct quantum-number id symbol kind group-symbol)\\n(defstruct field-quantum-number field quantum-number value)\\n(defstruct surface id kind coordinate-model modular-parameter)\\n(defstruct label-schema id role symbol)\\n(defstruct field id symbol insertion labels statistics zero-mode weight anti-weight)\\n(defstruct wick-term scalars coordinates tensors actions residuals)\\n(defstruct wick-rule surface left right terms)\\n(defstruct zero-mode surface kind consumes normalization two-pi-power)\\n(defstruct (runtime-context (:constructor %make-runtime-context))\\n  handle\\n  theory-id\\n  constants\\n  (scalar-atom-names (make-hash-table))\\n  scalar-monomials\\n  (symbols (make-array 0 :adjustable t :fill-pointer 0)))\\n(defun descriptor-symbol-name (value)\\n  (etypecase value\\n    (string value)\\n    (symbol (string-downcase (string value)))))\\n\\n(defun add-symbol (theory name)\\n  (let ((symbols (theory-symbols theory))\\n        (text (descriptor-symbol-name name)))\\n    (or (loop for index below (length symbols)\\n              when (string= (aref symbols index) text)\\n                return index)\\n        (vector-push-extend text symbols))))\\n\\n(defun make-theory (name hash)\\n  (let ((theory (%make-theory :name name :hash hash)))\\n    (add-symbol theory name)\\n    theory))\\n\\n(defun add-parameter (theory symbol role)\\n  (let ((id (length (theory-parameters theory))))\\n    (push (make-parameter :id id :symbol (add-symbol theory symbol) :role role)\\n          (theory-parameters theory))\\n    id))\\n\\n(defun add-quantum-number (theory symbol kind &key group)\\n  (let ((id (length (theory-quantum-numbers theory))))\\n    (push (make-quantum-number\\n           :id id\\n           :symbol (add-symbol theory (descriptor-symbol-name symbol))\\n           :kind kind\\n           :group-symbol (when group (add-symbol theory (descriptor-symbol-name group))))\\n          (theory-quantum-numbers theory))\\n    id))\\n\\n(defun add-field-quantum-number (theory field quantum-number value)\\n  (push (make-field-quantum-number\\n         :field field\\n         :quantum-number quantum-number\\n         :value value)\\n        (theory-field-quantum-numbers theory)))\\n\\n(defun add-surface (theory kind coordinate-model &key modular-parameter)\\n  (let ((id (length (theory-surfaces theory))))\\n    (push (make-surface :id id :kind kind :coordinate-model coordinate-model\\n                        :modular-parameter modular-parameter)\\n          (theory-surfaces theory))\\n    id))\\nField labels infer roles from declaration tags unless the caller passes a full\\nlabel row.  This is intentionally small and deterministic.\\n(defun role-of-label (label)\\n  (etypecase label\\n    (label-schema (label-schema-role label))\\n    (cons (second label))\\n    (symbol :vector-index)))\\n\\n(defun label-name (label)\\n  (etypecase label\\n    (label-schema (label-schema-symbol label))\\n    (cons (first label))\\n    (symbol label)))\\n\\n(defun make-label-rows (theory labels)\\n  (loop for label in labels\\n        for id from 0\\n        collect (make-label-schema\\n                 :id id\\n                 :role (role-of-label label)\\n                 :symbol (add-symbol theory (string-downcase (string (label-name label)))))))\\n\\n(defun add-field (theory symbol insertion labels statistics &key zero-mode weight anti-weight)\\n  (let ((id (length (theory-fields theory))))\\n    (push (make-field :id id\\n                      :symbol (add-symbol theory symbol)\\n                      :insertion insertion\\n                      :labels (make-label-rows theory labels)\\n                      :statistics statistics\\n                      :zero-mode zero-mode\\n                      :weight weight\\n                      :anti-weight anti-weight)\\n          (theory-fields theory))\\n    id))\\n(defun metadata-symbol-id (theory value)\\n  (etypecase value\\n    (integer value)\\n    (string (add-symbol theory value))\\n    (symbol (add-symbol theory (string-downcase (string value))))))\\n\\n(declaim (ftype function add-metadata))\\n\\n(defun normalized-metadata-form (theory form)\\n  (etypecase form\\n    (integer form)\\n    (cons\\n     (case (first form)\\n       (:rational form)\\n       (:parameter form)\\n       (:field-label form)\\n       (:add (list :add\\n                   (add-metadata theory (second form))\\n                   (add-metadata theory (third form))))\\n       (:mul (list :mul\\n                   (add-metadata theory (second form))\\n                   (add-metadata theory (third form))))\\n       (:bilinear (list :bilinear\\n                        (add-metadata theory (second form))\\n                        (add-metadata theory (third form))\\n                        (metadata-symbol-id theory (fourth form))))\\n       (otherwise (error \\"Unknown metadata form ~S.\\" form))))))\\n\\n(defun metadata-row-id (theory row)\\n  (let ((count (length (theory-metadata theory))))\\n    (loop for item in (theory-metadata theory)\\n          for index from 0\\n          when (equal item row)\\n            return (- count index 1))))\\n\\n(defun add-metadata (theory form)\\n  (let ((row (normalized-metadata-form theory form)))\\n    (if (integerp row)\\n        row\\n        (or (metadata-row-id theory row)\\n            (let ((id (length (theory-metadata theory))))\\n              (push row (theory-metadata theory))\\n              id)))))\\n\\n(defun add-wick-rule (theory surface left right terms)\\n  (push (make-wick-rule :surface surface :left left :right right :terms terms)\\n        (theory-wick-rules theory)))\\n\\n(defun add-zero-mode (theory surface kind consumes &key (normalization :one) (two-pi-power 0))\\n  (push (make-zero-mode :surface surface :kind kind :consumes consumes\\n                        :normalization normalization :two-pi-power two-pi-power)\\n        (theory-zero-modes theory)))\\n\\n(defun by-id (items reader)\\n  (sort (copy-list items) #\'< :key reader))\\nThe emitter prints only primitive descriptor data.  It does not branch on\\ntheory or field names; all semantic choices have already been lowered into\\ndense ids and compact tagged rows.\\n(defun zig-keyword (value)\\n  (substitute #\\\\_ #\\\\- (string-downcase (string value))))\\n\\n(defun emit-id-list (stream ids)\\n  (format stream \\"&.{\\")\\n  (loop for id in ids for first = t then nil\\n        do (format stream \\"~:[, ~;~]~D\\" first id))\\n  (format stream \\"}\\"))\\n\\n(defun emit-symbols (stream theory var)\\n  (format stream \\"const ~A = [_][]const u8{ \\" var)\\n  (loop for index below (length (theory-symbols theory))\\n        for first = t then nil\\n        do (format stream \\"~:[, ~;~]~S\\" first (aref (theory-symbols theory) index)))\\n  (format stream \\" };~%\\"))\\n\\n(defun emit-label (stream label)\\n  (format stream \\".{ .id = ~D, .role = .~A, .symbol = ~D }\\"\\n          (label-schema-id label)\\n          (zig-keyword (label-schema-role label))\\n          (label-schema-symbol label)))\\n(defun emit-field (stream field)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .insertion = .~A, .labels = &.{\\"\\n          (field-id field) (field-symbol field) (zig-keyword (field-insertion field)))\\n  (loop for label in (field-labels field) for first = t then nil\\n        do (progn\\n             (unless first (format stream \\", \\"))\\n             (emit-label stream label)))\\n  (format stream \\"}, .statistics = .~A\\"\\n          (zig-keyword (field-statistics field)))\\n  (when (field-zero-mode field)\\n    (format stream \\", .zero_mode_consumable = true\\"))\\n  (when (field-weight field)\\n    (format stream \\", .weight = ~D\\" (field-weight field)))\\n  (when (field-anti-weight field)\\n    (format stream \\", .anti_weight = ~D\\" (field-anti-weight field)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-surface (stream surface)\\n  (format stream \\".{ .id = ~D, .kind = .~A, .coordinate_model = .~A\\"\\n          (surface-id surface)\\n          (zig-keyword (surface-kind surface))\\n          (zig-keyword (surface-coordinate-model surface)))\\n  (when (surface-modular-parameter surface)\\n    (format stream \\", .modular_parameter = ~D\\" (surface-modular-parameter surface)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-parameter (stream parameter)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .role = .~A }\\"\\n          (parameter-id parameter)\\n          (parameter-symbol parameter)\\n          (zig-keyword (parameter-role parameter))))\\n\\n(defun emit-quantum-number (stream number)\\n  (format stream \\".{ .id = ~D, .symbol = ~D, .kind = .~A\\"\\n          (quantum-number-id number)\\n          (quantum-number-symbol number)\\n          (zig-keyword (quantum-number-kind number)))\\n  (when (quantum-number-group-symbol number)\\n    (format stream \\", .group_symbol = ~D\\" (quantum-number-group-symbol number)))\\n  (format stream \\" }\\"))\\n\\n(defun emit-rational-row (stream numerator denominator)\\n  (format stream \\".{ .numerator = ~D, .denominator = ~D }\\" numerator denominator))\\n\\n(defun emit-quantum-value (stream value)\\n  (etypecase value\\n    (integer\\n     (format stream \\".{ .integer = ~D }\\" value))\\n    (rational\\n     (format stream \\".{ .rational = \\")\\n     (emit-rational-row stream (numerator value) (denominator value))\\n     (format stream \\" }\\"))\\n    (cons\\n     (ecase (first value)\\n       (:symbol\\n        (format stream \\".{ .symbol = ~D }\\" (second value)))\\n       (:rational\\n        (format stream \\".{ .rational = \\")\\n        (emit-rational-row stream (second value) (third value))\\n        (format stream \\" }\\"))))))\\n\\n(defun emit-field-quantum-number (stream item)\\n  (format stream \\".{ .field = ~D, .quantum_number = ~D, .value = \\"\\n          (field-quantum-number-field item)\\n          (field-quantum-number-quantum-number item))\\n  (emit-quantum-value stream (field-quantum-number-value item))\\n  (format stream \\" }\\"))\\n\\n(defun emit-metadata (stream form)\\n  (case (first form)\\n    (:rational\\n     (format stream \\".{ .rational = \\")\\n     (emit-rational-row stream (second form) (third form))\\n     (format stream \\" }\\"))\\n    (:parameter\\n     (format stream \\".{ .parameter = ~D }\\" (second form)))\\n    (:field-label\\n     (format stream \\".{ .field_label = .{ .field = ~D, .label = ~D } }\\"\\n             (second form) (third form)))\\n    (:add\\n     (format stream \\".{ .add = .{ .left = ~D, .right = ~D } }\\"\\n             (second form) (third form)))\\n    (:mul\\n     (format stream \\".{ .mul = .{ .left = ~D, .right = ~D } }\\"\\n             (second form) (third form)))\\n    (:bilinear\\n     (format stream \\".{ .bilinear = .{ .left = ~D, .right = ~D, .form_symbol = ~D } }\\"\\n             (second form) (third form) (fourth form)))\\n    (otherwise (error \\"Unknown metadata form ~S.\\" form))))\\n\\n(defun side-name (value)\\n  (ecase value\\n    (:left \\"left\\")\\n    (:right \\"right\\")))\\n\\n(defun coordinate-slot-name (value)\\n  (ecase value\\n    (:position \\"position\\")\\n    (:holomorphic \\"holomorphic\\")\\n    (:antiholomorphic \\"antiholomorphic\\")))\\n\\n(defun emit-coordinate-ref (stream value)\\n  (etypecase value\\n    (symbol\\n     (format stream \\".{ .side = .~A, .slot = .position }\\" (side-name value)))\\n    (cons\\n     (format stream \\".{ .side = .~A, .slot = .~A }\\"\\n             (side-name (first value))\\n             (coordinate-slot-name (second value))))))\\n\\n(defun emit-label-ref (stream value)\\n  (format stream \\".{ .side = .~A, .slot = ~D }\\"\\n          (side-name (first value)) (second value)))\\n\\n(defun emit-zig-list (stream values printer)\\n  (format stream \\"&.{\\")\\n  (loop for value in values for first = t then nil\\n        do (progn\\n             (unless first (format stream \\", \\"))\\n             (funcall printer stream value)))\\n  (format stream \\"}\\"))\\n\\n(defun emit-scalar-factor (stream form)\\n  (etypecase form\\n    (symbol\\n     (ecase form\\n       (:one (format stream \\".one\\"))))\\n    (cons\\n     (ecase (first form)\\n       (:rational\\n        (format stream \\".{ .rational = \\")\\n        (emit-rational-row stream (second form) (third form))\\n        (format stream \\" }\\"))\\n       (:parameter\\n        (format stream \\".{ .parameter = ~D }\\" (second form)))\\n       (:neg-parameter-half\\n        (format stream \\".{ .neg_parameter_half = ~D }\\" (second form)))\\n       (:neg-i-parameter-half\\n        (format stream \\".{ .neg_i_parameter_half = ~D }\\" (second form)))\\n       (:parameter-half\\n        (format stream \\".{ .parameter_half = ~D }\\" (second form)))))))\\n\\n(defun derive-left-p (value)\\n  (and (consp value) (member :derive-left value)))\\n\\n(defun derive-right-p (value)\\n  (and (consp value) (member :derive-right value)))\\n\\n(defun emit-coordinate-factor (theory stream form)\\n  (ecase (first form)\\n    (:difference-power\\n     (format stream \\".{ .difference_power = .{ .left = \\")\\n     (emit-coordinate-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\", .exponent = ~D\\" (fourth form))\\n     (when (derive-left-p form)\\n       (format stream \\", .derive_left = true\\"))\\n     (when (derive-right-p form)\\n       (format stream \\", .derive_right = true\\"))\\n     (format stream \\" } }\\"))\\n    (:named-kernel\\n     (format stream \\".{ .named_kernel = .{ .symbol = ~D, .left = \\"\\n             (metadata-symbol-id theory (second form)))\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (fourth form))\\n     (when (derive-left-p form)\\n       (format stream \\", .derive_left = true\\"))\\n     (when (derive-right-p form)\\n       (format stream \\", .derive_right = true\\"))\\n     (format stream \\" } }\\"))\\n    (:green-exponential\\n     (format stream \\".{ .green_exponential = .{ .left = \\")\\n     (emit-coordinate-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-coordinate-ref stream (third form))\\n     (format stream \\" } }\\"))))\\n\\n(defun emit-tensor-factor (stream form)\\n  (ecase (first form)\\n    (:metric\\n     (format stream \\".{ .metric = .{ .left = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))\\n    (:momentum-index\\n     (format stream \\".{ .momentum_index = .{ .momentum = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .index = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))\\n    (:momentum-pair\\n     (format stream \\".{ .momentum_pair = .{ .left = \\")\\n     (emit-label-ref stream (second form))\\n     (format stream \\", .right = \\")\\n     (emit-label-ref stream (third form))\\n     (format stream \\" } }\\"))))\\n\\n(defun emit-side (stream side)\\n  (format stream \\".~A\\" (side-name side)))\\n\\n(defun emit-wick-term (theory stream term)\\n  (format stream \\".{ .scalars = \\")\\n  (emit-zig-list stream (or (wick-term-scalars term) \'(:one)) #\'emit-scalar-factor)\\n  (format stream \\", .coordinates = \\")\\n  (emit-zig-list stream (wick-term-coordinates term)\\n                 (lambda (out form) (emit-coordinate-factor theory out form)))\\n  (format stream \\", .tensors = \\")\\n  (emit-zig-list stream (wick-term-tensors term) #\'emit-tensor-factor)\\n  (format stream \\", .residuals = \\")\\n  (emit-zig-list stream (wick-term-residuals term) #\'emit-side)\\n  (format stream \\" }\\"))\\n\\n(defun emit-wick-rule (stream rule term-var)\\n  (format stream \\".{ .surface = ~D, .left = ~D, .right = ~D, .terms = &~A }\\"\\n          (wick-rule-surface rule)\\n          (wick-rule-left rule)\\n          (wick-rule-right rule)\\n          term-var))\\n\\n(defun emit-zero-mode (stream rule)\\n  (format stream \\".{ .surface = ~D, .kind = .~A, .consumes = \\"\\n          (zero-mode-surface rule)\\n          (zig-keyword (zero-mode-kind rule)))\\n  (emit-id-list stream (zero-mode-consumes rule))\\n  (unless (eq (zero-mode-normalization rule) :one)\\n    (format stream \\", .normalization = \\")\\n    (emit-scalar-factor stream (zero-mode-normalization rule)))\\n  (unless (zerop (zero-mode-two-pi-power rule))\\n    (format stream \\", .two_pi_power = ~D\\" (zero-mode-two-pi-power rule)))\\n  (format stream \\" }\\"))\\n(defun emit-row-array (stream type var rows printer)\\n  (format stream \\"const ~A = [_]d.~A{~%\\" var type)\\n  (dolist (row rows)\\n    (format stream \\"    \\")\\n    (funcall printer stream row)\\n    (format stream \\",~%\\"))\\n  (format stream \\"};~%\\"))\\n\\n(defun prepare-coordinate-symbols (theory form)\\n  (when (eq (first form) :named-kernel)\\n    (metadata-symbol-id theory (second form))))\\n\\n(defun prepare-emitter-symbols (theory)\\n  (dolist (rule (theory-wick-rules theory))\\n    (dolist (term (wick-rule-terms rule))\\n      (dolist (factor (wick-term-coordinates term))\\n        (prepare-coordinate-symbols theory factor)))))\\n\\n(defun emit-wick-term-arrays (stream theory rules prefix)\\n  (loop for rule in rules\\n        for index from 0\\n        for var = (format nil \\"~A_terms_~D\\" prefix index)\\n        collect var\\n        do (emit-row-array stream \\"WickTerm\\" var (wick-rule-terms rule)\\n                           (lambda (out term) (emit-wick-term theory out term)))))\\n\\n(defun emit-zig-descriptor (theory stream &key (descriptor-name \\"generated_descriptor\\") (prefix \\"generated\\"))\\n  (prepare-emitter-symbols theory)\\n  (let ((symbols-var (format nil \\"~A_symbols\\" prefix))\\n        (parameters-var (format nil \\"~A_parameters\\" prefix))\\n        (quantum-numbers-var (format nil \\"~A_quantum_numbers\\" prefix))\\n        (field-quantum-numbers-var (format nil \\"~A_field_quantum_numbers\\" prefix))\\n        (surfaces-var (format nil \\"~A_surfaces\\" prefix))\\n        (fields-var (format nil \\"~A_fields\\" prefix))\\n        (metadata-var (format nil \\"~A_metadata\\" prefix))\\n        (wick-var (format nil \\"~A_wick\\" prefix))\\n        (zero-modes-var (format nil \\"~A_zero_modes\\" prefix)))\\n  (emit-symbols stream theory symbols-var)\\n  (emit-row-array stream \\"Parameter\\" parameters-var\\n                  (by-id (theory-parameters theory) #\'parameter-id)\\n                  #\'emit-parameter)\\n  (emit-row-array stream \\"QuantumNumber\\" quantum-numbers-var\\n                  (by-id (theory-quantum-numbers theory) #\'quantum-number-id)\\n                  #\'emit-quantum-number)\\n  (emit-row-array stream \\"FieldQuantumNumber\\" field-quantum-numbers-var\\n                  (reverse (theory-field-quantum-numbers theory))\\n                  #\'emit-field-quantum-number)\\n  (emit-row-array stream \\"Surface\\" surfaces-var\\n                  (by-id (theory-surfaces theory) #\'surface-id)\\n                  #\'emit-surface)\\n  (emit-row-array stream \\"Field\\" fields-var\\n                  (by-id (theory-fields theory) #\'field-id)\\n                  #\'emit-field)\\n  (emit-row-array stream \\"MetadataExpr\\" metadata-var\\n                  (reverse (theory-metadata theory))\\n                  #\'emit-metadata)\\n  (let* ((rules (reverse (theory-wick-rules theory)))\\n         (term-vars (emit-wick-term-arrays stream theory rules prefix)))\\n    (format stream \\"const ~A = [_]d.WickRule{~%\\" wick-var)\\n    (loop for rule in rules\\n          for term-var in term-vars\\n          do (progn\\n               (format stream \\"    \\")\\n               (emit-wick-rule stream rule term-var)\\n               (format stream \\",~%\\")))\\n    (format stream \\"};~%\\"))\\n  (emit-row-array stream \\"ZeroModeRule\\" zero-modes-var\\n                  (reverse (theory-zero-modes theory))\\n                  #\'emit-zero-mode)\\n  (format stream \\"pub const ~A = d.Descriptor{~%\\" descriptor-name)\\n  (format stream \\"    .theory_symbol = 0,~%\\")\\n  (format stream \\"    .theory_hash = ~D,~%\\" (theory-hash theory))\\n  (format stream \\"    .symbols = &~A,~%\\" symbols-var)\\n  (format stream \\"    .parameters = &~A,~%\\" parameters-var)\\n  (format stream \\"    .quantum_numbers = &~A,~%\\" quantum-numbers-var)\\n  (format stream \\"    .field_quantum_numbers = &~A,~%\\" field-quantum-numbers-var)\\n  (format stream \\"    .surfaces = &~A,~%\\" surfaces-var)\\n  (format stream \\"    .fields = &~A,~%\\" fields-var)\\n  (format stream \\"    .metadata = &~A,~%\\" metadata-var)\\n  (format stream \\"    .wick_rules = &~A,~%\\" wick-var)\\n  (format stream \\"    .zero_modes = &~A,~%\\" zero-modes-var)\\n  (format stream \\"};~%\\")))\\nThe manifest is deliberately small: source hash, generated path, and library\\npath.  Runtime calls require a loaded library and otherwise fail before any\\nexpression object is built.  CFFI definitions are installed lazily because a\\nuser can inspect presets without loading CFFI or a generated shared object.\\n(defun write-build-manifest (path &key source-hash zig-path library-path)\\n  (with-open-file (stream path :direction :output)\\n    (format stream \\"(:source-hash ~S :zig-path ~S :library-path ~S)~%\\"\\n            source-hash zig-path library-path)))\\n\\n(defvar *abi-bound* nil)\\n(defvar *abi-callback-pointer* nil)\\n(defvar *abi-chunk-callback-pointer* nil)\\n(defvar *abi-sinks* (make-hash-table))\\n(defvar *next-abi-sink-id* 0)\\n(defvar *event-size* 0)\\n(defvar *factor-size* 0)\\n(defvar *name-size* 0)\\n(defvar *name-ptr-offset* 0)\\n(defvar *name-len-offset* 0)\\n(defvar *term-size* 0)\\n(defparameter *event-kinds*\\n  #(:sum-term-begin :sum-term-end :wick-term-begin :wick-term-end\\n    :scalar :coordinate :tensor :zero-mode :residual-operator))\\n\\n(declaim\\n (ftype function\\n        set-mem-aref* mem-ref* foreign-slot-value* null-pointer-p*\\n        pointer-address* inc-pointer* foreign-type-size* event-pointer-at\\n        name-pointer-at term-pointer-at term-expression\\n        %abi-last-error %abi-context-create\\n        %abi-context-destroy %abi-scalar-atom-name\\n        %abi-symbol-intern %abi-field-insert\\n        %abi-normal-ordering %abi-operator-list-freeze\\n        %abi-correlator-count %abi-correlator-run\\n        %abi-correlator-run-buffered %abi-correlator-expression-records\\n        %abi-expression-buffer-free))\\n\\n(declaim\\n (ftype function\\n        factor-name-token-by-id factor-coordinate-expression-values\\n        factor-tensor-expression-values factor-scalar-expression-values\\n        factor-zero-mode-expression-values factor-expression-values\\n        term-product-expression))\\n\\n(defun cffi-package ()\\n  (or (find-package \'#:cffi)\\n      (error \\"CFFI is required to use generated libraries.\\")))\\n\\n(defun cffi-symbol (name)\\n  (intern name (cffi-package)))\\n\\n(defun foreign-string-to-lisp* (&rest args)\\n  (apply (symbol-function (cffi-symbol \\"FOREIGN-STRING-TO-LISP\\")) args))\\n\\n(defun foreign-alloc* (&rest args)\\n  (apply (symbol-function (cffi-symbol \\"FOREIGN-ALLOC\\")) args))\\n\\n(defun foreign-free* (pointer)\\n  (funcall (symbol-function (cffi-symbol \\"FOREIGN-FREE\\")) pointer))\\n\\n(defun event-from-pointer (pointer)\\n  (let* ((type \'(:struct generated-event))\\n         (kind (foreign-slot-value* pointer type \'kind))\\n         (name-pointer (foreign-slot-value* pointer type \'name-ptr))\\n         (name-length (foreign-slot-value* pointer type \'name-len))\\n         (name (and (> name-length 0)\\n                    (not (null-pointer-p* name-pointer))\\n                    (foreign-string-to-lisp* name-pointer :count name-length))))\\n    (list :kind (aref *event-kinds* kind)\\n          :a (foreign-slot-value* pointer type \'a)\\n          :b (foreign-slot-value* pointer type \'b)\\n          :c (foreign-slot-value* pointer type \'c)\\n          :d (foreign-slot-value* pointer type \'d)\\n          :name name)))\\n\\n(defun ensure-abi-bindings ()\\n  (unless *abi-bound*\\n    (let ((defcstruct (cffi-symbol \\"DEFCSTRUCT\\"))\\n          (defcfun (cffi-symbol \\"DEFCFUN\\"))\\n          (defcallback (cffi-symbol \\"DEFCALLBACK\\"))\\n          (callback (cffi-symbol \\"CALLBACK\\"))\\n          (inc-pointer (cffi-symbol \\"INC-POINTER\\"))\\n          (foreign-type-size (cffi-symbol \\"FOREIGN-TYPE-SIZE\\"))\\n\\t          (foreign-slot-offset (cffi-symbol \\"FOREIGN-SLOT-OFFSET\\"))\\n\\t          (with-foreign-slots (cffi-symbol \\"WITH-FOREIGN-SLOTS\\"))\\n\\t          (mem-aref (cffi-symbol \\"MEM-AREF\\"))\\n          (mem-ref (cffi-symbol \\"MEM-REF\\"))\\n          (foreign-slot-value (cffi-symbol \\"FOREIGN-SLOT-VALUE\\"))\\n          (null-pointer-p (cffi-symbol \\"NULL-POINTER-P\\"))\\n          (pointer-address (cffi-symbol \\"POINTER-ADDRESS\\")))\\n      (eval `(,defcstruct generated-event\\n               (kind :uint8)\\n               (a :uint32)\\n               (b :uint32)\\n               (c :uint32)\\n               (d :int32)\\n               (name-ptr :pointer)\\n               (name-len :size)))\\n      (eval `(,defcstruct generated-factor\\n               (kind :uint8)\\n               (a :uint32)\\n               (b :uint32)\\n               (c :uint32)\\n               (d :int32)\\n               (name-id :uint32)))\\n      (eval `(,defcstruct generated-name\\n               (ptr :pointer)\\n               (len :size)))\\n      (eval `(,defcstruct generated-term\\n               (first-factor :size)\\n               (factor-count :size)))\\n      (eval `(defun set-mem-aref* (pointer type index value)\\n               (setf (,mem-aref pointer type index) value)))\\n      (eval `(defun mem-ref* (pointer type)\\n               (,mem-ref pointer type)))\\n      (eval `(defun foreign-slot-value* (pointer type slot)\\n               (,foreign-slot-value pointer type slot)))\\n      (eval `(defun null-pointer-p* (pointer)\\n               (,null-pointer-p pointer)))\\n      (eval `(defun pointer-address* (pointer)\\n               (,pointer-address pointer)))\\n      (eval `(defun inc-pointer* (pointer offset)\\n               (,inc-pointer pointer offset)))\\n      (eval `(defun foreign-type-size* (type)\\n               (,foreign-type-size type)))\\n      (eval `(defun event-pointer-at (events index)\\n               (,inc-pointer events (* index *event-size*))))\\n      (eval `(defun name-pointer-at (names index)\\n               (,inc-pointer names (* index *name-size*))))\\n      (eval `(defun term-pointer-at (terms index)\\n               (,inc-pointer terms (* index *term-size*))))\\n      (setf *event-size* (eval `(,foreign-type-size \'(:struct generated-event)))\\n            *factor-size* (eval `(,foreign-type-size \'(:struct generated-factor)))\\n            *name-size* (eval `(,foreign-type-size \'(:struct generated-name)))\\n            *name-ptr-offset* (eval `(,foreign-slot-offset \'(:struct generated-name) \'ptr))\\n            *name-len-offset* (eval `(,foreign-slot-offset \'(:struct generated-name) \'len))\\n            *term-size* (eval `(,foreign-type-size \'(:struct generated-term))))\\n      (eval `(defun term-expression (builder factors names term)\\n               (declare (optimize (speed 3) (safety 1) (debug 0)))\\n               (,with-foreign-slots ((first-factor factor-count) term (:struct generated-term))\\n                 (let ((items nil)\\n                       (scalars nil)\\n                       (greens nil)\\n                       (momentum-pairs nil)\\n                       (others nil))\\n                   (loop for index from first-factor below (+ first-factor factor-count)\\n                         for factor = (,inc-pointer factors (* index *factor-size*))\\n                         do (,with-foreign-slots ((kind a b c d name-id) factor (:struct generated-factor))\\n                              (let ((expr (factor-expression-values builder names kind a b c d name-id)))\\n                                (when (and expr (not (eql expr 1)))\\n                                  (push expr items)\\n                                  (cond\\n                                    ((= kind 4) (push expr scalars))\\n                                    ((and (= kind 5) (exp-green-expression-p expr))\\n                                     (push expr greens))\\n                                    ((and (= kind 6) (momentum-pair-expression-p expr))\\n                                     (push expr momentum-pairs))\\n                                    (t (push expr others)))))))\\n                   (term-product-expression items scalars greens momentum-pairs others)))))\\n      (eval `(,defcfun (\\"sc_generated_last_error\\" %abi-last-error) :pointer))\\n      (eval `(,defcfun (\\"sc_generated_context_create\\" %abi-context-create) :pointer\\n               (theory-id :uint32)))\\n      (eval `(,defcfun (\\"sc_generated_context_destroy\\" %abi-context-destroy) :void\\n               (context :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_scalar_atom_name\\" %abi-scalar-atom-name) :int\\n               (theory-id :uint32)\\n               (atom :uint32)\\n               (out-name :pointer)\\n               (out-name-len :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_symbol_intern\\" %abi-symbol-intern) :int\\n               (context :pointer)\\n               (name :string)\\n               (name-len :size)\\n               (out-symbol :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_field_insert\\" %abi-field-insert) :int\\n               (context :pointer)\\n               (field-id :uint16)\\n               (coordinates :pointer)\\n               (coordinate-count :size)\\n               (labels :pointer)\\n               (label-count :size)))\\n      (eval `(,defcfun (\\"sc_generated_normal_ordering\\" %abi-normal-ordering) :int\\n               (context :pointer)\\n               (field-count :size)))\\n      (eval `(,defcfun (\\"sc_generated_operator_list_freeze\\" %abi-operator-list-freeze) :int\\n               (context :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_count\\" %abi-correlator-count) :int\\n               (context :pointer)\\n               (out-count :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_run\\" %abi-correlator-run) :int\\n               (context :pointer)\\n               (payload :pointer)\\n               (callback :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_run_buffered\\" %abi-correlator-run-buffered) :int\\n               (context :pointer)\\n               (payload :pointer)\\n               (callback :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_correlator_expression_records\\" %abi-correlator-expression-records) :int\\n               (context :pointer)\\n               (out-terms :pointer)\\n               (out-term-count :pointer)\\n               (out-factors :pointer)\\n               (out-factor-count :pointer)\\n               (out-names :pointer)\\n               (out-name-count :pointer)))\\n      (eval `(,defcfun (\\"sc_generated_expression_buffer_free\\" %abi-expression-buffer-free) :void\\n               (terms :pointer)\\n               (term-count :size)\\n               (factors :pointer)\\n               (factor-count :size)\\n               (names :pointer)\\n               (name-count :size)))\\n      (eval `(,defcallback %abi-event-callback :int\\n               ((payload :pointer) (event :pointer))\\n               (let ((sink (gethash (mem-ref* payload :uint64) *abi-sinks*)))\\n                 (when sink\\n                   (funcall sink event))\\n                 0)))\\n      (eval `(,defcallback %abi-event-chunk-callback :int\\n               ((payload :pointer) (events :pointer) (event-count :size))\\n               (let ((sink (gethash (mem-ref* payload :uint64) *abi-sinks*)))\\n                 (when sink\\n                   (loop for index below event-count\\n                         do (funcall sink (event-pointer-at events index))))\\n                 0)))\\n      (setf *abi-callback-pointer* (eval `(,callback %abi-event-callback)))\\n      (setf *abi-chunk-callback-pointer* (eval `(,callback %abi-event-chunk-callback)))\\n      (setf *abi-bound* t))))\\n\\n(defun load-generated-library (path)\\n  (funcall (symbol-function (cffi-symbol \\"LOAD-FOREIGN-LIBRARY\\")) path)\\n  (ensure-abi-bindings)\\n  path)\\n\\n(defun check-abi (status)\\n  (unless (zerop status)\\n    (let ((message (%abi-last-error)))\\n      (error \\"Generated ABI call failed: ~A\\"\\n             (foreign-string-to-lisp* message)))))\\n\\n(defun keyword-name (name)\\n  (intern (string-upcase name) \'#:keyword))\\n\\n(defun constant-name (name)\\n  (etypecase name\\n    (string (keyword-name name))\\n    (symbol (keyword-name (string name)))))\\n\\n(defun normalize-runtime-constants (constants)\\n  (let ((table (make-hash-table)))\\n    (dolist (item constants table)\\n      (etypecase item\\n        (cons\\n         (setf (gethash (constant-name (car item)) table) (cdr item)))\\n        (symbol\\n         (setf (gethash (constant-name item) table) (constant-name item)))))))\\n\\n(defun set-runtime-constant (context name value)\\n  \\"Set one symbolic scalar constant for later expression construction.\\"\\n  (setf (gethash (constant-name name) (runtime-context-constants context)) value\\n        (runtime-context-scalar-monomials context) nil)\\n  value)\\n\\n(defun runtime-constant-value (context name)\\n  (multiple-value-bind (value present)\\n      (gethash name (runtime-context-constants context))\\n    (if present value name)))\\n\\n(defun make-runtime-context (&key library theory-id constants)\\n  (unless theory-id\\n    (error \\"A generated theory id is required.\\"))\\n  (when library\\n    (load-generated-library library))\\n  (ensure-abi-bindings)\\n  (let ((handle (%abi-context-create theory-id)))\\n    (when (null-pointer-p* handle)\\n      (check-abi -1))\\n    (%make-runtime-context :handle handle :theory-id theory-id\\n                           :constants (normalize-runtime-constants constants))))\\n\\n(defun destroy-runtime-context (context)\\n  (when (runtime-context-handle context)\\n    (%abi-context-destroy (runtime-context-handle context))\\n    (setf (runtime-context-handle context) nil)))\\n\\n(defun runtime-handle (context)\\n  (or (runtime-context-handle context)\\n      (error \\"No generated runtime context is active.\\")))\\n\\n(defun remember-runtime-symbol (context id name)\\n  (let ((symbols (runtime-context-symbols context)))\\n    (loop while (< (length symbols) id)\\n          do (vector-push-extend nil symbols))\\n    (setf (aref symbols (1- id)) (keyword-name name))\\n    id))\\n\\n(defun runtime-symbol-name (context id)\\n  (let ((symbols (runtime-context-symbols context)))\\n    (if (and (> id 0) (<= id (length symbols)))\\n        (or (aref symbols (1- id)) `(:symbol ,id))\\n        `(:symbol ,id))))\\n\\n(defun intern-runtime-symbol (context name)\\n  (let ((out (foreign-alloc* :uint32)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-symbol-intern (runtime-handle context) name (length name) out))\\n           (remember-runtime-symbol context (mem-ref* out :uint32) name))\\n      (foreign-free* out))))\\nRuntime insertion is intentionally shallow.  Lisp interns coordinate and label\\nnames once per context, passes only dense ids through C, and lets Zig own the\\noperator-list storage.\\n(defun copy-uint32-values (values)\\n  (let* ((count (length values))\\n         (pointer (foreign-alloc* :uint32 :count (max 1 count))))\\n    (loop for value in values\\n          for index from 0\\n          do (set-mem-aref* pointer :uint32 index value))\\n    (values pointer count)))\\n\\n(defun insert-runtime-field (context field-id coordinates labels)\\n  (multiple-value-bind (coord-pointer coord-count) (copy-uint32-values coordinates)\\n    (multiple-value-bind (label-pointer label-count) (copy-uint32-values labels)\\n      (unwind-protect\\n           (check-abi\\n            (%abi-field-insert (runtime-handle context) field-id coord-pointer coord-count label-pointer label-count))\\n        (foreign-free* label-pointer)\\n        (foreign-free* coord-pointer))))\\n  field-id)\\n\\n(defun normal-order-runtime-field (context count)\\n  (check-abi\\n   (%abi-normal-ordering (runtime-handle context) count))\\n  count)\\n\\n(defun freeze-runtime-operators (context)\\n  (check-abi\\n   (%abi-operator-list-freeze (runtime-handle context)))\\n  :generated)\\n\\n(defun count-correlator (context frozen)\\n  (declare (ignore frozen))\\n  (let ((out (foreign-alloc* :size)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-correlator-count (runtime-handle context) out))\\n           (mem-ref* out :size))\\n      (foreign-free* out))))\\n\\n(defun run-correlator-raw (context sink)\\n  (let* ((sink-id (prog1 *next-abi-sink-id*\\n                    (incf *next-abi-sink-id*)))\\n         (payload (foreign-alloc* :uint64)))\\n    (setf (gethash sink-id *abi-sinks*) sink)\\n    (set-mem-aref* payload :uint64 0 sink-id)\\n    (unwind-protect\\n         (check-abi\\n          (%abi-correlator-run (runtime-handle context) payload *abi-callback-pointer*))\\n      (remhash sink-id *abi-sinks*)\\n      (foreign-free* payload))))\\n\\n(defun run-correlator-buffered (context sink)\\n  (let* ((sink-id (prog1 *next-abi-sink-id*\\n                    (incf *next-abi-sink-id*)))\\n         (payload (foreign-alloc* :uint64)))\\n    (setf (gethash sink-id *abi-sinks*) sink)\\n    (set-mem-aref* payload :uint64 0 sink-id)\\n    (unwind-protect\\n         (check-abi\\n          (%abi-correlator-run-buffered (runtime-handle context) payload *abi-chunk-callback-pointer*))\\n      (remhash sink-id *abi-sinks*)\\n      (foreign-free* payload))))\\n\\n(defun run-correlator (context frozen sink)\\n  (declare (ignore frozen))\\n  (run-correlator-raw context\\n                      (lambda (event)\\n                        (funcall sink (event-from-pointer event)))))\\n\\n(defstruct (expression-builder (:constructor make-expression-builder (context)))\\n  context\\n  terms)\\n\\n(declaim\\n (inline name-ptr name-len name-token product-expression sum-expression))\\n\\n(defun name-ptr (name)\\n  (mem-ref* (inc-pointer* name *name-ptr-offset*) :pointer))\\n\\n(defun name-len (name)\\n  (mem-ref* (inc-pointer* name *name-len-offset*) :size))\\n\\n(defun name-token (name)\\n  (let ((pointer (name-ptr name))\\n        (length (name-len name)))\\n    (when (and (> length 0) (not (null-pointer-p* pointer)))\\n      (keyword-name (foreign-string-to-lisp* pointer :count length)))))\\n\\n(defun factor-name-token-by-id (names id)\\n  (and (> id 0) (aref names (1- id))))\\n\\n(defun product-expression (factors)\\n  (cond\\n    ((null factors) 1)\\n    ((null (cdr factors)) (car factors))\\n    (t (cons \'* factors))))\\n\\n(defun sum-expression (terms)\\n  (cond\\n    ((null terms) 0)\\n    ((null (cdr terms)) (car terms))\\n    (t (cons \'+ terms))))\\n\\n(defun exp-green-expression-p (expr)\\n  (and (consp expr) (eq (first expr) :exp-green)))\\n\\n(defun momentum-pair-expression-p (expr)\\n  (and (consp expr) (eq (first expr) :momentum-pair)))\\n\\n(defun explicit-green-expression (green exponent)\\n  `(expt (- ,(second green) ,(third green)) ,exponent))\\n\\n(defun product-factor-list (expr)\\n  (if (and (consp expr) (eq (first expr) \'*))\\n      (rest expr)\\n      (list expr)))\\n\\n(defun momentum-sum-expression (momenta)\\n  (cond\\n    ((null momenta) 0)\\n    ((null (cdr momenta)) (car momenta))\\n    (t (cons \'+ momenta))))\\n\\n(defun momentum-delta-expression (header momenta)\\n  (let ((power (second header))\\n        (sum (momentum-sum-expression momenta)))\\n    (product-expression\\n     (remove 1\\n             (list (if (zerop power) 1 `(expt (* 2 :pi) ,power))\\n                   `(:momentum-delta ,power ,sum))))))\\n\\n(defun rewrite-zero-mode-factors (items)\\n  (let ((rest nil)\\n        (momenta nil)\\n        (header nil)\\n        (has-delta nil))\\n    (dolist (item items)\\n      (when (and (consp item) (eq (first item) :momentum-delta-header))\\n        (setf has-delta t)))\\n    (dolist (item items)\\n      (cond\\n        ((and (consp item) (eq (first item) :momentum-delta-header))\\n         (setf header item))\\n        ((and (consp item) (eq (first item) :momentum-delta-momentum))\\n         (push (second item) momenta))\\n        ((and has-delta (consp item) (eq (first item) :residual))\\n         nil)\\n        (t (push item rest))))\\n    (if header\\n        (append (nreverse rest)\\n                (list (momentum-delta-expression header (nreverse momenta))))\\n        items)))\\n\\n(defun term-product-expression (items scalars greens momentum-pairs others)\\n  (if (and greens momentum-pairs)\\n      (let ((exponent (product-expression\\n                       (append\\n                        (loop for scalar in (nreverse scalars)\\n                              append (product-factor-list scalar))\\n                        (nreverse momentum-pairs)))))\\n        (product-expression\\n         (append\\n          (mapcar (lambda (green)\\n                    (explicit-green-expression green exponent))\\n                  (nreverse greens))\\n          (rewrite-zero-mode-factors (nreverse others)))))\\n      (product-expression (rewrite-zero-mode-factors (nreverse items)))))\\n\\n(defun factor-coordinate-expression-values (builder names a b d name-id)\\n  (let* ((context (expression-builder-context builder))\\n         (left (runtime-symbol-name context a))\\n         (right (runtime-symbol-name context b))\\n         (name (factor-name-token-by-id names name-id)))\\n    (cond\\n      ((null name) `(expt (- ,left ,right) ,d))\\n      ((eq name :exp-green) `(:exp-green ,left ,right))\\n      (t `(,name ,left ,right)))))\\n\\n(defun factor-tensor-expression-values (builder names a b name-id)\\n  (let* ((context (expression-builder-context builder))\\n         (left (runtime-symbol-name context a))\\n         (right (runtime-symbol-name context b))\\n         (name (or (factor-name-token-by-id names name-id) :tensor)))\\n    `(,name ,left ,right)))\\n\\n(defun scalar-atom-expression (context atom)\\n  (when (> atom 0)\\n    (let ((name\\n            (multiple-value-bind (cached present)\\n                (gethash atom (runtime-context-scalar-atom-names context))\\n              (if present\\n                  cached\\n                  (let ((out-name (foreign-alloc* :pointer))\\n                        (out-name-len (foreign-alloc* :size)))\\n                    (unwind-protect\\n                         (let ((status (%abi-scalar-atom-name\\n                                        (runtime-context-theory-id context)\\n                                        atom out-name out-name-len)))\\n                           (if (zerop status)\\n                               (let* ((pointer (mem-ref* out-name :pointer))\\n                                      (length (mem-ref* out-name-len :size))\\n                                      (token (keyword-name\\n                                              (foreign-string-to-lisp*\\n                                               pointer :count length))))\\n                                 (setf (gethash atom (runtime-context-scalar-atom-names context))\\n                                       token))\\n                               (setf (gethash atom (runtime-context-scalar-atom-names context))\\n                                     `(:scalar-atom ,atom))))\\n                      (foreign-free* out-name-len)\\n                      (foreign-free* out-name)))))))\\n      (if (keywordp name)\\n          (runtime-constant-value context name)\\n          name))))\\n\\n(defun rational-expression (numerator denominator)\\n  (cond\\n    ((= numerator denominator) 1)\\n    ((= denominator 1) numerator)\\n    (t `(/ ,numerator ,denominator))))\\n\\n(defun scalar-atom-power-expression (atom power)\\n  (cond\\n    ((or (null atom) (= power 0)) 1)\\n    ((= power 1) atom)\\n    (t `(expt ,atom ,power))))\\n\\n(defun signed-byte8 (value)\\n  (if (> value 127) (- value 256) value))\\n\\n(defun scalar-imaginary-factors (power)\\n  (ecase power\\n    (0 nil)\\n    (1 \'(:i))\\n    (2 \'(-1))\\n    (3 \'(-1 :i))))\\nThe expression folder consumes compact term/factor/name arrays from Zig.  It\\nbuilds the user-facing S-expression only after the expansion stream has already\\nbeen accepted, and it keeps scalar monomial decoding cached by atom/flags.\\n(defun make-scalar-monomial-expression (context atom denominator flags numerator)\\n  (let* ((imaginary-power (logand flags 3))\\n         (atom-power (signed-byte8 (ldb (byte 8 8) flags)))\\n         (atom-expr (scalar-atom-expression context atom))\\n         (coefficient (if (and (numberp atom-expr) (= atom-power 1))\\n                          (/ (* numerator atom-expr) denominator)\\n                          (rational-expression numerator denominator)))\\n         (atom-factor (unless (and (numberp atom-expr) (= atom-power 1))\\n                        (scalar-atom-power-expression atom-expr atom-power))))\\n    (product-expression\\n     (remove-if (lambda (factor) (or (null factor) (eql factor 1)))\\n                (append\\n                 (list coefficient)\\n                 (scalar-imaginary-factors imaginary-power)\\n                 (list atom-factor))))))\\n\\n(defun cached-scalar-monomial-expression (context atom denominator flags numerator)\\n  (loop for row in (runtime-context-scalar-monomials context)\\n        when (and (= atom (aref row 0))\\n                  (= denominator (aref row 1))\\n                  (= flags (aref row 2))\\n                  (= numerator (aref row 3)))\\n          return (aref row 4)\\n        finally\\n           (let ((expr (make-scalar-monomial-expression\\n                        context atom denominator flags numerator)))\\n             (push (vector atom denominator flags numerator expr)\\n                   (runtime-context-scalar-monomials context))\\n             (return expr))))\\n\\n(defun scalar-monomial-expression (builder atom denominator flags numerator)\\n  (let ((context (expression-builder-context builder)))\\n    (cached-scalar-monomial-expression context atom denominator flags numerator)))\\n\\n(defun factor-scalar-expression-values (builder names a b c d name-id)\\n  (let ((sign d))\\n    (cond\\n      ((and (= sign -1) (= name-id 0)) -1)\\n      (t\\n       (let ((name (factor-name-token-by-id names name-id)))\\n         (cond\\n           ((eq name :scalar-monomial)\\n            (scalar-monomial-expression builder a b c d))\\n           (name\\n             `(,name\\n               ,(runtime-symbol-name (expression-builder-context builder) a)\\n               ,(runtime-symbol-name (expression-builder-context builder) b)))\\n           (t 1)))))))\\n\\n(defun factor-zero-mode-expression-values (builder names a b name-id)\\n  (let ((name (factor-name-token-by-id names name-id)))\\n    (cond\\n      ((eq name :momentum-delta)\\n       `(:momentum-delta-header ,a ,b))\\n      ((eq name :momentum-delta-momentum)\\n       `(:momentum-delta-momentum\\n         ,(runtime-symbol-name (expression-builder-context builder) a)))\\n      ((eq name :eta-xi-zero-mode)\\n       `(:eta-xi-zero-mode ,(runtime-symbol-name (expression-builder-context builder) a)))\\n      ((eq name :bc-top-form)\\n       \'(:bc-top-form))\\n      (name `(,name))\\n      (t \'(:zero-mode)))))\\n\\n(defun factor-expression-values (builder names kind a b c d name-id)\\n  (case kind\\n    (4 (factor-scalar-expression-values builder names a b c d name-id))\\n    (5 (factor-coordinate-expression-values builder names a b d name-id))\\n    (6 (factor-tensor-expression-values builder names a b name-id))\\n    (7 (factor-zero-mode-expression-values builder names a b name-id))\\n    (8 `(:residual ,a))\\n    (otherwise 1)))\\n\\n(defun expression-name-vector (names count)\\n  (let ((tokens (make-array count)))\\n    (loop for index below count\\n          do (setf (aref tokens index)\\n                   (name-token (name-pointer-at names index))))\\n    tokens))\\n\\n(defun fold-expression-records (context)\\n  (let ((out-terms (foreign-alloc* :pointer))\\n        (out-term-count (foreign-alloc* :size))\\n        (out-factors (foreign-alloc* :pointer))\\n        (out-factor-count (foreign-alloc* :size))\\n        (out-names (foreign-alloc* :pointer))\\n        (out-name-count (foreign-alloc* :size))\\n        (builder (make-expression-builder context)))\\n    (unwind-protect\\n         (progn\\n           (check-abi\\n            (%abi-correlator-expression-records\\n             (runtime-handle context)\\n             out-terms out-term-count out-factors out-factor-count out-names out-name-count))\\n           (let ((terms (mem-ref* out-terms :pointer))\\n                 (term-count (mem-ref* out-term-count :size))\\n                 (factors (mem-ref* out-factors :pointer))\\n                 (factor-count (mem-ref* out-factor-count :size))\\n                 (names (mem-ref* out-names :pointer))\\n                 (name-count (mem-ref* out-name-count :size)))\\n             (unwind-protect\\n                  (let ((name-tokens (expression-name-vector names name-count)))\\n                    (loop for index below term-count\\n                          do (push (term-expression builder factors name-tokens\\n                                                    (term-pointer-at terms index))\\n                                   (expression-builder-terms builder))))\\n               (%abi-expression-buffer-free terms term-count factors factor-count names name-count)))\\n           (sum-expression (nreverse (expression-builder-terms builder))))\\n      (foreign-free* out-name-count)\\n      (foreign-free* out-names)\\n      (foreign-free* out-factor-count)\\n      (foreign-free* out-factors)\\n      (foreign-free* out-term-count)\\n      (foreign-free* out-terms))))\\n\\n(defun correlator-expression (context frozen)\\n  (declare (ignore frozen))\\n  (fold-expression-records context))\\n\\n(defun collect-correlator (context frozen &key (limit 1024))\\n  (let ((events nil)\\n        (count 0))\\n    (run-correlator context frozen\\n                    (lambda (event)\\n                      (when (>= count limit)\\n                        (error \\"Result collection limit reached.\\"))\\n                      (incf count)\\n                      (push event events)))\\n    (nreverse events)))\\n","n":0.015}}},{"i":42,"$":{"1":{"v":"Lisp Generated Fixture Declarations","n":0.5},"2":{"v":"This node records the minimal Lisp-owned declarations for the generated\\nfixtures.  They use the generic emitter from\\nLisp Descriptor Emitter and add one REPL macro\\nthat owns runtime lifetime and returns the symbolic expression directly.\\n(defpackage #:string-code.cft.fixtures\\n  (:use #:cl #:string-code.cft.descriptor)\\n  (:export\\n   #:free-fermion-10\\n   #:eta-xi-sphere\\n   #:eta-xi-torus\\n   #:bc-sphere\\n   #:free-boson-10\\n   #:make-free-fermion-runtime\\n   #:psi\\n   #:make-eta-xi-sphere-runtime\\n   #:make-eta-xi-torus-runtime\\n   #:eta\\n   #:xi\\n   #:make-bc-runtime\\n   #:b-field\\n   #:c-field\\n   #:make-free-boson-runtime\\n   #:dX\\n   #:expX\\n   #:with-correlator-context\\n   #:correlator\\n   #:corr\\n   #:ops\\n   #:R))\\n\\n(in-package #:string-code.cft.fixtures)\\n\\n(defun runtime-symbol-id (context value)\\n  (etypecase value\\n    (integer value)\\n    (string (intern-runtime-symbol context value))\\n    (symbol (intern-runtime-symbol context (string-downcase (string value))))))\\nThe REPL macro layer rewrites user field forms into explicit runtime insertions.\\nops just groups forms; R groups forms and then asks Zig to normal-order the\\nnumber of fields in that group.  No symbolic expression is built before the\\noperator list is frozen.\\n(eval-when (:compile-toplevel :load-toplevel :execute)\\n  (defun fixture-call-symbol (name)\\n    (if (symbolp name)\\n        (multiple-value-bind (symbol status)\\n            (find-symbol (symbol-name name) \\"STRING-CODE.CFT.FIXTURES\\")\\n          (if status symbol name))\\n        name))\\n\\n  (defun literal-symbol-form (value)\\n    (if (symbolp value) `\',value value))\\n\\n  (defun fixture-form-name (form)\\n    (and (consp form) (symbolp (first form)) (first form)))\\n\\n  (defun field-form-p (form)\\n    (and (consp form)\\n         (not (member (fixture-form-name form) \'(ops R)\\n                      :test #\'string-equal))))\\n\\n  (defun field-call-form (context form)\\n    (unless (consp form)\\n      (error \\"Correlator field form must be a list, got ~S.\\" form))\\n    `(,(fixture-call-symbol (first form)) ,context\\n      ,@(mapcar #\'literal-symbol-form (rest form))))\\n\\n  (declaim (ftype function correlator-body-forms correlator-form-forms))\\n\\n  (defun insertion-count (form)\\n    (cond\\n      ((field-form-p form) 1)\\n      ((and (consp form) (string-equal (fixture-form-name form) \'ops))\\n       (loop for item in (rest form) sum (insertion-count item)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'R))\\n       (loop for item in (rest form) sum (insertion-count item)))\\n      (t (error \\"Unknown correlator form ~S.\\" form))))\\n\\n  (defun correlator-form-forms (context form)\\n    (cond\\n      ((field-form-p form) (list (field-call-form context form)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'ops))\\n       (correlator-body-forms context (rest form)))\\n      ((and (consp form) (string-equal (fixture-form-name form) \'R))\\n       (append (correlator-body-forms context (rest form))\\n               `((normal-order-runtime-field ,context ,(insertion-count form)))))\\n      (t (error \\"Unknown correlator form ~S.\\" form))))\\n\\n  (defun correlator-body-forms (context forms)\\n    (loop for form in forms append (correlator-form-forms context form))))\\n\\n(defmacro with-correlator-context ((context (maker &rest runtime-args)) &body body)\\n  \\"Bind CONTEXT to a generated runtime and release it after BODY.\\"\\n  `(let ((,context (,(fixture-call-symbol maker) ,@runtime-args)))\\n     (unwind-protect\\n          (progn ,@body)\\n       (destroy-runtime-context ,context))))\\n\\n(defmacro correlator ((maker &rest runtime-args) &body fields)\\n  \\"Evaluate fixture field forms and return the symbolic correlator expression.\\"\\n  (let ((context (gensym \\"CONTEXT-\\"))\\n        (frozen (gensym \\"FROZEN-\\")))\\n    `(with-correlator-context (,context (,(fixture-call-symbol maker) ,@runtime-args))\\n       ,@(correlator-body-forms context fields)\\n       (let ((,frozen (freeze-runtime-operators ,context)))\\n         (correlator-expression ,context ,frozen)))))\\n\\n(defmacro corr ((maker &rest runtime-args) &body fields)\\n  \\"Alias for CORRELATOR.\\"\\n  `(correlator (,maker ,@runtime-args) ,@fields))\\nPreset accessors return fresh descriptors for inspection.  Runtime constructors\\ncreate C ABI contexts for evaluation.\\n(defun free-fermion-10 ()\\n  (string-code.cft.presets:free-fermion-10))\\n\\n(defun eta-xi-sphere ()\\n  (string-code.cft.presets:eta-xi-sphere))\\n(defun eta-xi-torus ()\\n  (string-code.cft.presets:eta-xi-torus))\\n\\n(defun bc-sphere ()\\n  (string-code.cft.presets:bc-sphere))\\n(defun free-boson-10 ()\\n  (string-code.cft.presets:free-boson-10))\\nThe user-facing runtime constructors resolve generated theory ids through the\\npreset registry.  The numeric ids stay compiler-owned.\\n(defun make-preset-runtime (name library &key constants)\\n  (make-runtime-context\\n   :library library\\n   :theory-id (string-code.cft.presets:preset-theory-id-for name)\\n   :constants constants))\\n\\n(defun make-free-fermion-runtime (&key library)\\n  (make-preset-runtime \'free-fermion-10 library))\\n\\n(defun psi (context mu z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        (list (runtime-symbol-id context mu))))\\n\\n(defun make-eta-xi-sphere-runtime (&key library)\\n  (make-preset-runtime \'eta-xi-sphere library))\\n\\n(defun make-eta-xi-torus-runtime (&key library)\\n  (make-preset-runtime \'eta-xi-torus library))\\n\\n(defun eta (context z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun xi (context z)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n(defun make-bc-runtime (&key library)\\n  (make-preset-runtime \'bc-sphere library))\\n\\n(defun b-field (context z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun c-field (context z)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z))\\n                        nil))\\n\\n(defun free-boson-constants (alpha-prime constants)\\n  (acons :alpha-prime alpha-prime constants))\\n\\n(defun make-free-boson-runtime (&key library (alpha-prime :alpha-prime) constants)\\n  (make-preset-runtime \'free-boson-10 library\\n                       :constants (free-boson-constants alpha-prime constants)))\\n\\n(defun dX (context mu z)\\n  (insert-runtime-field context 0\\n                        (list (runtime-symbol-id context z))\\n                        (list (runtime-symbol-id context mu))))\\n\\n(defun expX (context k z z-bar)\\n  (insert-runtime-field context 1\\n                        (list (runtime-symbol-id context z)\\n                              (runtime-symbol-id context z-bar))\\n                        (list (runtime-symbol-id context k))))\\n","n":0.042}}},{"i":43,"$":{"1":{"v":"Generated Fixture ABI","n":0.577},"2":{"v":"This node exposes the smallest C ABI for the descriptor fixtures emitted by\\nLisp Preset Compiler.  The exported entrypoints are\\ntheory-generic: a theory id selects the generated descriptor instance, and field\\nids distinguish runtime insertions.\\nconst std = @import(\\"std\\");\\nconst descriptor = @import(\\"correlators/descriptor.zig\\");\\nconst dispatch = @import(\\"correlators/generated_dispatch.zig\\");\\nconst kernel = @import(\\"kernel.zig\\");\\n\\nconst allocator = std.heap.c_allocator;\\nconst TheoryId = dispatch.TheoryId;\\nconst ContextTag = dispatch.ContextTag;\\n\\n/// Context is an opaque generated-theory runtime handle for the C ABI.\\nconst Context = struct {\\n    inner: ContextTag,\\n    frozen: ?kernel.Call.MultiOp = null,\\n};\\nThe ABI event is a stable POD record.  String names are borrowed for the\\nduration of the callback and copied by Lisp only when it collects events.\\n/// Event is one stable C ABI result event.\\npub const Event = extern struct {\\n    kind: u8,\\n    a: u32,\\n    b: u32,\\n    c: u32,\\n    d: i32,\\n    name_ptr: ?[*]const u8,\\n    name_len: usize,\\n};\\n\\n/// ExpressionTerm is one product in the expanded symbolic sum.\\npub const ExpressionTerm = extern struct {\\n    first_factor: usize,\\n    factor_count: usize,\\n};\\n\\n/// ExpressionFactor is one nontrivial symbolic product factor.\\npub const ExpressionFactor = extern struct {\\n    kind: u8,\\n    a: u32,\\n    b: u32,\\n    c: u32,\\n    d: i32,\\n    name_id: u32,\\n};\\n\\n/// ExpressionName is one borrowed symbolic name used by expression factors.\\npub const ExpressionName = extern struct {\\n    ptr: ?[*]const u8,\\n    len: usize,\\n};\\n\\n/// EventCallback receives one streamed result event.\\npub const EventCallback = *const fn (?*anyopaque, *const Event) callconv(.c) c_int;\\n\\n/// EventChunkCallback receives a bounded chunk of streamed result events.\\npub const EventChunkCallback = *const fn (?*anyopaque, [*]const Event, usize) callconv(.c) c_int;\\n\\nconst StreamState = struct {\\n    payload: ?*anyopaque,\\n    callback: EventCallback,\\n};\\n\\nconst event_chunk_capacity = 256;\\n\\nconst BufferedStreamState = struct {\\n    payload: ?*anyopaque,\\n    callback: EventChunkCallback,\\n    events: [event_chunk_capacity]Event = undefined,\\n    len: usize = 0,\\n\\n    fn flush(self: *@This()) !void {\\n        if (self.len == 0) return;\\n        if (self.callback(self.payload, &self.events, self.len) != 0) return error.CallbackFailed;\\n        self.len = 0;\\n    }\\n\\n    fn push(self: *@This(), event: Event) !void {\\n        self.events[self.len] = event;\\n        self.len += 1;\\n        if (self.len == self.events.len) try self.flush();\\n    }\\n};\\n\\nfn abiEvent(event: descriptor.ResultEvent) Event {\\n    const name_ptr: ?[*]const u8 = if (event.name) |name| name.ptr else null;\\n    const name_len: usize = if (event.name) |name| name.len else 0;\\n    return .{\\n        .kind = @intFromEnum(event.kind),\\n        .a = event.a,\\n        .b = event.b,\\n        .c = event.c,\\n        .d = event.d,\\n        .name_ptr = name_ptr,\\n        .name_len = name_len,\\n    };\\n}\\n\\nfn streamThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *StreamState = @ptrCast(@alignCast(payload));\\n    const stable_event = abiEvent(event);\\n    if (state.callback(state.payload, &stable_event) != 0) return error.CallbackFailed;\\n}\\n\\nfn bufferedStreamThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *BufferedStreamState = @ptrCast(@alignCast(payload));\\n    try state.push(abiEvent(event));\\n}\\nExpression records are the fast REPL path.  Zig still streams the correlator\\nonce, but this state stores only term ranges, nontrivial factors, and interned\\nborrowed names.  Lisp builds cons expressions from these compact rows after the\\nkernel returns.\\nconst NameKey = struct {\\n    ptr: usize,\\n    len: usize,\\n};\\n\\nconst ExpressionRecordState = struct {\\n    terms: std.ArrayList(ExpressionTerm) = .empty,\\n    factors: std.ArrayList(ExpressionFactor) = .empty,\\n    names: std.ArrayList(ExpressionName) = .empty,\\n    name_ids: std.AutoHashMap(NameKey, u32) = std.AutoHashMap(NameKey, u32).init(allocator),\\n    term_start: usize = 0,\\n\\n    fn deinit(self: *@This()) void {\\n        self.terms.deinit(allocator);\\n        self.factors.deinit(allocator);\\n        self.names.deinit(allocator);\\n        self.name_ids.deinit();\\n    }\\n\\n    fn nameId(self: *@This(), maybe_name: ?[]const u8) !u32 {\\n        const name = maybe_name orelse return 0;\\n        const key = NameKey{ .ptr = @intFromPtr(name.ptr), .len = name.len };\\n        if (self.name_ids.get(key)) |id| return id;\\n        const id: u32 = @intCast(self.names.items.len + 1);\\n        try self.names.append(allocator, .{ .ptr = name.ptr, .len = name.len });\\n        try self.name_ids.put(key, id);\\n        return id;\\n    }\\n\\n    fn finishTerm(self: *@This()) !void {\\n        try self.terms.append(allocator, .{\\n            .first_factor = self.term_start,\\n            .factor_count = self.factors.items.len - self.term_start,\\n        });\\n        self.term_start = self.factors.items.len;\\n    }\\n\\n    fn pushFactor(self: *@This(), event: descriptor.ResultEvent) !void {\\n        if (event.kind == .scalar and event.d == 0 and event.name == null) return;\\n        try self.factors.append(allocator, .{\\n            .kind = @intFromEnum(event.kind),\\n            .a = event.a,\\n            .b = event.b,\\n            .c = event.c,\\n            .d = event.d,\\n            .name_id = try self.nameId(event.name),\\n        });\\n    }\\n};\\n\\nfn expressionRecordThunk(payload: *anyopaque, event: descriptor.ResultEvent) !void {\\n    const state: *ExpressionRecordState = @ptrCast(@alignCast(payload));\\n    switch (event.kind) {\\n        .sum_term_begin => state.term_start = state.factors.items.len,\\n        .sum_term_end => try state.finishTerm(),\\n        .wick_term_begin, .wick_term_end => {},\\n        .scalar, .coordinate, .tensor, .zero_mode, .residual_operator => try state.pushFactor(event),\\n    }\\n}\\nErrors are reported by a process-local short buffer.  The ABI does not allocate\\nerrors or throw across the C boundary.\\nvar last_error_storage: [160]u8 = [_]u8{0} ** 160;\\n\\nfn clearError() void {\\n    last_error_storage[0] = 0;\\n}\\n\\nfn setErrorName(name: []const u8) c_int {\\n    const len = @min(name.len, last_error_storage.len - 1);\\n    @memcpy(last_error_storage[0..len], name[0..len]);\\n    last_error_storage[len] = 0;\\n    return -1;\\n}\\n\\nfn setError(err: anyerror) c_int {\\n    return setErrorName(@errorName(err));\\n}\\n\\n/// sc_generated_last_error returns the last ABI error string.\\nexport fn sc_generated_last_error() [*:0]const u8 {\\n    return @ptrCast(&last_error_storage);\\n}\\nContext creation and destruction dispatch on generated theory ids.  The\\nentrypoints are generic; no theory-specific exported functions are needed.\\n/// sc_generated_descriptor_abi_version returns the descriptor ABI version.\\nexport fn sc_generated_descriptor_abi_version() u32 {\\n    return descriptor.descriptor_abi_version;\\n}\\n\\n/// sc_generated_theory_hash returns the generated theory hash for a theory id.\\nexport fn sc_generated_theory_hash(raw_theory: u32) u32 {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| {\\n        _ = setError(err);\\n        return 0;\\n    };\\n    return dispatch.theoryHash(id);\\n}\\n\\n/// sc_generated_scalar_atom_name returns a descriptor parameter name for an atom id.\\nexport fn sc_generated_scalar_atom_name(raw_theory: u32, atom: u32, out_ptr: ?*?[*]const u8, out_len: ?*usize) c_int {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| return setError(err);\\n    const ptr = out_ptr orelse return setErrorName(\\"NullOutput\\");\\n    const len = out_len orelse return setErrorName(\\"NullOutput\\");\\n    const name = dispatch.scalarAtomParameterName(id, atom) orelse return setErrorName(\\"UnknownScalarAtom\\");\\n    ptr.* = name.ptr;\\n    len.* = name.len;\\n    return 0;\\n}\\n\\n/// sc_generated_context_create allocates a context for a generated theory id.\\nexport fn sc_generated_context_create(raw_theory: u32) ?*Context {\\n    clearError();\\n    const id = dispatch.theoryId(raw_theory) catch |err| {\\n        _ = setError(err);\\n        return null;\\n    };\\n    const ctx = allocator.create(Context) catch |err| {\\n        _ = setError(err);\\n        return null;\\n    };\\n    ctx.* = .{ .inner = ContextTag.create(id) catch |err| {\\n        allocator.destroy(ctx);\\n        _ = setError(err);\\n        return null;\\n    } };\\n    return ctx;\\n}\\n\\n/// sc_generated_context_destroy releases a generated context.\\nexport fn sc_generated_context_destroy(ctx: ?*Context) void {\\n    const handle = ctx orelse return;\\n    handle.inner.destroy();\\n    allocator.destroy(handle);\\n}\\nThe mutation entrypoints operate on compact ids and slices supplied by Lisp.\\nfn byteSlice(ptr: ?[*]const u8, len: usize) ![]const u8 {\\n    const data = ptr orelse return error.NullPointer;\\n    return data[0..len];\\n}\\n\\nfn u32Slice(ptr: ?[*]const u32, len: usize) ![]const u32 {\\n    if (len == 0) return &.{};\\n    const data = ptr orelse return error.NullPointer;\\n    return data[0..len];\\n}\\n\\n/// sc_generated_symbol_intern interns a runtime symbol in one context.\\nexport fn sc_generated_symbol_intern(ctx: ?*Context, name_ptr: ?[*]const u8, name_len: usize, out_symbol: ?*u32) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const out = out_symbol orelse return setErrorName(\\"NullOutput\\");\\n    const name = byteSlice(name_ptr, name_len) catch |err| return setError(err);\\n    out.* = handle.inner.symbolIntern(name) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_field_insert appends one generated field occurrence.\\nexport fn sc_generated_field_insert(ctx: ?*Context, field_id: u16, coords_ptr: ?[*]const u32, coord_len: usize, labels_ptr: ?[*]const u32, label_len: usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const coords = u32Slice(coords_ptr, coord_len) catch |err| return setError(err);\\n    const labels = u32Slice(labels_ptr, label_len) catch |err| return setError(err);\\n    handle.inner.fieldInsert(field_id, coords, labels) catch |err| return setError(err);\\n    handle.frozen = null;\\n    return 0;\\n}\\n\\n/// sc_generated_normal_ordering tags the last count fields as one normal product.\\nexport fn sc_generated_normal_ordering(ctx: ?*Context, count: usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    handle.inner.normalOrdering(count) catch |err| return setError(err);\\n    handle.frozen = null;\\n    return 0;\\n}\\n\\n/// sc_generated_operator_list_freeze freezes the current operator list.\\nexport fn sc_generated_operator_list_freeze(ctx: ?*Context) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    handle.frozen = handle.inner.freeze() catch |err| return setError(err);\\n    return 0;\\n}\\nCount and stream run against the last frozen operator list.  Count uses the\\ndescriptor count sink and does not allocate Lisp expression data.\\nfn frozen(handle: *Context) !kernel.Call.MultiOp {\\n    return handle.frozen orelse error.OperatorListNotFrozen;\\n}\\n\\n/// sc_generated_correlator_count returns the number of accepted branches.\\nexport fn sc_generated_correlator_count(ctx: ?*Context, out_count: ?*usize) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const out = out_count orelse return setErrorName(\\"NullOutput\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    out.* = handle.inner.count(ops) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_correlator_run streams result events to a callback.\\nexport fn sc_generated_correlator_run(ctx: ?*Context, payload: ?*anyopaque, callback: ?EventCallback) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const cb = callback orelse return setErrorName(\\"NullCallback\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = StreamState{ .payload = payload, .callback = cb };\\n    handle.inner.run(ops, &state, streamThunk) catch |err| return setError(err);\\n    return 0;\\n}\\n\\n/// sc_generated_correlator_run_buffered streams bounded event chunks.\\nexport fn sc_generated_correlator_run_buffered(ctx: ?*Context, payload: ?*anyopaque, callback: ?EventChunkCallback) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const cb = callback orelse return setErrorName(\\"NullCallback\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = BufferedStreamState{ .payload = payload, .callback = cb };\\n    handle.inner.run(ops, &state, bufferedStreamThunk) catch |err| return setError(err);\\n    state.flush() catch |err| return setError(err);\\n    return 0;\\n}\\nThe expression-record export owns the temporary arrays until Lisp has copied or\\nfolded them.  On every allocation failure it frees earlier arrays before\\nreturning an ABI error.\\n/// sc_generated_correlator_expression_records returns compact expression terms and factors.\\nexport fn sc_generated_correlator_expression_records(\\n    ctx: ?*Context,\\n    out_terms: ?*[*]ExpressionTerm,\\n    out_term_count: ?*usize,\\n    out_factors: ?*[*]ExpressionFactor,\\n    out_factor_count: ?*usize,\\n    out_names: ?*[*]ExpressionName,\\n    out_name_count: ?*usize,\\n) c_int {\\n    clearError();\\n    const handle = ctx orelse return setErrorName(\\"NullContext\\");\\n    const terms_out = out_terms orelse return setErrorName(\\"NullOutput\\");\\n    const term_count_out = out_term_count orelse return setErrorName(\\"NullOutput\\");\\n    const factors_out = out_factors orelse return setErrorName(\\"NullOutput\\");\\n    const factor_count_out = out_factor_count orelse return setErrorName(\\"NullOutput\\");\\n    const names_out = out_names orelse return setErrorName(\\"NullOutput\\");\\n    const name_count_out = out_name_count orelse return setErrorName(\\"NullOutput\\");\\n    const ops = frozen(handle) catch |err| return setError(err);\\n    var state = ExpressionRecordState{};\\n    defer state.deinit();\\n    handle.inner.run(ops, &state, expressionRecordThunk) catch |err| return setError(err);\\n    const factors = state.factors.toOwnedSlice(allocator) catch |err| return setError(err);\\n    const terms = state.terms.toOwnedSlice(allocator) catch |err| {\\n        allocator.free(factors);\\n        return setError(err);\\n    };\\n    const names = state.names.toOwnedSlice(allocator) catch |err| {\\n        allocator.free(terms);\\n        allocator.free(factors);\\n        return setError(err);\\n    };\\n    terms_out.* = terms.ptr;\\n    term_count_out.* = terms.len;\\n    factors_out.* = factors.ptr;\\n    factor_count_out.* = factors.len;\\n    names_out.* = names.ptr;\\n    name_count_out.* = names.len;\\n    return 0;\\n}\\n\\n/// sc_generated_expression_buffer_free releases compact expression buffers.\\nexport fn sc_generated_expression_buffer_free(\\n    terms: ?[*]ExpressionTerm,\\n    term_count: usize,\\n    factors: ?[*]ExpressionFactor,\\n    factor_count: usize,\\n    names: ?[*]ExpressionName,\\n    name_count: usize,\\n) void {\\n    if (terms) |ptr| allocator.free(ptr[0..term_count]);\\n    if (factors) |ptr| allocator.free(ptr[0..factor_count]);\\n    if (names) |ptr| allocator.free(ptr[0..name_count]);\\n}\\nThe tests exercise the ABI on one tensor/coordinate path and one count path.\\nconst AbiRecorder = struct {\\n    count: usize = 0,\\n    saw_tensor: bool = false,\\n    saw_coordinate: bool = false,\\n\\n    fn push(payload: ?*anyopaque, event_ptr: *const Event) callconv(.c) c_int {\\n        const self: *AbiRecorder = @ptrCast(@alignCast(payload.?));\\n        const event = event_ptr.*;\\n        self.count += 1;\\n        if (event.kind == @intFromEnum(descriptor.ResultEventKind.tensor) and event.a == 1 and event.b == 2) {\\n            self.saw_tensor = true;\\n        }\\n        if (event.kind == @intFromEnum(descriptor.ResultEventKind.coordinate) and event.a == 3 and event.b == 4) {\\n            self.saw_coordinate = true;\\n        }\\n        return 0;\\n    }\\n};\\n\\ntest \\"generated C ABI counts and streams free-fermion events\\" {\\n    try selfTest();\\n}\\n\\n/// selfTest runs the generated C ABI fixture invariants.\\npub fn selfTest() !void {\\n    const ctx = sc_generated_context_create(@intFromEnum(dispatch.first_theory_id)) orelse return error.ContextCreateFailed;\\n    defer sc_generated_context_destroy(ctx);\\n\\n    var mu: u32 = 0;\\n    var nu: u32 = 0;\\n    var z: u32 = 0;\\n    var w: u32 = 0;\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"mu\\".ptr, 2, &mu));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"nu\\".ptr, 2, &nu));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"z\\".ptr, 1, &z));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_symbol_intern(ctx, \\"w\\".ptr, 1, &w));\\n\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_field_insert(ctx, 0, &.{z}, 1, &.{mu}, 1));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_field_insert(ctx, 0, &.{w}, 1, &.{nu}, 1));\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_operator_list_freeze(ctx));\\n\\n    var count: usize = 0;\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_correlator_count(ctx, &count));\\n    try std.testing.expectEqual(@as(usize, 1), count);\\n\\n    var recorder = AbiRecorder{};\\n    try std.testing.expectEqual(@as(c_int, 0), sc_generated_correlator_run(ctx, &recorder, AbiRecorder.push));\\n    try std.testing.expect(recorder.saw_tensor);\\n    try std.testing.expect(recorder.saw_coordinate);\\n}\\n","n":0.024}}},{"i":44,"$":{"1":{"v":"Lisp CFT System","n":0.577},"2":{"v":"The Lisp system loads descriptor emission first, then preset declarations, then\\nthe REPL fixture helpers that insert fields through the generated C ABI.\\n(defsystem \\"string-code-cft\\"\\n  :description \\"Lisp CFT presets, descriptor emission, and generated C ABI runtime helpers.\\"\\n  :serial t\\n  :depends-on (\\"uiop\\")\\n  :components ((:file \\"string-code-cft-descriptor\\")\\n               (:file \\"string-code-cft-presets\\")\\n               (:module \\"presets\\"\\n                :serial t\\n                :components\\n                ((:file \\"free-fermion-10\\")\\n                 (:file \\"eta-xi\\")\\n                 (:file \\"bc-sphere\\")\\n                 (:file \\"free-boson-10\\")))\\n               (:file \\"string-code-cft-fixtures\\")))\\n","n":0.13}}},{"i":45,"$":{"1":{"v":"Lisp Preset Compiler","n":0.577},"2":{"v":"This node lowers compact Lisp CFT presets to descriptor rows and emits the Zig\\nfixture and dispatch modules imported by the generated C ABI.  It does not evaluate\\ncorrelators and it does not own REPL insertion helpers.\\nPackageThe public surface is deliberately small: define presets, inspect one lowered\\ntheory, emit generated Zig sources, or compile the fixed generated ABI library.\\n(eval-when (:compile-toplevel :load-toplevel :execute)\\n  (require :asdf))\\n\\n(defpackage #:string-code.cft.presets\\n  (:use #:cl #:string-code.cft.descriptor)\\n  (:export\\n   #:define-cft-preset\\n   #:preset-theory\\n   #:preset-theory-id-for\\n   #:write-generated-fixtures\\n   #:write-generated-sources\\n   #:compile-preset-library\\n   #:free-fermion-10\\n   #:eta-xi-sphere\\n   #:eta-xi-torus\\n   #:bc-sphere\\n   #:free-boson-10))\\n\\n(in-package #:string-code.cft.presets)\\nDescriptor LayersThe preset path has four layers, and only the first one is user-facing:\\nThe preset file declares CFT data: parameters, surfaces, fields, Wick\\n  contractions, and zero modes.\\nThe Lisp descriptor rows are compact normalized data for Zig: symbols,\\n  surfaces, fields, metadata, Wick terms, and zero-mode rows.\\nThe generated Zig fixture needs internal names: table prefixes, descriptor\\n  constants, generated theory type names, and stable theory ids.\\nThe C ABI receives a theory id at runtime so the REPL can select a compiled\\n  generated theory.\\nThe generated names and ids are compiler data.  They are kept here, not in\\npreset files.\\nRegistryThe registry stores compile-time identity for the generated ABI.  The builder is\\na function so users can inspect a fresh descriptor before compiling.\\n(defstruct preset name builder theory-name hash theory-id)\\n(defstruct field-shape id insertion labels)\\n\\n(defparameter *presets* (make-hash-table))\\n(defparameter *next-theory-id* 1)\\nPreset names generate theory names, type names, descriptor prefixes, and theory\\nids.  Numeric suffixes such as -10 are treated as dimension labels and are\\nnot part of generated Zig type names.  Surface suffixes are kept only when a\\nfamily has more than one generated surface.\\n(defun preset-options-form-p (form)\\n  (and (consp form) (keywordp (first form))))\\n\\n(defun split-name-parts (name)\\n  (let ((text (string-downcase (string name)))\\n        (start 0)\\n        (parts nil))\\n    (loop for index from 0 below (length text)\\n          when (char= (aref text index) #\\\\-)\\n            do (progn\\n                 (when (< start index)\\n                   (push (subseq text start index) parts))\\n                 (setf start (1+ index))))\\n    (when (< start (length text))\\n      (push (subseq text start) parts))\\n    (nreverse parts)))\\n(defun numeric-name-part-p (part)\\n  (and (> (length part) 0)\\n       (loop for char across part always (digit-char-p char))))\\n\\n(defun surface-name-part-p (part)\\n  (member part \'(\\"sphere\\" \\"torus\\" \\"disk\\") :test #\'string=))\\n\\n(defun drop-trailing-numeric-parts (parts)\\n  (loop while (and parts (numeric-name-part-p (car (last parts))))\\n        do (setf parts (butlast parts)))\\n  parts)\\n\\n(defun drop-trailing-surface-part (parts)\\n  (if (and parts (surface-name-part-p (car (last parts))))\\n      (butlast parts)\\n      parts))\\n(defun join-name-parts (parts separator)\\n  (with-output-to-string (stream)\\n    (loop for part in parts\\n          for first = t then nil\\n          do (progn\\n               (unless first\\n                 (write-string separator stream))\\n               (write-string part stream)))))\\n\\n(defun generated-theory-name (name)\\n  (join-name-parts (drop-trailing-surface-part (split-name-parts name)) \\"-\\"))\\nTheory ids are assigned in preset load order.  Hashes are generated from the\\ncanonical theory name and declaration forms.\\n(defun next-theory-id-form ()\\n  (prog1 *next-theory-id*\\n    (incf *next-theory-id*)))\\n\\n(defun hash-byte (hash byte)\\n  (logand #xffffffff (* #x01000193 (logxor hash byte))))\\n\\n(defun generated-theory-hash (name forms)\\n  (let ((hash #x811c9dc5)\\n        (text (with-output-to-string (stream)\\n                (prin1 name stream)\\n                (prin1 forms stream))))\\n    (loop for char across text\\n          do (setf hash (hash-byte hash (char-code char))))\\n    hash))\\nThe macro accepts optional metadata for experiments, but built-in preset files\\ndo not need ABI headers.\\n(defmacro define-cft-preset (name &body body)\\n  \\"Define NAME as a descriptor preset from CFT declaration forms.\\"\\n  (let* ((user-options (if (preset-options-form-p (first body)) (first body) nil))\\n         (forms (if user-options (rest body) body))\\n         (theory-name (or (getf user-options :name)\\n                          (generated-theory-name name)))\\n         (hash (or (getf user-options :hash)\\n                   (generated-theory-hash theory-name forms)))\\n         (theory-id (or (getf user-options :theory-id)\\n                        (next-theory-id-form))))\\n    `(progn\\n       (defun ,name ()\\n         (build-cft-preset ,theory-name ,hash \',forms))\\n       (setf (gethash \',name *presets*)\\n             (make-preset :name \',name\\n                          :builder #\',name\\n                          :theory-name ,theory-name\\n                          :hash ,hash\\n                          :theory-id ,theory-id))\\n       \',name)))\\nTablesPreset declarations resolve names to compact descriptor ids.  Fields and labels\\nuse normalized string keys so case differences in Lisp symbols do not leak into\\nthe descriptor.\\n(defun table-get (table key label)\\n  (multiple-value-bind (value found) (gethash key table)\\n    (unless found\\n      (error \\"Unknown ~A ~S.\\" label key))\\n    value))\\n\\n(defun option-value (items key &optional default)\\n  (let ((tail (member key items)))\\n    (if tail (second tail) default)))\\n(defun field-key (name)\\n  (string-downcase (string name)))\\n\\n(defun label-key (field label)\\n  (list (field-key field) (string-downcase (string label))))\\nField labels must be visible before weight metadata is parsed, because weights\\nmay refer to the field being declared.\\n(defun register-field-shape (fields labels field labels-spec)\\n  (let ((field-name (field-key field))\\n        (field-id (hash-table-count fields)))\\n    (setf (gethash field-name fields)\\n          (make-field-shape :id field-id :insertion nil :labels labels-spec))\\n    (loop for label in labels-spec\\n          for slot from 0\\n          do (setf (gethash (label-key field (first label)) labels) slot))\\n    field-id))\\n\\n(defun set-field-shape-insertion (fields field insertion)\\n  (setf (field-shape-insertion (table-get fields (field-key field) \\"field\\"))\\n        insertion))\\nReferencesReference parsers convert preset names into descriptor ids.  Integer ids are\\naccepted only as an escape hatch for generated code.\\n(defun parse-parameter-ref (parameters value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get parameters value \\"parameter\\"))))\\n\\n(defun parse-quantum-number-ref (quantum-numbers value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get quantum-numbers value \\"quantum number\\"))))\\n\\n(defun parse-quantum-number-kind (kind)\\n  (ecase kind\\n    ((:u1 :u1-charge u1 u1-charge) :u1-charge)\\n    ((:ade-irrep ade-irrep) :ade-irrep)))\\nQuantum-number values are deliberately scalar.  U(1) charges lower to exact\\nintegers or rationals; representation-valued numbers lower to symbol ids.  The\\nZig descriptor validator checks that the value kind matches the declared\\nquantum-number kind.\\n(defun parse-field-ref (fields value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (field-shape-id (table-get fields (field-key value) \\"field\\")))))\\n\\n(defun parse-quantum-value (theory value)\\n  (etypecase value\\n    (integer value)\\n    (rational value)\\n    (string `(:symbol ,(add-symbol theory value)))\\n    (symbol `(:symbol ,(add-symbol theory (string-downcase (string value)))))\\n    (cons\\n     (ecase (first value)\\n       ((:rational rational)\\n        `(:rational ,(second value) ,(third value)))))))\\n\\n(defun parse-field-quantum-numbers (theory quantum-numbers field-id specs)\\n  (dolist (spec specs)\\n    (destructuring-bind (name value) spec\\n      (add-field-quantum-number theory\\n                                field-id\\n                                (parse-quantum-number-ref quantum-numbers name)\\n                                (parse-quantum-value theory value)))))\\n\\n(defun parse-label-ref (labels field value)\\n  (etypecase value\\n    (integer value)\\n    (symbol (table-get labels (label-key field value) \\"field label\\"))))\\nMetadataMetadata forms describe field weights and similar declaration-time expressions.\\nThey lower to the compact descriptor metadata language.\\n(defun parse-metadata-form (theory parameters fields labels form)\\n  (etypecase form\\n    (integer form)\\n    (cons\\n     (ecase (first form)\\n       ((:rational rational)\\n        `(:rational ,(second form) ,(third form)))\\n       ((:parameter parameter)\\n        `(:parameter ,(parse-parameter-ref parameters (second form))))\\n       ((:field-label field-label)\\n        `(:field-label ,(parse-field-ref fields (second form))\\n                       ,(parse-label-ref labels (second form) (third form))))\\n       ((:add add +)\\n        `(:add ,(parse-metadata-id theory parameters fields labels (second form))\\n               ,(parse-metadata-id theory parameters fields labels (third form))))\\n       ((:mul mul *)\\n        `(:mul ,(parse-metadata-id theory parameters fields labels (second form))\\n               ,(parse-metadata-id theory parameters fields labels (third form))))\\n       ((:bilinear bilinear dot)\\n        `(:bilinear ,(parse-metadata-id theory parameters fields labels (second form))\\n                    ,(parse-metadata-id theory parameters fields labels (third form))\\n                    ,(fourth form)))))))\\nThe descriptor builder interns duplicate metadata rows, so repeated symbols\\nsuch as \\\\(k\\\\) in \\\\(k \\\\cdot k\\\\) do not allocate duplicate descriptor nodes.\\n(defun parse-metadata-id (theory parameters fields labels form)\\n  (add-metadata theory (parse-metadata-form theory parameters fields labels form)))\\nWick TermsScalar factors are user-facing products of exact rationals, i, and named\\nparameters.  The parser lowers common products to the compact descriptor tags\\nused by Zig.\\n(defun i-symbol-p (value)\\n  (and (symbolp value) (string-equal value \'i)))\\n\\n(defun rational-factor (value)\\n  (cond\\n    ((integerp value) value)\\n    ((rationalp value) value)\\n    ((and (consp value) (member (first value) \'(:rational rational)))\\n     (/ (second value) (third value)))\\n    (t nil)))\\nThe product parser is intentionally narrow.  Unsupported scalar forms fail at\\npreset compile time instead of leaking into the generated descriptor.\\n(defun scalar-product-factors (form)\\n  (if (and (consp form) (member (first form) \'(* :mul mul)))\\n      (rest form)\\n      (list form)))\\n\\n(defun scalar-factor-product (parameters form)\\n  (let ((coefficient 1)\\n        (imaginary-power 0)\\n        (parameter nil))\\n    (dolist (factor (scalar-product-factors form))\\n      (let ((rational (rational-factor factor)))\\n        (cond\\n          (rational\\n           (setf coefficient (* coefficient rational)))\\n          ((i-symbol-p factor)\\n           (setf imaginary-power (mod (1+ imaginary-power) 4)))\\n          ((and (consp factor) (member (first factor) \'(:parameter parameter)))\\n           (when parameter\\n             (error \\"Scalar factor has more than one parameter: ~S.\\" form))\\n           (setf parameter (parse-parameter-ref parameters (second factor))))\\n          ((symbolp factor)\\n           (when parameter\\n             (error \\"Scalar factor has more than one parameter: ~S.\\" form))\\n           (setf parameter (parse-parameter-ref parameters factor)))\\n          (t\\n           (error \\"Unsupported scalar factor ~S in ~S.\\" factor form)))))\\n    (values coefficient imaginary-power parameter)))\\nThe final lowering preserves the compact descriptor format without exposing it\\nto preset authors.\\n(defun parse-scalar-factor (parameters form)\\n  (if (eq form :one)\\n      :one\\n      (multiple-value-bind (coefficient imaginary-power parameter)\\n          (scalar-factor-product parameters form)\\n        (cond\\n          ((and (null parameter) (zerop imaginary-power) (= coefficient 1))\\n           :one)\\n          ((and (null parameter) (zerop imaginary-power))\\n           `(:rational ,(numerator coefficient) ,(denominator coefficient)))\\n          ((and parameter (zerop imaginary-power) (= coefficient 1))\\n           `(:parameter ,parameter))\\n          ((and parameter (zerop imaginary-power) (= coefficient 1/2))\\n           `(:parameter-half ,parameter))\\n          ((and parameter (zerop imaginary-power) (= coefficient -1/2))\\n           `(:neg-parameter-half ,parameter))\\n          ((and parameter (= imaginary-power 1) (= coefficient -1/2))\\n           `(:neg-i-parameter-half ,parameter))\\n          (t\\n           (error \\"Scalar factor cannot be lowered to the current compact descriptor: ~S.\\"\\n                  form))))))\\nCoordinate factors are already descriptor-shaped.  The parser only normalizes\\noperator names and preserves coordinate slots.\\n(defun parse-coordinate-factor (form)\\n  (ecase (first form)\\n    ((:difference-power difference-power)\\n     `(:difference-power ,(second form)\\n                         ,(third form)\\n                         ,(fourth form)\\n                         ,@(cddddr form)))\\n    ((:named-kernel named-kernel)\\n     `(:named-kernel ,(second form)\\n                     ,(third form)\\n                     ,(fourth form)\\n                     ,@(cddddr form)))\\n    ((:green-exponential green-exponential)\\n     `(:green-exponential ,(second form)\\n                          ,(third form)))))\\nTensor factors need the current Wick pair because (:left mu) and (:right k)\\nrefer to labels on different fields.\\n(defun parse-side-label-ref (labels left-field right-field ref)\\n  (destructuring-bind (side value) ref\\n    (ecase side\\n      (:left `(:left ,(parse-label-ref labels left-field value)))\\n      (:right `(:right ,(parse-label-ref labels right-field value))))))\\n(defun parse-tensor-factor (labels left-field right-field form)\\n  (ecase (first form)\\n    ((:metric metric)\\n     `(:metric ,(parse-side-label-ref labels left-field right-field (second form))\\n               ,(parse-side-label-ref labels left-field right-field (third form))))\\n    ((:momentum-index momentum-index)\\n     `(:momentum-index ,(parse-side-label-ref labels left-field right-field (second form))\\n                       ,(parse-side-label-ref labels left-field right-field (third form))))\\n    ((:momentum-pair momentum-pair)\\n     `(:momentum-pair ,(parse-side-label-ref labels left-field right-field (second form))\\n                      ,(parse-side-label-ref labels left-field right-field (third form))))))\\nThe high-level Wick syntax binds field-call arguments to endpoint-local label\\nand coordinate refs.  Single fields have one position coordinate; pair fields\\nuse holomorphic and antiholomorphic slots.\\n(defun insertion-coordinate-slots (insertion)\\n  (ecase insertion\\n    (:single \'(:position))\\n    (:pair \'(:holomorphic :antiholomorphic))))\\n\\n(defun coordinate-ref-form (side slot)\\n  (if (eq slot :position)\\n      side\\n      (list side slot)))\\n(defun bind-field-call (fields side call label-env coordinate-env)\\n  (destructuring-bind (field &rest args) call\\n    (let* ((shape (table-get fields (field-key field) \\"field\\"))\\n           (label-count (length (field-shape-labels shape)))\\n           (slots (insertion-coordinate-slots (field-shape-insertion shape)))\\n           (labels (subseq args 0 label-count))\\n           (coordinates (subseq args label-count)))\\n      (unless (= (length coordinates) (length slots))\\n        (error \\"Field call ~S has ~D coordinate arguments; expected ~D.\\"\\n               call (length coordinates) (length slots)))\\n      (loop for label in labels\\n            for slot from 0\\n            do (setf (gethash label label-env) (list side slot)))\\n      (loop for coordinate in coordinates\\n            for slot in slots\\n            do (setf (gethash coordinate coordinate-env)\\n                     (coordinate-ref-form side slot)))\\n      field)))\\nExpression lowering is intentionally small: products, coordinate differences,\\nnamed coordinate kernels, Green exponentials, and the supported tensor\\nstructures.\\n(defun env-ref (env name label)\\n  (multiple-value-bind (value found) (gethash name env)\\n    (unless found\\n      (error \\"Unknown ~A variable ~S.\\" label name))\\n    value))\\n\\n(defun coordinate-ref-position-p (ref)\\n  (symbolp ref))\\n\\n(defun difference-form-p (form)\\n  (and (consp form) (member (first form) \'(- :difference difference))))\\n(defun lower-difference-power (coordinate-env difference exponent)\\n  (unless (and (difference-form-p difference)\\n               (= (length difference) 3))\\n    (error \\"Expected coordinate difference, got ~S.\\" difference))\\n  (let ((left (env-ref coordinate-env (second difference) \\"coordinate\\"))\\n        (right (env-ref coordinate-env (third difference) \\"coordinate\\")))\\n    `(:difference-power ,left ,right ,exponent\\n                        ,@(when (coordinate-ref-position-p left) \'(:derive-left))\\n                        ,@(when (coordinate-ref-position-p right) \'(:derive-right)))))\\n(defun named-coordinate-kernel-p (form coordinate-env)\\n  (and (consp form)\\n       (= (length form) 3)\\n       (symbolp (first form))\\n       (gethash (second form) coordinate-env)\\n       (gethash (third form) coordinate-env)))\\n\\n(defun parse-expression-coordinate (coordinate-env form)\\n  (cond\\n    ((and (consp form) (member (first form) \'(/ :div div))\\n          (= (length form) 3)\\n          (eql (second form) 1))\\n     (lower-difference-power coordinate-env (third form) -1))\\n    ((and (consp form) (member (first form) \'(pow expt))\\n          (= (length form) 3))\\n     (lower-difference-power coordinate-env (second form) (third form)))\\n    ((and (consp form) (member (first form) \'(green-exp green-exponential))\\n          (= (length form) 3))\\n     `(:green-exponential ,(env-ref coordinate-env (second form) \\"coordinate\\")\\n                          ,(env-ref coordinate-env (third form) \\"coordinate\\")))\\n    ((named-coordinate-kernel-p form coordinate-env)\\n     `(:named-kernel ,(first form)\\n                     ,(env-ref coordinate-env (second form) \\"coordinate\\")\\n                     ,(env-ref coordinate-env (third form) \\"coordinate\\")\\n                     :derive-left))\\n    (t nil)))\\n(defun parse-expression-tensor (label-env form)\\n  (when (consp form)\\n    (case (first form)\\n      ((metric)\\n       `(:metric ,(env-ref label-env (second form) \\"label\\")\\n                 ,(env-ref label-env (third form) \\"label\\")))\\n      ((momentum-index)\\n       `(:momentum-index ,(env-ref label-env (second form) \\"label\\")\\n                         ,(env-ref label-env (third form) \\"label\\")))\\n      ((momentum-pair)\\n       `(:momentum-pair ,(env-ref label-env (second form) \\"label\\")\\n                        ,(env-ref label-env (third form) \\"label\\"))))))\\nThe expression term collector classifies product factors and leaves all scalar\\nfactors together so (* -1/2 i alpha-prime) lowers as one scalar.\\n(defun expression-product-factors (form)\\n  (if (and (consp form) (member (first form) \'(* :mul mul)))\\n      (loop for factor in (rest form) append (expression-product-factors factor))\\n      (list form)))\\n\\n(defun parse-expression-term (parameters label-env coordinate-env expression residuals)\\n  (let ((scalar-factors nil)\\n        (coordinates nil)\\n        (tensors nil))\\n    (dolist (factor (expression-product-factors expression))\\n      (let ((coordinate (parse-expression-coordinate coordinate-env factor))\\n            (tensor (parse-expression-tensor label-env factor)))\\n        (cond\\n          (coordinate (push coordinate coordinates))\\n          (tensor (push tensor tensors))\\n          (t (push factor scalar-factors)))))\\n    (make-wick-term\\n     :scalars (list (parse-scalar-factor\\n                     parameters\\n                     (if scalar-factors\\n                         `(* ,@(reverse scalar-factors))\\n                         :one)))\\n     :coordinates (reverse coordinates)\\n     :tensors (reverse tensors)\\n     :residuals residuals)))\\nThe descriptor-shaped term form remains as an explicit escape hatch for\\ngenerated or debugging code.\\n(defun parse-term (parameters labels left-field right-field form)\\n  (unless (eq (first form) \'term)\\n    (error \\"Expected TERM form, got ~S.\\" form))\\n  (let ((body (rest form)))\\n    (make-wick-term\\n     :scalars (mapcar (lambda (item) (parse-scalar-factor parameters item))\\n                      (option-value body :scalars \'(:one)))\\n     :coordinates (mapcar #\'parse-coordinate-factor\\n                          (option-value body :coordinates nil))\\n     :tensors (mapcar (lambda (item)\\n                        (parse-tensor-factor labels left-field right-field item))\\n                      (option-value body :tensors nil))\\n     :residuals (option-value body :residuals nil))))\\nDeclarationsDeclaration forms append descriptor rows in source order.  Quantum-number\\nschema rows are separate from field quantum-number assignments so the later\\nbasis generator can scan them without touching Wick data.\\n(defun apply-preset-form\\n    (theory parameters quantum-numbers surfaces fields labels form)\\n  (ecase (first form)\\n    (parameter\\n     (setf (gethash (second form) parameters)\\n           (add-parameter theory (third form) (or (fourth form) :scalar-parameter))))\\n    (quantum-number\\n     (setf (gethash (second form) quantum-numbers)\\n           (add-quantum-number theory\\n                               (or (option-value (cdddr form) :symbol)\\n                                   (string-downcase (string (second form))))\\n                               (parse-quantum-number-kind (third form))\\n                               :group (option-value (cdddr form) :group))))\\n    (surface\\n     (setf (gethash (second form) surfaces)\\n           (add-surface theory (third form) (fourth form)\\n                        :modular-parameter\\n                        (when (option-value (cddddr form) :modular-parameter)\\n                          (parse-parameter-ref\\n                           parameters\\n                           (option-value (cddddr form) :modular-parameter))))))\\n    (field\\n     (destructuring-bind (id symbol insertion label-spec statistics &rest options) (rest form)\\n       (let* ((field-id (register-field-shape fields labels id label-spec))\\n              (weight-form (option-value options :weight))\\n              (anti-weight-form (option-value options :anti-weight))\\n              (weight-id (when weight-form\\n                           (parse-metadata-id theory parameters fields labels weight-form)))\\n              (anti-weight-id (cond\\n                                ((null anti-weight-form) nil)\\n                                ((equal anti-weight-form weight-form) weight-id)\\n                                (t (parse-metadata-id theory parameters fields labels\\n                                                      anti-weight-form)))))\\n         (set-field-shape-insertion fields id insertion)\\n         (unless (= field-id\\n                    (add-field theory symbol insertion label-spec statistics\\n                               :zero-mode (option-value options :zero-mode)\\n                               :weight weight-id\\n                               :anti-weight anti-weight-id))\\n           (error \\"Field table drift while declaring ~S.\\" id))\\n         (parse-field-quantum-numbers\\n          theory quantum-numbers field-id\\n          (option-value options :quantum-numbers nil)))))\\n    (wick\\n     (destructuring-bind (surface-id left &rest body) (rest form)\\n       (if (and body (consp (first body)) (not (eq (caar body) \'term)))\\n           (let* ((right (first body))\\n                  (expression (second body))\\n                  (options (cddr body))\\n                  (label-env (make-hash-table))\\n                  (coordinate-env (make-hash-table))\\n                  (left-field (bind-field-call fields :left left label-env coordinate-env))\\n                  (right-field (bind-field-call fields :right right label-env coordinate-env)))\\n             (add-wick-rule\\n              theory\\n              (table-get surfaces surface-id \\"surface\\")\\n              (parse-field-ref fields left-field)\\n              (parse-field-ref fields right-field)\\n              (list (parse-expression-term parameters label-env coordinate-env\\n                                           expression\\n                                           (option-value options :residuals nil)))))\\n           (destructuring-bind (left-field right-field) left\\n             (add-wick-rule\\n              theory\\n              (table-get surfaces surface-id \\"surface\\")\\n              (parse-field-ref fields left-field)\\n              (parse-field-ref fields right-field)\\n              (mapcar (lambda (term)\\n                        (parse-term parameters labels left-field right-field term))\\n                      body))))))\\n    (zero-mode\\n     (destructuring-bind (surface-id kind consumes &rest options) (rest form)\\n       (add-zero-mode theory\\n                      (table-get surfaces surface-id \\"surface\\")\\n                      kind\\n                      (mapcar (lambda (field) (parse-field-ref fields field)) consumes)\\n                      :normalization (option-value options :normalization :one)\\n                      :two-pi-power (option-value options :two-pi-power 0))))))\\nBuilding a preset uses only small name tables and returns the descriptor theory.\\n(defun build-cft-preset (name hash forms)\\n  (let ((theory (make-theory name hash))\\n        (parameters (make-hash-table))\\n        (quantum-numbers (make-hash-table))\\n        (surfaces (make-hash-table))\\n        (fields (make-hash-table :test #\'equal))\\n        (labels (make-hash-table :test #\'equal)))\\n    (dolist (form forms theory)\\n      (apply-preset-form\\n       theory parameters quantum-numbers surfaces fields labels form))))\\nFixture EmissionPresets are emitted in generated theory-id order.  The ids are assigned when\\nthe preset files load through ASDF.\\n(defun sorted-presets ()\\n  (sort (loop for preset being the hash-values of *presets*\\n              collect preset)\\n        #\'< :key #\'preset-theory-id))\\n(defun preset-name-parts (preset)\\n  (drop-trailing-numeric-parts (split-name-parts (preset-name preset))))\\n\\n(defun preset-family-parts (preset)\\n  (drop-trailing-surface-part (preset-name-parts preset)))\\n\\n(defun same-family-p (left right)\\n  (equal (preset-family-parts left) (preset-family-parts right)))\\n\\n(defun preset-family-count (preset presets)\\n  (loop for item in presets count (same-family-p preset item)))\\nGenerated Zig names are derived from the preset set.  A family with one surface\\ngets a short type such as Bc; a family with multiple surfaces keeps the\\nsurface suffix, such as EtaXiSphere.\\n(defun generated-name-parts (preset presets)\\n  (if (> (preset-family-count preset presets) 1)\\n      (preset-name-parts preset)\\n      (preset-family-parts preset)))\\n\\n(defun generated-prefix (preset presets)\\n  (join-name-parts (generated-name-parts preset presets) \\"_\\"))\\n\\n(defun generated-descriptor-name (preset presets)\\n  (format nil \\"~A_descriptor\\" (generated-prefix preset presets)))\\n\\n(defun capitalized-name-part (part)\\n  (string-capitalize part))\\n\\n(defun generated-type-name (preset presets)\\n  (with-output-to-string (stream)\\n    (dolist (part (generated-name-parts preset presets))\\n      (write-string (capitalized-name-part part) stream))))\\n(defun preset-key (name)\\n  (etypecase name\\n    (symbol\\n     (or (find-symbol (symbol-name name) (find-package \'#:string-code.cft.presets))\\n         name))\\n    (string\\n     (find-symbol (string-upcase name) (find-package \'#:string-code.cft.presets)))))\\n\\n(defun registered-preset (name)\\n  (table-get *presets* (preset-key name) \\"preset\\"))\\n\\n(defun preset-theory (name)\\n  \\"Build the descriptor theory declared by preset NAME.\\"\\n  (funcall (preset-builder (registered-preset name))))\\n\\n(defun preset-theory-id-for (name)\\n  \\"Return the generated ABI theory id for preset NAME.\\"\\n  (preset-theory-id (registered-preset name)))\\nThe prologue is the only Zig syntax that is not descriptor data.\\n(defun emit-fixture-prologue (stream)\\n  (format stream \\"const descriptor = @import(\\\\\\"descriptor.zig\\\\\\");~%~%\\")\\n  (format stream \\"const d = descriptor;~%~%\\")\\n  (format stream \\"fn cref(side: d.Side) d.CoordinateRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = .position };~%\\")\\n  (format stream \\"}~%~%\\")\\n  (format stream \\"fn bulk(side: d.Side, slot: d.CoordinateSlot) d.CoordinateRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = slot };~%\\")\\n  (format stream \\"}~%~%\\")\\n  (format stream \\"fn lref(side: d.Side, slot: d.Id) d.LabelRef {~%\\")\\n  (format stream \\"    return .{ .side = side, .slot = slot };~%\\")\\n  (format stream \\"}~%~%\\"))\\nEach preset becomes one descriptor plus one generated theory type.\\n(defun emit-fixture-type (stream preset presets)\\n  (emit-zig-descriptor (funcall (preset-builder preset)) stream\\n                       :prefix (generated-prefix preset presets)\\n                       :descriptor-name (generated-descriptor-name preset presets))\\n  (format stream \\"~%/// ~A is the lowered generated preset.~%\\"\\n          (generated-type-name preset presets))\\n  (format stream \\"pub const ~A = d.GeneratedTheory(~A);~%~%\\"\\n          (generated-type-name preset presets)\\n          (generated-descriptor-name preset presets)))\\nThe generated fixture self-test checks descriptor validity without allocating\\ncorrelator expressions.\\n(defun emit-fixture-self-test (stream presets)\\n  (format stream \\"/// selfTest validates that generated preset descriptors are loadable.~%\\")\\n  (format stream \\"pub fn selfTest() !void {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    try d.validateDescriptor(~A);~%\\"\\n            (generated-descriptor-name preset presets)))\\n  (format stream \\"}~%\\"))\\n(defun write-generated-fixtures\\n    (&key (path \\"src/cft-code/correlators/generated_fixtures.zig\\"))\\n  \\"Write the Zig fixture module from registered Lisp presets.\\"\\n  (with-open-file (stream path :direction :output :if-exists :supersede\\n                               :if-does-not-exist :create)\\n    (let ((presets (sorted-presets)))\\n      (emit-fixture-prologue stream)\\n      (dolist (preset presets)\\n        (emit-fixture-type stream preset presets))\\n      (emit-fixture-self-test stream presets)))\\n  path)\\nDispatch EmissionThe dispatch module is generated with the fixtures.  It owns TheoryId, the\\ncontext union, and every switch over generated theory types.\\n(defun emit-dispatch-prologue (stream)\\n  (format stream \\"const std = @import(\\\\\\"std\\\\\\");~%\\")\\n  (format stream \\"const fixtures = @import(\\\\\\"generated_fixtures.zig\\\\\\");~%\\")\\n  (format stream \\"const kernel = @import(\\\\\\"../kernel.zig\\\\\\");~%~%\\")\\n  (format stream \\"const allocator = std.heap.c_allocator;~%~%\\"))\\n\\n(defun emit-theory-id (stream presets)\\n  (format stream \\"/// TheoryId selects one generated fixture through the generic ABI.~%\\")\\n  (format stream \\"pub const TheoryId = enum(u32) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    ~A = ~D,~%\\"\\n            (generated-prefix preset presets)\\n            (preset-theory-id preset)))\\n  (format stream \\"};~%~%\\")\\n  (format stream \\"pub const first_theory_id = TheoryId.~A;~%~%\\"\\n          (generated-prefix (first presets) presets)))\\n(defun emit-context-fields (stream presets)\\n  (format stream \\"pub const ContextTag = union(TheoryId) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"    ~A: *fixtures.~A.Context,~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"~%\\"))\\nThe context methods are direct switches.  They do not allocate and expose only\\nthe operations the C ABI needs.\\n(defun emit-create-method (stream presets)\\n  (format stream \\"    pub fn create(id: TheoryId) !ContextTag {~%\\")\\n  (format stream \\"        return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => .{ .~A = try fixtures.~A.contextCreate(allocator) },~%\\"\\n            (generated-prefix preset presets)\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-destroy-method (stream presets)\\n  (format stream \\"    pub fn destroy(self: ContextTag) void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.contextDestroy(inner),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-symbol-intern-method (stream presets)\\n  (format stream \\"    pub fn symbolIntern(self: ContextTag, name: []const u8) !u32 {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.symbolIntern(inner, name),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-field-insert-method (stream presets)\\n  (format stream \\"    pub fn fieldInsert(self: ContextTag, field_id: u16, coords: []const u32, labels: []const u32) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| try fixtures.~A.fieldInsert(inner, field_id, coords, labels),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-normal-ordering-method (stream presets)\\n  (format stream \\"    pub fn normalOrdering(self: ContextTag, field_count: usize) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| try fixtures.~A.normalOrdering(inner, field_count),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-freeze-method (stream presets)\\n  (format stream \\"    pub fn freeze(self: ContextTag) !kernel.Call.MultiOp {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => |inner| fixtures.~A.operatorListFreeze(inner),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n(defun emit-count-method (stream presets)\\n  (format stream \\"    pub fn count(self: ContextTag, ops: kernel.Call.MultiOp) !usize {~%\\")\\n  (format stream \\"        return switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => fixtures.~A.correlatorCount(ops),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        };~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-run-method (stream presets)\\n  (format stream \\"    pub fn run(self: ContextTag, ops: kernel.Call.MultiOp, state: anytype, thunk: anytype) !void {~%\\")\\n  (format stream \\"        switch (self) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"            .~A => try fixtures.~A.correlatorRun(ops, state, thunk),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"        }~%\\")\\n  (format stream \\"    }~%~%\\"))\\n\\n(defun emit-context-end (stream)\\n  (format stream \\"};~%~%\\"))\\nThe remaining dispatch functions decode raw ids and expose generated theory\\nhashes to Lisp.\\n(defun emit-scalar-atom-name-function (stream presets)\\n  (format stream \\"pub fn scalarAtomParameterName(id: TheoryId, atom: u32) ?[]const u8 {~%\\")\\n  (format stream \\"    return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        .~A => fixtures.~A.scalarAtomParameterName(atom),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%~%\\"))\\n\\n(defun emit-theory-id-function (stream presets)\\n  (format stream \\"pub fn theoryId(raw: u32) !TheoryId {~%\\")\\n  (format stream \\"    return switch (raw) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        ~D => .~A,~%\\"\\n            (preset-theory-id preset)\\n            (generated-prefix preset presets)))\\n  (format stream \\"        else => error.UnknownTheory,~%\\")\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%~%\\"))\\n(defun emit-theory-hash-function (stream presets)\\n  (format stream \\"pub fn theoryHash(id: TheoryId) u32 {~%\\")\\n  (format stream \\"    return switch (id) {~%\\")\\n  (dolist (preset presets)\\n    (format stream \\"        .~A => fixtures.~A.theoryHash(),~%\\"\\n            (generated-prefix preset presets)\\n            (generated-type-name preset presets)))\\n  (format stream \\"    };~%\\")\\n  (format stream \\"}~%\\"))\\n\\n(defun write-generated-dispatch\\n    (&key (path \\"src/cft-code/correlators/generated_dispatch.zig\\"))\\n  \\"Write the generated Zig dispatcher for registered presets.\\"\\n  (let ((presets (sorted-presets)))\\n    (with-open-file (stream path :direction :output :if-exists :supersede\\n                                 :if-does-not-exist :create)\\n      (emit-dispatch-prologue stream)\\n      (emit-theory-id stream presets)\\n      (emit-context-fields stream presets)\\n      (emit-create-method stream presets)\\n      (emit-destroy-method stream presets)\\n      (emit-symbol-intern-method stream presets)\\n      (emit-field-insert-method stream presets)\\n      (emit-normal-ordering-method stream presets)\\n      (emit-freeze-method stream presets)\\n      (emit-count-method stream presets)\\n      (emit-run-method stream presets)\\n      (emit-context-end stream)\\n      (emit-scalar-atom-name-function stream presets)\\n      (emit-theory-id-function stream presets)\\n      (emit-theory-hash-function stream presets)))\\n  path)\\nCompilationThe compile helper writes the generated fixture and dispatch sources imported\\nby src/cft-code/generated_abi.zig.\\n(defun write-generated-sources ()\\n  \\"Write all generated Zig modules derived from registered Lisp presets.\\"\\n  (write-generated-fixtures)\\n  (write-generated-dispatch))\\n\\n(defun compile-preset-library\\n    (&key\\n       (library-path \\"zig-out/lib/libstring_code_cft_generated.so\\")\\n       (global-cache-dir \\"/tmp/zig-global-cache\\")\\n       (zig \\"zig\\"))\\n  \\"Emit registered presets and compile the generated C ABI shared library.\\"\\n  (write-generated-sources)\\n  (uiop:run-program\\n   (list zig \\"build-lib\\" \\"-OReleaseFast\\"\\n         \\"--dep\\" \\"tensor-code\\"\\n         \\"-Mroot=src/cft-code/generated_abi.zig\\"\\n         \\"-Mtensor-code=src/tensor-code/tensor-code.zig\\"\\n         \\"-lc\\" \\"--name\\" \\"string_code_cft_generated\\"\\n         \\"-dynamic\\" \\"--global-cache-dir\\" global-cache-dir\\n         \\"-femit-bin\\" library-path)\\n   :output *standard-output*\\n   :error-output *error-output*)\\n  library-path)\\n","n":0.017}}},{"i":46,"$":{"1":{"v":"Lisp Free Fermion Preset","n":0.5},"2":{"v":"This preset declares the indexed sphere free fermion.  The only Wick datum is\\nthe \\\\(\\\\eta_{\\\\mu\\\\nu}/(z-w)\\\\) contraction.\\n(in-package #:string-code.cft.presets)\\n(define-cft-preset free-fermion-10\\n  (surface sphere :sphere :rational)\\n  (quantum-number spin10 :ade-irrep :group d5)\\n  (field psi \\"psi\\" :single ((mu :vector-index)) :fermionic\\n         :quantum-numbers ((spin10 vector)))\\n  (wick sphere (psi mu z) (psi nu w)\\n    (* (metric mu nu) (/ 1 (- z w)))))\\n","n":0.137}}},{"i":47,"$":{"1":{"v":"Lisp bc Sphere Preset","n":0.5},"2":{"v":"The sphere \\\\(bc\\\\) preset declares a single \\\\(b(z)c(w)\\\\) rational-pole\\ncontraction and the \\\\(c\\\\) top-form zero mode.\\n(in-package #:string-code.cft.presets)\\n(define-cft-preset bc-sphere\\n  (surface sphere :sphere :rational)\\n  (quantum-number ghost-number :u1)\\n  (field b \\"b\\" :single nil :fermionic\\n         :quantum-numbers ((ghost-number -1)))\\n  (field c \\"c\\" :single nil :fermionic\\n         :zero-mode t\\n         :quantum-numbers ((ghost-number 1)))\\n  (wick sphere (b z) (c w)\\n    (/ 1 (- z w)))\\n  (zero-mode sphere :top-form-fermion (c)))\\n","n":0.13}}},{"i":48,"$":{"1":{"v":"Lisp Free Boson Preset","n":0.5},"2":{"v":"This preset declares the ten-dimensional free boson on the sphere.  The scalar\\nparameter alpha-prime is symbolic in the descriptor and can be assigned a\\nruntime convention value by the Lisp REPL layer.\\n(in-package #:string-code.cft.presets)\\nThe field declarations keep label-dependent conformal weights in descriptor\\nmetadata.  The \\\\(k \\\\cdot k\\\\) expression references the expX momentum label\\nby name, not by a numeric slot.\\n(define-cft-preset free-boson-10\\n  (parameter alpha-prime \\"alpha-prime\\" :scalar-parameter)\\n  (surface sphere :sphere :rational)\\n  (quantum-number spin10 :ade-irrep :group d5)\\n  (field dX \\"dX\\" :single ((mu :vector-index)) :bosonic\\n         :quantum-numbers ((spin10 vector)))\\n  (field expX \\"expX\\" :pair ((k :momentum)) :bosonic\\n         :zero-mode t\\n         :weight (* (rational 1 4)\\n                    (* (parameter alpha-prime)\\n                       (dot (field-label expX k)\\n                            (field-label expX k)\\n                            eta)))\\n         :anti-weight (* (rational 1 4)\\n                         (* (parameter alpha-prime)\\n                            (dot (field-label expX k)\\n                                 (field-label expX k)\\n                                 eta))))\\nThe \\\\(dX dX\\\\) contraction carries the metric tensor and the second-order\\nrational pole.\\n  (wick sphere (dX mu z) (dX nu w)\\n    (* -1/2 alpha-prime\\n       (metric mu nu)\\n       (pow (- z w) -2)))\\nThe \\\\(dX\\\\,e^{ikX}\\\\) contraction leaves the plane wave as a residual operator,\\nwhich keeps the expansion tree fixed for the streaming kernel.\\n  (wick sphere (dX mu z) (expX k w wb)\\n    (* -1/2 i alpha-prime\\n       (momentum-index k mu)\\n       (/ 1 (- z w)))\\n    :residuals (:right))\\nThe exponential-exponential rule emits holomorphic and antiholomorphic Green\\nfactors with the shared momentum-pair tensor.\\n  (wick sphere (expX p z zb) (expX k w wb)\\n    (* 1/2 alpha-prime\\n       (momentum-pair p k)\\n       (green-exp z w)\\n       (green-exp zb wb))\\n    :residuals (:left :right))\\nThe sphere zero-mode factor is the stringbook momentum-conservation\\nnormalization for ten noncompact bosons.\\n  (zero-mode sphere :boson-momentum-conservation (expX) :two-pi-power 10))\\n","n":0.064}}}]}');let tu=e=>{let{onClickResultItem:n}=e,{emacsTheme:t}=(0,_.useContext)(eX.N),r=t[1],i=(0,_.useRef)(null),[a,s]=(0,_.useState)([]),[c,d]=(0,_.useState)(""),u=function(e,n){let[t,r]=(0,_.useState)(e);return(0,_.useEffect)(()=>{let n=setTimeout(()=>{r(e)},300);return()=>{clearTimeout(n)}},[e,300]),t}(c,0),p=(0,_.useMemo)(()=>{console.log("Initializing Fuse with index...");let e=n7.Z.parseIndex(td);return new n7.Z(ta,{keys:["tags","title","content"],minMatchCharLength:2,includeMatches:!0},e)},[]);(0,_.useEffect)(()=>{if(u.trim().length<2){s([]);return}s(p.search(u))},[u,p]);let m=(0,_.useCallback)(e=>{d(e.target.value)},[]);return(0,o.jsxs)(n9.h,{children:[(0,o.jsx)(te.x,{children:(0,o.jsxs)(tn.B,{borderColor:r.fg,children:[(0,o.jsx)(tt.Z,{pointerEvents:"none",children:(0,o.jsx)(q.SearchIcon,{})}),(0,o.jsx)(tr.I,{type:"text",ref:i,onChange:m,placeholder:"Search node"})]})}),!!a.length&&(0,o.jsx)(eU.$B,{autoHeight:!0,autoHide:!0,autoHeightMax:500,renderThumbVertical:e=>{let{style:n,...t}=e;return(0,o.jsx)(l.xu,{style:{...n,borderRadius:0},...t})},children:(0,o.jsx)(to.f,{maxHeight:500,children:(0,o.jsx)(ti.aV,{spacing:3,children:a.map(e=>(0,o.jsx)(tc,{result:e,onClick:n},e.item.id))})})})]})},tp=e=>{let{nodeById:n,setPreviewNode:t,onClickResultItem:r}=e,{isOpen:i,onOpen:s,onClose:l}=(0,a.q)(),c=(0,_.useCallback)(e=>{let o=n[e];l(),o&&(t(o),history.replaceState(null,"",window.location.pathname+"#".concat(o.id)),r&&r(o.id))},[n,r,l,t]);return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(d.u,{label:"Search node",children:(0,o.jsx)(u.h,{m:1,"aria-label":"Search node",variant:"subtle",icon:(0,o.jsx)(q.SearchIcon,{}),onClick:e=>{e.currentTarget.blur(),s()}})}),(0,o.jsxs)(n4.u_,{isOpen:i,onClose:l,children:[(0,o.jsx)(n5.Z,{}),(0,o.jsx)(tu,{onClickResultItem:c})]})]})},tm=t.e(101).then(t.bind(t,99101)),tf=t.g.window?t(2026).f$:null,th=t.g.window?t(2026).s6:null;function tg(){let[e,n]=(0,_.useState)(!1);return((0,_.useEffect)(()=>{n(!0)},[]),e)?(0,o.jsxs)(o.Fragment,{children:[(0,o.jsxs)(b(),{children:[(0,o.jsx)("title",{children:e9}),(0,o.jsx)("link",{rel:"stylesheet",href:"https://cdnjs.cloudflare.com/ajax/libs/prism/9000.0.1/themes/prism-tomorrow.min.css"})]}),(0,o.jsx)(tb,{})]}):null}function tb(){let[e,n]=nu("3d",!1),[t,r]=nu("tagCols",{}),[p,m]=(0,_.useState)({nodeIds:[],excludedNodeIds:[]}),[h,g]=nu("physics",R),[b,y]=nu("filter",I),[x,w]=nu("visuals",B),[z,T]=(0,_.useState)(null),[S,j]=(0,_.useState)(null),[O,L]=nu("behavior",F),[A,W]=nu("mouse",D),[M,q]=nu("coloring",E),[H,Z]=nu("local",P),[N,{set:X,reset:G,undo:K,redo:V,canUndo:U,canRedo:Y}]=(0,C.Z)({}),{past:Q,present:$,future:J}=N,[ee,en]=(0,_.useState)(null),{isOpen:et,onOpen:eo,onClose:ei}=(0,a.q)(),ea=(0,_.useRef)({}),es=(0,_.useRef)({}),el=(0,_.useRef)({}),ec=(0,_.useRef)([]),ed=(0,_.useRef)(null),[eu,ep]=(0,_.useState)({}),em=(0,_.useRef)({}),ef=(0,_.useRef)({nodes:[],links:[]}),eh=e=>{var n,t,r;let o=ea.current;ec.current=null!==(n=e.tags)&&void 0!==n?n:[];let i=null!==(t=e.nodes)&&void 0!==t?t:[],a=null!==(r=e.links)&&void 0!==r?r:[],s=i.reduce((e,n)=>{var t;return{...e,[n.file]:[...null!==(t=e[n.file])&&void 0!==t?t:[],n]}},{}),l=Object.keys(s).flatMap(e=>{var n;let t=null!==(n=s[e])&&void 0!==n?n:[],r=t.find(e=>0===e.level),o=t.filter(e=>0!==e.level);return r?o.map(e=>{let n=t.filter(n=>{var t,r,o;return!(n.level>=e.level)&&!(n.pos>=e.pos)&&!!(null===(r=e.olp)||void 0===r?void 0:r.includes(null!==(o=null===(t=n.title)||void 0===t?void 0:t.replace(/ *\[\d*\/\d*\] */g,""))&&void 0!==o?o:""))}).reduce((e,n)=>(n.level>e.level&&(e=n),e),r);return{source:e.id,target:(null==n?void 0:n.id)||r.id,type:"heading"}}):[]}),c=Object.keys(s).flatMap(e=>{var n;let t=null!==(n=s[e])&&void 0!==n?n:[],r=t.find(e=>0===e.level),o=t.filter(e=>0!==e.level);return r?o.map(e=>({source:e.id,target:r.id,type:"parent"})):[]});ea.current=Object.fromEntries(i.map(e=>[e.id,e]));let d=[],u=[...a,...l,...c].map(e=>{let n=e.source,t=e.target;return ea.current[n]?ea.current[t]?e:(d.push({id:t,tags:["bad"],properties:{FILELESS:"yes",bad:"yes"},file:"",title:t,level:0,pos:0,olp:null}),{...e,type:"bad"}):(d.push({id:n,tags:["bad"],properties:{FILELESS:"yes",bad:"yes"},file:"",title:n,level:0,pos:0,olp:null}),{...e,type:"bad"})});ea.current={...ea.current,...Object.fromEntries(d.map(e=>[e.id,e]))},es.current=u.reduce((e,n)=>{var t,r;return{...e,[n.source]:[...null!==(t=e[n.source])&&void 0!==t?t:[],n],[n.target]:[...null!==(r=e[n.target])&&void 0!==r?r:[],n]}},{});let p=[...i,...d];el.current=p.reduce((e,n)=>{var t;let r=null===(t=n.properties)||void 0===t?void 0:t.ROAM_REFS;if(!(null==r?void 0:r.includes("cite")))return e;let o=r.replace(/cite:(.*)/g,"$1");return o?{...e,[o]:n}:e},{});let m=ef.current;if(0===m.nodes.length){let e=JSON.parse(JSON.stringify({nodes:p,links:u}));ef.current=e,T(e);return}let f=[...m.nodes.flatMap(e=>{var n;let t=null!==(n=ea.current[null==e?void 0:e.id])&&void 0!==n&&n;return t?[{...e,...t}]:[]}),...Object.keys(ea.current).filter(e=>!o[e]).map(e=>ea.current[e])],h=f.reduce((e,n,t)=>{let r=null==n?void 0:n.id;return{...e,[r]:t}},{}),g=u.map(e=>{let[n,t]=nt(e);return{...e,source:f[h[n]],target:f[h[t]]}});T({nodes:f,links:g})};(0,_.useEffect)(()=>{if(!z){eh(i.a);return}ef.current=z},[z]);let{setEmacsTheme:eg}=(0,_.useContext)(eX.N),eb=(0,_.useRef)({nodeIds:[],excludedNodeIds:[]});(0,_.useRef)(F).current=O;let e_=(0,_.useRef)(null);eb.current=p,(0,_.useEffect)(()=>{let e=ed.current;if(e&&!(p.nodeIds.length>1)){if(!p.nodeIds.length&&h.gravityOn){e.zoomToFit();return}setTimeout(()=>{e.zoomToFit(5,200)},50)}},[p.nodeIds]);let[ey,ex]=(0,f.iP)(),ek=(0,_.useRef)(),[ev,ew]=(0,_.useState)(null),[eC,ez]=(0,_.useState)({left:0,top:0,right:void 0,bottom:void 0}),eT=(0,a.q)();(0,s.O)({ref:ek,handler:()=>{eT.onClose()}});let eS=(e,n,t)=>{t?ez(t):ez({left:n.pageX,top:n.pageY,right:void 0,bottom:void 0}),ew(e),eT.onOpen()},eR=(e,n)=>{if("remove"===n){m(n=>({nodeIds:n.nodeIds.filter(n=>n!==e.id),excludedNodeIds:[...n.excludedNodeIds,e.id]}));return}if("replace"===n){m({nodeIds:[e.id],excludedNodeIds:[]});return}p.nodeIds.includes(e.id)||m(n=>({excludedNodeIds:n.excludedNodeIds.filter(n=>n!==e.id),nodeIds:[...n.nodeIds,e.id]}))},[eI,eE]=nu("mainWindowWidth",ey);return(0,_.useEffect)(()=>{let e=window.location.hash;if(!e)return;let n=e.replace("#",""),t=ea.current[n];t&&(j(n),X(t))},[X]),(0,o.jsx)(nS.Provider,{value:{...eu},children:(0,o.jsxs)(l.xu,{display:"flex",alignItems:"flex-start",flexDirection:"row",height:"100vh",overflow:"clip",children:[(0,o.jsx)(nG,{physics:h,setPhysics:g,threeDim:e,setThreeDim:n,filter:b,setFilter:y,visuals:x,setVisuals:w,mouse:A,setMouse:W,behavior:O,setBehavior:L,tagColors:t,setTagColors:r,coloring:M,setColoring:q,local:H,setLocal:Z,tags:ec.current}),(0,o.jsx)(l.xu,{position:"absolute",children:z&&(0,o.jsx)(t_,{nodeById:ea.current,linksByNodeId:es.current,webSocket:e_.current,variables:eu,physics:h,graphData:z,threeDim:e,emacsNodeId:S,filter:b,visuals:x,behavior:O,mouse:A,scope:p,setScope:m,tagColors:t,setPreviewNode:X,sidebarHighlightedNode:ee,windowWidth:ey,windowHeight:ex,openContextMenu:eS,contextMenu:eT,handleLocal:eR,mainWindowWidth:eI,setMainWindowWidth:eE,setContextMenuTarget:ew,graphRef:ed,clusterRef:em,coloring:M,local:H})}),(0,o.jsx)(l.xu,{position:"relative",zIndex:4,width:"100%",children:(0,o.jsx)(c.k,{className:"headerBar",h:10,flexDir:"column",children:(0,o.jsx)(c.k,{alignItems:"center",h:10,justifyContent:"flex-end",children:(0,o.jsxs)(c.k,{height:"100%",flexDirection:"row",children:[p.nodeIds.length>0&&(0,o.jsx)(d.u,{label:"Return to main graph",children:(0,o.jsx)(u.h,{m:1,icon:(0,o.jsx)(k.DvO,{}),"aria-label":"Exit local mode",onClick:()=>m(e=>({...e,nodeIds:[]})),variant:"subtle"})}),(0,o.jsx)(tp,{nodeById:ea.current,setPreviewNode:X,onClickResultItem:e=>{j(e)}}),(0,o.jsx)(d.u,{label:et?"Close sidebar":"Open sidebar",children:(0,o.jsx)(u.h,{m:1,icon:(0,o.jsx)(v.iBV,{}),"aria-label":"Close file-viewer",variant:"subtle",onClick:et?ei:eo})})]})})})}),(0,o.jsx)(l.xu,{position:"relative",zIndex:4,children:(0,o.jsx)(np,{isOpen:et,onOpen:eo,onClose:ei,previewNode:$,setPreviewNode:X,canUndo:U,canRedo:Y,previousPreviewNode:K,nextPreviewNode:V,resetPreviewNode:G,setSidebarHighlightedNode:en,openContextMenu:eS,scope:p,setScope:m,windowWidth:ey,tagColors:t,setTagColors:r,filter:b,setFilter:y,macros:eu.katexMacros,attachDir:eu.attachDir||"",useInheritance:eu.useInheritance||!1,nodeById:ea.current,linksByNodeId:es.current,nodeByCite:el.current})}),eT.isOpen&&(0,o.jsx)("div",{ref:ek,children:(0,o.jsx)(er,{scope:p,target:ev,background:!1,coordinates:eC,handleLocal:eR,menuClose:eT.onClose.bind(eT),webSocket:e_.current,setPreviewNode:X,setFilter:y,filter:b,setTagColors:r,tagColors:t})})]})})}let t_=function(e){let{graphRef:n,physics:t,graphData:r,threeDim:i,linksByNodeId:a,filter:s,emacsNodeId:c,nodeById:d,visuals:u,behavior:f,mouse:g,scope:b,local:y,setScope:k,webSocket:v,tagColors:C,setPreviewNode:z,sidebarHighlightedNode:T,windowWidth:R,windowHeight:I,setContextMenuTarget:E,openContextMenu:B,contextMenu:F,handleLocal:D,variables:P,clusterRef:O,coloring:L}=e,{dailyDir:A,roamDir:W}=P,[M,q]=(0,_.useState)(null),H=(0,p.F)(),{emacsTheme:Z}=(0,_.useContext)(eX.N),N=(e,n,t)=>{switch(e){case g.preview:z(n),history.replaceState(null,"",window.location.pathname+"#".concat(n.id));break;case g.local:D(n,f.localSame);break;case g.follow:!function(e,n,t){t.send(JSON.stringify({command:e,data:n}))}("open",{id:n.id},v);break;case g.context:B(n,t)}},X=(0,_.useRef)(null);(0,_.useEffect)(()=>{c&&q(d[c])},[c]);let G=(0,_.useRef)({}),K=(0,_.useRef)({}),V=(0,_.useMemo)(()=>{var e;K.current={};let n=null==r?void 0:null===(e=r.nodes)||void 0===e?void 0:e.filter(e=>{var n,t,r,o;return!(s.dirsBlocklist.length&&s.dirsBlocklist.some(n=>{var t;return null==e?void 0:null===(t=e.file)||void 0===t?void 0:t.includes(n)})||s.dirsAllowlist.length>0&&!s.dirsAllowlist.some(n=>{var t;return null==e?void 0:null===(t=e.file)||void 0===t?void 0:t.includes(n)})||s.tagsBlacklist.length&&s.tagsBlacklist.some(n=>{var t;return(null==e?void 0:null===(t=e.tags)||void 0===t?void 0:t.indexOf(n))>-1})||s.tagsWhitelist.length>0&&!s.tagsWhitelist.some(n=>{var t;return(null==e?void 0:null===(t=e.tags)||void 0===t?void 0:t.indexOf(n))>-1})||s.filelessCites&&(null==e?void 0:null===(n=e.properties)||void 0===n?void 0:n.FILELESS)||(null==s?void 0:s.bad)&&(null==e?void 0:null===(t=e.properties)||void 0===t?void 0:t.bad)||s.dailies&&A&&(null===(r=e.file)||void 0===r?void 0:r.includes(A)))&&(!s.noter||null===(o=e.properties)||void 0===o||!o.NOTER_PAGE)||(K.current={...K.current,[e.id]:e},!1)}).filter(e=>{var n;let t=(null!==(n=a[null==e?void 0:e.id])&&void 0!==n?n:[]).filter(e=>!K.current[e.source]&&!K.current[e.target]);return!s.orphans||(s.parent?0!==t.length:0!==t.length&&t.some(e=>!["parent","heading"].includes(e.type)))}),t=n.map(e=>e.id),o=r.links.filter(e=>{let[n,r]=nt(e);return!!(t.includes(n)&&t.includes(r))&&(s.parent?"heading"===s.parent?"parent"!==e.type:"heading"!==e.type:!["parent","heading"].includes(e.type))});G.current=o.reduce((e,n)=>{var t,r;let[o,i]=nt(n);return{...e,[o]:[...null!==(t=e[o])&&void 0!==t?t:[],n],[i]:[...null!==(r=e[i])&&void 0!==r?r:[],n]}},{});let i=o.map(e=>{let[n,t]=nt(e);return{target:n,source:t,weight:"cite"===e.type?1:2}});if("community"===L.method){let e=x()().nodes(t).edges(i);O.current=e()}return{nodes:n,links:o}},[s,r,L.method]),[U,Y]=(0,_.useState)({nodes:[],links:[]});(0,_.useEffect)(()=>{if(!b.nodeIds.length)return;let e=b.nodeIds.length>1?U.nodes.filter(e=>!b.excludedNodeIds.includes(e.id)):[],n=e.map(e=>e.id),t=nJ({ids:b.nodeIds,excludedIds:b.excludedNodeIds,n:y.neighbors,linksByNodeId:G.current}),r=V.nodes.filter(r=>{if(e.length){var o;return!n.includes(r.id)&&(null!==(o=G.current[r.id])&&void 0!==o?o:[]).some(e=>{let[n,t]=nt(e);return!b.excludedNodeIds.includes(n)&&!b.excludedNodeIds.includes(t)&&(b.nodeIds.includes(n)||b.nodeIds.includes(t))})}return t.includes(r.id)}).map(e=>({...e,x:0,y:0,vy:0,vx:0})),o=[...e,...r],i=o.map(e=>e.id),a=(b.nodeIds.length>1?U.links:[]).filter(e=>{b.excludedNodeIds.some(n=>nt(e).includes(n))}),s=V.links.filter(e=>{let[t,r]=nt(e);return!(a.length&&n.includes(r)&&n.includes(t))&&i.includes(t)&&i.includes(r)}).map(e=>{let[n,t]=nt(e);return{source:n,target:t}});Y({nodes:o,links:[...a,...s]})},[y.neighbors,s,b,b.excludedNodeIds,b.nodeIds,r,V.links,V.nodes]),(0,_.useEffect)(()=>{(async()=>{let e=n.current,r=await tm;t.gravityOn&&!(b.nodeIds.length&&!t.gravityLocal)?(e.d3Force("x",r.forceX().strength(t.gravity)),e.d3Force("y",r.forceY().strength(t.gravity)),i&&e.d3Force("z",r.forceZ().strength(t.gravity))):(e.d3Force("x",null),e.d3Force("y",null),i&&e.d3Force("z",null)),t.centering?e.d3Force("center",r.forceCenter().strength(t.centeringStrength)):e.d3Force("center",null),t.linkStrength&&e.d3Force("link").strength(t.linkStrength),t.linkIts&&e.d3Force("link").iterations(t.linkIts),t.charge&&e.d3Force("charge").strength(t.charge),e.d3Force("collide",t.collision?r.forceCollide().radius(t.collisionStrength):null)})()},[t,i,b]),(0,_.useEffect)(()=>{var e;null===(e=n.current)||void 0===e||e.d3ReheatSimulation()},[t,b.nodeIds.length]);let Q=(0,_.useRef)(0),[$,J]=(0,_.useState)(1),[ee,en]=(0,m._7)(e=>J(e),{duration:u.animationSpeed,algorithm:S[u.algorithmName]}),[et,er]=(0,m._7)(e=>J(Math.min($,-1*(e-1))),{duration:u.animationSpeed,algorithm:S[u.algorithmName]}),eo=(0,_.useMemo)(()=>{var e;if(!X.current)return{};let n=G.current[X.current.id];return n?Object.fromEntries([null===(e=X.current)||void 0===e?void 0:e.id,...n.flatMap(e=>[e.source,e.target])].map(e=>[e,{}])):{}},[X.current,G.current]);(0,_.useEffect)(()=>{(null==T?void 0:T.id)?q(T):q(null)},[T]);let ei=(0,_.useRef)(null);(0,_.useEffect)(()=>{if(X.current=M,M&&(ei.current=M),!u.highlightAnim)return M?J(1):J(0);M?ee():(en(),$>.5?et():J(0))},[M]);let ea=(0,_.useMemo)(()=>Object.fromEntries(j.map(e=>{let n=eY(e,H);return[e,Object.fromEntries(j.map(e=>[e,h.Z(n,eY(e,H))]))]})),[Z]),es=(0,_.useMemo)(()=>{var e,n,t;let r=null!==(t=G.current[null===(e=ei.current)||void 0===e?void 0:e.id])&&void 0!==t?t:[];return Object.fromEntries([null===(n=ei.current)||void 0===n?void 0:n.id,...r.flatMap(e=>nt(e))].map(e=>[e,{}]))},[JSON.stringify(M),ei.current,G.current]),el=(0,_.useMemo)(()=>eY(u.labelTextColor,H),[u.labelTextColor,Z]),ec=(0,_.useMemo)(()=>eY(u.labelBackgroundColor,H),[u.labelBackgroundColor,Z]),[ed,eu]=(0,_.useState)(!1),ep=(0,_.useRef)(1),em={graphData:b.nodeIds.length?U:V,width:R,height:I,backgroundColor:eY(u.backgroundColor,H),warmupTicks:1===b.nodeIds.length?100:b.nodeIds.length>1?20:0,onZoom:e=>{let{k:n,x:t,y:r}=e;return ep.current=n},nodeColor:e=>n2({node:e,theme:H,visuals:u,cluster:O.current,coloring:L,emacsNodeId:c,highlightColors:ea,highlightedNodes:eo,previouslyHighlightedNodes:es,linksByNodeId:G.current,opacity:$,tagColors:C}),nodeRelSize:u.nodeRel,nodeVal:e=>nY({node:e,highlightedNodes:eo,linksByNodeId:G.current,opacity:$,previouslyHighlightedNodes:es,visuals:u})/Math.pow(ep.current,u.nodeZoomSize),nodeCanvasObject:(e,n,t)=>{!function(e){var n,t,r,o;let{labelBackgroundColor:i,labelTextColor:a,node:s,ctx:l,globalScale:c,highlightedNodes:d,previouslyHighlightedNodes:u,visuals:p,opacity:m,filteredLinksByNodeId:f,nodeRel:h,hoverNode:g,lastHoverNode:b}=e;if(!s||!p.labels)return;let _=null!==(n=null==g?void 0:g.id)&&void 0!==n?n:"",y=null!==(t=null==b?void 0:b.id)&&void 0!==t?t:"",x=null!==(r=f[s.id])&&void 0!==r?r:[],k=!!(d[s.id]||u[s.id]),v=Math.min(5*(c-p.labelScale)+2*Math.pow(Math.min(x.length,p.labelDynamicDegree),p.labelDynamicStrength),1);if(v<.01&&!k)return;let w=null!==(o=s.title)&&void 0!==o?o:"",C=w.substring(0,p.labelLength),z=Math.cbrt(p.nodeRel*nY({node:s,highlightedNodes:d,linksByNodeId:f,opacity:m,previouslyHighlightedNodes:u,visuals:p})/Math.pow(c,p.nodeZoomSize)),T=p.labelFontSize/Math.cbrt(Math.pow(c,p.nodeZoomSize)),S=[1.1*l.measureText(C).width,T].map(e=>e+.5*T),R=n$(v,p,c,m,k);if(p.labelBackgroundColor&&p.labelBackgroundOpacity){let e=nQ(i,R*p.labelBackgroundOpacity);l.fillStyle=e,l.fillRect(s.x-S[0]/2,s.y-S[1]/2+z,...S)}l.textAlign="center",l.textBaseline="middle";let I=nQ(a,R);l.fillStyle=I,l.font="".concat(T,"px Sans-Serif");let E=nV()(C,{width:p.labelWordWrap}).split("\n"),B=w.length>p.labelLength?[...E.slice(0,-1),"".concat(E.slice(-1),"...")]:E,F=[_,y].includes(s.id)?1+.3*m:1;B.forEach((e,n)=>{l.fillText(e,s.x,s.y+F*z*8+p.labelLineSpace*T*n)})}({nodeRel:u.nodeRel,filteredLinksByNodeId:G.current,lastHoverNode:ei.current,node:e,ctx:n,globalScale:t,highlightedNodes:eo,previouslyHighlightedNodes:es,visuals:u,opacity:$,labelTextColor:el,labelBackgroundColor:ec,hoverNode:M})},nodeCanvasObjectMode:()=>"after",linkDirectionalParticles:u.particles?u.particlesNumber:void 0,linkDirectionalArrowLength:u.arrows?u.arrowsLength:void 0,linkDirectionalArrowRelPos:u.arrowsPos,linkDirectionalArrowColor:u.arrowsColor?()=>eY(u.arrowsColor,H):void 0,linkColor:e=>{var n;let t="object"==typeof e.source?e.source.id:e.source,r="object"==typeof e.target?e.target.id:e.target,o=n6(e,X.current),i=n6(e,ei.current),a=o||i;return u.refLinkColor&&"ref"===e.type?a&&(u.refLinkHighlightColor||u.linkHighlight)?ea[u.refLinkColor][u.refLinkHighlightColor||u.linkHighlight]($):ea[u.refLinkColor][u.backgroundColor](u.highlightFade*$):u.citeLinkColor&&(null===(n=e.type)||void 0===n?void 0:n.includes("cite"))?a&&(u.citeLinkHighlightColor||u.linkHighlight)?ea[u.citeLinkColor][u.citeLinkHighlightColor||u.linkHighlight]($):ea[u.citeLinkColor][u.backgroundColor](u.highlightFade*$):n8({sourceId:t,targetId:r,needsHighlighting:a,theme:H,cluster:O.current,coloring:L,highlightColors:ea,linksByNodeId:G.current,opacity:$,visuals:u})},linkWidth:e=>{if(1===u.highlightLinkSize)return u.linkWidth;let n=n6(e,X.current),t=n6(e,ei.current);return n||t?u.linkWidth*(1+$*(u.highlightLinkSize-1)):u.linkWidth},linkDirectionalParticleWidth:u.particlesWidth,d3AlphaDecay:t.alphaDecay,d3AlphaMin:t.alphaMin,d3VelocityDecay:t.velocityDecay,onNodeClick:(e,n)=>{let t=n.timeStamp-Q.current<200;if(Q.current=n.timeStamp,t)return N("double",e,n);let r=Q.current;return setTimeout(()=>{if(Q.current===r)return N("click",e,n)},200)},onNodeHover:e=>{u.highlight&&!ed&&(M||(er(),J(0)),q(e))},onNodeRightClick:(e,n)=>{N("right",e,n)},onNodeDrag:e=>{q(e),eu(!0)},onNodeDragEnd:()=>{q(null),eu(!1)}};return(0,o.jsx)(l.xu,{overflow:"hidden",onClick:F.onClose,children:i?(0,o.jsx)(th,{ref:n,...em,nodeThreeObjectExtend:!0,nodeOpacity:u.nodeOpacity,nodeResolution:u.nodeResolution,linkOpacity:u.linkOpacity,nodeThreeObject:e=>{if(!u.labels||u.labels<3&&!eo[e.id])return;let n=new w.Z(e.title.substring(0,40));return n.color=eY(u.labelTextColor,H),n.backgroundColor=eY(u.labelBackgroundColor,H),n.padding=2,n.textHeight=8,n}}):(0,o.jsx)(tf,{ref:n,...em,linkLineDash:e=>{var n;return u.citeDashes&&(null===(n=e.type)||void 0===n?void 0:n.includes("cite"))?[u.citeDashLength,u.citeGapLength]:u.refDashes&&"ref"==e.type?[u.refDashLength,u.refGapLength]:null}})})}}},function(e){e.O(0,[737,265,876,228,866,13,874,446,884,888,774,179],function(){return e(e.s=48312)}),_N_E=e.O()}]);